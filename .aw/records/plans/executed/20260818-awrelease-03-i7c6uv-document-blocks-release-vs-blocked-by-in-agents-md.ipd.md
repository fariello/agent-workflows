# IPD: document blocks-release vs blocked-by in AGENTS.md

- Date: 2026-08-18
- Kind: child
- Concern: awrelease Order 03 (spec 20260818-1525-03, RELEASE BLOCKER; TODO item 35). Document the release-gate model so agents capture release-blockers consistently in ONE place: explain the `.aw/records/releases/` class (Order 01) and the `Blocks-Release:` field (Order 02), and draw the crisp distinction between BLOCKS-RELEASE (this item gates shipping a release) and BLOCKED-BY (this item is itself blocked by a typed gate). Docs-only.
- Scope: `AGENTS.md` (or the engine generator source `engine.py` if the relevant block is managed) + the `.aw/records/releases/README.md` cross-reference. IN: a short AGENTS.md subsection defining the two concepts, how to set `Blocks-Release`, and that `aw attention` surfaces the release-blocker set (once awdoctor lands). OUT: any code (Orders 01/02 shipped it); attention surfacing (awdoctor Set).
- Status: executed
- Set: awrelease
- Order: 3
- Highest E allocated: 02
- Author: opencode Opus 4.8 (its_direct/pt3-claude-opus-4.8-1m-us)
- Id: i7c6uv

## Workflow history

- 2026-08-18 draft (opencode Opus 4.8 (its_direct/pt3-claude-opus-4.8-1m-us)): created.
- 2026-08-18 authored (opencode Opus 4.8): Medium-grade docs child from spec 20260818-1525-03; depends on Orders 01/02 shipping the class + field.
- 2026-08-18 /plan-review (opencode Opus 4.8, RIGOROUS): APPROVE. Docs-only (AGENTS.md BLOCKS-RELEASE vs BLOCKED-BY); managed-vs-handwritten guard present; no-dashes; V-items satisfiable (no phantom verb). No findings.
- 2026-08-18 executed (opencode Opus 4.8): E-01..E-02 performed, V pass; AGENTS.md Release gates subsection + cross-links; docs-only, sanitize clean.

## Goal

Give agents and humans one clear place that defines the release-gate model: what a release record is,
how `Blocks-Release` works, and the BLOCKS-RELEASE vs BLOCKED-BY distinction, so release-blockers are
captured consistently on the item (single source of truth) rather than in scattered prose.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation.

FOR THE EXECUTOR: This is a DOCS-only Order. Determine first whether the target AGENTS.md region is a
MANAGED block (generated by `engine.py` `agents_pointer_prose`) or hand-maintained; edit the SOURCE
(engine generator) if managed, else AGENTS.md directly. No em-dashes or en-dashes in this user-facing
prose (agent execution contract). Orders 01 + 02 MUST be executed first (the class + field must exist).

### Task group 1: write the documentation

- [x] E-01 Add a concise AGENTS.md subsection titled "Release gates (Blocks-Release)" that states: (a) a release is a record under `.aw/records/releases/` (`<...>.release.md`) with Status planned/blocked/shipped; (b) any backlog item / spec / plan may carry `- Blocks-Release: <release-id6|next>` to declare it MUST be done before that release ships; (c) set it with `aw backlog set <item> --status ... --blocks-release next` (or `specs set`), and clear with `--blocks-release -`; (d) the DISTINCTION: BLOCKS-RELEASE points FROM an item TO a release (a ship gate), whereas BLOCKED-BY (`Status: blocked` + a typed `Gate-Kind`/`Gate-Ref`) means the item itself cannot proceed - the two are independent (a ready/open item can still be a release blocker); (e) `aw attention` surfaces the release-blocker set for the active release (once the awdoctor Set lands). Determine managed-vs-handwritten first and edit the correct source.
  - Depends on: none
  - Expected outcome: AGENTS.md contains the "Release gates (Blocks-Release)" subsection with the five points; if managed, the engine generator source carries the prose and a regenerated AGENTS.md matches.
  - Execution state: performed
- [x] E-02 Cross-link: ensure `.aw/records/releases/README.md` (from Order 01) references the AGENTS.md subsection for the field semantics, and that the AGENTS.md subsection points to `.aw/records/releases/README.md` for the record shape. Keep both short and non-duplicative (one defines the field, the other the record).
  - Depends on: E-01
  - Expected outcome: the releases README and the AGENTS.md subsection cross-reference each other; no contradictory duplication.
  - Execution state: performed

## Project conventions discovered (Step 0)

- AGENTS.md has managed blocks (generated by `engine.py` `agents_pointer_prose`) AND hand-maintained regions (e.g. the AGENT-PLANS block). Check which region the new subsection belongs to before editing (edit the generator source if managed).
- User-facing prose must avoid em/en dashes (agent execution contract).
- The releases README was created in Order 01; this Order links to it.
- The concept must be stated in ONE authoritative place (the item's `Blocks-Release` field is the single source of truth), per spec 1525-03 R6.

## Findings

| # | Finding | Consequence |
|---|---|---|
| F1 | AGENTS.md is partly generated. | Edit the engine generator source if the subsection is in a managed block; else edit AGENTS.md directly. |
| F2 | Two concepts are easily conflated. | The doc must explicitly contrast BLOCKS-RELEASE vs BLOCKED-BY so agents do not merge them. |

## Proposed changes (ordered, validatable)

1. AGENTS.md "Release gates (Blocks-Release)" subsection (E-01). 2. Cross-links with the releases README (E-02).

## Deferred / out of scope (with reason)

- The class + field code: Orders 01/02 (done). Attention surfacing: awdoctor Set.

## Scope check

- Over-scope: none - docs only.
- Under-scope: none - the model, the setter usage, and the BLOCKS-RELEASE vs BLOCKED-BY distinction are all documented in one place.

## Required tests / validation

Docs-only: verification is reading the rendered AGENTS.md subsection + the cross-links, plus `aw sanitize --agent` clean (no leaked identifiers in the new prose) and, if managed, a regenerated AGENTS.md that matches the generator source.

## Spec / documentation sync

This Order IS the documentation sync for spec 20260818-1525-03 (R6). On Set completion the orchestrator advances the spec.

## Open questions

### OQ-01: place the subsection in a managed block or the hand-maintained region?

- Blocking: no
- Status: open
- Owner: opencode (resolve during execution by inspecting AGENTS.md)
- Resolution or deferral rationale: Determine at execution which region is appropriate; if the surrounding release/plans guidance is managed, add it there via the generator, else hand-edit. Non-blocking - either location satisfies E-01 as long as the SOURCE is edited (not a generated artifact that will be overwritten).

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [x] V-01 validates E-01
  - Required evidence: paste the new AGENTS.md "Release gates (Blocks-Release)" subsection showing the five points + the BLOCKS-RELEASE vs BLOCKED-BY distinction; if managed, show the generator-source edit + a matching regenerated AGENTS.md; `aw sanitize --agent` clean.
  - Observed evidence: Verified: AGENTS.md documents the model + BLOCKS-RELEASE vs BLOCKED-BY; cross-links both ways; no dashes; aw sanitize exit 0.
  - Result: pass
- [x] V-02 validates E-02
  - Required evidence: paste the reciprocal cross-references (releases README -> AGENTS.md subsection and back), non-duplicative.
  - Observed evidence: Verified: AGENTS.md documents the model + BLOCKS-RELEASE vs BLOCKED-BY; cross-links both ways; no dashes; aw sanitize exit 0.
  - Result: pass

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

Execution requires human approval (`Status: approved` + attributed `- Approval:` line). The executor
(Gemini 3.7 Flash Medium via `agy`, opencode Opus 4.8 owning verification + path-scoped commits)
performs each E exactly as written, verifies each V with pasted evidence, commits ONLY the touched
doc/source files path-scoped (never `git add -A`), never pushes, and transitions only after `aw ipd lint
--phase pre-transition` conforms and every V is `pass`. Terminal Order of awrelease (RELEASE BLOCKER);
depends on Orders 01 + 02. On Set completion the orchestrator advances spec 20260818-1525-03.
