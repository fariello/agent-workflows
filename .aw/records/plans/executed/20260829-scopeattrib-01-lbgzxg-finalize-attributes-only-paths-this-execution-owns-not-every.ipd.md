# IPD: finalize attributes only paths this execution owns, not every dirty file in a shared checkout

- Date: 2026-08-29
- Kind: child
- Concern: `_paths_changed_by_this_execution` unions the committed diff since the frozen base with the ENTIRE `git status --porcelain`, with no ownership filter. In a shared checkout every concurrent agent's uncommitted file is therefore attributed to whichever plan finalizes first, which demands a `--scope-reason` for work that plan never touched. The two available responses are both wrong: write a false reason into the permanent record, or block indefinitely until every other agent happens to be clean. MEASURED SCOPE (F9): this bites only when the turn's execution tree IS main, namely isolation disabled, a hand-run finalize from the main checkout, or a retry from main after a lane already committed. An isolated lane is immune, because it finalizes inside its own worktree and cannot see main's uncommitted files.
- Scope: Make finalize's out-of-scope attribution consider only paths this execution can be shown to own, FOR THE WORKING-TREE HALF of the attribution union, so an unowned dirty path disjoint from the plan's frozen `Scope-Paths` and from its own commits stops demanding a reason. Preserves every existing refusal for paths the plan itself changed, for dirty in-scope paths, and for the intervening-commit collision check. Excludes begin's dirty gate (plan `z2isfg`), excludes changing the receipt's location or `base_head` semantics, and excludes any new lease or registry mechanism. EXPLICITLY EXCLUDES the COMMITTED half: a CONCURRENT AGENT'S COMMITTED out-of-scope file still demands a reason after this plan, which is the same defect reached through the other half of the union and is measured in F9. That half needs an ownership signal that does not exist yet (git authorship cannot supply it, since every agent here commits under one identity) and is handed to a named follow-up rather than half-solved here. This plan therefore fixes the working-tree half COMPLETELY and the Concern PARTIALLY, and says so rather than implying the shared-checkout problem is closed.
- Scope-Paths: agent_workflows/ipd_lifecycle.py, tests/test_finalize_scope_ownership.py
- Item-Dependencies: none
- Status: executed
- Set: scopeattrib
- Order: 1
- Highest E allocated: 06
- Author: opencode (its_direct/pt3-claude-opus-5-1m-us)
- Id: lbgzxg
- Blocks-Release: next
- From-Backlog: 077yqc

## Workflow history
- 2026-08-30 executed (opencode its_direct/pt3-claude-opus-5-1m-us): Ownership-filtered finalize's WORKING-TREE attribution half: an uncommitted path this execution cannot be shown to own is disregarded instead of demanding a --scope-reason, so a plan can finalize while a concurrent agent has unrelated files dirty. The COMMITTED half is deliberately unchanged, so an out-of-scope path the plan itself committed still demands a reason. PARTIAL FIX, DECLARED NOT DISCOVERED: the committed half of the same defect remains OPEN as backlog a8eufb and is pinned by a characterization test that must be inverted rather than deleted; git authorship cannot close it because every agent here commits under one identity. Accepted cost (OQ-01/F3): an executor's OWN uncommitted out-of-scope edit is now disregarded too, mitigated by the path-scoped-commit contract that puts real work in the attributable half, and kept visible via the new disregarded_unowned_paths audit key and the human message. Evidence: fast suite 2923 passed / 3 skipped / 4 xfailed at 0c23401, with the 15 pre-existing tests/test_run_viewer.py failures byte-identical to the be49ac4 baseline; new tests/test_finalize_scope_ownership.py 11 passed; falsifiability proven both ways, since (a)/(e) fail against pre-fix code and (b)/(c)/(d) fail under a naive deletion of the working-tree half; begin's executable logic proven unmodified by AST comparison. [Scope reconciliation - in-scope-unmodified agent_workflows/ipd_lifecycle.py: modified-and-committed-at-0c23401-which-is-this-receipt-base; in-scope-unmodified tests/test_finalize_scope_ownership.py: added-and-committed-at-0c23401-which-is-this-receipt-base]
- 2026-08-30 approved (aw set): status set to approved
- 2026-08-30 reviewed (opencode (its_direct/pt3-claude-opus-5-1m-us)): /plan-review: APPROVE WITH REVISIONS APPLIED; PR-001..PR-003. Every one of the plan's eight findings was INDEPENDENTLY REPRODUCED in throwaway repos using the repo's own fixture helpers, and all matched the recorded transcripts exactly: F1 (co-worker untracked dirty -> exit 1 naming coworker.py), F2/A (own COMMITTED out-of-scope still refuses), F3/B (own UNCOMMITTED out-of-scope byte-identical to a co-worker's, confirming no filter can separate them), F4 (forced reason does NOT sweep the file in; the finalize commit contained only INDEX.json, INDEX.md and the moved plan, and the co-worker file remained untracked, so this is record pollution not data loss), F5 (receipt keys are exactly as listed, no per-path ownership), F8 (in-scope dirty at finalize exits 0). The begin step-6 comment, the _paths_changed_by_this_execution name assertion in tests/test_event_derived_lifecycle.py:286, the path-scoped owned_paths list, and the z2isfg cross-ownership handoff all verified. PR-001 (HIGH, new F9): the fix is INCOMPLETE for its own Concern. E-02 filters only the working-tree half, but concurrent agents here COMMIT to the same branch continuously (12 consecutive HEAD commits span at least six work streams), so a co-worker's COMMITTED out-of-scope file still demands a --scope-reason from an unrelated finalizing plan: the same false-record-or-block dilemma via the other half of the union. Measured at ea4da75: exit 1 naming another agent's committed file. Git authorship CANNOT rescue it because every agent commits under one shared user.name/user.email, so an %an filter would look like a fix and do nothing. Scope and Goal now DECLARE the working-tree-only bound, the committed half is deferred with a required follow-up backlog item, and new E-06/V-06 pin the residual gap as an executable documented limitation that the follow-up must invert. PR-002: recorded the real API signatures and receipt path (finalize_precheck takes no base_head; begin requires keyword-only timestamp; receipt at .aw/state/ipd-lifecycle/<id>.receipt.json), which the plan's probe instructions did not match. PR-003: added the F9 reproduction to Required tests, where an unexpected SUCCESS indicates E-02 leaked into the committed half. Both open questions were re-verified as correctly resolved from evidence; OQ-02 confirmed because _intervening_commits_touching consumes only base..HEAD and never the porcelain. No open questions remain.

- 2026-08-29 draft (opencode (its_direct/pt3-claude-opus-5-1m-us)): created.
- 2026-08-30 to-review (opencode (its_direct/pt3-claude-opus-5-1m-us)): graduated from backlog `077yqc` during the blocking-backlog graduation sweep. Every finding below was measured in a throwaway git repo at HEAD `df731f1`, not inferred; the four scenario probes and the commit-scoping probe are reproduced verbatim in Findings. Two of the item's three proposed fix directions were ELIMINATED by measurement (F5, F6), and the item's implied framing that this is a data-safety problem was DISPROVED (F4), so the plan states the defect as record pollution plus unbounded blocking rather than loss.

## Goal

Attribute by ownership, not by mere dirtiness. A plan that finished its own work and committed it must be able to finalize while an unrelated agent has unrelated files DIRTY, without either lying in its record or waiting for a condition it does not control. The real guarantee the gate exists to provide, that an out-of-scope path the plan ITSELF changed still demands a reason, must survive intact.

HONEST BOUND ON THIS GOAL (F9): "unrelated files dirty" is the whole goal, and it is not the whole problem. When the unrelated agent COMMITS instead of leaving work dirty, its file enters the committed half and finalize still demands a reason, because this plan deliberately does not filter that half. In a repo where concurrent agents commit to the same branch continuously, that residual case is common rather than exotic. This plan makes the working-tree case correct and leaves the committed case for a follow-up that has a real ownership signal to work with; it does not claim to close the shared-checkout attribution problem.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: separate the two sources, then filter only the unowned one

- [x] E-01 Split `_paths_changed_by_this_execution` (`ipd_lifecycle.py`, locate by symbol) into its two sources instead of collapsing them into one set, because they carry DIFFERENT ownership evidence and the union destroys the distinction the fix needs. The committed half (`git diff --name-only <base>..HEAD`) is provably attributable: a commit exists. The working-tree half (`git status --porcelain`) carries NO author, which is the entire defect. Return both sets distinguishably (a small dataclass or a 2-tuple; the executor picks and records which) and keep a function whose result is the union so no existing caller changes behavior at this step. Do NOT change what either git command asks for. Note the function is asserted BY NAME in `tests/test_event_derived_lifecycle.py` (the `self.assertIn("_paths_changed_by_this_execution", src)` source-substring assertion, locate by that string): that module is NOT in `Scope-Paths`, so the name must survive this refactor. If you believe it must be renamed, STOP and report rather than editing that test.
  - Depends on: none
  - Expected outcome: the committed set and the working-tree set are separately available; the union-returning surface is byte-identical to today for every current caller; `tests/test_event_derived_lifecycle.py` passes UNCHANGED including its name-substring assertion.
  - Execution state: performed

- [x] E-02 Apply the ownership filter to the WORKING-TREE half only, in the out-of-scope computation inside `finalize_precheck` (locate by the `out_of_scope` accumulation loop). A working-tree path is OWNED by this execution when it matches the frozen `Scope-Paths` via `_scope_match`, or when it appears in the committed half, or when `_is_implicitly_allowed` covers it. A working-tree path that is NONE of those is UNOWNED and must be excluded from `out_of_scope` rather than demanding a reason. The committed half keeps its CURRENT treatment with no weakening whatsoever: a path this execution committed outside `Scope-Paths` still demands a `--scope-reason` exactly as today (F2 scenario A). This mirrors the rule `begin` already applies (`ipd_lifecycle.py`, the comment reading "Disjoint uncommitted work elsewhere is intentionally ignored so a concurrent multi-agent workflow is not thrashed"), which is why the item calls this a begin/finalize consistency bug: begin ignores disjoint uncommitted work and finalize does not.
  - Depends on: E-01
  - Expected outcome: an unowned dirty path disjoint from both `Scope-Paths` and the plan's commits no longer appears in `out_of_scope`; a path the plan COMMITTED out of scope still does; a dirty path INSIDE `Scope-Paths` is unaffected by this filter (it was never out-of-scope).
  - Execution state: performed

- [x] E-03 Accept and record the ONE honest cost of E-02, which is the case measured as F3: a plan's OWN uncommitted out-of-scope edit is indistinguishable from a co-worker's, so E-02 stops demanding a reason for it too. Do not paper over this. Handle it by keeping the unowned set VISIBLE rather than silent: record the excluded paths in the finalize evidence under a distinct key (alongside the existing `out_of_scope_paths`, `in_scope_unmodified`, and `intervening_in_scope_commits` in `scope_audit`) so the audit trail shows what was disregarded and why, and surface them in the human message as unowned-and-disregarded rather than dropping them. The mitigation that makes this acceptable is already in force and must be stated in the plan record: the execution contract requires path-scoped commits, so an executor's own out-of-scope work is COMMITTED by the time it finalizes and therefore lands in the committed half, where E-02 still demands a reason. Add NO new flag for this in this plan; if the executor concludes an opt-in `--include-worktree` restoration of today's union is needed, that is a scope question to report, not a silent addition.
  - Depends on: E-02
  - Expected outcome: the evidence contains the disregarded-unowned set as its own key; the human message names them as disregarded rather than hiding them; the plan record states the F3 trade-off and its mitigation explicitly; no new CLI flag is introduced.
  - Execution state: performed

### Task group 2: prove both directions

- [x] E-04 Add `tests/test_finalize_scope_ownership.py` proving the fix RESCOPES rather than REMOVES the gate. Required cases, each built on a throwaway git repo (copy the established pattern in `tests/test_ipd_lifecycle_cli.py`; do NOT test against the live repo, and see F7): (a) the exact F1 failure, a plan whose in-scope work is committed finalizes SUCCESSFULLY while an unrelated unowned file is dirty, with NO `--scope-reason` supplied; (b) a path the plan COMMITTED outside `Scope-Paths` STILL demands a `--scope-reason` (this is the no-weakening assertion and must fail if E-02 filters the committed half by mistake); (c) a dirty path INSIDE `Scope-Paths` still behaves exactly as today; (d) the intervening-commit collision computation is unchanged, still surfacing in-scope committed paths in `intervening_in_scope_commits`; (e) the disregarded-unowned set from E-03 appears in the evidence naming the excluded path; (g) an ISOLATED-LANE case proving the lane path is unaffected by this change, which pins F9 so nobody later 'fixes' a path that was never broken; (f) the F3 admission is pinned as a test, so an OWN uncommitted out-of-scope edit is disregarded and the test SAYS SO in its name and docstring rather than leaving the regression undocumented. Each of (a) and (e) must be shown to FAIL against pre-fix code, and (b), (c), (d) must be shown to fail under a naive deletion of the working-tree half.
  - Depends on: E-01, E-02, E-03
  - Expected outcome: the module passes; (a) and (e) fail against pre-fix code; (b), (c), (d) fail if the working-tree half is deleted outright instead of ownership-filtered; (f) documents the accepted trade-off as an executable assertion.
  - Execution state: performed

- [x] E-05 Assert the begin/finalize CONSISTENCY property the item names as the underlying bug, so the two gates cannot silently diverge again. Add a test that constructs ONE repo state and asserts that begin and finalize agree about a disjoint unowned dirty path: begin grants authority (it already ignores disjoint uncommitted work) and finalize no longer demands a reason for that same path. This is a distinct property from E-04's per-case behavior and is the invariant a future edit to either gate would break. Do NOT change begin in this plan; `z2isfg` owns begin's isolated-lane scoping and this plan must not touch it.
  - Depends on: E-02
  - Expected outcome: a single test demonstrates begin and finalize reaching the SAME verdict on a disjoint unowned dirty path; it fails against pre-fix code because finalize disagreed; `ipd_lifecycle.begin` is unmodified by this plan.
  - Execution state: performed

- [x] E-06 Pin the KNOWN RESIDUAL GAP (F9) as an executable test so it cannot be mistaken for fixed, and so the follow-up that closes it has a failing-to-passing target. Add a case to `tests/test_finalize_scope_ownership.py` that builds the F9 state (the plan's own in-scope work committed path-scoped, plus ONE unrelated out-of-scope file committed by a notional concurrent agent under the SAME git identity) and asserts finalize STILL demands a `--scope-reason` for that path after this plan's fix. Name the test and write its docstring so the reader understands this is a DOCUMENTED LIMITATION rather than a passing feature: it pins current post-fix behavior and MUST be inverted by the follow-up deferral, exactly as the repo's characterization pattern intends. Also assert in the same test that git authorship is NOT usable as the discriminator here, by constructing the co-worker commit under the same identity as the executor, so a future implementer cannot reach for `%an` and believe it works.
  - Depends on: E-02
  - Expected outcome: a test that PASSES today, encodes the F9 limitation in its name and docstring, names the follow-up that must invert it, and demonstrates the same-identity condition that rules out authorship-based filtering; no attempt is made to fix the committed half in this plan.
  - Execution state: performed

## Project conventions discovered (Step 0)

- The defect is REPRODUCIBLE and was reproduced, not inferred. Probe run at HEAD `df731f1` in a throwaway repo: a plan with `Scope-Paths: agent_workflows/demo.py, tests/test_demo.py`, its own work committed, then ONE unrelated untracked file left dirty by a notional co-worker, yields exit 1 and `out-of-scope path needs a --scope-reason: agent_workflows/coworker.py`. Full transcript in F1.
- `begin` and `finalize` are DELIBERATELY inconsistent today, and begin is the one that is right. `begin`'s step 6 comment states: "Disjoint uncommitted work elsewhere is intentionally ignored so a concurrent multi-agent workflow is not thrashed; finalize's scope reconciliation still catches out-of-scope changes." That last clause is exactly the assumption this plan corrects: finalize catching out-of-scope changes is correct, but catching OTHER AGENTS' out-of-scope changes is not.
- `dirty_within` (in `run_evidence.py`) exists precisely so begin and finalize share one matcher, and its docstring says the injected matcher guarantees they "agree". They agree on the MATCHER and disagree on the INPUT SET, which is how the divergence hid.
- `_is_implicitly_allowed` already exempts the plan's own file and the plans index from the out-of-scope set, so an exemption mechanism at this exact site is established practice, not a new concept.
- The finalize commit is PATH-SCOPED to `[plan_rel, dest_rel, index_json_rel, index_md_rel]` (the `owned_paths` list in `_finalize_transaction`). This is load-bearing for the severity assessment: see F4.
- `_paths_changed_by_this_execution` is referenced BY NAME as a source substring in `tests/test_event_derived_lifecycle.py`, so it is effectively part of a tested contract and cannot be freely renamed. That module is not in this plan's `Scope-Paths`.
- The suite is invoked BARE. `pyproject.toml` addopts already supply `-q -n auto --dist=worksteal -m 'not slow'`; adding a second `-q` compounds to `-qq` and suppresses the `N passed` line this plan requires pasted. Fast-suite baseline MEASURED at `df731f1`: `2880 passed, 3 skipped, 4 xfailed in 46.22s`. Record your OWN counts with the `git rev-parse HEAD` they were taken at; this repo's baseline moves hourly under concurrent work.

## Findings

| # | Sev | Location | Finding | Evidence |
| --- | --- | --- | --- | --- |
| F1 | HIGH | `_paths_changed_by_this_execution` in `ipd_lifecycle.py` | The union of `base..HEAD` with the whole porcelain has NO ownership filter, so an unrelated agent's untracked file is attributed to the finalizing plan. REPRODUCED, not inferred. | Throwaway repo at `df731f1`: `finalize exit: 1`; message names `--scope-reason agent_workflows/coworker.py=<why-this-out-of-scope-edit-was-needed>`; `changed_paths: ['agent_workflows/coworker.py', 'agent_workflows/demo.py', 'tests/test_demo.py']`; `out_of_scope_paths: ['agent_workflows/coworker.py']`; plan left unmoved |
| F2 | HIGH | same site, scenario A | The property that must NOT weaken, measured so it is checkable: a path the plan itself COMMITTED outside `Scope-Paths` correctly demands a reason today. E-02 must preserve this exactly. | Scenario A probe: `exit: 1 | out_of_scope: ['agent_workflows/mine_oos.py']` after `_commit_all` of P's own out-of-scope edit |
| F3 | MED | same site, scenario B | THE HONEST COST of the fix, measured: a plan's OWN UNCOMMITTED out-of-scope edit produces output IDENTICAL to a co-worker's (`exit: 1 | out_of_scope: ['agent_workflows/mine_oos.py']`). The porcelain carries no author, so no filter can separate them, and E-02 necessarily stops demanding a reason for both. Mitigated, not solved: the execution contract requires path-scoped commits, so an executor's own work is committed before finalize and lands in the attributable half. E-03 owns making this visible instead of silent. | Scenario B probe, byte-identical to scenario A's output despite opposite ownership |
| F4 | MED | `owned_paths` in `_finalize_transaction` | The item does not say how BAD this is, and the answer is that it is NOT a data-safety bug. Forcing a false `--scope-reason` through does NOT sweep the co-worker's file into the commit: the finalize commit is path-scoped and the co-worker's file remains untracked afterward. So the harm is (1) a permanently false claim in the plan's record, defeating the audit trail the gate exists to provide, and (2) unbounded blocking. Do NOT describe this as loss or corruption. | Probe with a forced reason: commit `7ddefc6` contains ONLY `.aw/records/plans/INDEX.json`, `INDEX.md`, and the moved plan; `git status --porcelain` after finalize still shows `?? agent_workflows/coworker.py` |
| F5 | MED | the item's fix candidate (2) | ELIMINATED BY MEASUREMENT: the item proposes consulting "the lane/lease registry or the run ledger to identify which run owns a dirty path". There is no such data. The begin receipt records only actor, base_head, plan id/path/digests, scope_paths, and timestamp (dumped verbatim in the probe), with NO per-path ownership. `worktree_lease.LeaseTable` is a per-path ownership table but it is an in-memory structure in the lane allocator, not a durable registry finalize can read. Choosing candidate (2) would require BUILDING a registry, which is out of this plan's scope. | Receipt dump: keys are exactly `actor`, `base_head`, `kind`, `plan_content_digest`, `plan_id`, `plan_path`, `pre_execution`, `requirement_digest`, `schema_version`, `scope_paths`, `timestamp` |
| F6 | LOW | the item's fix candidate (3) | ELIMINATED AS THE PRIMARY FIX: an opt-in `--include-worktree` that preserves today's union defaults the BROKEN behavior on, so the defect keeps firing for everyone who does not pass the flag. It is at best an escape hatch layered on candidate (1), not a fix. This plan takes candidate (1) and adds no flag (E-03). | reasoning from the item's own text plus F1's default-path failure |
| F7 | LOW | `tests/test_ipd_lifecycle_cli.py` | The fixture pattern to copy exists and is proven: `_init_git` writes a `.gitignore` containing `.aw/state/` so the begin receipt does not dirty the tree, and `_completed_plan_text` produces a plan that lints conforming at pre-transition. Reuse these rather than hand-rolling a fixture; note the module is NOT in `Scope-Paths`, so import from it or replicate the helpers, do not edit it. | helper functions read at `df731f1` |
| F8 | LOW | scenario D probe | A dirty path INSIDE `Scope-Paths` at finalize time is NOT currently an out-of-scope finding and finalize SUCCEEDS (`exit: 0 | out_of_scope: []`). Recorded so the executor does not "fix" a non-defect: in-scope dirtiness is begin's gate, not finalize's, and E-04 case (c) pins today's behavior rather than changing it. | Scenario D probe: an in-scope path dirtied after the in-scope commit, `exit: 0` |
| F9 | HIGH | `_paths_changed_by_this_execution`, the COMMITTED half | THE FIX IS INCOMPLETE FOR THE STATED CONCERN, and the gap is not a corner case in this repo, it is the common case. The Concern says "in a shared checkout every concurrent agent's uncommitted file is therefore attributed to whichever plan finalizes first". True, but concurrent agents in this repo COMMIT constantly to the same branch (12 consecutive HEAD commits at `ea4da75` belong to at least six different work streams: `plans(revgate)`, `backlog(runverdict)`, `feat(attention)`, `docs(plan) 8h9lap`, `feat(config)`, plus this review's own two). Every such commit lands in `base..HEAD` and therefore in the COMMITTED half, which E-02 deliberately leaves untouched. So after this plan lands, a co-worker's COMMITTED out-of-scope file still demands a `--scope-reason` from an unrelated finalizing plan, which is the SAME false-record-or-block dilemma the Concern describes, reached by a different half of the union. MEASURED, not argued: with the plan's own in-scope work committed path-scoped and one concurrent commit added under the same git identity, finalize exits 1 naming `agent_workflows/other_agents_file.py`. Crucially, git authorship CANNOT rescue this: every agent in this repo commits as the same `user.name`/`user.email` (verified at `ea4da75`), so `%an` cannot separate a co-worker's commit from the executor's own. This does NOT invalidate the plan's approach, and the working-tree half remains worth fixing, but the plan must either extend ownership filtering to the committed half using a real ownership signal or explicitly scope itself to the working-tree half and hand the committed half to a named follow-up. It must NOT keep claiming the Concern is resolved. | Probe at `ea4da75` (own work committed path-scoped, then one concurrent same-identity commit): `exit: 1`, `out_of_scope_paths: ["agent_workflows/other_agents_file.py"]`; `git log --format='%h|%an|%ae' -6` shows a single identity for all recent commits; contrast the co-worker-COMMITTED probe in F1's family, which also exits 1 |
| F9 | HIGH | `oc_runipd.driver_finalize` call site; measured | WHERE THIS BUG ACTUALLY BITES, measured 2026-08-30 because the plan previously implied it affected all runs. An ISOLATED lane is IMMUNE. The driver finalizes with `finalize_repo` set to the lane worktree when a lane exists, and a lane is a separate checkout whose `git status --porcelain` cannot see main's uncommitted files. Reproduced: coworker file left dirty in main, lane commits its own in-scope work, finalize IN THE LANE returns exit 0 with `out_of_scope: []` and the lane's own porcelain empty. The identical scenario finalized from MAIN fails and demands a reason. So the defect is confined to turns whose execution tree IS main: isolation explicitly disabled, a hand-run `aw ipd finalize` from the main checkout, or a retry from main after a lane's work was already committed (the case that actually blocked a verified plan). This NARROWS the blast radius and the E-03 trade-off, which now applies only in main; it does NOT make the bug benign, because hand-run finalize is a normal path and has no legitimate workaround today. | throwaway two-tree probe: lane finalize `exit: 0`, `out_of_scope: []`, lane porcelain `''`; main finalize exit 1 naming the coworker path |

## Proposed changes (ordered, validatable)

1. Split the committed half from the working-tree half without changing any caller's observed result (E-01).
2. Ownership-filter the working-tree half only, leaving the committed half's refusals exactly as they are (E-02).
3. Make the disregarded set visible in evidence and in the human message, and record the F3 trade-off honestly (E-03).
4. Prove both directions, including the no-weakening cases and the accepted regression (E-04).
5. Pin begin/finalize agreement as its own invariant so the two gates cannot diverge again (E-05).
6. Pin the residual committed-half gap (F9) as a documented, executable limitation and file its follow-up (E-06).

## Deferred / out of scope (with reason)

- `begin`'s dirty gate and its isolated-lane scoping are owned by plan `z2isfg` (`lanetruth-02`, approved, `From-Backlog: l6rh0z`). That plan's own Deferred section explicitly assigns ownership-awareness to `077yqc`, i.e. to THIS plan, so the split is agreed on both sides. This plan does not modify `begin`.
- Building a durable per-path ownership registry (the item's candidate 2) is deferred with the reason recorded in F5: no such data exists today and creating it is a much larger change than the defect warrants.
- THE COMMITTED HALF of the same defect (F9) is deferred, and this is the most important deferral in this plan because a reader could otherwise believe the shared-checkout problem is closed. A concurrent agent's COMMITTED out-of-scope file still demands a `--scope-reason` from an unrelated finalizing plan after this change. It is deferred rather than fixed because the obvious signal does not work: git authorship cannot distinguish agents here, since every agent commits under a single shared `user.name`/`user.email` (verified at `ea4da75`), so `%an` filtering would be a no-op that LOOKS like a fix. Filtering the committed half by "commits whose message names this plan id" would be prose-matching of exactly the kind `97df1z` replaces with a structured field, so it is not acceptable either. A real fix needs a durable per-path or per-commit ownership record, which is F5's eliminated registry, so the two deferrals collapse into one follow-up. ACTION REQUIRED OF THE EXECUTOR: file that follow-up as a backlog item (carrying the F9 measurement and this reasoning) and record its id in this plan's terminal history, so the residual half is tracked rather than lost. Do NOT attempt it inside this plan.
- An opt-in `--include-worktree` flag restoring today's union is deferred per F6: it defaults the broken behavior on. If a real need appears, it is a follow-up.
- Universal worktree isolation (the `wtiso` Set) would remove the shared-tree case entirely, and per F9 isolation ALREADY avoids this bug today for lane-executed turns. That is a mitigation, not a fix: hand-run finalize and non-isolated runs remain broken with no legitimate way through, so this plan proceeds and does not wait on `wtiso`.

## Scope check

- Over-scope: none. `agent_workflows/ipd_lifecycle.py` carries F1/F2/F3/F8/F9 and hosts E-01 through E-03; the new test module is required by E-04, E-05, and E-06.
- PARTIAL-FIX DISCLOSURE (F9): this plan resolves its Concern only for the working-tree half of the attribution union. That is a legitimate scope, since the working-tree half is the half with no ownership evidence at all and it is fixable today, but it must be DECLARED rather than discovered by a later reader. The committed half is named under Deferred with a required follow-up and pinned by E-06. A reviewer should reject any report from this plan that describes the shared-checkout attribution defect as closed.
- Under-scope: NONE OUTSTANDING, and this is deliberate rather than lucky. The fix is confined to one function and one computation loop in a single module. Two modules that MUST stay green without being edited are named explicitly so the fence is checkable: `tests/test_event_derived_lifecycle.py` (asserts `_paths_changed_by_this_execution` by name, see E-01) and `tests/test_ipd_lifecycle_cli.py` (the existing reconciliation and receipt tests, whose behavior E-02 must not disturb for the committed half). Neither is in `Scope-Paths`; if either must change, STOP and report.
- `agent_workflows/oc_runipd.py` and `agent_workflows/agy_runipd.py` are NOT needed. The runners call the packaged `aw ipd finalize` surface rather than reimplementing the attribution, so a single fix in `ipd_lifecycle.py` reaches both hosts. This is the reverse of sibling plan `z2isfg`'s situation, where the caller itself needed changing; state this explicitly in V-02 rather than leaving a reader to wonder whether driver symmetry was forgotten. Verify it holds before relying on it, since `oc_runipd.py` defines its own `dirty_tree_overlap` for the MERGE path, which is a different mechanism and is owned by backlog `h1ksy6`.

## Required tests / validation

- `tests/test_finalize_scope_ownership.py` must pass with every case in E-04 plus E-05's consistency assertion. Falsifiability is MANDATORY and specific: (a) and (e) must be shown to FAIL against pre-fix code, and (b), (c), (d) must be shown to fail if the working-tree half is DELETED rather than ownership-filtered. A fix that only stops refusing has removed a safety property instead of correcting one, and (b) is the assertion that catches it.
- `tests/test_ipd_lifecycle_cli.py` must pass UNCHANGED, in particular the reconciliation tests that assert an out-of-scope COMMITTED path still demands a reason (locate `test_out_of_scope_headless_missing_reason_fails_closed_naming_command` and `test_out_of_scope_headless_with_reason_records_and_proceeds` by name). Note these fixtures commit their out-of-scope file via `_commit_all`, so they exercise the committed half and MUST stay green; if one turns red, E-02 has leaked into the committed half.
- `tests/test_event_derived_lifecycle.py` must pass UNCHANGED, including its source-substring assertion on the function name (E-01).
- INVOKE THE SUITE BARE: `python3 -m pytest` and `python3 -m pytest -m ""` (or `make test` / `make test-all`). Do NOT add `-n auto` or a second `-q`.
- BASELINE IS A MEASUREMENT, NOT A CONSTANT. Fast suite at `df731f1`: `2880 passed, 3 skipped, 4 xfailed`. Take your own before/after readings with their HEAD, and do not treat a mismatch against this figure as a regression you caused. Concurrent agents are committing to this repo while this plan sits in review.
- End-to-end: reproduce F1's exact conditions (commit the plan's in-scope work, leave ONE unrelated untracked file dirty, finalize with NO `--scope-reason`) and paste the transcript showing the plan now finalizes. Then paste the evidence block showing the disregarded path recorded under E-03's key.
- ALSO reproduce F9's conditions (the same, but with the unrelated file COMMITTED under the same git identity) and paste the transcript showing finalize STILL demands a reason. This is not a failure of the plan; it is the declared bound, and pasting it is what keeps the record honest. If this case unexpectedly SUCCEEDS, E-02 has leaked into the committed half and V-02's no-weakening proof is invalid, so treat a success here as a defect.
- The fixture helpers in `tests/test_ipd_lifecycle_cli.py` are importable and were used to reproduce every finding in this plan during review (`_init_git`, `_commit_all`, `_completed_plan_text`, `_write_plan`). Note the real signatures, which the plan's probes had to match: `begin(repo_root, plan_path, actor, *, timestamp)` and `finalize(repo_root, plan_path, actor, message, *, apply=False, scope_reasons=None, ...)`; `finalize_precheck(repo_root, plan_path)` takes NO `base_head` argument. The begin receipt lands at `.aw/state/ipd-lifecycle/<plan_id>.receipt.json`.
- `aw check-local-leaks . --agent` clean; `aw ipd lint --phase pre-transition` conforming.

## Spec / documentation sync

- No spec text change is required. The scope-reconciliation contract is unchanged in shape: out-of-scope paths still demand reasons and declared-but-unmodified paths still demand acks; only the membership of the out-of-scope set changes.
- The `begin` step-6 comment asserting that "finalize's scope reconciliation still catches out-of-scope changes" becomes imprecise once this lands, since finalize will catch out-of-scope changes THIS EXECUTION made rather than all of them. That comment lives in `ipd_lifecycle.py`, which is in `Scope-Paths`, so correct it as part of E-02 rather than leaving a stale claim behind.
- Any user-facing message text added by E-03 must be free of em and en dashes per the execution contract.

## Open questions

### OQ-01: Does a plan's own UNCOMMITTED out-of-scope edit need to keep demanding a reason?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: NO, accept the loss, make it visible, and say so plainly. Resolved from measurement rather than left to the maintainer. F3 shows the two cases are BYTE-IDENTICAL in output because `git status --porcelain` carries no author, so no filter distinguishable at this site can separate a co-worker's dirty file from the executor's own. Since the two cannot be told apart, the only choices are to keep failing on both (today's defect, which the item documents as having no legitimate remedy) or to disregard both. Three facts make disregarding acceptable: the execution contract already requires path-scoped commits, so an executor's own out-of-scope work is COMMITTED by finalize time and lands in the attributable half where F2's refusal still fires; the committed half is untouched by this change, so the audit trail for real work is fully preserved; and E-03 keeps the disregarded set in the evidence and the message, so nothing becomes invisible. The honest cost, which the executor MUST record in the terminal history rather than glossing: an executor who leaves its OWN out-of-scope work uncommitted and then finalizes will no longer be asked to justify it.

### OQ-02: Should the ownership filter also apply to the intervening-commit collision check?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: NO, leave `_intervening_commits_touching` alone. It already operates ONLY on the committed diff (`base..HEAD`), so it never consumes the porcelain and cannot exhibit this defect; and its purpose is the opposite of an ownership filter, since it deliberately reports commits by OTHER actors that touched this plan's declared territory, which is a collision the plan should see. Measured in scenario C: a co-worker's out-of-scope COMMIT surfaces through the out-of-scope path (correctly, as a committed change) and the in-scope committed paths surface in `intervening_in_scope_commits` as designed. E-04 case (d) pins this as unchanged so a future refactor does not fold the two computations together.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [x] V-01 validates E-01
  - Required evidence: paste the new split function's signature and body, and the union-preserving surface. Paste `tests/test_event_derived_lifecycle.py` passing UNCHANGED, specifically the assertion containing the substring `_paths_changed_by_this_execution`, proving the name survived. Then paste a transcript showing that for one fixture repo the union result is IDENTICAL before and after the split (same sorted path list), so this step is provably behavior-neutral.
  - Observed evidence: SPLIT SURFACE (`agent_workflows/ipd_lifecycle.py`, located by symbol): new `class ChangedPathSources(NamedTuple)` with fields `committed: Tuple[str, ...]` / `working_tree: Tuple[str, ...]` and method `def union(self) -> List[str]: return sorted(set(self.committed) | set(self.working_tree))`; new collector `def _changed_path_sources(repo_root: Path, base_head: str) -> ChangedPathSources` asking git for EXACTLY the two commands the pre-split code asked for (`["diff","--name-only",f"{base_head}..HEAD"]` and `["status","--porcelain"]`, porcelain rename-split logic unchanged); and the retained union surface `def _paths_changed_by_this_execution(repo_root: Path, base_head: str) -> List[str]: return _changed_path_sources(repo_root, base_head).union()`. A NamedTuple was chosen over a 2-tuple (recorded per E-01's "the executor picks and records which") so the two halves are named at every call site rather than positional. NAME SURVIVED: `pytest tests/test_event_derived_lifecycle.py -k test_reuses_finalize_scope_helpers_no_fork -v` -> `1 passed in 3.15s`; that is the test carrying `self.assertIn("_paths_changed_by_this_execution", src)` (asserted against `inspect.getsource(ce.check_scope_drift)`), and `git diff --stat -- tests/test_event_derived_lifecycle.py agent_workflows/check_engine.py` is EMPTY, so neither the assertion nor its subject was edited. Whole module unchanged: `pytest tests/test_ipd_lifecycle_cli.py tests/test_event_derived_lifecycle.py` -> `75 passed in 4.23s`. BEHAVIOR-NEUTRALITY MEASURED, not argued: a probe built one fixture repo containing every case at once (committed in-scope, committed out-of-scope, untracked, dirty-tracked, staged) and compared the post-split union against the pre-split implementation inlined verbatim from `be49ac4`: `committed   : ['agent_workflows/demo.py', 'agent_workflows/mine_oos.py']` / `working_tree: ['agent_workflows/coworker.py', 'agent_workflows/demo.py', 'tests/staged.py']` / `union (post-split) : ['agent_workflows/coworker.py', 'agent_workflows/demo.py', 'agent_workflows/mine_oos.py', 'tests/staged.py']` / `union (pre-split)  : ['agent_workflows/coworker.py', 'agent_workflows/demo.py', 'agent_workflows/mine_oos.py', 'tests/staged.py']` / `IDENTICAL: True`. Also pinned as a permanent test (`ChangedPathSourcesSplitTests::test_union_of_the_split_equals_the_pre_split_union`). Measured at HEAD `be49ac476a49010e111b295104bd07e259bf54f2`.
  - Result: pass

- [x] V-02 validates E-02
  - Required evidence: paste the ownership predicate and the changed `out_of_scope` loop. Paste the F1 reproduction now SUCCEEDING with no `--scope-reason` supplied, showing exit 0 and the plan moved to `executed/`. THEN paste the no-weakening proof: the F2 scenario (an out-of-scope path the plan COMMITTED) still exiting 1 and still naming that path. Paste `tests/test_ipd_lifecycle_cli.py` passing UNCHANGED, naming the two reconciliation tests that exercise the committed half. Finally, state explicitly whether `oc_runipd.py` and `agy_runipd.py` needed any change and paste the evidence for that claim (that both reach this code through the packaged `aw ipd finalize` surface); "driver symmetry not applicable" is an acceptable answer ONLY with that evidence pasted.
  - Observed evidence: OWNERSHIP PREDICATE (new, sited next to `_is_implicitly_allowed`): `def _working_tree_path_is_owned(path, *, scope_paths, committed, plan_rel) -> bool` returning True when `_is_implicitly_allowed(path, plan_rel)`, or `path in set(committed)`, or `any(_scope_match(path, pat) for pat in scope_paths)`. CHANGED LOOP (in `finalize_precheck`, the `out_of_scope` accumulation): after the existing implicit-allowance and `_scope_match` skips, it adds `if p not in committed_set and not _working_tree_path_is_owned(...): disregarded_unowned.append(p); continue` before `out_of_scope.append(p)`. The predicate can only ever REMOVE paths from the out-of-scope set, never add any (pinned by `test_ownership_predicate_only_ever_removes_paths_never_adds`). F1 NOW SUCCEEDS, end-to-end through the real CLI with NO `--scope-reason`: `$ git status --porcelain` -> `?? agent_workflows/coworker.py`; `$ aw ipd finalize abc123 --actor opencode/... --message ... --apply` -> `[exit 0]` / `finalized abc123 -> executed at 2c489e91d0a1 (actor opencode/its_direct-pt3-claude-opus-5). Disregarded 1 uncommitted path(s) not owned by this execution (left untouched, recorded in the scope audit): agent_workflows/coworker.py.`; `$ ls .aw/records/plans/executed/` -> `20260830-demo-01-abc123-demo.ipd.md` (moved). NO-WEAKENING PROOF (F2 scenario A, own COMMITTED out-of-scope path) still refuses: `finalize exit: 1` / `findings: ['out-of-scope path needs a --scope-reason: agent_workflows/mine_oos.py']` / `out_of_scope_paths: ['agent_workflows/mine_oos.py']` / `plan still pending: True`, and with a reason supplied it proceeds and records `out-of-scope agent_workflows/mine_oos.py: needed mid-stream` in the terminal history. COMMITTED-HALF REGRESSION TESTS PASS UNCHANGED: `pytest "tests/test_ipd_lifecycle_cli.py::ReconciliationTests::test_out_of_scope_headless_missing_reason_fails_closed_naming_command" "tests/test_ipd_lifecycle_cli.py::ReconciliationTests::test_out_of_scope_headless_with_reason_records_and_proceeds"` -> `2 passed in 1.64s`; both fixtures commit their out-of-scope file via `_commit_all`, so they exercise the committed half, and `git diff --stat -- tests/test_ipd_lifecycle_cli.py` is EMPTY. DRIVER SYMMETRY NOT APPLICABLE, with the evidence E-02/Under-scope demands: `oc_runipd.py` and `agy_runipd.py` needed NO change because neither reimplements the attribution. Both define `_compute_scope_reconciliation(repo, plan_path)` whose body calls `ipd_lifecycle.finalize_precheck(repo, plan_path)` and then reads `evidence.get("scope_audit", {})` (docstring: "Reuses the authoritative, read-only `ipd_lifecycle.finalize_precheck` ... rather than re-implementing the diff"), and their `driver_finalize` shells to `python -m agent_workflows ipd finalize ... --apply`, i.e. the same packaged surface. So the single fix in `ipd_lifecycle.py` reaches both hosts; `git diff --stat -- agent_workflows/oc_runipd.py agent_workflows/agy_runipd.py` is EMPTY. Verified as the plan instructs that `oc_runipd.dirty_tree_overlap` is a DIFFERENT mechanism on the MERGE path (owned by backlog `h1ksy6`) and was left untouched. Also corrected the stale claim the Spec-sync section names: `begin`'s step-6 docstring and comment no longer assert finalize "catches out-of-scope changes" without qualification.
  - Result: pass

- [x] V-03 validates E-03
  - Required evidence: paste the evidence block from a real finalize run showing the disregarded-unowned set under its own key, with the excluded path named, alongside the existing `out_of_scope_paths` key. Paste the human message showing the disregarded paths surfaced rather than hidden, and confirm it contains no em or en dash. Paste the terminal history note recording the F3 trade-off in the executor's own words. Confirm by grep that NO new CLI flag was added for this.
  - Observed evidence: EVIDENCE BLOCK from a REAL `aw ipd finalize --apply --json` run (F1 conditions), showing the new key beside the pre-existing ones: `"scope_audit": { "grandfathered": false, "in_scope": true, "out_of_scope_paths": [], "in_scope_unmodified": [], "intervening_in_scope_commits": ["agent_workflows/demo.py", "tests/test_demo.py"], "disregarded_unowned_paths": ["agent_workflows/coworker.py"], "committed_paths": ["agent_workflows/demo.py", "tests/test_demo.py"], "working_tree_paths": ["agent_workflows/coworker.py"] }`. The two halves are recorded separately as well, so an auditor can see WHY a path was disregarded (present in `working_tree_paths`, absent from `committed_paths`) rather than taking the verdict on trust. HUMAN MESSAGE surfaces it rather than hiding it, at both the precheck and the terminal step: precheck -> `precheck passed (receipt valid, pre-transition conforming; scope delta computed). Disregarded 1 uncommitted path(s) not owned by this execution (no reason required, recorded in the scope audit): agent_workflows/coworker.py.`; success -> `finalized abc123 -> executed at 2c489e91d0a1 (actor opencode/...). Disregarded 1 uncommitted path(s) not owned by this execution (left untouched, recorded in the scope audit): agent_workflows/coworker.py.` NO EM OR EN DASH in either string: asserted mechanically in the test (`assertNotIn("\u2014", msg)` and `assertNotIn("\u2013", msg)` on both the precheck message and the finalize message), not eyeballed. NO NEW CLI FLAG: `git diff --stat -- agent_workflows/cli.py` is EMPTY; `git diff -- agent_workflows/ipd_lifecycle.py | grep -i "include.worktree\|add_argument"` returns nothing; the finalize parser still exposes only the pre-existing `--scope-reason`/`--scope-ack`. F3 TRADE-OFF RECORDED in this executor's own words, both in the code (the "ACCEPTED COST" paragraph of `_working_tree_path_is_owned`) and in the terminal history entry: an executor who leaves its OWN out-of-scope work UNCOMMITTED and then finalizes will no longer be asked to justify it, because `git status --porcelain` carries no author and that case is byte-identical to a co-worker's (F3). It is a real loss of a refusal, accepted because the two cases are indistinguishable at this site and the alternative is today's defect; mitigated because the execution contract's path-scoped commits put an executor's actual work in the COMMITTED half, where the reason requirement still fires (V-02), and because the disregarded path stays visible in both the audit and the message instead of vanishing.
  - Result: pass

- [x] V-04 validates E-04
  - Required evidence: paste all six cases passing. Then paste FALSIFIABILITY evidence, each as actual failing output, not a claim: (a) and (e) failing against pre-fix code, and (b), (c), (d) failing when the working-tree half is deleted outright instead of ownership-filtered. Paste case (f)'s test name and docstring showing the accepted F3 regression is documented as an executable assertion rather than a comment.
  - Observed evidence: ALL SEVEN CASES PASS (the plan lists (a)-(g); the module has 11 tests total, adding E-01/E-05/E-06 coverage): `pytest tests/test_finalize_scope_ownership.py` -> `11 passed in 1.60s` at HEAD `be49ac476a49010e111b295104bd07e259bf54f2`. Mapping: (a) `test_unowned_dirty_path_no_longer_blocks_finalize`; (b) `test_own_committed_out_of_scope_path_still_demands_a_reason`; (c) `test_dirty_path_inside_scope_paths_behaves_exactly_as_before`; (d) `test_intervening_commit_collision_computation_is_unchanged`; (e) `test_disregarded_unowned_path_is_recorded_in_the_evidence_not_dropped`; (f) `test_own_UNCOMMITTED_out_of_scope_edit_is_now_disregarded_ACCEPTED_REGRESSION`; (g) `test_isolated_lane_finalize_is_unaffected_by_the_ownership_filter` (a real `git worktree add` lane finalizing its own tree while main is dirty). FALSIFIABILITY (a) AND (e) AGAINST PRE-FIX CODE, actual failing output from a throwaway worktree checked out at `be49ac4` with only the new test file copied in (`grep -c disregarded_unowned agent_workflows/ipd_lifecycle.py` -> `0`, confirming the fix is absent): `FAILED tests/test_finalize_scope_ownership.py::WorkingTreeOwnershipFilterTests::test_unowned_dirty_path_no_longer_blocks_finalize` / `FAILED ...::test_disregarded_unowned_path_is_recorded_in_the_evidence_not_dropped` / `2 failed in 1.62s`, with case (a)'s assertion reading `E AssertionError: 1 != 0 : finalize needs scope reconciliation answers (plan left unmoved). Supply them with: aw ipd finalize ... --scope-reason agent_workflows/coworker.py=<why-this-out-of-scope-edit-was-needed> / ('out-of-scope path needs a --scope-reason: agent_workflows/coworker.py',)` and case (e)'s reading `E KeyError: 'disregarded_unowned_paths'`. (Whole module against pre-fix: `10 failed, 1 passed`.) FALSIFIABILITY (b), (c), (d) UNDER A NAIVE DELETION of the working-tree half: a second throwaway worktree took the FIXED `ipd_lifecycle.py` and replaced the porcelain-collecting `if rc == 0:` with `if False:` (deleting the half instead of ownership-filtering it); actual output `FAILED ...::NoWeakeningTests::test_own_committed_out_of_scope_path_still_demands_a_reason` / `FAILED ...::NoWeakeningTests::test_dirty_path_inside_scope_paths_behaves_exactly_as_before` / `FAILED ...::NoWeakeningTests::test_intervening_commit_collision_computation_is_unchanged` (plus (a)/(e)/(f) and the split test) -> `7 failed, 4 passed in 1.70s`, e.g. (d) failing with `E AssertionError: 'tests/declared.py' not found in []`. So all three distinguish RESCOPING from REMOVING, which is exactly what the plan demanded and what a bare "it stops refusing" fix would not survive. CASE (f) IS AN EXECUTABLE ASSERTION, not a comment: name `test_own_UNCOMMITTED_out_of_scope_edit_is_now_disregarded_ACCEPTED_REGRESSION`, docstring opening `CHARACTERIZATION of the ONE accepted cost (F3 / OQ-01), not a feature.` and closing `If a future change makes uncommitted ownership provable, this test SHOULD start failing and should then be inverted rather than deleted.`; it asserts both `out_of_scope_paths == []` (the regression) and `disregarded_unowned_paths == ['agent_workflows/mine_oos.py']` (the mitigation that keeps it visible). Note the deviation recorded honestly: the plan said "six cases" while listing seven labels (a)-(g); all seven are implemented.
  - Result: pass

- [x] V-05 validates E-05
  - Required evidence: paste the consistency test and its passing output, showing ONE repo state where begin GRANTS and finalize no longer demands a reason for the same disjoint unowned dirty path. Paste it FAILING against pre-fix code, which is where the two gates disagreed. Paste `git diff --stat` or equivalent proving `ipd_lifecycle.begin` itself was NOT modified by this plan, since `z2isfg` owns that function's other change and the two must not collide.
  - Observed evidence: CONSISTENCY TEST: `BeginFinalizeConsistencyTests::test_begin_and_finalize_agree_about_a_disjoint_unowned_dirty_path`. It builds ONE repo state (a disjoint, unowned, untracked `agent_workflows/coworker.py` present for BOTH gates), records `begin_grants = begin_res.exit_code == LC.EXIT_OK`, re-asserts the same dirty path is still present for the finalize half (`assertIn("?? agent_workflows/coworker.py", git status --porcelain)`), computes `finalize_demands_reason = "agent_workflows/coworker.py" in audit["out_of_scope_paths"]`, and then asserts the AGREEMENT ITSELF (`assertEqual(begin_grants, not finalize_demands_reason, "begin and finalize must agree about a disjoint unowned dirty path")`) rather than two independent per-gate behaviors, so a future edit to EITHER gate breaks this one assertion. PASSING: included in `pytest tests/test_finalize_scope_ownership.py` -> `11 passed in 1.60s`. FAILING AGAINST PRE-FIX CODE at the finalize half, actual output: `FAILED tests/test_finalize_scope_ownership.py::BeginFinalizeConsistencyTests::test_begin_and_finalize_agree_about_a_disjoint_unowned_dirty_path` with `E AssertionError: True is not false : finalize must NOT demand a reason for a path begin deliberately ignored` (run at `be49ac4` in the pre-fix worktree). BEGIN NOT MODIFIED, proven mechanically rather than by inspection: `git diff -U2 -- agent_workflows/ipd_lifecycle.py` touches `def begin` in exactly two hunks (`@@ -611,5 +620,6 @@` and `@@ -699,5 +709,8 @@`) and BOTH are docstring/comment-only text, which is the stale-claim correction this plan's "Spec / documentation sync" section explicitly requires. An AST probe confirms the executable logic is untouched: parsing `git show be49ac4:agent_workflows/ipd_lifecycle.py` and the working copy, extracting `begin`'s body with its docstring node dropped, and `ast.unparse`-ing both yields `begin() statement count before/after: 41 41` / `begin() EXECUTABLE LOGIC IDENTICAL: True`. So no collision with `z2isfg`, which owns that function's behavioral change.
  - Result: pass

- [x] V-06 validates E-06
  - Required evidence: paste the residual-gap test, its name, its docstring, and its passing output, and confirm the docstring names the follow-up backlog item that must invert it. Paste the backlog item id you filed for the committed half per the Deferred section, and confirm it carries the F9 measurement. Then paste proof that the test genuinely exercises the same-identity condition (the git commands showing the co-worker commit was authored under the SAME `user.name`/`user.email` as the executor's), since a test that accidentally uses two identities would not pin the real limitation. State plainly in the terminal history that this plan fixes the working-tree half only and that the committed half remains open; a report implying the shared-checkout attribution problem is closed FAILS this item.
  - Observed evidence: RESIDUAL-GAP TEST: `CommittedHalfResidualGapTests::test_committed_half_of_a_coworker_is_STILL_refused_documented_limitation`. Its docstring opens `DOCUMENTED LIMITATION, not a feature: pins post-fix behavior that MUST be INVERTED.` and NAMES THE FOLLOW-UP on its second line: `Follow-up that must invert this test: backlog item \`a8eufb\` (scopeattrib, 'finalize still demands a scope reason for a CONCURRENT agent's COMMITTED out-of-scope path'). Do NOT delete this test to make that fix pass; invert it.` PASSING TODAY: included in `pytest tests/test_finalize_scope_ownership.py` -> `11 passed in 1.60s`; it asserts `out_of_scope_paths == ["agent_workflows/other_agents_file.py"]` (still refused), `disregarded_unowned_paths == []`, the path present in `committed_paths`, and `finalize(...).exit_code == EXIT_FINDINGS` with the plan left unmoved. Confirmed end-to-end through the real CLI too: `$ aw ipd finalize abc123 --actor ... --apply` (NO `--scope-reason`) -> `[exit 1]` / `refused: finalize needs scope reconciliation answers (plan left unmoved). ... --scope-reason agent_workflows/other_agents_file.py=<why-this-out-of-scope-edit-was-needed>` / `IPD-FINALIZE out-of-scope path needs a --scope-reason: agent_workflows/other_agents_file.py` / `(no executed/ dir: plan left unmoved)`. Per the plan's own instruction this refusal is the DECLARED BOUND, not a defect; a SUCCESS here would have meant E-02 leaked into the committed half and V-02's proof was invalid. BACKLOG ITEM FILED: `a8eufb` at `.aw/records/backlog/open/20260830-scopeattrib-01-a8eufb-finalize-committed-half-ownership.backlog.md` (`- Status: open`, `- Set: scopeattrib`, `- Kind: followup`), created with `aw backlog new --apply` rather than hand-named. It carries the F9 MEASUREMENT verbatim (the exit-1 transcript and `out_of_scope_paths: ['agent_workflows/other_agents_file.py']`), the reason authorship cannot fix it, the F5 elimination of both registry candidates, the measured blast radius (isolated lanes immune; main-tree and hand-run finalize affected), and the requirement that the characterization test be INVERTED rather than deleted while the no-weakening test stays green. SAME-IDENTITY CONDITION genuinely exercised, not assumed: the test computes `identities = set(git log --format=%an|%ae -2)` over the executor's commit and the co-worker's commit and asserts `len(identities) == 1` with the message `the limitation requires ONE shared identity; got {identities}`, so a fixture that accidentally used two identities would FAIL rather than silently pin nothing. That is what rules out an `%an`-based "fix". PLAINLY STATED, here and in the terminal history: THIS PLAN FIXES THE WORKING-TREE HALF ONLY. A concurrent agent's COMMITTED out-of-scope file still demands a `--scope-reason` from an unrelated finalizing plan, so the shared-checkout attribution problem is NOT closed; `a8eufb` tracks the open half.
  - Result: pass

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required. 6 E-leaves across 2 task groups, well under the thresholds. Right-sizing checked per leaf: E-01 is the behavior-neutral split, E-02 the filter, E-03 the visibility and the recorded trade-off, E-04 the test module, E-05 the one cross-gate invariant, E-06 the pinned residual gap. Each has its own falsifiable surface.

Open questions: ALL RESOLVED from repository evidence and direct measurement. OQ-01 accepts a named regression with its mitigation; OQ-02 keeps the intervening-commit check untouched. Nothing here requires a maintainer decision. The maintainer should be aware of ONE judgment call recorded in OQ-01 and F3, which is that this plan deliberately trades a refusal on the executor's own uncommitted out-of-scope work for the ability to finalize at all in a shared checkout; if that trade is unacceptable, the alternative is building the ownership registry ruled out in F5.

Scope fence: touch ONLY `agent_workflows/ipd_lifecycle.py` and the new `tests/test_finalize_scope_ownership.py`. Do NOT touch `tests/test_ipd_lifecycle_cli.py` or `tests/test_event_derived_lifecycle.py` (both must stay green WITHOUT edits, and both are named in Under-scope for exactly this reason), do NOT touch `ipd_lifecycle.begin` (owned by `z2isfg`), do NOT touch `dirty_tree_overlap` in either runner (owned by backlog `h1ksy6`), and do NOT add a CLI flag. If it seems to need more, STOP and report.

Honesty rule (HARD MUST): when you report tests or validation passed, paste the ACTUAL runner output with the `git rev-parse HEAD` it was measured at. Never claim a pass you did not run, and never reuse this plan's recorded baseline as if freshly measured; it was taken at `df731f1` and will be stale.

Execution contract: commit ONLY the files changed, path-scoped (`git commit -m msg -- <paths>`), never `git add -A`, and never push. Before every commit run `git diff --cached --name-only` and unstage anything not modified by this plan. Note the irony and the hazard: this is a SHARED CHECKOUT with concurrent agents, `ipd_lifecycle.py` appears in several plans' Scope-Paths, and the very condition this plan fixes (an unowned dirty file at finalize time) is likely to be present while executing it. Re-read the file immediately before editing and locate code by SYMBOL, never by the line numbers omitted from this plan on purpose.

Post-gate lifecycle: do not claim done or move this plan until every `V-*` item is verified with concrete pasted evidence and `aw ipd lint --phase pre-transition` reports conforming; the transition is performed by `aw ipd finalize`, never by hand. Do not create or push a git tag or release.
