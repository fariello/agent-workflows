# IPD: Prove the composed control on the merged result across all four children

- Date: 2026-09-19
- Kind: child
- Concern: Spec `r07vma`'s acceptance criteria are mostly properties of the COMPOSED control, not of any one child: that exactly one function decides conformance and both consumers call it (criterion 1); that a deliverable cannot be expressed as a conforming row (criterion 3); that the probe still runs behind the shape check (criterion 9); that the two refusals are distinguishable (criterion 10); that a shape refusal spends zero model calls (criterion 11); and that the suite and `aw check all` are no worse than baseline (criterion 13). No single child can observe any of those, because each holds only its own half of the change.
  THIS CHILD EXISTS BECAUSE THE ORCHESTRATOR MAY NOT HOLD THAT WORK, which is the Set's own thesis applied to the Set. The parent `d1u4sy` is retired PROGRAMMATICALLY once its children are `executed`, with the pre-transition `E-*`/`V-*` checkpoint deliberately skipped, so a verification parked on it would be marked complete having never run. That is not hypothetical: `rh5tt6` was retired 2026-09-08 with a commit message stating "Its own `E-*`/`V-*` items were NOT performed" while its E-02 read `Execution state: pending`. Spec R1b names the remedy and the in-tree precedent is `svacmz`, which carries `- Item-Dependencies:` naming all three of its siblings for exactly this reason.
- Scope: The whole-Set verification, performed and verified by an agent turn. IN: proving criteria 1, 3, 4, 9, 10, 11 and 13 on the MERGED result of all four siblings; pinning that the semantic probe's seven functions and its test file are intact; and pinning that no second implementation of the conformance rule exists anywhere. OUT: any product change whatsoever, which is what keeps this child a verification rather than a fifth feature; and re-proving what a sibling already validated in isolation (criteria 2, 5, 6, 7, 8, 12 belong to children 01 through 04 and are CITED here, not re-run).
- Scope-Paths: tests/test_orchestrator_shape_composed.py
- Item-Dependencies: executed:dpdyed, executed:r3xk1f, executed:0xmk4e, executed:68uhp0
- Status: approved
- Readiness: go-pending-approval
- Set: orchtyped
- Order: 5
- Highest E allocated: 05
- Author: opencode/its_direct/pt3-claude-opus-5-1m-us
- Id: h9cbn4
- Approval: 2026-09-19, recorded via aw ipd set: status set to approved
- From-Spec: r07vma
- Blocks-Release: next
- Work-Kind: bug
- Priority: high

## Workflow history
- 2026-09-19 approved (aw set): status set to approved

- 2026-09-19 reviewed (aw set): /plan-review round 1: APPROVE WITH REVISIONS APPLIED; PR-501..PR-505 plus two lower findings all FIXED, none deferred, none open. Readiness go-pending-approval. Record: .aw/records/reviews/20260919-orchtyped-05-h9cbn4-prove-the-composed-control-on-the-merged-result-across-all-f.review.md. SELF-REVIEW (same agent/model authored it), so its value rests on EXECUTING the claims: I resolved all seven probe functions and summed their AST spans, ran the probe suites, ran the BARE suite, computed `aw check all` twice, diffed the probe test file against the Set's own authoring commit, and read `evaluate_set_retirement`, `check_checkpoint` and `_VALIDATION_RULES` in source. ONE BLOCKER. PR-501: the requirement that `tests/test_orchestrator_probe.py` be BYTE-UNCHANGED across the Set ALREADY FAILS AT HEAD, and not through any fault of this Set - the maintainer's commit `75b90271` rewrote that file at 16:26 on 2026-09-19, two hours AFTER the Set was authored at 14:22 (`a4808752`), taking it from 1265 to 3006 lines with 2270 insertions and 529 deletions. An executor hitting an unsatisfiable pin has two exits and the likelier one under pressure is to REVERT a co-worker's refactor so its own validation passes, which the shared-checkout rule forbids outright and which would be committed by the very child whose purpose is honest verification. The spec's own wording asks for the probe suite passing UNEDITED, and the honest reading is unedited BY THIS SET, so the pin is now three-part: absent from the SET'S OWN diff (union of the five children's commits, not a range over main), seven functions resolving with their AST span re-derived (I measured exactly 476, so the spec's figure is still precise), and the suite passing (96 passed across the probe and cache suites, with `TheExcerptHasAKnownLIMIT` surviving at line 1228). TWO HIGHS. PR-503: criteria 10 and 11 are ALREADY asserted by child 03's E-04/E-05, so this plan was silently re-running a sibling's work while its own deferral section forbids exactly that for six other criteria; what child 03 genuinely cannot observe is the POST-MIGRATION inversion, a CONFORMING queue passing the shape check and REACHING the probe, so E-04 now requires a MIXED queue and fails on single-non-conformer evidence. PR-502: the criterion-1 "no second child-table scan" grep fires on TWO legitimate pre-existing scanners (`ipd_set_plan.parse_child_table` at `:344` and `runner_shared.child_table_rows` at `:7008`, which child 01 composes rather than replaces), plus three known-good `RECOGNIZED_STATUS` references, so the search must enumerate expected hits BEFORE running or it reads a shipped design as a violation. Also PR-505 (the three real items WILL be migrated by child 04, not "may", so a fixture reading the live files would assert that a CONFORMING row is refused, inverting criterion 3; now literal fixtures with a cited revision), PR-504 (`aw check all` moved 243 -> 270 in twenty minutes during this review from unrelated merges, so the baseline must be taken in the executing worktree and never quoted) and F-10 (the suite pass total moved 7305 -> 7080 across this Set's lifetime purely from the test tabulation, independent evidence for the node-id rule). VERIFIED AND UNCHANGED: F-1's central claim (`evaluate_set_retirement` references none of the seven checklist tokens), PR-005's probe-payload measurement (a bare continuation line IS in the payload, a `- Context:` line is NOT), all three real item texts verbatim, `d1u4sy`'s five conforming rows, and `svacmz`'s three sibling dependencies. Nothing was relaxed: the byte pin was replaced by three checks that are harder to fake and that cannot be satisfied by reverting someone else's work, and E-04 is materially harder than as authored. Validation: `aw ipd lint --phase author` and `--phase review-finalize` both conform, watermark unchanged at 05; bare suite `1 failed, 7080 passed, 3 skipped, 2 xfailed` with the single failure proven pre-existing by running the node alone; `aw check all` reports 0 findings against this plan.

- 2026-09-19 to-review (opencode/its_direct/pt3-claude-opus-5-1m-us): Authored from approved spec `r07vma` as Order 05 of Set `orchtyped`, and as the direct application of R1b to this Set: the whole-Set verification lives here, with `- Item-Dependencies:` naming all four siblings, rather than on the parent where it would be retired unperformed. Follows the `svacmz` precedent the spec cites, which another agent authored independently for the same reason. Adds tests and evidence only; a product change in this child's diff is a scope violation.

## Goal

Prove that the four children compose into the control spec `r07vma` specifies: one rule with two consumers, a deliverable that cannot be written into a row, a probe still standing behind the shape check, two refusals an operator can tell apart, and a suite no worse than before.

READ THE SCOPE PRECISELY: this child adds NO product code. Its whole value is that the composed properties are checked by an agent turn against the merged tree, which is the one thing the runner's programmatic retirement of the parent cannot do.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: the composed properties

- [x] E-01 PROVE ONE RULE, TWO CONSUMERS (criterion 1) ON THE MERGED TREE. Show the single conformance function, show both the review-side and run-side call sites reaching it, and prove that no second implementation exists: no second row pattern, no second status list, no second child-table scan in either consumer. This is the criterion most likely to have been violated quietly, because each of children 02 and 03 could have satisfied its own tests with a local copy.
  A NAIVE "NO SECOND CHILD-TABLE SCAN" GREP FIRES ON TWO PRE-EXISTING SCANNERS AND MUST NOT BE READ AS A VIOLATION (added at review, PR-502). Measured at review, `agent_workflows/` already contains TWO independent readers of the `## Child IPDs, sequence, and dependencies` heading, both predating this Set and both legitimate: `ipd_set_plan.parse_child_table` (`:344`, which returns `{order: (dep_orders,)}` for the ORDER GRAPH) and `runner_shared.child_table_rows` (`:7008`, which returns FULL CELL TUPLES and is deliberately shared by the probe cache and `parse_declared_child_orders`, whose docstring records exactly why). Child 01's E-02 is required to COMPOSE those two rather than add a third. So the claim to prove is NOT "only one function mentions the heading", which is false today and would read as a failure; it is that THE CONFORMANCE RULE has one definition and that NEITHER CONSUMER re-derives it. STATE THE SEARCH AND ITS EXPECTED HITS BEFORE RUNNING IT, and account for every hit as either the one rule, one of those two pre-existing scanners, or a violation. Likewise `RECOGNIZED_STATUS` legitimately appears in `ipd_schema` (its definition, `:262`), in `completion.py` (`:537`) and in `ipd_schema`'s own validator (`:355`), so a bare grep for a "second status list" has known-good hits too. A grep whose expected hits are not enumerated in advance proves nothing, which is the failure mode V-01 already warns about in general terms and which this note makes concrete.
  - Depends on: none
  - Expected outcome: the function named, both call sites pasted, and a search whose EXPECTED hits were enumerated in advance, with every actual hit accounted for as the one rule, a named pre-existing scanner (`parse_child_table`, `child_table_rows`), or a violation.
  - Execution state: performed

- [x] E-02 PROVE A DELIVERABLE CANNOT BE EXPRESSED AS A CONFORMING ROW (criterion 3), using the three REAL parent-only items the spec names rather than invented fixtures: `5e4sb6` E-01 ("Produce the function-by-function INVENTORY"), `wfjsp4` E-02 ("VERIFY THE BROWSE AFFORDANCE ACTUALLY ARRIVED"), and `tb63qv` E-01 ("After both children are executed, verify the Set's combined outcome"). All three must be REFUSED.
  `wfjsp4` E-02 IS THE LOAD-BEARING CASE AND MUST BE INCLUDED. It opens with an allowlisted verb ("VERIFY") and names no child, so it would have PASSED the vocabulary rule the spec's draft proposed. It is the evidence that the by-construction claim is stronger than the detection approach it replaced, and a validation without it proves the weaker thing.
  ALL THREE TEXTS WILL BE GONE FROM THE WORKING TREE BY THE TIME THIS RUNS, SO READ THEM FROM GIT AND PIN THE REVISION (sharpened at review, PR-505). Child 04 rewrites exactly these plans, so the items this criterion is about are pre-migration text, and a fixture built from the post-migration file would assert that a CONFORMING row is refused, which is the opposite of the claim. I verified all three are present and quotable at HEAD `c58ec3ab`: `5e4sb6` E-01 opens "Produce the function-by-function INVENTORY the backlog item names as its first r...", `wfjsp4` E-02 opens "VERIFY THE BROWSE AFFORDANCE ACTUALLY ARRIVED, which is the entire point of the...", and `tb63qv` E-01 opens "After both children are `executed`, verify the Set's combined outcome and record...". COPY THE THREE ITEM TEXTS INTO THIS CHILD'S OWN TEST FILE AS LITERAL FIXTURES rather than reading the plan files at test time, and record the revision they came from. The reason is that a test which reads a live plan file becomes a test of child 04's migration instead of a test of the grammar, and it would then break every time anyone edits those plans; a frozen literal with a cited provenance is stable and is what criterion 3 actually needs.
  - Depends on: E-01
  - Expected outcome: all three real items refused; `wfjsp4` E-02 explicitly among them with its verb noted; the three texts embedded as literal fixtures with the git revision they were taken from named, not read from the live plan files.
  - Execution state: performed

- [x] E-03 PROVE THE PROBE SURVIVED AND STILL BLOCKS (criterion 9). Show an orchestrator whose ROWS all conform but which carries an obligation in a BARE INDENTED continuation line is STILL refused, by the probe. PROVE THIS WITH A BARE INDENTED CONTINUATION LINE AND NOT WITH `## Completion criteria` (corrected at review, PR-005): that SECTION IS NOT IN THE PROBE'S PAYLOAD AT ALL. Verified in-process at review by calling `runner_shared.orchestrator_probe_excerpt` on a fixture whose `## Completion criteria` said "SOMEONE MUST MIGRATE THE DATABASE BEFORE ANY CHILD RUNS": the rendered excerpt contains only the checklist item action text and the child-table row cells, and the string is absent. `probe_cache_payload` returns exactly the two keys `e_items` and `child_table_rows`, so no prose section outside a checklist item ever reaches the model (pre-existing limit, tracked as backlog `rmcqw8`, pinned by `tests/test_orchestrator_probe.py::TheExcerptHasAKnownLIMIT`). A `- Key: value` continuation line is ALSO invisible, because `e_item_action_blocks` stops at the first line matching `ipd_lint._SUBFIELD_RE`. So the ONLY shape that demonstrates criterion 9 is a BARE indented continuation line under a conforming row; anything else reports a pass the mechanism did not earn, and this item FAILS if the evidence uses one. Then pin the probe's integrity on the merged tree: its seven functions present (`enforce_orchestrator_probe_gate`, `ask_orchestrator_probe`, `orchestrator_probe_excerpt`, `queued_orchestrator_targets`, `classify_probe_reply`, `probe_cache_payload`, `probe_refusal_remedy`) and `tests/test_orchestrator_probe.py` byte-unchanged across the whole Set's diff, not merely across one child's.
  THIS IS THE PIN FOR THE SPEC'S OWN BLOCKER FINDING. `r07vma` SR-001 records that the spec's draft proposed retiring this probe, that an approved spec (`25kzda` 2.5b) forbids substituting a syntactic rule for it, and that the substitution would have deleted 476 lines of code and 1265 lines of tests. Verifying the probe intact at the END of the Set is what makes that correction stick.
  DO NOT PIN BYTE-IDENTITY OF `tests/test_orchestrator_probe.py`, BECAUSE THAT PIN ALREADY FAILS AT HEAD AND WOULD FAIL THIS CHILD FOR SOMEBODY ELSE'S LEGITIMATE WORK (corrected at review, PR-501). Measured at HEAD `c58ec3ab`: the maintainer's commit `75b90271` ("test: tabulate eleven more suites") rewrote that file on 2026-09-19 at 16:26, AFTER this Set was authored (`a4808752`, 14:22), and `git diff --stat a4808752 HEAD -- tests/test_orchestrator_probe.py` reports 2270 insertions and 529 deletions, taking the file from 1265 lines to 3006. So a diff against ANY plausible Set base already shows the file changed, before a single child has run. The byte-identity pin is therefore unsatisfiable through no fault of this Set, and an executor facing it would either report a false failure or (far worse) revert a co-worker's refactor to make its own validation pass, which the shared-checkout rule forbids outright. THE SPEC'S OWN WORDING DOES NOT DEMAND BYTES: criterion 9 asks for `tests/test_orchestrator_probe.py` passing UNEDITED, and the honest reading is UNEDITED BY THIS SET, not unchanged in absolute terms. So pin it three ways, each of which survives a third party's refactor while still catching the substitution SR-001 exists to prevent: (a) the file is absent from the SET'S OWN diff, computed as the union of the five children's commits rather than as a range over main (a range silently includes every other party's work, which is the defect being fixed here); (b) the seven functions resolve on the merged tree, which I verified at review (all seven `hasattr` True, AST spans summing to exactly 476, so the spec's figure is still exact); and (c) the suite passes, which I also verified at review (`tests/test_orchestrator_probe.py tests/test_orchestrator_probe_cache.py` -> 96 passed), INCLUDING `TheExcerptHasAKnownLIMIT`, which survived the refactor at line 1228 and is the class criterion 9's prose limit rests on. IF THE FILE IS IN THE SET'S OWN DIFF, that is the real violation and this item must FAIL; a change made by anyone else is not this Set's to police, and reverting it is never the remedy.
  - Depends on: E-01
  - Expected outcome: the prose-only obligation still refused by the probe; the seven functions present with their 476-line span re-derived; the probe's test suite PASSING and absent from the SET'S OWN diff (not byte-compared against main, which already differs through a third party's refactor).
  - Execution state: performed

- [x] E-04 PROVE THE TWO REFUSALS ARE DISTINGUISHABLE AND THE ORDERING HOLDS (criteria 10 and 11). Paste the shape refusal and the probe refusal side by side, showing DIFFERENT rule ids, so an operator knows which control fired and therefore which remedy applies. Then show a run the shape check refused made ZERO probe invocations, asserted through the probe's injected spawn seam rather than inferred from the code path.
  CHILD 03 ALREADY ASSERTS BOTH OF THESE, SO SAY WHAT THIS ITEM ADDS OR IT IS THE DUPLICATION THIS PLAN'S OWN DEFERRAL FORBIDS (added at review, PR-503). Measured at review: `0xmk4e` E-04 covers criteria 9 AND 10 (its expected outcome reads "the two rule ids differ") and its E-05 covers criterion 11 with the non-vacuous injected-counter method PR-303 prescribed. This plan's Scope says criteria 2, 5, 6, 7, 8 and 12 are CITED rather than re-run, but it silently re-runs 10 and 11, which are equally a sibling's validated work. WHAT IS GENUINELY DIFFERENT AND IS THEREFORE WHAT THIS ITEM MUST DEMONSTRATE: child 03 proves both properties in ITS OWN worktree, where child 04's migration has not happened and every live orchestrator is non-conforming. On the MERGED tree the corpus is migrated, so the interesting case inverts: a queue of CONFORMING orchestrators must pass the shape check and REACH the probe, which is the ordering's other half and which child 03 cannot observe at all. So prove the pair on a MIXED queue: at least one non-conforming orchestrator (shape refusal, zero probe invocations) AND at least one conforming one (shape check passes, probe IS reached and its injected double IS called). The second half is the addition; a re-run of child 03's single-non-conformer fixture is not, and this item FAILS if the evidence shows only that.
  - Depends on: E-03
  - Expected outcome: on a MIXED queue, the non-conforming orchestrator draws a shape refusal with zero probe invocations while the conforming one passes the shape check and REACHES the probe (its injected double called), with the two rule ids pasted side by side; and a statement of what this adds over child 03's same-criteria assertions.
  - Execution state: performed

### Task group 2: the Set's own bar

- [x] E-05 ESTABLISH THE SUITE AND `aw check all` BASELINE-OR-BETTER ON THE MERGED TREE (criterion 13), and state both counts. Run the suite BARE. For `aw check all`, report the per-rule delta rather than a clean result: the repository carries pre-existing findings unrelated to this Set, so an absolute-green bar would be unsatisfiable and would invite editing the number instead of the code.
  TAKE THE `aw check all` BASELINE IN YOUR OWN WORKTREE AT THE MOMENT YOU START, AND NEVER FROM A DOCUMENT, BECAUSE THE TOTAL MOVES FAST (added at review, PR-504). Measured during this review: 243 findings at `c138a219` and 270 at `c58ec3ab`, a 27-finding move in about twenty minutes from another party's merges, with no change from this Set at all. So a plan-recorded absolute total is worthless within the hour, and the PER-RULE delta is the only durable comparison. The rule distribution at review, offered as orientation and NOT as a fixture to reproduce: `check.scope-drift` 169, `check.ipd-uncarried-obligation` 88, then single-digit counts for `check.lifecycle-transition-invalid`, `check.name-nonconformant`, `check.from-backlog-gate-mismatch`, `check.live-bug-ungated`, `check.id6-collision`, `check.from-backlog-dangling` and `check.system-layout-missing`. NOTE THAT CHILD 04 REWRITES TWELVE TRACKED PLANS, so `check.scope-drift` and `check.ipd-uncarried-obligation` are the two rules whose counts could legitimately move on the merged tree; a move in either is a fact to EXPLAIN, not automatically a regression, and a move in an unrelated rule is the signal worth chasing.
  THE SUITE BASELINE HAS A KNOWN FAILURE AND IT IS NOT THIS SET'S (added at review): measured bare at HEAD `c58ec3ab`, `1 failed, 7080 passed, 3 skipped, 2 xfailed`, the failure being `tests/test_plan_readiness.py::ApprovalGateRealCorpusTests::test_no_pending_plan_is_refused_on_a_verdict_today` naming three `reaskscore` plans (`s0gnha`, and two siblings) another party is editing. Note the pass TOTAL also moved (7305 at child 03's review to 7080 here) purely through the maintainer's test-tabulation refactor `75b90271`, which is exactly why the comparison is by failing NODE ID and never by total.
  ALSO CONFIRM CRITERION 4 BY CITATION: a conforming orchestrator and a cross-child final child coexist. The proof is this Set itself, whose parent `d1u4sy` carries five conforming rows while this child carries the cross-child verification with four sibling dependencies. I verified both halves at review: `d1u4sy`'s five rows read exactly `E-01 CONFIRM dpdyed REACHED executed` through `E-05 CONFIRM h9cbn4 REACHED executed`, and `svacmz` carries `- Item-Dependencies: executed:skn8uk, executed:ty7w6o, executed:dy9ymn`. Cite `svacmz` as the independent precedent rather than inventing a fixture.
  - Depends on: E-02, E-04
  - Expected outcome: suite counts before and after with failing node ids compared (never totals) and the known `reaskscore` failure identified by name if present; a PER-RULE `aw check all` delta against a baseline taken in the executing worktree, with any move in `check.scope-drift` or `check.ipd-uncarried-obligation` explained against child 04's twelve rewritten plans; criterion 4 satisfied by this Set's own shape with the precedent cited.
  - Execution state: performed

## Project conventions discovered (Step 0)

- THE PARENT IS RETIRED PROGRAMMATICALLY with the E/V checkpoint SKIPPED, which is why this child exists. `runner_shared.evaluate_set_retirement` decides on four facts (an orchestrator exists, at least one child, every child `executed`, no unresolved child-table row) and contains zero references to `E-`, `V-`, `items` or `checklist`.
- `svacmz` IS THE IN-TREE PRECEDENT for this shape: a final child carrying `- Item-Dependencies: executed:skn8uk, executed:ty7w6o, executed:dy9ymn`, whose child-table row says it owns the verification "so it is performed and verified by an agent turn instead of being retired unperformed".
- THE PROBE'S SPAWN SEAM IS INJECTABLE BY DESIGN: `ask_orchestrator_probe`'s docstring records that every test injects it and that the real spawn is unreachable by construction, because "a test that spends tokens is not a test". E-04 uses that.
- `aw check all` CARRIES PRE-EXISTING FINDINGS unrelated to this Set, so criterion 13 is a per-rule DELTA and never an absolute green. Capture the tally before the first edit, IN YOUR OWN WORKTREE, because the total moves fast: measured 243 at `c138a219` and 270 at `c58ec3ab`, twenty minutes apart, entirely from other parties' merges. `check.scope-drift` (169) and `check.ipd-uncarried-obligation` (88) dominate, and they are also the two rules child 04's twelve rewritten plans could legitimately move.
- SUITE BARE: `python3 -m pytest`. `addopts` already supplies `-q -n auto --dist=worksteal -m 'not slow'`. Do not add `-n0`, a second `-q`, or `-p no:randomly`. Compare failing NODE IDS, not totals. THE TOTAL IS PROVABLY UNSTABLE ACROSS THIS SET'S OWN LIFETIME: 7305 passed at child 03's review, 7080 at this one, the difference being the maintainer's test-tabulation commit `75b90271` and no behavior change at all.
- THE SUITE'S ONE KNOWN FAILURE IS NOT THIS SET'S: `tests/test_plan_readiness.py::ApprovalGateRealCorpusTests::test_no_pending_plan_is_refused_on_a_verdict_today`, naming three `reaskscore` plans another party is editing. Confirmed at this review by running the node alone.
- `tests/test_orchestrator_probe.py` IS NOT BYTE-STABLE AND MUST NOT BE PINNED AS SUCH. The maintainer rewrote it in `75b90271` (2270 insertions, 529 deletions, 1265 -> 3006 lines) two hours AFTER this Set was authored, so any byte comparison against main already fails. The probe's INTEGRITY is provable three other ways: the seven functions resolve with an AST span summing to exactly 476 (the spec's own figure, re-derived at review), the suite passes (96 passed across it and the cache suite), and `TheExcerptHasAKnownLIMIT` survives at line 1228.
- TWO LEGITIMATE CHILD-TABLE SCANNERS ALREADY EXIST, both predating this Set: `ipd_set_plan.parse_child_table` (`:344`, order graph) and `runner_shared.child_table_rows` (`:7008`, full cell tuples, deliberately shared by two consumers). Child 01 COMPOSES them. A criterion-1 grep must enumerate these as expected hits or it will read a pre-existing design as a violation.
- THIS CHILD MUST NOT CHANGE PRODUCT CODE. Its only declared path is its own test file; a product edit here would make the verification self-fulfilling. NOR MAY IT REVERT ANOTHER PARTY'S WORK to make a pin pass, which is the specific temptation the byte-identity pin created and which the shared-checkout rule forbids.

## Findings

| Id | Severity | Location (symbol / content anchor) | Finding | Evidence |
| --- | --- | --- | --- | --- |
| F-1 | HIGH | `runner_shared.evaluate_set_retirement` | The parent's items are performed by nobody, so a whole-Set verification parked there is marked complete unperformed. This child is R1b's remedy applied to this Set. | zero references to `E-`/`V-`/`items`/`checklist` in that function; `rh5tt6` retired 2026-09-08 with its E-02 pending |
| F-2 | HIGH | criteria 1, 3, 9, 10, 11, 13 | Each is a property of the MERGED result and is unobservable from inside any one child, so without this child they would be claimed rather than verified. | the criteria's own wording |
| F-3 | HIGH | `wfjsp4` E-02 | It opens with "VERIFY" and names no child, so it would have PASSED the draft's vocabulary rule while being a genuine deliverable. It is the decisive case for the by-construction claim and E-02 must include it. | the item's text; the draft's allowlist measured at 11 flags of 32 ROWS AS THE SPEC DATED IT, a figure that has since moved to 38 across 12 orchestrators (re-measured at review); the 11-of-32 ratio is quoted here only as the spec's own historical measurement and must not be re-derived as current |
| F-4 | HIGH | `25kzda` 2.5b; `r07vma` SR-001 | The probe must be intact at the Set's end, because an approved spec forbids substituting a syntactic rule and the spec's own draft proposed doing so. Verifying it here is what makes the correction durable. **BUT NOT BY BYTE-IDENTITY OF THE TEST FILE (see F-7):** the seven functions plus their 476-line AST span, the passing suite, and the file's absence from the SET'S diff are the pins that survive a third party's refactor. | both spec texts; the 476-line span re-derived by AST at review (exact); `TheExcerptHasAKnownLIMIT` present at `tests/test_orchestrator_probe.py:1228` |
| F-5 | MEDIUM | children 02 and 03 | Each could satisfy its own tests with a local copy of the rule, so criterion 1 is the one most likely to be quietly violated and needs a search across the merged tree rather than per-child trust. **REFINED AT REVIEW (PR-502):** the search must enumerate its expected hits first, because two legitimate child-table scanners and three legitimate `RECOGNIZED_STATUS` references already exist. | R3's requirement read against the two consumers' independence; both scanners located at review |
| F-6 | LOW | `aw check all` | Pre-existing unrelated findings make an absolute-green bar unsatisfiable, so criterion 13 must be a per-rule delta. **QUANTIFIED AT REVIEW (PR-504):** the total moved 243 -> 270 in twenty minutes from unrelated merges, so a plan-recorded total is stale within the hour and the baseline must be taken in the executing worktree. | both tallies measured during this review at `c138a219` and `c58ec3ab` |
| F-7 | BLOCKER | `tests/test_orchestrator_probe.py`; commit `75b90271` | **Added at review (PR-501).** The byte-unchanged pin ALREADY FAILS AT HEAD, before any child runs: the maintainer rewrote the file on 2026-09-19 at 16:26 (2270 insertions, 529 deletions, 1265 -> 3006 lines), two hours AFTER this Set was authored at 14:22. So the item is unsatisfiable through no fault of this Set, and the two ways out are both bad: report a false failure, or REVERT a co-worker's refactor to make this child's own validation pass, which the shared-checkout rule forbids and which is the more likely choice under pressure to finish the Set. | `git diff --stat a4808752 HEAD -- tests/test_orchestrator_probe.py`; `git log -1 --format='%ci' 75b90271` (16:26) vs `a4808752` (14:22) |
| F-8 | HIGH | `0xmk4e` E-04 and E-05 | **Added at review (PR-503).** Criteria 10 and 11 are ALREADY asserted by child 03, so re-running them here is the duplication this plan's own deferral section forbids for criteria 2, 5, 6, 7, 8 and 12. What child 03 cannot observe is the post-migration case: a CONFORMING queue passing the shape check and REACHING the probe. That inversion is this item's actual contribution and was unstated. | `0xmk4e` E-04's expected outcome ("the two rule ids differ") and E-05's injected-counter method, both read at review |
| F-9 | MEDIUM | the three real items in `5e4sb6`, `wfjsp4`, `tb63qv` | **Added at review (PR-505).** The plan said these "may have been MIGRATED by child 04". They WILL have been: child 04 rewrites exactly these plans. A fixture reading the live files post-migration would assert that a CONFORMING row is refused, inverting criterion 3; and a test that reads a live plan file is a test of child 04's migration rather than of the grammar. | all three texts quoted verbatim from HEAD at review; child 04's Scope naming the pending-plans tree |
| F-10 | LOW | the suite total | **Added at review.** The pass total moved 7305 -> 7080 across this Set's own lifetime purely from `75b90271`'s test tabulation, with no behavior change, which is independent evidence for the plan's existing node-id rule and worth recording so an executor does not read the drop as breakage. | both totals, the second measured bare at this review |

## Proposed changes (ordered, validatable)

1. E-01 proves one rule and two consumers, with a search whose expected hits (including the two pre-existing child-table scanners) are enumerated in advance.
2. E-02 proves the three real deliverables are refused, `wfjsp4` E-02 included, from literal fixtures carrying a cited pre-migration revision.
3. E-03 proves the probe still blocks the prose case and is intact by the three pins that survive a third party's refactor: absent from the SET'S diff, seven functions at a 476-line span, suite passing.
4. E-04 proves the two refusals differ by rule id and that a shape refusal spends nothing, ON A MIXED QUEUE whose conforming half reaches the probe, which is what child 03 cannot observe.
5. E-05 establishes the suite and per-rule `aw check all` bars from a baseline taken in the executing worktree, and cites criterion 4 from this Set's own shape.

## Deferred / out of scope (with reason)

- ANY PRODUCT CHANGE: strictly out of scope. This child adds tests and evidence only; a product edit would make its own verification self-fulfilling. If a composed property FAILS, the remedy is a finding and a corrective plan, not a fix here.
  - Carrier-Declined: A deliberate boundary that defines this child's value; there is nothing to hand off.
- ANY REVERT OR ADJUSTMENT OF ANOTHER PARTY'S WORK TO MAKE A PIN PASS: forbidden, and named explicitly because the byte-identity pin this review removed created exactly that temptation (PR-501). `tests/test_orchestrator_probe.py` was rewritten by the maintainer in `75b90271` after this Set was authored; the correct response to a third-party change that breaks a pin is to fix the PIN (which this review did) or to report it, never to undo the change. A revert here would be a shared-checkout violation committed by the child whose entire purpose is honest verification.
  - Carrier-Declined: An absolute boundary from the repository's own contract, not deferred work.
- RE-PROVING WHAT A SIBLING ALREADY VALIDATED IN ISOLATION: criteria 2, 5, 6, 7, 8 and 12 belong to children 01 through 04 and are CITED here rather than re-run. Re-running them would duplicate a child's own assertion, which is the redundancy spec R1b's measurement found in three real parent rows. THE SAME RULE NOW BINDS CRITERIA 10 AND 11, which this plan was silently re-running (PR-503): child 03's E-04 and E-05 already assert both, so what E-04 here proves is the POST-MIGRATION inversion (a conforming queue passing the shape check and reaching the probe) that no sibling can observe. The boundary is the same boundary; it was simply applied inconsistently.
  - Carrier-Declined: Owned by named siblings in this Set; citing is the correct treatment.
- TYPING THE ORCHESTRATOR'S PROSE SECTIONS so the probe becomes unnecessary: out of this Set. Spec Section 3a limit 1 records the residue as an honest limit rather than an omission.
  - Carrier: rmcqw8
- MEASURING HOW MUCH OF THE HISTORICAL VIOLATION POPULATION LIVED IN ROWS VERSUS PROSE: spec Section 3a limit 2 asks for this ratio, and E-03's prose-case fixture touches it without quantifying it across the corpus. A proper measurement needs the pre-migration census child 04 takes, so it belongs with that data. CARRIED BY THE SPEC RATHER THAN BY `68uhp0` (repointed at review, PR-001): a sibling that reaches `executed` in this same Set is a dead carrier the moment the Set completes, and this obligation is meant to OUTLIVE the Set. Spec Section 3a limit 2 is where the unanswered ratio is recorded.
  - Carrier-Evidence: .aw/records/specs/20260919-r07vma-01-r07vma-orchestrator-conformance-parser-and-repair-loop.spec.md

## Scope check

- Over-scope: none. One declared path, its own test file, touched by every item.
- Under-scope: deliberately narrow. If proving a composed property requires a test helper that cannot live in this child's own file, that path must be DECLARED before editing; and if it requires a PRODUCT change, that is a finding to report rather than a scope amendment, because this child's separation from product code is what makes its verification meaningful. ONE PATH THIS CHILD MIGHT BE TEMPTED TO TOUCH AND MUST NOT: `tests/test_orchestrator_probe.py` is NOT declared and never becomes declarable here, because criterion 9 pins it as unedited BY THIS SET and an edit from this child would be the violation it exists to detect (PR-501). If a pin against that file cannot be satisfied, fix the pin's definition or report it; do not edit or revert the file.

## Required tests / validation

`python3 -m pytest` BARE on the merged tree, with the baseline measured in the executing worktree and compared by failing NODE ID rather than by total, since concurrent work moves the total.

THE BASELINE'S KNOWN STATE AT REVIEW, so an executor can tell breakage from inheritance: bare at HEAD `c58ec3ab`, `1 failed, 7080 passed, 3 skipped, 2 xfailed`, the failure being `tests/test_plan_readiness.py::ApprovalGateRealCorpusTests::test_no_pending_plan_is_refused_on_a_verdict_today` naming three `reaskscore` plans another party is editing. The pass TOTAL is not comparable across time (7305 at child 03's review, 7080 here, the difference being commit `75b90271`'s test tabulation and no behavior change), which is why the rule is failing node ids.

Beyond the suite: the search proving no second rule implementation, WITH its expected hits enumerated in advance and the two pre-existing child-table scanners accounted for; the three real deliverables refused from literal pre-migration fixtures with their revision cited; the prose-only case still refused by the probe via a BARE indented continuation line; the probe intact by the three pins that survive a third party's refactor (absent from the SET'S OWN diff, seven functions at a re-derived 476-line AST span, suite passing) and NOT by a byte comparison against main, which already fails at HEAD; both refusals side by side with distinct rule ids; zero probe invocations on a shape refusal via the injected seam AND a nonzero count on a conforming one; and the per-rule `aw check all` delta against a baseline taken in the executing worktree.

## Spec / documentation sync

N/A with reason: this child verifies spec `r07vma` rather than changing it, and `25kzda` 2.5b is verified intact rather than amended. No `.spec.md` path is declared. If a composed property proves unachievable as specified, that is a finding against `r07vma` to report to the maintainer, not an edit to make from inside its own implementing Set.

## Open questions

### OQ-01: If a composed property fails here, should this child report and stop, or should the Set be held open until a corrective child lands?

- Blocking: no
- Status: open
- Owner: maintainer
- Resolution or deferral rationale: NOT blocking, because the honest behaviour on failure is already determined by the repository's rules: a `V-*` item whose evidence does not hold is recorded `failed` or `blocked`, the plan is not finalized, and `aw ipd finalize` refuses at the pre-transition checkpoint. So this child stops by construction rather than by choice. The question is what happens to the SET: the parent cannot retire while this child is unexecuted (retirement requires every child `executed`), so a failure here correctly holds the whole Set open, which is the desired outcome and needs no mechanism. PROPOSED DIRECTION: report the failing property as a finding, leave this child unfinalized, and let the maintainer decide between a corrective child and a scope reduction. Do NOT weaken a criterion to make this child pass; that would be the fabricated-completion failure the whole Set exists to prevent, committed by the very child meant to catch it.
  EVERY MECHANICAL CLAIM ABOVE WAS VERIFIED FROM SOURCE AT REVIEW, so this is a settled description rather than a hopeful one. `ipd_schema.VALIDATION_RESULTS` is exactly `{pending, pass, blocked, failed}` and `_VALIDATION_RULES` requires nonempty `Observed evidence` for `failed` and `blocked`, so a failing property is RECORDABLE with its evidence rather than forcing a choice between lying and leaving a blank. `ipd_lint.check_checkpoint` refuses at `pre-transition` unless every `V-*` reads `pass`, so finalize cannot proceed on a recorded failure. And `runner_shared.evaluate_set_retirement` contains zero references to `E-`, `V-`, `items`, `checklist`, `execution state`, `lint` or `checkpoint` (all seven confirmed absent), which is both why the parent cannot inspect this child's result and why retirement's "every child executed" condition is the thing that holds the Set open. NOTE THE ONE HONEST GAP, stated because the rest is airtight: nothing PREVENTS an executor from weakening a criterion and passing, since the prohibition above is a rule and not a mechanism. That is the same self-assessment limit spec Section 3a limit 3 already records for the review loop, and it is why this child's `V-*` items demand pasted evidence a reader can check rather than a claim.
- Carrier-Declined: NO OBLIGATION OUTLIVES THIS PLAN (added at review, PR-002). The failure behaviour is already determined by shipped rules rather than by a decision this plan owes: a `V-*` whose evidence does not hold is recorded `failed`, `aw ipd finalize` refuses at the pre-transition checkpoint, and `evaluate_set_retirement` cannot retire the parent while this child is unexecuted. So the Set is held open BY CONSTRUCTION. If a composed property does fail, the remedy is a corrective plan authored at that point, which is a new artifact rather than a deferred obligation.


## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [x] V-01 validates E-01
  - Required evidence: name the one conformance function and paste BOTH call sites (review-side and run-side) reaching it. Paste the search output bearing on a second row pattern, a second status list and a second child-table scan in `agent_workflows/`. State what you searched for BEFORE the output and enumerate the hits you EXPECT, since a grep that looked for the wrong thing proves nothing and an unaccounted hit is indistinguishable from a violation. ACCOUNT FOR THE KNOWN-GOOD HITS BY NAME (PR-502): `ipd_set_plan.parse_child_table` and `runner_shared.child_table_rows` are two legitimate pre-existing child-table readers that child 01 composes rather than replaces, and `RECOGNIZED_STATUS` legitimately appears at its definition and in `completion.py`. Concluding a violation from either, or declaring victory without accounting for them, both FAIL this item.
  - Observed evidence: Single conformance function is `agent_workflows.ipd_lint.orchestrator_row_conformance`. Review-side call site in `agent_workflows/ipd_lint.py:1754` (`check_orchestrator_rows`, called from `lint_text` during `review-finalize` and `pre-transition`): `res = orchestrator_row_conformance(text, doc=doc)`. Run-side call site in `agent_workflows/runner_shared.py:15783` (`enforce_orchestrator_shape_gate`, called from `initialize_run_core` ahead of probe and run_dir creation): `res = _lint.orchestrator_row_conformance(target.text)`. Search for second row pattern, second status list, second child-table scanner across `agent_workflows/*.py`: expected hits enumerated in advance are `ipd_lint._ORCH_ROW_RE` (`:222`), `ipd_set_plan.parse_child_table` (`:344`), `runner_shared.child_table_rows` (`:7008`), and `ipd_schema.RECOGNIZED_STATUS` (`:262`, `:355`, `completion.py:537`, `ipd_lint.py:39`). Actual AST inspection finds exactly 1 regex compile hit for `CONFIRM ... REACHED` (`ipd_lint.py:222`), and 0 second implementations. Verified in `tests/test_orchestrator_shape_composed.py::TestOneRuleTwoConsumers` (all 4 tests passing).
  - Result: pass

- [x] V-02 validates E-02
  - Required evidence: paste each of the three real items and the refusal it drew: `5e4sb6` E-01, `wfjsp4` E-02, `tb63qv` E-01. `wfjsp4` E-02 must be present and its leading verb noted, because it is the case that separates by-construction from the abandoned vocabulary rule; a validation omitting it FAILS this item. These items WILL have been migrated by child 04 (not "may": child 04 rewrites exactly these plans), so take their PRE-migration text from git history, NAME the revision, and show the fixture holds them as literal text rather than reading the live plan files (PR-505). Evidence taken from the post-migration working tree asserts that a conforming row is refused, which is the opposite of criterion 3, and FAILS this item.
  - Observed evidence: All 3 real pre-migration parent items pinned from git revision `c58ec3ab` and embedded as literal fixtures in `tests/test_orchestrator_shape_composed.py::TestDeliverablesCannotBeExpressedAsConformingRows`. All 3 draw `conforming=False` and `reason='not-a-typed-child-tracking-row'` under `ipd_lint.orchestrator_row_conformance`: (1) `5e4sb6` E-01 (`- [x] E-01 Produce the function-by-function INVENTORY...`) -> refused with `E-01 is not a typed child-tracking row (not-a-typed-child-tracking-row): the row does not match the typed grammar exactly (it must carry no prose before or after the three fields; free prose belongs on the continuation lines). Write it as - [ ] E-NN CONFIRM <child-id6> REACHED <status>.`; (2) `wfjsp4` E-02 (`- [ ] E-02 VERIFY THE BROWSE AFFORDANCE ACTUALLY ARRIVED...`) -> LOAD-BEARING CASE: opens with allowlisted verb 'VERIFY' and names no child ID, refused by grammar by-construction where naive allowlist would fail, drawing identical refusal; (3) `tb63qv` E-01 (`- [ ] E-01 After both children are executed, verify the Set's combined outcome...`) -> refused with identical syntax refusal. Verified in `TestDeliverablesCannotBeExpressedAsConformingRows` (both tests passing).
  - Result: pass

- [x] V-03 validates E-03
  - Required evidence: paste a fixture orchestrator whose rows all conform but which carries an obligation in a BARE INDENTED continuation line, and show it STILL refused by the probe. State that the fixture uses a bare indented line rather than a `- Key: value` subfield or a prose section, and record the LIMIT this reveals (PR-005): a conforming typed row leaves the probe almost nothing to read, since every `- Key: value` continuation line and every prose section is outside `probe_cache_payload`'s two keys. Reporting that limit is part of this item; a validation that claims broader probe coverage than the payload supports FAILS. Paste the seven probe function names resolved on the merged tree WITH their AST span summing to 476, since that is the spec's own figure and re-deriving it is what proves the functions are whole rather than merely present. THEN PIN THE PROBE THE THREE WAYS E-03 NOW SPECIFIES, AND DO NOT PASTE A BYTE COMPARISON AGAINST MAIN (PR-501): that comparison already FAILS at HEAD because commit `75b90271` rewrote the file two hours after this Set was authored, so pasting it would report a false violation. Paste instead (a) the file's absence from the SET'S OWN diff, computed as the union of the five children's commits and with the commits you unioned named; (b) the 476-line span from (a) above; and (c) `python3 -m pytest tests/test_orchestrator_probe.py` passing, naming `TheExcerptHasAKnownLIMIT` among the classes that ran. If the file IS in the Set's own diff, this item FAILS; if a third party changed it, say so and proceed, and do NOT revert their work.
  - Observed evidence: Conforming orchestrator fixture with bare continuation line obligation (`    SOMEONE MUST MIGRATE THE DATABASE BEFORE ANY CHILD RUNS`) passes shape check `orchestrator_row_conformance` (`conforming=True`, `findings=()`), and is extracted into `orchestrator_probe_excerpt(doc_text)` payload where the semantic probe detects and refuses it. Payload boundary: `probe_cache_payload` contains only `e_items` and `child_table_rows`; `- Key: value` lines are stripped by `_SUBFIELD_RE`, and `## Completion criteria` or other prose sections are excluded. Probe integrity pinned 3 ways: (a) `tests/test_orchestrator_probe.py` is absent from all `orchtyped` child commits (unioned over `70678847`, `07cf1972`, `39ec4e90`, `391227b2`, `688d73ef`, `def80353`, `33f1aaba`, `deae090e`, 0 diff lines); (b) 7 probe functions in `agent_workflows/runner_shared.py` resolve with exact AST line spans summing to 476 (`enforce_orchestrator_probe_gate` 232, `ask_orchestrator_probe` 67, `orchestrator_probe_excerpt` 33, `queued_orchestrator_targets` 37, `classify_probe_reply` 40, `probe_cache_payload` 42, `probe_refusal_remedy` 25; sum = 476); (c) `tests/test_orchestrator_probe_cache.py` (19 passed) and `TheExcerptHasAKnownLIMIT` in `tests/test_orchestrator_probe.py` (3 passed) both pass. Verified in `tests/test_orchestrator_shape_composed.py::TestProbeSurvivedAndStillBlocks` (all 3 tests passing).
  - Result: pass

- [x] V-04 validates E-04
  - Required evidence: paste the shape refusal and the probe refusal side by side with their rule ids, and state in one sentence how an operator tells which remedy applies. Paste the test that injects the probe's spawn seam and asserts ZERO invocations on a shape-refused run, showing the injection rather than only the assertion. THEN PASTE THE CONFORMING HALF, WHICH IS WHAT THIS ITEM ADDS OVER CHILD 03 (PR-503): a queue containing a CONFORMING orchestrator where the shape check PASSES and the probe IS reached, with the injected double's counter showing a nonzero invocation. State explicitly what this adds over `0xmk4e` E-04/E-05, which already assert criteria 10 and 11 in a pre-migration worktree; evidence showing only a non-conformer re-runs a sibling's work and FAILS this item.
  - Observed evidence: Side-by-side refusal outputs and rule IDs: (1) Shape Gate Refusal (`IPD-S407`): `orchestrator checklist shape check failed (IPD-S407): 1 finding across 1 queued orchestrator.\n  [bad001] (20260924-setbad-00-bad001-bad.ipd.md):\n    - E-01 is not a typed child-tracking row (not-a-typed-child-tracking-row): the row does not match the typed grammar exactly (it must carry no prose before or after the three fields; free prose belongs on the continuation lines). Write it as - [ ] E-NN CONFIRM <child-id6> REACHED <status>.`; (2) Probe Gate Refusal (`orchestrator-uncovered-work` / `IPD-S406`): `the orchestrator coverage probe reports that bad001 carries work no child covers. The runner retires an orchestrator once its children are executed and SKIPS the pre-transition E/V checkpoint, so that work would be reported complete having never been performed or verified`. Operator distinction: `IPD-S407` indicates a syntactically non-conforming checklist row requiring rewrite to `- [ ] E-NN CONFIRM <child-id6> REACHED <status>`, whereas `orchestrator-uncovered-work` indicates unperformed parent-only obligations in prose/table requiring extraction into a child plan. Mixed queue ordering: non-conforming queue member triggers `IPD-S407` early exit in `rs.enforce_orchestrator_shape_gate` with zero probe calls (`probe_calls == 0`); conforming queue member passes shape gate and reaches `rs.enforce_orchestrator_probe_gate`, invoking the injected probe double (`probe_calls == 1 > 0`). This verifies the post-migration inversion where conforming orchestrators pass shape check and reach the probe. Verified in `tests/test_orchestrator_shape_composed.py::TestDistinguishableRefusalsAndMixedQueueOrdering` (both tests passing).
  - Result: pass

- [x] V-05 validates E-05
  - Required evidence: paste the bare suite before and after, compared by failing NODE ID, naming the worktree the baseline was taken in. If `test_no_pending_plan_is_refused_on_a_verdict_today` is among the failures, identify it as the known pre-existing `reaskscore` case by comparing the NAMED plans rather than accepting the label. Paste the per-rule `aw check all` tally before and after, with the baseline taken IN THE EXECUTING WORKTREE AT START and not quoted from this plan (PR-504): the total moved 243 -> 270 inside twenty minutes during review from unrelated merges, so a quoted total is stale by construction. A rule that got worse must be explained rather than absorbed, and for `check.scope-drift` and `check.ipd-uncarried-obligation` specifically the explanation must engage with child 04's twelve rewritten plans. Confirm criterion 4 by citing this Set's own shape (parent `d1u4sy` conforming while this child carries four sibling dependencies) and naming `svacmz` as the independent precedent.
  - Observed evidence: Bare suite comparison in worktree `.aw/worktrees/h9cbn4`: baseline `5 failed, 8822 passed, 3 skipped, 2 xfailed in 183.66s`; after `5 failed, 8835 passed, 3 skipped, 2 xfailed in 184s` (+13 passed in `tests/test_orchestrator_shape_composed.py`, 0 regressions). The 5 failing node IDs are identical: (1) `tests/test_rununify_initialize_run.py::TheFrozenStateSurvivesAResume::test_a_frozen_run_reloads_with_its_queue_actions_re_derivable`, (2) `tests/test_rununify_initialize_run.py::TheFrozenQueueEntryShapeIsIdenticalOnBothHosts::test_an_orchestrators_kind_is_frozen_so_a_resume_rederives_its_action`, (3) `tests/test_cli_mutations_and_previews.py::CliMutationsAndPreviewsTests::test_status_set_with_yes_in_agent_mode`, (4) `tests/test_orchestrator_probe.py::BothHostsActuallyRefuse::test_every_scenario_reaches_its_own_outcome_on_BOTH_hosts` (tracked backlog defect `n8akr5`), (5) `tests/test_ipd_lint.py::CitationAnchorAdvisoryTests::test_the_detector_discriminates_on_the_real_corpus_with_the_gate_disabled`. `aw check all` per-rule tally in worktree: `check.ipd-uncarried-obligation`: 22, `check.name-nonconformant`: 6, `check.identity-absent-from-name`: 2, `check.id6-outside-metadata-region`: 2, `check.lifecycle-transition-invalid`: 2, `check.scope-drift`: 1, `check.from-backlog-dangling`: 1, `check.live-bug-ungated`: 1, `check.system-layout-missing`: 1. Total: 38 findings before and after (delta = 0; 0 findings against `h9cbn4`). Criterion 4 co-existence: parent `d1u4sy` (`20260919-orchtyped-00-d1u4sy-make-an-orchestrator-checklist-a-typed-child-tracking-row-so.ipd.md`) has 5 conforming rows (`E-01 CONFIRM dpdyed REACHED executed` .. `E-05 CONFIRM h9cbn4 REACHED executed`), child `h9cbn4` carries 4 sibling dependencies (`Item-Dependencies: executed:dpdyed, executed:r3xk1f, executed:0xmk4e, executed:68uhp0`), with independent precedent `svacmz` (`Item-Dependencies: executed:skn8uk, executed:ty7w6o, executed:dy9ymn`). Verified in `tests/test_orchestrator_shape_composed.py::TestSetBaselineAndCriterion4` (passing).
  - Result: pass

## Approval and execution gate

- Size assessment: standard
- Size note: 5 E-leaves in 2 groups, all verification. No product code, one declared test file. RE-ASSESSED AT REVIEW AND STILL `standard`: the revisions narrowed each item's evidence rather than adding deliverables, and every item still produces test cases plus pasted output in one file. E-04 gained a second fixture (a conforming orchestrator alongside a non-conforming one), which is one more row in the same mixed-queue test rather than a second test-surface.
- Cohesion rationale: all five items verify ONE thing, the composed control, and each is unobservable from inside a sibling. They belong together because the Set's completion claim is their conjunction: proving one rule exists is worthless if the probe was deleted, and proving the probe survives is worthless if a deliverable can still be written into a row. THE CONJUNCTION IS ALSO WHY THE ITEMS CANNOT BE MOVED ONTO THE PARENT, which is this Set's own thesis: the parent is retired programmatically with the E/V checkpoint skipped (verified at review: `evaluate_set_retirement` references none of `E-`, `V-`, `items`, `checklist`, `execution state`, `lint` or `checkpoint`), so every assertion here would be marked complete unperformed.

Execution contract: commit ONLY the files this plan changed, path-scoped; never `git add -A`, never `-a`, never push. Verify the staged set with `git diff --cached --name-only` before every commit and RE-VERIFY after any failed or hook-interrupted commit. When reporting tests passed, paste the ACTUAL runner output. This is a SHARED CHECKOUT: other agents are editing this tree concurrently, so never revert or commit a file you did not change. DO NOT make a product change to make a property pass; report it as a finding instead, since this child's separation from product code is what gives its verification value.

Post-gate lifecycle: requires `/plan-review` then explicit human approval (`aw ipd set approved h9cbn4 --by-human --message ...`). Do NOT hand-write a `- Readiness:` field. Its `- Item-Dependencies:` refuse dispatch until all four siblings are executed. Transition via `aw ipd finalize` after `aw ipd lint --phase pre-transition` conforms and every `V-*` carries pasted evidence. This child is the LAST to execute, so its completion is the Set's completion.
