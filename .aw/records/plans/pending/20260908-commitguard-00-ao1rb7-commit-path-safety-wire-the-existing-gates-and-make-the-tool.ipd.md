# IPD: Commit-path safety: wire the existing gates and make the tooled path mandatory

- Date: 2026-09-08
- Kind: orchestrator
- Concern: Backlog item `wjl471` contains two independently valuable halves with very different risk profiles, and running them as one plan would couple a near-free configuration change to a contract change that rewrites the guidance installed into every managed repo. Its FINDING 2 is that four of six shipped gate verbs are invoked by nothing and no `pre-push` hook exists at all, which is pure wiring of working code. Its MAINTAINER RULING of 2026-09-01 is that the commit contract must say MUST rather than PREFER, which cannot land honestly until `aw commit` has a plan-less form to comply with and until the retry its wording DESCRIBES actually exists.
- Scope: Orchestration only. Sequence the two children, hold the Set's shared constraint (the honest-limit disclosure rule), and define what makes the whole Set complete. Every deliverable belongs to a child; this parent performs no implementation work of its own.
- Scope-Paths: .aw/records/plans/pending/20260908-commitguard-01-kbqpkn-wire-the-four-unwired-gates-and-install-the-missing-pre-push.ipd.md, .aw/records/plans/pending/20260908-commitguard-02-y9vpvv-add-a-plan-less-aw-commit-and-land-the-ruled-must-wording.ipd.md
- Item-Dependencies: none
- Status: reviewed
- Readiness: go-pending-approval
- Set: commitguard
- Order: 0
- Highest E allocated: 03
- Author: opencode its_direct/pt3-claude-opus-5-1m-us
- Id: ao1rb7
- From-Backlog: wjl471

## Workflow history

- 2026-09-09 reviewed (opencode its_direct/pt3-claude-opus-5-1m-us): /plan-review at HEAD `2845941c`; APPROVE WITH REVISIONS APPLIED, readiness `go-pending-approval`; PR-701..PR-708 all FIXED in place, none deferred, none REPLAN. `aw ipd lint --phase author` CONFORMING before review and `--phase review-finalize` CONFORMING after, and all three Set members plus `lqly9m` lint conforming, so nothing here is structural. THE SET SHAPE IS RIGHT AND THE SPLIT ARGUMENT IS CORRECT: FINDING 2 reproduces EXACTLY as filed (the four gates each appear ZERO times in `.pre-commit-config.yaml`, the two wired ones appear once and twice, the only installed git hook is `pre-commit`, `aw commit` still refuses without a selector at `work_cmd.py:138`, and the PREFER paragraph still carries "immune to this by construction" at `engine.py:1252`). THE ONE SERIOUS FINDING IS ABOUT THE RUNNER, NOT THE DESIGN: the gate asserted that a runner retiring this parent "skips nothing that was never performed elsewhere", and that is FALSE for E-03. E-01 and E-02 are genuinely superseded by a runner, but E-03 compares Order 01's shipped disclosures against Order 02's shipped contract sentences, which neither child can see, and `retire_orchestrator` deliberately skips the `pre-transition` E/V checkpoint (`ROLLUP_OMITTED_GATES["pre-transition-ev-checkpoint"]`, spec `77tr3o` R-5 shape (b)), so a runner would mark this parent `executed` with the Set's own honesty criterion unchecked. E-03 was NOT deleted (AGENTS.md says a parent should carry orchestration, and this comparison is genuine orchestration); instead the caveat is stated, V-03 now REFUSES a completion mark without a named performer, and new OQ-02 puts the keep-here-versus-add-an-Order-03 choice to the maintainer. FOUR MEASURED STALE FACTS CORRECTED. `29wvmj` is `executed`, not "approved", and its `pre-merge-commit` keys have ALREADY LANDED, so Order 01 extends a two-entry list rather than racing a sibling; `default_stages: [pre-commit]` is pinned deliberately for a reason the file documents, which is a live trap for the `pre-push` work and is now called out; `lqly9m` is `to-review`, so Order 02's `executed:lqly9m` edge is currently UNSATISFIED and the Set cannot finish in one pass; and the suite baseline was wrong in both count and named failure (`1 failed, 5919 passed, 3 skipped, 2 xfailed`, failing on `test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, environmental, caused by 189 untracked `opencode-recovery/*.md` files belonging to another party, NOT `test_orchestrator_retirement`). Also verified by CALLING it that `parse_child_table` returns `{'1': (), '2': ()}` for this table, i.e. the out-of-Set typed edge contributes no intra-Set ordering by design and is enforced at dispatch instead, which is now documented so nobody "fixes" the table. E-count 3 to 3; no E-item added or removed; no child plan and no product code touched by this review.
- 2026-09-08 to-review (opencode its_direct/pt3-claude-opus-5-1m-us): Authored from backlog item `wjl471` as the Order-00 parent of a two-child Set. WHY A SET RATHER THAN ONE PLAN: the item's two halves have different risk, different prerequisites and different reviewers' concerns. Order 01 (`kbqpkn`) wires four gates that already work and depends on nothing, so it can land immediately. Order 02 (`y9vpvv`) replaces two paragraphs of the managed block that is installed into EVERY managed repo, and carries the item's own HARD ORDERING CONSTRAINT that its retry sentence must not ship before the retry exists; it therefore declares `Item-Dependencies: executed:lqly9m` (the graduated `egqt32` plan). Coupling them would block the free half behind the gated half. WHAT IS DELIBERATELY NOT IN THIS SET: the item's FINDING 1, the agent-context detector and the new commit guard, with its four open questions (where the guard lives, what accident it catches, how it phrases the refusal, how an override is audited). That is a real design needing a maintainer ruling on shape, and the item itself says the gate wiring "may catch most of the remaining accidents at near-zero cost", so the cheap wins are separated from the design. VERIFIED AT HEAD `a2e0438a` that both halves are live: the four gates each appear ZERO times in `.pre-commit-config.yaml` while the two wired ones appear, the only installed git hook is `pre-commit`, `aw commit` still refuses without a plan selector (`work_cmd.py:138`), and the PREFER paragraph still carries the "immune to this by construction" claim (`engine.py:1252-1256`).
- 2026-09-08 draft (opencode its_direct/pt3-claude-opus-5-1m-us): created.

## Goal

Land the two halves of `wjl471` in the right order: turn on the guards that already work, and make the tooled commit path mandatory only once it is compliable and only once the contract's descriptive claims are true.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: sequence the children and hold the Set-wide constraint

- [ ] E-01 Execute Order 01 (`kbqpkn`, wire the four unwired gates and install the `pre-push` stage) and confirm it reports conforming before proceeding. It depends on nothing, so it goes first and its value does not wait on the contract half. This item is ORCHESTRATION: the deliverables are the child's, and this parent's job is to confirm the child ran and passed rather than to perform any wiring itself.
  BOTH CHILDREN ARE `to-review` AND NEITHER IS APPROVED, measured at review, so neither this item nor E-02 can begin until a human approves each child on its own terms. The parent's own gate already says approving the parent does not approve the children; this is the operational consequence.
  - Depends on: none
  - Expected outcome: `kbqpkn` is `executed` with every `V-*` carrying observed evidence.
  - Execution state: pending

- [ ] E-02 Execute Order 02 (`y9vpvv`, plan-less `aw commit` plus the ruled MUST wording) only after its declared dependency is satisfied, and confirm it reports conforming. It declares `Item-Dependencies: executed:lqly9m`, which encodes the item's hard ordering constraint; its own E-05 re-proves the retry by behavior at execution time and provides the documented fallback (ship the imperative, withhold the descriptive sentence) if the retry is absent. Do NOT relax that edge to make the Set finish sooner: the whole reason it exists is to stop the managed block describing behavior the code lacks.
  `lqly9m` IS NOT YET RUNNABLE, MEASURED AT REVIEW, so expect this edge to hold rather than to pass. `lqly9m` (Set `hookretry`, Order 01) is `- Status: to-review` with no `Readiness`, so it is neither approved nor executed, and `executed:lqly9m` cannot be satisfied until it is run. Under a runner, `edge_satisfied` re-checks the edge AT DISPATCH and marks this item `dependency-blocked`, continuing the run rather than failing it, which means a `commitguard` run today lands Order 01 and correctly defers Order 02. That is the designed behavior and is NOT a defect to report; what it does mean is that the SET cannot complete until `lqly9m` is reviewed, approved and executed, so do not plan on finishing `commitguard` in one pass.
  THE CHILD TABLE CANNOT CARRY THIS EDGE, AND THAT IS CORRECT. Verified by calling `ipd_set_plan.parse_child_table` on this file: it returns `{'1': (), '2': ()}` with no error, i.e. Order 02's `executed:lqly9m` contributes NO intra-Set ordering, because `_dep_tokens` deliberately ignores a typed edge naming a plan outside the Set, "exactly how the runner treats an out-of-queue target". So the table showing `executed:lqly9m` is documentation for a human, and the ENFORCEMENT is the child's own `- Item-Dependencies:` field read at dispatch. Do not "fix" the table to make the edge appear in the parse, and do not conclude from the empty parse that the edge is unenforced.
  - Depends on: E-01
  - Expected outcome: `y9vpvv` is `executed` with the wording landed and its retry claim either proven or explicitly withheld; OR an explicit record that the `executed:lqly9m` edge was unsatisfied, this item is outstanding, and the Set is incomplete.
  - Execution state: pending

- [ ] E-03 Enforce the Set-wide HONESTY CONSTRAINT across both children's output, because it is the one requirement the item says "still matters" and it spans them: "a bypassable guard must never be DESCRIBED as a boundary, because that is how a fail-open check comes to be trusted." Order 01 turns bypassable gates ON, and Order 02 rewrites the paragraph that describes the commit path's guarantees, so both can violate it independently. After both children complete, re-read the shipped disclosures and the shipped wording together and confirm no sentence claims a boundary that `--no-verify` defeats.
  **READ THIS BEFORE RELYING ON THIS ITEM: A RUNNER WILL NEVER PERFORM IT.** `aw oc run` / `aw agy run` RETIRE an orchestrator once every child is `executed`, and `retire_orchestrator` DELIBERATELY SKIPS the `pre-transition` E/V checkpoint. The reason is recorded verbatim in `ipd_lifecycle.ROLLUP_OMITTED_GATES["pre-transition-ev-checkpoint"]`: "an orchestrator's items are performed by NOBODY (the runner supersedes its coordination role), so the requirement is unsatisfiable by construction rather than unsatisfied by neglect", resolved by the maintainer as spec `77tr3o` R-5 shape (b). So under a runner this parent is marked `executed` with E-03 NEVER PERFORMED and V-03 NEVER VERIFIED, and the cross-child honesty check the Set's own completion criteria require would silently not happen.
  WHY IT IS STILL SITED HERE, AND WHAT MAKES THAT SAFE. The check genuinely spans both children: Order 01 owns its own disclosures (its E-05 preserves `prepush_authorization_gate.py` and `precommit_scope_gate.py` wording) and Order 02 owns the managed-block sentences (its spec-sync section holds the retry-sentence honesty rule), so each child correctly guards ITS OWN half and NEITHER can compare the two halves against each other. Deleting this item would lose that comparison, and AGENTS.md is explicit that an orchestrator SHOULD carry a checklist of orchestration. The mitigation is therefore not relocation but EXPLICIT HUMAN OWNERSHIP: this Set must be finished by an agent or human told to "execute commitguard", not left to a runner's automatic retirement. If a runner retires this parent, treat E-03 as OUTSTANDING and perform it in a follow-up before trusting the Set's completion criteria.
  IF YOU CONCLUDE THE CHECK MUST BE RUNNER-EXECUTED, do not move the work into either child (neither can see the other's output). Add a THIRD child, Order 03, that performs the cross-child read after both land, per AGENTS.md's rule that work found on a parent gets a child rather than a deletion. That is a Set-shape change and needs the maintainer, so it is OQ-02 rather than an instruction.
  - Depends on: E-02
  - Expected outcome: a stated confirmation that every gate disclosure and every contract sentence shipped by this Set describes local, bypassable feedback accurately, with any offending sentence corrected AND QUOTED; plus an explicit statement of WHO performed this item (human, agent, or "not performed because a runner retired the parent").
  - Execution state: pending

## Child IPDs, sequence, and dependencies

| Order | File | What it does | Depends on |
|---|---|---|---|
| 01 | `20260908-commitguard-01-kbqpkn-wire-the-four-unwired-gates-and-install-the-missing-pre-push.ipd.md` | Confirms each unwired gate's intent, proves it runs clean, registers the cleared ones, installs the `pre-push` stage so the push gate can fire, and adds a declared-list wiring test | none |
| 02 | `20260908-commitguard-02-y9vpvv-add-a-plan-less-aw-commit-and-land-the-ruled-must-wording.ipd.md` | Adds `aw commit --no-plan` so the MUST is compliable, lands the ruled MUST wording verbatim, and applies the companion narrowing | `executed:lqly9m` (the graduated `egqt32` retry) |

The two children are otherwise INDEPENDENT: Order 01 touches `.pre-commit-config.yaml` and the wiring test, Order 02 touches the commit verb and the managed block. They share no source file, so Order 01 need not wait on Order 02's dependency.

ON THE `Depends on` CELL FOR ORDER 02, so no one "fixes" it: `parse_child_table` returns `{'1': (), '2': ()}` for this table, i.e. `executed:lqly9m` contributes NO intra-Set ordering. That is by design (`_dep_tokens` ignores a typed edge naming a plan outside this Set, matching how the runner treats an out-of-queue target), and the edge is ENFORCED from the child's own `- Item-Dependencies:` field at dispatch, not from this table. The cell is documentation for a human reader. MEASURED AT REVIEW: `lqly9m` is `- Status: to-review`, so the edge is currently UNSATISFIED and a `commitguard` run will land Order 01 and defer Order 02 as `dependency-blocked`. That is correct behavior, not a fault.

## Completion criteria (the whole Set is done only when)

- Every gate whose exclusion was found to be an OVERSIGHT is registered and demonstrably fires; every gate whose exclusion was found to be DELIBERATE is documented as such and left alone. (Re-verified at review: `precommit-scope-gate`, `prepush-authorization-gate`, `backlog-blocking-close-gate` and `ipd-dependency-statement-gate` each appear ZERO times in `.pre-commit-config.yaml`, while `ipd-executed-gate` appears once and `ipd-status-untooled-gate` twice, so FINDING 2 still reproduces exactly.)
- A `pre-push` hook stage is installed and the push gate fires on push and not on commit. (Re-verified at review: the only installed hook in `.git/hooks` is `pre-commit`, and `default_install_hook_types` is `[pre-commit, pre-merge-commit]`, so the stage genuinely does not exist yet. Note `default_stages` is pinned to `[pre-commit]` deliberately, so the push gate needs its OWN explicit `stages:` rather than relying on the default.)
- `aw commit --no-plan -m <msg> -- <paths>` commits through `offer_commit` with the skipped protections named, and still excludes a concurrent agent's staged files.
- `engine.py` carries the ruled MUST wording verbatim and `AGENTS.md` renders it, with the descriptive retry sentence present only if the retry was demonstrated.
- The `git reset`/`git stash` prohibition is byte-identical to before.
- No sentence shipped by either child describes a bypassable guard as an authority boundary.

## Cross-IPD validation

- NO DOUBLE-EDIT of `.pre-commit-config.yaml`: only Order 01 touches it. CORRECTED AT REVIEW: `29wvmj` is no longer merely "approved", it is `executed`, and its `pre-merge-commit` work HAS LANDED. Verified at HEAD: `default_install_hook_types: [pre-commit, pre-merge-commit]` and `default_stages: [pre-commit]` are already in the file, with the `ipd-executed-transition-gate` opting into `pre-merge-commit` through its own explicit `stages:`. So Order 01 is EXTENDING a list that already has two entries, not racing a pending sibling, and the check is now a confirmation rather than a coordination risk. Confirm after Order 01 lands that `default_install_hook_types` contains `pre-commit`, `pre-merge-commit` AND `pre-push`, and that `29wvmj`'s two keys plus the comment block explaining them are byte-unchanged.
- MIND `default_stages: [pre-commit]`, WHICH IS PINNED DELIBERATELY AND IS A TRAP FOR THE `pre-push` WORK. The file's own comment records why: it would otherwise default to ALL stages, so adding a hook type would also run every content-MUTATING hook (ruff --fix, whitespace/eof fixers) during that stage, "rewriting a merge result mid-merge". The same hazard applies to `pre-push`: adding the stage to `default_install_hook_types` without giving the push gate its OWN explicit `stages:` would either not run it or run the mutating hooks on push. Order 01 must follow the established pattern (pin `default_stages`, opt the one gate in explicitly), and this cross-check confirms it did.
- NO CONTRADICTION between the wiring and the wording: Order 01 makes more gates fire on commit, and Order 02's wording tells agents the tooled path costs no round trip. If a newly wired gate REFUSES (rather than rewrites), that is correct and the wording's retry claim does not cover it; confirm the wording does not imply otherwise.
- ONE SOURCE FOR THE MANAGED BLOCK: `AGENTS.md` is generated from `engine.py`. Confirm the committed `AGENTS.md` matches a fresh regeneration, so the Set does not leave a hand-edited block behind.
- THIS SECTION IS PART OF E-03 AND INHERITS ITS RUNNER CAVEAT: a runner that retires this parent performs none of these cross-checks. Whoever finishes the Set by hand must run them explicitly.

## Deferred / out of scope (with reason)

- THE AGENT-CONTEXT DETECTOR AND THE NEW COMMIT GUARD (the item's FINDING 1 and OQ-1 through OQ-4). The item's environment-marker matrix across five agent hosts is durable evidence and is committed with the item, but the guard itself needs a maintainer ruling on where it lives (pre-commit hook sees the staged set but not the INTENT; a `git` wrapper sees intent but is invasive; reading `reflog` is too late), what accident it catches, and how an override is recorded. None of that is required to wire four working gates or to land a wording ruling.
- SETTING `AI_AGENT` OURSELVES IN THE DRIVERS. Part of the detection design.
- `mjx7ne`'s `commit_gateway` / `deny_push` HOST CAPABILITIES, which the item notes grep to zero enforcement. A separate open item.
- THE OS-LEVEL CONFINEMENT QUESTION (research prompt `q65sz3`). The item explicitly does not depend on it.
- `suugsf`, `a8eufb`, `077yqc`. Related open items about the contract not warning about a shared checkout and about finalize misattributing a concurrent agent's files; all outside this Set.

## Scope check

- Over-scope: none. This parent's `Scope-Paths` are its two children, and it performs no implementation.
- Scope-Paths justification, added at review because a parent declaring only its children is unusual and a reader should know it is deliberate: this orchestrator's only writes are to the two child plan files (their `- Status:` and workflow-history lines as they move through the lifecycle) plus its own file, which `_is_implicitly_allowed` already covers. E-03 READS `.pre-commit-config.yaml`, `agent_workflows/engine.py`, `AGENTS.md` and the two hook modules to perform its comparison, and reading is not declaring: if E-03 finds a sentence needing correction, the fix belongs in the OWNING child's fence (Order 01 for the hook disclosures, Order 02 for the managed block), not here. If a correction is discovered after both children are terminal, it is a new corrective IPD, since AGENTS.md forbids adding commits to an executed plan.
- Under-scope: the new guard and the detector are deliberately not in this Set (see Deferred), so `wjl471` will remain open after this Set completes unless the maintainer decides the wiring plus the wording is the whole answer. Also under-scope by construction: this parent fixes nothing itself, so every sentence E-03 finds wanting becomes a child's edit or a follow-up plan.

## Required tests / validation

- Each child's own validation must pass on its own terms; this parent adds no test of its own.
- After both children land: `pre-commit run --all-files` clean, the bare `python3 -m pytest` summary line pasted and compared to the baseline, and a fresh `AGENTS.md` regeneration producing no diff.
- THE BASELINE WAS WRONG AND IS PERISHABLE. This plan recorded "1 failed, 5648 passed on `main`, the known `test_orchestrator_retirement` failure". Re-measured at review with the bare suite: `1 failed, 5919 passed, 3 skipped, 2 xfailed`, and the failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, NOT `test_orchestrator_retirement`. It is ENVIRONMENTAL: 189 untracked `opencode-recovery/*.md` files belonging to another party in this shared checkout trip a prose-parity assertion. Measure your OWN baseline before and after and judge on the failure-set DELTA (after minus before must be EMPTY), never on an absolute count, and name the failing node ids rather than a count.
- NOTE THE IRONY WORTH CHECKING DELIBERATELY: `pre-commit run --all-files` is being used to validate a Set whose Order 01 CHANGES `.pre-commit-config.yaml`. Run it AFTER re-installing hooks (`pre-commit install`), because `default_install_hook_types` only takes effect at install time and a stale `.git/hooks` would make a green run meaningless. At review the only installed hook was `pre-commit`, so this is the state you are starting from.

## Open questions

### OQ-01: After this Set lands, is `wjl471` closed or does it stay open for the guard?

- Blocking: no
- Status: open
- Owner: maintainer
- Resolution or deferral rationale: DEFERRED because it is a scope judgement, and recorded so the item is not closed by accident. This Set delivers the item's FINDING 2 and its MAINTAINER RULING in full, but NOT its FINDING 1 (the agent-detecting guard), which is the item's headline. RECOMMENDATION: keep `wjl471` open after this Set, or split the guard into its own item carrying the environment-marker evidence table, since that table is the durable record and the raw dumps it cites are gitignored and local-only. Closing the item on this Set alone would lose the guard design and the five-host measurement with it.
  ONE MECHANICAL NOTE ADDED AT REVIEW, so the recommendation is actionable rather than advisory. Both children carry `- From-Backlog: wjl471`, which is the machine-readable handoff, and the item carries NO `- Blocks-Release:` (confirmed: child `kbqpkn`'s own history records that it inherits none despite `Priority: high`). So there is no release-gate handoff obligation to satisfy and no `check.blocking-item-closed-without-gate` risk either way; the only hazard is a HUMAN closing the item on partial delivery. If the maintainer chooses the split, the new item should carry the environment-marker table verbatim, because the raw dumps behind it are gitignored and would not survive.

### OQ-02: Should the cross-child honesty check (E-03) become a third child?

- Blocking: no
- Status: open
- Owner: maintainer
- Resolution or deferral rationale: RAISED AT REVIEW, not by the author, because the measurement that prompts it is about the RUNNER rather than about this plan's design. E-03 is legitimate orchestration (it compares Order 01's shipped disclosures against Order 02's shipped contract sentences, which neither child can see from inside its own fence), and AGENTS.md is explicit that an orchestrator SHOULD carry a checklist of orchestration. BUT `retire_orchestrator` deliberately skips the `pre-transition` E/V checkpoint on the recorded premise that "an orchestrator's items are performed by NOBODY", so under `aw oc run` this parent reaches `executed` with E-03 unperformed and V-03 unverified, and the Set's completion criterion "no sentence shipped by either child describes a bypassable guard as an authority boundary" would be silently unchecked.
  THE TWO HONEST OPTIONS, and neither is the reviewer's to pick. (a) KEEP IT HERE and accept that this Set must be finished by a human or an agent told to execute the Set, with E-03 treated as outstanding if a runner retires the parent; the plan now says this explicitly and V-03 requires naming the performer, so the failure is loud rather than silent. (b) ADD AN ORDER 03 CHILD that performs the cross-child read after both land, which AGENTS.md prescribes for work found on a parent that no child covers ("do NOT delete it: ADD A CHILD for it"), at the cost of a third plan for one comparison. Option (b) is more robust and option (a) is cheaper; the choice depends on whether this Set will be run by a runner, which the maintainer decides. NON-BLOCKING because option (a) is now safe by disclosure: the plan states the caveat, and V-03 refuses a completion mark with no named performer, so nothing silently passes while this question is open.

## Validation and cross-check (verify before reporting the Set complete)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: paste `kbqpkn`'s final status showing `executed`, and paste its `V-01` through `V-06` observed-evidence blocks (or a summary quoting each) demonstrating they carry real pasted evidence rather than assertions.
  - Observed evidence:
  - Result: pending

- [ ] V-02 validates E-02
  - Required evidence: paste `y9vpvv`'s final status showing `executed`, paste the evidence that its `Item-Dependencies: executed:lqly9m` edge was SATISFIED at dispatch (not waived), and state which E-05 outcome occurred (retry demonstrated and full wording shipped, or retry absent and the descriptive sentence withheld). ALTERNATIVELY, if the edge was NOT satisfied (measured at review: `lqly9m` is `to-review`, so this is the expected near-term outcome), paste `lqly9m`'s current status, record this item as OUTSTANDING rather than complete, and state that the Set is incomplete. Do NOT mark this V-item complete on a `dependency-blocked` outcome: a deferred item is not a performed one.
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: paste every disclosure sentence shipped by Order 01 and every contract sentence shipped by Order 02, and for each state whether it describes a bypassable local check accurately. Paste any sentence corrected as a result. A blanket assertion that "the disclosures are fine" does not satisfy this item; the sentences must be quoted. ALSO paste the three cross-IPD checks' results (the `default_install_hook_types` contents including `29wvmj`'s byte-unchanged keys, the wiring-versus-wording non-contradiction, and the `AGENTS.md` no-diff regeneration), since they are part of E-03's cross-child scope.
  - STATE WHO PERFORMED THIS ITEM. If a runner retired this parent, E-03 was NOT performed (`ROLLUP_OMITTED_GATES["pre-transition-ev-checkpoint"]` skips the E/V checkpoint by design), and the honest record is "not performed; outstanding", not a completed V-item. Marking this complete without a named performer is exactly the false completion the omitted-gate documentation warns about.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

This orchestrator is `to-review`. It must not be executed until a human sets it `approved` with `aw ipd set approved <plan>`; no `- Readiness:` field is written here, because that field is `/plan-review`'s attested output and hand-writing it would forge a review that never happened. Each child carries its own approval gate and must be approved on its own terms; approving the parent does not approve the children.

This parent holds ORCHESTRATION ONLY: E-01 and E-02 sequence the children and E-03 is a cross-child honesty check that neither child can perform alone. No implementation work lives here in the sense of producing a deliverable.

BUT THE CLAIM THAT A RUNNER "SKIPS NOTHING THAT WAS NEVER PERFORMED ELSEWHERE" IS FALSE FOR E-03, AND REVIEW CORRECTED IT. E-01 and E-02 are genuinely superseded by a runner (it executes the children itself), so for those two the claim holds. E-03 is different: it is a COMPARISON of the two children's shipped output that neither child performs, and `retire_orchestrator` deliberately skips the `pre-transition` E/V checkpoint (`ROLLUP_OMITTED_GATES["pre-transition-ev-checkpoint"]`, spec `77tr3o` R-5 shape (b)), so a runner marks this parent `executed` with E-03 never performed. Do not read the omitted-gate documentation as permission for that: it justifies the skip on the premise that a parent's items are performed by nobody, which is exactly why work that only the parent covers must not be left silently unperformed. See OQ-02 for the two options, and V-03 for the requirement to NAME the performer.

CURRENT STATE, MEASURED AT REVIEW, so nobody plans a one-pass run: both children are `to-review` and unapproved, and Order 02's dependency `lqly9m` is also `to-review`. So the earliest useful action is approving and running Order 01 alone; Order 02 will correctly defer as `dependency-blocked` until `lqly9m` is reviewed, approved and executed. The Set will therefore complete in at least two sittings, and E-03 comes last by construction.

Execution contract: each child commits only its own `Scope-Paths`, path-scoped, never `git add -A`, never push. This is a SHARED CHECKOUT with other agents working concurrently, and ~189 untracked `opencode-recovery/*.md` files belong to another party: do not stage them, and note they cause the one pre-existing suite failure. Never reach for `--no-verify` anywhere in this Set: Order 01 turns gates ON and Order 02 makes the tooled path mandatory, so bypassing a hook to land either change would contradict the Set's own purpose. When both children are `executed` and E-03's cross-child check carries pasted evidence WITH A NAMED PERFORMER, this parent may be retired to `.aw/records/plans/executed/` through `aw ipd finalize`. Note OQ-01: retiring this Set does NOT by itself close backlog item `wjl471`, which retains the guard design.
