# IPD: unify the two host runners onto one shared runner library, phased behind a measured inventory

- Date: 2026-08-29
- Kind: orchestrator
- Concern: `oc_runipd.py` (3144 lines) and `agy_runipd.py` (3143 lines) are near-duplicate host runners that have DIVERGED. Measured at HEAD `c7f41b9`: 72 top-level symbols are shared, and only 35 of those are byte-identical while 37 have drifted; 2637 of `oc_runipd`'s 2843 top-level-definition lines sit in shared symbols, so roughly 93 percent of each runner is duplicated logic. Five features exist only in the opencode runner and seven only in the agy runner. Recent lifecycle work went into opencode alone, so agy silently lacks it. A generic `host_runner` and a host-abstraction layer already exist and BOTH runners import neither.
- Scope: ORCHESTRATOR - authors NO product code. Its own execution work is (E-01) the function-by-function inventory the backlog item demands as a deliverable, (E-02) the characterization baseline that makes the Set's behavior-preservation claim falsifiable, and (E-03) whole-Set verification. Children carry all extraction. This plan owns the child table, the sequencing decision against the 21 unexecuted plans that already declare these modules, the anti-regression contract every child inherits, and the Set completion criteria. Explicitly EXCLUDES changing runner BEHAVIOR: this Set is a de-duplication, and every divergence it reconciles must be reconciled to an existing behavior, never to a new one.
- Scope-Paths: .aw/records/plans/pending, .aw/records/research, tests
- Item-Dependencies: none
- Status: approved
- Set: rununify
- Order: 0
- Highest E allocated: 03
- Author: opencode (its_direct/pt3-claude-opus-5-1m-us)
- Id: 5e4sb6
- Approval: 2026-08-30, recorded via aw ipd set: status set to approved
- Blocks-Release: next
- From-Backlog: dhuape

## Workflow history
- 2026-09-03 approved (opencode its_direct/pt3-claude-opus-5-1m-us): CHILDREN 01 AND 02 AUTHORED from E-01's inventory, which is everything the re-pointed gate authorizes; children 03+ remain unauthored because they reconcile diverged symbols and are gated behind `lanectn` plus the E-02 characterization baseline. Both are `to-review`, `aw ipd lint` conforming, zero blocking open questions, and carry `Blocks-Release: next`. `2r306y` (order 01) deletes the 5 class (d) re-forks and makes the anti-re-fork guard symmetric; `818uru` (order 02) extracts the 34 class (a) commons into a new `runner_common.py` and declares `Item-Dependencies: executed:2r306y`. THREE CORRECTIONS to this orchestrator's own child table, applied above rather than left to mislead an executor: `Heartbeat` is already fixed so the `stallfp-01` overlap is moot; the re-fork list is 5 not 4, and the two additions (`_read_id`, `_read_status`, owned by `selectors.py`) sit in BOTH runners, so child 01 must touch `oc_runipd` which the table assumed it would not; and the common count is 34 not 35. TWO FINDINGS THAT SHAPED CHILD 02 and were not visible before the inventory: a naive lift FAILS because four of the 34 call symbols outside the set, two of them class (c) DIVERGED, so those are injected rather than imported; and `DriverError` is a LATENT BUG, two distinct classes whose hand-written translation wrapper is already documented at `agy_runipd.py:87-93`, making it the highest-value single symbol in the Set. ALSO RECORDED as out of scope but real: `agy_runipd` already imports 40 names FROM `oc_runipd`, so the runners are not peers and a shared library is a layering correction, not only de-duplication. `aw check plans` 13 errors before and after; sanitizer clean.
- 2026-09-03 approved (opencode its_direct/pt3-claude-opus-5-1m-us): SEQUENCING GATE RE-POINTED from `wtiso` to `lanectn` by maintainer ruling; status UNCHANGED, no code touched, no E/V item added or removed. The old gate was unusable: `runstop` (7) and `lanetruth` (4) are all `executed/` with their code in both runners, so they cannot collide and are simply part of E-01's measurement, while `wtiso` is 7 of 9 `superseded` (6 retired UNLANDED 2026-09-02, design ported to `lanectn`, 7 plans `reviewed`, unexecuted, editing both runner modules). So it waited on a Set that cannot arrive while its successor was still ahead of it. NOT A LOOSENING, measured in `tvnq50`: `lanectn`'s plans name NONE of the 39 safe-slice symbols, and the four runner symbols they do name (`reconcile_disposition`, `execute_item`, `build_isolation_notice`, `build_prompt`) are all class (c) DIVERGED, hence already inside the deferred set. REJECTED, recorded so it is not re-proposed: editing `runstop`/`wtiso`/`lanetruth` to be resilient to a merged library, which is useless because all 11 are terminal and would invert this Set's behavior-preserving contract. ALSO CORRECTED three stale counts that would have mis-sized a child: the roadmap's 35 byte-identical -> 34, its 37 diverged -> 52, and OQ-03's heading, which now records E-01's 6-of-52 partial answer and why the other 46 stay deferred. `aw ipd lint` advisory-only; `aw check plans` 13 errors before and after; sanitizer clean.
- 2026-09-03 approved (opencode its_direct/pt3-claude-opus-5-1m-us): PARTIAL EXECUTION OF E-01 ONLY, deliberately bounded; plan STAYS `approved` in `pending/` and is NOT claimed executed. The maintainer authorized the inventory ALONE (read-only: research artifact only, no child authored, no runner touched, no shared module created) and directed that the sequencing gate be decided FROM the measurement rather than guessed at beforehand. DELIVERED: research artifact `tvnq50` (`.aw/records/research/20260903-rununify-00-tvnq50-runner-symbol-inventory-four-class-partition.findings.md`), created with `aw research new --apply`, carrying the FOUR-class partition of all 88 shared plus 50 single-side symbols at HEAD `769989ce`, per-symbol line counts and anchors, the repo-wide class (d) sweep, and the reproducible method. V-01 is recorded `failed`, NOT `pass`, because that item's own bar requires an authoritative-side decision for every class (c) symbol and only 6 of 52 have one; the other 46 need 46 implementation pairs read, which this slice was not authorized to do, and 51 of 52 symbols share an identical last-touch date on both sides so git recency cannot decide them. THE PLAN'S NUMBERS ARE STALE AND TWO OF THE CHANGES ARE DECISION-RELEVANT, not cosmetic: shared symbols 72 -> 88, diverged 37 -> 52, common 35 -> 34, file sizes 3144/3143 -> 6683/4819. (1) `Heartbeat` is ALREADY FIXED (`agy_runipd.py:43` imports it from `render_stream`), so child 01 must be RE-SCOPED rather than executed as written, and the real re-fork list is five symbols including two the plan never named (`_read_id`, `_read_status`, owned by `selectors.py`, duplicated in BOTH runners). (2) `driver_actor`, cited by E-01 as the sole host-naming-only symbol that motivates the normalization step, is now STRUCTURALLY diverged; the sole such symbol is `print_status`. REFRAMED: the "93 percent duplicated" claim is asymmetric - 90.4 percent of agy's definition lines sit in shared symbols against only 70.4 percent of oc's, and host-specific splits 47 oc to 3 agy, so this is one shared program plus an opencode-specific extension, not two copies of one program. OQ-02 ANSWERED FROM EVIDENCE: DESIGNATE, do not absorb - `plan_readiness.py` is imported by `status_set.py` and `ipd_schema.py`, so absorbing it into a runner library would make the schema and status layers depend on a driver runtime module. STILL OPEN FOR THE MAINTAINER, deliberately not decided here: the sequencing gate cannot be evaluated as written, because it waits on `wtiso`, six of whose eight plans were retired UNLANDED on 2026-09-02 and replaced by the `lanectn` Set (7 plans, `reviewed`, unexecuted) which edits these same files. E-02 and E-03 remain `pending`. Suite unaffected (no product code touched); `aw ipd lint` advisory-only after the edit; `aw check plans` 13 errors before and after; `aw sanitize --agent` clean; `aw research index --check` 35 pre-existing findings before and after, none from `tvnq50`.
- 2026-08-30 approved (aw set): status set to approved
- 2026-08-30 reviewed (opencode (its_direct/pt3-claude-opus-5-1m-us)): /plan-review: REVIEWED - OPEN QUESTIONS; PR-001..PR-005. Every quantitative claim independently re-measured at c7f41b9 and reproduced EXACTLY (72 shared symbols, 35 byte-identical, 37 diverged, the 2843/2637/206/294 line sums, the five largest shared symbols and deltas, the 1229-line raw diff, the oc-only/agy-only sets, terminate_process differing by exactly one docstring line). Two HIGH structural gaps FIXED. PR-001 (F10): the three-class partition was STRUCTURALLY BLIND to symbols a NON-runner module already owns; executed runnernorm-01 extracted render_stream.py, oc_runipd imports it, agy never does and re-forked Palette/_one_line/_strip_ansi (byte-identical) and Heartbeat (drifted) because the guard at tests/test_render_stream.py:286 checks only oc. Added class (d), a repo-wide sweep over all agent_workflows/*.py, a new child 01 to delete the re-forks, and a SYMMETRIC both-runners guard. PR-002 (F11): 'both driver suites pass' was the ONLY behavior-preservation proof and is insufficient: 95 oc tests vs 21 agy, and integrate_lane_branch (2nd-largest divergence) has ZERO references anywhere in tests/ while build_parser/extract_session_id/build_prompt have zero in the agy suites, so an agy regression in the biggest diverged symbols would leave both suites green. Added E-02 characterization baseline (on the repo's test_wtiso_characterization.py precedent) gated ahead of every reconciling child, with V-02 requiring a deliberate break-and-revert so the baseline cannot be decorative. PR-003: corrected F5's 'at least eight' to the measured FIVE. PR-004: recorded the stallfp-01 E-05 overlap (a strict subset of child 01). PR-005: named the runner suites with measured counts. OQ-01 (sequencing) remains OPEN and BLOCKING: maintainer priority call, no interactive channel in this run; its recommendation is now sharper since F10 identified a safer first slice.

- 2026-08-29 draft (opencode (its_direct/pt3-claude-opus-5-1m-us)): created.
- 2026-08-30 to-review (opencode (its_direct/pt3-claude-opus-5-1m-us)): graduated from backlog `dhuape` during the blocking-backlog graduation sweep. The item's divergence snapshot was RE-MEASURED rather than trusted, and it was materially understated: an AST comparison shows 37 of 72 shared symbols have drifted, not a handful, and the item's own "def-name comm" method could not see this because a drifted function still has the same name (F1). Two of the item's premises were corrected by measurement: the host-abstraction modules it nominates as the extraction home are a GENERATOR and an orchestration layer, not a process runner, while the actual generic worker runner it does not mention (`host_runner`) is the real precedent (F4). The dominant risk turned out not to be the refactor itself but SEQUENCING: 21 unexecuted plans already declare these two modules in Scope-Paths, and at least eight of them explicitly defer TO this item while landing duplicated code in both runners meanwhile (F5). That reframes this plan's central job and is why it is an orchestrator with an inventory-first gate rather than a single refactor child.

## Goal

Get to ONE implementation of everything that is not host-specific, without changing what either runner does and without colliding with the large body of approved work already queued against these files. The inventory comes first because the backlog item asks for it as a deliverable and because no honest child scope can be drawn until the common/specific/divergent partition is known.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

This orchestrator authors NO product code. Its execution work is the inventory deliverable and the whole-Set verification; the children carry every extraction.

### Task group 1: the inventory, then the Set

- [x] E-01 Produce the function-by-function INVENTORY the backlog item names as its first required deliverable, as a durable research artifact under `.aw/records/research/` created with `aw research new` (do not hand-name it). Classify EVERY top-level symbol in both runners as (a) COMMON, identical or trivially unifiable; (b) HOST-SPECIFIC, genuinely tied to one CLI's invocation, event stream, or binary resolution; (c) DIVERGED, present in both but drifted, which is the class the item calls a divergence bug and which measurement shows is the LARGEST class at 37 symbols; or (d) ALREADY-EXTRACTED, a symbol a NON-runner module already owns, so the runner copy is a RE-FORK to delete rather than a symbol to extract (see F10; this class is mandatory and a two-runner-only comparison is structurally blind to it). For each class (c) symbol, record which side is authoritative and WHY, with the evidence, since that judgment is the actual intellectual work of this Set and must not be improvised per-child later. Use a mechanical method, not reading: an AST comparison of per-symbol source, plus a host-token-normalized comparison to separate "differs only by host naming" from "differs substantively" (measurement showed only ONE symbol, `driver_actor`, differs by naming alone, so this distinction matters). The class (d) sweep must compare each runner symbol against ALL of `agent_workflows/*.py`, not just against the other runner. The inventory must state the measured totals and the HEAD they were taken at, because they will move.
  - Depends on: none
  - Expected outcome: a committed research artifact partitioning all symbols into the FOUR classes with a per-symbol authoritative-side decision and evidence for class (c) and a named owning module for class (d); the totals and their measurement HEAD are recorded; the method is reproducible by a reader.
  - Execution state: performed
  - Execution note: PERFORMED WITH ONE DECLARED GAP, and the gap is stated rather than papered over. Executed 2026-09-03 at HEAD `769989ce` as a DELIBERATELY BOUNDED slice: the maintainer authorized the inventory ALONE (read-only, no child authored, no runner touched) and directed that the sequencing gate be decided FROM this measurement rather than before it. Artifact: `.aw/records/research/20260903-rununify-00-tvnq50-runner-symbol-inventory-four-class-partition.findings.md` (id6 `tvnq50`), created with `aw research new --apply`, not hand-named. DELIVERED: the FOUR-class partition over all 88 shared plus 50 single-side symbols, with per-symbol line counts and `path:line` anchors, the repo-wide class (d) sweep, the reproducible method, and the totals with their HEAD. NOT DELIVERED, and this is the gap: a per-symbol authoritative-side decision for all 52 class (c) symbols. SIX carry a measured decision via a strict-call-superset signal (`run_queue`, `driver_begin`, `extract_session_id`, `parse_plan_file`, `dependency_status_detailed` -> opencode; `build_isolation_notice` -> antigravity). The other 46 are UNDECIDED because the mechanical signals are silent: last-touch dates are IDENTICAL on both sides for 51 of 52 symbols (the two files are habitually edited in one commit), so git recency has almost no discriminating power here, and deciding the rest requires reading 46 implementation pairs. Recording a guess per symbol would be worse than recording the gap, because a child would then reconcile to an unexamined "decision" believing it was evidenced. All 46 sit in the slice the sequencing gate defers anyway. THREE FINDINGS THAT CHANGE A DECISION: (1) the plan's counts are stale - 88 shared not 72, 52 diverged not 37, 34 common not 35, and the files are 6683/4819 lines not 3144/3143; (2) `Heartbeat` is ALREADY FIXED (`agy_runipd.py:43` imports it from `render_stream`), so child 01 must be re-scoped rather than executed, and the real re-fork list is five symbols including two the plan never named (`_read_id`, `_read_status`, owned by `selectors.py`, duplicated in BOTH runners); (3) `driver_actor`, the symbol the plan cites as the sole host-naming-only case that motivates the normalization step, is now STRUCTURALLY diverged, and the sole host-naming-only symbol is `print_status`. Also reframed: the "93 percent duplicated" claim is asymmetric - 90.4 percent of agy's definition lines sit in shared symbols but only 70.4 percent of oc's, and the host-specific split is 47 oc to 3 agy, so this is one shared program plus an opencode-specific extension rather than two copies of one program.

- [ ] E-02 Establish the CHARACTERIZATION BASELINE that makes this Set's behavior-preservation claim falsifiable, BEFORE any child reconciles anything. "Both driver suites green" is NOT currently sufficient proof and must not be relied on as-is: measured at `20c0fd7`, the opencode runner suites carry 95 tests while the agy runner suites carry 21, and the largest DIVERGED symbols are the least covered on the agy side (`integrate_lane_branch`, the second-largest divergence at 42 differing lines, has ZERO references anywhere in `tests/`; `build_parser` at 160 differing lines, `extract_session_id` at 22, and `build_prompt` at 34 have ZERO references in the agy suites). A reconciliation that silently changes agy behavior in exactly those symbols would leave both suites green. Follow the repo's existing precedent for this, `tests/test_wtiso_characterization.py`, which PINS behavior that exists so a later phase must deliberately come and invert the assertion. Write characterization tests that pin the CURRENT observable behavior of both hosts for every class (c) symbol a child will reconcile, prioritizing the ones with no agy coverage. This E-item writes TESTS ONLY and changes no runner logic, so it does not violate this orchestrator's no-product-code fence in spirit; if the repo's conventions make a test-only change count as product code, it becomes child 00 instead, executed before child 01.
  - Depends on: E-01
  - Expected outcome: a committed characterization suite that pins present behavior of both runners for the symbols children will touch, with the uncovered-symbol list from E-01 explicitly addressed; a reconciliation that changes agy behavior now FAILS a test instead of passing silently.
  - Execution state: pending

- [ ] E-03 After the children execute, verify the whole Set: everything classified COMMON in E-01 has exactly ONE implementation, each runner retains only its host-specific surface, every class (c) divergence was reconciled to the authoritative side recorded in E-01, every class (d) re-fork was deleted in favor of the owning module's definition, and NO runner behavior changed. The single-implementation check must be AST-level and REPO-WIDE across `agent_workflows/*.py`, not a pairwise check of the two runners: a pairwise check passes while a third copy sits in another module, which is exactly the state measured today (F10). The behavior-preservation proof is the load-bearing one, and it rests on E-02's characterization baseline plus both hosts' suites, NOT on the suites alone, because the suites are asymmetric enough today to miss an agy-side regression in the largest diverged symbols.
  - Depends on: E-01, E-02
  - Expected outcome: one implementation per common symbol (AST-verified, repo-wide); both driver suites green AND E-02's characterization suite green; every E-01 class (c) decision honored and every class (d) re-fork removed; no behavior change claimed or found; the opencode-only lifecycle features are present for both hosts or their absence is explicitly justified per-symbol.
  - Execution state: pending

## Child IPDs, sequence, and dependencies

The child breakdown is DELIBERATELY NOT FIXED HERE, and that is a decision rather than an omission. E-01's inventory determines the honest seams, and inventing child scopes before measuring which of the 37 drifted symbols are reconcilable would be exactly the guesswork this plan exists to avoid. What IS fixed is the shape and the constraints:

| Order | What it does | Depends on |
|---|---|---|
| 01 | **AUTHORED 2026-09-03 as `2r306y`** (`to-review`). Delete the class (d) ALREADY-EXTRACTED re-forks and make the anti-re-fork guard SYMMETRIC. RE-SCOPED against E-01's measurement, and this row was stale in two ways: `Heartbeat` is ALREADY FIXED (`agy_runipd.py:43`), so the `stallfp-01` overlap is resolved and must not be re-done; and the sweep found TWO re-forks this row never named (`_read_id`, `_read_status`, owned by `selectors.py`, re-forked in BOTH runners), which is why child 01 touches `oc_runipd` too. Real list: 5 symbols. | E-01 inventory |
| 02 | **AUTHORED 2026-09-03 as `818uru`** (`to-review`, `Item-Dependencies: executed:2r306y`). Extract the class (a) COMMON symbols into a new `runner_common.py`. COUNT CORRECTED: **34**, not 35 (33 AST-identical plus `print_status`, identical only after host-token normalization). The "pure move, verified by an identity assertion" framing HOLDS but a naive lift FAILS: four of the 34 call symbols OUTSIDE the set (`run_checked` -> `pinned_child_env`, `discover_plans` -> `parse_plan_file`, `save_state` -> `write_report`, `validate_manifest` -> `parse_dependency_token`), two of them class (c) DIVERGED, so those dependencies are INJECTED rather than imported. Also found: `DriverError` is a latent BUG, two distinct classes with a documented translation wrapper (`agy_runipd.py:87-93`), sequenced first as the highest-value single symbol. | 01 |
| 03+ | One child per COHESIVE GROUP of class (c) DIVERGED symbols, each reconciling to the authoritative side E-01 recorded, and each REQUIRING E-02's characterization tests to exist for the symbols it touches before it may reconcile them. Grouped by seam (for example prompt construction, selector expansion, run initialization), NOT one child per symbol and NOT one giant child. | 02, E-02 |
| last | Close the named feature-parity gaps (the opencode-only lifecycle/self-finalize surface, and any agy-only helper E-01 classifies as common) in the direction E-01 decided. | 03+ |

Hard constraints every child inherits, stated once here:
- A child may NOT change behavior. Every reconciliation goes to an EXISTING behavior on one side, chosen and justified in E-01.
- A child must land in BOTH runners or in neither. A one-sided extraction recreates the divergence it is removing, and a one-sided GUARD lets it recur silently: `render_stream` was extracted a Set ago with an oc-only guard, and agy re-forked four of its symbols with nothing noticing (F10). Every guard a child adds must assert over BOTH runners.
- A child may not reconcile a class (c) symbol that E-02 has not pinned with a characterization test, because for several of the largest diverged symbols the existing suites cannot detect an agy-side behavior change at all (F11).
- No child may exceed one cohesive seam. `execute_item` alone is 572 lines and is the single largest shared symbol; it may need a child to itself, and if E-01 shows it must be split further, split it.
- Children are SEQUENTIAL, not parallel, because they all edit the same two files. Declaring them parallel would guarantee the merge conflicts this repo is already suffering from.

## Completion criteria (the whole Set is done only when)

- Every symbol E-01 classified COMMON has exactly ONE implementation, verified at the AST level rather than by grep (the `2c122z` plan-review found a `grep "return True"` proof that could never fail; do not repeat that class of unfalsifiable evidence), and verified REPO-WIDE across `agent_workflows/*.py` rather than pairwise between the two runners, since a pairwise check passes while a third copy sits in another module (F10).
- Each runner contains only its host-specific surface plus imports of the shared library.
- Every class (c) divergence was reconciled to the side E-01 named authoritative, with the reconciliation recorded.
- Every class (d) re-fork was removed in favor of the module that already owns the symbol, and no runner carries an inline copy of anything `render_stream` or another extracted module owns.
- The opencode-only lifecycle features either exist for both hosts or carry a per-symbol justification for remaining host-specific.
- NO behavior changed. Both hosts' driver suites pass, AND E-02's characterization suite passes. The suites alone are explicitly NOT sufficient here (F11): several of the largest diverged symbols have zero agy-side coverage, so the characterization baseline is what makes this criterion falsifiable.
- The shared library is imported by both runners, and a test asserts that NEITHER runner defines a symbol the shared library owns. That guard must be SYMMETRIC over both runners and must also cover the previously extracted modules; the existing `tests/test_render_stream.py:286` guard checks only `oc_runipd`, which is exactly why agy's re-fork went unnoticed.

## Cross-IPD validation

- CID-1: exactly one implementation per common symbol, AST-verified REPO-WIDE across `agent_workflows/*.py` (not pairwise between the two runners, which cannot see a third copy: F10).
- CID-2: both drivers import the shared library; neither redefines a shared symbol; the guard asserting this is symmetric over both runners and also covers previously extracted modules such as `render_stream`.
- CID-3: every child landed symmetrically; no child left one host behind.
- CID-4: no behavior change; both driver suites green at every child boundary, not merely at the end, AND E-02's characterization suite green at every child boundary. Green suites alone do not discharge CID-4 (F11).

## Project conventions discovered (Step 0)

- The runners bypass the host-abstraction layer entirely, exactly as the item says: `oc_runipd.py` and `agy_runipd.py` contain ZERO references to `host_adapters`, `host_launchers`, `host_capability_registry`, or `host_runner`.
- But the item's nominated extraction home is WRONG, which changes the design. `host_adapters` describes itself as "a PURE generator" that emits Agent Skills packages and adapter metadata, and `host_launchers` is "thin launchers over the generic `host_runner`" for the Set coordinator. Neither is a process runner for a driver's turn loop. Folding driver logic into them would put runtime behavior inside a generator.
- The RIGHT precedent exists and the item does not mention it: `agent_workflows/host_runner.py` is a 349-line "generic structured host worker runner" whose docstring states its purpose as "One evidence-gated worker interface the Set coordinator uses to start, monitor, time-out, cancel, and collect a VALIDATED terminal envelope from an isolated task, on any supported host, without duplicating semantics per host". That is the same problem statement as this item, solved once for a different caller. Study it before choosing the shared module's shape; a NEW module (the item's `runipd_core`/`host_runner` candidate) is likely correct, but it should follow this one's conventions.
- The repo has a NAMED precedent for taking a narrow slice instead of waiting for this refactor: plan `97df1z` extracts ONE predicate into a new `agent_workflows/plan_readiness.py` shared by both runners, and records in its own Deferred section that full unification is "tracked by backlog `dhuape`; this plan shares ONLY this predicate to fix the bug without taking that refactor". That module does not exist yet, so `97df1z` will CREATE the first shared-runner-helper module. This Set should extend that module or explicitly supersede its placement, not open a third home.
- The `terminate_process` copies are near-identical in both runners with escalating SIGINT/SIGTERM/SIGKILL over a process group, and `runstop` Phase 0 already records that they are byte-identical apart from a docstring line, which is why its single-implementation check must be repo-wide. Coordinate: that plan and this Set are both trying to de-duplicate the same function.
- Both runners are under ACTIVE concurrent edit. At authoring time a live `aw oc run wtiso` process owned five lane worktrees, and uncommitted changes to both runner modules and both runner test modules were present in the working tree from another session.

## Findings

| # | Sev | Location | Finding | Evidence |
| --- | --- | --- | --- | --- |
| F1 | HIGH | both runner modules | THE ITEM UNDERSTATES THE DIVERGENCE, and its method is why. It reports "~954 changed lines" and a handful of present-in-one-not-the-other functions found by "def-name comm". But a name comparison cannot see a function that exists in both and has DRIFTED. An AST comparison of per-symbol source at `c7f41b9` shows 72 shared symbols of which only 35 are byte-identical and 37 have diverged. So the divergence class the item calls a bug is the LARGEST class, not an exception list of five. | AST per-symbol digest comparison: `shared symbols: 72 / BYTE-IDENTICAL: 35 / DIVERGED: 37`; raw line diff of the two committed files: 1229 differing lines |
| F2 | HIGH | both runner modules | THE DUPLICATION IS ~93 PERCENT, which sizes the work honestly. Of `oc_runipd`'s 2843 lines inside top-level definitions, 2637 sit in symbols that also exist in agy; only 206 lines are opencode-only and 294 are agy-only. So this is not a refactor with a large shared core, it is two copies of one program with small host-specific tips. | measured line sums per symbol class at `c7f41b9` |
| F3 | HIGH | shared symbols | THE WORK CONCENTRATES IN A FEW HUGE FUNCTIONS, so child sizing must follow the measurement rather than the symbol count. The largest shared symbols are `execute_item` at 572 lines, `build_parser` at 201, `expand_selectors` at 173, `initialize_run` at 149, and `run_queue` at 117 (opencode side), and `run_queue` differs by 38 lines between hosts while `integrate_lane_branch` differs by 20. A child per symbol would be absurd for the small ones and impossible for `execute_item`. | per-symbol line counts and deltas at `c7f41b9` |
| F4 | HIGH | `host_adapters.py`, `host_launchers.py`, `host_runner.py` | THE ITEM'S PROPOSED HOME IS THE WRONG LAYER, and the right precedent is a module the item never mentions. `host_adapters` is self-described as "a PURE generator" for skills/adapter metadata; `host_launchers` is orchestration over `host_runner`. Neither runs a driver turn. Meanwhile `host_runner.py` already solves "one worker interface on any supported host without duplicating semantics per host" for the Set coordinator, which is precisely this item's goal for a different caller. Folding driver logic into the generator would be a layering violation; ignoring `host_runner`'s conventions would duplicate a solved design. | the three modules' docstrings; zero imports of any of them in either runner |
| F5 | HIGH | `.aw/records/plans/pending/` | SEQUENCING IS THE DOMINANT RISK, not the refactor. TWENTY-ONE unexecuted plans declare `oc_runipd.py` or `agy_runipd.py` in `Scope-Paths` (17 approved, 2 reviewed, 2 to-review), including the six-child `runstop` Set, the six-child `wtiso` Set, the four-plan `lanetruth` Set, and several singletons. FIVE of those 21 explicitly defer to `dhuape` by name while landing duplicated code in both runners in the meantime, with phrases like "this child lands the same call in both, no de-duplication" and "Driver unification (backlog `dhuape`)". So every day this Set waits, the duplication GROWS by design, and every day it runs first, it invalidates 21 plans' line citations and scope fences. | measured at `20c0fd7`: 21 plans matched on `Scope-Paths`; the deferral text quoted from `runstop-01`, `runstop-03`, `runstop-06`, `fullauto-01`, `stallfp-01` (all five both declare a runner AND name `dhuape`); `runstop-00` also defers by name but declares only its own plan file in Scope-Paths |
| F6 | MED | plan `97df1z` | The FIRST shared-runner-helper module is already scoped but does not exist yet: `97df1z` will create `agent_workflows/plan_readiness.py` and share exactly one predicate between the drivers, recording that full unification belongs to this item. This Set must extend or supersede that placement rather than opening a third location, or the repo ends up with a shared-helper module per bug fix, which is the same drift at a different granularity. | `97df1z` Scope-Paths and Deferred section; `plan_readiness.py` absent at `c7f41b9` |
| F7 | MED | `runstop-01` (`2ouj70`) | An OVERLAP that must be resolved rather than raced: `runstop` Phase 0 also de-duplicates `terminate_process`, recording that both runners carry byte-identical copies and that its single-implementation check must therefore be repo-wide. Two plans independently de-duplicating the same function will conflict. | `runstop-01`'s conventions section |
| F8 | MED | the item's feature list | The item's specific gap list VERIFIED as still accurate for names: opencode-only are `action_for`, `finalize_orchestrator`, `_read_kind`, `_set_children_all_executed`, `run_opencode`; agy-only are `render_agy_event`, `resolve_agy`, `run_agy_turn`, `_one_line`, `_strip_ansi`, plus `Heartbeat` and `Palette` classes the item does not list. Note `run_opencode` and `run_agy_turn` are the genuinely host-specific turn drivers and are correctly NOT common; the item's framing of `run_opencode` as a missing feature is a category error. | symbol-set difference at `c7f41b9` |
| F9 | LOW | working tree at authoring time | Both runner modules AND both runner test modules had uncommitted changes from a concurrent session, and a live driver process owned five lane worktrees. Any executor must assume contention on exactly these files. | `git status` and `ps` at authoring time |
| F10 | HIGH | `agy_runipd.py`, `render_stream.py`, and 8 other modules | THE PARTITION IS MISSING A FOURTH CLASS, and the omission is structural rather than cosmetic: a symbol can be duplicated in a NON-runner module that already owns it, which a two-runner comparison cannot see by construction. This state exists TODAY and has a directly relevant precedent. The EXECUTED `runnernorm-01` (`dg28i9`) already extracted the render layer into `agent_workflows/render_stream.py`, and `oc_runipd` imports it (`oc_runipd.py:39-51`) while `agy_runipd` NEVER imports `render_stream` and instead carries its own `Palette` (byte-identical to the shared one), `_one_line` (byte-identical), `_strip_ansi` (byte-identical), and `Heartbeat` (drifted, 49 lines vs 51). Those four are class (a)/(c) under the plan's three-class scheme, which would send a child to "extract" a symbol that was already extracted a Set ago. Worse, the guard test that should have caught this is ONE-SIDED: `tests/test_render_stream.py:286` asserts only `oc_runipd` has no inline definition, so agy re-forked silently. Nine further runner symbols share a name with a non-runner module's symbol and must be triaged in the same sweep (`PlanRecord` vs `plans.py`, `should_color` vs `term.py`, `sha256_file`/`_run_git` vs `layout_inventory.py`, `validate_manifest` vs `benchmark_manifest.py`, `load_state` vs `leak_sanitizer_config.py`, plus `_read_id`/`_read_status`/`build_parser`/`main` which are likely legitimate same-name-different-purpose and must be classified, not assumed). This also proves the anti-re-forking guard in the completion criteria must be SYMMETRIC and repo-wide, since the existing asymmetric one is precisely how this happened. | measured at `20c0fd7`: `grep -n "class Heartbeat\|class Palette" agent_workflows/*.py` finds both in `agy_runipd.py` AND `render_stream.py`; zero `render_stream` matches in `agy_runipd.py`; AST comparison of the runner copies against all `agent_workflows/*.py`; `tests/test_render_stream.py:286-292`; executed plan `20260825-runnernorm-01-dg28i9` |
| F11 | HIGH | `tests/`, both runner suites | "BOTH DRIVER SUITES PASS" IS NOT YET A SUFFICIENT BEHAVIOR-PRESERVATION PROOF, which matters because it is the ONLY proof this Set's central contract rests on. The suites are severely asymmetric: 95 tests for the opencode runner (`test_oc_runipd.py` 2924 lines + `_cli` + `_shim`) versus 21 for agy (`test_agy_runipd_cli.py` 708 lines + `_shim`). The asymmetry is worst exactly where the reconciliation risk is highest: `integrate_lane_branch` (the 2nd-largest divergence, 42 differing lines) has ZERO references in ANY file under `tests/`; `build_parser` (largest divergence, 160 differing lines), `extract_session_id` (22), and `build_prompt` (34) have ZERO references in the agy suites. So a child could reconcile agy's `integrate_lane_branch` to the opencode behavior, change agy's real integration semantics, and see both suites stay green. The repo already has the right pattern for closing this (`tests/test_wtiso_characterization.py`, which pins existing behavior so a later phase must deliberately invert the assertion), and this Set must use it BEFORE reconciling rather than trusting the suites. | measured at `20c0fd7`: `python3 -m pytest` collect + run gives `95 passed` for the three oc runner files and `21 passed` for the two agy files; per-symbol reference counts in the agy suites are 0 for `build_parser`/`extract_session_id`/`build_prompt`; `grep -rl integrate_lane_branch tests/` returns nothing |

## Proposed changes (ordered, validatable)

1. Measure and publish the four-class inventory with per-symbol authoritative-side decisions (E-01).
2. Sequencing against the queued plans is DECIDED (OQ-01: interleaved; gate RE-POINTED 2026-09-03 from `wtiso` to `lanectn`, see the SEQUENCING GATE below). Take the re-fork deletions, then the class (a) extraction; hold the diverged slice until `lanectn` lands.
3. Establish the characterization baseline that makes behavior-preservation falsifiable (E-02).
4. Delete the class (d) re-forks and make the existing one-sided guard symmetric (child 01).
5. Extract the class (a) common symbols as a provably behavior-neutral child (child 02). COUNT RE-MEASURED 2026-09-03: **34** (33 AST-identical plus `print_status`, identical after host-token normalization), not the 35 measured at `c7f41b9`.
6. Reconcile the class (c) diverged symbols in cohesive, sequential groups, each to a decided authoritative side, each gated on its characterization tests existing. COUNT RE-MEASURED 2026-09-03: **52**, not 37; they diverged further while this plan sat.
7. Close the feature-parity gaps in the decided direction.
8. Verify one-implementation-per-common-symbol at the AST level repo-wide, both suites green, and the characterization baseline green (E-03).

## Deferred / out of scope (with reason)

- CHANGING BEHAVIOR is out of scope for the entire Set. This is de-duplication; any behavior question a child uncovers becomes a separate backlog item rather than an opportunistic fix, because a refactor that also changes behavior cannot be verified by "both suites still pass".
- A unified top-level `aw run` or `aw run stop` facade is out of scope, even though `runstop`'s orchestrator defers that verb to this item. Unifying the LIBRARY does not require unifying the CLI surface, and merging the two user-facing commands is a separate, user-visible decision.
- The `wtiso` Phase 3 resolver consolidation, which also touches both runners, stays with that plan. This Set must not absorb it.
- `terminate_process` de-duplication overlaps with `runstop` Phase 0 (F7). Whichever executes second must find the function already shared; do not both extract it. Measured at `20c0fd7`, the two copies differ by exactly ONE line (opencode carries a docstring agy lacks), confirming `runstop-01`'s characterization of them as byte-identical apart from a docstring.
- The `Heartbeat` re-fork overlaps with `stallfp-01` (`kaga7s`) E-05, which already scopes replacing `agy_runipd.Heartbeat` with an import of `render_stream.Heartbeat` AND extending the one-sided guard in `tests/test_render_stream.py` to cover agy. That is a strict subset of this Set's child 01. Do NOT both do it: if `stallfp-01` executes first, child 01 must find `Heartbeat` already imported and confine itself to the remaining class (d) symbols (`Palette`, `_one_line`, `_strip_ansi`, plus whatever E-01's repo-wide sweep adds); if child 01 executes first, `stallfp-01` E-05 becomes a no-op and must be recorded as such rather than silently re-done.

## Scope check

- Over-scope: none. This orchestrator writes a research artifact, a characterization test suite, and its own plan record; it declares no product MODULE path, which is correct for an orchestrator that authors no product code. The `tests` path is declared deliberately for E-02: a characterization suite pins existing behavior and changes none, and adding it here rather than in a child is what lets EVERY child inherit a falsifiable baseline instead of each child inventing its own. If the repo's conventions treat any `tests/` change as product work, E-02 becomes child 00 and this path is dropped; E-02 states that fallback.
- Under-scope: THE CHILD PLANS ARE NOT YET WRITTEN, deliberately. Their scopes depend on E-01's measurement, and pre-committing them would be guesswork. This is an explicit, recorded gate: the Set cannot proceed past E-01 until the children are authored from the inventory and reviewed. A reviewer should hold this plan to that, and should NOT approve any child that was written before the inventory existed.
- The children WILL declare `agent_workflows/oc_runipd.py`, `agent_workflows/agy_runipd.py`, the new shared module, and both runner test modules. That fence must be stated per child, not inherited vaguely.

## Required tests / validation

- E-01's inventory is validated by REPRODUCIBILITY, not by assertion: a reader must be able to re-run the stated method and get the stated partition. Include the method (AST per-symbol comparison plus host-token normalization) and the measurement HEAD in the artifact.
- Each child must show both hosts' driver suites green BEFORE and AFTER its change, since behavior preservation is the whole contract. The suites are `tests/test_oc_runipd.py` + `tests/test_oc_runipd_cli.py` + `tests/test_oc_runipd_shim.py` (measured `95 passed` at `20c0fd7`) and `tests/test_agy_runipd_cli.py` + `tests/test_agy_runipd_shim.py` (measured `21 passed` at `20c0fd7`). Paste counts for both.
- GREEN SUITES ARE NECESSARY BUT NOT SUFFICIENT, and any child that treats them as sufficient has not validated its change (F11). The 95-vs-21 asymmetry means the agy side of the largest diverged symbols is effectively unguarded, so each child must ALSO show E-02's characterization tests for the symbols it touched, green before and after.
- CID-1's proof must be AST-based AND repo-wide. A grep for a symbol name is not proof of single implementation, because a name can legitimately appear in an import, a call, or a docstring. A pairwise two-runner comparison is also not proof, because a third copy in another module passes it, which is the state measured today (F10).
- The anti-re-forking guard must be a TEST, not a convention: assert that neither runner module defines a top-level symbol the shared library owns, in BOTH directions, and extend the same guard to the already-extracted modules. Without it, the next bug fix re-forks a helper and nothing notices; that is not hypothetical, it already happened to `render_stream` (F10).
- INVOKE THE SUITE BARE: `python3 -m pytest` and `python3 -m pytest -m ""` (or `make test` / `make test-all`). Do NOT add `-n auto` or a second `-q`. Note that this repo's pytest config enables `xdist` and random ordering, so a bare per-file invocation prints its count on the final line but `-q -p no:randomly` may suppress it; paste whatever the bare invocation actually printed.
- BASELINE IS A MEASUREMENT, NOT A CONSTANT. Fast suite during this sweep at `df731f1`: `2880 passed, 3 skipped, 4 xfailed`. Both runner modules and both runner test modules were dirty from a concurrent session at authoring time, so expect your baseline to differ; take your own with its HEAD.
- `aw check-local-leaks . --agent` clean; `aw ipd lint --phase pre-transition` conforming for this plan and every child.

## Spec / documentation sync

- No existing spec pins the two-runner structure, so no spec text change is forced. However, spec `25kzda` (aw-run deterministic run and verify) governs runner behavior generally; the executor should record that this Set changed structure only and left that spec's requirements untouched.
- If the shared module lands as a new file, its module docstring must state the same single-implementation rule this orchestrator enforces, so a future contributor adding a runner helper knows where it goes. Follow `host_runner.py`'s docstring conventions (F4).
- The `dhuape` item's own framing that the host-abstraction layer is the natural home should be corrected in the record (F4), so a later reader does not re-derive the wrong plan from the closed item.

## Open questions

### OQ-01: Does this Set run BEFORE or AFTER the 21 queued plans that already declare these modules?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: RESOLVED 2026-08-30 by maintainer decision, recorded via the graduation sweep's interactive prompt: INTERLEAVED, as recommended. The maintainer chose to take the safe slices NOW and defer the 37 diverged symbols until the `runstop`, `wtiso`, and `lanetruth` Sets have landed. Combined with the review's F10/F11 findings this fixes the order: child 01 (the class (d) re-fork deletions) first, then the 35 byte-identical extraction, then STOP until those Sets land; the 37 diverged symbols additionally stay gated behind E-02's characterization baseline. The gate below is DISCHARGED for the deferred-slice portion only: children reconciling diverged symbols still may not be authored until the named Sets have landed. The original framing and evidence are preserved below for the record.
  PRIOR RATIONALE (retained for the record): THIS WAS THE ONE DECISION THIS PLAN COULD NOT MAKE FROM REPOSITORY EVIDENCE, because it is a priority and risk-appetite call, which is the maintainer's to make. The evidence is laid out so the decision is informed. Running this Set FIRST invalidates the line citations, scope fences, and in some cases the E-item structure of 21 unexecuted plans, including three multi-child Sets that are already approved and one that is mid-flight in a LIVE run; the cost is a large re-review burden and a real chance of silently breaking an approved plan's assumptions. Running it LAST means the duplication keeps GROWING by design, because five of those plans explicitly land the same code in both runners and defer de-duplication to this item, so the 37-symbol divergence will be larger and the reconciliation decisions harder. A THIRD option exists and may be best: run it INTERLEAVED, deferring the 37 diverged symbols until the `runstop`, `wtiso`, and `lanetruth` Sets have landed while taking the safe slices now.
  RECOMMENDATION (STRENGTHENED by the review's F10/F11 measurements): the interleaved option, and the review has now identified WHICH slice to take first, which the original framing did not have. Take child 01 (the class (d) re-fork deletions) FIRST, ahead of even the 35 byte-identical extraction. It is the safest work in the Set by a clear margin: those symbols are already owned by an existing module that `oc_runipd` already imports, three of the four agy copies are byte-identical to the owner, no queued plan reasons about them, and one queued plan (`stallfp-01` E-05) is already doing a strict subset of it. Then take the 35 byte-identical symbols. Defer the 37 diverged symbols, which are exactly what those Sets are actively editing, AND gate them behind E-02's characterization baseline, because F11 shows the current suites cannot detect an agy-side regression in the largest of them. The maintainer should confirm or override; this plan does not proceed on a guess about their sequencing priorities.
  NOTE FOR WHOEVER ANSWERS: this plan record was itself being processed by a live `aw oc run` at review time (`aw oc run 20260829-rununify-00-5e4sb6 ...`), so confirm no child was auto-authored from the pre-review version of the child table before answering.

### OQ-02: Where does the shared library live, and does it absorb `plan_readiness.py`?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: A NEW dedicated module, following `host_runner.py`'s conventions, and it SUBSUMES the placement of `plan_readiness.py` rather than coexisting with it. Resolved from evidence. The item's candidates `host_adapters`/`host_launchers` are eliminated by F4: they are a generator and an orchestration layer, so driver runtime logic does not belong in them. `host_runner.py` is the right precedent but the wrong home, because it serves the Set coordinator's worker-envelope contract rather than a driver's turn loop; copy its conventions, do not extend it. And `plan_readiness.py` (created by plan `97df1z`, F6) is the first shared-runner-helper module, so the correct end state is ONE shared runner library that this Set either absorbs it into or explicitly designates as part of. Leaving both is how a repo acquires a shared-helper module per bug fix, which is the same drift at finer granularity. E-01 must state which of absorb-or-designate it chose.

### OQ-03: Which side wins for each of the diverged symbols? (52, re-measured 2026-09-03; 37 at authoring)

- Blocking: no
- Status: deferred
- PARTIALLY ANSWERED BY E-01, 2026-09-03: 6 of 52 have a measured authoritative side (`run_queue`, `driver_begin`, `extract_session_id`, `parse_plan_file`, `dependency_status_detailed` -> opencode; `build_isolation_notice` -> antigravity), from a strict call-superset test after host-token normalization. The other 46 are UNDECIDED and stay deferred: `git log -L` returns the SAME last-touch date on both sides for 51 of 52 symbols (both files are edited in one commit), so recency cannot decide them, and the rest needs 46 implementation pairs read. V-01 is recorded `failed` for exactly this gap rather than claiming a pass. See research `tvnq50`.
- Owner: E-01 of this plan (trigger: the inventory's per-symbol authoritative-side decisions)
- Resolution or deferral rationale: DELIBERATELY DEFERRED TO E-01, which is this plan's first execution item, rather than guessed here. This is not an evasion: a per-symbol authoritative-side decision requires reading 37 pairs of implementations, which is the inventory's whole purpose and is exactly what the backlog item asks for as its deliverable. What IS decided here is the RULE those decisions must follow: reconcile to an EXISTING behavior on one side, never to a new synthesis, and record the evidence per symbol. The default presumption, to be overridden only with a stated reason, is that the opencode side is authoritative for lifecycle and finalize logic (because the item establishes and measurement confirms that recent lifecycle work landed there and agy never received it) and that neither side is presumed authoritative for anything else.

## Validation and cross-check (verify before reporting the Set complete)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: paste the created research artifact's path, showing it was created by `aw research new` rather than hand-named. Paste the FOUR-class partition with per-symbol entries and the measured totals with the HEAD they were taken at. Paste the reproducible method and a transcript of it running. For class (c), paste at least the authoritative-side decision and its one-line evidence for the five largest diverged symbols, and confirm all 37 carry a decision. For class (d), paste the owning module for each re-forked symbol and confirm the sweep compared against ALL of `agent_workflows/*.py` rather than only the other runner; the partition MUST account for agy's `Palette`/`_one_line`/`_strip_ansi`/`Heartbeat` as class (d) owned by `render_stream`, and if it does not, the sweep was not repo-wide and this item FAILS. State which of absorb-or-designate you chose for `plan_readiness.py` per OQ-02. An inventory that classifies symbols without deciding authority for class (c) does NOT satisfy this item.
  - Observed evidence: **PARTIAL, AND THEREFORE RECORDED `failed` RATHER THAN `pass`.** This V-item states its own bar explicitly - "An inventory that classifies symbols without deciding authority for class (c) does NOT satisfy this item" - and 46 of 52 class (c) symbols carry no authoritative-side decision. Marking this `pass` would be a false claim against a bar the plan wrote precisely to prevent it. What IS satisfied, and what is not:

    SATISFIED. Artifact path, tool-created rather than hand-named:
```
$ aw research new --kind findings --slug runner-symbol-inventory-four-class-partition --set rununify ... --apply
wrote <repo>/.aw/records/research/20260903-rununify-00-tvnq50-runner-symbol-inventory-four-class-partition.findings.md
next step (informational): run `aw research index` to refresh the manifest
```
    The FOUR-class partition with per-symbol entries and `path:line` anchors is in the artifact, with the measurement HEAD recorded as `769989ce` in the body. Measured totals:
```
shared: 88 {'a-common-identical': 33, 'a-common-hostnaming': 1, 'c-diverged': 52, 'd-already-extracted': 2}
oc-only: 47 {'b-host-specific-oc': 47}
agy-only: 6 {'d-already-extracted': 3, 'b-host-specific-agy': 3}
```
    The method is recorded and reproducible (AST fingerprint via `ast.dump(ast.parse(ast.unparse(node)), include_attributes=False)`, then a host-token-normalized second pass, then a repo-wide class (d) sweep).

    CLASS (d) SATISFIED, AND THE `render_stream` REQUIREMENT MET WITH ONE CORRECTION THE V-ITEM COULD NOT ANTICIPATE. The sweep compared every runner symbol against ALL of `agent_workflows/*.py`, not just the other runner:
```
re-forks (shared):
  _read_id      oc:  3L agy:  3L  oc matches selectors.py(identical); agy matches selectors.py(identical)
  _read_status  oc:  3L agy:  3L  oc matches selectors.py(identical); agy matches selectors.py(identical)
re-forks (single-side):
  Palette       agy: 20L  matches render_stream.py(identical)
  _one_line     agy:  6L  matches render_stream.py(identical)
  _strip_ansi   agy:  2L  matches render_stream.py(identical)
```
    `Palette`/`_one_line`/`_strip_ansi` ARE accounted for as class (d) owned by `render_stream`, as demanded. `Heartbeat` is NOT, and its absence is the correct result rather than a failed sweep: it has already been fixed while this plan sat, so it is no longer a re-fork to find. Proof:
```
$ grep -n "render_stream" agent_workflows/agy_runipd.py | head -3
34:# `render_stream` silently did not reach `aw agy run`. It is now imported, like `Statusline`,
43:from agent_workflows.render_stream import Heartbeat as Heartbeat
```
    Two re-forks the plan never named were found instead (`_read_id`, `_read_status`, owned by `selectors.py`, duplicated in BOTH runners), which is what a repo-wide sweep is for.

    OQ-02 SATISFIED: **DESIGNATE, not absorb**, and forced by evidence rather than preference. `plan_readiness.py` has NON-RUNNER consumers, so absorbing it into a runner library would make the schema and status-transition layers depend on a driver runtime module:
```
$ grep -rln "plan_readiness" agent_workflows/ --include=*.py
agent_workflows/agy_runipd.py
agent_workflows/oc_runipd.py
agent_workflows/status_set.py
agent_workflows/ipd_schema.py
```

    NOT SATISFIED, WHICH IS WHY THIS ITEM FAILS. The V-item demands the authoritative-side decision for the five largest diverged symbols plus a decision for ALL of them. SIX symbols carry a measured decision, derived from a strict call-superset test after host-token normalization:
```
symbol                          superset side   calls present on that side only
run_queue                       opencode        StreamTracker, _set_children_all_executed, finalize_orchestrator
driver_begin                    opencode        begin_baseline_env
extract_session_id              opencode        startswith
parse_plan_file                 opencode        _read_kind
dependency_status_detailed      opencode        dependency_target_id6, edge_satisfied, parse_dependency_token
build_isolation_notice          antigravity     _shared
```
    Of the five LARGEST diverged symbols (`run_queue` +70, `build_parser` +30, `build_isolation_notice` +26, `execute_item` +20, `integrate_lane_branch` +20), only THREE are decided; `build_parser` and `execute_item` are not, and neither are 44 others. WHY, measured rather than asserted: `git log -L` per symbol region returns the SAME last-touch date on both sides for 51 of 52 symbols (only `main` differs, oc 2026-09-01 vs agy 2026-08-30), because the two files are habitually edited in one commit, so git recency has almost no discriminating power here. Deciding the remaining 46 requires reading 46 implementation pairs, which this bounded read-only slice was not authorized to undertake. Recording a guess per symbol would be WORSE than recording the gap, because a child plan would then reconcile to an unexamined "decision" while believing it was evidenced. All 46 sit inside the slice the sequencing gate defers.

    SCOPE NOTE: the maintainer authorized the INVENTORY ALONE on 2026-09-03 (read-only, no child authored, no runner touched), and directed that the sequencing gate be decided FROM this measurement. So this item is `failed` against its full bar by DESIGN of that bounded authorization, not by an execution defect, and E-02/E-03 remain `pending`.
  - Result: failed

- [ ] V-02 validates E-02
  - Required evidence: paste the characterization suite's path and its passing output with the HEAD. For each symbol E-01 listed as having zero agy-side coverage (measurement at `20c0fd7` says at least `integrate_lane_branch`, `build_parser`, `extract_session_id`, `build_prompt`), paste the specific test that now pins its CURRENT behavior on the agy side, or a per-symbol justification for why pinning it is impossible. Then prove the baseline is load-bearing rather than decorative: deliberately alter one pinned agy behavior, paste the resulting FAILURE, and revert it. A characterization suite that passes both before and after such an alteration is not a baseline and this item FAILS.
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: paste the AST-level duplicate-symbol check output proving one implementation per common symbol, explicitly confirm it is AST-based rather than grep-based, and confirm its scope was REPO-WIDE across `agent_workflows/*.py` rather than a pairwise runner comparison. Paste both hosts' driver suite results with counts and their HEAD, and E-02's characterization suite passing. Paste the anti-re-forking TEST passing, and show it FAILING when a shared symbol is deliberately redefined in EACH runner in turn (both directions, since the one-sided version of this guard is what allowed F10). Paste a per-symbol confirmation that every class (c) reconciliation matched E-01's recorded decision, and for any that did not, the recorded reason. Confirm every class (d) re-fork is gone by pasting a check that `agy_runipd` no longer defines any symbol `render_stream` owns. Finally, state plainly that NO behavior changed and back it with the suites PLUS the characterization baseline rather than assertion; if any behavior did change, this item FAILS and the change belongs in its own plan.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required for this orchestrator (3 E-leaves, 1 task group). Each E-leaf is one concern executable in one focused pass: E-01 measures and partitions, E-02 pins current behavior, E-03 verifies the assembled Set. Note for the reviewer: the SET is large, and its right-sizing is enforced through the child constraints in the child table rather than here, because the child boundaries are legitimately unknown until E-01 measures them.
- Density advisories (`IPD-Z602`) fire on E-02 and E-03 at the review-finalize checkpoint. They were EVALUATED, not dismissed: in both cases the clauses the heuristic counts are the measured JUSTIFICATION for a single deliverable, not multiple deliverables. E-02 produces exactly one artifact (a characterization suite) and E-03 performs exactly one activity (verify the assembled Set) with one V-item each. Splitting either would separate a claim from the evidence that makes it falsifiable, which is the opposite of the intent. The advisories are informational and do not affect the conforming disposition (`aw ipd lint --phase review-finalize` exits 0).

SEQUENCING GATE, RE-POINTED 2026-09-03 (maintainer ruling). The gate defers the diverged slice until `lanectn` has landed AND E-02's characterization baseline exists. The safe slice is authorized now, in this order: child 01 (the class (d) re-fork deletions), then the class (a) byte-identical extraction. An executor reaching the diverged slice must STOP and confirm `lanectn` has landed.

WHY THE PREVIOUS WORDING WAS UNUSABLE, kept because a reader will otherwise re-derive it: the 2026-08-30 INTERLEAVED ruling deferred the diverged slice until `runstop`, `wtiso` and `lanetruth` landed. `runstop` (7 plans) and `lanetruth` (4) are all `executed/`, and their code is in BOTH runners at this HEAD, so they can no longer collide with anything and their symbols are simply part of E-01's measurement. `wtiso` is 7 of 9 `superseded`: 6 plans were retired UNLANDED 2026-09-02 and the containment design was PORTED to `lanectn` (7 plans, `reviewed`, unexecuted), which edits both runner modules. So the old gate waited on a Set that cannot arrive while its actual successor was still ahead of it.

WHY THIS IS A RE-POINT AND NOT A LOOSENING, measured in E-01's inventory (research `tvnq50`, HEAD `769989ce`): the safe slice and `lanectn` are DISJOINT. `lanectn`'s 7 plans name NONE of the 39 safe-slice symbols (the 5 class (d) re-forks or the 34 class (a) commons) even once. The runner symbols they DO name are `reconcile_disposition`, `execute_item`, `build_isolation_notice` and `build_prompt`, and all four are class (c) DIVERGED, i.e. already inside the deferred set. The deferred set therefore already covers everything `lanectn` touches, and the safe slice was never in contention.

REJECTED ALTERNATIVE, recorded so it is not re-proposed: editing `runstop`/`wtiso`/`lanetruth` to be resilient to a merged library. Useless, because all 11 of those plans are already terminal (`executed/` or `superseded/`), so there is no document to change and the repo forbids adding commits to an executed plan. It would also invert this Set's contract by changing shipped, verified callers to accommodate a library that does not exist yet, when the de-duplication is required to be behavior-preserving.

CHILD 01 EXECUTION CONSTRAINT (live at the time of this ruling): another session held UNCOMMITTED edits to `render_stream.py` and `tests/test_render_stream.py`, the module owning three of the five re-forks. Their change was unrelated (progress-bar percentage formatting), but it is why this Set's existing execution contract to re-read both modules immediately before editing and to locate code by SYMBOL is load-bearing here rather than boilerplate.

SECOND GATE: the children do not exist and must be authored FROM E-01's inventory, not before it. A reviewer should refuse any child plan whose scope was written without the measurement behind it.

Open questions: ALL RESOLVED. OQ-01 is answered by maintainer decision (INTERLEAVED; see its resolution and the partially-discharged gate below). OQ-02 is resolved from evidence (a new dedicated module following `host_runner.py`'s conventions, subsuming `plan_readiness.py`'s placement). OQ-03 is deliberately deferred to E-01 with the decision RULE fixed here.

Scope fence for THIS plan: write only the E-01 research artifact, the E-02 characterization tests under `tests/`, and this plan's own record. Author NO product code, touch NEITHER runner module, and do not create the shared module. E-02 may add tests but may NOT modify a runner to make a test pass: a characterization test that requires a source change is reporting a finding, not a fix. If executing this orchestrator seems to require editing a runner, that work belongs to a child that does not exist yet, so STOP and report.

Honesty rule (HARD MUST): when you report tests or validation passed, paste the ACTUAL runner output with the `git rev-parse HEAD` it was measured at. Never claim a pass you did not run. Specific to this Set: do NOT claim a de-duplication is complete on the basis of a grep, and do NOT describe a behavior change as a refactor.

Execution contract: commit ONLY the files changed, path-scoped (`git commit -m msg -- <paths>`), never `git add -A`, and never push. This Set is the highest-collision work in the repo: both runner modules and both runner test modules were dirty from another session at authoring time, a live driver owned five lane worktrees, and 21 unexecuted plans declare these files. Every child must re-read both modules immediately before editing, locate code by SYMBOL (line numbers in any citation will have moved), and STOP rather than overwrite a co-worker's in-flight change.

Post-gate lifecycle: do not claim done or move this plan until every `V-*` item is verified with concrete pasted evidence and `aw ipd lint --phase pre-transition` reports conforming; the transition is performed by `aw ipd finalize`, never by hand. Do not create or push a git tag or release.
