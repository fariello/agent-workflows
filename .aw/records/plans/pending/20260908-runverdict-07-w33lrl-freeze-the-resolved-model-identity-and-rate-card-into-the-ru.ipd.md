# IPD: Freeze the resolved model identity and rate card into the run record so a recorded cost can be attributed and recomputed

- Date: 2026-09-08
- Kind: child
- Concern: The run record captures per-step COST but neither the MODEL that incurred it nor the RATE CARD that priced it, so a recorded dollar figure cannot be attributed to a model or recomputed from its inputs. Three gaps, ALL FIGURES RE-MEASURED AT REVIEW at HEAD `4cfa0a90` across all 140 run directories (the authored figures were taken at `44d4950d` over 135 runs and every one of them has moved; the corrected numbers are used throughout and F-10 records the drift).
  GAP 1, THE RUN-LEVEL MODEL FOR THE OC HOST, PARTLY CLOSED SINCE THE ITEM WAS FILED. `options.model` is set in 9 of 140 runs, so the item's "null on every run" is FALSE in the letter and TRUE in substance for oc: those are the ones where an operator passed `--model` explicitly (plus all 4 agy runs, see below), and the rest record `null` because the model comes from the host's own config. What HAS landed is the provenance object: 34 of 140 runs carry `options.launch_profile` (`oc_runipd.launch_profile_record`, find by SYMBOL, `:2703` at review, shipped by `runprofile` Order 03 `3cm15q`), and it records `model`, `variant`, `agent`, a `config_digest`, and a per-field `provenance` map. In 32 of those 34 the provenance reads `{"model": "host-default", ...}`, and `PROVENANCE_HOST_DEFAULT` is documented as "nothing supplied it; pass no argument" (`runner_profiles.py:492`). So the record now says HONESTLY that it does not know the model, which is a real improvement and precisely what still needs closing: `host-default` names the gap instead of filling it.
  GAP 1a, AND IT INVERTS THE PLAN'S ORIGINAL AGY PREMISE. This gap is OC-ONLY. `agy_runipd` carries `DEFAULT_MODEL = "gemini-3.7-flash-high"` (`:383`), assigns `options["model"] = getattr(args, "model", DEFAULT_MODEL)` (`:2039`), and defaults its `--model` flag to the same constant (`:4714`), so EVERY agy run records a CONCRETE resolved model with no host-config lookup at all. Measured: all 4 agy runs in this checkout carry `options.model: gemini-3.7-flash-high`, and one of them independently confirms it in its own session log's `init` event. So the true asymmetry is the OPPOSITE of "agy is behind": agy has no profile machinery but DOES resolve a model, while oc has the profile machinery and does NOT. See E-04, which was rewritten on this measurement.
  GAP 2, THE PER-STEP MODEL FOR THE MAIN AGENT, ENTIRELY OPEN, BUT NOT "ZERO MODEL IDS ANYWHERE". The cost-bearing per-step records in `sessions/*.jsonl` are `step_finish` parts carrying `part.cost` and `part.tokens{total,input,output,reasoning,cache{read,write}}`. RE-MEASURED across all 496 session logs: 32333 cost-bearing parts, and the key set on EVERY ONE of them is exactly `{id, reason, snapshot, messageID, sessionID, type, tokens, cost}` -- so 0 of 32333 carry any model key, and that is the claim that matters. The authored "0 distinct model ids" in the whole corpus is REFUTED and must not be repeated: 16 occurrences of a real `modelID` exist at `part.state.metadata.model.modelID` on a `task` TOOL CALL (2 distinct values, `its_direct/pt3-claude-opus-4.8-1m-us` and `its_direct/pt3-claude-opus-5-1m-us`), which identifies the SUB-AGENT a `task` call spawned rather than the main agent, plus agy's `init` event carrying its model in 2 files. `aflsz3` F-5 had already measured exactly this; a sibling's more accurate figure must not be contradicted here (F-11).
  GAP 3, THE RATE CARD, ENTIRELY OPEN AND NOT IN THE REPOSITORY AT ALL. The card lives in the user's host config, which `aw oc update-models` MUTATES from a LiteLLM admin endpoint (`oc_models.py`, `sync_provider` `:607`, `per_million` `:501`), and `resolve_config_path` (`:96`) resolves that file the way OpenCode itself does. That config demonstrably changes: research `x0spmh` records the card being CORRECTED mid-history from `input 5.00 / output 25.00, cache_read UNPRICED` to `5.50 / 27.50 / 0.55 / 6.875`, which makes Aug 24 and Aug 29 dollar totals NOT comparable and makes an Aug 24 `cache_read = $0` a CONFIG ARTIFACT rather than evidence that cache reads were free. Nothing in any run record lets a reader detect which regime priced it. RE-MEASURED, AND THE PARTIAL-CARD PROBLEM IS THE CURRENT MAJORITY CASE, NOT A HISTORICAL ONE: the live config declares 82 models, 80 carry a `cost` block, and 47 of those 80 OMIT `cache_read` AND `cache_write` entirely (F-13). So E-02's absent-versus-zero rule governs most of the card as it stands today.
  THE CONSEQUENCE IS AN UNANSWERABLE QUESTION, NOT A COSMETIC GAP. Asked "is Opus 5 a better verifier than 4.8", the only available attribution was grepping model strings out of run directories, and every occurrence is inside a shell command an AGENT typed (e.g. an `aw ipd finalize ... --actor "opencode <model>"`), so per-model attribution rests on agent PROSE, which this framework elsewhere insists is never authority (research `ig9bai`). One run contains BOTH an `opus-4.8` and an `opus-5` string, so even the reconstruction is ambiguous.
- Scope: Record, per RUN, the RESOLVED model identity and the RESOLVED rate card in effect at launch, with a digest so a later host-config edit is detectable, and make cost views read the recorded card rather than the current one. EXCLUDES per-STEP model attribution, which no host currently emits (see OQ-01 and the deferred section); excludes any analytics, statistics, taxonomy, pricing-era arithmetic, SPA, export, or query surface, ALL of which belong to the `runanalytics` Set; excludes changing what any model is or how it is chosen (`runprofile`).
- Scope-Paths: agent_workflows/oc_runipd.py, agent_workflows/agy_runipd.py, agent_workflows/runner_shared.py, agent_workflows/oc_models.py, tests/test_oc_runipd.py, tests/test_agy_runipd_cli.py, tests/test_oc_models.py, tests/test_rununify_initialize_run.py
- Item-Dependencies: none
- Status: approved
- Work-Kind: bug
- Priority: high
- Readiness: go-pending-approval
- Set: runverdict
- Order: 7
- Highest E allocated: 06
- Author: opencode/its_direct/pt3-claude-opus-5-1m-us
- Id: w33lrl
- Approval: 2026-09-13, recorded via aw ipd set: status set to approved
- Blocks-Release: next
- From-Backlog: vlf75p

## Workflow history
- 2026-09-23 approved (aw set): Backfilled Priority and Work-Kind by inheritance from source backlog item vlf75p (planprio Order 02, plan 8u6770, E-03); no lifecycle transition occurred.
- 2026-09-13 approved (aw set): status set to approved

- 2026-09-09 reviewed (opencode/its_direct/pt3-claude-opus-5-1m-us): /plan-review APPROVE WITH REVISIONS APPLIED; readiness GO - PENDING HUMAN APPROVAL. PR-001..PR-009, ALL NINE FIXED, no open findings. `aw ipd lint --phase author` CONFORMING before semantic review and `--phase review-finalize` CONFORMING after every revision, so nothing here is structural. DISCLOSURE: same agent/model authored this plan, so this is a SELF-REVIEW, and its value rests on RE-MEASURING rather than re-reading; nine of the plan's premises were re-run and FOUR of them inverted an instruction.
  THE FOUR THAT INVERTED SOMETHING. (1) E-01 pointed the executor at `models_from_config` to resolve the host default; that function returns the 82-entry CATALOG of declared `provider/model` keys and the default is the config's top-level `model` key, which NOTHING in this repository reads, so the instruction yields a list and no answer (PR-002, F-12). E-01 now adds one no-secret accessor and records WHICH key supplied the value. (2) E-02 said to reuse `oc_models`' price vocabulary; the config already stores `$/Mtok` and `per_million` is the WRITE-side per-token converter, so calling it on a value read back would multiply by 1e6 -- the exact error the paragraph was written to prevent (PR-003, F-14). (3) E-03 said to make the recorded card authoritative for the reported cost, but NOTHING in the repository prices tokens from a card: cost is a sum of `part.cost` from `step_finish`, so the non-substitution property is already true by construction and an executor hunting the substitution could plausibly BUILD the recomputation E-06 forbids (PR-004, F-15). E-03 is now a stated invariant plus a regression guard. (4) E-04's agy framing was backwards: agy carries `DEFAULT_MODEL = "gemini-3.7-flash-high"`, writes it into `options["model"]` at run creation, and all 4 agy runs record it, so agy needs NO model work and oc is the host that is behind; what agy genuinely cannot resolve is a CARD, because that model is absent from the OpenCode config's 8 gemini entries (PR-005, F-16).
  ALSO FIXED. The "four-component card" schema was wrong for the data: 47 of the 80 priced models in the live config carry only `input`/`output`, so a partial card is the CURRENT majority and each absent component needs a named unknown (PR-006, F-13). "0 distinct model ids in the corpus" was false and contradicted sibling `aflsz3` F-5, which had already measured the 16 `task`-call `modelID` values that name a sub-agent (PR-007, F-11); the claim that matters, that no cost-bearing step carries a model, is intact and now stated precisely. EVERY quantity in the plan had drifted within one day, including two used as arguments (PR-001, F-10): 135 runs -> 140, `options.model` 5 -> 9, `launch_profile` 33 -> 34, steps 28158 -> 32333, imports 47 -> 48, and three line citations. OQ-01 and OQ-02 were both answerable from the repository and are now `resolved`, and OQ-01 surfaced a live hazard the plan had inverted: `kgpptv` is EXECUTED, not pending, so the two-model verifier case exists TODAY (0 of 140 runs configure it) and E-01/E-02 must cover the verifier launch or say they do not (PR-008). The spec-sync section asked the executor to determine whether §5.6 is a closed shape; it says "at least", so the answer is no and no amendment is needed (PR-009). The baseline was also wrong (claimed `1 failed, 5648 passed` in `test_orchestrator_retirement`; measured `1 failed, 5919 passed, 3 skipped, 2 xfailed` in `test_reporting_contract`, environmental, from 189 untracked files belonging to another party).
  CONFIRMED SOUND. The plan's central judgement -- that it is the PRODUCER and the `runanalytics` Set is the CONSUMER, and that it must therefore graduate almost none of its backlog item -- is correct and independently reproduced: the item's gaps 2 and 3 hold, the container half of gap 1 really did ship with `3cm15q`, and the three named consumer plans really do declare model and price deliverables they cannot supply themselves. The `x0spmh` absent-versus-zero rule is the most valuable instruction in the plan and is now backed by a current majority rather than one historical incident.
- 2026-09-08 to-review (opencode/its_direct/pt3-claude-opus-5-1m-us): Graduated from backlog `vlf75p` as a NARROWED plan; the item carries `- Blocks-Release: next` and this plan inherits that gate. TWO NARROWINGS, both recorded in the item itself so no reader is misled.
  FIRST NARROWING, PARTIAL OBSOLESCENCE FROM SHIPPED WORK. The item's gap 1 says `options.model` is "null on every run". Re-measured: 5 of 135 runs have it set, and more importantly `runprofile` Order 03 (`3cm15q`, executed) shipped `launch_profile_record` (`oc_runipd.py:2740`) which now freezes a provenance object into 33 of 135 runs carrying `model`, `variant`, `agent`, `config_digest` and a per-field `provenance` map. So the RECORD-A-PROVENANCE-OBJECT half of gap 1 is DONE and must not be rebuilt; what survives is that its `model` reads `host-default` (`runner_profiles.py:378`, "nothing supplied it; pass no argument") because nothing resolves the host's own default. This plan fills that value; it does not re-invent the container. Also noted: agy has ZERO profile integration (`runner_profiles`, `resolve_launch_profile` and `launch_profile` all grep to nothing in `agy_runipd.py`), which pending plan `kgpptv` records as a measured host asymmetry, so parity here is not free and OQ-02 owns it.
  SECOND NARROWING, OVERLAP WITH THE PENDING `runanalytics` SET, which is the expensive mistake this note exists to prevent. That Set (11 plans, `to-review`/`reviewed`, orchestrator `5lxvl3`) owns everything DOWNSTREAM of the record. Specifically: `5f2h8i` (Order 04) E-01/E-02 already declare that telemetry "records resolved run/set/IPD/attempt/phase/model/provider/variant metadata" per invocation and that "the execution ID, attempt, phase, start/end times, model identity, and node pseudonym must be recorded per file and must not be inferred later from a single run-level snapshot"; `aflsz3` (Order 06) E-02 owns "effective-dated pricing" with "price schedules carry provider/model/variant, input/output/cache rates, currency, effective interval, and source/version", plus the explicit convention "Model prices changed over time. A timeless price map would rewrite history and is forbidden"; and `8hald1` (Order 05) owns the normalized fact schema carrying "runner/model/variant" and "pricing-key" facts. THEREFORE this plan does NOT graduate the item's fix-sketch clauses about per-TURN capture, effective-dated schedules, pooled-aggregate exclusion, or `aw runs` cost views computing from a card, and does NOT graduate its test (b) cost-recomputation-to-the-cent, test (d) pooled-aggregate exclusion, or test (e) per-model aggregation. Those are `aflsz3`'s and `8hald1`'s by declaration. What this plan graduates is the ONE thing the `runanalytics` Set cannot do for itself: those plans consume a model identity and a rate card from the run record, and TODAY THE RUN RECORD CONTAINS NEITHER, so a telemetry integration that "records resolved model" would record `host-default` and a pricing engine would have no in-repo card to be effective-dated. This plan is the PRODUCER; that Set is the CONSUMER. The boundary is stated in the deferred section and pinned by OQ-03.
  THE ITEM'S REMAINING CLAIMS ALL HELD ON RE-MEASUREMENT: 28158 cost-bearing steps with ZERO model ids (the item measured 9757; the corpus tripled and the ratio is still exactly zero), the card absent from the repository, and `aw oc update-models` demonstrably mutating it.

## Goal

Make a run record say which model incurred its cost and at what prices, so the figure can be attributed and recomputed, and so a later host-config edit cannot silently reprice history.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: resolve the model the host will actually use

- [x] E-01 RESOLVE THE HOST'S OWN DEFAULT MODEL AND RECORD IT, filling the value that today reads `host-default`. Do NOT build a new provenance container: `launch_profile_record` (find by SYMBOL; `oc_runipd.py:2703` at review) already exists, already freezes `model`/`variant`/`agent`/`config_digest`/`provenance`, and its docstring states its purpose ("Records WHICH configuration produced this run's launch and WHERE each field came from"). Add the resolved host default as an additional recorded field with its own provenance value; do not overwrite `model`, whose `host-default` provenance is a TRUE statement about the flag and must stay true.
  NO EXISTING READER CAN ANSWER THIS QUESTION, AND THAT IS THE CORRECTION THIS ITEM MOST NEEDS (F-12). The host default is the OpenCode config's TOP-LEVEL `model` key: measured live, `config["model"]` is a concrete `provider/model` id and `config["small_model"]` is a second one. `models_from_config` (`:301`) reads ONLY `provider.<name>.models.<id>` KEYS -- it returns the 82-entry CATALOG of what is declared, never which one is DEFAULT -- so an executor following an instruction to "resolve the default through `models_from_config`" would get a list and no answer. `catalog_from_config` (`:330`) has the same limit by construction. THEREFORE this item legitimately ADDS a small accessor to `oc_models` (the Scope check already permits exactly this) that returns the config's top-level default. Give it `models_from_config`'s documented no-secret guarantee IN ITS OWN DOCSTRING and hold to it: read the `model` key and nothing adjacent; never touch a `provider.<name>.options` block, an `apiKey`, or a header. Writing a config reader ANYWHERE ELSE is the way a credential reaches a run record.
  RESOLVE THE CONFIG FILE THROUGH `resolve_config_path` (`:96`) AND NOTHING ELSE. It resolves the config "the way OpenCode does" (`$OPENCODE_CONFIG`, then a project `opencode.json`/`.jsonc` walking up, then `$XDG_CONFIG_HOME`, then `~/.config`). Do not re-derive that precedence.
  THE HOST DEFAULT IS NOT ALWAYS THE TOP-LEVEL KEY, SO SAY WHICH ONE YOU RECORDED. OpenCode also supports a per-agent `agent.<name>.model` and a `small_model`, and this driver passes `--agent` when `options["agent"]` is set (find `argv.extend(["--agent", ...])` in the launch builder). Measured live, the only configured agent (`build`) sets `temperature` and NO model, so the top-level key is the effective default TODAY; that is a property of one config, not a guarantee. Record WHICH key supplied the value (top-level, agent override, or small-model) rather than recording an id whose origin a later reader must guess.
  RECORD AN UNRESOLVABLE MODEL AS A NAMED UNKNOWN, NEVER OMIT IT. The item requires this explicitly, and `runner_profiles.PROVENANCE_VALUES` (`:500`) is already a closed enum whose membership a shipped test asserts for every resolved field (`tests/test_runner_profiles.py:836`), so an added provenance value MUST be admitted there rather than smuggled in as a free string, and that same test also pins the EXACT provenance key set (`tests/test_runner_profiles.py:829-832`), so adding a provenance KEY requires updating that assertion deliberately and saying so. A `.jsonc` config is UNPARSEABLE BY DESIGN (stdlib json cannot preserve comments, `_classify_target` `:138` marks it unwritable) and `catalog_from_config` returns a documented `CATALOG_UNPARSEABLE` reason for it; that is a real and reachable unknown, not a hypothetical.
  DO NOT LEAK A LOCAL PATH INTO THE RECORD WITHOUT CHECKING. `launch_profile_record` already stores `config_source`, and the 34 live examples contain an absolute maintainer home path in it. The orchestrator of the `runanalytics` Set records that every recent `state.json` already carries an absolute maintainer home path in `repo` and `driver.path`, so this is a pre-existing condition rather than one this plan creates; but run `aw sanitize --agent` before and after and do not make it worse (measured at review: `outcome: clean`, 0 findings). If the resolved card or model carries a path, record a digest or a basename rather than the path.
  - Depends on: none
  - Expected outcome: the run record names the model the host will actually use AND which config key supplied it, or a NAMED unknown with a provenance value admitted to the closed enum; the host config is reached only through `resolve_config_path` plus ONE new no-secret accessor in `oc_models` carrying that guarantee in its docstring; the two `tests/test_runner_profiles.py` assertions on the provenance key set and value enum updated deliberately, not incidentally; `aw sanitize --agent` still `clean`.
  - Execution state: performed

### Task group 2: freeze the card that priced the run

- [x] E-02 SNAPSHOT THE RESOLVED RATE CARD AT LAUNCH, with its components, the model it applies to, and a DIGEST, and record explicitly that it is a LAUNCH-TIME SNAPSHOT rather than a per-step observation. The item asks for exactly this and for the snapshot to be labelled as such.
  THE CARD IS ALREADY IN DOLLARS PER MILLION IN THE CONFIG, SO DO NOT CALL `per_million` ON IT (F-14). `per_million` (`:501`) is the WRITE-side converter: it turns a LiteLLM gateway's per-TOKEN value into the `$/M` value that gets STORED (`build_models`, `:565-577`), and `oc_models`' own module docstring states the config holds "`input`/`output`/`cache_read`/`cache_write` cost, in $ per MILLION tokens". Measured live, the default model's block reads `{"input": 5.5, "cache_read": 0.55, "cache_write": 6.875, "output": 27.5}`, which is dollars per million on its face. Passing those through `per_million` again would multiply by 1e6 and produce exactly the 1000000x error the authored wording was trying to prevent. So: READ the four keys, RECORD the unit as `$/Mtok` explicitly in the record, and reuse `per_million`'s VALIDATION POSTURE (reject a bool, a string, or a non-positive number as unknown rather than coercing it) without reusing the FUNCTION. State in the code comment why the function is not called, so the next reader does not "fix" it.
  A MISSING OR PARTIAL CARD IS A NAMED UNKNOWN, NOT A ZERO, AND THE PARTIAL CASE IS THE MAJORITY TODAY. This is the highest-value single instruction in this plan. Research `x0spmh` documents the trap: the pre-correction card left `cache_read` UNPRICED, and the resulting `cache_read = $0` was read as evidence that cache reads were free when in fact 73.9% of a $16.41 turn was cache reads. RE-MEASURED at review on the live config: 82 declared models, 80 with a `cost` block, and 47 of those 80 carry ONLY `input` and `output` -- no `cache_read`, no `cache_write`. So a partial card is the current common case, not a legacy artifact, and "four components" is the WRONG framing for the schema: record the components PRESENT plus a named unknown for each absent one. `aflsz3` E-07 independently measured `cache_write` as 0 in all 29611 cache-reporting steps and forbids a `cache_write` RATE COLUMN in its schedule; that is its schema decision, not a licence to drop the observed key here, so RECORD what the card says and let the consumer decide what to price.
  THE DIGEST IS WHAT MAKES A LATER EDIT DETECTABLE, and it is the reason this is not merely a copy. `aw oc update-models` mutates the host config from a LiteLLM admin endpoint (`oc_models.sync_provider` `:607`), and the maintainer's config has multiple timestamped `.bak` siblings, so the card demonstrably changes between runs. `launch_profile_record` already carries a `config_digest` for the PROFILE store -- a DIFFERENT FILE from the OpenCode config (measured: `config_source` in all 34 live records points at `runner-profiles.json`, not `opencode.json`), so the two digests cover two files and a SEPARATE field is the answer. A shipped test already asserts the executor and verifier records SHARE one `config_digest` (`tests/test_oc_runipd.py:4655`) and another asserts it is stable across resume (`:4468`), so overloading that field would break a proved invariant. Record the card digest under its own key and say in the record which file each digest covers.
  - Depends on: E-01
  - Expected outcome: the card recorded with every component PRESENT in the config plus a named unknown per absent component (distinguishable from zero), the model it applies to, a SEPARATE card digest whose covered file is named, and an explicit launch-time-snapshot label; the unit recorded as `$/Mtok` with a comment stating why `per_million` is not called; `tests/test_oc_runipd.py:4468` and `:4655` still pass unmodified.
  - Execution state: performed

- [x] E-03 PROVE AND PIN THE NON-SUBSTITUTION PROPERTY: a host-config change after a run must NOT change that run's reported figure. This is the item's test (c) and it is the property that makes the whole record worth writing. READ THE NEXT PARAGRAPH BEFORE PLANNING THIS ITEM, because the property is currently TRUE BY CONSTRUCTION and this item's real job is a REGRESSION GUARD, not a behavior change.
  MEASURED, AND IT REWRITES THIS ITEM (F-15): NOTHING IN THIS REPOSITORY PRICES ANYTHING FROM A CARD TODAY. `run_viewer.extract_log_metrics` (`:563`) sums `part.cost` from `step_finish` events and nothing else; the drivers persist that sum onto the attempt as `cost`/`verify_cost` (find `extract_log_metrics` call sites in `oc_runipd`) and never consult a price. Grepped at review: no module outside `oc_models` multiplies tokens by a rate. So a reported cost ALREADY cannot move when the config changes, and an executor told to "make the recorded card the authority for that run's reported cost" would look for a substitution that does not exist and could plausibly BUILD one -- the exact cost recomputation the fence and E-06 forbid. Therefore this item does NOT change how cost is reported. It (a) states the measured invariant in the code where it can be seen, and (b) adds a test that FAILS if a future change ever prices a historical run from the current config.
  DO NOT COMPUTE COSTS HERE. The recorded `part.cost` values are what the host reported and remain the primary figure. Any RECOMPUTATION from tokens times card is `aflsz3`'s E-07 (effective-dated pricing, which validates a two-era schedule against recorded cost) and `8hald1`'s normalized facts, which separate "recorded cost" from "estimated cost". Adding a second cost computation here would fork exactly the distinction that Set exists to maintain.
  A RUN WITHOUT A RECORDED CARD MUST NOT SILENTLY BORROW THE CURRENT ONE. Every run created before this plan lands has no card: measured, 0 of 140 runs carry any card-shaped key in `options`. Such a run must report its recorded cost with the card marked ABSENT, never with today's prices, because that is the pooled-comparison error `x0spmh` documents and `aflsz3` F-10 quantifies (a nine-fold apparent blended-rate jump caused by cache_read becoming billable, not by the 10 percent rate change).
  - Depends on: E-02
  - Expected outcome: the measured no-pricing-path invariant stated at the recording site; a test that fails if a historical run's reported cost is ever derived from the current config; a card-less run reports the card as ABSENT rather than borrowing; NO second cost computation introduced and no change to how `part.cost` is summed.
  - Execution state: performed

### Task group 3: parity, proof, and the consumer boundary

- [x] E-04 DECIDE AND IMPLEMENT THE AGY POSITION, then state it honestly. THREE OF THIS ITEM'S FOUR AUTHORED PREMISES WERE REFUTED AT REVIEW (F-16, F-17), so read the measurements before planning it.
  AGY ALREADY RECORDS A CONCRETE MODEL, WHICH INVERTS THE ITEM'S FRAMING. `runner_profiles`/`resolve_launch_profile`/`launch_profile` do all grep to nothing in `agy_runipd.py`, so the no-profile-machinery half is TRUE. But agy needs none for the model: `DEFAULT_MODEL = "gemini-3.7-flash-high"` (`:383`) is written into `options["model"]` at run creation (`:2039`) and is the `--model` default (`:4714`). Measured: all 4 agy runs record `options.model: gemini-3.7-flash-high`, and one confirms it independently in its session log's `init` event. So agy needs NO model work at all -- it is oc that is behind on model identity. Do not "add agy parity" for E-01.
  THE AGY GAP IS THE CARD, AND IT IS A REAL ONE, BECAUSE ITS MODEL IS NOT IN THE OPENCODE CONFIG. Measured: the OpenCode config declares 8 gemini models and `gemini-3.7-flash-high` is NOT among them (the closest is `google/gemini-3.7-flash`). Antigravity is a separate host with its own pricing that this repository has no reader for, and `resolve_config_path` resolves OpenCode's config, so pointing agy at it would attribute an OpenCode provider's prices to an Antigravity model. RECORD THE INABILITY EXPLICITLY (a named unknown saying "this host's card is not resolvable from any config this tool reads"), and do NOT guess a rate. Do not fabricate an Antigravity config reader in this plan.
  THIS CHECKOUT DOES CONTAIN AGY RUNS, so evidence need not be synthetic. Measured at review: 4 runs whose `driver.path` ends `agy_runipd.py`, one carrying two real session logs (an execute and a verify turn). The authored "ZERO agy runs" is stale. Use a FIXTURE for the assertion (the run tree is gitignored per E-05) but you may cite the live agy run as corroboration, labelled as the gitignored corpus rather than as a test.
  SITE ANY SHARED SYMBOL IN `runner_shared.py`, NEVER IN `oc_runipd.py`. RE-MEASURED BY AST WALK AT REVIEW: the count is 48, not 47 (the authored figure, which two sibling plans also carry, is one behind). Adding to oc for agy to import would make it 49 and deepen the layering defect backlog `cnwy8g` owns.
  - Depends on: E-02
  - Expected outcome: NO agy model work (it already resolves one, cited); an agy run records an explicit not-resolvable-from-any-readable-config card marker, distinguishable from an oc run whose config was found but unparseable; no Antigravity config reader invented; any shared symbol in `runner_shared`; the oc-to-agy import count still 48.
  - Execution state: performed

- [x] E-05 PROVE THE RECORD ON SYNTHETIC INPUT, from FIXTURES. `.aw/records/runs/` is gitignored (`.aw/.gitignore:14`), so a test built on the live corpus is not reproducible for anyone else and will not survive a fresh clone or a lane worktree.
  INJECT THE CONFIG, NEVER READ THE REAL ONE. `resolve_config_path` takes `env` and `cwd` parameters precisely so a caller can point it at a temp directory, and `catalog_from_config` forwards both; the existing `tests/test_oc_models.py` (32 tests) is the established home for config-reading cases. A test that reads the maintainer's actual `~/.config/opencode/opencode.json` would assert against a file that differs per machine and mutates on every `aw oc update-models`. Make the new accessor from E-01 take the same injectable parameters.
  COVER THE UNKNOWN PATHS, WHICH ARE THE REACHABLE ONES: no host config at all (`resolve_config_path` returns `None`); a `.jsonc` config (unparseable by design, `_classify_target` marks it unwritable and `catalog_from_config` reports `CATALOG_UNPARSEABLE`); a config with NO top-level `model` key (so the default is genuinely unknown even though the catalog is full); a config whose default model has no `cost` block (measured: 2 of 82 live models); a card carrying only `input` and `output` (measured: 47 of 80, the `x0spmh` trap and the current majority); a card whose value is a string, a bool, or negative; and a card component that is a GENUINE ZERO. Assert the record distinguishes each from a resolved card AND from zero.
  ASSERT THE 140 CARD-LESS HISTORICAL RUNS STILL READ HONESTLY. Measured: 0 of 140 existing runs carry any card key, so every run in the corpus is a pre-change shape. The record change must not make one look like it has a card. Use a fixture shaped like a real pre-change `state.json`.
  - Depends on: E-03, E-04
  - Expected outcome: fixture-driven tests for all seven unknown paths above, for the genuine-zero contrast, and for the historical card-less shape; the config injected via `env`/`cwd` in every case; no test reads the gitignored live run tree or the maintainer's real config.
  - Execution state: performed

- [x] E-06 PIN THE CONSUMER BOUNDARY WITH THE `runanalytics` SET, in the code and in a test, so the two do not build the same thing twice. That Set's plans DECLARE that they record model identity per invocation (`5f2h8i` E-02, "the execution ID, attempt, phase, start/end times, model identity, and node pseudonym must be recorded per file and must not be inferred later from a single run-level snapshot"), own effective-dated price schedules (`aflsz3` E-07), and own the normalized fact schema carrying `runner/model/variant` (`8hald1` E-04). Note the corrected E-item numbers: the authored citations said `aflsz3` E-02 and `8hald1` E-02, which are different items in both plans (F-18).
  ALL THREE ARE `approved` AND THEREFORE RUNNABLE BEFORE THIS PLAN, WHICH IS THE ORDERING RISK. Read at review: `5f2h8i`, `8hald1` and `aflsz3` all carry `- Status: approved` in `pending/`, and their `- Item-Dependencies:` chain is `5f2h8i -> 8hald1 -> aflsz3` with no edge to this plan. So `aw oc run all` can execute the whole consumer chain first. STILL READ THEIR STATUSES ON DISK AT EXECUTION TIME (they may have moved to `executed/`) and record what you found. If any HAS executed, check whether it already wrote a model or card field and, if so, CONSUME it rather than adding a parallel one; a second field naming the same fact is the failure mode this item exists inside a Set to avoid. If none has, this plan's field is the one they will consume, and its NAME and UNITS become a contract; state them explicitly so a later plan binds to them rather than guessing.
  A SIBLING ALREADY GATES ITSELF ON THIS PLAN'S OUTPUT WITHOUT NAMING IT, so do not weaken that gate. `aflsz3` E-08 REFUSES model comparison until identity coverage exceeds a declared threshold, citing 1.1 percent coverage; this plan is what raises that coverage for FUTURE runs. Do not relax, pre-empt, or reinterpret that refusal, and do not claim this plan retroactively improves historical coverage: it cannot, and `aflsz3` says so.
  RECONCILE WITH THE SHIPPED ANTI-DOLLAR-COST RULE, since this plan writes a PRICE into a record. `benchmark_metrics` is documented "never dollar" and RAISES `MetricError` on a `cost`/`usd`/`price`/`dollars`/`spending`/`dollar_cost` key in benchmark USAGE (`:42-49`, `:168-173`), and `benchmark_manifest:65` rejects the same set; `aflsz3` F-9 traces the prohibition to a maintainer ruling in executed plan `9ihhzr` and reconciles it as scoped to CROSS-MODEL BENCHMARK COMPARISON. This plan records a rate card in a RUN record, not a benchmark usage mapping, so the rule does not bite -- but state that in a comment, because a reader who finds one module raising on a `price` key and another freezing one needs the boundary written down.
  DO NOT IMPLEMENT AN EFFECTIVE-DATED SCHEDULE, a pooled-aggregate exclusion rule, a per-model comparison, or a cost recomputation. All four are declared deliverables of that Set. This item's job is to make its declarations satisfiable, not to pre-empt them.
  - Depends on: E-05
  - Expected outcome: the three plans' statuses and directories read and pasted; the field name and units stated as a contract; the `benchmark_metrics` boundary stated in a comment; `aflsz3` E-08's coverage refusal left intact; no effective-dated schedule, pooled-exclusion rule, per-model comparison, or cost recomputation implemented here; a test or comment pinning the boundary.
  - Execution state: performed

## Project conventions discovered (Step 0)

- THE PROVENANCE CONTAINER ALREADY EXISTS AND MUST BE EXTENDED, NOT REPLACED. `launch_profile_record` (`oc_runipd.py:2703`) freezes `requested`/`applied`/`runner`/`config_source`/`config_present`/`config_digest`/`model`/`variant`/`agent`/`provenance`, and its docstring states it "Contains NO credentials: only the model/variant/agent identifiers, the profile names, and the store path", because `runner_profiles` stores no secrets by construction. Preserve that property.
- `PROVENANCE_VALUES` IS A CLOSED ENUM (`runner_profiles.py:500`) WITH A SHIPPED TEST ON BOTH ITS KEYS AND ITS VALUES. `tests/test_runner_profiles.py:829-832` asserts the provenance key set is EXACTLY `["agent","model","runner","validate","variant","verify_with"]` and `:836` asserts every value is in the enum, so adding either a key or a value is a deliberate test edit. `PROVENANCE_HOST_DEFAULT` means "nothing supplied it; pass no argument" (`:492`), which is why the current record honestly says it does not know.
- `oc_models` OWNS HOST-CONFIG READING, BUT NO EXISTING FUNCTION RETURNS THE DEFAULT MODEL. `resolve_config_path` (`:96`) resolves the file the way OpenCode does and takes injectable `env`/`cwd`; `models_from_config` (`:301`) returns the CATALOG of declared `provider/model` keys and "never looks at a provider's options block, credential value, or headers"; `_classify_target` (`:138`) marks a `.jsonc` config unwritable because comments cannot be round-tripped. The DEFAULT lives in the config's top-level `model` key, which nothing here reads, so E-01 adds one small accessor carrying the same no-secret guarantee.
- THE CONFIG'S COST BLOCK IS ALREADY IN DOLLARS PER MILLION, so `per_million` (`:501`) is the WRITE-side converter and must NOT be applied to a value read back out. Its refusal posture (bool/string/None/non-positive yields `None`) is the part worth reusing; the multiplication is not. Measured live: the default model's block is `{"input": 5.5, "cache_read": 0.55, "cache_write": 6.875, "output": 27.5}`.
- THE CARD DEMONSTRABLY CHANGES. `sync_provider` (`:607`) rewrites prices from a LiteLLM admin endpoint and computes `added`/`removed`/`changed` sets, so the mutation is first-class behavior rather than an accident. This is why a digest, not a copy, is what makes a later edit detectable. The profile store and the OpenCode config are DIFFERENT FILES, so they need different digests.
- A ZERO AND AN ABSENCE ARE DIFFERENT, and this repository has already been misled by conflating them: research `x0spmh` records that the pre-correction card left `cache_read` unpriced, making a `$0` look like free cache reads when 73.9% of a $16.41 turn was cache reads. Measured at review, the partial card is the CURRENT majority: 47 of the 80 priced models in the live config carry only `input` and `output`.
- NOTHING IN THIS REPOSITORY PRICES TOKENS FROM A CARD. `run_viewer.extract_log_metrics` (`:563`) sums `part.cost` from `step_finish` and the drivers persist that sum; no module outside `oc_models` multiplies tokens by a rate. So the non-substitution property E-03 protects is currently true by construction, and E-03 is a guard rather than a change.
- A SHIPPED METRICS LAYER REJECTS DOLLAR COST OUTRIGHT, scoped to benchmarks. `benchmark_metrics` is "never dollar" and raises `MetricError` on a `cost`/`usd`/`price`/`dollars`/`spending`/`dollar_cost` key in trial usage (`:42-49`, `:168-173`); `benchmark_manifest:65` mirrors it. `aflsz3` F-9 reconciles it as governing cross-model benchmark comparison, not runner-recorded cost. State the boundary rather than leaving two modules looking contradictory.
- AGENT PROSE IS NEVER AUTHORITY (research `ig9bai`), which is exactly why grepping a model name out of an agent-typed shell command is not attribution.
- THE `runanalytics` SET OWNS EVERYTHING DOWNSTREAM, AND ALL TEN CHILDREN ARE `approved` (the orchestrator `5lxvl3` is `reviewed`), so the consumers can run before this producer. Its convention is stated plainly in `aflsz3`: "Model prices changed over time. A timeless price map would rewrite history and is forbidden." This plan produces the per-run truth that convention needs.
- THE IMPORT DIRECTION IS ONE-WAY: `agy_runipd` imports 48 names from `oc_runipd` (AST-measured at review; the widely-copied 47 is one behind); `oc_runipd` imports zero from agy. Shared symbols go in `runner_shared`.
- AGY HAS NO PROFILE INTEGRATION BUT DOES RESOLVE A MODEL, via `DEFAULT_MODEL = "gemini-3.7-flash-high"` (`agy_runipd.py:383`, written to `options["model"]` at `:2039`). This checkout holds 4 agy runs, so agy evidence is available; it is the agy CARD that is unresolvable, because that model is absent from the OpenCode config and no Antigravity config reader exists here.
- `.aw/records/runs/` IS GITIGNORED (`.aw/.gitignore:14`). Use fixtures, and inject `env`/`cwd` rather than reading the real host config. Suite bare: `python3 -m pytest`; measure the baseline in the executing worktree and compare failing NODE IDS, never totals. Measured at review on `4cfa0a90`: `1 failed, 5919 passed, 3 skipped, 2 xfailed`, the single failure being `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, caused by 189 untracked `opencode-recovery/*.md` files belonging to another party in this shared checkout. It is ENVIRONMENTAL, it is not `test_orchestrator_retirement` as this plan originally claimed, and those files must not be touched.

## Findings

F-1..F-9 were authored at HEAD `44d4950d` over 135 runs. F-10..F-18 were added at review at HEAD `4cfa0a90` over 140 runs, and each corrects a figure or a premise above; where they disagree, the review figure is the current one.

| Id | Severity | Location | Finding | Evidence |
| --- | --- | --- | --- | --- |
| F-1 | PARTLY OBSOLETE | `oc_runipd.py:2703`, `:3019` | The item says `options.model` is null on EVERY run; RE-MEASURED it is set in 9 of 140 (5 of 135 when authored), and `launch_profile_record` now freezes a full provenance object into 34 of 140. So the CONTAINER half of gap 1 shipped with `runprofile` `3cm15q` and must not be rebuilt. | scanned all 140 `state.json`: 9 with `options.model`, 34 with `options.launch_profile` |
| F-2 | HIGH | `runner_profiles.py:492` | What survives of gap 1: the recorded `model` reads `host-default` ("nothing supplied it; pass no argument") in 32 of the 34 profile-bearing runs. The record honestly names the gap instead of filling it. | read all 34 `launch_profile` objects |
| F-3 | HIGH | `sessions/*.jsonl` | The cost-bearing step records carry NO model key: 32333 `step_finish` parts, key set exactly `{id, reason, snapshot, messageID, sessionID, type, tokens, cost}` on every one. Superseded in its second half by F-11: the corpus is NOT devoid of model ids. | walked all 496 session logs; enumerated the key set of every part carrying `cost` |
| F-4 | HIGH | `oc_models.py:96`, `:607` | The rate card is not in the repository at all; it lives in the host config, which `aw oc update-models` mutates from a LiteLLM endpoint. | source read; the maintainer's config has multiple timestamped `.bak` siblings |
| F-5 | HIGH | research `x0spmh` | The card was corrected mid-history from `5.00/25.00, cache_read UNPRICED` to `5.50/27.50/0.55/6.875`, so pooled dollar comparisons across that boundary are invalid and an Aug 24 `cache_read = $0` is a config artifact. Cache reads were 73.9% of a $16.41 turn. `aflsz3` F-3 independently reproduced both eras against 29648 of 29671 steps. | the research record; the sibling plan's least-squares reconstruction |
| F-6 | OVERLAP | `5f2h8i` E-02, `aflsz3` E-07, `8hald1` E-04 | The `runanalytics` Set DECLARES per-invocation model metadata, effective-dated price schedules with source/version and effective intervals, and a normalized fact schema carrying `runner/model/variant`. Those are its deliverables, not this plan's; but they CONSUME a record that today contains neither a model nor a card. E-item numbers corrected per F-18. | read those three plans' E-items and conventions |
| F-7 | SUPERSEDED BY F-16/F-17 | `agy_runipd.py` | Authored as "agy has zero profile integration AND zero agy runs, so parity is not free and agy evidence is synthetic". The profile half holds; the model half and the zero-runs half are both false. See F-16 and F-17. | superseded at review |
| F-8 | MED | `oc_models.py:301`, `:501`, `:138` | The no-secret catalog reader, the untrusted-value refusal, and the `.jsonc`-unwritable classification already exist, so the security property and the unknown paths are established rather than needing invention. Note the LIMIT recorded in F-12: none of them returns the DEFAULT model. | source read of the three docstrings |
| F-9 | LOW | 34 `launch_profile` objects | `config_source` already records an absolute maintainer home path, and the `runanalytics` orchestrator measured the same in `repo` and `driver.path`. Pre-existing, not created here, but do not make it worse. Measured at review: `aw sanitize --agent` is `clean`, 0 findings. | read the live objects; ran the sanitizer |
| F-10 | MED | every figure in the authored plan | **EVERY QUANTITY IN THE PLAN MOVED IN ONE DAY, INCLUDING THE ONES USED AS ARGUMENTS.** 135 runs -> 140; `options.model` 5 -> 9; `launch_profile` 33 -> 34; cost-bearing steps 28158 -> 32333; oc-to-agy imports 47 -> 48; `launch_profile_record` `:2740` -> `:2703`; `PROVENANCE_HOST_DEFAULT` `:378` -> `:492`; `PROVENANCE_VALUES` `:380` -> `:500`. The gate already warns against navigating by line number; this is the measurement proving the warning applies to the plan's own citations. | re-ran every count at review |
| F-11 | MED | `part.state.metadata.model.modelID` in 16 session records | **"0 DISTINCT MODEL IDS" IS FALSE AND CONTRADICTS A SIBLING'S BETTER MEASUREMENT.** 16 real `modelID` values exist (2 distinct), on `task` TOOL CALLS, identifying the SUB-AGENT rather than the main agent; agy's `init` event carries its model in 2 more files. `aflsz3` F-5 had already measured exactly this ("only 16 of 462 session files mention `modelID` ... so it identifies the SUB-AGENT's model"). The claim that matters (no cost-bearing step carries a model) is intact; the absolute claim is not, and asserting it would put two plans in the same repository in contradiction. | enumerated every model/provider key path across all 496 session logs |
| F-12 | HIGH | `oc_models.py:301`, `:330`; live config top-level `model` key | **E-01 AS AUTHORED POINTED THE EXECUTOR AT A READER THAT CANNOT ANSWER ITS QUESTION.** It says to resolve the host default through `models_from_config`, which reads only `provider.<name>.models.<id>` KEYS and returns the 82-entry catalog; the DEFAULT is the config's top-level `model` key, which nothing in this repository reads. So the instruction yields a list and no answer, and an executor could either stall or invent a second config reader (the one thing the item forbids). A small accessor in `oc_models` is required and is already permitted by the Scope check. | called `resolve_config_path` and `models_from_config` in-process; read the live config's key set |
| F-13 | HIGH | live config: 82 models, 80 priced, 47 without `cache_read` | **THE PARTIAL CARD IS THE CURRENT MAJORITY, NOT A HISTORICAL ARTIFACT, SO "FOUR COMPONENTS" IS THE WRONG SCHEMA.** 47 of the 80 priced models carry only `input` and `output`. A schema that assumes four components must either invent zeros for 59 percent of the card (the `x0spmh` trap, at scale) or carry a named unknown per component. Also measured: 2 of 82 models have no `cost` block at all. | walked every `provider.*.models.*.cost` block in the resolved config |
| F-14 | HIGH | `oc_models.py:501`, `:565-577`, module docstring | **REUSING `per_million` ON A CONFIG VALUE WOULD INTRODUCE THE 1000000x ERROR E-02 WARNS ABOUT.** The config stores `$/Mtok` by construction (the module docstring says so; `build_models` calls `per_million` on the GATEWAY's per-token value before storing). E-02 as authored said to "reuse `oc_models`' price vocabulary" by name, and the nearest reading is to call that function on a value already in the target unit. The refusal posture is what should be reused; the multiplication must not be. | read the converter, its one call site, and a live cost block |
| F-15 | HIGH | `run_viewer.py:563`; grep across `agent_workflows/` | **E-03'S PREMISE IS INVERTED: NOTHING PRICES FROM A CARD TODAY, SO THERE IS NO SUBSTITUTION TO STOP.** Reported cost is the sum of `part.cost` from `step_finish` events, persisted onto the attempt; no module outside `oc_models` multiplies tokens by a rate. An executor told to "make the recorded card the authority for the reported cost" would search for a substitution that does not exist and might BUILD the tokens-times-card path the same plan forbids in E-06 and the fence. The honest item is a regression GUARD plus a stated invariant. | read the metric extractor and its call sites; grepped for any rate multiplication |
| F-16 | HIGH | `agy_runipd.py:383`, `:2039`, `:4714`; 4 agy runs | **AGY ALREADY RECORDS A CONCRETE MODEL, SO THE PLAN'S AGY FRAMING IS BACKWARDS.** `DEFAULT_MODEL = "gemini-3.7-flash-high"` is written into `options["model"]` at run creation and is the flag default; all 4 agy runs carry it, and one corroborates it in its session log's `init` event. Agy needs no model work; oc is the host that is behind. What agy genuinely CANNOT resolve is a CARD: that model is absent from the OpenCode config's 8 gemini entries, and no Antigravity config reader exists here, so pointing it at OpenCode's config would attribute one provider's prices to another host's model. | read the three sites; scanned all 4 agy `state.json` and both agy session logs; searched the config for the model id |
| F-17 | MED | 4 `state.json` with `driver.path` ending `agy_runipd.py` | "This checkout holds ZERO agy runs, so any agy evidence is necessarily synthetic" is stale. There are 4, one with a real execute and a real verify session log. Fixtures remain required for ASSERTIONS (the tree is gitignored), but the plan must not instruct the executor to label real evidence synthetic. | driver census over all 140 runs |
| F-18 | LOW | `aflsz3` E-07/E-08, `8hald1` E-04 | The boundary citations name the wrong E-items: effective-dated pricing is `aflsz3` E-07 (E-02 is the unclassified/overlap accounting) and the fact schema is `8hald1` E-04 (E-02 is schema-drift tolerance). E-06 requires reading those plans, so a wrong pointer sends the reader to the wrong item. Also: all ten children are `approved` while this plan is `to-review`, and no dependency edge connects them, so the consumers can execute first. | read both plans' E-item lists and front matter |

## Proposed changes (ordered, validatable)

1. E-01 fills the host-default model value by adding ONE no-secret accessor for the config's top-level `model` key (nothing existing returns it, F-12), extending the existing provenance container and admitting any new provenance value and key to the closed enum and its shipped test.
2. E-02 snapshots the card component-by-component with a SEPARATE digest and an explicit launch-time label, recording the unit as `$/Mtok` WITHOUT calling `per_million` (F-14) and distinguishing an absent component from zero (F-13).
3. E-03 states the measured no-pricing-path invariant and adds a regression guard, rather than changing a substitution that does not exist (F-15).
4. E-04 records agy's card as unresolvable-by-construction while adding NO agy model work, since agy already resolves one (F-16), siting any shared symbol in `runner_shared`.
5. E-05 proves every unknown path, the genuine-zero contrast, and the historical card-less shape from fixtures with an injected config.
6. E-06 pins the producer/consumer boundary with the `runanalytics` Set from those plans' on-disk statuses, and states the `benchmark_metrics` dollar-cost boundary.

## Deferred / out of scope (with reason)

- NOT GRADUATED AS ALREADY SHIPPED, and recorded in the backlog item rather than only here: the RECORD-A-PROVENANCE-OBJECT half of the item's gap 1. `runprofile` Order 03 (`3cm15q`, executed) shipped `launch_profile_record` with `model`/`variant`/`agent`/`config_digest`/`provenance`, present in 34 of 140 runs. This plan extends it; rebuilding it would duplicate an executed plan's deliverable.
- NOT GRADUATED AS OWNED BY THE `runanalytics` SET (all ten children `approved`), with the owner named per clause so nothing is silently dropped: EFFECTIVE-DATED PRICE SCHEDULES with currency, effective intervals and source/version (`aflsz3` E-07); COST RECOMPUTATION from tokens times card and the recorded-versus-estimated split (`aflsz3` E-07, `8hald1` E-05); POOLED-AGGREGATE EXCLUSION and missingness handling (`aflsz3` E-06/E-08); PER-MODEL AGGREGATION and model comparison with price-era stratification (`aflsz3` E-08 and its required analysis 12, which REFUSES the model arm at the measured 1.1 percent coverage); PER-TURN and per-attempt model capture (`5f2h8i` E-02, which explicitly requires per-file identity "not inferred later from a single run-level snapshot"); and any `aw runs` cost VIEW change (`mm5p3v` Order 08, plus spec `25kzda`'s fixed `aw runs` leaf table). The item's tests (b), (d) and (e) belong to those plans by the same reasoning. This plan is the producer they consume.
- PER-STEP MODEL ATTRIBUTION IN `sessions/*.jsonl`. OQ-01. Not graduated because no host emits it on a cost-bearing record: 32333 of 32333 `step_finish` parts carry no model key, and the item's own fix sketch says to "Prefer capturing what the host reports per step IF it can be made to emit it; otherwise snapshot at launch". This plan does the otherwise. Making the host emit it is an upstream question, and `5f2h8i` separately owns per-invocation telemetry. Note the 16 `task`-call `modelID` values (F-11) do NOT close this: they name a sub-agent, not the turn that incurred the cost.
- ANTIGRAVITY PRICING. E-04 records agy's card as unresolvable rather than resolving it, because that host's rates live outside every config this tool reads and its default model is absent from the OpenCode config (F-16). Building an Antigravity config reader is a separate piece of work with its own security surface and is not graduated here.
- CHANGING WHICH MODEL IS USED, or per-role model routing. `runprofile` owns that (`3cm15q` and `kgpptv` both executed; `kgpptv` shipped `verify_with`, `options.verify_model` and `options.verify_launch_profile`, present in 0 of 140 runs so far because no run has configured a verifier profile).
- AUTOMATIC PRICE SCRAPING. `aflsz3` excludes it explicitly, and `aw oc update-models` already exists as a deliberate operator action. This plan reads what is configured; it does not fetch.
- REPAIRING THE HISTORICAL RECORD. All 140 existing runs are card-less and must READ as such (E-03); back-filling a card onto them from today's config is precisely the history-rewriting `aflsz3` forbids.

## Scope check

- Over-scope: `oc_models.py` is in scope for exactly ONE addition, the no-secret default-model accessor F-12 shows is missing (plus its tests); do NOT change `sync_provider`, `build_models`, `per_million`, the write path, the atomic write, or the backup behavior. `runner_shared.py` is in scope only to hold a shared symbol. Do NOT touch `run_viewer.py` or any `aw runs` view.
- Under-scope: stated rather than left as `none`. After this plan a run knows its model and card, but nothing yet RECOMPUTES a cost, stratifies by price era, excludes unknown-card runs from an aggregate, or attributes a single STEP to a model. Each is named above with its owner. Two further honest limits: the improvement is FORWARD-ONLY, so `aflsz3` E-08's model-comparison refusal stays correct for every existing run; and the launch snapshot is a RUN-level fact, so a run whose verifier uses a different profile (the `kgpptv` capability, configured in 0 of 140 runs so far) will legitimately have two models that one field cannot express (OQ-01).
- `tests/test_runner_profiles.py` is NOT in `- Scope-Paths:` but WILL need editing if E-01 adds a provenance key or value, because `:829-832` pins the exact key set and `:836` pins the value enum. Make the edit and carry a `--scope-reason` at finalize rather than working around a deliberate contract test.
- ADDED TO `- Scope-Paths:` AT EXECUTION TIME, `tests/test_rununify_initialize_run.py`: this clause's own reasoning applied to a DIFFERENT contract test than the one it anticipated. That file pins the frozen `options` key set as a four-class partition with three counts, so ANY new frozen option key trips it by design, and its own comments record four previous plans making exactly this edit (union 34 -> 35 -> 36 -> 37). E-01/E-02 add one SHARED key (`cost_attribution`) and one OC-CONDITIONAL key (`verify_cost_attribution`), so the edit is unavoidable and is the response the tripwire exists to elicit (DECISION 16-w33lrl-D2). It is DECLARED here rather than deferred to a `--scope-reason` because `aw commit` and `aw check`'s `check.scope-drift` rule both evaluate the WORKING TREE against `- Scope-Paths:` and fail closed BEFORE finalize is reachable, so an undeclared edit cannot be committed at all. `tests/test_runner_profiles.py` is correspondingly NOT in this change: DECISION 16-w33lrl-D1 records why E-01's provenance-enum route was not taken, and both of its pinned assertions pass unmodified.

## Required tests / validation

`python3 -m pytest` bare in an isolated worktree, with the baseline measured THERE and pasted, comparing failing NODE IDS not totals. Do NOT add flags: `pyproject.toml` `addopts` already supplies `-q -n auto --dist=worksteal -m 'not slow'`. Measured at review on `4cfa0a90`: `1 failed, 5919 passed, 3 skipped, 2 xfailed`, the failure being `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` from 189 untracked `opencode-recovery/*.md` files belonging to another party; expect it, do not fix it, and do not touch those files.

Fixtures for every run-record shape; never the gitignored live tree, and never the maintainer's real host config (inject `env`/`cwd`). `tests/test_oc_models.py` (32 tests) is where config-reading cases belong. Three existing assertions are contracts this plan must not break and must be named in evidence: `tests/test_oc_runipd.py:4468` (the `launch_profile` `config_digest` survives a resume unchanged), `:4655` (the executor and verifier records share one `config_digest`), and `tests/test_runner_profiles.py:836` (every provenance value is in the closed enum). If `:829-832`'s exact key set must change, change it deliberately and say why.

Run `aw sanitize --agent` before and after and paste both, since this plan writes host-derived values into a tracked-adjacent record. Measured at review: `outcome: clean`, 0 findings.

## Spec / documentation sync

Spec `25kzda` (`.aw/records/specs/20260826-0718-01-aw-run-deterministic-run-and-verify.spec.md`, `- Status: approved`) §5.6 fixes the machine-output shape for a final item and does NOT include a model or a card field, and its `aw runs` leaf table is what the `runanalytics` orchestrator records Orders 08 and 09 as owing a DECLARED amendment for. THIS PLAN ADDS TO `state.json`'s `options`, which §5.6 does not enumerate, so no amendment is expected.

READ AT REVIEW, SO THE EXECUTOR NEED NOT REDISCOVER IT: §5.6 says "Each final item has at least" before its JSON block, which is an explicitly OPEN lower bound, not a closed shape; and §7 is a worked EXAMPLE (a table of hypothetical items and a narrated invocation), not a schema. Neither governs `state.json`'s `options`. So NO amendment to `25kzda` is required and the spec file is correctly ABSENT from `- Scope-Paths:`. Re-read §5.6's preamble at execution time and paste it; if a later amendment has closed that shape, declare the spec path before executing, because the runners announce declared spec edits before a run starts and the finalize scope gate reconciles declared against actual.

A SECOND SPEC-BOUND-TO-CODE HAZARD, WHICH THIS PLAN DOES NOT TRIP BUT MUST NOT START TRIPPING: `tests/test_run_flag_surface.py` parses §2.1's flag grammar and fails in BOTH directions (a spec-declared flag the code lacks, and the converse). This plan adds no run FLAG, so it is clear. If the work ever grows a flag, the spec edit and the registration must land in the same change; `5f2h8i` E-07 records the precedent.
Do NOT edit §4.2's finding-code table under any circumstances: it is transcribed verbatim into `run_evidence.RUN_FINDING_CODES` (a 13-row tuple whose length is asserted at `run_evidence.py:1649`) with a byte-equality test, so editing a cell IS a code change.

## Open questions

### OQ-01: Can the host be made to emit a per-step model id, and if so does that supersede the launch snapshot?

- Blocking: no
- Status: resolved
- Owner: this plan's executor for the measurement, the maintainer for any upstream request
- Resolution or deferral rationale: RESOLVED AT REVIEW, in two halves. FIRST, TODAY THE ANSWER IS NO, and the measurement is now exact: all 32333 cost-bearing `step_finish` parts across 496 session logs carry the key set `{id, reason, snapshot, messageID, sessionID, type, tokens, cost}` and nothing else. The 16 `modelID` values that DO exist (F-11) sit on `task` tool calls and name a SUB-AGENT, so they cannot be repurposed. The host would have to change; that is upstream of this repository and is not this plan's work. SECOND, THE SUPERSESSION QUESTION IS ALREADY ANSWERED BY THE REPOSITORY: `5f2h8i` E-02 states per-invocation identity "must be recorded per file and must not be inferred later from a single run-level snapshot", so the eventual home for per-turn identity is that plan's telemetry record, and this plan's run-level snapshot is the coarser fact that remains true. They coexist; neither supersedes the other.
  THE TWO-MODEL CASE IS LIVE, NOT HYPOTHETICAL, WHICH IS WHY THIS IS RECORDED RATHER THAN DROPPED. `runprofile` Order 06 (`kgpptv`) is EXECUTED, not pending as this plan originally said: `--verify-with` ships, `resolve_launch_pair` freezes a second launch, and `options.verify_model`/`options.verify_launch_profile` are written when a verifier profile is configured. Measured, 0 of 140 runs configure one, so no existing run has two models -- but the capability exists TODAY and the first run that uses it will have two. THEREFORE E-01 and E-02 must record the card and default for the VERIFIER launch too when `options.verify_launch_profile` is present, or state explicitly that they do not and why; a single run-level field that silently describes only the executor would misattribute the verifier's cost the first time the feature is used.

### OQ-02: Does agy record a card in this plan, or record that it cannot?

- Blocking: no
- Status: resolved
- Owner: this plan's executor
- Resolution or deferral rationale: RESOLVED AT REVIEW: it records that it CANNOT, and the reason is stronger than the authored rationale. The authored reasoning ("agy has zero profile integration and zero runs, so building profile support here is out of scope") rested on two facts, one of which is false: there ARE 4 agy runs (F-17), and agy already resolves a concrete model without any profile machinery (F-16). So the real finding is narrower and firmer: agy's model `gemini-3.7-flash-high` is NOT in the OpenCode config (which declares 8 gemini models, none of them that one), and Antigravity's own pricing lives in no file this tool reads. Resolving a card for it would require inventing an Antigravity config reader -- a new security surface, in a plan about cost attribution -- or, worse, silently pricing it from an OpenCode provider's card, which would attribute one vendor's rates to another's model. Record the named inability. No maintainer escalation is needed: nothing is being silently dropped, and the deferred section names Antigravity pricing as separate work.

### OQ-03: Is the field name and unit contract this plan sets binding on the `runanalytics` Set, and who reconciles if that Set lands first?

- Blocking: no
- Status: open
- Owner: this plan's executor for the reading, the maintainer for the coordination
- Resolution or deferral rationale: NOT blocking, because E-06 requires those plans' statuses be READ at execution time and the CONSUME-rather-than-duplicate rule applies whichever landed first, so the plan is executable in either order. It stays OPEN rather than resolved because the sequencing is genuinely the maintainer's, and the review sharpened the risk rather than removing it: all ten `runanalytics` children are `approved` while this plan is only `to-review`, and their dependency chain (`5f2h8i` -> `8hald1` -> `aflsz3`) contains NO edge to this plan, so `aw oc run all` can execute the entire consumer chain before this producer exists. None of the three declares a field name for the model or the card, so whoever executes second must bind to the first's name rather than adding a sibling field. The executor's obligation is bounded and stated in E-06; the ordering decision is not theirs to make.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [x] V-01 validates E-01
  - Required evidence: paste the resolved model as it appears in a real `state.json` for a run where no `--model` was passed, showing an actual model id rather than `host-default`, ALONGSIDE the recorded name of the config key that supplied it (top-level `model`, an agent override, or `small_model`). Paste the unchanged `model` field beside it, proving the true statement about the flag was preserved. Paste the new provenance value and its admission to `PROVENANCE_VALUES`, plus the updated `tests/test_runner_profiles.py` key-set and enum assertions PASSING (`:829-832`, `:836`) with their actual output. Paste the new accessor's docstring showing the no-secret guarantee, and paste the accessor's body proving it reads only the default-model key and never a `provider.*.options` block. Paste `aw sanitize --agent` before and after (baseline at review: `clean`, 0 findings). If `options.verify_launch_profile` is present, paste the verifier's resolved default too, or paste the explicit statement that the verifier launch is not covered and why (OQ-01).
  - Observed evidence: **THE GAP IS CLOSED AND THE OLD FIELD IS UNCHANGED.** From a LIVE frozen `state.json` (`tests.test_rununify_initialize_run.live_options('oc_runipd')`, which drives a real `initialize_run` with NO `--model`):
    `options.model` -> `null`; `options.launch_profile.provenance.model` -> `"host-default"`; `options.launch_profile.config_digest` -> `0d35fe7db671a7a3...` (covers `runner-profiles.json`).
    The NEW `options.cost_attribution` on the SAME run: `{"card": {"cache_read": 0.55, "cache_write": 6.875, "input": 5.5, "output": 27.5}, "card_config": "opencode.json", "card_config_covers": "opencode.json", "card_config_digest": "74714be105d46515b295fb3451c3f9d0fedd6a73ceb01dba9a178e7034c16c4e", "card_model": "uri/its_direct/pt3-claude-opus-5-1m-us", "card_reason": "", "cost_basis": "launch-time snapshot only: ...", "host": "oc", "kind": "launch-time-snapshot", "model": "uri/its_direct/pt3-claude-opus-5-1m-us", "model_source": "model", "unit": "$/Mtok"}`.
    So an ACTUAL model id is recorded where nothing could answer before, `model_source: "model"` NAMES the top-level config key that supplied it, and the `host-default` provenance is PRESERVED unchanged because it is a true statement about the flag.
    **DEVIATION FROM THE AUTHORED PLAN, AND WHY IT IS THE HONEST DESIGN:** this item anticipated adding a provenance VALUE and KEY, requiring the `tests/test_runner_profiles.py:829-832`/`:836` assertions to be edited. IT DID NOT. The resolved default is recorded in a SEPARATE `options.cost_attribution` record, not as a new field inside `launch_profile.provenance`, so the closed enum and both assertions are UNTOUCHED and still pass unmodified (`425 passed` over `tests/test_runner_profiles.py tests/test_oc_runipd.py tests/test_oc_models.py tests/test_hardened_profile_request.py tests/test_runner_profiles_e2e.py`). This is strictly better than the authored route: the two facts answer DIFFERENT questions (`provenance.model` = "which tier supplied the flag", `cost_attribution.model_source` = "which config key names the model"), and overloading one closed vocabulary with the other's answers is how a value comes to mean two things. `tests/test_runner_profiles.py` is therefore NOT in this change at all, so the plan's conditional scope note needed no `--scope-reason`.
    NO-SECRET ACCESSOR, docstring quoted: `default_model_from_config` -> "Reads ONLY the default-model identifiers: `agent.<agent>.model` when `agent` is named and declares one, else the top-level `model`. It never looks at a provider's options block, credential value, or headers, so no secret can reach the caller (the same guarantee :func:`models_from_config` carries, and for the same reason)." Body proof: it touches only `config.get("agent")[agent]["model"]` and `config.get("model")`, and `test_the_accessor_reads_NO_credential_bearing_key` PASSES with an `apiKey` and an `X-Token` header planted in the fixture, asserting neither string reaches the output. `resolve_config_path` is the ONLY file-resolution path used.
    VERIFIER LAUNCH COVERED (OQ-01), not excused: with `exec` -> `verify_with: check` configured, `EXECUTOR: {"model": "uri/alpha", "model_source": "profile", "card": {"input": 5.5, "output": 27.5, "cache_read": "absent", "cache_write": "absent"}}` and `VERIFIER: {"model": "uri/beta", "model_source": "profile", "card": {"input": 1.0, "cache_read": 0.1, "output": 4.0, "cache_write": "absent"}}` -> two models, two cards, separately attributable. Written as the conditional `options.verify_cost_attribution`.
    `aw sanitize --agent` BEFORE: `{"outcome":"clean","exit":0,"findings":0}`. AFTER: `{"outcome":"clean","exit":0,"findings":0}`.
  - Result: pass

- [x] V-02 validates E-02
  - Required evidence: paste the recorded card from a real `state.json`, showing every component PRESENT in the config, a named unknown for each ABSENT one, the model it applies to, the card digest, and the launch-time-snapshot label. Paste the recorded unit string (`$/Mtok`) and the code comment stating why `per_million` is NOT called; a V-02 that shows a `per_million` call on a config value FAILS. Paste a card carrying only `input`/`output` (the measured 47-of-80 majority case) beside one whose `cache_read` is a GENUINE ZERO, showing they are DISTINGUISHABLE in the record; that contrast is the `x0spmh` trap and a V-02 without it is incomplete. Paste the card digest's own key and the name of the file it covers, and paste `tests/test_oc_runipd.py:4468` and `:4655` passing UNMODIFIED, proving the profile `config_digest` was not overloaded.
  - Observed evidence: **THE CARD IS FROZEN, COMPONENT BY COMPONENT, WITH ITS OWN DIGEST.** Live record (full JSON in V-01): `"card": {"cache_read": 0.55, "cache_write": 6.875, "input": 5.5, "output": 27.5}`, `"card_model": "uri/its_direct/pt3-claude-opus-5-1m-us"`, `"card_config_digest": "74714be105d46515b295fb3451c3f9d0fedd6a73ceb01dba9a178e7034c16c4e"`, `"card_config_covers": "opencode.json"`, `"kind": "launch-time-snapshot"`, `"unit": "$/Mtok"`.
    SEPARATE DIGEST UNDER ITS OWN KEY, WITH THE COVERED FILE NAMED: `card_config_digest` covers `opencode.json` while `launch_profile.config_digest` (`0d35fe7db671a7a3...`) covers `runner-profiles.json`. The two are different values for different files on the same run, so nothing was overloaded. Digest sensitivity proved by `test_the_card_digest_is_its_OWN_key_and_NAMES_the_file_it_covers`, which edits the fixture's cost block and asserts the digest CHANGES.
    `per_million` NOT CALLED, and the comment says why. From `card_from_config`'s docstring: "`per_million` IS DELIBERATELY NOT CALLED HERE, and calling it would be a 1000000x error. That function is the WRITE-side converter: it turns a LiteLLM gateway's per-TOKEN figure into the `$/Mtok` value that gets STORED (see :func:`build_models`). ... What IS reused is that function's REFUSAL POSTURE: a bool, a string, a None, or a non-positive number is untrusted input and yields an unknown rather than a coerced price." Asserted by `test_per_million_IS_NOT_APPLIED_to_a_value_read_out_of_the_config`: `components["input"] == 5.5` while `oc_models.per_million(5.5) == 5500000.0`. A V-02 showing a `per_million` call on a config value would FAIL; there is none (grep of the changed files finds `per_million` only in its own definition, its one `build_models` call site, and the comments explaining the refusal).
    ABSENT vs GENUINE ZERO, THE `x0spmh` TRAP, DISTINGUISHABLE IN THE RECORD: partial card (the measured 47-of-80 majority case) -> `card.cache_read == "absent"`; card declaring `cache_read: 0` -> `card.cache_read == 0.0`. Asserted at BOTH layers: `test_an_ABSENT_component_and_a_GENUINE_ZERO_are_DISTINGUISHABLE` (accessor) and `test_a_PARTIAL_card_and_a_GENUINE_ZERO_survive_into_the_record_distinguishably` (record). Every component is always NAMED, so no absence is silently omitted.
    THE THREE NAMED CONTRACTS PASS UNMODIFIED: `tests/test_oc_runipd.py` resume-digest and executor/verifier-shared-digest assertions and `tests/test_runner_profiles.py`'s provenance enum are untouched by this change (`git diff --stat` lists neither `tests/test_runner_profiles.py` nor any edit to those assertions) and the combined run reports `425 passed`.
  - Result: pass

- [x] V-03 validates E-03
  - Required evidence: paste the measured invariant as it now appears in the code (the statement that no path prices tokens from a card). Paste the new regression test and its actual output, and show it FAILS when a card-derived price is deliberately substituted (paste that deliberate-break run, then revert it). Paste a run's reported cost, EDIT the host config's prices, and paste the same run's reported cost again showing it UNCHANGED. Paste a pre-change run (no recorded card) showing its card reported as ABSENT rather than borrowed. Paste a grep over the changed files for a tokens-times-rate expression, returning nothing new.
  - Observed evidence: **THE INVARIANT IS STATED AT THE RECORDING SITE AND GUARDED BY A TEST THAT PROVABLY FAILS ON A BREAK.** In `runner_shared.COST_NOT_RECOMPUTED_HERE`: "launch-time snapshot only: no path in this driver prices tokens from this card; the reported cost remains the sum of host-reported step costs and cannot change when the host config does", with the surrounding comment adding "DO NOT 'FIX' THIS BY WIRING THE CARD INTO A COST COMPUTATION. Recomputation from tokens times card (and the recorded-versus-estimated split it requires) is `run_analytics_pricing`'s ...". The string is also CARRIED IN the record as `cost_basis`, so a consumer reading only the JSON learns it.
    SUBSTITUTION TEST AGAINST THE REAL EXTRACTOR: `test_a_reported_cost_does_not_move_when_the_host_config_is_repriced` calls `run_viewer.extract_log_metrics` on a two-step log, reads `2.5`, REPRICES the host config 100x (`input` 5.5 -> 550.0, `output` 27.5 -> 2750.0, the exact mutation `aw oc update-models` performs), re-reads, and asserts `2.5 == 2.5`. PASSED.
    CONTROLLED NEGATIVE, RUN AND THEN REVERTED: with `def _DELIBERATE_BREAK_reprice(tokens, rate): return tokens * rate / 1_000_000` inserted into `runner_shared.py`, `python3 -m pytest tests/test_oc_runipd.py -k test_the_runner_path_contains_NO_tokens_times_rate_expression` reported `FAILED tests/test_oc_runipd.py::CostIsNeverRecomputedFromTheCardTests::test_the_runner_path_contains_NO_tokens_times_rate_expression` / `1 failed in 4.89s`. The break was then removed (`grep -c "_DELIBERATE_BREAK" agent_workflows/runner_shared.py` -> `0`) and the same command reported `1 passed in 4.79s`. So the guard is evidence, not decoration.
    A CARD-LESS RUN REPORTS ABSENT, NEVER BORROWED: `test_a_run_with_NO_recorded_card_reports_it_ABSENT_rather_than_borrowing` asserts a pre-change `options` shape carries no `cost_attribution` key and that a reader gets `None` rather than a substituted current card. No back-fill onto history was performed: the change only WRITES the key at run creation, so every pre-existing run stays card-less by construction.
    NO SECOND COST COMPUTATION AND NO CHANGE TO HOW `part.cost` IS SUMMED: `run_viewer.py` is NOT in this change (`git diff --stat` does not list it), and the guard test scans `oc_runipd.py`, `agy_runipd.py`, `runner_shared.py` and `run_viewer.py` for `per_million(`, `1_000_000` and `input_per_mtok`, returning nothing new.
  - Result: pass

- [x] V-04 validates E-04
  - Required evidence: paste the agy cannot-resolve marker from a FIXTURE-based agy record, and paste an oc record whose config was found but unparseable, showing the two are DISTINGUISHABLE. Paste proof NO agy model work was added: show agy's `DEFAULT_MODEL` path unchanged and cite a live agy run's `options.model`. Paste the search of the OpenCode config for agy's default model id, returning nothing, as the justification for the marker. Paste the AST-measured oc-to-agy import count before and after, showing it did not increase from 48 (NOT 47; see F-10). Paste the grep confirming agy's profile-integration status at execution time, since it may have changed.
  - Observed evidence: **AGY RECORDS A NAMED INABILITY AND GAINED NO MODEL WORK.** Fixture-based agy record: `{"host": "agy", "model": "gemini-3.7-flash-high", "model_source": "host-default-constant", "card": {}, "card_reason": "host-card-not-in-any-readable-config", "card_config": "", "card_config_digest": "", "kind": "launch-time-snapshot", "unit": "$/Mtok"}`. Also confirmed through a REAL agy `initialize_run` (`live_options('agy_runipd')`), which freezes exactly that record.
    DISTINGUISHABLE FROM A FOUND-BUT-UNPARSEABLE OC CONFIG: agy -> `card_reason: "host-card-not-in-any-readable-config"`; oc with a `.jsonc` config -> `model_reason: "unparseable-config"`. `test_the_agy_inability_is_DISTINGUISHABLE_from_an_unparseable_oc_config` asserts the two differ, because one means "no reader exists" and the other means "the file needs hand-editing".
    NO AGY MODEL WORK: `agy_runipd.DEFAULT_MODEL == "gemini-3.7-flash-high"` unchanged, and the source still contains `"model": getattr(args, "model", DEFAULT_MODEL),` verbatim (`test_agy_model_resolution_was_NOT_touched`). The live agy run's `options.model` is `gemini-3.7-flash-high`, i.e. this host already resolved a concrete model and needed nothing from this plan.
    THE MARKER IS JUSTIFIED, NOT A SHRUG: `oc_models.models_from_config(<config>)` does not contain `gemini-3.7-flash-high`, and `card_from_config(<config>, "gemini-3.7-flash-high")` returns `({}, "model-not-in-config")` (`test_agys_model_is_absent_from_the_opencode_config_which_JUSTIFIES_the_marker`). Re-measured against the live config at execution time: it declares 8 gemini models (`gemini-3.5-flash`, `gemini-3.1-pro-preview`, `gemini-3.1-flash-lite`, `gemini-3.1-flash-lite-preview`, `gemini-2.5-pro`, `gemini-2.5-flash`, `gemini-2.5-flash-lite`, `gemini-3.7-flash`) and `gemini-3.7-flash-high` is NOT among them. No Antigravity config reader was invented: `test_NO_antigravity_config_reader_was_invented` asserts `antigravity.json`/`ANTIGRAVITY_CONFIG`/`apiKey` appear nowhere in the module and that it CALLS no `resolve_config_path`/`card_from_config`/`resolve_host_default_model`/`resolve_api_key` (asserted over parsed CALL sites, not substrings, because the module's own prose legitimately names `resolve_config_path` while explaining why agy must not use it).
    IMPORT COUNT: **56 BEFORE AND 56 AFTER, NOT 48.** The plan's figure (and its F-10 correction of 47 -> 48) is STALE AGAIN; AST-measured at execution time across all 8 `from agent_workflows.oc_runipd import` statements the count is 56, which `tests/test_rununify_initialize_run.py`'s own comments independently corroborate ("drops the oc-to-agy import coupling backlog `cnwy8g` tracks from 56 to 53"). It did NOT increase: the shared symbol is sited in `runner_shared` and `test_the_shared_symbol_lives_in_runner_shared_NOT_in_oc_runipd` asserts `cost_attribution_record.__module__ == "agent_workflows.runner_shared"` and `len(from_oc) == 56`.
    AGY PROFILE INTEGRATION RE-GREPPED AT EXECUTION TIME: `runner_profiles` still appears in `agy_runipd.py` only inside comments explaining the asymmetry; `resolve_launch_profile`/`launch_profile` remain absent, and `tests/test_oc_runipd.py:6955`'s `assertFalse(hasattr(agy_runipd, "launch_profile_record"))` still passes. So the no-profile-machinery half of F-7 still holds.
  - Result: pass

- [x] V-05 validates E-05
  - Required evidence: paste the tests and their actual runner output for EVERY unknown path: no config at all, `.jsonc` config, config with no top-level `model` key, default model with no `cost` block, card carrying only `input`/`output`, and a card value that is a string, a bool, or negative. Paste the GENUINE-ZERO case beside the absent case. For each, paste the recorded value showing it is distinguishable from a resolved card AND from zero. Paste the historical card-less fixture's result. Paste proof no test reads `.aw/records/runs/` and no test reads the real host config (show the injected `env`/`cwd` in each new test).
  - Observed evidence: **EVERY UNKNOWN PATH IS FIXTURE-DRIVEN AND PASSES.** `python3 -m pytest tests/test_oc_models.py -k "HostDefaultModel or RateCard" -v -p no:randomly -o addopts=""`, actual output:
    ```
    HostDefaultModelTests::test_a_full_catalog_with_NO_default_key_is_still_unknown PASSED
    HostDefaultModelTests::test_a_jsonc_config_is_UNPARSEABLE_BY_DESIGN_and_named_as_such PASSED
    HostDefaultModelTests::test_a_malformed_default_is_REFUSED_not_recorded PASSED
    HostDefaultModelTests::test_an_agent_override_wins_and_says_so PASSED
    HostDefaultModelTests::test_an_agent_that_declares_no_model_falls_through PASSED
    HostDefaultModelTests::test_no_config_at_all_is_a_NAMED_unknown PASSED
    HostDefaultModelTests::test_small_model_is_NOT_a_fallback PASSED
    HostDefaultModelTests::test_the_PATH_is_never_recorded_only_the_basename_and_digest PASSED
    HostDefaultModelTests::test_the_accessor_reads_NO_credential_bearing_key PASSED
    HostDefaultModelTests::test_the_top_level_model_key_is_the_default_and_the_key_is_RECORDED PASSED
    RateCardFromConfigTests::test_a_full_four_component_card_is_read_verbatim PASSED
    RateCardFromConfigTests::test_a_model_absent_from_the_config_is_a_DIFFERENT_named_unknown PASSED
    RateCardFromConfigTests::test_a_model_with_NO_cost_block_is_a_NAMED_unknown PASSED
    RateCardFromConfigTests::test_an_ABSENT_component_and_a_GENUINE_ZERO_are_DISTINGUISHABLE PASSED
    RateCardFromConfigTests::test_an_untrusted_component_value_is_MALFORMED_not_coerced PASSED
    RateCardFromConfigTests::test_no_model_to_price_is_named_rather_than_empty PASSED
    RateCardFromConfigTests::test_per_million_IS_NOT_APPLIED_to_a_value_read_out_of_the_config PASSED
    RateCardFromConfigTests::test_the_unit_is_declared_and_is_dollars_per_million PASSED
    18 passed
    ```
    THE SEVEN REQUIRED PATHS, each with its recorded value: no config at all -> `model_reason: "no-config-found"`; `.jsonc` config -> `"unparseable-config"` (digest still frozen, since the file WAS read); no top-level `model` key while the catalog is FULL -> `"no-default-model-key"` (the same test proves `models_from_config` returns `("uri/alpha",)`, so this is genuinely a default-key absence and not an empty config); default model with no `cost` block -> `card_reason: "model-has-no-cost-block"`; card carrying only `input`/`output` -> `card.cache_read == "absent"`; a value that is a string, bool or negative -> `"malformed"` (asserted over `True, False, "5.5", None, -1, [5.5]`, and asserted NOT EQUAL to `"absent"`, because an unusable assertion and a missing one need different operator action); GENUINE ZERO -> `0.0`. All seven also asserted at the RECORD level by `test_every_unknown_is_NAMED_rather_than_omitted` and `test_a_PARTIAL_card_and_a_GENUINE_ZERO_survive_into_the_record_distinguishably` (`22 passed` for the record-level classes).
    HISTORICAL CARD-LESS SHAPE: `test_a_run_with_NO_recorded_card_reports_it_ABSENT_rather_than_borrowing` uses a fixture shaped like a pre-change `state.json` (`{"run_id": ..., "options": {"model": None, "opencode": "opencode"}}`) and asserts no card key is present and none is substituted.
    INJECTION PROVED, NO LIVE READS: every new `oc_models` test passes `env=`/`cwd=` explicitly, and every new record-level test sets `OPENCODE_CONFIG` to a `TemporaryDirectory` path under `mock.patch.dict`. No new test touches `.aw/records/runs/` (gitignored) or the maintainer's real `~/.config/opencode/opencode.json`. Note the corpus state measured in THIS lane worktree: `.aw/records/runs/` contains **0** run directories, which is itself why a corpus-based assertion would not survive a lane and why fixtures were mandatory.
  - Result: pass

- [x] V-06 validates E-06
  - Required evidence: paste the `- Status:` line and directory of `5f2h8i`, `aflsz3` and `8hald1`, read at validation time (all three were `approved` in `pending/` at review). State which branch applied and what was done. Paste the field NAME and UNIT as the stated contract. Paste the code comment reconciling this record with `benchmark_metrics`' dollar-cost prohibition. Paste proof this plan implemented NO effective-dated schedule, NO pooled-aggregate exclusion, NO per-model comparison and NO cost recomputation: name each and show the absence, since the whole point of this V-item is that the boundary held. Paste proof `aflsz3` E-08's coverage refusal was not weakened.
  - Observed evidence: **THE CONSUMER SET HAS EXECUTED, SO THE CONSUME-RATHER-THAN-DUPLICATE BRANCH APPLIED.** Read on disk at validation time (all three were `approved` in `pending/` at review, and all three have since moved):
    ```
    5f2h8i: executed/ -> - Status: executed
    8hald1: executed/ -> - Status: executed
    aflsz3: executed/ -> - Status: executed
    ```
    WHICH BRANCH APPLIED AND WHAT WAS DONE: since they landed FIRST, E-06 required checking whether any of them already writes a model or card field to bind to rather than adding a parallel one. NONE DOES: `run_analytics.py` READS `options.model` (`_model_of`, `state["options"]["model"]`) and no `run_analytics*` module writes into `options` at all (grep for `options[` across all 19 modules returns nothing). `run_analytics_pricing` carries its own `PriceRates`/`PriceEra`/`PriceSchedule`, which are its effective-dated SCHEDULE and not a per-run record. So there was no existing field to consume and no duplicate was created: this plan's field is the one they will consume.
    THE CONTRACT, STATED: field name `runner_shared.COST_ATTRIBUTION_KEY == "cost_attribution"` (with the conditional verifier twin `verify_cost_attribution`), unit `oc_models.CARD_UNIT == "$/Mtok"`, components `oc_models.CARD_COMPONENTS == ("input", "cache_read", "cache_write", "output")` with `"absent"`/`"malformed"` as the named unknowns. Pinned by `test_the_field_name_and_unit_are_a_stated_contract`.
    `benchmark_metrics` BOUNDARY WRITTEN DOWN, in `runner_shared.BENCHMARK_DOLLAR_BOUNDARY` and the comment above it: "`benchmark_metrics` is documented 'never dollar' and RAISES `MetricError` on a `cost`/`usd`/`price`/`dollars`/`spending`/`dollar_cost` key in a benchmark's trial USAGE mapping ... THIS record is a RUN record's launch snapshot, not a benchmark usage mapping, and it exists for the opposite reason: to make an already-recorded dollar figure ATTRIBUTABLE. ... If a future change ever routes this card into `benchmark_metrics`, that prohibition applies and this snapshot is not an exception to it." The prohibition is asserted still live (all six forbidden keys present in `_FORBIDDEN_COST_KEYS`).
    THE FOUR EXCLUDED DELIVERABLES, EACH NAMED AND SHOWN ABSENT (`test_this_plan_implements_NO_effective_dated_schedule_or_recomputation`): NO effective-dated schedule (`PriceSchedule`/`PriceEra` exist in `run_analytics_pricing` and `hasattr` is False on all three runner modules); NO cost recomputation (`price_step` likewise absent from the runners, plus V-03's tokens-times-rate grep); NO pooled-aggregate exclusion rule (`stratify_by_era` absent from the runners); NO per-model comparison (nothing added to any `aw runs` view; `run_viewer.py` is not in this change).
    `aflsz3` E-08's COVERAGE REFUSAL LEFT INTACT: `run_analytics_statistics.CORPUS_BASELINE["model_identity_coverage"] == 0.011` unchanged, and `run_analytics_query`'s caveat that "model identity is near-absent in historical data" is untouched. This plan is FORWARD-ONLY and cannot improve historical coverage, so that refusal stays correct.
  - Result: pass

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

Scope fence: touch ONLY the seven paths in `- Scope-Paths:`, plus `tests/test_runner_profiles.py` if and only if E-01 changes the provenance key set or value enum (carry a `--scope-reason` for it). Do NOT rebuild `launch_profile_record`; extend it. Do NOT write a config reader anywhere but the ONE new accessor in `oc_models`. Do NOT call `per_million` on a value read out of the config. Do NOT change `oc_models.sync_provider`, `build_models`, `per_million` itself, the write path, the atomic write, or the backups. Do NOT invent an Antigravity config reader. Do NOT implement an effective-dated price schedule, a pooled-aggregate exclusion rule, a per-model comparison, or a cost recomputation: all four are `runanalytics` deliverables. Do NOT touch `run_viewer.py` or any `aw runs` view. Do NOT back-fill a card onto a historical run. Do NOT add a symbol to `oc_runipd` for `agy_runipd` to import. Do NOT edit spec `25kzda` (§5.6's "at least" preamble was read at review and admits the addition), and never its §4.2 finding-code table. Do NOT touch the 189 untracked `opencode-recovery/*.md` files: they belong to another party in this shared checkout and cause the one known suite failure. If the work GENUINELY requires a path outside the fence, MAKE THE EDIT AND JUSTIFY IT, since `aw ipd finalize` refuses to complete until every out-of-scope path carries a `--scope-reason` and every declared-but-unmodified path carries a `--scope-ack`.

RE-LOCATE BY SYMBOL, NEVER BY THE LINE NUMBERS IN THIS PLAN, AND THE PROOF IS THIS PLAN'S OWN CITATIONS. `oc_runipd.py` and `agy_runipd.py` are the highest-contention files in this repository and are being edited by live runs. Measured at review, ONE DAY after authoring: `launch_profile_record` moved `:2740` -> `:2703`, `PROVENANCE_HOST_DEFAULT` `:378` -> `:492`, `PROVENANCE_VALUES` `:380` -> `:500`, and the oc-to-agy import count 47 -> 48 (F-10). Every corrected number above is itself a snapshot and will drift again. Find `launch_profile_record`, `resolve_launch_pair`, `resolve_launch_profile`, `runner_profiles.resolve`, `PROVENANCE_VALUES`, `oc_models.resolve_config_path`, `models_from_config`, `per_million`, `agy_runipd.DEFAULT_MODEL`, and `run_viewer.extract_log_metrics` by name.

Execution contract: commit ONLY the files this plan changed, path-scoped (`git commit -m msg -- <paths>`); never `git add -A`, never `-a`, never push. Verify the staged set with `git diff --cached --name-only` before every commit and RE-VERIFY after any failed or hook-interrupted commit, since `pre-commit` can restore another agent's paths into the index. Paste ACTUAL runner output when reporting tests passed; never claim a suite result you did not run.

Post-gate lifecycle: requires explicit human approval (`aw ipd set approved w33lrl --by-human --message ...`) before execution. Do NOT hand-write a `Readiness:` field: that is `/plan-review`'s attested output. Transition via `aw ipd finalize` after `aw ipd lint --phase pre-transition` conforms and every `V-*` carries pasted evidence. ON COMPLETION, close backlog `vlf75p`, which this plan carries as `- From-Backlog:` and whose `- Blocks-Release: next` gate it inherits. The item's already-shipped and `runanalytics`-owned portions are recorded in the item itself with their owners named, so closing it does not silently drop them.
