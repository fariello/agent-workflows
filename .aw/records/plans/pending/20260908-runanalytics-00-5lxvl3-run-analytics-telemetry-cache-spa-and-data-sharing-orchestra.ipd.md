# IPD: Run analytics, telemetry, cache, SPA, and data-sharing orchestration

- Date: 2026-09-08
- Kind: orchestrator
- Concern: Coordinate a privacy-safe, incremental, statistically honest analytics system for OpenCode and Agy driver runs from capture through local report, agent query, and explicit sharing.
- Scope: Sequence Orders 01 through 10, preserve their ownership boundaries, map every prompt requirement to an executable child, and define whole-Set completion evidence.
- Scope-Paths: .aw/records/plans/pending
- Item-Dependencies: none
- Status: reviewed
- Readiness: go-pending-approval
- Set: runanalytics
- Order: 0
- Highest E allocated: 01
- Author: Codex
- Id: 5lxvl3

## Workflow history

- 2026-09-10 readiness re-check (opencode its_direct/pt3-claude-opus-5-1m-us): `- Readiness:` CHANGED `no-go` -> `go-pending-approval`. THIS IS A RE-CHECK, NOT A REVIEW: no finding was re-derived and no plan content was re-critiqued. The three `no-go` conditions were RECOMPUTED with the shipped predicates and each was found clear: `plan_readiness.has_unresolved_blocking_question` -> False; `review_findings.subject_gating_blocks` -> empty; `plan_readiness.newest_verdict` polarity -> neutral (not negative). Specifically, its blocking OQ-01 is moot on re-measurement and the finding it escalated (PR-002) is now closed in review round 2. Performed at HEAD `84111de2` at the maintainer's explicit instruction of 2026-09-10, who was shown that 10 of 15 `no-go` plans were held by stale bookkeeping and chose to have them hand-fixed with evidence recorded rather than re-reviewed. HUMAN APPROVAL IS STILL REQUIRED AND WAS NOT GIVEN: `go-pending-approval` means the plan awaits sign-off, and nothing here approves it or clears it to execute. Only a review may set `go`.

- 2026-09-08 reviewed (opencode/its_direct/pt3-claude-opus-5-1m-us): /plan-review REVIEWED - OPEN QUESTIONS; PR-001..PR-009, eight FIXED and one OPEN; Readiness no-go pending OQ-01. The verdict token is stated explicitly because `plan_readiness.newest_verdict` reads the newest review record's first verdict token and falls back to a negative scan when none is present. THE OPEN ONE IS OQ-01 (`Blocking: yes`, PR-002) and it is a maintainer decision on scope: all ten children carry EXACTLY THREE E-items (31 across the Set) against a pending-corpus range of 3 to 9 and a median of 6, and four of those items each bundle multiple independent deliverables and test surfaces (Order 07 E-02 is the entire SPA; Order 09's three items are three distinct trust surfaces; Order 06 E-02 is pricing plus all statistics; Order 10 E-03 is the whole 33-scenario matrix plus performance, sanitizer, offline, suite, lint and diff). The count-based size lint fires above 18 leaves so three passes by construction and catches none of it. Not resolved unilaterally because re-cutting four children rewrites another agent's Set. THE STRUCTURAL FIX THAT WAS NOT OPTIONAL: this plan's own history was ordered OLDEST-FIRST, which made `check.lifecycle-transition-invalid` read the recorded stream as `to-review -> draft` and refuse it as a backwards transition (verified via `ipd_lifecycle._plan_status_events` + `validate_transition`; all eleven plans in the Set share the defect). CORRECTED MEASUREMENTS, each of which changes what a child must do: this checkout holds 135 runs and ZERO Agy runs (120 `oc_runipd.py`, 13 legacy `runipd.py`, 2 `ipdrunner.py`), so "no live run corpus" as asserted in Orders 01/05/06/10 is FALSE and Agy parity evidence is necessarily synthetic, while the two legacy generations are real unnamed schema drift; every recent `state.json` carries an absolute maintainer home path in BOTH `repo` and `driver.path`, so the forbidden-value canary is in the first field an ingester reads; `resolve_target_runs` given an explicit PATH inside the reserved tree RETURNS it as a run (the scan excludes it only by name-prefix depth), so Order 01's exclusion is specifically a containment test on the explicit-target branch, and `completion.run_id_candidates` is an undeclared THIRD enumerator that will offer `analytics` as a completion candidate; the analytics tree is ALREADY worker-forbidden via `worktree_lease.FORBIDDEN_WORKER_PATH_HINTS`, so no child may weaken that predicate; and spec `25kzda` fixes the `aw runs` leaf table at lines 142-144, so Orders 08 and 09 owe a DECLARED amendment as Order 04 already does. ALSO FIXED: `Scope-Paths` enumerated all ten child files, every one of which is implicitly in scope and would have forced ten `--scope-ack` flags at finalize; the gate carried NO execution contract at all (no scope fence, no paste-actual-output honesty rule, no path-scoped commit/never-push, no lifecycle move); V-01 restated the criteria instead of demanding pasted evidence; E-01 did not forbid the parent performing a child's work; and 16 escaped backticks rendered literally.
- 2026-09-08 to-review (Codex): decomposed the feature into ten dependency-ordered children and mapped storage, telemetry, analytics, SPA, agent access, sharing, and acceptance requirements.
- 2026-09-08 draft (Codex): created from the requested implementation prompt.

## Goal

Deliver a local-first analytics capability for every `oc_runipd.py` and `agy_runipd.py` execution record. The finished system incrementally caches privacy-safe facts, analyzes time/cost/tokens/activity/outcomes/resources with visible uncertainty, publishes one self-contained offline SPA plus agent-readable data, and permits data sharing only through explicit tiered export and a separate approved submission action.

## Detailed Implementation Checklist (TODO)

Execution-state rule: the orchestrator contains no feature implementation. The runner sequences its children and retires this plan only after every child has completed its own execution and validation gates.

THIS PARENT HOLDS ORCHESTRATION AND NOTHING BEYOND IT, and the distinction is mechanical rather than stylistic. `aw oc run` / `aw agy run` retire an Order-0 orchestrator once every child is `executed` on disk and deliberately SKIP the pre-transition E/V checkpoint (`ipd_lifecycle.ROLLUP_OMITTED_GATES['pre-transition-ev-checkpoint']`, spec `77tr3o` R-5) on the premise that a parent's items are performed by NOBODY. Any work parked here is therefore marked complete having never been performed OR verified; `84j8d7` is the measured precedent. E-01 below is sequencing, which the runner genuinely performs itself, so the premise holds for it. If a later revision finds a step this Set needs that no child covers, ADD A CHILD; do not add an item here, and do not delete the sequencing item either (a human executing `execute runanalytics` with no runner relies on it for ordering and completeness).

### Task group 1: Sequence the child-owned work

- [ ] E-01 SEQUENCE THE TEN CHILDREN IN DEPENDENCY ORDER, confirming each is `executed` on disk before dispatching the next, and STOP on the first that does not reach `executed`. Return any failed cross-contract validation to the child that owns the contract; DO NOT PERFORM ANY CHILD'S WORK FROM HERE, because if a child appears to need a change this parent could make, the child table is wrong and the fix belongs in the child.
  - Depends on: none
  - Expected outcome: all ten children are `executed` on disk in an order consistent with the table's `Depends on` column, with none skipped, no child's work performed by this parent, and no file under `agent_workflows/` or `tests/` touched by this parent's own commits.
  - Execution state: pending

## Child IPDs, sequence, and dependencies

| Order | Id | File | Primary ownership | Depends on |
|---:|---|---|---|---|
| 01 | `xbwq8n` | `20260908-runanalytics-01-xbwq8n-canonical-run-root-analytics-namespace-and-discovery-isolati.ipd.md` | Canonical runs root, reserved analytics tree, discovery/target exclusion | none |
| 02 | `bzz5e6` | `20260908-runanalytics-02-bzz5e6-privacy-safe-incremental-per-run-analytics-cache.ipd.md` | Per-run cache, privacy projection, fingerprints, locks, atomicity | 01 |
| 03 | `lhccjf` | `20260908-runanalytics-03-lhccjf-cross-host-resource-telemetry-schema-collector-and-sampling.ipd.md` | Telemetry schema, probes, pseudonyms, collector, config | 02 |
| 04 | `5f2h8i` | `20260908-runanalytics-04-5f2h8i-opencode-and-agy-runner-telemetry-lifecycle-integration.ipd.md` | OpenCode/Agy integration, resume/retry/verifier/shutdown parity, runner spec | 03 |
| 05 | `8hald1` | `20260908-runanalytics-05-8hald1-normalized-run-ingestion-fact-schema-and-conservation-checks.ipd.md` | Source adapters, normalized facts, provenance, conservation, quality | 02, 04 |
| 06 | `aflsz3` | `20260908-runanalytics-06-aflsz3-activity-taxonomy-effective-dated-pricing-statistics-and-fin.ipd.md` | Taxonomy, pricing eras, statistics, analyses, ranked findings | 05 |
| 07 | `6eq3oq` | `20260908-runanalytics-07-6eq3oq-self-contained-offline-analytics-spa-and-report-bundle.ipd.md` | Latest/snapshot bundle and accessible self-contained SPA | 06 |
| 08 | `mm5p3v` | `20260908-runanalytics-08-mm5p3v-agent-friendly-aw-runs-analyze-and-query-interface.ipd.md` | Fixed analyze/query CLI leaves and agent protocol | 07 |
| 09 | `ixis0c` | `20260908-runanalytics-09-ixis0c-sanitized-export-explicit-submission-and-setup-wizard-contro.ipd.md` | Export tiers, separate submission, consent, wizard/config | 08 |
| 10 | `9xycbh` | `20260908-runanalytics-10-9xycbh-integration-fixtures-documentation-packaging-and-regression.ipd.md` | Fixture corpus, docs, package proof, all acceptance evidence | 01 through 09 |

All runtime dependencies are execution-state edges, not file-overlap guesses. Orders 08 and 09 are deliberately serialized because both extend the routing-sensitive CLI. Order 10 does not invent behavior; it sends failures back to the owning child.

FILE OVERLAP IS NOT A HAZARD HERE AND MUST NOT BE REPORTED AS ONE. `aw oc run` / `aw agy run` give each execute item its own ISOLATED WORKTREE by default (`isolate_worktree`, default True at `oc_runipd.py:3063`), sort the queue with dependency DEPTH as the first key, and RE-CHECK dependencies at dispatch (an unmet edge marks that one item `dependency-blocked` and continues). So the serialization above is a correctness statement about the CLI grammar, not a warning that the runner needs help. Do not raise queue order, file overlap, or this Order-0 parent's placement in the queue as a risk; those are settled by the runner.

THE SHARED-CHECKOUT CAVEAT THAT IS REAL: fourteen other pending plans declare `oc_runipd.py` or `agy_runipd.py` in their own `Scope-Paths` (measured 2026-09-08 across `.aw/records/plans/pending/`), so Order 04, which is the only child in this Set touching either runner, will meet concurrent edits there. That is a merge-and-revalidate matter the runner already handles, and Order 04 should reconcile against HEAD rather than against its authoring citations.

## Completion criteria (the whole Set is done only when)

- `<resolved-runs-root>` resolves normally to `<repo>/.aw/records/runs`, and all analytics writes stay under its reserved `analytics/` child.
- The reserved tree contains latest user-facing outputs directly and uses `cache/`, `snapshots/`, and `exports/` for their named purposes; no analytics descendant is treated as a driver run.
  MEASURED AT REVIEW, so Order 01 knows exactly which half of the job is missing rather than re-deriving it: today's `run_viewer.discover_run_dirs` (`run_viewer.py:1077-1092`) filters children by the `run-` name prefix at ONE directory level, so it already ignores a nested `analytics/snapshots/run-*` by accident of depth, and `resolve_target_runs` by NAME/substring returns nothing for it. But `resolve_target_runs` given the explicit PATH of that directory RETURNS IT (verified: a synthetic `analytics/snapshots/run-20260101T000000Z-1/state.json` resolves as a run), because the explicit-target branch (`run_viewer.py:1113-1129`) tests only `is_dir()` plus the presence of `state.json` and applies no containment check. So the exclusion Order 01 owes is specifically a CONTAINMENT test on the explicit-target and file-target branches, not a change to the directory scan, and its E-03 adversarial case (a directory deliberately named `run-*` inside the reserved tree) is a real defect rather than a hypothetical. `completion.run_id_candidates` (`completion.py:448-460`) is a THIRD enumerator, listing every non-dot child of the runs root, and it WILL offer `analytics` as a shell-completion candidate the moment the directory exists (verified); it is not named in Order 01's `Scope-Paths` and must be added there or explicitly deferred with a reason.
- Source run directories are read-only inputs. Re-running analysis changes only tool-owned analytics outputs.
- Per-run caches use source-root/run identity, schema/version metadata, strong freshness inputs, incomplete-run rechecks, atomic publication, and safe concurrent-writer behavior.
- Cache, report, agent, metrics-export, and redacted-event artifacts contain no prompt/conversation/content, raw commands, arbitrary environment data, raw path/host/user identity, secrets, or other forbidden canaries.
  THE CANARIES ARE ALREADY IN THE SOURCES, WHICH IS WHY THIS IS THE SET'S HIGHEST-RISK REQUIREMENT AND WHY IT MUST NOT BE PROVEN ONLY WITH SEEDED FIXTURES. Measured at review: every one of the last 20 `state.json` files carries an ABSOLUTE MAINTAINER HOME PATH in both its `repo` and its `driver.path` fields, so the very first field an ingester reads is a forbidden value. Orders 02, 05, 09 and 10 must therefore prove the boundary against REAL local artifacts as well as planted canaries, since a fixture corpus authored by the same agent that authored the allowlist can only test the cases that agent already thought of. The deterministic authority for this class is the repo's own leak-sanitizer, not a hand-rolled regex: Order 10's sanitizer scan MUST consume `aw sanitize --agent` (`agent_workflows/leak_sanitizer.py`, whose `scan_text`/`scan_working_tree`/`scan_wheel` entry points already exist) rather than reimplementing detection, per P11 and P8. A second private-data detector in `run_analytics_*` would be exactly the drift the single-source rule forbids.
  ALSO SETTLED, so no child re-derives it: the reserved `analytics/` tree is ALREADY worker-forbidden. `worktree_lease.FORBIDDEN_WORKER_PATH_HINTS` contains `.aw/records/runs/`, and `path_is_worker_forbidden('.aw/records/runs/analytics/index.html')` returns True today, so an isolated worker lane may not write analytics output and `lane_containment` will refuse such a request as a coordinator surface. Order 01 must not weaken that predicate to make analytics writable from a lane; analytics is produced by the human or coordinator invoking `aw runs analyze`, not inside a worker turn.
- Both runners emit equivalent best-effort per-invocation telemetry with start/end basic snapshots by default, separately opt-in periodic samples, per-attempt/node identity, graceful degradation, and bounded overhead. PARITY IS PROVEN SYNTHETICALLY, NOT FROM THE LOCAL CORPUS, for the reason recorded in the next criterion.
- Current and historical OpenCode/Agy records normalize into phase-aware, attempt-aware facts with provenance, quality, missingness, overlap, and cost/token/time conservation.
  MEASURED AT REVIEW (2026-09-08) AND LOAD-BEARING FOR ORDERS 04, 05 AND 10: this checkout's 135 run directories contain ZERO Agy runs. Every `state.json` names an OpenCode-lineage driver (`agent_workflows/oc_runipd.py` 120, the legacy `tools/ipdrunner/runipd.py` 13, `tools/ipdrunner/ipdrunner.py` 2), and `agy_runipd.py` appears as the driver of none. So the Agy side of "both hosts" has NO observed on-disk shape to normalize against, and the three legacy driver paths are a REAL schema-drift axis the plans do not name. Two obligations follow. FIRST, Order 05's Agy adapter and Order 10's cross-host fixtures are written against the Agy runner's CODE and synthetic fixtures, and neither may claim corpus validation for Agy; a passing Agy adapter test proves conformance to the adapter's own assumptions, not to a real Agy run. SECOND, the two legacy driver generations MUST appear in Order 10's fixture corpus as their own scenarios, because they are the actual historical schema variance in this repo and scenario 3 ("mixed-runner corpus") as written would otherwise be satisfied entirely by OpenCode-lineage runs. Note also that the phrase "no live run corpus in this checkout", asserted in Orders 01, 05, 06 and 10, is FALSE as written: 135 runs exist and are ignored/disposable. The correct statement is that the corpus is present but is single-host, unversioned as test evidence, and must not be committed.
- All 16 mandatory analyses and at least four additional corpus-supported analyses are reproducible for aggregate and phase-separated views across time, cost, token components, runner, model/provider/variant, price era, and relevant dimensions.
- Recorded costs remain recorded; estimates use effective-dated documented price entries and are visibly labeled with source/version/effective interval.
- Findings are ranked and machine-readable, show evidence/effect/sample/coverage/uncertainty/caveats/alternatives/experiments, and never state correlation as causation.
- `analytics/index.html` is a self-contained offline SPA with exact data tables, linked controls, accessibility, injection safety, no network dependency, and documented large-data behavior. "USEFUL" IS NOT A CRITERION because it is not falsifiable; the testable form is that the bundle opens from `file://` with network denied and issues zero requests, every chart's numbers reconcile with the same values returned by `aw runs query`, and hostile strings in every rendered field are inert. Order 07 and Order 10 own those, and a screenshot is not evidence for any of them.
- `aw runs analyze` and `aw runs query` provide stable human and agent surfaces without breaking current viewer/ledger routing or creating dynamic aliases.
  THE ROUTING CONSTRAINT IS ALREADY IMPLEMENTED AND MUST BE REUSED, NOT RE-INVENTED: `aw runs` dispatches through `_ViewerOrLeafSubParsersAction`, which routes a first positional matching a leaf name to that leaf and otherwise delegates to a sibling VIEWER parser, and the `--` escape for a target that collides with a leaf name is an existing documented contract (`cli.py:1970-2010`, and the shared `_register_run_leaf` at `cli.py:1793`). Order 08's new leaves must be registered through that mechanism. NOTE the existing `_register_run_leaf` gives every leaf a single `target` positional documented as "a run ID or a ledger.jsonl path", which does NOT fit `analyze [TARGET ...]` or `query <view>`; Order 08 already records that it must not force its parsers through a helper whose arguments do not fit, and the `list` leaf (`cli.py:2106-2120`) is the in-repo precedent for registering a run leaf directly instead. Order 08 must cite that precedent rather than inventing a third registration path.
- Export is local, tiered, inspectable, and explicit. Raw export requires every-time risk acknowledgement. Submission is a separate explicit command and is unavailable until an endpoint policy is approved.
- Setup presents analytics as optional, asks separately about periodic sampling, never grants submission consent, and supports inspect/reconfigure/disable/delete behavior.
- The installed wheel and sdist contain all code/data/assets, and the mandatory end-to-end matrix, offline proof, sanitizer, performance measurements, bare full suite, lints, index check, and diff check pass.

## Cross-IPD validation

| Requirement family | Primary owner | Required handoff/cross-check |
|---|---|---|
| Canonical disposable locations and discovery isolation | 01 | 02, 07, 08, 09 use only its helpers; 10 searches for duplicate literals and missed selectors. |
| Safe incremental cache | 02 | 05 writes only its allowlisted schema; 08 reports hit/miss reasons; 10 proves selective rebuild. |
| Telemetry collection | 03 | 04 invokes exactly its lifecycle API; 05 parses its versioned events; 10 measures overhead and failures. |
| Runner parity and contemporaneous nodes | 04 | 05 fixture adapters preserve invocation/attempt/node/phase identity; 10 covers both hosts and resume. |
| Normalized fact contract | 05 | 06, 07, 08, and 09 consume the same facts; no consumer reparses sensitive run sources. |
| Attribution/pricing/statistics/findings | 06 | 07 renders and 08 queries identical measures; 10 compares chart/table/query values. |
| SPA and bundle | 07 | 08 publishes/locates it; 09 exports only companion-safe data; 10 proves offline/package behavior. |
| Human and agent CLI | 08 | 09 adds fixed leaves after it; 10 verifies routing collisions and documented examples. THE SPEC AMENDMENT IS OWED HERE AND ITS TARGET IS NAMED, because "amend the CLI grammar spec if one exists" is not a resolvable instruction and Order 08 currently states it conditionally. Spec `25kzda` (`- Status: approved`, aw-run deterministic run and verify) enumerates the `aw runs` leaves explicitly in its Section 3 command table (`aw runs show`, `aw runs evidence`, `aw runs verify-ledger` at spec lines 142-144), so ADDING `analyze` and `query` to that surface changes a table the spec fixes. Orders 08 and 09 MUST each list that spec file in their own `Scope-Paths` (as Order 04 already correctly does) so the runners' pre-run spec-impact announcement names it and the finalize scope gate reconciles it; a plan that edits an approved spec it never declared is exactly the drift the declaration requirement exists to catch. |
| Export/submission/setup | 09 | 10 attacks every sensitivity boundary and proves zero network outside explicit submit. |
| Integration evidence | 10 | Every failure is fixed in the owning child scope, then the complete matrix is rerun. |

Additional invariants:

- One vocabulary and enum set governs runner, phase, attempt, outcome, model/provider/variant, metric, taxonomy class, quality, pricing status, telemetry mode, and sensitivity tier.
- Every numeric chart value can be traced through query output to normalized fact IDs and source provenance without exposing sensitive source content.
- Aggregate review-plus-execute values reconcile with their separated components; verifier and recovery remain independently visible.
- Activity duration never exceeds elapsed time without a displayed overlap measure; missing time is displayed as unattributed, not assigned heuristically.
- Analyzer, report open, wizard, installation, cleanup, and export never submit data.
- A cache or report schema change invalidates or migrates explicitly; it never silently misreads old artifacts.

## Deferred / out of scope (with reason)

- A hosted collection/aggregation service, endpoint selection, legal/privacy approval, server retention, and deletion operations are not defined by this repository request. Until approved, submission remains unavailable.
- Automatic changes to prompts, gates, tests, model routing, or runner policy based on findings are excluded. Findings propose measured experiments for human review.
- Conversation-content NLP and source-code semantic analysis are excluded because they conflict with the minimized cache/output boundary.
- Real user run data is not committed or bundled with these IPDs.
- A live web dashboard, server, database, or network-loaded frontend dependency is unnecessary for the requested offline deliverable.
- Claims of anonymity, causal inference, or statistically valid ANOVA without assumptions and sample sufficiency are prohibited.

## Scope check

- Over-scope: no child-independent implementation is assigned to the orchestrator; network service operation and automatic optimization remain excluded.
- Under-scope: the ten children collectively own storage, incremental cache, privacy, telemetry, runner integration, historical ingestion, fact integrity, all analytics, price changes, findings, SPA, agent interface, export/submission, wizard, packaging, docs, and all mandatory tests.
- RIGHT-SIZING IS THE ONE UNRESOLVED STRUCTURAL RISK IN THIS SET, and it is a maintainer decision (OQ-01), so it is recorded here rather than silently accepted. MEASURED at review across all eleven plans: every child carries EXACTLY THREE E-items, 31 across the Set. That uniformity is the signal. The house corpus does not cluster at three; pending children range from 3 to 9 E-items and the executed corpus reaches 14, and the median pending child is 6. A constant-3 shape across ten children of visibly different sizes indicates the checklist granularity was chosen to fit a template rather than derived from the work, and the count-based size lint cannot see it (it fires above 18 leaves / 5 groups, so three always passes).
  THE CONSEQUENCE IS CONCRETE, not aesthetic. Three E-items must cover, per child, from two to four new source modules plus their tests, so several single items each name multiple independent deliverables and would need several unrelated V-items. The clearest cases: Order 07 E-02 is one item for the ENTIRE SPA (linked filters, metric/phase toggles, charts, tables, findings, pricing, quality, and raw-data exploration); Order 09 spans three different trust surfaces in three items (local bundle tiers, network transport, interactive wizard consent) where the transport and the consent model each deserve their own pass; Order 06 E-02 bundles effective-dated pricing WITH every required statistical aggregation, which are separate failure domains joined only by sequence; and Order 10 E-03 is a single item covering the 33-scenario acceptance matrix, performance budgets, the sanitizer scan, the offline proof, the bare suite, lint, and diff. Each of those satisfies the workflow's split test (multiple distinct deliverables, multiple independent test surfaces, executable as two or more independent passes), and a weaker executing model is most likely to lose the tail of such an item, which is exactly where the privacy and offline proofs sit.
  This is an UNDER-SCOPE finding on checklist granularity, NOT a claim that the ten-child decomposition is wrong: the ownership boundaries between children are sound and the dependency chain is coherent. The remedy is finer E/V granularity within the affected children (07, 09, 06, 10 first), or splitting those children, and it is deliberately NOT applied unilaterally because re-cutting four children's checklists is a re-authoring of another agent's Set that changes what a human already reviewed. See OQ-01.

## Required tests / validation

Order 10 must map each test to its owning requirement and retain final evidence for:

- The 33 mandatory scenarios enumerated in Order 10.
- Hand-calculated metric and price-era golden outputs.
- Aggregate/separate phase reconciliation and time overlap/unattributed accounting.
- Cache no-op, selective rebuild, active/resumed run, concurrency, interruption, corruption, and schema change.
- Telemetry host parity, node changes, disabled/basic/periodic modes, probe failures, cleanup, privacy, and overhead. Agy-side parity evidence is SYNTHETIC by necessity (no Agy run exists in this corpus) and must be labeled as such rather than reported as cross-host corpus validation.
- The two LEGACY driver generations present in this corpus (`tools/ipdrunner/runipd.py`, 13 runs; `tools/ipdrunner/ipdrunner.py`, 2 runs) as their own ingestion scenarios, since they are the real historical schema variance here and Order 10's scenario 3 would otherwise be satisfied entirely by OpenCode-lineage runs.
- A leak-sanitizer scan that CONSUMES `aw sanitize --agent` rather than reimplementing detection, run over the cache, report, agent outputs, both safe export tiers, diagnostics, and submission receipts.
- Every required and corpus-selected analysis, chart, exact table, query view, and finding contract.
- Offline/no-network SPA, accessibility, hostile-string safety, deterministic bundle, size handling, and packaged assets.
- Export tier separation, raw confirmation, endpoint/consent/auth/receipt safeguards, archive safety, and zero implicit network.
- Existing runner, viewer, ledger, profile, setup, and CLI regression behavior.
- Author and review-finalize lints, plans index check, spec lint after amendments, wheel/sdist build/install, bare `python3 -m pytest`, and `git diff --check`.

## Open questions

### OQ-01: Do the four densest children get re-cut into finer E/V items (or split) before this Set is approved?

- Blocking: yes
- Status: resolved
- Owner: maintainer
- Finding: PR-002
- Resolution or deferral rationale: RESOLVED FROM THE TREE 2026-09-10, NOT BY ASKING: THE QUESTION IS MOOT BECAUSE OUTCOME (b) ALREADY HAPPENED AND THE MAINTAINER ALREADY APPROVED THE RESULT. It was NOT put to them in the `/askme` round, because spending a maintainer prompt on a decision the repository has already recorded is exactly what this workflow forbids.
  THE PREMISE IS DEAD. The finding rests on "all ten children carry EXACTLY THREE E-items (31 across the Set)". MEASURED AT HEAD `c329713c`, the nine work children carry 6, 8, 7, 8, 9, 8, 8, 9 and 10 E-items, 73 across the children plus this parent's 1, for 74 in the Set rather than 31. Each child's V-count equals its E-count, so the E/V bijection held through the re-cut. The four children the finding singled out as densest are precisely the ones now most finely cut: Order 06 `aflsz3` 9, Order 07 `6eq3oq` 8, Order 09 `ixis0c` 9, Order 10 `9xycbh` 10.
  THE SET IS NOW WITHIN THE CORPUS IT WAS MEASURED AGAINST. Re-measured across all 104 pending plans carrying E-items: range 1 to 11, median 6. Every child now sits at or above that median, so the density defect the finding described no longer exists in the artifacts.
  AND THE MAINTAINER HAS ALREADY SIGNED OFF ON THE RE-CUT SHAPE. All nine work children were set `approved` in commit `a0b242f0` (2026-09-08 17:22), AFTER the review that raised this question. Approval of the re-cut children is a stronger answer than a fresh opinion about whether re-cutting was warranted, and it selects option (b), the reviewer's own recommendation, without renumbering the Set or changing ownership.
  WHAT REMAINS TRUE AND MUST NOT BE READ AWAY: this parent (`5lxvl3`) is still `reviewed`, not `approved`, so the Set still needs the orchestrator's own sign-off before it runs; that is a separate hold from this question. Nothing here validates the children's CONTENT, only that the density finding is spent.
  ORIGINAL REVIEW REASONING RETAINED BELOW, with its now-stale 31-item measurement, so a reader can see what changed rather than wondering why a blocking question closed. OPEN, and it needs the maintainer because the choice is about scope and risk appetite, not about repository fact. THE MEASUREMENT is in the Scope check above: all ten children carry exactly three E-items (31 in the Set), against a pending-corpus range of 3 to 9 and a median of 6, and four items each bundle multiple independent deliverables and test surfaces (Order 07 E-02 = the whole SPA; Order 09's three items = three distinct trust surfaces; Order 06 E-02 = pricing plus all statistics; Order 10 E-03 = the entire 33-scenario acceptance matrix plus performance, sanitizer, offline, suite, lint and diff). The count-based lint passes at three by construction, so nothing mechanical will catch this. WHY IT IS BLOCKING: an over-broad E-item is discharged by a single checkmark, and the work most likely to be dropped from the tail of these particular items is the privacy boundary, the offline proof and the packaging proof, which are the three things this Set's value depends on and the three hardest to notice missing later. WHY IT IS NOT RESOLVED HERE: re-cutting four children's checklists rewrites another agent's authored Set and changes the shape a human would be approving, which is the maintainer's call. THE OPTIONS ARE (a) approve as authored and accept the density, relying on each child's V-items to catch a partial pass; (b) re-cut Orders 06, 07, 09 and 10 into finer E/V items within their existing child boundaries, which is bounded work and changes no ownership; or (c) split those four into additional children, which is the most thorough and the most expensive, and renumbers the Set. The reviewer's recommendation is (b): it addresses the measured defect, leaves the sound ten-child decomposition and its dependency chain intact, and costs one authoring pass rather than a re-plan.

## Validation and cross-check (verify before reporting the Set complete)

Validation-state rule: the runner retires an orchestrator from child status. Do not fabricate an independent implementation checkpoint for this file.

- [ ] V-01 validates E-01
  - Required evidence: PASTE each child's `- Status:` line read from its own file, showing all ten `executed`; PASTE `git log --oneline` over `.aw/records/plans/` showing the order in which they reached `executed` is consistent with the table's `Depends on` column; PASTE `git diff --stat` over THIS parent's own commits, showing it touched no file under `agent_workflows/` or `tests/` (that is the check proving this parent performed no child's work); and PASTE `aw index plans --check` output showing clean.
    THE SET-LEVEL BEHAVIOR IS NOT VALIDATED HERE. Order 10's V-01..V-03 own the acceptance matrix, the offline/privacy/packaging proof, and the bare-suite evidence. Re-asserting from this parent that the feature works, without Order 10 having recorded that evidence, is a FAILED validation, not a passing one.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: exception
- Cohesion rationale: the Set is intentionally large because it spans capture, normalization, analysis, presentation, agent access, and privacy-sensitive sharing. Ten narrowly owned children keep implementation passes bounded, while this orchestrator carries only sequencing. Whole-feature acceptance is Order 10's, not this parent's.

Execution requires explicit human approval (`- Status: approved`). Each child is independently approvable and executable; approving this parent does not approve them.

SCOPE FENCE, AS A DECLARATION AND NOT A HALT. This orchestrator's declared scope is `.aw/records/plans/pending` and it authors NO product code. If executing it appears to require a change under `agent_workflows/` or `tests/`, that is a child's work and the child table is wrong. Do not expand scope casually; if the work genuinely requires a path outside the fence, MAKE THE EDIT AND JUSTIFY IT, because `aw ipd finalize` refuses to complete until every out-of-scope changed path carries a `--scope-reason` and every declared-but-unmodified path carries a `--scope-ack`. Do NOT stop the run over a scope question. The conditions that DO warrant stopping are genuinely unsafe ones: an unresolvable conflict with a co-worker's concurrent edit, or a prerequisite whose symbols do not exist.

HARD MUST, HONESTY: when you report that tests or validation passed, PASTE THE ACTUAL RUNNER OUTPUT, including the `N passed` summary line from a BARE `python3 -m pytest` (the configured `addopts` already supply `-q -n auto -m 'not slow'`; do not add flags). Never claim a success you did not run, and never mark a `V-*` item from the matching execution checkmark.

COMMIT DISCIPLINE: commit ONLY the files you changed, path-scoped (`git commit -m msg -- <paths>`), never `git add -A`, never `-a`, and NEVER push. THIS IS A SHARED CHECKOUT: verify with `git diff --cached --name-only` before every commit and RE-VERIFY after any failed or hook-interrupted commit, since a rejected hook can leave paths you never staged in the index. Prefer the tooled path (`aw commit <plan> -- <paths>`).

LIFECYCLE MOVE: on completion the transition is `aw ipd finalize <plan> --actor <agent/model> --message <summary> --apply`, which runs the pre/post-transition gates, writes the attributed newest-first history line, moves the file to `.aw/records/plans/executed/`, refreshes the index, and makes the path-scoped lifecycle commit in one transaction. Do not hand-move the file or hand-write the terminal `Status:`.

RETIREMENT IS THE RUNNER'S, AND IT IS HONEST ONLY BECAUSE THIS PARENT HOLDS NO WORK OF ITS OWN. `runner_shared.evaluate_set_retirement` retires this plan once EVERY child's on-disk `- Status:` is exactly `executed`, spending no agent turn, so an Order-0 parent in the queue is correct and needs no human step; measured at review, `decide_orchestrator_dispatch` currently returns `terminate` naming all ten unexecuted children rather than retiring anything. That rollup deliberately SKIPS the pre-transition E/V checkpoint, which is safe here ONLY because E-01 is sequencing the runner performs itself. Do not read this parent's eventual `executed` status as evidence that the feature was verified: that evidence is Order 10's V-01 through V-03 and lives there. The orchestrator must not be marked executed manually ahead of its children.
