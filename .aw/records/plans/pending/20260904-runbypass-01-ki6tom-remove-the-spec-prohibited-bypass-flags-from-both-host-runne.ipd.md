# IPD: remove the spec-prohibited bypass flags from both host runners

- Date: 2026-09-04
- Kind: child
- Concern: **THE PROHIBITION THIS PLAN EXISTS TO ENFORCE NO LONGER EXISTS. THE SPEC SENTENCE WAS DELETED ON 2026-09-06 BY MAINTAINER RULING (`844d195c`), AND THE PLAN'S ENTIRE THESIS IS VOID. DO NOT EXECUTE IT AS WRITTEN.** See F-13, F-14, F-15 and BLOCKING OQ-02, which is the only question that now matters. The paragraph below is the plan's ORIGINAL premise, retained verbatim for the record and no longer true.
  WHAT THE SPEC SAYS NOW, verified at HEAD: 2.1 `:161` keeps "There is no `--skip-audit` flag and no GIT hook-bypass flag on `run`" and nothing else. `:162` records the supersession explicitly, states that `--dangerous` "is REMOVED from this prohibition and `7ckptx` R4.1c is the controlling authority on host permission posture", and that banning the RUNNER sense of `--no-verify` "removed the only per-model control that exists today while protecting nothing the checker was not already protecting". So of the three violations this plan set out to fix, `--dangerous` and `--no-verify`/`--no-audit` are no longer prohibited at all, and the one prohibition that survives (`--skip-audit`, plus the GIT sense of `--no-verify`) was ALREADY satisfied before this plan was written (F-10, and `git_commit_helper.py:484` never passes it).
  ORIGINAL PREMISE, NOW FALSE: Spec `25kzda` 2.1 `:140` states flatly, as a one-line prohibition rather than a preference: "There is no `--no-verify`, `--skip-audit`, `--dangerous`, or hook-bypass flag on `run`." BOTH host runners violate it. Re-measured at HEAD `d4c677c5` (line numbers ALL moved since authoring; locate by symbol): `agy_runipd.py:4390-4396` registers `--dangerously-skip-permissions`/`--dangerous`, and `:4404-4409` registers `--no-verify`/`--no-audit` to skip the turn-2 skeptical validation. The opencode host registers `--validate`/`--verify`/`--audit` as a `BooleanOptionalAction` (`:7199-7205` on `start`, AND `:7270-7276` on `resume`), which makes argparse AUTO-GENERATE `--no-validate`/`--no-verify`/`--no-audit`, so the prohibited spelling is reachable there too even though nobody wrote it. Verified live: `--no-verify`, `--no-audit`, and `--no-validate` all parse on oc `start` and yield `validate=False`, and `--no-verify` also parses on oc `resume`.
  THE ORIGINAL FRAMING OF THE agy PERMISSION DEFAULT AS "THE SAFETY DEFECT" WAS WRONG AND HAS BEEN WITHDRAWN (see F-9, and the round-1 review note). That `True` default is a DECIDED CONSTRAINT carrying an explicit maintainer ruling of 2026-09-01, normatively protected by approved spec `7ckptx` R4.1c, and pinned by a test built to fail if it is flipped. So the conformance problem here is a NAMING problem plus a genuinely open design question, not a latent safety hole; the flag spelling `--dangerous` is prohibited while the CAPABILITY it aliases is required.
- Scope: **SUSPENDED PENDING OQ-02.** The scope below describes conformance work against a prohibition that no longer exists (F-13). Nothing in it may be executed until the maintainer answers OQ-02, because every removal it specifies would now DELETE A SANCTIONED, OPERATIONALLY REQUIRED FLAG rather than fix a violation. The text is retained so the decision has something concrete to act on.
  ORIGINAL SCOPE, NOW VOID: Bring both runners' `run`/`start` AND `resume` parsers into conformance with spec 2.1's prohibition, WITHOUT changing what a run actually DOES on either host. Two concerns, kept separate because they carry different blast radii: (1) remove the prohibited SPELLINGS (`--dangerous`, `--no-verify`, `--no-audit`) while preserving each host's ability to express its real, non-prohibited intent under a conformant name; (2) decide and record what agy's verification control is called and defaults to once `--no-verify` is gone, since agy gates on `not no_verify` (defaults ON) while opencode gates on `validate` (defaults OFF) and the two must not be silently harmonized. EXPLICITLY EXCLUDES flipping agy's `--dangerously-skip-permissions` default, which spec `7ckptx` R4.1c forbids to any plan (F-9); this plan removes the prohibited ALIAS `--dangerous` and keeps the canonical long flag and its `True` default exactly as shipped. Also excludes registering spec 2.1's POLICY flags (`uyeko5`, now EXECUTED, owns them), excludes `--type`/`--action`/`--json`, and excludes changing the verifier's own logic.
- Scope-Paths: agent_workflows/oc_runipd.py, agent_workflows/agy_runipd.py, tests/test_oc_runipd.py, tests/test_agy_runipd_cli.py, tests/test_runner_shared.py
- Item-Dependencies: none
- Status: reviewed
- Readiness: no-go
- Set: runbypass
- Order: 1
- Highest E allocated: 06
- Author: opencode its_direct/pt3-claude-opus-5-1m-us
- Id: ki6tom
- Blocks-Release: next
- From-Spec: 25kzda

## Workflow history
- 2026-09-07 reviewed (opencode its_direct/pt3-claude-opus-5-1m-us): /plan-review ROUND 2 at HEAD `7ef72b72`: REVIEWED - OPEN QUESTIONS, readiness NO-GO. PR-201 (BLOCKER) through PR-205; four FIXED and PR-201 OPEN as blocking OQ-02, which only the maintainer can answer. **THE PLAN'S CONTRACT WAS WITHDRAWN AND THE PLAN NO LONGER HAS A TARGET.** The spec sentence its Concern quotes, "There is no `--no-verify`, `--skip-audit`, `--dangerous`, or hook-bypass flag on `run`", was DELETED on 2026-09-06 at `844d195c` on the maintainer's own 2026-09-05 ruling, for the same three reasons this plan's OQ-01 later recorded: unexamined external-model provenance, a spec-versus-spec contradiction with `7ckptx` R4.1c, and the conflation of the GIT `--no-verify` with the RUNNER one. Spec `:162` now says in as many words that `--dangerous` "is REMOVED from this prohibition". Verified that `844d195c` is an ancestor of HEAD but NOT of `d4c677c5`, the HEAD round 1 measured at, so round 1 was correct when written and this plan was overtaken by a change it could not see. THE CONSEQUENCE IS DIRECTIONAL, NOT COSMETIC: of the three live violations the plan identified, none is prohibited any longer, so E-02, E-03 and E-05 would each DELETE A FLAG THE SPEC PERMITS, and E-01's contract test would assert a prohibition the spec does not make and fail permanently against a conformant tree. E-03 is the most dangerous: it would delete agy's only per-model verification control, which OQ-01 Part One itself ruled must not be deleted before a replacement exists, and I measured that the replacement is NOT in place (the `f2mrsw` tri-state ships but is DEAD in both drivers, since neither passes `validate=` into `resolve()` nor reads `ResolvedLaunch.validate`; the plan that would have wired it, `mn3gwr`, was retired UNRUN; the `hostdefault` Set that replaced it is unexecuted). THE ONE SURVIVING PROHIBITION NEEDS NO WORK: `--skip-audit` is rejected on all four subparsers and greps to zero in both runners, and the GIT sense is enforced by the commit gateway, not a parser. So there are ZERO live violations left, which is why the disposition is a maintainer decision (retire, re-point at backlog `behjg6`, or narrow to a regression guard) rather than a smaller edit. ALSO FOUND: the plan's own OQ-01 Part Two already CONTAINS this finding ("THE SPEC IS WRONG TO FORBID THESE FLAGS") and then concludes E-02 "stands", which does not follow from its own premise; its E-04 sequencing names the wrong successors; a fifth registration site exists in `agy_run.py:286` and is correctly out of scope because `aw agy exec` is a different verb; and every line number had drifted again by ~330 lines in agy and ~400 in oc. Lint conforming at `--phase author` and `--phase review-finalize`; `--phase pre-execution` correctly REFUSES on OQ-02.
- 2026-09-07 to-review (aw set): status set to to-review

- 2026-09-05 reviewed (opencode its_direct/pt3-claude-opus-5-1m-us): /plan-review round 1: APPROVE WITH REVISIONS APPLIED; PR-101 (BLOCKER) through PR-105 all FIXED; NO-GO, because OQ-01 remains `Blocking: yes`. Verified at HEAD `d4c677c5` by parsing the REAL parsers rather than equivalents. Lint conforming at `--phase author` and again at `--phase review-finalize`. THE BLOCKER: E-02, this plan's self-declared centerpiece ("the real safety defect", the item that "must land even if OQ-01 stalls E-04"), required flipping agy's `--dangerously-skip-permissions` default to opt-in, which approved spec `7ckptx` R4.1c FORBIDS to any plan on recorded operational evidence, which Non-goal 7 records as a maintainer ruling of 2026-09-01 saying the default "MUST KEEP IT", which a code comment guards with "Do NOT 'harden' it", and which `TestAntigravitySkipPermissionsDefaultIsPinned` pins by asserting the literal source strings E-02 would have edited, with a failure message naming R4.1c. So the plan's most safety-framed item was prohibited work that would have been reverted at CI, and it taught a reader that a decided constraint was an open defect. E-02 is REPLACED (not softened) by an alias-only removal of the prohibited `--dangerous` spelling, and its validation is inverted to PROVING non-disturbance of the default, the fallback, the consumer read, and the pinning test; F-2 is struck through and superseded by F-9. Repaired rather than rejected because the plan's actual thesis is sound and independently verified: the prohibited spellings really are registered, and F-3's insight that `BooleanOptionalAction` makes the oc violation invisible to grep is correct and valuable. PR-102 (HIGH): that violation is on TWO subparsers, and the plan named one - oc `resume` registers the same action (with `default=None`), so `--resume <id> --no-verify` parses today and a `start`-only fix plus a `start`-only test would have left the spec violated while reporting conformance; E-01 now covers four subparsers and V-05 fails on a `start`-only paste. PR-103 (MEDIUM): the tree moved when `uyeko5` executed, so F-8's concurrency warning is spent, its count is 11 not 12, every cited line number is stale, and - substantively - `uyeko5` landed `runner_shared.RUN_POLICY_FLAGS`, a shared data-driven flag surface that makes "hand-edit each host" no longer obviously right; new E-06 re-locates by symbol and forces that decision to be recorded before any parser is touched. PR-104 (MEDIUM): `--skip-audit` already conforms on both hosts, so only three of the four named spellings are live violations; it stays in the test as regression coverage and the honesty rule forbids reporting it as removed. PR-105 (LOW): E-01's prohibition table was to be hand-copied, where the repo already parses spec `25kzda` tables out of the spec FILE, so it is now spec-derived where practical. Readiness stays `no-go` deliberately: OQ-01 needs a maintainer reading of an approved spec's intent and a safety-posture call, and the plan is right to gate itself. Four recorded decisions, all reversible.
- 2026-09-04 to-review (opencode its_direct/pt3-claude-opus-5-1m-us): Authored at the maintainer's direction, who chose "create an IPD" when asked how to route finding F-11 from the `/plan-review` of `uyeko5` (`runflags-01`). F-11 was found while auditing `uyeko5`'s flag surface against spec 2.1 and deliberately NOT folded into that plan: removing a shipped flag that defaults `True` is a permission-handling behavior change with a blast radius unrelated to registering the eight policy flags `uyeko5` owns, and widening a plan already touching the two highest-contention modules in the repo would have been the wrong trade. EVERY CLAIM BELOW WAS MEASURED AT `d0919400`, not inherited from the spec text. THE FINDING THAT SHAPED THE PLAN: the two hosts gate verification on OPPOSITE defaults and a comment at `agy_runipd.py:3077-3082` says so explicitly, so "just delete `--no-verify` on both" would silently flip agy's verification from ON to OFF or opencode's from OFF to ON depending on which spelling survived. That is why E-04 exists as its own item with an owner-facing question rather than being bundled into E-01. Also measured: `tests/test_oc_runipd.py:1981-1984` explicitly asserts `--no-verify` parses on the opencode host, so this plan MUST update a shipped test rather than claiming no test breaks.

## Goal

Make the shipped `run` parsers on both hosts satisfy spec `25kzda` 2.1's bypass-flag prohibition, and make agy's permission auto-approval opt-IN rather than opt-OUT, without silently changing either host's verification default as a side effect.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

**EVERY ITEM BELOW IS SUSPENDED. DO NOT PERFORM ANY OF THEM.** The spec sentence they enforce was deleted at `844d195c` (F-13), so E-01 would pin a prohibition the spec does not make and E-02, E-03 and E-05 would each delete a flag the spec now permits, one of which (`--dangerous`) is operationally required and protected by `7ckptx` R4.1c. E-06's re-location work is the only part that survives on its merits, and it survives as INPUT TO A DECISION rather than as a step. OQ-02 is `Blocking: yes` and asks the maintainer whether this plan is retired, re-pointed at the `behjg6` correction, or narrowed; the pre-execution lint refuses while it stands, which is the correct outcome. The items are kept rather than deleted so the decision has something concrete to act on and so the record shows what was proposed.

### Task group 1: prove the violation across every parser that carries it

- [ ] E-01 **SUSPENDED (F-13): this test would assert a prohibition the spec deleted, and would therefore fail permanently against a conformant tree.** If OQ-02 directs a narrowed plan, the surviving contract is only "`--skip-audit` is rejected on all four subparsers, and no runner flag reaches `git commit --no-verify`", which is ALREADY TRUE (F-15) and would be a pure regression guard rather than a failing-first test. Note also that `tests/test_run_flag_surface.py:94-133` already parses this spec section and its docstring still cites the deleted sentence, so any spec-derived table here would compete with an existing spec-derived test. Original text follows.
  Write the failing-first CONTRACT TEST before touching a parser: assert that NONE of spec 2.1's prohibited flag spellings is accepted by either host, driven by a DATA table of the prohibited names.
  DERIVE THE TABLE FROM THE SPEC FILE, not from a hand-copied list. There is precedent in this repo for parsing the spec text directly (`tests/test_run_evidence_completion.py:1029` parses spec `25kzda` 4.2's table out of the spec file, with a `skipTest` when the spec is absent), and the value is that the test then fails when the spec GROWS a prohibited name rather than passing on a stale transcription. `runner_shared` already establishes the same discipline for the positive surface, deriving registration from `RUN_POLICY_FLAGS` as data (`runner_shared.py:1242`, `:1381`). If you transcribe instead, say why and pin the spec line.
  COVER EVERY PARSER THAT CARRIES THE VIOLATION, which is FOUR subparsers, not two. Measured at HEAD `d4c677c5`: oc `start` accepts `--no-verify`, `--no-audit`, and `--no-validate`; oc `resume` ALSO accepts `--no-verify` (a second `BooleanOptionalAction` at `oc_runipd.py:7270-7276` the original plan never mentioned); agy `start` accepts `--no-verify`, `--no-audit`, and `--dangerous`; agy `resume` already rejects them. A test covering only `start` would leave oc `resume` violating the spec and report success.
  `--skip-audit` IS ALREADY CONFORMANT ON BOTH HOSTS (measured: agy `start` REJECTS it; it greps to zero in both runners). Keep it in the table as a REGRESSION GUARD, and state that it is already-passing rather than implying four live violations where there are three.
  ASSERT BY PARSING, NOT BY GREP, because the opencode violation is INVISIBLE to grep: `--no-verify` appears nowhere in `oc_runipd.py`, yet `BooleanOptionalAction` generates it, so a grep-based test would report opencode conformant while the flag parses fine.
  - Depends on: none
  - Expected outcome: one table-driven test, derived from the spec file where practical, over all four subparsers, failing at HEAD for the three live violations and already passing for `--skip-audit`, driven by parsing rather than by grep. Paste the failure.
  - Execution state: pending

- [ ] E-02 **SUSPENDED (F-13, F-14): the alias is NO LONGER PROHIBITED, so its removal now has no justification.** Spec `:162` says in as many words that `--dangerous` "is REMOVED from this prohibition and `7ckptx` R4.1c is the controlling authority on host permission posture". This item's ENTIRE remaining rationale was "the spelling is prohibited while the capability is required"; the first half is now false, leaving only the second. The plan's own OQ-01 asserts this item "stands", which does not follow from its own premise (F-14). Removing a working alias of an operationally required flag, on the authority of a deleted sentence, is a gratuitous break of an operator's muscle memory. Original text follows.
  REMOVE THE PROHIBITED ALIAS `--dangerous` FROM agy, AND CHANGE NOTHING ELSE ABOUT THAT FLAG. The canonical `--dangerously-skip-permissions`, its `default=True`, its `--no-...` opt-out, the options fallback (`agy_runipd.py:1834-1836`), and the consumer read (`:2571-2572`) ALL STAY EXACTLY AS SHIPPED.
  THIS ITEM REPLACES A WITHDRAWN ONE, and the reason must not be lost: the original E-02 required flipping that default to opt-in, calling it "the actual safety defect". That is FORBIDDEN. Approved spec `7ckptx` R4.1c (`:277-283`) says no work tracing to it "may flip Antigravity's `--dangerously-skip-permissions` default to `False`", records a maintainer ruling of 2026-09-01 on operational evidence that running without it "failed or deadlocked repeatedly", and requires "its own decision, its own evidence that the deadlock is gone, and an explicit supersession" before any change. `TestAntigravitySkipPermissionsDefaultIsPinned` (`tests/test_lane_permission_posture.py:315`) pins all three sites and is documented as "A REGRESSION GUARD IN THE OPPOSITE DIRECTION FROM EVERY OTHER CHECK HERE". Its `test_the_flag_is_present_on_an_unattended_turns_argv` asserts the literal source strings `argv.append("--dangerously-skip-permissions")` and `options.get("dangerously_skip_permissions", True)`, so the withdrawn E-02 would have failed a test whose failure message says exactly why it must not be done.
  SO THE ONLY CONFORMANT EDIT IS TO THE ALIAS: `--dangerous` is a spec-prohibited SPELLING (`25kzda:140`), while the capability it aliases is operationally REQUIRED (and OQ-01's 2026-09-05 annotation records the maintainer saying so independently). Drop the alias string; keep the flag.
  VERIFY YOU DID NOT DISTURB THE PIN: run `tests/test_lane_permission_posture.py` and paste it green. If it fails, you changed something R4.1c protects.
  - Depends on: E-01
  - Expected outcome: `aw agy run --dangerous` fails to parse; `--dangerously-skip-permissions` still parses, still defaults `True`, still reaches the child argv; `tests/test_lane_permission_posture.py` green and untouched; no edit to the options fallback or consumer read.
  - Execution state: pending

### Task group 2: remove the prohibited spellings without changing behavior by accident

- [ ] E-03 **SUSPENDED (F-13), AND THIS IS THE MOST DANGEROUS ITEM TO PERFORM NOW.** Spec `:162` states that banning the runner sense of `--no-verify` "removed the only per-model control that exists today while protecting nothing the checker was not already protecting", and OQ-01 Part One already ruled that agy's ability to disable verification MUST NOT be deleted before its replacement exists. Performing this item would delete that control on the authority of a sentence the same maintainer removed. The replacement is also NOT yet in place: `f2mrsw` shipped the per-profile `validate` tri-state and it is EXECUTED, but the value is never wired into either driver (measured: `resolve_launch_profile` at `oc_runipd.py:2683-2691` passes no `validate=` argument, and `ResolvedLaunch.validate` has no reader in either runner), and the `hostdefault` Set that owns the wiring is unexecuted. Original text follows.
  REMOVE the prohibited verification spellings from agy's parser: `--no-verify` and its alias `--no-audit` (measured at HEAD `d4c677c5`: `agy_runipd.py:4404-4409`; locate by symbol). The `--dangerous` alias is E-02's, not this item's.
  DO NOT simply delete the `dest`: `no_verify` is read from run-state options at `:3378-3384` and drives `verifier_expected` at `:3468`, it is FROZEN into every run's options at `:1837` (`getattr(args, "no_verify", False)`), and FIVE existing tests write `"no_verify"` into state options (`tests/test_agy_runipd_cli.py:154`, `:204`, `:247`, `:306`, `:520`). The run-state KEY and the CLI FLAG are different surfaces; this item removes the flag spelling, and any state-key rename is a separate migration this plan does not take on. State plainly which you changed.
  MIND THE FREEZE SITE at `:1837`: once no CLI flag sets `no_verify`, that `getattr` defaults `False`, so a NEW run freezes `no_verify: False` and verification stays ON. That is the correct outcome and it is exactly what E-04's replacement control must continue to be able to override; note it so E-04 does not have to rediscover it.
  - Depends on: E-01
  - Expected outcome: `aw agy run --no-verify` and `--no-audit` both fail to parse; the run-state `no_verify` option key still works so no frozen run changes meaning; the five tests writing that key still pass unchanged; the freeze site's new effective default (`False`, verification ON) stated.
  - Execution state: pending

- [ ] E-04 **SUSPENDED, AND ITS PREMISE HAS PARTLY DISSOLVED RATHER THAN MERELY BEING DEFERRED.** OQ-01 deferred this item until `kgpptv` and `f2mrsw` E-03 land. Re-measured at this review: `f2mrsw` is EXECUTED (its E-03 tri-state and precedence chain ship in `runner_profiles.py:1004-1017`), and `kgpptv` is `approved` and unexecuted. But the sequencing OQ-01 described is now incomplete in a way that matters: the tri-state is DEAD IN BOTH DRIVERS (neither passes `validate=` into `resolve()` nor reads `ResolvedLaunch.validate`), the plan that would have wired it (`mn3gwr`) was RETIRED UNRUN as superseded, and the `hostdefault` Set that replaced it (`tm2cz8` `approved`, `ybkmzp` `to-review`, both `Blocks-Release: next`) is what actually owns making verification per-host configuration. So "hold E-04 until kgpptv and f2mrsw land" names the wrong successors; the real gate is the `hostdefault` Set. Also note the spec no longer forbids the flag this item was replacing, so the item may not be needed at all. Original text follows.
  DECIDE AND IMPLEMENT what replaces `--no-verify` on agy, which is the one item carrying a real design question rather than a mechanical edit. The two hosts DELIBERATELY disagree today and a comment says so: `agy_runipd.py:3077-3082` records that agy "gates the verifier on `not no_verify` (verification defaults ON here), whereas `oc` gates on `validate` (which defaults OFF)".
  So there are two conformant shapes and they are NOT equivalent: (a) give agy a `--validate`/`--no-validate` `BooleanOptionalAction` matching opencode's spelling but keeping agy's `default=True`, which conforms only if the auto-generated `--no-validate` is deemed outside the prohibition (the spec forbids `--no-verify`, `--skip-audit`, `--dangerous`, and hook-bypass flags by name, and does NOT name `--no-validate`); or (b) give agy a positively-named opt-out that the spec does not forbid.
  WHAT YOU MUST NOT DO is harmonize the DEFAULT as a side effect of renaming the flag. Turning agy's verification from ON to OFF to match opencode would be a silent safety regression wearing a conformance fix's clothes; turning opencode's from OFF to ON would change every existing opencode run's cost and duration. Preserve both defaults, and if you believe one is wrong, say so as a finding for a separate plan.
  NOTE THE UNRESOLVED TENSION honestly: `BooleanOptionalAction` is how the opencode host got its violation in the first place (E-01), so choosing (a) reintroduces an auto-generated negation. If `--no-validate` is also judged prohibited, only (b) is conformant. OQ-01 carries this and is BLOCKING.
  - Depends on: E-03
  - Expected outcome: agy has a conformant way to disable verification, verification still defaults ON for agy and OFF for opencode, both shown side by side, and the choice between (a) and (b) is recorded with its reasoning rather than made silently.
  - Execution state: pending

- [ ] E-05 **SUSPENDED (F-13): these spellings are no longer prohibited, and this item would also rewrite a SHIPPED PASSING TEST into asserting the opposite of what the spec now says.** `tests/test_oc_runipd.py:1984-1998` documents `--no-validate`/`--no-verify`/`--no-audit` as the explicit opt-out and passes at HEAD; converting it to assert rejection would encode the deleted prohibition into the test suite, which is the hardest kind of error to unwind later. Re-measured at HEAD: the two oc registrations are now at `:7601-7607` (`start`, `default=False`) and `:7672-7678` (`resume`, `default=None`), not the `:7199`/`:7270` this plan cites. Original text follows.
  REMOVE the prohibited spellings from opencode's parser, which requires editing the `BooleanOptionalAction` registration rather than deleting a string: the action generates `--no-verify` and `--no-audit` from the `--verify` and `--audit` ALIASES. Drop those two aliases (keeping `--validate`, whose generated `--no-validate` is not a spec-prohibited NAME, subject to OQ-01) or replace the action with explicit flags, whichever preserves today's behavior more directly; justify which.
  TWO REGISTRATIONS, NOT ONE. Measured at HEAD `d4c677c5`: `oc_runipd.py:7199-7205` on the `start` subparser AND `:7270-7276` on the `resume` subparser, the second with `default=None` because a resume override must be distinguishable from an unset flag. The original plan named only the first. Verified live: `parse_args(["resume", "runid", "--no-verify"])` PARSES and yields `validate=False`, so fixing only `start` leaves the spec violated on `resume` while a `start`-only test reports success. Preserve the `default=None` on `resume`; collapsing it to `False` would turn "operator said nothing" into "operator said no".
  UPDATE THE SHIPPED TEST that pins the violation: `tests/test_oc_runipd.py:1987-1995` asserts `--no-verify` parses and sets `validate=False`, and the next case does the same for `--no-audit` (line numbers moved since authoring). These must become assertions that the prohibited spellings are REJECTED. Do not delete the surrounding coverage of `--validate`/`--no-validate`, which is legitimate and must keep passing.
  DO NOT change opencode's `validate` DEFAULT, which is `False` on `start`, and do not touch the `no_verify`/`no_audit` STATE-KEY compatibility read at `:6034` (`validate = not (opts.get("no_verify") or opts.get("no_audit"))`), which exists so a run frozen under the old option keys still resolves correctly.
  - Depends on: E-01
  - Expected outcome: `--no-verify` and `--no-audit` fail to parse on BOTH the `start` and `resume` subparsers; `--validate`/`--no-validate` still work on both; `validate` still defaults `False` on `start` and `None` on `resume`; the frozen-state compatibility read at `:6034` is untouched; the two shipped test cases now assert rejection and the rest of that test still passes.
  - Execution state: pending

- [ ] E-06 **SUSPENDED AS A STEP, BUT ITS INSTRUCTION IS THE ONE THAT PROVED CORRECT: this item told the executor to re-validate the plan's premises before editing, and doing exactly that is what found F-13.** The re-location is already done at this review and recorded below, so nothing remains to perform; the shared-registry-versus-per-host decision is moot while there is nothing to remove. RE-MEASURED AT HEAD: agy's permission flag is at `:4723-4730` (not `:4390`), agy's verify flag at `:4737-4743` (not `:4404`), oc's two `BooleanOptionalAction` registrations at `:7601-7607` and `:7672-7678` (not `:7199`/`:7270`), oc's legacy state read at `:8029-8030` (not `:6034`), and agy's argv append at `:2799`. The drift is roughly 330 lines in agy and 400 in oc since round 1, which is a second demonstration of why this item existed. Original text follows.
  RE-LOCATE EVERY SITE AND RE-VALIDATE THE PLAN'S PREMISES AGAINST THE POST-`uyeko5` TREE BEFORE EDITING, because the ground this plan was authored on has already moved.
  WHAT CHANGED: `uyeko5` (`runflags-01`) was `pending` when this plan was written and is now EXECUTED (`.aw/records/plans/executed/20260903-runflags-01-uyeko5-...`, finalized in `251b7399`). F-8's warning that the two "must not run concurrently" is therefore SPENT, and its "12 other unexecuted plans" count is stale (measured 11 at HEAD `d4c677c5`). Every line number in this plan is also stale: agy's permission flag moved from the cited `:3976` to `:4390`, its verify flag from `:3990` to `:4404`, oc's action from `:6063` to `:7199`, and oc's legacy state read from `:4924` to `:6034`.
  THE SUBSTANTIVE CONSEQUENCE, which is not merely renumbering: `uyeko5` introduced `runner_shared.RUN_POLICY_FLAGS` (`runner_shared.py:1242`) plus `register_run_policy_flags` (`:1381`), a DATA-DRIVEN shared registry both hosts now call for the policy surface. So the repo now has a canonical mechanism for declaring the run flag surface once, and a per-host hand edit is no longer automatically the right shape. DECIDE AND RECORD whether the prohibited-spelling removal belongs in that shared table (with the prohibition expressed as data, which would also give E-01 a second in-code source of truth) or stays a per-host parser edit. Either answer is defensible; making it silently is not. If the shared module must change, that is a fifth path and `tests/test_runner_shared.py` is already in the fence.
  - Depends on: none
  - Expected outcome: every site re-located by symbol at the executing HEAD with the new line numbers recorded; F-8's concurrency warning explicitly retired as spent; the shared-registry-versus-per-host-edit decision recorded with its reasoning before any parser is touched.
  - Execution state: pending

## Project conventions discovered (Step 0)

- THE PROHIBITION IS BY NAME, NOT BY CAPABILITY. Spec 2.1 `:140` forbids four specific spellings on `run`. It does not say verification cannot be disabled; it says these flags do not exist. That distinction is what makes E-04 a design question rather than a deletion, and it is also what makes E-02 an alias-only edit: `--dangerous` is a forbidden NAME for a capability that is operationally REQUIRED.
- A PROHIBITED FLAG NAME AND A PROTECTED FLAG BEHAVIOR CAN COEXIST ON THE SAME ARGUMENT, and this repo has exactly that case. `--dangerously-skip-permissions` carries a prohibited alias (`25kzda:140`) AND a normatively protected default (`7ckptx` R4.1c). Conformance work must therefore separate NAME edits from DEFAULT edits with care; conflating them is what made the original E-02 unexecutable (F-9).
- WHEN AN APPROVED SPEC FORBIDS A CHANGE, A TEST MAY EXIST THAT FAILS IN THE OPPOSITE DIRECTION FROM NORMAL. `tests/test_lane_permission_posture.py:315` is documented as "A REGRESSION GUARD IN THE OPPOSITE DIRECTION FROM EVERY OTHER CHECK HERE" and asserts literal SOURCE STRINGS, so a plan that edits those lines fails a test whose message explains why the edit is forbidden. Before proposing a safety "hardening", grep the specs and the tests for an existing ruling.
- `BooleanOptionalAction` SILENTLY DOUBLES A FLAG'S SURFACE. Every `--x` declared with it also accepts `--no-x`, for every alias. This is how the opencode host violates a prohibition that appears nowhere in its source, and it is why E-01 must assert by PARSING rather than by grep. IT ALSO APPEARS ON MORE THAN ONE SUBPARSER: `start` and `resume` register it separately with different defaults (`False` versus `None`), so an audit must enumerate subparsers, not modules.
- THE RUN-STATE OPTION KEY AND THE CLI FLAG ARE SEPARATE SURFACES, and both hosts already rely on the distinction: opencode reads legacy `no_verify`/`no_audit` keys at `:6034` purely so a run frozen under an older CLI still resolves. Removing a flag must not rename a state key.
- THE TWO HOSTS' VERIFICATION DEFAULTS DIVERGE DELIBERATELY and a code comment documents it (`agy_runipd.py:3463-3468`). Unlike the `--full-auto` divergence that `uyeko5` E-07 normalized by maintainer ruling, this one has NOT been ruled on, so this plan preserves it and asks.
- THE FLAG SURFACE IS NOW DECLARED AS DATA IN A SHARED MODULE (`runner_shared.RUN_POLICY_FLAGS`, landed by the now-executed `uyeko5`), which is the canonical mechanism this repo's no-duplicate-paths convention would prefer for anything describing the run flag surface. E-06 must decide explicitly whether the prohibition belongs there (F-11).

## Findings

| # | Finding | Evidence |
|---|---------|----------|
| F-1 | agy REGISTERS THREE PROHIBITED SPELLINGS: `--dangerous` (an alias of `--dangerously-skip-permissions`) and `--no-verify`/`--no-audit`. Verified live at HEAD `d4c677c5`: all three parse on `start`; agy `resume` rejects all of them. | `agy_runipd.py:4390-4396`, `:4404-4409`; spec `25kzda:140` |
| F-2 | ~~agy AUTO-APPROVES ALL TOOL PERMISSIONS BY DEFAULT, which is the safety defect~~ **WITHDRAWN IN REVIEW AS A DEFECT CLAIM. SUPERSEDED BY F-9.** The `default=True` is factually confirmed (parser `agy_runipd.py:4390-4396`; options fallback `:1834-1836`; consumer `:2571-2572`), but it is a DECIDED CONSTRAINT under an explicit maintainer ruling, not a defect, and no plan may flip it. The original E-02 built on this framing and has been replaced. | facts confirmed at `:4390-4396`, `:1834-1836`, `:2571-2572`; the ruling and prohibition at F-9 |
| F-3 | **THE opencode VIOLATION IS INVISIBLE TO GREP, and it is on TWO subparsers, not one.** `--no-verify` appears nowhere in `oc_runipd.py`, but `--validate`/`--verify`/`--audit` is a `BooleanOptionalAction` on BOTH `start` and `resume`, so argparse generates `--no-validate`/`--no-verify`/`--no-audit` on each. Verified live against the real parser (not an equivalent one): `start --no-verify`, `start --no-audit`, `start --no-validate`, and `resume --no-verify` ALL parse and yield `validate=False`. The `resume` registration was missed by the original plan. | `oc_runipd.py:7199-7205` (`start`, `default=False`), `:7270-7276` (`resume`, `default=None`); live `parse_args` on `oc_runipd.build_parser()` |
| F-4 | A SHIPPED TEST PINS THE VIOLATION, so this plan cannot claim "no existing test breaks". `tests/test_oc_runipd.py` explicitly asserts `--no-verify` and `--no-audit` parse and set `validate=False`. | `tests/test_oc_runipd.py:1987-1995` (moved from the `:1975-1989` cited at authoring) |
| F-5 | **THE HOSTS' VERIFICATION DEFAULTS ARE OPPOSITE AND DELIBERATE, so renaming the flag risks flipping one.** agy gates on `not no_verify` (verification ON by default); opencode gates on `validate` (OFF by default). A code comment states the difference and warns against copying the expression. This is why E-04 is its own item with a blocking question. | `agy_runipd.py:3463-3468`; `oc_runipd.py:6034` |
| F-6 | THE `no_verify` STATE KEY IS LOAD-BEARING FOR FROZEN RUNS AND FOR TESTS, independent of the CLI flag: agy freezes it into every run's options, reads it back to compute `verifier_expected`, opencode reads it as a legacy-compatibility fallback, and five agy tests write it directly. Removing the flag must not remove the key. | `agy_runipd.py:1837` (freeze), `:3378-3384`, `:3468`; `oc_runipd.py:6034`; `tests/test_agy_runipd_cli.py:154`, `:204`, `:247`, `:306`, `:520` |
| F-7 | PROVENANCE: this plan exists because `/plan-review` of `uyeko5` (`runflags-01`) audited spec 2.1's flag surface and found the prohibition violated. It was recorded there as F-11 and explicitly excluded from that plan's fence, then routed here by maintainer choice rather than fixed in place. | `uyeko5` F-11 and its "Deferred / out of scope" entry |
| F-8 | ~~CONTENTION: ... whichever runs second must re-locate every site by symbol ... they must not run concurrently~~ **SPENT AS OF THE ROUND-1 REVIEW.** `uyeko5` is now EXECUTED (finalized in `251b7399`), so the concurrency hazard cannot arise and this plan is unambiguously the one running second. The stale-line-number consequence is REAL and now owned by E-06; the count is 11 other pending plans declaring these modules at HEAD `d4c677c5`, not 12. Re-measure rather than trusting either number. | `.aw/records/plans/executed/20260903-runflags-01-uyeko5-...`; `git log` `251b7399`; `grep -l` over `.aw/records/plans/pending/` -> 11 |
| F-9 | **BLOCKER, FOUND IN REVIEW: THE ORIGINAL E-02 WAS FORBIDDEN BY AN APPROVED SPEC AND WOULD HAVE FAILED A PURPOSE-BUILT GUARD TEST.** Spec `7ckptx` (`Status: approved`) R4.1c states that no work tracing to it "may flip Antigravity's `--dangerously-skip-permissions` default to `False`", on measured evidence that running without it "failed or deadlocked repeatedly", and requires "its own decision, its own evidence that the deadlock is gone, and an explicit supersession" first. Its Non-goal 7 records the maintainer ruling of 2026-09-01 and says the default "MUST KEEP IT". A code comment at `agy_runipd.py:2613-2624` says "Do NOT 'harden' it". And `TestAntigravitySkipPermissionsDefaultIsPinned` (`tests/test_lane_permission_posture.py:315-351`) pins all three sites, including the literal source strings the withdrawn E-02 would have edited, with a failure message naming R4.1c. So the plan's self-described "actual safety defect" was a decided constraint, and its highest-priority item would have been reverted at review or CI. | spec `7ckptx:277-283` (R4.1c), `:92-99` (Non-goal 7); `agy_runipd.py:2613-2624`; `tests/test_lane_permission_posture.py:315-351` (measured green at HEAD `d4c677c5`) |
| F-10 | `--skip-audit` IS ALREADY CONFORMANT, so only THREE of the four named spellings are live violations. Measured: agy `start` REJECTS `--skip-audit`, and the string greps to zero in both runner modules. The plan's Scope and E-01 both listed it as something to remove. Keep it in the contract test as a regression guard, but do not describe it as a violation being fixed. | live `parse_args` rejection on `agy_runipd.build_parser()`; `grep -n skip-audit` over both runners -> no match |
| F-11 | A SHARED, DATA-DRIVEN FLAG REGISTRY NOW EXISTS, so a per-host hand edit is no longer self-evidently the right shape. `uyeko5` landed `runner_shared.RUN_POLICY_FLAGS` (declaring the run flag surface as DATA) and `register_run_policy_flags`, which both hosts now call. This is the canonical mechanism KISS and the repo's no-duplicate-paths convention would point at, and it also offers E-01 a second in-code source of truth for the prohibition. Whether the removal belongs there or stays per-host is now a real decision (E-06), and it implies a fifth path plus `tests/test_runner_shared.py`. | `runner_shared.py:1242` (`RUN_POLICY_FLAGS`), `:1381` (`register_run_policy_flags`), `:102-112` (the data-driven rationale); call sites `oc_runipd.py:7197`, and the agy twin |
| F-12 | THERE IS PRECEDENT FOR DERIVING A TEST FROM THE SPEC FILE ITSELF, which is stronger than E-01's hand-copied table: `tests/test_run_evidence_completion.py:1029` parses spec `25kzda` 4.2's table straight out of the spec file and `skipTest`s when the spec is absent. A test built that way fails when the SPEC grows a prohibited name; a transcribed list silently goes stale. **AND THE PRECEDENT IS NOW DECISIVE FOR A DIFFERENT REASON:** `tests/test_run_flag_surface.py` ALREADY parses spec 2.1's grammar stanza and its docstring still cites "the `--no-verify`/`--skip-audit`/`--dangerous` group that `:140` states does NOT exist on `run`" as its reason for scoping to the grammar block. That comment is now stale prose describing a deleted sentence, which is a second artifact of F-13 and evidence that E-01's spec-derived table would have been built on the same vanished line. | `tests/test_run_evidence_completion.py:1029`, `:1077`, `:1551`; `tests/test_run_flag_surface.py:94-133` |
| F-13 | **THE PROHIBITION WAS DELETED FROM THE SPEC ON 2026-09-06, ELEVEN HOURS AFTER ROUND 1 REVIEWED THIS PLAN, AND EVERY REMAINING ITEM IS NOW WRONG IN DIRECTION.** Commit `844d195c` ("spec(25kzda): drop the --no-verify/--dangerous ban; gate on V-evidence") removed the sentence this plan's Concern quotes, on the maintainer's 2026-09-05 ruling, for the same three reasons the plan's own OQ-01 later recorded: unexamined external-model provenance, a spec-versus-spec contradiction with `7ckptx` R4.1c, and the conflation of the GIT `--no-verify` with the RUNNER one. What survives at `:161` is only "There is no `--skip-audit` flag and no GIT hook-bypass flag on `run`", and `:162` states that `--dangerous` "is REMOVED from this prohibition". So E-02 (drop the `--dangerous` alias), E-03 (drop agy's `--no-verify`/`--no-audit`), and E-05 (drop opencode's generated negations) would each now REMOVE A FLAG THE SPEC PERMITS, and E-01's contract test would ASSERT A PROHIBITION THE SPEC NO LONGER MAKES, failing forever against a conformant tree. Verified: `844d195c` is an ancestor of HEAD and NOT an ancestor of `d4c677c5`, the HEAD round 1 measured at, so round 1 was correct at the time and is now superseded by a change it could not have seen. | `844d195c` (dated 2026-09-06 09:19); spec `:161`, `:162`; `git merge-base --is-ancestor 844d195c d4c677c5` -> false, `... HEAD` -> true |
| F-14 | **THE PLAN'S OWN OQ-01 RESOLUTION ALREADY CONTAINS THIS FINDING AND DOES NOT ACT ON IT.** Its Part Two records the maintainer ruling that "THE SPEC IS WRONG TO FORBID THESE FLAGS", says the requirement "should have been CAPTURED SOMEWHERE rather than left as a prohibition", and files backlog `behjg6` (`open`, `Blocks-Release: next`) to correct 2.1. But it then concludes "WHAT THIS PLAN MAY STILL DO, UNCHANGED. E-02's alias-only removal of the `--dangerous` SPELLING stands", which does not follow from its own premise: if the prohibition is wrong and being withdrawn, then removing a spelling BECAUSE it is prohibited is removing it for a reason that no longer holds. The resolution was also written on 2026-09-07, the day AFTER the spec sentence was actually deleted, yet describes the correction as still pending ("it is now tracked as backlog `behjg6`") rather than as already landed in 2.1. So the plan is one step behind its own record. | plan OQ-01 Part Two; `.aw/records/backlog/open/20260907-specflags-01-behjg6-...backlog.md`; `844d195c` predates the OQ-01 annotation by one day |
| F-15 | **THE `--skip-audit` PROHIBITION THAT SURVIVES IS SATISFIED, AND THE GIT-SENSE ONE IS ENFORCED ELSEWHERE, SO THERE IS NO CONFORMANCE WORK LEFT AT ALL.** Measured at HEAD: `--skip-audit` is REJECTED on oc `start`, oc `resume`, agy `start`, and agy `resume`, and greps to zero in both runner modules (F-10 already established this). The surviving GIT-sense prohibition is enforced by the commit gateway, not by a parser: `git_commit_helper.py` documents "never `--no-verify`; never `push`" (`:9`, `:365`, `:484`) and `commit_lock.py:175` states its behavior "is emphatically NOT `--no-verify` in disguise". Neither is a flag on `run`. So after F-13 this plan has ZERO live violations to fix, which is why the correct disposition is a maintainer decision and not a smaller edit. | live `parse_args` over all four subparsers; `grep -n skip-audit` both runners -> no match; `git_commit_helper.py:9`, `:365`, `:484`; `commit_lock.py:175` |
| F-16 | A FIFTH REGISTRATION SITE EXISTS AND IS CORRECTLY OUT OF SCOPE, recorded so a later reader does not think the audit missed it. `agy_run.py:286` registers `--no-verify`/`--skip-verify`/`--no-audit`/`--skip-audit`. That module is `aw agy exec`, a SEPARATE single-target verb (its own docstring: "exposed as the non-colliding `aw agy exec` subcommand"; `aw agy --help` lists `exec` distinctly from `runipd`/`run`/`runagy`), and spec 2.1 governs the `run` surface only. It is named here as deliberately excluded rather than left to look like an omission, since it is the one place `--skip-audit` IS registered anywhere in the package. | `agy_run.py:9-11`, `:286`; `aw agy --help` |

## Proposed changes (ordered, validatable)

0. Re-locate every site by symbol at the executing HEAD, retire F-8's spent concurrency warning, and decide shared-registry versus per-host edit before touching a parser (E-06).
1. Table-driven parse-based contract test over spec 2.1's prohibited spellings, derived from the spec file where practical, covering all FOUR subparsers, failing at HEAD for the three live violations (E-01).
2. Remove agy's prohibited `--dangerous` ALIAS ONLY, leaving the canonical flag, its `True` default, and all three protected sites untouched (E-02).
3. Remove agy's prohibited `--no-verify`/`--no-audit` spellings, leaving the run-state key intact (E-03).
4. Decide and implement agy's conformant verification control, preserving both hosts' defaults (E-04, OQ-01 blocking).
5. Remove opencode's auto-generated prohibited spellings on BOTH the `start` and `resume` subparsers, and update the two shipped test cases that pin them (E-05).

## Deferred / out of scope (with reason)

- FLIPPING agy's `--dangerously-skip-permissions` DEFAULT. Newly and emphatically out of scope (F-9): approved spec `7ckptx` R4.1c forbids it to any plan, a maintainer ruling of 2026-09-01 decided it on operational evidence, and a purpose-built test pins it. Changing it needs its own decision, its own evidence the deadlock is gone, and an explicit supersession of R4.1c. This plan removes the prohibited ALIAS and nothing else about that flag.
- REGISTERING SPEC 2.1'S POLICY FLAGS. `uyeko5` (`runflags-01`) owned them and is now EXECUTED. This plan removes prohibited flags; that one added required flags. Same spec section, opposite directions.
- `--type`, `--action`, AND `--json` ON `run`. All three are spec 2.1 grammar entries that are also unbuilt (`--action` greps to zero; `--json` exists only on `status`). Recorded so they are not mistaken for shipped, but adding them is selection/dispatch work, not bypass-flag removal.
- HARMONIZING THE TWO HOSTS' VERIFICATION DEFAULTS. Deliberately divergent and documented in code (F-5). Unlike `--full-auto`, no maintainer ruling exists, so this plan preserves both and asks rather than normalizing under cover of a conformance fix.
- RENAMING THE `no_verify` RUN-STATE OPTION KEY. Load-bearing for frozen runs and five tests (F-6). A key migration needs its own compatibility story.
- AUDITING FOR OTHER HOOK-BYPASS PATHS. Spec `:135` also forbids "hook-bypass" flags generically. This plan closes the four NAMED spellings plus the permission default; a general audit for `--no-verify`-equivalent escape hatches elsewhere in the runners is not attempted here and is stated rather than implied.

## Scope check

- **WHOLE-PLAN SCOPE VERDICT AT ROUND 2: THE PLAN IS NOW ENTIRELY OVER-SCOPE, because its target no longer exists (F-13).** Every removal it specifies acts on a flag the spec permits, and the one prohibition that survives is already satisfied (F-15). This is not a scope creep to trim; it is a plan whose scope evaporated underneath it. OQ-02 owns the disposition.
- Over-scope REMOVED IN ROUND 1: the original E-02 flipped agy's permission default, which approved spec `7ckptx` R4.1c forbids to any plan (F-9). That was the plan's largest behavior change and it is gone; E-02 is now an alias-only edit. This is the one finding that could not have been fixed by adding evidence, because the work itself was prohibited.
- Over-scope: none remaining. Every edit removes a spec-prohibited flag spelling or updates a test that pins the violation.
- Scope widened in review: `tests/test_runner_shared.py` added to `Scope-Paths` because E-06 may conclude the removal belongs in the shared data-driven registry (F-11). If E-06 decides per-host instead, that path goes unmodified and needs a `--scope-ack` at finalize.
- Under-scope, DELIBERATE and stated: the generic "hook-bypass flag" clause of spec `:140` is closed only for the named spellings, not by a general audit.
- Under-scope: whether `--no-validate` (argparse-generated from the surviving `--validate`) is itself prohibited is OQ-01's second half, and the answer decides whether E-04 option (a) is conformant at all. Note this now affects FOUR subparsers, since `--no-validate` is generated on oc `resume` too.
- Correction, not a scope change: `--skip-audit` was listed as a spelling to remove but is already rejected on both hosts (F-10). Only three of the four named spellings are live violations.

## Required tests / validation

- E-01's contract test, demonstrated FAILING at pre-change HEAD and passing after, across all FOUR subparsers, asserting by PARSING rather than by grep (F-3).
- agy: `--dangerously-skip-permissions` shown UNCHANGED in name, default, and reach into the child argv, and `tests/test_lane_permission_posture.py` green (F-9). This is the inverse of the original plan's demand and is now the safety-critical check: the evidence must show the default was NOT flipped.
- Both hosts: `--no-verify`, `--no-audit`, and `--dangerous` shown REJECTED, on `resume` as well as `start` where the parser carries them. `--skip-audit` shown rejected as pre-existing conformance, not as a fix (F-10).
- Verification defaults shown SIDE BY SIDE and UNCHANGED: agy ON, opencode OFF (F-5). This is the regression this plan is most likely to cause.
- The five agy tests writing the `no_verify` state key pass unchanged (F-6), and opencode's frozen-state compatibility read at `:6034` is untouched.
- `tests/test_oc_runipd.py`'s two pinning cases updated to assert rejection, with the rest of that test still passing (F-4).
- Three driver/guard suites green: `tests/test_oc_runipd.py`, `tests/test_agy_runipd_cli.py`, and `tests/test_lane_permission_posture.py`. The third is NOT optional: it is the guard that would have caught the withdrawn E-02.
- Full suite BARE (`python3 -m pytest`), compared against YOUR OWN pre-change measurement at the HEAD you started from. No `-n0`, no second `-q`, no `-p no:randomly`.
- Measure in the PRIMARY checkout, not a scratch worktree (`tests/test_run_viewer.py` shows ~15 phantom failures in a detached worktree; backlog `dh0uno`).

## Spec / documentation sync

- **THIS SECTION'S PREMISE IS VOID (F-13).** The plan cannot make the CLI satisfy spec `:140` because `:140` no longer says what the plan quotes: the prohibition was deleted at `844d195c` and what remains at `:161` covers only `--skip-audit` and the GIT hook-bypass sense, both already satisfied (F-15). The real remaining spec work is backlog `behjg6`'s correction of 2.1 to CAPTURE the required capability rather than forbid it, and that is a spec edit needing its own approval, which OQ-02 option (b) would make this plan's job.
- ONE STALE IN-CODE COMMENT IS WORTH REPORTING WHOEVER OWNS THE FOLLOW-UP: `tests/test_run_flag_surface.py:94-133` still justifies its grammar-block scoping by citing "the `--no-verify`/`--skip-audit`/`--dangerous` group that `:140` states does NOT exist on `run`". The test passes and its SCOPING is still correct, but its stated reason now describes a deleted sentence. Fixing that comment is not this plan's business while OQ-02 is open, and it is not in `Scope-Paths`; it belongs with `behjg6`.
- ORIGINAL, NOW FALSE: This plan makes the shipped CLI satisfy spec `25kzda` 2.1 `:140`. It does NOT change the spec text.
- It also must not disturb approved spec `7ckptx` R4.1c, whose protection of agy's permission default this plan now respects rather than violates (F-9). No edit to that spec is needed or permitted here; superseding R4.1c would be a separate, evidence-gated decision.
- If OQ-01 is answered such that a capability must be renamed, the new flag name is NOT in the spec's grammar block; record whether the spec's 2.1 grammar needs the conformant replacement added, and if so say that the spec edit is a follow-up rather than editing an `approved` spec inside this plan.
- If any user-facing doc enumerates `aw agy run` or `aw oc run` flags, update it; otherwise state N/A with the paths checked.

## Open questions

### OQ-01: What replaces agy's `--no-verify`, and is the argparse-generated `--no-validate` itself prohibited?

- Blocking: yes
- Status: resolved
- Owner: maintainer
- Resolution or deferral rationale: RESOLVED 2026-09-07 BY MAINTAINER RULING: DEFER E-04 UNTIL `kgpptv` AND `f2mrsw` LAND, and record that THE SPEC IS WRONG TO FORBID THESE FLAGS. Two distinct parts, both load-bearing.
  PART ONE, THE SEQUENCING. E-04 is DEFERRED, not answered on its merits, because the honest answer depends on a capability that does not exist yet. The real need behind `--no-verify` is PER-MODEL ROUTING, not a global switch, and that lands as `runprofile` Order 6 (`kgpptv`, the verifier turn resolving its own profile) plus `f2mrsw` E-03 (a per-profile `validate` tri-state). Once both are in, "skip verification for this model" is CONFIGURATION rather than a CLI bypass, which is the state in which E-04 can be decided without removing the only available control. So: land E-01, E-02, E-03, E-05, E-06 now (they are mechanical and independently correct), and hold E-04. Do NOT delete agy's ability to disable verification before the replacement exists.
  PART TWO, THE SPEC IS WRONG, AND THAT IS THE MORE IMPORTANT HALF. The maintainer ruled that spec `25kzda` 2.1 is INCORRECT to forbid these flags outright: `--dangerous` (and its `--dangerously-skip-permissions` spelling) is 100% OPERATIONALLY REQUIRED on agy, which stalls on permission prompts nearly every run without it and gets killed, and that requirement should have been CAPTURED SOMEWHERE rather than left as a prohibition the code then has to violate in practice. The prohibition-by-name is therefore not a safety property being defended; it is a contract that never matched operational reality. This does NOT authorize this plan to edit the spec: the correction is its own change with its own maintainer approval, and it is now tracked as backlog `behjg6` (`Blocks-Release: next`). What it DOES do is settle the by-name-versus-by-intent reading this question posed: neither reading is adopted, because the premise (that these spellings must not exist) is itself under correction.
  WHAT THIS PLAN MAY STILL DO, UNCHANGED. E-02's alias-only removal of the `--dangerous` SPELLING stands, because approved spec `7ckptx` R4.1c independently forbids flipping the DEFAULT and E-02 does not touch it; removing a prohibited alias while the capability and its default survive is exactly the narrow edit already reviewed. Do NOT read "the spec is wrong" as license to restore the alias, flip a default, or widen E-02.
  ANNOTATION 2026-09-05 SUPERSEDED IN PART: its closing sentence said this question deliberately stayed open pending a by-name-versus-by-intent ruling. That ruling is now moot for the reason in Part Two, and the sequencing in Part One is the operative answer.
- ANNOTATION 2026-09-05 (does NOT answer this question): the maintainer's response to being asked OQ-01 was that the question as posed missed the operational reality, and it surfaced two facts this plan should carry. (1) `--dangerous` ON agy IS OPERATIONALLY REQUIRED: without it that host stalls on permission prompts nearly every time and gets killed, so its removal must not be read as a safety win to be taken unconditionally. THE ROUND-1 REVIEW CONFIRMED THIS FROM THE REPOSITORY ITSELF and found it is not merely operational advice but a NORMATIVE PROHIBITION: approved spec `7ckptx` R4.1c forbids any plan from flipping that default, recording the same maintainer ruling dated 2026-09-01, and `tests/test_lane_permission_posture.py:315` pins it (F-9). The plan's original E-02 contradicted both and has been withdrawn; E-02 now removes only the prohibited ALIAS. So this half of the annotation is SETTLED and no longer needs the maintainer. (2) THE REAL NEED BEHIND `--no-verify` IS PER-MODEL ROUTING, not a global switch: verification should depend on which model did the work (the maintainer measured Opus needing no material corrections after validation, while a cheaper executor plausibly benefits from a stronger checker). That capability DOES NOT EXIST today (per-role model routing greps to zero; both turns read one shared `options["model"]`), so deleting the flag before it exists would remove the only available control. It is now planned: `runprofile` Order 6 (`kgpptv`) gives the verifier its own frozen profile, and `f2mrsw` E-03 already resolves a per-profile `validate` tri-state. Once both land, 'skip verification for Opus' is CONFIGURATION rather than a CLI bypass, which is the state in which this question can be answered honestly. This annotation deliberately leaves OQ-01 `open` and `Blocking: yes`: the maintainer has not ruled on the by-name versus by-intent reading, and the argparse-generated `--no-validate` on BOTH hosts is still unresolved.
- Resolution or deferral rationale: BLOCKING because E-04 cannot be implemented without the answer and a wrong guess is a silent safety regression. Spec `:135` forbids `--no-verify`, `--skip-audit`, `--dangerous`, and "hook-bypass" flags BY NAME; it does not name `--no-validate`. If `--no-validate` is acceptable, agy can take a `--validate` `BooleanOptionalAction` with `default=True` (matching opencode's spelling while keeping agy's ON default) and the fix is mechanical. If the prohibition is read by INTENT rather than by name, then any negation that disables verification is prohibited, `BooleanOptionalAction` is the wrong mechanism on both hosts, and opencode's surviving `--no-validate` must go too, which widens this plan. The one option NOT on the table is deleting agy's ability to disable verification without saying so, or flipping either host's default while renaming the flag (F-5): both would be safety changes disguised as conformance edits. Needs the maintainer because it is a reading of an `approved` spec's intent and a safety-posture call, not a fact the repository can settle.

### OQ-02: The prohibition this plan enforces was deleted from the spec. Retire the plan, re-point it at the `behjg6` correction, or narrow it to the surviving prohibition?

- Blocking: yes
- Status: open
- Owner: maintainer
- Finding: PR-201
- Resolution or deferral rationale: **THIS QUESTION REPLACES THE PLAN'S PURPOSE, WHICH IS WHY ONLY THE MAINTAINER CAN ANSWER IT.** The facts are settled and measured; the disposition is a judgement about what this release wants.
  WHAT HAPPENED. This plan's Concern quotes spec `25kzda` 2.1: "There is no `--no-verify`, `--skip-audit`, `--dangerous`, or hook-bypass flag on `run`." That sentence was DELETED on 2026-09-06 at `844d195c`, on the maintainer's own 2026-09-05 ruling, for three reasons the commit body records: it entered verbatim from unexamined external-model output, it contradicted approved spec `7ckptx` R4.1c, and it conflated the GIT `--no-verify` with the RUNNER one. Spec `:162` now states that `--dangerous` "is REMOVED from this prohibition". Verified: `844d195c` is an ancestor of HEAD but NOT of `d4c677c5`, the HEAD round 1 measured at, so round 1 was right at the time and this plan has been overtaken by a change it could not see (F-13).
  WHY THAT VOIDS THE PLAN RATHER THAN TRIMMING IT. Of the three live violations the plan identified, two (`--dangerous`, and agy's `--no-verify`/`--no-audit`) are no longer prohibited, and opencode's generated negations are no longer prohibited either. The one prohibition that SURVIVES (`--skip-audit`, plus the GIT sense of `--no-verify`) was already satisfied before the plan was written: `--skip-audit` is rejected on all four subparsers and greps to zero in both runners, and the git sense is enforced by the commit gateway rather than by a parser (F-15). So the plan has ZERO live violations left, and performing E-02, E-03 or E-05 would DELETE SANCTIONED FLAGS, one of which is operationally required and protected by `7ckptx` R4.1c.
  WHAT THE OPTIONS COST. (a) RETIRE IT to `not-executed/` with a header naming `844d195c`, and let backlog `behjg6` (`open`, `Blocks-Release: next`) carry the real remaining work, which is correcting 2.1 to CAPTURE the required capability instead of forbidding it. Cheapest and most honest; costs the loss of E-01's contract-test idea and E-06's re-location work, both of which are cheap to re-derive and the latter of which is already recorded here. (b) RE-POINT this plan at `behjg6`'s correction, making it the spec-amendment plan rather than a code-removal plan. Preserves the `Blocks-Release: next` gate in a plan that already carries the full evidence base, but it is a different kind of change (a spec edit, needing its own approval) than the fence and every E-item describe, so it is close to a rewrite. (c) NARROW it to a regression guard only: keep E-01 reduced to "`--skip-audit` stays rejected on all four subparsers and no runner flag reaches `git commit --no-verify`", drop E-02 through E-05 entirely. Small, genuinely useful, and honest, but it is a test-only plan whose value is modest and which does not discharge `Blocks-Release: next`.
  RECOMMENDATION: (a), with (c)'s regression guard folded into `behjg6`'s eventual plan if it is wanted. (b) is defensible if the maintainer prefers to keep this plan's release gate and evidence rather than re-file. What must NOT happen is executing the plan as written, and NOT because the plan is careless but because the contract it enforces was withdrawn.
  WHY I DID NOT DECIDE IT MYSELF. Retiring a plan that carries `Blocks-Release: next`, or converting it into a spec-amendment plan, is a scope and release-priority decision, and the workflow forbids guessing a human decision. The repository answers what is TRUE here; it does not answer what this release should DO about it.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

**EVERY `V-*` ITEM BELOW IS SUSPENDED WITH ITS `E-*` PARTNER (F-13).** They are retained for the E/V bijection and for the record. V-01 through V-05 would each certify the removal of a flag the spec now permits, so producing their evidence would be producing evidence for the wrong outcome; V-06's re-location half is already satisfied in E-06's note above. Before any of them is worked, OQ-02 must be answered.

- [ ] V-01 validates E-01
  - Required evidence: paste the contract test FAILING at pre-change HEAD, naming the prohibited spellings accepted on each host, then passing after. Paste the test's DATA table AND state whether it is derived from the spec FILE or transcribed (F-12); if transcribed, paste the justification and the pinned spec line. Paste coverage of all FOUR subparsers (oc `start`, oc `resume`, agy `start`, agy `resume`), since a `start`-only test would leave oc `resume` violating the spec and still report success (F-3). Paste evidence the test asserts by PARSING (a `SystemExit`/parse-failure assertion), not by grepping source. Note `--skip-audit` as already-passing regression coverage rather than a fixed violation (F-10).
  - Observed evidence:
  - Result: pending

- [ ] V-02 validates E-02
  - Required evidence: paste `aw agy run --dangerous` REJECTED. Then paste PROOF OF NON-DISTURBANCE, which is this item's real burden: `--dangerously-skip-permissions` still parses and still yields `dangerously_skip_permissions is True`; the options fallback and consumer read are UNCHANGED (paste the diff showing no edit to those lines); and `python3 -m pytest tests/test_lane_permission_posture.py` green, with the `TestAntigravitySkipPermissionsDefaultIsPinned` cases named. State explicitly that the withdrawn default-flip was NOT performed and cite `7ckptx` R4.1c as the reason (F-9). A green full suite does not substitute: name that test class.
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: paste `aw agy run --no-verify` and `--no-audit` each REJECTED. Paste the five `tests/test_agy_runipd_cli.py` cases that write the `no_verify` state key passing UNCHANGED, proving the flag was removed without removing the key (F-6). State explicitly which surface you changed (flag) and which you did not (state key), and paste the freeze site at `agy_runipd.py:1837` showing what a NEW run now freezes for `no_verify`.
  - Observed evidence:
  - Result: pending

- [ ] V-04 validates E-04
  - Required evidence: state which option (a) or (b) OQ-01 was answered with, and by whom. Paste agy's new verification control in `--help` and paste a run with verification DISABLED through it. Then paste the two hosts' verification defaults SIDE BY SIDE showing agy still ON and opencode still OFF (F-5) - this is the specific regression this item risks, so a passing suite does not substitute for the side-by-side. If option (a) was chosen, paste whether `--no-validate` is generated and confirm that was accepted as conformant rather than overlooked.
  - Observed evidence:
  - Result: pending

- [ ] V-05 validates E-05
  - Required evidence: paste `--no-verify` and `--no-audit` REJECTED on BOTH oc subparsers (`start` AND `resume`; the `resume` half is the one the plan originally missed, so a `start`-only paste fails this item), and `--validate`/`--no-validate` still working on both. Paste `validate`'s default still `False` on `start` and `None` on `resume`, since collapsing the latter would turn "unset" into "disabled". Paste the diff of the two pinning cases in `tests/test_oc_runipd.py` (near `:1987`) now asserting rejection while the surrounding `--validate` coverage survives (F-4). Paste `oc_runipd.py:6034`'s legacy-key read UNCHANGED. Then both driver suites and the bare full suite with counts, compared against your own pre-change measurement.
  - Observed evidence:
  - Result: pending

- [ ] V-06 validates E-06
  - Required evidence: paste the re-located line numbers for every site this plan cites, measured at the executing HEAD, alongside the stale numbers they replace. Paste `git log` evidence that `uyeko5` is executed and state plainly that F-8's "must not run concurrently" warning is SPENT rather than repeating it as a live hazard. Paste the re-measured count of pending plans declaring these modules. Then paste the recorded decision on shared-registry (`runner_shared.RUN_POLICY_FLAGS`) versus per-host parser edit, with its reasoning and which paths it implies (F-11); if per-host, say so explicitly so `tests/test_runner_shared.py` can be `--scope-ack`ed at finalize rather than looking like an omission.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: 6 E-leaves across 2 task groups, one concern throughout: make the `run` parsers satisfy spec 2.1's bypass prohibition. Right-sizing assessed per item, not by count. E-06 is re-location and the shared-registry decision, and it is FIRST in effect (depends on nothing) because every line number in this plan is stale and the correct implementation SHAPE is now an open choice; E-01 is the contract test; E-02 is the `--dangerous` alias removal alone, kept separate because its real burden is proving the protected default was NOT disturbed; E-03 and E-05 are the per-host verification-spelling removals, separate because the opencode one spans two subparsers and updates a shipped test while the agy one is a deletion; E-04 is isolated precisely because it carries the only real design question and the only cross-host regression risk. E-01, E-02, E-03, E-05 and E-06 are each executable and validatable on their own; only E-04 is gated on OQ-01.
- ROUND-1 REVIEW CHANGED THIS PLAN'S SHAPE, and the reason belongs in the gate: the original E-02 was the plan's self-declared centerpiece ("the actual safety defect", the item that "must land even if OQ-01 stalls E-04") and it was FORBIDDEN by approved spec `7ckptx` R4.1c. It has been replaced by an alias-only edit whose validation proves the opposite of what the original demanded. Anyone reading this plan's history should understand that the safety framing was withdrawn on evidence, not softened.

**DO NOT EXECUTE THIS PLAN. ITS CONTRACT WAS WITHDRAWN.** The spec sentence every item enforces was deleted at `844d195c` (F-13), so E-01 would pin a prohibition that no longer exists and E-02, E-03 and E-05 would each delete a flag the spec now permits. Only OQ-02 matters, and only the maintainer can answer it.

Open questions: OQ-02 (retire, re-point at `behjg6`, or narrow to the surviving prohibition) is BLOCKING and OPEN, and it gates the WHOLE plan rather than one item. OQ-01 is `resolved` but was answered against the pre-`844d195c` spec and its Part Two contains the very finding that voids the plan without acting on it (F-14); its E-04 sequencing also names the wrong successors, since `f2mrsw` is executed, the tri-state is dead in both drivers, and the `hostdefault` Set now owns the wiring.

This plan is `reviewed` and requires explicit human approval before execution. Readiness stays `no-go`, now for a stronger reason than in round 1: not a pending design question inside a valid plan, but a plan whose premise is void. That is a genuine not-ready condition, not a missing signature.

Scope fence: touch ONLY `agent_workflows/oc_runipd.py`, `agent_workflows/agy_runipd.py`, `tests/test_oc_runipd.py`, `tests/test_agy_runipd_cli.py`, and (only if E-06 so decides) `tests/test_runner_shared.py`. Do NOT register any of spec 2.1's POLICY flags (`uyeko5` already did). Do NOT rename the `no_verify` run-state option KEY or touch `oc_runipd.py:6034`'s legacy-compatibility read. Do NOT change opencode's `validate` default (`False` on `start`, `None` on `resume`) or agy's verification default (ON): preserving both while renaming is the whole difficulty of E-04. Do NOT flip, weaken, or "harden" agy's `--dangerously-skip-permissions` default, and do NOT edit its options fallback or consumer read: approved spec `7ckptx` R4.1c forbids it and `tests/test_lane_permission_posture.py` pins it (F-9). Do NOT change the verifier's logic, only the flag surface that reaches it. Do NOT add `--type`, `--action`, or `--json`. Do not broaden CASUALLY; if the work GENUINELY requires a path outside the fence, MAKE THE EDIT AND JUSTIFY IT: `aw ipd finalize` refuses to complete until every out-of-scope path carries a `--scope-reason` and every declared-but-unmodified path carries a `--scope-ack`, so an unjustified widening is CAUGHT AT THE GATE rather than prevented by halting a run (maintainer ruling 2026-09-01; a scope fence DECLARES, it does not halt).

Honesty rule (HARD MUST): **A FIFTH FORBIDDEN CLAIM IS ADDED AT ROUND 2, and it is now the most important one: do NOT report that this plan brings the runners into spec conformance, and do NOT cite spec 2.1's four-spelling prohibition as current. It was deleted at `844d195c` (F-13). Any evidence paste that quotes the old sentence as the governing contract is a false claim about the spec, whatever the parsers do.** The rest of this rule applies only if OQ-02 revives some part of the plan.
  Paste the ACTUAL runner output with the `git rev-parse HEAD` it was measured at, from the PRIMARY checkout. The load-bearing evidence is (1) E-01's contract test observed FAILING at pre-change HEAD across all four subparsers, including the opencode host whose violation no grep can see and its `resume` half the plan originally missed; (2) the two verification defaults shown side by side and UNCHANGED after E-04, because that is the regression a green suite is least likely to catch; and (3) `tests/test_lane_permission_posture.py` green with its pinning class named, proving agy's protected permission default was NOT touched. FOUR CLAIMS ARE FORBIDDEN because they were made in this plan and found wrong: do NOT describe agy's permission default as a safety defect this plan fixes (F-9, it is a decided constraint under an approved spec); do NOT report `--skip-audit` as a violation removed (F-10, it already conformed); do NOT report the oc fix as complete on the basis of the `start` subparser alone (F-3, `resume` carries it too); and do NOT repeat F-8's "must not run concurrently" as a live hazard (`uyeko5` has executed). Also do NOT report the generic "hook-bypass" clause of spec `:140` as fully closed: this plan closes named spellings only, and no general audit was performed.

Execution contract: RE-READ both runner modules immediately before editing and locate every parser and fallback site BY SYMBOL, never by the line numbers in this plan: EVERY line number cited here is stale (the tree moved when `uyeko5` executed), these are the highest-contention files in the repo, and 11 other pending plans declare them. E-06 exists to do that re-location first and to decide whether the shared `runner_shared` registry is now the right home. OQ-01 is BLOCKING and must be answered before E-04. Commit ONLY the files changed, path-scoped (`git commit -m msg -- <paths>`), never `git add -A`, never push. Verify with `git diff --cached --name-only` before every commit and re-verify after any hook interruption, since a failed hook invalidates the check. If a co-worker's in-flight change cannot be safely combined with an edit, STOP and report rather than overwriting.

Post-gate lifecycle: do not claim done or move this plan until every `V-*` item is verified with concrete pasted evidence and `aw ipd lint --phase pre-transition` reports conforming; the transition is performed by `aw ipd finalize`, never by hand.
