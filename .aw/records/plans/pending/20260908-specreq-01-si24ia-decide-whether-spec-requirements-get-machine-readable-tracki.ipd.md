# IPD: Decide whether spec requirements get machine-readable tracking, by measuring the corpus and costing the retrofit before building anything

- Date: 2026-09-08
- Kind: child
- Concern: Nothing in this repository can answer "which requirements of an approved spec are still unbuilt?" A spec is visible only as a WHOLE artifact: `aw attention` maps `approved` to `ready` and `implementing` to `active` (`attention_contract.py:262-272`), so an `approved` spec does appear, but there is no way to say "6 of 9 requirements are built". So a spec can sit half-built for weeks with nothing asking which half. Measured: spec `c4gd2h` has been `implementing` for 9 days with ONE workflow-history line, carries `- Blocks-Release: next`, and holds 23 `- R<n>.` requirement bullets that nothing parses.
  THE THREE GAPS ARE REAL AND I VERIFIED EACH. (1) NO PER-REQUIREMENT MODEL: `agent_workflows/specs.py` is 1066 lines and grepping it for `requirement|R[0-9]|partial` yields exactly two incidental hits (`:1048`, `:1063`), both about evidence-artifact resolution; `validate_spec` (`:211-326`) checks status, gate fields, history, priority and work-kind, and nothing else; there is no requirement-level counterpart to `From-Backlog`/`From-Spec`, both of which are whole-artifact id6 links. (2) NO PARTIAL STATE: the canonical enum `SPEC_STATUSES` (`attention_contract.py:241-253`) has nine values and none expresses partial coverage. (3) `implemented` IS NOT SEMANTICALLY VERIFIED, which the code states about itself at `attention_contract.py:453-463`: `aw specs` enforces "presence + format + resolvability, NOT semantic verification that the work truly happened".
  THIS IS AN OPEN QUESTION AND THE MAINTAINER EXPLICITLY DID NOT COMMIT TO IT: "I'm not sure if having a spec is sufficient, since we're not really going back to them for 'hey, what's in the specs that we still need to address'. Maybe that should be a thing? Definitely not sure." So this plan DECIDES; it does not build a tracking mechanism. Writing an implementation plan for an undecided design would be inventing the maintainer's answer.
  THE DECISIVE COST FINDING, which the item does not have and which is why measuring must precede deciding: THERE IS NO SINGLE PARSABLE REQUIREMENT CONVENTION. I surveyed all 28 specs mechanically and found SIX incompatible id conventions in use plus prose-only specs, and THIRTEEN of the 28 have no machine-parsable requirement id at all. So this is not "add a parser"; it is a corpus-wide authoring project. That is the strongest available input to the item's own cost/benefit question and it points at a cheaper answer than the full mechanism.
  THE SURVEY'S HEADLINE NUMBERS DO NOT REPRODUCE AND ITS METHOD IS THE REASON (review PR-301, PR-302). Re-measured at review: the corpus is **29** specs, not 28, and **7** are `approved`, not 6. More importantly the six-grep method MISSES a whole convention and so misclassifies the single largest requirement set in the tree: `7ckptx` states 45 requirements as BARE LINE-START ids (`R1.1 For an isolated turn...`), which the plan's bullet-anchored `- R<n>.<n>` grep cannot see, and 122 `R<n>.<n>` references appear in its prose. So `7ckptx` was counted as prose-only when it is in fact the most systematically id'd spec in the corpus. With that seventh convention added, the prose-only count is **14 of 29**, not 13 of 28, and the parsable requirement total is roughly 174 rather than the ~64 the plan's six forms imply. The lesson for E-01 is METHODOLOGICAL and it is why that item is rewritten: a fixed list of greps invented before reading the corpus will always miss a form nobody thought of, so the survey must ENUMERATE FROM THE FILES rather than test the corpus against a guess.
  AND THE RETROFIT COST IS OVERSTATED BECAUSE IT COUNTS DEAD SPECS (review PR-303). Of the 14 prose-only specs, **8 are terminal** (7 `implemented`, 1 `superseded`). Nobody retrofits requirement ids into a spec whose work is finished, so the live retrofit burden is **6 specs**, not 13 or 14. That materially weakens the plan's own central argument against option C, which is exactly why the correction has to be in the plan and not discovered by its executor: the decisive number the maintainer is being asked to weigh is 6, and 13 would have made option C look roughly twice as expensive as it is.
- Scope: Answer the item's five open questions with measured evidence and produce a written recommendation the maintainer can accept or reject, covering: whether requirement tracking should exist at all, and if so the minimum viable form (a convention plus an `aw check` rule, versus a parsed model plus an attention view), the retrofit cost per spec, and whether `implemented` should be computed from coverage. Deliverables are a research record with the corpus measurement and a recommendation section in this plan. EXCLUDES implementing any requirement parser, any new spec status, any new front-matter field, any `aw check` rule, any change to `aw attention`, and any edit to a spec's requirement text. Nothing in `agent_workflows/` changes.
- Scope-Paths: .aw/records/research
- Item-Dependencies: none
- Status: reviewed
- Readiness: go-pending-approval
- Set: specreq
- Order: 1
- Highest E allocated: 05
- Author: opencode/its_direct/pt3-claude-opus-5-1m-us
- Id: si24ia
- From-Backlog: f1sw71

## Workflow history

- 2026-09-10 reviewed (opencode its_direct/pt3-claude-opus-5-1m-us): /plan-review; APPROVE WITH REVISIONS APPLIED; readiness GO - PENDING HUMAN APPROVAL. Findings PR-301..PR-309, all FIXED in place, no deferrals, no open questions added. Structural preflight `aw ipd lint --phase author` conformed before semantic review and `--phase review-finalize` conformed after. DISCLOSURE: same model family and session lineage as the author, so this is close to a self-review and worth less than an independent one.
  THE FRAMING IS RIGHT AND I DID NOT WEAKEN IT. A decision plan is the correct response to an open question whose requester said "definitely not sure", the refusal to build anything is principled rather than timid, E-04's insistence on recommending rather than presenting is the right call, and demanding a disconfirming case is the mark of an honest survey. All five structural claims re-verified true: `specs.py` at 1066 lines with exactly two incidental hits (`:1048`, `:1063`), `validate_spec` at `:211`, the nine-value `SPEC_STATUSES` with no partial state, `_SPEC_MAP`'s `approved`->ready and `implementing`->active, and `APPROVAL_FLOOR`'s own written admission that `aw specs` enforces "presence + format + resolvability, NOT semantic verification". F-10 verified exactly: six pending plans name `f1sw71` and exclude it, and only this plan carries `- From-Backlog: f1sw71`.
  BUT THE SURVEY THAT IS THIS PLAN'S ENTIRE VALUE DOES NOT REPRODUCE, AND ITS METHOD IS WHY. The six greps were all anchored on a leading `- ` bullet, so they could not see `7ckptx`'s 45 BARE LINE-START requirements (`R1.1 For an isolated turn...`), and the tree's most systematically id'd spec was filed as prose-only. That is a seventh convention, and it lifts the parsable requirement total from ~64 to roughly 174. A method that can only find forms the author already thought of cannot measure convention diversity, which is the one thing this item exists to measure, so E-01 was rewritten to DERIVE the convention list from the files.
  AND THE DECISIVE COST NUMBER IS INFLATED ROUGHLY 2x, which matters because it is the plan's central argument against the full mechanism. Of the prose-only specs, EIGHT are terminal (7 `implemented`, 1 `superseded`) and nobody retrofits a finished spec, so the LIVE retrofit burden is 6 specs, not 13. One of those 6 (`25kzda`) is already section-addressable via its 47 numbered sections, cited that way in prose and in `run_recovery.py:64`, so for it "retrofit" may mean declaring an existing scheme canonical. Six-minus-one is a materially different decision from thirteen, and the maintainer must weigh the corrected figure. Corpus totals also moved again: 29 specs and 7 `approved` at review, against 28/6 authored and 27/5 filed.
  THREE MORE CORRECTIONS, ONE OF WHICH COULD HAVE DESTROYED A CO-WORKER'S WORK. Both named validation gates were measured wrong: `aw research index --check` is ALREADY RED at HEAD (exit 1, 49 pre-existing findings), so requiring it to prove consistency is unmeetable and it is now a DELTA gate; and the suite baseline is wrong in both halves, since actual is `1 failed, 5958 passed` while the named `tests/test_orchestrator_retirement.py` PASSES (`112 passed`), the real failure being a walk over 189 files in a GITIGNORED `opencode-recovery/` directory owned by another party that an executor could have "cleaned up". An explicit prohibition was added. Separately the `specdirs` collision is not a future risk but a present one: `1bdxcp` MOVES every spec into a status subdirectory, so a path-keyed survey table is stale on arrival and E-01 must enumerate recursively and key rows by id6/stem.
  TWO GAPS FILLED WITH EVIDENCE THE PLAN ASKED FOR BUT DID NOT SUPPLY. E-02 demanded a disconfirming case while naming none, leaving its most important instruction to executor initiative; two strong lines already exist and are now named, the working `trjfyy` -> `xipfy1` backlog workaround, and three reviewed plans (`jxxec8`, `iuxtjy`, `y9s4vm`) that priced this exact gap and deliberately shipped advisory-only around it. And a fourth option was added to the option space, convention-only with NO check rule, because the measurement points at it and because a rule over a corpus with non-conforming live specs must either exempt them or block unrelated work. One last datum, reported rather than silently fixed: this plan cites the retry budget as "`25kzda` 2.1" four times and §2.1 is the command grammar, so a plan about requirement addressability misaddressed a requirement, which is the drift it argues for tracking, observed in itself.
- 2026-09-08 to-review (opencode/its_direct/pt3-claude-opus-5-1m-us): Graduated from backlog `f1sw71`. The item carries no `- Blocks-Release:` so none is inherited or invented.
  DELIBERATELY A DECISION PLAN, NOT AN IMPLEMENTATION PLAN, and that is the single most important thing about it. The item is an OPEN QUESTION filed at the maintainer's request with an explicit non-commitment, and it ends with five questions that must be answered "before any implementation". A plan that built a requirement model would be answering those five questions on the maintainer's behalf, on a feature whose own author said "definitely not sure", and would add authoring burden to every future spec on an agent's guess. So the deliverable here is a measurement and a recommendation.
  EVERY FACTUAL CLAIM IN THE ITEM VERIFIED TRUE, with two stale numbers corrected. `specs.py` is now 1066 lines, not 1006, and its two incidental `requirement` hits moved from `:988,:1003` to `:1048,:1063`. The corpus is now 28 specs, not 27, with 6 `approved` rather than 5 (re-counted by parsing `^- Status:` across `.aw/records/specs/*.spec.md`, and cross-checked against `aw next --format json`). Every structural claim held: the `_SPEC_MAP` mapping, the nine-value `SPEC_STATUSES` enum with no partial value, `APPROVAL_FLOOR`'s own admission that `implemented` is not semantically verified, and `check.from-spec-dangling` (`check_engine.py:123-125`) validating only that an id6 resolves.
  THE FINDING THAT CHANGES THE ANSWER, and I measured it rather than assuming a convention existed. I counted requirement-id forms across all 28 specs with one grep per convention. Result: `- G<n> \`[Must]\`` in 7 specs, `- R<n>.` in 1 (that one holding 23 ids), `- R<n> (MUST)` in 3, `- **R-<n>**` in 2, `| I-<n> |` table rows in 1, and dotted `R<n>.<n>` sub-ids in 1 (`7ckptx`, whose own history treats `R3.3a-1` as a parent id whose halves are cited separately). THIRTEEN of 28 specs match NONE of these and state requirements in bare prose `MUST`, including `25kzda`, the release-gating run spec. So a parser cannot be pointed at the corpus: 13 specs would need ids INVENTED from prose and several need convention reconciliation. That reframes the item's cost/benefit question from "is 27 specs enough to justify a parser" to "is the retrofit worth it, and is there a cheaper 80 percent".
  THE ITEM'S OWN EVIDENCE GOT STRONGER WHILE I CHECKED IT. Its example of one spec section in three implementation states (`25kzda` 2.1's retry budget) is verified live: the range check is WIRED (`run_recovery.validate_retry_budget` called from `runner_shared.py`), while `plan_retry` and `retry_budget_remaining` have ZERO production callers and the module says so about itself. And item `trjfyy`, which tracks that, had to CORRECT one of its own filed claims on re-verification, which is itself an instance of the drift this item is about.
  NOTHING COVERS THIS AND SIX PENDING PLANS SAY SO EXPLICITLY. `rnkqrc`, `m867ox`, `y9s4vm`, `jxxec8`, `iuxtjy` and `st5klo` each name `f1sw71` and exclude it as out of scope; two record it as a numbered finding. No plan carries `- From-Backlog: f1sw71`. So the item is live, unclaimed, and known to be a real gap by adjacent work.

## Goal

Give the maintainer a decision-ready answer to "should spec requirements be tracked machine-readably, and if so in what minimum form?", grounded in a measured survey of the 28-spec corpus and an honest retrofit cost, so the answer is chosen rather than defaulted into by whoever writes the next plan.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: measure the corpus, because the decision turns on the retrofit cost

- [ ] E-01 SURVEY EVERY SPEC'S REQUIREMENT CONVENTION AND RECORD THE PER-SPEC RETROFIT COST, one row per spec.
  THIS IS THE LOAD-BEARING ITEM. The item's cost/benefit question cannot be answered without knowing how many specs would need requirement ids INVENTED versus merely parsed.
  DERIVE THE CONVENTION LIST FROM THE FILES; DO NOT TEST THE CORPUS AGAINST A FIXED LIST OF GREPS (review PR-302). This is the one instruction that matters most, because the authored survey did the opposite and got the corpus wrong in both directions. It ran one grep per PRE-GUESSED form, all six anchored on a leading `- ` bullet, and therefore could not see `7ckptx`'s 45 BARE LINE-START requirements (`R1.1 For an isolated turn...`), classifying the most systematically id'd spec in the tree as prose-only. A method that can only find forms you already thought of cannot measure convention diversity, which is the very thing this item exists to measure. So: read every spec, extract the candidate requirement-id tokens it actually uses, and let the convention list FALL OUT of that. Report any form the earlier survey missed as a finding about the method, not just a number.
  RE-MEASURE EVERYTHING; TREAT NO NUMBER IN THIS PLAN AS INPUT. At review the corpus was 29 specs (7 `approved`), against the plan's 28 and 6, having already moved once since the item was filed (27/5). Expect it to have moved again.
  CLASSIFY EACH SPEC INTO ONE OF THREE COSTS, since that is what the decision needs: PARSABLE AS-IS (has consistent machine-readable ids), NEEDS RECONCILIATION (has ids in a convention that differs from whatever is chosen, or mixes two), or NEEDS IDS INVENTED (prose `MUST` only, so a human must decide what the discrete requirements even are). The third class is the expensive one and its count is the decisive number.
  SPLIT EVERY CLASS BY LIFECYCLE STATE, BECAUSE A TERMINAL SPEC IS NOT RETROFIT WORK (review PR-303). Nobody adds requirement ids to a spec whose work is finished, so a raw prose-only count overstates the cost. Measured at review: 14 prose-only specs, of which 8 are terminal (7 `implemented`, 1 `superseded`), leaving a LIVE retrofit burden of 6. Report both figures and make the LIVE one the headline, because that is the number the maintainer is actually being asked to weigh and the inflated one argues against option C roughly twice as hard as the evidence supports.
  COUNT THE REQUIREMENTS, NOT ONLY THE SPECS. A spec with 45 sub-ids and a spec with 4 are not equal retrofit work. Report a total requirement count for the parsable set so the eventual view's size is known; at review that total was roughly 174 once `7ckptx`'s 45 were included, against the ~64 the plan's six forms implied.
  NOTE THE SPECS WITH NO `- Id:` AT ALL, and get the SCOPE of this right, because the plan understated it (review PR-304). It is not three specs: **19 of 29** carry no `- Id:`. Three are non-terminal (one `approved`, two `deferred`) and those are the ones that block a `<spec-id6>.<req-id>` join key today, but the whole-corpus figure is what tells the maintainer whether id6-keyed requirement tracking is viable at all over historical specs. Report both.
  RECORD SPEC ADDRESSING SCHEMES THAT ARE NOT REQUIREMENT IDS, because two specs are governed by them and a survey that ignores them will conclude "unaddressable" about text that is in fact precisely cited. `25kzda` carries 47 NUMBERED SECTIONS and this repository routinely cites it as "`25kzda` 2.9" or "spec 2.1's 0..10 bound" (see `run_recovery.py:64`); `pqsx96` uses `| I-<n> |` table rows. Numbered sections are a real, already-used addressing scheme and option B or C should consider adopting them rather than inventing parallel ids for such specs.
  DO NOT NORMALIZE OR EDIT ANY SPEC while surveying. This item reads; it changes nothing. Rewriting a requirement list to fit a convention would be implementing the undecided design.
  - Depends on: none
  - Expected outcome: a per-spec table of convention, requirement count, and retrofit class, with reproducible commands; the count of specs needing ids invented stated as the decisive cost number; specs lacking an `- Id:` flagged.
  - Execution state: pending

- [ ] E-02 MEASURE THE HARM THAT TRACKING WOULD ACTUALLY HAVE PREVENTED, using the corpus's own history rather than argument.
  A FEATURE THAT ADDS AUTHORING BURDEN TO EVERY SPEC NEEDS EVIDENCE OF AVERTED HARM, not just a plausible gap. Look for cases where a partially-implemented spec caused real waste: an agent re-implementing a shipped requirement, a plan blocked while confirming which half of a spec was live, or an `implemented` spec with an unbuilt requirement.
  THREE CANDIDATE CASES ARE ALREADY IDENTIFIED and should be checked rather than rediscovered. First, `c4gd2h`: `implementing` since 2026-08-30 (11 days at review, not the 9 the Concern states), `- Blocks-Release: next`, 23 requirement bullets, one history line, all four re-verified; the item states this uncertainty was hit directly while filing `1m3nul`, which had to caution its implementer to verify which stop levels existed before documenting them. Second, the RETRY BUDGET in `25kzda`, whose SECTION NUMBER THIS PLAN CITES WRONGLY (review PR-305): §2.1 is "Command grammar", not the retry budget. The correct anchors, located at review, are §1.1 (the runner engine "manages a configurable retry budget") and the §5-range text at `:586` defining the 0..10 frozen range; the code itself cites this as "spec 2.1's 0..10 bound" (`run_recovery.py:64`, `runner_shared.py:1994`), so the SPEC and the CODE disagree about the section number and the executor must say which is right rather than propagating either. The substance holds: `validate_retry_budget` IS called (`runner_shared.py:2103`), while `plan_retry` and `retry_budget_remaining` have ZERO callers outside their own module, verified by grep. Third, the two `deferred` specs untouched since 2026-08-08.
  NOTE THAT THIS MISCITATION IS ITSELF EVIDENCE, and the strongest single datum E-02 has. A plan whose whole subject is requirement addressability cited a spec requirement by the wrong section number, and the code that implements it cites a section number the spec does not use for it. That is precisely the drift per-requirement tracking would catch, observed in this plan's own text. Report it as such; do not quietly fix the number and move on.
  LOOK FOR THE STRONGEST DISCONFIRMING CASE TOO, and report it. If the corpus shows that whole-artifact status has been SUFFICIENT in practice, that is the answer and it must not be buried: the honest outcome of this plan may be "do not build this". A survey that can only conclude yes is not a survey.
  TWO DISCONFIRMING LINES ARE ALREADY AVAILABLE AND MUST BE ENGAGED, NOT REDISCOVERED (review PR-306), because the plan demanded a disconfirming case without supplying one, which left its most important instruction resting on the executor's initiative. FIRST, THE WORKAROUND DEMONSTRABLY WORKS: `trjfyy` tracks the split retry-budget requirement as an ordinary backlog item and graduated it to plan `xipfy1`, so the corpus's own answer to a partially-built requirement has been "file an item", and backlog `f1sw71`'s own dependency note says routing obligations to items and plans "works today". SECOND, AND STRONGER, ADJACENT WORK HAS ALREADY PRICED THIS GAP AND DECIDED TO SHIP AROUND IT: `jxxec8` deliberately implements the "minimum useful version" of a graduation guard, declining the already-implemented verdict precisely because this tracking does not exist, and says so IN ITS OUTPUT; `iuxtjy` and `y9s4vm` do the same. Three reviewed plans concluding "advisory is enough, we do not need the verdict" is real evidence that whole-artifact status has been survivable. Whether it is evidence of sufficiency or of accumulated workaround cost is the judgement E-04 must make explicitly.
  QUANTIFY WHERE POSSIBLE. "An agent spent a turn confirming which requirements shipped" is a cost; "this feels risky" is not.
  - Depends on: none
  - Expected outcome: the concrete cases where per-requirement tracking would have prevented measurable waste, each cited to an artifact or commit, plus the strongest case that it would NOT have helped; a plain statement of which way the evidence points.
  - Execution state: pending

### Task group 2: cost the options honestly, including doing nothing

- [ ] E-03 SPECIFY THE THREE VIABLE OPTIONS AT EQUAL DETAIL, so they can be compared rather than one being a straw man.
  THE OPTIONS, each to be costed against E-01's numbers: (A) DO NOTHING, keeping whole-artifact status and relying on backlog items and plans to carry known gaps, which is what happens today and demonstrably works for `trjfyy`. (B) A CONVENTION PLUS A CHECK RULE: pick one requirement-id form, document it for NEW specs only, and add an `aw check` rule that a spec's requirements are well formed, with no attention view and no computed status. (C) THE FULL MECHANISM: parsed requirement ids, a plan-side declaration of which requirements it implements, validated as resolvable, a "requirements outstanding" view in `aw attention`, and `implemented` computed from coverage.
  COST OPTION A AT THE SAME DETAIL AS THE OTHERS. It is the incumbent and the item's own dependency note observes that routing obligations to backlog items and plans "works today". A comparison that treats doing nothing as a non-option is rigged.
  FOR EACH OPTION STATE: authoring burden per new spec, retrofit burden using E-01's three classes, which of the item's three gaps it actually closes, what it would have caught in E-02's cases, and its failure mode when someone does not follow it. The last is what usually decides: a convention nobody follows is worse than no convention because it produces false confidence.
  ANSWER THE ITEM'S FIVE QUESTIONS EXPLICITLY, one at a time, since they are the item's actual ask: do requirements get stable ids and are they parsed; does a plan declare which requirements it implements and is that validated; does `aw attention` gain a view or is it only an `aw check` rule; is `implemented` computed from coverage; and is the corpus large enough to justify it.
  NOTE WHERE EACH OPTION WOULD TOUCH, without touching it: the enum lives at `attention_contract.py:241-253` and is pinned by `tests/test_attention_contract.py`'s totality tests, so a new status is a contract change; a check rule would register beside `check.from-spec-dangling` (`check_engine.py:123-125`); a new `aw specs` subcommand would be a sibling of `check` (`cli.py:4383`). Say so, and change none of them.
  COST A FOURTH OPTION THE PLAN OMITS, because the measurement points at it and a three-option set that excludes it is not the honest option space (review PR-307). OPTION B-MINUS: adopt an addressing convention for NEW specs and add NO check rule at all, relying on review to enforce it the way spec structure is already enforced. This is materially cheaper than B (no rule to write, no corpus to keep green, no CI surface) and closes gap 1 for everything authored from now on, which is where the maintainer's stated pain actually recurs. It also sidesteps the failure mode that usually decides these questions: a check rule over a corpus with 14 non-conforming specs must either exempt them (so the rule proves little) or fail closed (so it blocks unrelated work until 6 live specs are retrofitted). State that trade explicitly.
  ALSO CONSIDER REUSING NUMBERED SECTIONS RATHER THAN MINTING REQUIREMENT IDS. `25kzda` has 47 numbered sections already cited in prose and in code comments, so for section-structured specs the addressing scheme exists and needs only to be declared canonical. An option that adopts what the corpus already does is cheaper than one that asks 6 live specs to adopt something new, and E-01 is now required to report which specs are section-addressable.
  - Depends on: E-01, E-02
  - Expected outcome: three options specified at equal detail, each costed against the measured retrofit classes, each mapped to the gaps it closes and the E-02 cases it would have caught, each with its failure mode; the item's five questions answered explicitly.
  - Execution state: pending

- [ ] E-04 STATE A RECOMMENDATION WITH ITS REASONING AND ITS STRONGEST COUNTERARGUMENT, and make clear the decision is the maintainer's.
  RECOMMEND ONE OPTION PLAINLY. A survey that ends "here are three options, you decide" hands back the same undecided question the item already contains and wastes the measurement.
  INCLUDE THE STRONGEST ARGUMENT AGAINST THE RECOMMENDATION, sourced from the evidence rather than invented for balance. If E-01 shows 13 specs need ids invented, that is a real argument against option C regardless of its benefits.
  BE EXPLICIT THAT THE CHOICE IS THE MAINTAINER'S. This is a cost/benefit and authoring-burden judgment on a feature its requester was openly unsure about, which makes it exactly the class of decision an agent must not settle. Recommend; do not decide.
  IF THE RECOMMENDATION IS "DO NOTHING", SAY SO WITHOUT HEDGING. That is a legitimate and possibly correct outcome, and the item should then be closed as decided rather than left open forever.
  - Depends on: E-03
  - Expected outcome: one recommended option stated plainly, its reasoning tied to E-01 and E-02 evidence, its strongest counterargument stated, and an explicit statement that the maintainer decides.
  - Execution state: pending

### Task group 3: make the work durable

- [ ] E-05 IMMORTALIZE THE SURVEY AS A RESEARCH RECORD, because a decision input that lives only in a plan body is lost once the plan is filed under `executed/`.
  USE THE TOOLING, NEVER HAND-NAME. `aw research new` derives the conforming name and starter front matter and is dry-run by default; `aw research index --apply` maintains the manifest. Hand-naming a research file or hand-editing the index is explicitly forbidden.
  THE RECORD MUST CONTAIN THE REPRODUCIBLE MEASUREMENT, not just conclusions: the exact commands, the per-spec table, and the date and HEAD measured at. A corpus survey dates immediately, and a future reader needs to know whether to re-run it or trust it.
  NOTE THAT NO RESEARCH RECORD ON THIS TOPIC EXISTS. I checked: the only near-hit discusses per-requirement disposition in a worker/verifier RUN protocol, which is a different subject. So this is a genuine addition rather than a duplicate.
  DO NOT DROP ANYTHING INTO `.aw/inbox/`, which is a gitignored raw-drop zone for external material and is not a records tree.
  - Depends on: E-04
  - Expected outcome: a research record created through `aw research new` with the reproducible per-spec survey, the harm cases, the option comparison and the recommendation, plus the manifest refreshed through `aw research index`; no hand-named file and no hand-edited index.
  - Execution state: pending

## Project conventions discovered (Step 0)

- SEVEN INCOMPATIBLE REQUIREMENT CONVENTIONS EXIST AND 14 OF 29 SPECS HAVE NONE, of which 8 are TERMINAL so the live retrofit burden is 6. Re-measured at review: `- G<n> \`[Must]\`` (7 specs, 46 ids), `- R<n> (MUST)` (3, 19), `- **R-<n>**` (1, 16), `- R<n>.` (1, 23), `| I-<n> |` (1, 15), and the SEVENTH the plan's method could not see, BARE LINE-START `R<n>.<n>` (1 spec, `7ckptx`, 45 ids). Roughly 174 parsable ids in total. The plan's "six conventions, 13 of 28" is superseded; treat the whole tally as a datum to re-derive, not to cite.
- THE SURVEY METHOD IS THE FINDING, NOT JUST THE COUNT. All six of the plan's greps were anchored on a leading `- ` bullet, so a spec using bare line-start ids was invisible to every one of them, and the tree's most systematically id'd spec was filed as prose-only. Enumerate id forms FROM the files; never test the corpus against a pre-guessed list.
- NUMBERED SECTIONS ARE AN ADDRESSING SCHEME ALREADY IN USE. `25kzda` carries 47 numbered sections and is cited in prose and in code comments as "spec 2.1's 0..10 bound" (`run_recovery.py:64`). Any convention decision should consider adopting this rather than minting parallel ids.
- 19 OF 29 SPECS CARRY NO `- Id:` AT ALL (3 of them non-terminal). The plan's "three specs" counts only the live ones; the corpus figure is what decides whether an id6-keyed join is viable historically.
- THE SPEC STATUS ENUM IS A PINNED CONTRACT. `SPEC_STATUSES` (`attention_contract.py:241-253`) is nine values, deliberately single-defined "so the coverage test diffs one symbol per tree", and `tests/test_attention_contract.py` asserts totality. Adding a status is a contract change, not a code tweak.
- THE CODE ALREADY ADMITS GAP 3 IN WRITING. `APPROVAL_FLOOR` (`attention_contract.py:453-463`) states `aw specs` enforces "presence + format + resolvability, NOT semantic verification that the work truly happened". Quote it rather than re-deriving it.
- `From-Backlog` AND `From-Spec` ARE WHOLE-ARTIFACT LINKS. `check.from-spec-dangling` (`check_engine.py:123-125`) is a literal id6 set-membership test. There is no requirement-level join key, and that absence is exactly gap 1.
- SIX PENDING PLANS ALREADY EXCLUDE THIS ITEM BY ID (`rnkqrc`, `m867ox`, `y9s4vm`, `jxxec8`, `iuxtjy`, `st5klo`). Consume their reasoning; do not re-derive it.
- THE `specdirs` SET IS A COLLISION RISK FOR THIS PLAN TOO, not only for a future implementation (review PR-309). `1bdxcp` (`to-review`) declares `.aw/records/specs` in scope and MOVES every spec into a status subdirectory. This plan's deliverable is a per-spec table keyed by PATH, so if `1bdxcp` lands first or concurrently, every path in the survey is stale on arrival. Two consequences E-01 must honor: enumerate specs RECURSIVELY (`find .aw/records/specs -name '*.spec.md'`, not a flat glob), and record the id6 or filename stem beside every path so the table survives the move. Note `y4bdoz` (`reviewed`) exists precisely because a non-recursive spec reader goes blind in that world.
- RESEARCH RECORDS ARE TOOL-MANAGED. `aw research new` and `aw research index` own naming and the manifest; hand-naming is forbidden.
- Suite bare: `python3 -m pytest`. Measure the baseline in the executing worktree and compare failing NODE IDS, never totals. THE AUTHORED BASELINE IS WRONG IN BOTH HALVES: re-measured at review, main gives `1 failed, 5958 passed, 3 skipped, 2 xfailed`, and the named `tests/test_orchestrator_retirement.py` PASSES (`112 passed`). The one real failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, tripping over 189 files in a GITIGNORED `opencode-recovery/` directory owned by another party. NEVER delete or modify that directory to green the suite. This plan changes no code, so its own expectation is a NO-DELTA run.
- `aw research index --check` IS ALREADY RED AT HEAD: exit 1, 49 pre-existing findings unrelated to this plan. It is a DELTA gate here, never a pass/fail gate. `aw specs check` does pass (exit 0).

## Findings

| Id | Severity | Location (measured at HEAD `fac69fbd`) | Finding | Evidence |
| --- | --- | --- | --- | --- |
| F-1 | HIGH | `.aw/records/specs/*.spec.md` (**29** files) | NO SINGLE PARSABLE REQUIREMENT CONVENTION EXISTS, and the count is worse than six: **SEVEN** forms are in use and **14 of 29** specs match none, including `25kzda`, the release-gating run spec. A parser cannot simply be pointed at the corpus. CORRECTED AT REVIEW in three ways, see F-13/F-14/F-15. | re-enumerated at review |
| F-13 | HIGH | `7ckptx`; the plan's own six greps | **THE SURVEY METHOD MISSED A CONVENTION AND SO MISCLASSIFIED THE LARGEST REQUIREMENT SET IN THE TREE.** All six greps were anchored on a leading `- ` bullet, so `7ckptx`'s 45 BARE LINE-START requirements (`R1.1 For an isolated turn...`) were invisible and it was filed as prose-only. It is in fact the most systematically id'd spec in the corpus, with 122 `R<n>.<n>` prose references. The parsable id total rises from ~64 to ~174. A fixed grep list cannot measure convention diversity. | `grep -cE '^R[0-9]+(\.[0-9]+)* '` -> 45 |
| F-14 | HIGH | 14 prose-only specs by `- Status:` | **THE RETROFIT COST IS OVERSTATED ~2x BECAUSE IT COUNTS DEAD SPECS.** Of the 14 prose-only specs, **8 are terminal** (7 `implemented`, 1 `superseded`), which nobody retrofits. The LIVE retrofit burden is **6 specs**, not 13 or 14. This materially weakens the plan's own central argument against option C, so it must be corrected before the maintainer weighs it. | status parse per prose-only spec |
| F-15 | MED | `25kzda` (47 numbered sections) | **A REAL ADDRESSING SCHEME ALREADY EXISTS AND THE SURVEY IGNORES IT.** `25kzda` is routinely cited by section ("`25kzda` 2.9", and `run_recovery.py:64` says "spec 2.1's 0..10 bound"), so its text IS precisely addressable without requirement ids. Concluding "unaddressable" about it is wrong, and adopting what the corpus already does is cheaper than asking 6 live specs to adopt something new. | section count; citation grep |
| F-2 | N/A | `attention_contract.py:262-272` | SPECS ARE VISIBLE AT WHOLE-ARTIFACT LEVEL: `_SPEC_MAP` maps `approved` to `ready` and `implementing` to `active`, so a spec awaiting implementation does surface. The gap is granularity, not visibility. | source read |
| F-3 | N/A | `attention_contract.py:241-253` | NO PARTIAL-IMPLEMENTATION STATE. `SPEC_STATUSES` is nine values and none expresses "6 of 9 requirements built". Pinned by `tests/test_attention_contract.py` totality tests, so any addition is a contract change. | source read |
| F-4 | N/A | `agent_workflows/specs.py` (1066 lines), `validate_spec:211-326` | NO REQUIREMENT MODEL. Grepping for `requirement|R[0-9]|partial` yields two incidental hits (`:1048`, `:1063`), both about evidence resolution; `validate_spec` checks status, gate, history, priority, work-kind and nothing else. Corrects the item's stale 1006-line and `:988,:1003` citations. | grep, source read |
| F-5 | N/A | `attention_contract.py:453-463`, `check_engine.py:123-125` | `implemented` IS NOT SEMANTICALLY VERIFIED, and the code says so about itself; `check.from-spec-dangling` validates only that an id6 resolves to a spec, never that requirements are covered. | source read |
| F-6 | MED | spec `c4gd2h` | THE SHARPEST CASE IS LIVE: `implementing` since 2026-08-30 with ONE history line, `- Blocks-Release: next`, and 23 `- R<n>.` requirement bullets that nothing parses. Its uncertainty was hit directly while filing `1m3nul`. | spec read; that item's text |
| F-7 | MED | `25kzda` §1.1 and `:586` (**NOT** §2.1), `run_recovery.py` | ONE REQUIREMENT IN THREE IMPLEMENTATION STATES, substance verified: `validate_retry_budget` IS called (`runner_shared.py:2103`), while `plan_retry` and `retry_budget_remaining` have zero callers outside their own module. **BUT THE SECTION CITATION IS WRONG**: §2.1 is "Command grammar". See F-16. Tracked by `trjfyy` -> plan `xipfy1`. | call-site grep; section read |
| F-16 | MED | this plan's own text; `run_recovery.py:64` | **THE PLAN MISCITES A SPEC REQUIREMENT BY SECTION NUMBER, WHICH IS ITSELF ITS BEST EVIDENCE.** It says "`25kzda` 2.1's retry budget" four times; §2.1 is the command grammar. The code ALSO says "spec 2.1's 0..10 bound", so spec and code disagree about the number. A plan about requirement addressability, misaddressing a requirement, is the drift it argues for tracking, observed in itself. | §2.1 read; comment read |
| F-8 | MED | corpus tally | THE ITEM'S MEASUREMENT IS STALE AND SO IS THE PLAN'S: **29** specs at review, not 28 or 27, with **7** `approved`, not 6 or 5. It has moved twice in three days, so every corpus number here is a datum to re-derive. | parsed `^- Status:` over 29 files |
| F-9 | MED | **19** specs (3 non-terminal) | **19 OF 29 SPECS CARRY NO `- Id:` AT ALL**, not three. The plan's "three" counts only the non-terminal ones (one `approved`, two `deferred`), which are what block a join key today; the corpus figure is what decides whether an id6-keyed requirement scheme is viable over historical specs at all. | front-matter grep over all 29 |
| F-10 | N/A | six pending plans | NOTHING COVERS THIS AND ADJACENT WORK KNOWS IT: `rnkqrc`, `m867ox`, `y9s4vm`, `jxxec8`, `iuxtjy` and `st5klo` each name `f1sw71` and exclude it, two as numbered findings. No plan carries `- From-Backlog: f1sw71`. | grep across pending plans |
| F-11 | LOW | `.aw/records/research/` | NO RESEARCH RECORD ON THIS TOPIC EXISTS. The only near-hit discusses per-requirement disposition in a worker/verifier run protocol, a different subject. So E-05 adds rather than duplicates. | grep across 63 research entries |
| F-12 | LOW | `aw specs --help` | NO HOST FOR A REQUIREMENTS VIEW: the subcommands are `new`, `set`, `note`, `check`, `migrate`. A view would be a new subcommand, a sibling of `check`. | ran `--help` |
| F-17 | HIGH | `aw research index --check`; `python3 -m pytest` | **BOTH NAMED VALIDATION GATES WERE MEASURED WRONG, AND ONE INVITES DESTRUCTION.** `aw research index --check` is ALREADY RED at HEAD (exit 1, 49 pre-existing findings), so requiring it to prove consistency is unmeetable; it must be a DELTA gate. And the suite baseline is wrong in both halves: actual is `1 failed, 5958 passed`, the named `test_orchestrator_retirement.py` PASSES, and the real failure walks a gitignored `opencode-recovery/` directory of 189 files belonging to another party, which an executor could "clean up" and destroy a co-worker's work. | both commands run at review |
| F-18 | MED | `1bdxcp` (`to-review`), `y4bdoz` (`reviewed`) | **THE `specdirs` SET COLLIDES WITH THIS PLAN, not only with a future implementation.** `1bdxcp` declares `.aw/records/specs` in scope and MOVES every spec into a status subdirectory, so a path-keyed per-spec table is stale on arrival. E-01 must enumerate RECURSIVELY and key rows by id6/stem, not path. | scope-path read |
| F-19 | MED | `trjfyy`/`xipfy1`; `jxxec8`, `iuxtjy`, `y9s4vm` | **THE DISCONFIRMING EVIDENCE E-02 DEMANDS ALREADY EXISTS AND THE PLAN SUPPLIES NONE.** The backlog workaround demonstrably works (`trjfyy` -> `xipfy1`), and THREE reviewed plans priced this exact gap and chose to ship advisory-only around it, saying so in their own output. A plan that asks for a disconfirming case without naming the strongest one leaves its most important instruction to executor initiative. | those plans read |

## Proposed changes (ordered, validatable)

1. E-01 surveys every spec (29 at review, re-derive), DERIVING the convention list from the files rather than testing against a fixed grep list, classifying each into parsable-as-is, needs-reconciliation, or needs-ids-invented, SPLIT BY LIFECYCLE STATE so the live retrofit burden is the headline, with requirement counts, section-addressability noted, and reproducible commands.
2. E-02 measures the harm per-requirement tracking would actually have prevented, engaging the two already-identified disconfirming lines, and reporting this plan's own miscitation as evidence.
3. E-03 specifies FOUR options (do nothing, convention-only, convention plus check rule, full mechanism) at equal detail and answers the item's five questions explicitly.
4. E-04 states one recommendation with its reasoning and strongest counterargument, and that the maintainer decides.
5. E-05 immortalizes the survey as a research record through `aw research new` and refreshes the manifest.

## Deferred / out of scope (with reason)

- IMPLEMENTING ANY REQUIREMENT MODEL, PARSER, STATUS, FIELD, CHECK RULE OR ATTENTION VIEW. The whole point: the item is an open question whose author said "definitely not sure", and it lists five things that must be decided "before any implementation". Building any of it here would answer those five questions on the maintainer's behalf and impose authoring burden on every future spec from an agent's guess.
- EDITING ANY SPEC'S REQUIREMENT TEXT, including retrofitting ids or normalizing conventions. That IS the retrofit whose cost this plan measures. Doing it while measuring it would be implementing the undecided option.
- ADDING A SPEC STATUS FOR PARTIAL IMPLEMENTATION. `SPEC_STATUSES` (`attention_contract.py:241-253`) is a pinned single-defined contract with totality tests. It may be an outcome of the decision; it is not a step in making it.
- MAKING `implemented` COMPUTED FROM COVERAGE. Question 4 of the item's five. Answered in E-03, not built.
- THE `specdirs` REORGANIZATION. Independent work (`wfjsp4`, `y4bdoz`, `1bdxcp`) that moves specs into status subdirectories and declares `specs.py` in scope. Untouched here. NOT merely a future risk (F-18): it invalidates a path-keyed survey, so E-01 must enumerate recursively and key rows by id6/stem.
- FIXING THE SPECS WITH NO `- Id:` (F-9), of which there are **19**, three non-terminal. A real defect for id6 selectors and worth its own item, but not this plan's concern; flagged for the maintainer rather than swept in. Minting ids for grandfathered legacy specs is a records change the maintainer must authorize.
- REPAIRING THE 49 PRE-EXISTING `aw research index --check` FINDINGS (F-17). They belong to other artifacts and predate this plan; fixing them here would be a large undeclared scope expansion. E-05 proves only that its own record adds no NEW finding.
- CLEANING UP `opencode-recovery/` TO GREEN THE SUITE (F-17). It is gitignored, holds 189 files belonging to another party in a shared checkout, and is the sole cause of the one baseline failure. Deleting it would destroy a co-worker's work.

## Scope check

- Over-scope: none. The only declared path is `.aw/records/research`, written by E-05. E-01 through E-04 are analysis whose output lands in that record and in this plan's own body.
- Under-scope: stated rather than left as `none`. Deliberately, NOTHING in `agent_workflows/` changes and no spec is edited, so after this plan the three gaps still exist exactly as described. That is the intended end state: the deliverable is a decision, not a mechanism. If the maintainer accepts a recommendation other than "do nothing", the implementation is a SEPARATE plan and this one must not be read as having delivered it.

## Required tests / validation

THIS PLAN CHANGES NO CODE, so the primary validation is EVIDENCE QUALITY, not a green suite. Each `V-*` demands the measurement be reproducible: pasted commands with their actual output, not summaries. Still run `python3 -m pytest` bare in the executing worktree and show a NO-DELTA result against the worktree baseline, comparing failing node ids, since a plan claiming to change no code must prove it. Run `aw specs check` (expected: all specs conform; verified at review, exit 0). Any count in the deliverable must be reproducible by a pasted command; a number I cannot re-derive is not evidence.

TWO NAMED GATES WERE MEASURED WRONG AND ARE CORRECTED HERE (review PR-308), because an executor who trusted either would either chase a phantom failure or destroy a co-worker's files.

FIRST, `aw research index --check` DOES NOT PASS AT HEAD AND IS NOT A CLEAN GATE. Measured at review: exit 1 with **49** pre-existing findings (`dangling-citation`, `stale-state-to-promote`, `adopted-without-consumer`), none of them caused by this plan. So requiring it to "prove the manifest is consistent" sets an unmeetable bar. The correct instruction: run it BEFORE and AFTER E-05 and prove your record adds NO NEW finding, comparing the finding sets rather than the exit code. Do NOT attempt to fix the 49; they are other artifacts' problems and repairing them here would be a large undeclared scope expansion.

SECOND, THE SUITE BASELINE IS WRONG IN BOTH ITS COUNT AND ITS NAMED TEST, AND THE REAL FAILURE INVITES A DESTRUCTIVE "FIX". The plan states `1 failed, 5648 passed` with the failure being `tests/test_orchestrator_retirement.py`. Re-measured bare at review: `1 failed, 5958 passed, 3 skipped, 2 xfailed`, and that file PASSES outright (`112 passed`). The single real failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, which walks the repo and trips over 189 files in a GITIGNORED `opencode-recovery/` directory belonging to another party. An executor told to expect a different failure could "clean up" that directory to make the suite green, destroying a co-worker's work in a shared checkout. DO NOT DELETE OR MODIFY `opencode-recovery/`. Measure your own baseline in your own worktree and judge on the node-id delta.

## Spec / documentation sync

NO SPEC IS AMENDED AND NONE MAY BE. This plan decides whether a mechanism should exist; it changes no behavior any spec describes, and it must not edit a spec's requirement text even to make it parsable (that is the retrofit being costed). That is the justification to record.
THE DECISION'S CONSEQUENCE FOR SPEC AUTHORING IS THE POINT, and if the maintainer accepts option B or C, the FOLLOW-ON plan will need to amend whichever spec governs spec authoring, declaring that file in its `- Scope-Paths:` so the runners announce the edit at run start. Identify that spec in the recommendation so the follow-on does not have to rediscover it, and note whether the requirement-id convention would belong there or in `.aw/records/specs/README.md`.
DO NOT EDIT `.aw/records/specs/README.md` HERE either: documenting a convention is implementing option B.

## Open questions

### OQ-01: Should this plan recommend, or only present options?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: RESOLVED as RECOMMEND, with the decision explicitly left to the maintainer. Presenting three costed options and stopping would hand back the same undecided question the item already contains, wasting the measurement: the maintainer's stated difficulty is not a lack of options but not knowing whether it is worth it. A recommendation with its strongest counterargument is more useful and still leaves the choice open, which is the line an agent must respect on a cost/benefit and authoring-burden judgment.

### OQ-02: If the recommendation is a convention, does it apply retroactively?

- Blocking: no
- Status: open
- Owner: the maintainer for the scope call, this plan's executor for the recommendation
- Resolution or deferral rationale: NOT blocking, because E-01 measures the retrofit cost either way and E-03 must cost both variants. It is recorded because it is where the cost is concentrated. REVISED AT REVIEW, AND THE REVISION CUTS THE STAKES ROUGHLY IN HALF: the plan wrote "13 of 28 specs would need ids INVENTED", but 8 of the prose-only specs are TERMINAL and nobody retrofits a finished spec, so the real retroactive burden is **6 live specs** (F-14). A going-forward-only convention is nearly free but leaves those 6 outside the mechanism, and two of them are exactly where the motivating pain is (`25kzda`, release-gating, and the `deferred` pair). Note also that `25kzda` is already section-addressable (F-15), so for it "retrofit" may mean declaring an existing scheme canonical rather than inventing ids. Six specs, one of which needs no invention, is a materially different decision from thirteen, and the recommendation must be made against the corrected figure.

### OQ-03: Should the three specs with no `- Id:` be fixed as part of any requirement work?

- Blocking: no
- Status: open
- Owner: the maintainer
- Resolution or deferral rationale: NOT blocking and deliberately not fixed here. RE-SCOPED AT REVIEW (F-9): it is not three specs but **19 of 29** that carry no `- Id:`, of which three are non-terminal (one `approved`, two `deferred`). The three live ones are what block a `<spec-id6>.<req-id>` join key today and are the immediate prerequisite for option C; the 19 are what tell the maintainer whether an id6-keyed scheme can ever address the HISTORICAL corpus, which matters if the point is to ask "what in our specs is still unbuilt" across everything rather than only new work. Still a standalone defect worth its own item, and still not fixed here, because pre-cutover legacy spec names are grandfathered and minting ids for them is a records change the maintainer must authorize. E-01 reports both figures so the decision is made against the real scope.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: paste the per-spec table covering ALL specs in the tree, with each spec's convention, requirement count, retrofit class, LIFECYCLE STATE, and id6-or-stem key (not only a path, per F-18). Paste the exact commands used, with their raw output, so a reader can re-derive the counts; a summary without commands is not evidence. Show the enumeration was RECURSIVE (`find`, not a flat glob). State the total spec count and the count in each of the three retrofit classes, and confirm the three counts sum to the total. STATE THE LIVE RETROFIT BURDEN SEPARATELY from the raw prose-only count, with terminal specs excluded, and say which figure you treat as decisive. Show HOW you derived the convention list from the files, and name any form the review's seven did not include, or confirm you found none beyond them. Paste the requirement total for the parsable set. Paste the count of specs carrying no `- Id:` for the WHOLE corpus and for non-terminal specs only. Note which specs are section-addressable. Confirm by `git status` that no spec file was modified.
  - Observed evidence:
  - Result: pending

- [ ] V-02 validates E-02
  - Required evidence: for each harm case, paste the artifact or commit evidence, not a description: for `c4gd2h` its `- Status:`, its history lines and its requirement count; for the retry budget the call-site grep showing `validate_retry_budget` called and `plan_retry`/`retry_budget_remaining` with zero callers outside their module, PLUS the correct section anchor in `25kzda` (§2.1 is the command grammar, so state where the requirement actually lives and whether the spec or `run_recovery.py:64`'s "spec 2.1" comment is wrong). Report that miscitation as a harm datum in its own right. Paste BOTH already-identified disconfirming lines with their evidence (the `trjfyy` -> `xipfy1` workaround, and the three reviewed plans that priced this gap and shipped advisory-only), plus any stronger one you find. State plainly which way the evidence points, including if it points at "do nothing".
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: paste the FOUR options at equal detail, each with authoring burden, retrofit burden expressed in E-01's classes AND against the live-only figure, which of the three gaps it closes, which E-02 cases it would have caught, and its failure mode. Paste the item's five questions each with its explicit answer. Confirm option A (do nothing) is costed at the same detail as the others, and that the convention-only option (B-minus) is present rather than collapsed into B. State the check-rule dilemma explicitly: a rule over a corpus with non-conforming live specs must either exempt them or block unrelated work. Confirm by `git status` that nothing in `agent_workflows/` was modified.
  - Observed evidence:
  - Result: pending

- [ ] V-04 validates E-04
  - Required evidence: paste the recommendation, its reasoning tied to specific E-01 and E-02 evidence, and its strongest counterargument. Confirm the reasoning uses the CORRECTED live retrofit figure rather than the raw prose-only count, since the two differ by roughly 2x and the plan's original argument against option C rested on the inflated one. Paste the explicit statement that the maintainer decides. If the recommendation is "do nothing", confirm it is stated without hedging and say what should then happen to backlog `f1sw71`.
  - Observed evidence:
  - Result: pending

- [ ] V-05 validates E-05
  - Required evidence: paste the `aw research new` invocation and the path it derived, proving the file was tool-named rather than hand-named. Paste the record's measurement section showing the reproducible commands and the HEAD and date measured at. Paste `aw research index --check` BEFORE and AFTER, and prove your record adds NO NEW finding by comparing the finding SETS: it is already red at HEAD (49 findings at review), so a bare exit code proves nothing and a green run is not achievable. Confirm you repaired none of the pre-existing 49. Paste `python3 -m pytest` bare with the worktree baseline beside it and a node-id comparison showing NO DELTA, stating the counts YOU observe (the plan's `1 failed, 5648 passed` and its named failing test are both wrong; expect the `opencode-recovery/` reporting-contract failure instead). Confirm you did not delete or modify `opencode-recovery/`. Paste `aw specs check`. Paste `git diff --cached --name-only` showing only research-tree paths and this plan.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

Scope fence: touch ONLY `.aw/records/research` (plus this plan's own file). Do NOT modify anything in `agent_workflows/`. Do NOT edit any spec, including adding requirement ids or normalizing a convention: that is the retrofit being costed, not a step in costing it. Do NOT add a spec status, a front-matter field, an `aw check` rule, an `aw specs` subcommand, or an `aw attention` view. Do NOT edit `.aw/records/specs/README.md`. Do NOT touch the `specdirs` Set's files. Do NOT hand-name the research file or hand-edit its index; use `aw research new` and `aw research index`. Do NOT drop anything into `.aw/inbox/`. If the work GENUINELY requires a path outside the fence, MAKE THE EDIT AND JUSTIFY IT, since `aw ipd finalize` refuses to complete until every out-of-scope path carries a `--scope-reason` and every declared-but-unmodified path carries a `--scope-ack`.

RE-LOCATE BY SYMBOL, NEVER BY THE LINE NUMBERS IN THIS PLAN, and RE-MEASURE THE CORPUS rather than reusing my counts. Find `SPEC_STATUSES`, `_SPEC_MAP`, `APPROVAL_FLOOR`, `validate_spec` and `check_from_spec_dangling` by name. The spec count and status tally WILL have moved: they moved from 27/5 at the item's filing, to 28/6 at authoring, to **29/7** at review, three figures in a few days, so treat every number here as a datum to reproduce and none as input.

AND DO NOT INHERIT THIS PLAN'S SURVEY METHOD, which review found to be the source of its worst errors. Six greps anchored on a leading bullet missed an entire convention and misclassified the tree's most systematically id'd spec as prose-only; counting terminal specs as retrofit work inflated the decisive cost figure roughly 2x; and the `- Id:`-less count was low by a factor of six. Derive conventions FROM the files, split every cost by lifecycle state, and enumerate recursively so the `specdirs` migration cannot invalidate the table.

THIS PLAN'S OUTPUT IS A RECOMMENDATION, NOT A DECISION. The maintainer chooses. Do not write an approval or readiness attestation for the design, and do not treat a recommendation as license to start implementing it in a later E-item.

Execution contract: commit ONLY the files this plan changed, path-scoped (`git commit -m msg -- <paths>`); never `git add -A`, never `-a`, never push. Verify the staged set with `git diff --cached --name-only` before every commit and RE-VERIFY after any failed or hook-interrupted commit, since `pre-commit` can restore another agent's paths into the index. Paste ACTUAL runner output when reporting tests passed; never claim a suite result you did not run.

Post-gate lifecycle: requires explicit human approval (`aw ipd set approved si24ia --by-human --message ...`) before execution. Do NOT hand-write a `Readiness:` field: that is `/plan-review`'s attested output. Transition via `aw ipd finalize` after `aw ipd lint --phase pre-transition` conforms and every `V-*` carries pasted evidence. ON COMPLETION, backlog `f1sw71` (carried as `- From-Backlog:`) should be set according to the outcome: `done` if the recommendation is "do nothing" and the maintainer accepts it, since the open question is then answered and closed; `graduated` if the maintainer accepts a mechanism, because the implementation is then a separate plan and the item's substance survives. Do NOT close it `done` on the strength of a recommendation the maintainer has not accepted. That item carries no release gate, so none is inherited.
