# IPD: Require symbol and content anchors in IPD citations, because a file:line reference expires silently and misdirects an executor to unrelated valid code

- Date: 2026-09-18
- Kind: child
- Concern: An IPD citation of the form `somefile.py:897-905` is true only at the instant it is written, and the plan pipeline is deliberately longer than that instant. A plan is authored, reviewed (often twice), approved, and then executed days later, while other agents commit to the same files concurrently in a shared checkout. By execution time the line numbers name different code.
  THE FAILURE IS SILENT MISDIRECTION, NOT A DANGLING POINTER, AND THAT IS THE WHOLE POINT. A drifted line number almost never points at nothing; it points at OTHER, VALID, PLAUSIBLE-LOOKING code. A dangling reference announces itself and costs an executor one grep. A drifted one does not announce itself, so the executor reads the wrong construct, believes the plan described it, and reasons from there. MEASURED on plan `216rgg` at HEAD `d445e6e6` while preparing to execute it: it cites the cross-type emission at `check_engine.py:897-905`, and what is actually at those lines today is the `seen_ids` dictionary initialization plus a comment about identity slots. The real emission (the branch whose message contains `different type`) is roughly 44 lines further down. Its other three anchors have drifted the same way, and its `doctor.py:530` citation now lands on a BLANK LINE.
  THE CORPUS IS SATURATED WITH THESE. MEASURED at the same HEAD: **96 of 101** pending plans (95%) carry file:line citations, **2816** of them in total, the densest single plan carrying 102. So this is a property of the authoring convention, not a lapse by one author.
  EVERY COUNT IN THIS PLAN IS A DATED SNAPSHOT OF A MOVING CORPUS, NOT A LIVE INVARIANT. RE-MEASURED at HEAD `8087387e` (after `216rgg` and `di08i9` executed and merged): **94 of 99** pending plans (94%), **2771** citations. The SHAPE is what must survive re-measurement, and it did: saturation stayed at roughly 95% while the absolute numbers moved within hours. An implementer must RE-DERIVE these and report the denominator rather than quoting this plan, which is the same discipline `2lcqno` Section 4 cost 4 imposes for the same reason.
  THE OBVIOUS METRIC UNDERSTATES THE PROBLEM AND MUST NOT BE USED AS THE MEASURE OF IT. Scanning the same 2816 citations for ones that are PROVABLY dead (the file no longer exists, or the line number exceeds the file's length) finds only **8**, which is 0%. That near-zero is not reassurance, it is the symptom restated: a line number stays syntactically valid while becoming semantically wrong, which is exactly why the corpus looks healthy and reads wrong. Any validation built for this defect must therefore key on the ANCHOR'S FORM, never on whether the target resolves.
  RE-MEASURED AT REVIEW at HEAD `7562ca6c`, and the SHAPE held again while every number moved: **90 of 93** pending plans (96%), **2802** citations by the front-matter-inclusive scan and **2773** through the fence-aware structural view, densest plan still 102. The provably-dead count re-derived as **132 of 2764** (4%) under a resolver that also searches `agent_workflows/` and `tests/` for a bare filename; the original 0% and this 4% are both small, and the conclusion is unchanged, but an implementer must state WHICH resolution rule produced the number rather than inheriting a bare percentage.
  REVIEW CANNOT FIX THIS, WHICH IS WHY THE CONTRACT MUST CHANGE. `216rgg` was reviewed twice and its own history records that "every line number and count in this plan was MEASURED at HEAD rather than carried from the spec". That claim was TRUE when written. Verification cannot preserve a reference type that expires by construction, so more diligence is not the remedy and asking for it would be the wrong conclusion to draw.
- Scope: The IPD authoring contract's treatment of code citations, the scaffold guidance an author starts from, and a lint diagnostic that nudges new plans toward durable anchors. IN: stating the anchor rule in the IPD spec; emitting it in `aw ipd scaffold`'s `## Project conventions discovered (Step 0)` guidance so an author meets it before writing findings; adding ONE advisory `aw ipd lint` diagnostic at `info` that reports a bare file:line anchor in a plan being authored, with a `--legacy`-style exemption for plans predating the rule. OUT: retrofitting the 2816 existing citations (see Deferred, and note this is the single largest thing this plan deliberately does not do); any change to `check_engine`'s rules; any change to how REVIEWS cite code; forbidding line numbers outright.
- Scope-Paths: agent_workflows/ipd_lint.py, agent_workflows/ipd_authoring.py, tests/test_ipd_lint.py, tests/test_ipd_templates.py, .aw/system/workflows/assess/templates/ipd.md, .aw/records/specs/20260802-1904-01-ipd-structure-and-linting.spec.md
- Item-Dependencies: none
- Status: approved
- Readiness: go-pending-approval
- Set: citeanchor
- Order: 1
- Highest E allocated: 06
- Author: opencode/its_direct/pt3-claude-opus-5-1m-us
- Id: mzc019
- Approval: 2026-09-19, recorded via aw ipd set: status set to approved

## Workflow history
- 2026-09-19 approved (aw set): status set to approved

- 2026-09-19 reviewed (opencode/its_direct/pt3-claude-opus-5-1m-us): /plan-review round 1: APPROVE WITH REVISIONS APPLIED; PR-001..PR-008 all FIXED, none deferred, none open; Readiness go-pending-approval. Reviewed at HEAD `7562ca6c`; plan byte-identical to the lane input, so no pre-review snapshot. THE DIAGNOSIS IS CORRECT AND THE MECHANISM WAS NOT: three of the four surfaces this plan named behave differently from how it described them, each measured rather than reasoned about. (1) The detector as specified could not have flagged this plan's OWN headline evidence: 99% of the corpus's citations already sit beside a backticked token and the drifted `216rgg` anchors are backticked themselves, so the test scored them anchored; the corrected form measures 20% and flags them. (2) The scaffold body lives in `ipd_authoring`, not `ipd_schema`, and editing it breaks a byte-parity pin on the shipped template (failure reproduced). (3) An advisory prints no code and no message without `--detail`, so the nudge reached nobody; added E-05. Also corrected the `Scope-Paths` fence (three paths missing, one spurious), a SELF-carrier that resolves today and vanishes at execution (added E-06), and a V-01 demand for a spec status transition that `implemented -> *` refuses. Watermark 04 -> 06. Full record: `.aw/records/reviews/20260918-citeanchor-01-mzc019-require-symbol-and-content-anchors-in-ipd-citations-because.review.md`.
- 2026-09-18 to-review (opencode/its_direct/pt3-claude-opus-5-1m-us): Authored at the maintainer's instruction ("we need to stop putting line numbers in plans"), after the drift was measured on `216rgg` while preparing to execute it. Every claim in this plan is anchored by SYMBOL or by CONTENT STRING rather than by line number, which is both the rule it proposes and the only honest way to state it. THE COUNTS TO RE-DERIVE, not to trust: 96/101 plans, 2816 citations, 8 provably dead, and `216rgg`'s four drifted anchors. The maintainer chose a plan over a backlog item.
- 2026-09-18 rebased onto `8087387e` (opencode/its_direct/pt3-claude-opus-5-1m-us): NO content revision, and the plan's own thesis gained a NATURAL EXPERIMENT worth recording. `216rgg` executed and merged in the interim, rewriting the exact region of `check_engine.py` this plan cites as its headline evidence. RESULT: all four symbol anchors used here (`ipd_lint._structural_lines`, `ipd_schema.H_PROJECT_CONVENTIONS`, `check_engine.CARRIER_CUTOVER_DATE`, `check_engine.carrier_severity_for_plan`) still resolve, and the `C_*` constant count is unchanged at 30, so nothing in this plan needed repair. Had those been line numbers they would now be as stale as the ones this plan was written to describe. Counts re-derived and annotated as snapshots (94/99, 2771); `aw ipd lint` still CONFORMING.
- 2026-09-18 draft (opencode/its_direct/pt3-claude-opus-5-1m-us): created.

## Goal

Make a code citation in a NEW IPD point at something that survives the gap between authoring and execution, by naming a symbol or a quoted content string instead of a line number, so an executor who follows a citation arrives at the construct the author meant rather than at whatever code now occupies that offset.

READ THE GOAL PRECISELY: this plan changes what a FUTURE plan is asked to write, and repairs NOTHING that already exists. It is a contract-and-nudge change, not a migration. The 2816 existing citations stay exactly as they are, and Deferred says why.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: state the rule

- [ ] E-01 STATE THE ANCHOR RULE IN THE IPD SPEC, in `.aw/records/specs/20260802-1904-01-ipd-structure-and-linting.spec.md`, as a normative item covering how an IPD cites code. Require, in order of preference: (a) a SYMBOL path (`module.function`, `Class.method`, a `RuleSpec` row keyed by its rule id, a dict key); (b) a QUOTED CONTENT STRING unique enough to grep (for a branch with no symbol of its own, cite the message literal it emits, e.g. the branch whose message contains `different type`); (c) a line number ONLY as a trailing convenience appended to (a) or (b), never as the sole anchor. State the REASON in the spec and not merely the rule, because a rule whose reason is absent gets re-litigated: a symbol survives insertion, deletion, and file moves, while an offset survives none of them, and a drifted offset misdirects rather than failing.
  ALSO STATE THE ONE LEGITIMATE EXCEPTION so the rule is not read as absolute: a citation whose SUBJECT IS THE LINE ITSELF (a lint diagnostic's reported position, a traceback, a diff hunk quoted as evidence) is correctly a line number, because there the offset is the fact being reported rather than a pointer to a construct.
  - Depends on: none
  - Expected outcome: the spec carries the anchor rule, its rationale, its preference order, and the line-as-subject exception; the amendment is declared in this plan's `- Scope-Paths:` and announced by the runner per the spec-amendment contract.
  - Execution state: pending

### Task group 2: put it where an author will meet it

- [ ] E-02 EMIT THE RULE IN THE SCAFFOLD SO AN AUTHOR READS IT BEFORE WRITING FINDINGS, not after review. Add a one-line convention stating the anchor rule and pointing at the spec. KEEP IT TO ONE LINE: the scaffold's value is that it is read, and every line added to a skeleton lowers the odds the next one is.
  THE BODY LIVES IN `ipd_authoring`, NOT `ipd_schema`, AND GETTING THIS WRONG STRANDS THE ITEM. Corrected at review after measurement: `ipd_schema` owns only the HEADING CONSTANT (`H_PROJECT_CONVENTIONS`) and its position in `CHILD_H2_ORDER`; the emitted BODY is the `ipd_authoring._SECTION_BODY` entry keyed by that constant, whose current value is the single bullet `- TODO: relevant conventions discovered during Step 0.`, and `build_skeleton` renders it. Grep evidence: `H_PROJECT_CONVENTIONS` appears in exactly two files, and the only occurrence outside the schema's own declaration is that `_SECTION_BODY` key. So edit `ipd_authoring`.
  THE EDIT IS NOT ONE FILE: IT BREAKS A BYTE-PARITY PIN, AND THE PIN IS THE POINT. `tests/test_ipd_templates.py` asserts `.aw/system/workflows/assess/templates/ipd.md` is BYTE-IDENTICAL to `build_skeleton`'s output ("child template drifted from build_skeleton; regenerate it"). MEASURED THIS REVIEW by adding one convention line to `_SECTION_BODY` in memory and running that one test: it FAILED with exactly that message. So E-02 must REGENERATE the child template in the same change. Do NOT hand-edit the template to match; regenerate it from `build_skeleton` so the generator stays the single source of truth.
  THE ORCHESTRATOR TEMPLATE IS DELIBERATELY UNTOUCHED: `ORCHESTRATOR_H2_ORDER` has no `## Project conventions discovered (Step 0)` heading at all (verified: the string does not appear in `orchestrator-ipd.md`), so its byte-parity pin is unaffected. If a run finds otherwise, regenerate both rather than relaxing either pin.
  DO NOT ADD THE NEW LINE TO `_AUTHORING_PLACEHOLDERS`. That tuple is the anchored set of strings meaning "still a stub", consumed by `authoring_placeholders_resolved` to gate the `check.ipd-draft-ready-to-review` nudge. The convention line is PERMANENT GUIDANCE an author is not expected to delete, so listing it would make every plan look forever-unfinished and silence that nudge. Note the existing conventions placeholder is ALSO absent from that tuple today, so the correct move is to leave the tuple alone entirely.
  WHY THE SCAFFOLD AND NOT ONLY THE SPEC: an author who is writing a Findings table is not reading the IPD spec at that moment, and the measured saturation (re-derived at review: 90 of 93 pending plans, 96%) shows the current default behavior is to reach for a line number. The scaffold is the surface that changes the default.
  - Depends on: E-01
  - Expected outcome: a freshly scaffolded IPD contains the anchor convention in its Step 0 section; the child template is REGENERATED so `tests/test_ipd_templates.py` passes; `aw ipd lint` still reports the scaffold output conforming.
  - Execution state: pending

- [ ] E-03 ADD ONE ADVISORY LINT DIAGNOSTIC AT `info`, NOT AN ERROR, reporting a bare file:line anchor in a plan's prose. Allocate a fresh stable code in the existing namespace (the codes are declared as `C_*` module constants in `ipd_lint`, grouped by area); do not reuse a retired code (`IPD-D701` is retired and MUST NOT be revived). Open a NEW area block rather than extending `IPD-I3xx`, which is the id-family group and has nothing to do with citations.
  THE NAIVE FORM OF THIS TEST IS PROVABLY USELESS HERE, AND CORRECTING IT IS THE SUBSTANCE OF THIS ITEM. As originally written the rule fired only when a citation carried "NO accompanying symbol or quoted content string" on the same bullet or cell. MEASURED AT REVIEW over the whole pending corpus through `_structural_lines`: of 2773 citations, 2766 (99%) sit on a line that ALSO contains a backticked token, and only 7 do not. Worse, the test as written FAILS ON THIS PLAN'S OWN HEADLINE EVIDENCE: the drifted `216rgg` anchors are themselves backticked (`` `check_engine.py:897-905` ``, `` `doctor.py:530` ``), and the file path `check_engine.py` is itself a dotted token, so a naive "is there a symbol nearby" test scores them as ANCHORED and emits nothing. A rule that cannot flag the single example the plan was written from is not a weak rule, it is the wrong rule.
  SO THE TEST IS: DOES A DURABLE ANCHOR ACCOMPANY IT, WHERE THE CITATION ITSELF IS NOT EVIDENCE OF ONE. Exclude from the candidate-anchor set (a) the matched citation text, (b) a backticked token that is only a bare file path (`` `check_engine.py` ``), and (c) a backticked bare line range (`` `:906-915` ``, the filename-less continuation form, of which the corpus carries 2628). What remains that counts as durable is a QUALIFIED IDENTIFIER (`module.function`, `Class.method`) or a quoted content string. MEASURED with that correction: 558 of 2773 (20%) flag, and the `216rgg` Findings-row cell and its prose sentence BOTH flag while its E-01 bullet correctly does not (it names `check_engine.check_collisions` beside the offset, which is exactly the compliant (a)+(c) form E-01 blesses). The implementer must RE-DERIVE these numbers and must pin the `216rgg`-shaped cases as fixtures; a detector that reports near-zero on this corpus has reproduced the defect described here and must not ship.
  IT MUST BE `info` AND MUST NOT GATE, for a reason this repository has already paid for twice: `check.setid-collision` shipped at `error` for behavior that was later ruled CORRECT, and `gjadwm` records that a gate which false-positives trains agents to bypass it. A citation-form heuristic will have false positives (prose that mentions a line number for a legitimate reason, a quoted traceback), so it must nudge and never refuse. Per `artifact_core.drift_exit_code`, an `info` finding does not drive a nonzero exit; note that `ipd_lint` does not call that helper, and instead reaches the same outcome by carrying the finding in `LintResult.advisories`, which never flips the disposition. Emit into `advisories`; do NOT add to `diagnostics`.
  RESPECT THE EXISTING SCANNER'S FENCE HANDLING rather than re-implementing it: `ipd_lint._structural_lines` already returns only the lines OUTSIDE fenced code, indented code, YAML front matter, and block quotes, as `(1-based line number, line)` pairs, and its docstring names that as what structural checks see. VERIFIED AT REVIEW on a synthetic plan: a `foo.py:123` inside a ```` ``` ```` fence and one inside a 4-space-indented pasted traceback are BOTH excluded, while an ordinary bullet's citation is returned. Reusing that helper is what makes that true for free.
  KNOWN AND ACCEPTED LIMIT, stated so nobody later reports it as a bug: the helper's unit is a LINE, so a multi-line E-item whose symbol sits on the first line and whose offset sits on an indented continuation line is judged per line and the continuation flags. That is a false positive, it is why this rule is `info`, and it is the reason the item must not be promoted without the measurement the Deferred section demands.
  - Depends on: E-01
  - Expected outcome: a plan citing `foo.py:123` with no durable anchor draws one `info` advisory naming the new code; a plan citing a qualified symbol, or a symbol plus a trailing line number, draws none; a citation whose only companion is a BARE PATH or a BARE LINE RANGE in backticks still DRAWS one (the 99%-vs-20% correction above); a line number inside a fenced or indented block draws none; the `216rgg`-shaped fixtures flag; `aw ipd lint` exit status and disposition are UNCHANGED in every case.
  - Execution state: pending

- [ ] E-04 EXEMPT PLANS THAT PREDATE THE RULE, so the diagnostic reports on plans being authored and not on ~90 plans nobody is touching. Gate the new diagnostic on the plan's own `- Date:` against a cutover constant, following the pattern the durable-carrier rule in `check_engine` already uses for this exact problem (a module-level compact `YYYYMMDD` constant plus a per-artifact helper that downgrades a pre-cutover artifact).
  READ THE PRECEDENT'S SHAPE CORRECTLY, BECAUSE IT DOWNGRADES SEVERITY AND DOES NOT SUPPRESS. `carrier_severity_for_plan` returns `error` post-cutover and `info` pre-cutover; the finding is still EMITTED for an old plan. THIS rule is `info` in BOTH tiers by E-03's ruling, so "downgrade to info" is a no-op and the gate must instead SUPPRESS pre-cutover: emit nothing at all for an old plan. Say so explicitly in the code comment, so a later reader does not "restore parity" with the carrier rule and thereby reintroduce ~558 advisories on untouchable history. Borrow the precedent's DATE MECHANISM, not its severity ladder.
  COPY ONE MORE THING FROM THE PRECEDENT: a plan with NO parseable `- Date:` must be treated as PRE-cutover (suppressed), because `IPD-M101` already owns the missing-Date complaint and this rule must not invent a second consequence for it. That is the carrier helper's stated reasoning and it applies unchanged.
  SET THE CUTOVER STRICTLY AFTER THE NEWEST EXISTING PLAN DATE, AND NOTE THIS PLAN IS ITSELF THE BOUNDARY CASE. Measured at review: the newest `- Date:` in the pending corpus is `2026-09-18`, which is THIS PLAN's own date, and no pending plan is dated later. A cutover of `20260918` would therefore fire on this very plan (which cites `foo.py:123` three times as illustrative fixtures) and on any same-day sibling. Choose a cutover strictly greater than the newest date present at execution time, re-measured then rather than quoted from here; `20260919` matches the carrier constant's value and its reasoning, but re-derive it.
  WHY A DATE AND NOT A SWEEP: without the gate, the first run reports hundreds of advisories for work no author can act on, and a diagnostic that fires mostly on untouchable history is one every reader learns to skip, which destroys the value of the 1 finding that matters.
  - Depends on: E-03
  - Expected outcome: `aw ipd lint` over the current pending corpus reports ZERO new-code advisories, INCLUDING on this plan itself; the same lint over a synthetic post-cutover plan with a bare file:line anchor reports exactly one; a plan with no `- Date:` reports none.
  - Execution state: pending

- [ ] E-05 MAKE THE ADVISORY VISIBLE BY DEFAULT IN HUMAN OUTPUT, because as things stand E-03 would ship a nudge that nudges nobody. ADDED AT REVIEW after measuring the existing surface: in `ipd_lint`'s human render path, the per-advisory lines are printed only `if has_adv and detail`, where `detail` comes from `--detail`/`--long`. Without that flag the whole finding collapses into the single word `advisory` in the status line, with NO code and NO message. MEASURED on the one pending plan that currently carries advisories (`5e4sb6`, two `IPD-Z602` findings): the default invocation prints one line ending `advisory` and zero finding lines, and only `--detail` reveals them. An author who does not know to pass a flag they have no reason to suspect learns nothing, and E-02's whole premise is that the author must MEET the rule.
  THE FIX IS DELIBERATELY NARROW: surface the NEW code's advisory text by default, and leave `IPD-Z602`'s existing default-quiet behavior exactly as it is. Do NOT make every advisory verbose by default, which would change unrelated output for every plan in the tree and is a separate decision with its own blast radius. If the implementer finds no way to distinguish one advisory code from another at that point in the render path without a wider refactor, STOP AT THAT BOUNDARY: record the finding, leave E-03's advisory as-is, and file a carrier rather than reshaping the whole render path under this plan's scope.
  EXIT STATUS AND DISPOSITION MUST NOT MOVE. This item changes only what is PRINTED. The `--agent`/`--json` paths already emit every advisory with `severity: "info"` unconditionally (verified this review: the uncarried-obligation advisory appears in `--json` on this very plan), so they need no change and must not gain one.
  - Depends on: E-03
  - Expected outcome: a post-cutover plan with a bare anchor shows the new code and its message in DEFAULT human output with no extra flag; `IPD-Z602`'s default output is byte-unchanged; exit status and disposition are unchanged in every case; `--agent`/`--json` output is unchanged.
  - Execution state: pending

- [ ] E-06 REPLACE THIS PLAN'S SELF-CARRIER WITH A REAL ONE, which is a required act and not bookkeeping. ADDED AT REVIEW. The `## Deferred / out of scope` row for promoting the diagnostic to a gating severity currently carries `- Carrier: mzc019`, this plan's own id6. File a backlog item with `aw backlog new` for the promotion decision (carrying the measurement E-03's corpus run produces, so the promotion is decided on numbers rather than a hunch), then replace that row's `- Carrier:` value with the new item's id6. Do this BEFORE finalize.
  WHY THIS IS AN E-ITEM AND NOT A GATE SENTENCE: the obligation is invisible to the gate that exists to catch it. `check.ipd-uncarried-obligation` fires only at `pre-transition`, and measured at review it reports this plan's row as SATISFIED, because a self-reference resolves while the plan is still non-terminal. So no checkpoint will ever stop a run over this, and a reminder buried in the execution contract would be the only thing standing between the promotion decision and silent disappearance. Something a gate cannot see needs a checklist item and a validation item.
  DO NOT "FIX" THE RESOLVER UNDER THIS PLAN. That the resolver accepts a self-reference is arguably a defect in `check_engine`, but this plan's `- Scope-Paths:` deliberately excludes `check_engine.py` and a resolver change would affect every carrier in the tree. File it separately if the executor judges it worth filing, and say so in the validation evidence; do not widen this plan.
  - Depends on: E-03
  - Expected outcome: a backlog item exists for the promotion decision; the Deferred row's `- Carrier:` names that item's id6 and no longer names `mzc019`; `aw ipd lint --phase pre-transition` still reports the row satisfied, now for a legitimate reason.
  - Execution state: pending

## Project conventions discovered (Step 0)

- CITATIONS IN THIS PLAN ARE DELIBERATELY SYMBOL-ANCHORED, INCLUDING THE ONES DESCRIBING THE DEFECT. Every location named here is a module, function, constant, heading constant, or quoted content string. That is not stylistic: a plan proposing this rule while citing line numbers would be self-refuting, and the drift it describes would have corrupted its own evidence before an executor read it.
- `ipd_lint` DECLARES STABLE RULE CODES AS `C_*` MODULE CONSTANTS, grouped by area (`IPD-P001` parse, `IPD-M1xx` metadata, `IPD-H2xx` headings, `IPD-I3xx` ids, `IPD-S4xx` states, `IPD-Q501` open questions, `IPD-Z6xx` size, `IPD-N001` name). A new diagnostic adds a constant to that block in a NEW area block; codes are stable and are not recycled, and `IPD-D701` is RETIRED and must not be revived.
- THE LINTER ALREADY HAS FENCE-AWARE ITERATION in `ipd_lint._structural_lines`, plus a `Diagnostic` carrying a 1-based line and a `render` that formats `path:line:col code message`. A new textual rule reuses that helper; re-implementing fence detection is how a rule starts flagging pasted tracebacks. NOTE ITS UNIT IS A LINE, not a bullet, so a multi-line bullet is judged per line (see E-03's accepted limit).
- THE CUTOVER-DATE PATTERN FOR A NEW OBLIGATION ALREADY EXISTS in `check_engine`'s durable-carrier rule: a module-level compact `YYYYMMDD` constant, a legacy severity constant, and a per-artifact helper that returns the legacy severity for an artifact dated before the cutover. E-04 borrows its DATE MECHANISM but NOT its severity ladder, because that precedent downgrades rather than suppresses and this rule is `info` in both tiers.
- ADVISORIES ARE THE NON-GATING CHANNEL, AND `ipd_lint` DOES NOT USE `drift_exit_code`. Verified at review: the helper exempts only `info`, but `ipd_lint` never calls it; instead `lint_text` returns a `LintResult` whose `advisories` list is separate from `diagnostics`, and only `diagnostics` set the `error` disposition. So a non-gating rule is one that emits into `advisories`. The `--agent`/`--json` renderers stamp every advisory `severity: "info"` unconditionally.
- ADVISORIES ARE QUIET BY DEFAULT IN HUMAN OUTPUT, which E-05 exists to address: the per-advisory lines print only when `--detail`/`--long` is passed, so by default the entire finding collapses to the word `advisory` in the status line with no code and no message.
- THIS SPEC CANNOT BE STATUS-TRANSITIONED. `20260802-1904-01-ipd-structure-and-linting.spec.md` is `implemented`, and the only legal onward transitions are `superseded` and `deferred`. Amend it with `aw specs note` and leave its status alone (see the spec-sync section).
- A SELF-REFERENCING `- Carrier:` RESOLVES SUCCESSFULLY AND IS THEREFORE A SILENT TRAP: `check_engine._resolve_carrier` accepts any owner with a non-terminal status and does not special-case the citing plan, so a plan naming its own id6 passes the gate until the moment it is executed, at which point the obligation disappears. E-06 exists because of this.
- NOTHING IN THE REPOSITORY CURRENTLY VALIDATES A CITATION. Grepping `ipd_lint` for `cite`/`citation` returns nothing, and the IPD spec is silent on anchor form. So this is a NEW obligation, not a tightening of an existing one, which is why it enters at `info`.
- ONE SHIPPED WORKFLOW ACTIVELY ASKS FOR `file:line`: the `assess` workflow instructs a reporter to give "`file:line` where known". That is a FINDINGS REPORT about a tree at a moment, not a plan to be executed later, so it is defensible and is deliberately out of scope here; but an implementer should read it before assuming the phrase should be purged repo-wide.

## Findings

| Id | Severity | Location (symbol / content anchor) | Finding | Evidence |
| --- | --- | --- | --- | --- |
| F-1 | HIGH | plan `216rgg`, its `- Concern:` and E-01/E-02/E-04 bullets | Every core code anchor in an APPROVED, twice-reviewed plan has drifted before execution. Its cross-type citation now lands on `seen_ids` initialization and a comment; its `doctor.py` hardcode citation lands on a blank line. | The four cited anchors compared against the current `check_engine.check_collisions` and `doctor` sources at HEAD `d445e6e6` |
| F-2 | HIGH | `.aw/records/plans/pending/*.ipd.md` | The convention is corpus-wide, not local: 96 of 101 pending plans carry file:line citations, 2816 in total, densest single plan 102. RE-MEASURED at `8087387e`: 94 of 99, 2771 citations. RE-MEASURED AT REVIEW at `7562ca6c`: 90 of 93 (96%), 2802 citations raw / 2773 through the fence-aware view, densest still 102. The shape (~95% saturation) held across all three measurements while the absolute counts moved within hours, which is why the criteria are written in shape terms. | Regex scan of the pending tree at HEAD `d445e6e6`, re-run at `8087387e` and at `7562ca6c` |
| F-3 | HIGH | same corpus | THE INTUITIVE METRIC IS USELESS AND MISLEADING HERE. Only 8 of 2816 citations (0%) are provably dead; re-derived at review as 132 of 2764 (4%) under a resolver that also searches `agent_workflows/` and `tests/` for a bare filename. Either way it is small, and either way it is the wrong measure: a line number stays syntactically valid while becoming semantically wrong, so a validator must key on anchor FORM, never on resolvability. State which resolution rule produced the number. | The same scan, checking file existence and line-count bounds, under both resolution rules |
| F-4 | HIGH | plan `216rgg` `## Workflow history` | REVIEW IS NOT THE REMEDY. Both reviews verified the citations and recorded that every line number was measured at HEAD. They were correct at the time; the references expired afterward. Diligence cannot preserve an expiring reference type. | The plan's own history entries |
| F-5 | MEDIUM | `ipd_lint` `C_*` constant block (30 constants); `ipd_authoring._SECTION_BODY` keyed by `ipd_schema.H_PROJECT_CONVENTIONS`; `ipd_lint._structural_lines` | The three surfaces a new rule needs already exist and are cheap to extend: a stable-code block, a scaffold-emitted conventions body, and a fence-aware line iterator. No new subsystem is required. CORRECTED AT REVIEW: the conventions BODY lives in `ipd_authoring`, not `ipd_schema`, which owns only the heading constant. | Source read; `C_*` constant count grepped; `H_PROJECT_CONVENTIONS` grepped across the package |
| F-6 | MEDIUM | `check_engine.CARRIER_CUTOVER_DATE`, `_CARRIER_LEGACY_SEVERITY`, `carrier_severity_for_plan` | The legacy-exemption problem is SOLVED ALREADY in this repository, by a compact `YYYYMMDD` cutover constant plus a per-artifact helper returning the legacy severity for a pre-cutover artifact. E-04 should copy its DATE MECHANISM rather than invent a second one, but NOT its severity ladder (it downgrades; this rule must suppress). | Source read |
| F-7 | LOW | `.aw/system/workflows/assess/assess.md` | A shipped workflow asks reporters for `file:line`. Defensible for a point-in-time findings report and deliberately out of scope, but it means "line numbers are always wrong" would be too strong a rule to state. | The workflow text |
| F-8 | INFO | `check.setid-collision` history; backlog `gjadwm` | PRECEDENT FOR ENTERING AT `info`: this repository has already shipped a rule at `error` that flagged correct behavior, and has recorded that a false-positive gate trains agents to bypass it. A heuristic about prose form must not gate. | D153; the `gjadwm` item |
| F-9 | HIGH | the detector condition E-03 originally specified ("no accompanying symbol or quoted content string") | ADDED AT REVIEW. THE ORIGINALLY SPECIFIED TEST CANNOT SEE THIS PLAN'S OWN HEADLINE DEFECT. 2766 of 2773 structural citations (99%) sit on a line that already contains a backticked token, and the drifted `216rgg` anchors are THEMSELVES backticked and contain a dotted token (`check_engine.py`), so a naive nearby-symbol test scores them anchored and emits nothing. Excluding the citation itself, bare paths, and bare line ranges from the candidate-anchor set moves the flag rate to 558 of 2773 (20%) and makes the `216rgg` cell and prose flag while its compliant E-01 bullet does not. | Corpus scan through `_structural_lines` under both conditions; the `216rgg` Findings row and prose sentence evaluated directly |
| F-10 | HIGH | `ipd_lint` human render path (the `if has_adv and detail` guard); `ipd_authoring._SECTION_BODY`; `tests/test_ipd_templates.py` | ADDED AT REVIEW, two independent mechanism errors that would each strand an item. (a) An advisory is INVISIBLE in default human output: measured on `5e4sb6`, the default run prints one line ending `advisory` and zero finding lines, so E-03's nudge would reach nobody without `--detail`. Hence E-05. (b) The conventions body is in `ipd_authoring`, and editing it BREAKS a byte-parity pin: adding one line in memory and running `tests/test_ipd_templates.py` FAILED with "child template drifted from build_skeleton; regenerate it". Hence E-02's regeneration requirement and the corrected fence. | Both measured this review; test failure reproduced |
| F-11 | HIGH | this plan's `- Scope-Paths:`; its Deferred row `- Carrier:`; the target spec's `- Status:` | ADDED AT REVIEW, three contract errors in the plan's own metadata. The fence declared `ipd_schema.py` (not needed) and omitted `ipd_authoring.py`, the template, and its parity test (all required), so the run would have taken three out-of-scope edits plus one `--scope-ack`. The Deferred row's `- Carrier: mzc019` is a SELF-carrier that resolves only while the plan is non-terminal and vanishes on execution. And V-01 required a tooled `- Status:` update on a spec whose `implemented` status permits only `superseded`/`deferred`, so the demand was unsatisfiable. | `parse_scope_paths` run on the declared value; `_resolve_carrier('mzc019')` returned `ok`; `transition_allowed('implemented', ...)` False for every review-ish target |

## Proposed changes (ordered, validatable)

1. E-01 states the anchor rule, its rationale, its preference order, and the line-as-subject exception in the IPD structure-and-linting spec.
2. E-02 emits a one-line form of the rule in the scaffold's Step 0 section (in `ipd_authoring`, regenerating the byte-pinned child template), so an author meets it before writing findings.
3. E-03 adds one `info` lint advisory for a citation carrying no durable anchor, reusing the existing fence-aware iterator, with the bare-path and bare-line-range companions explicitly NOT counting as anchors.
4. E-04 gates that advisory on a date cutover (SUPPRESSING pre-cutover rather than downgrading) so it reports on new plans only.
5. E-05 makes the advisory visible in default human output, without which E-03 nudges nobody.
6. E-06 replaces this plan's self-carrier with a real backlog item, which no checkpoint can enforce.

## Deferred / out of scope (with reason)

- RETROFITTING THE 2816 EXISTING CITATIONS: deliberately NOT done, and this is the largest deferral in the plan. Rewriting anchors across 96 plans would touch other agents' in-flight and approved work in a shared checkout for cosmetic gain, would produce an enormous diff with no behavior change, and would risk corrupting the evidence in plans currently being executed. The correct posture for an existing plan is the one this conversation used successfully: treat a line number as a HINT, locate the construct by symbol or by grepping the quoted message, and proceed.
  - Carrier-Declined: Deliberately not carried. The deferral is permanent by design rather than pending: the existing citations are historical records that were true when written, and the contract change governs new plans only. There is no future state in which this sweep becomes correct, so filing a carrier for it would imply otherwise.
- FORBIDDING LINE NUMBERS OUTRIGHT: rejected. F-7 shows a legitimate use (a point-in-time findings report), and E-01 records a second (a citation whose subject IS the line). The rule requires a DURABLE anchor to be present, not the absence of a number.
  - Carrier-Declined: A rejected alternative, recorded so it is not re-proposed; there is nothing to carry.
- MAKING THE DIAGNOSTIC GATE (`warning` or `error`): deferred until the `info` rule has run long enough to measure its false-positive rate on real authored plans. Promoting it is a separate, evidence-bearing decision. The measurement it waits on is now specified rather than vague: the false-positive rate on plans authored AFTER the cutover, with the known line-granularity false positive E-03 records counted as such.
  THE CARRIER BELOW IS A PLACEHOLDER THAT THE EXECUTOR MUST REPLACE, and E-06 owns that. As authored this row named `mzc019`, this plan's OWN id6, which is a self-carrier and silently defeats the mechanism: `check_engine._resolve_carrier` accepts any owner whose status is non-terminal, and measured at review it returned `ok` for `mzc019` only because this plan is currently `to-review`. The moment this plan reaches `executed` that same reference turns terminal, the obligation classes `done` in `aw attention`, and the promotion decision vanishes with no record, which is exactly the disappearance the carrier field exists to prevent. The resolver does NOT reject a self-reference today, so nothing else will catch this.
  - Carrier: mzc019
- HOW REVIEWS CITE CODE: a review is written and consumed within one sitting against a known HEAD, so its citations expire far less dangerously. Possibly worth the same treatment later; not evidenced yet.
  - Carrier-Declined: Speculative, with no measurement behind it. Filing a carrier would assert a defect this plan has not demonstrated.
- THE `assess` WORKFLOW'S `file:line` INSTRUCTION (F-7): out of scope, defensible as written.
  - Carrier-Declined: Judged correct as-is rather than deferred, so there is no work to hand off.

## Scope check

- Over-scope: none. Each declared path is touched by a named E-item: the IPD spec (E-01), `agent_workflows/ipd_authoring.py` and the regenerated `.aw/system/workflows/assess/templates/ipd.md` plus its pin `tests/test_ipd_templates.py` (E-02), `agent_workflows/ipd_lint.py` (E-03/E-04/E-05), and `tests/test_ipd_lint.py` (the pins for E-03/E-04/E-05). E-06 touches no declared path: it edits THIS PLAN and creates a new backlog record, neither of which belongs in a `Scope-Paths` allowlist.
- CORRECTED AT REVIEW: the fence originally declared `agent_workflows/ipd_schema.py`, which E-02 does not need to touch (the schema owns only the heading constant, not the emitted body), and omitted `ipd_authoring.py`, the template, and the template's parity test, all three of which E-02 MUST change. As authored, the run would have hit an out-of-scope edit on three paths and a declared-but-unmodified `--scope-ack` on a fourth. Both halves are now declared correctly.
- Under-scope: the spec amendment is DECLARED in `- Scope-Paths:` on purpose, per the plan-may-amend-a-spec contract, so both runners announce the declared spec edit before the run and reconcile it at finalize. An implementer who finds a further surface that must change must declare it before editing rather than reconciling it afterward.

## Required tests / validation

`python3 -m pytest` bare. Do NOT add flags: `addopts` already supplies `-q -n auto --dist=worksteal -m 'not slow'`. Establish the baseline IN THE EXECUTING WORKTREE and compare by failing NODE ID rather than by total, since the total moves with concurrent work.

Beyond the suite: `aw ipd lint` over the full pending corpus before and after, showing the count of the NEW code is zero both times (E-04's exemption) and that no plan's disposition changed; the same lint over a synthetic post-cutover plan showing exactly one `info` advisory; a scaffolded plan showing the new convention line present and the scaffold still conforming; and the exit status AND disposition of every `aw ipd lint` invocation above, proving the new rule never changes either.

TWO TEST FILES ARE LOAD-BEARING AND MUST BOTH BE RUN AND PASTED INDIVIDUALLY, not merely absorbed into the bare-suite total. `tests/test_ipd_templates.py` is the byte-parity pin E-02 breaks unless the template is regenerated (measured at review: it FAILS on a `_SECTION_BODY` edit alone), and `tests/test_ipd_lint.py` carries the new rule's pins. A bare-suite summary alone cannot show which of those two moved.

THE DETECTOR'S DISCRIMINATION IS ITSELF A VALIDATION TARGET, not just its unit cases. Run the new detector over the whole pending corpus with the cutover gate disabled and report the flagged count against its denominator. This is the measurement that distinguishes the corrected rule from the naive one, which the review measured at 20% versus 0% on the same corpus; a near-zero result means the shipped rule cannot see the defect this plan exists to address.

## Spec / documentation sync

`.aw/records/specs/20260802-1904-01-ipd-structure-and-linting.spec.md` is DECLARED IN SCOPE and AMENDED by E-01, which adds the citation-anchor rule. This is an ADDITION to the authoring contract, not a reversal: nothing in the spec currently addresses anchor form (verified at review: grepping `ipd_lint` for `cite`/`citation` returns nothing and the spec is silent), so no existing requirement changes meaning and no prior plan becomes retroactively non-conforming (E-04's cutover is what guarantees the second half of that claim).

WHY THE AMENDMENT BELONGS IN THIS CHANGE rather than a follow-up: the lint advisory and the scaffold line are both ENFORCEMENT of a rule that must first EXIST in the contract. Landing the nudge without the spec would leave a diagnostic citing no authority, which is the drift between code and contract that `216rgg` is separately having to close for `check.setid-collision`.

HOW TO RECORD THE AMENDMENT, MEASURED AT REVIEW, BECAUSE THE OBVIOUS ROUTE IS REFUSED. This spec's `- Status:` is `implemented`, and `attention_contract.SPEC_TRANSITIONS['implemented']` permits only `superseded` and `deferred`; `transition_allowed('implemented', X)` returns False for every other target, including a no-op re-set. So `aw specs set` CANNOT move this spec to a review-ish state, and V-01's original wording ("confirm the spec's `- Status:` and history were updated by a tooled verb") asked for something impossible in one half and undesirable in the other: the status MUST NOT change at all here, because the spec is genuinely implemented and this is an amendment to it, not a reversion.
USE `aw specs note <path> --message ...`, which appends a workflow-history record WITHOUT touching status and is exactly this edit's shape. Precedent in this very spec: it already carries a `2026-08-26 note (aw specs)` history entry recording a scoping amendment. Do NOT hand-append a history line, and do NOT attempt a status transition. If the spec's status has changed by execution time, re-derive the legal transition set before acting rather than assuming this paragraph still holds.
ADDING A NUMBERED SPEC ITEM CHANGES A PINNED SURFACE, so check before renumbering: `tests/test_ipd_templates.py` reads the OTHER IPD spec (`20260726-1340-01-ipd-spec.spec.md`) and asserts content about it, and Section 10's numbered list is referenced by rule-code prose elsewhere. Prefer APPENDING a new numbered item to Section 10's enumeration (or a new subsection) over renumbering existing ones, and paste evidence that no test reading either spec changed behavior.

## Open questions

### OQ-01: Should the anchor rule also cover citations to NON-CODE artifacts (a spec section, a README paragraph)?

- Blocking: no
- Status: open
- Owner: maintainer
- Resolution or deferral rationale: NOT blocking, because every E-item here concerns code citations and none depends on the answer. It is worth asking because the same expiry applies in a weaker form: `2lcqno` Section 1 finding 4 is a durable anchor today, but a spec that gains a section renumbers. RECOMMENDATION: leave non-code citations alone for now. Section headings are already near-symbolic (they are titles, not offsets), so the failure mode is far milder, and widening the rule would add scope with no measurement behind it. Revisit only if a drifted spec-section citation is ever observed to mislead someone.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: paste the spec diff showing the anchor rule, its stated RATIONALE (not just the rule), the (a)/(b)/(c) preference order, and the line-as-subject exception. Paste the runner's declared-spec-edit announcement for this run, or if run outside a runner, state that and paste the finalize scope reconciliation instead. Paste the `aw specs note` invocation and its output, and paste the spec's `- Status:` line BEFORE and AFTER showing it is UNCHANGED at `implemented` (a transition is refused here and would be wrong; see the spec-sync section). Confirm no history line was hand-appended. Paste `aw specs check` on the spec, and `python3 -m pytest tests/test_ipd_templates.py` passing, since it reads the sibling IPD spec.
  - Observed evidence:
  - Result: pending

- [ ] V-02 validates E-02
  - Required evidence: scaffold a throwaway IPD, paste its `## Project conventions discovered (Step 0)` section showing the new line, and paste `aw ipd lint` on that file reporting conforming. Paste the added line's source in `ipd_authoring._SECTION_BODY` (NOT `ipd_schema`) and confirm it is ONE line. Paste `python3 -m pytest tests/test_ipd_templates.py` PASSING, which is the byte-parity pin this edit breaks unless the child template was regenerated; state explicitly that the template was REGENERATED from `build_skeleton` rather than hand-edited. Paste the template diff showing only the conventions body changed. Confirm by grep that the new line was NOT added to `_AUTHORING_PLACEHOLDERS`, and paste `authoring_placeholders_resolved` returning True for a fully-authored plan carrying the new convention line, proving the draft-ready nudge is not silenced. Delete the throwaway and show `git status --porcelain` clean of it.
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: paste the new `C_*` constant and its code, and confirm by grep that the code is not already used anywhere in `ipd_lint` or the tests and is not the retired `IPD-D701`. Paste SIX lint runs over synthetic fixtures with their outputs: (a) a bare `foo.py:123` anchor -> exactly one advisory with the new code; (b) a qualified symbol anchor -> zero; (c) a symbol PLUS a trailing line number -> zero, proving the rule keys on a MISSING durable anchor and not on the digits; (d) `foo.py:123` inside a fenced block AND inside a 4-space-indented block -> zero, proving the fence-aware iterator is reused; (e) a citation whose ONLY companion token is a BARE PATH in backticks (`` `check_engine.py` ``) -> one, and (f) one whose only companion is a BARE LINE RANGE (`` `:906-915` ``) -> one. Cases (e) and (f) are the anti-regression pins for the measured 99%-false-negative defect and MUST NOT be dropped.
    PLUS THE HEADLINE FIXTURE, which is the whole point of the rule: build fixtures reproducing `216rgg`'s actual drifted citations (its Findings-row Location cell `` `check_engine.py:897-905` `` and the prose sentence containing it) and show BOTH flag, while its E-01 bullet form (which names `check_engine.check_collisions` beside the offset) does NOT. A detector that reports nothing on the `216rgg` cell has reproduced the defect and must not ship.
    PLUS THE CORPUS DISCRIMINATION MEASUREMENT: run the detector over the whole pending corpus with the cutover gate disabled and paste the flagged count and the denominator. State the percentage. A result near 0% means the naive form was shipped; the review measured 20% (558 of 2773) for the corrected form. RE-DERIVE, do not quote.
    PLUS A MUTATION CHECK: break the durable-anchor condition so case (b) or (c) starts firing, show the pin FAILS, restore, show it passes. Paste the exit status AND disposition of every run above, all UNCHANGED from the no-rule baseline, and confirm the finding arrives in `LintResult.advisories` and not `diagnostics`.
  - Observed evidence:
  - Result: pending

- [ ] V-04 validates E-04
  - Required evidence: paste `aw ipd lint` over the ENTIRE pending corpus showing ZERO advisories of the new code, and state the corpus size scanned so the zero has a denominator (order of 90 plans; RE-DERIVE it, do not quote this plan's numbers, which were 101 at authoring, 99 hours later, and 93 at review). Paste the same lint run against THIS PLAN specifically showing zero, since this plan contains three illustrative `foo.py:123` strings and its own `- Date:` was the newest in the corpus at review. Paste the same lint over a synthetic plan dated AFTER the cutover with a bare anchor, showing exactly one, and over a synthetic plan with NO `- Date:`, showing zero. Paste the cutover constant, the date-comparison helper, and the newest `- Date:` present in the corpus at execution time, showing the constant is strictly greater. Confirm no plan's overall disposition changed by comparing the before and after dispositions.
  - Observed evidence:
  - Result: pending

- [ ] V-05 validates E-05
  - Required evidence: paste the DEFAULT human-mode `aw ipd lint` output (no `--detail`, no `--long`) for a post-cutover fixture carrying a bare anchor, showing the new code AND its message present. Paste the default output for `5e4sb6` (or whichever plan then carries `IPD-Z602`) before and after the change and show it is BYTE-IDENTICAL, proving the existing advisory's default quietness was not altered. Paste `--agent` output before and after showing it unchanged. Paste exit status and disposition for all of the above, unchanged. If the narrow fix proved impossible within the stated boundary, paste the evidence of that, cite the carrier filed, and mark this item `blocked` rather than `pass`.
  - Observed evidence:
  - Result: pending

- [ ] V-06 validates E-06
  - Required evidence: paste the `aw backlog new` invocation and its output, including the minted id6. Paste the Deferred row's `- Carrier:` line showing that id6 and NOT `mzc019`, and confirm by grep that `mzc019` no longer appears as a `- Carrier:` value anywhere in this plan. Paste `aw ipd lint --phase pre-transition` for this plan showing the row satisfied. State whether a separate item was filed for the resolver's self-reference acceptance, and confirm `check_engine.py` is absent from this plan's committed diff (`git diff --name-only`), proving the scope boundary held.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Size note: 6 E-leaves in 2 groups (4 at authoring; E-05 and E-06 added at review). Still small by design: the contract change plus a non-gating nudge, with the expensive option (retrofitting the ~2800 existing citations) deliberately excluded rather than deferred to a child.
- Cohesion rationale: E-01 is the authority the others enforce, so landing them apart would leave either a rule nobody meets or a diagnostic citing nothing. E-03, E-04 and E-05 are ONE diagnostic with its exemption and its visibility: shipping E-03 without E-04 fires hundreds of advisories on untouchable history and trains readers to ignore the rule on its first run, and shipping it without E-05 ships a nudge that prints no code and no message unless a flag nobody knows to pass is supplied, which is a nudge in name only. E-06 is small and must land here because it repairs THIS plan's own carrier, which no later plan would know to look at.

Execution contract: commit ONLY the files this plan changed, path-scoped; never `git add -A`, never `-a`, never push. Verify the staged set with `git diff --cached --name-only` before every commit and RE-VERIFY after any failed or hook-interrupted commit, since a rejected hook can leave paths in the index. When reporting tests passed, paste the ACTUAL runner output. This is a SHARED CHECKOUT: other agents are editing this tree concurrently, so never revert or commit a file you did not change. `216rgg` has since executed and merged, so the note about it being in flight no longer applies; this plan still does not touch `check_engine.py`, and E-06 explicitly forbids widening into it.

SCOPE FENCE: the declared `- Scope-Paths:` is the DECLARATION the runner reconciles against at finalize, not a stop signal. If an out-of-scope edit proves necessary, MAKE IT and then JUSTIFY it with `aw ipd finalize --scope-reason <path>=<why>`; if a declared path turns out not to need changing, acknowledge it with `--scope-ack <path>`. Do STOP and report only for a genuinely unsafe condition: an unresolvable concurrent-edit conflict in `ipd_lint.py` or `ipd_authoring.py` (both are high-traffic), or a prerequisite symbol this plan names having disappeared.

Post-gate lifecycle: this plan is `to-review` and requires `/plan-review` followed by explicit human approval (`aw ipd set approved mzc019 --by-human --message ...`) before execution. On completion, transition via `aw ipd finalize` after `aw ipd lint --phase pre-transition` conforms and every `V-*` carries pasted evidence.
