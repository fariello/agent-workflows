# IPD: Add a plan-less aw commit and land the ruled MUST wording

- Date: 2026-09-08
- Kind: child
- Concern: The maintainer ruled on 2026-09-01 that the commit contract must say agents MUST use the tooled path, not PREFER it, and supplied the exact replacement wording. That wording cannot land honestly yet for two reasons. FIRST, `aw commit` REQUIRES a plan selector, so every plan-less commit (a backlog item, a spec edit, a typo fix) has no tooled path to comply with, which would make non-compliance the COMMON case and trains agents to ignore the rule. SECOND, the ruled wording contains a DESCRIPTIVE sentence about a hook-rewrite retry that the code does not yet perform, so shipping it early would make the contract installed into every managed repo document behavior that does not exist.
- Scope: Close the gap, then land the wording. IN: a plan-less commit mode so the MUST is compliable, and the ruled replacement of the two `engine.py` paragraphs with the retry sentence gated on its prerequisite. OUT: the agent-context detector, any new guard, and gate wiring (sibling Order 01 owns that).
- Scope-Paths: agent_workflows/work_cmd.py, agent_workflows/cli.py, agent_workflows/engine.py, AGENTS.md, tests/test_work_commit.py, tests/test_work_primitives.py
- Item-Dependencies: executed:lqly9m
- Status: reviewed
- Readiness: no-go
- Set: commitguard
- Order: 2
- Highest E allocated: 08
- Author: opencode its_direct/pt3-claude-opus-5-1m-us
- Id: y9vpvv
- From-Backlog: wjl471

## Workflow history

- 2026-09-09 reviewed (opencode its_direct/pt3-claude-opus-5-1m-us): /plan-review at HEAD `26644c26`; REVIEWED - OPEN QUESTIONS, readiness `no-go` on the newly raised blocking OQ-03 ALONE; PR-901..PR-903 and PR-905..PR-912 FIXED in place, PR-904 escalated as blocking OQ-03, none deferred, none REPLAN. `aw ipd lint --phase author` CONFORMING before review and `--phase review-finalize` CONFORMING after. THE PLAN'S DIAGNOSIS IS CORRECT AND ITS EVIDENCE REPRODUCES: `aw commit` still refuses without a selector (`work_cmd.py:138`, and `aw commit --no-plan ...` exits 2 today with "unrecognized arguments: --no-plan"), the PREFER paragraph still carries "immune to this by construction" (`engine.py:1252`), the graceful-degradation precedent is real (`work_cmd.py:454-458`), and the ruled wording is quoted verbatim in the item (`wjl471:132-138`). FIVE MEASURED FINDINGS THE PLAN DID NOT ANTICIPATE, three of them able to make an executor either fail or ship a silent no-op. (1) BLOCKER PR-901: E-04's regeneration is DRIFT-PRESERVED AWAY TODAY. `_apply_section_consent` preserves the ON-DISK body when the manifest holds a recorded hash for a section that does not match disk, and `.aw/system/managed-sections.json` records `AGENTS.md#aw:pointer` with `a9deb5a1...` while the live body hashes `8baff639...`, so `matches_recorded` is FALSE. MEASURED by calling `merge_aw_block` with a sentinel line appended to the pointer section: `action='refreshed'`, sentinel ABSENT from the output, and the only diff three blank lines. An executor would see a near-clean diff and read it as idempotent. The prior precedent plan `uyd3lw` E-02 verified `recorded_hash` returned NONE and is now STALE. Added E-07 to fix the record before regenerating and V-07 to demand the sentinel proof. (2) BLOCKER PR-902: E-01's `--no-plan` CANNOT be a plain flag beside a `REMAINDER` positional. MEASURED across five argv shapes: with `plan` made `nargs='?'`, `aw commit --no-plan -m msg -- a.py b.py` parses `plan='a.py'` and drops `b.py` into the remainder, so the FIRST PATH IS SILENTLY SWALLOWED AS A SELECTOR; and `aw commit y9vpvv --no-plan -- a.py` never sets the flag at all because REMAINDER eats it. E-01 now specifies the recovery, mirroring the existing `-m`/`--no-commit`/`--dir` recovery the verb already performs (`work_cmd.py:398-414`, `:77-124`), and V-01 requires the multi-path case as proof. (3) HIGH PR-903: E-02's "print which protections were skipped" would BREAK AN EXISTING TEST if the notice names the raw command. `tests/test_work_primitives.py:243` asserts `"git commit" not in inspect.getsource(work_cmd.run_commit)`; verified the string is absent today and that appending a notice containing it makes the assertion fail. That file was NOT in `Scope-Paths` (added). (4) HIGH PR-904: the ruled wording CONTRADICTS the very next paragraph of the same managed block, which says agents MUST commit `path-scoped (git commit -m msg -- <path>)` (`engine.py:1259`, `AGENTS.md:63`) and is repeated at four driver-prompt sites. Landing "You MUST commit through `aw commit`, not raw `git commit`" beside it ships a block that mandates both. New OQ-03 (`Blocking: yes`) puts the reconciliation to the maintainer, because the ruling did not authorize editing that paragraph and a reviewer may not silently widen a dictated wording change. (5) MEDIUM PR-905: E-05's fallback ("ship the imperative, withhold the retry sentence") had no stated mechanics; the sentence is the THIRD of five in the ruled text and E-05 now says to delete exactly that sentence and record the verbatim shipped text. ALSO CORRECTED: the call-site count is 7 not 8 and only 6 are plan-less, since `work_cmd.py:470` is the plan-bearing one (F-2); the suite baseline was wrong in both count and named failure (actual `2 failed, 5918 passed, 3 skipped, 2 xfailed`, one an ENVIRONMENTAL `test_reporting_contract.py` failure caused by 189 gitignored `opencode-recovery/*.md` files belonging to another party, the other a 30s-timeout SIGINT test, and NOT the `test_orchestrator_retirement` case named); `tests/test_work_commit.py` DOES NOT EXIST and the plan named it as if it did, so E-08 now states it is a new file and names the existing sibling to extend; `CONTRIBUTING.md:179` was checked as the spec-sync section instructs and does NOT say PREFER (it points at the managed block), so it correctly stays out of the fence, which is now recorded rather than left as an executor task. Verified sound and left alone: the plan-less gap, the two-plan-derived-protections analysis, the graceful-degradation reuse, the `Item-Dependencies: executed:lqly9m` edge (`lqly9m` is `reviewed`, not executed, so the edge is UNSATISFIED and a runner will correctly mark this `dependency-blocked`), and the decision to implement a flag rather than a sibling verb. E-count 6 to 8; `Highest E allocated` 06 -> 08.

- 2026-09-08 to-review (opencode its_direct/pt3-claude-opus-5-1m-us): Authored from backlog item `wjl471`. GATE NOTE: this item carries NO `- Blocks-Release:`, so this plan inherits none. THIS IS ORDER 02 of a two-child Set; sibling `kbqpkn` (Order 01) wires the four unwired gates and is independent. THE HARD ORDERING CONSTRAINT IS REAL, IS THE ITEM'S OWN, AND IS NOW SATISFIABLE: the item states that the retry sentence in the ruled wording is DESCRIPTIVE and "MUST NOT ship before `egqt32` layer 1 (the bounded single retry in `git_commit_helper.offer_commit`) lands, or the contract installed into every managed repo would document behavior the code does not have." `egqt32` has since been GRADUATED and its plan exists: `.aw/records/plans/pending/20260908-hookretry-01-lqly9m-...ipd.md`, carrying `From-Backlog: egqt32`, so this plan declares `Item-Dependencies: executed:lqly9m` to make that constraint machine-readable rather than prose. NOTE FOR THE EXECUTOR, because it changes where the retry lives: `lqly9m` found that the item's premise about WHERE the retry goes is obsolete. `offer_commit` no longer performs the commit itself; it delegates to `commit_lock.commit_isolated`, so the retry lands there. The ruled wording says "it re-stages that path and retries ONCE", which stays TRUE of the tooled path as a whole regardless of which layer performs it, so the sentence needs no change, but E-05 must VERIFY the shipped behavior matches the sentence rather than assuming. THE PLAN-LESS GAP IS CONFIRMED LIVE at HEAD `a2e0438a`: `work_cmd.py:138` returns "a <plan> selector is required", so `aw commit` without a plan exits before doing anything. THE ITEM'S EVIDENCE THAT THE HELPER NEVER NEEDED A PLAN ALSO VERIFIES: `offer_commit` takes no plan argument and there are 8 call sites of it across the package that pass none, and `run_commit` uses the plan for exactly two things, the `Scope-Paths` refusal (`work_cmd.py:436-452`) and the engine validation (`:459-465`). CRUCIALLY the graceful-degradation path the item points at is real and is the shape a plan-less mode should reuse: when a plan declares no `Scope-Paths`, `run_commit` prints "note - plan declares no Scope-Paths allowlist; committing the requested paths without scope enforcement" (`:454-458`) and PROCEEDS, so plan-less is that same path with a reworded notice rather than a new code path.
- 2026-09-08 draft (opencode its_direct/pt3-claude-opus-5-1m-us): created.

## Goal

Make the ruled MUST compliable before making it mandatory. An agent told it MUST commit through `aw commit` needs a form of `aw commit` that fits every legitimate commit, and the contract must not describe a retry the code does not perform.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: make the MUST compliable

- [ ] E-01 Add a PLAN-LESS commit mode, so a backlog item, a spec edit or a typo fix has a tooled path. The item asks this be decided here and suggests a shape without prescribing it: `aw commit --no-plan -m <msg> -- <paths>`, or a sibling verb. IMPLEMENT `--no-plan` on the existing verb rather than a new verb, because the behavior is identical apart from two skipped checks and a second verb would duplicate the argument surface, the locking and the outcome reporting. Reuse the EXISTING graceful-degradation path rather than writing a new branch: when a plan declares no `Scope-Paths`, `run_commit` already prints a notice and proceeds without scope enforcement (`work_cmd.py:454-458`), so plan-less is that same flow with a reworded notice. The selector requirement to relax is at `work_cmd.py:138` ("a <plan> selector is required").
  ARGPARSE WILL SILENTLY EAT YOUR FIRST PATH IF YOU ADD THE FLAG NAIVELY, AND THIS IS MEASURED, NOT HYPOTHETICAL. `p_commit` declares a REQUIRED `plan` positional followed by `path_argv` with `nargs=argparse.REMAINDER` (`cli.py:1570-1588`). REMAINDER captures everything after the last positional is filled, so the obvious change (make `plan` `nargs='?'` and add a `--no-plan` `store_true`) produces two wrong parses, both reproduced in this review:
  * `aw commit --no-plan -m msg -- a.py b.py` yields `plan='a.py'`, `path_argv=['b.py']`. THE FIRST PATH IS CONSUMED AS A SELECTOR and the commit either fails on a bogus selector or, worse, commits only the remaining paths.
  * `aw commit y9vpvv --no-plan -- a.py` yields `no_plan=False` with `--no-plan` sitting inside the remainder, so the flag is IGNORED rather than refused.
  THE FIX IS THE PATTERN THE VERB ALREADY USES, so this is reuse rather than new machinery: `run_commit` already RECOVERS `-m`/`--message`/`--no-commit` that REMAINDER swallowed from the pre-`--` segment (`work_cmd.py:398-414`) and `_split_remainder` already recovers a swallowed `--dir` (`work_cmd.py:77-124`). Recover `--no-plan` in exactly that pre-`--` scan, and take the PATH LIST only from after the `--` marker (which `_split_remainder` already returns), never from the `plan` positional. Whatever shape you choose, the acceptance test is behavioral: with two or more paths after `--`, EVERY path must reach `offer_commit` and NONE may be interpreted as a selector.
  - Depends on: none
  - Expected outcome: `aw commit --no-plan -m <msg> -- <paths>` commits ALL the named paths through `offer_commit` with no plan, printing a notice that scope enforcement is skipped; omitting BOTH a plan and `--no-plan` still refuses as today.
  - Execution state: pending

- [ ] E-02 Keep the two plan-derived protections OFF only where they cannot apply, and say so out loud. `run_commit` uses the plan for exactly two things: the `Scope-Paths` refusal (`work_cmd.py:436-452`) and the engine validation of the plan itself (`:459-465`). Both are meaningless without a plan, so `--no-plan` skips both, and that is a REDUCTION in safety that must be visible rather than silent: print which protections were skipped, in the same spirit as the existing no-Scope-Paths notice. CRITICALLY, do NOT let `--no-plan` weaken the protection that has nothing to do with plans: `offer_commit`'s snapshot-then-intersect is what structurally prevents sweeping a concurrent agent's staged edits into your commit, and the item is explicit that this is "the ONLY mechanism in the repo that STRUCTURALLY prevents" that. Plan-less mode must still route through `offer_commit` with explicit paths, never through a raw `git commit`.
  DO NOT PUT THE LITERAL STRING `git commit` IN THAT NOTICE, because an existing test forbids it in this function's source. `tests/test_work_primitives.py:236-243` (`test_commit_delegates_to_shared_helper`) reads `inspect.getsource(work_cmd.run_commit)` and asserts `"git commit" not in src`, whose purpose is to prove the verb never forks its own commit path. VERIFIED in this review that the string is absent today and that appending a notice containing it makes the assertion FAIL. So word the notice around the protections rather than the raw command (for example "no plan: skipping Scope-Paths enforcement and plan validation"), and do NOT weaken or delete that test to make room: its exactness is what keeps a future editor from reintroducing a forked commit path.
  - Depends on: E-01
  - Expected outcome: the skipped protections are named in the output; a co-worker's staged file is still excluded from a `--no-plan` commit, demonstrated; and `test_commit_delegates_to_shared_helper` still passes unmodified.
  - Execution state: pending

- [ ] E-03 Refuse the AMBIGUOUS invocation rather than guessing. Decide and implement what happens when a plan selector AND `--no-plan` are both given: that is a contradiction, so it must be a usage error with exit 2, not a silent precedence rule, for the same reason the sibling `--commit`/`--no-commit` pair is registered as an `add_mutually_exclusive_group` (`cli.py:733-746`). Also decide the `-m` requirement: `aw commit <plan>` derives its message from the plan (`work_cmd.py:469`, `f"work: {plan_rel}"`), so a plan-less commit has no message source and `-m` must be REQUIRED under `--no-plan`; refuse without it rather than committing with a placeholder.
  ARGPARSE'S OWN MUTUAL-EXCLUSION CANNOT ENFORCE THIS, so implement the refusal in the handler. A `add_mutually_exclusive_group` pairs two OPTIONS; here one side is a POSITIONAL that REMAINDER may have swallowed (see E-01's measurements), so argparse never sees the conflict. Detect it in `run_commit` after E-01's recovery scan, and return 2 with a message naming both tokens.
  - Depends on: E-01
  - Expected outcome: plan plus `--no-plan` exits 2 (including the shape where the flag arrives after the selector and had to be recovered from the remainder); `--no-plan` without `-m` exits 2; both refusals name the problem.
  - Execution state: pending

### Task group 2: land the ruled wording, in order

- [ ] E-04 Replace the `engine.py` paragraph with the maintainer's RULED WORDING VERBATIM, and treat it as dictated text rather than as a draft to improve. The target is the "PREFER THE TOOLED COMMIT PATH" paragraph (`agent_workflows/engine.py:1252-1256`, rendered at `AGENTS.md:60`), whose current "immune to this by construction" claim the item shows is true for index pollution and FALSE for auto-fix rejection. The ruled replacement is quoted verbatim in the backlog item at `wjl471:132-138` and must be used as given: it opens "USE THE TOOLED COMMIT PATH. You MUST commit through `aw commit`, not raw `git commit`." and ends with the reporting obligation "If no form of `aw commit` fits your case, you MUST say so explicitly in your report, naming what you ran and why, and re-verify the staged set as above." PRESERVE THE ESCAPE HATCH AS A REPORTING OBLIGATION, NOT PERMISSION: the item is explicit that this is what makes it enforceable, because a silent raw `git commit` becomes a visible contract violation while a declared one is auditable. Regenerate `AGENTS.md` through `engine.py`; never hand-edit the managed block.
  DO NOT REGENERATE UNTIL E-07 HAS RECONCILED THE MANIFEST, or your edit is SILENTLY DISCARDED. Measured in this review, not inferred: `merge_aw_block` runs each section through `_apply_section_consent`, which PRESERVES the on-disk body instead of writing ours when the manifest holds a recorded hash for that section that does not match disk. `.aw/system/managed-sections.json` records `AGENTS.md#aw:pointer` as `a9deb5a1...` while the live on-disk body hashes `8baff639...`, so the drift rule FIRES TODAY. Calling `merge_aw_block` with a sentinel line appended to the pointer section returned `action='refreshed'` with the sentinel ABSENT and a three-blank-line diff, which reads as "already current" rather than "your edit was thrown away". So E-07 is a hard prerequisite of this item, and the completion criterion for THIS item is that the new wording is present in the FILE, never merely in the generator.
  - Depends on: E-01, E-02, E-03, E-07
  - Expected outcome: `engine.py` carries the ruled wording verbatim and `AGENTS.md`'s managed block RENDERS it on disk; the reporting-obligation sentence is present; `tests/test_shared_checkout_contract.py::NoDriftTests::test_repo_agents_block_equals_generated` still passes, which is the standing proof the file equals the generator.
  - Execution state: pending

- [ ] E-05 VERIFY THE DESCRIPTIVE SENTENCE IS TRUE BEFORE SHIPPING IT, which is the hard ordering constraint made concrete. The ruled wording asserts "When a MUTATING hook rewrites one of your own paths and rejects, it re-stages that path and retries ONCE, so whitespace or format churn costs you no round trip and no re-verify." That is DESCRIPTIVE, and the item forbids shipping it before the retry exists. This plan declares `Item-Dependencies: executed:lqly9m` for that reason. At execution time, PROVE the behavior rather than trusting the dependency: run the scratch reproduction (a hook that strips trailing whitespace and exits nonzero) through `aw commit` and show it succeeds on the retry with the rewritten path included. IF THE RETRY IS NOT PRESENT, DO NOT SHIP THE SENTENCE: land the MUST imperative (which the item says carries no such dependency) and hold the retry sentence, reporting why. Note `lqly9m` places the retry inside `commit_lock.commit_isolated` rather than `offer_commit`, so verify by BEHAVIOR through the tooled path, not by grepping one function.
  THE WITHHOLDING MECHANICS ARE SPECIFIED HERE so the fallback is not left to judgement. The ruled paragraph is FIVE sentences and the descriptive one is the THIRD. To withhold it, DELETE THAT ONE SENTENCE ENTIRELY and ship the other four unchanged; do NOT soften it into a "will retry" or a "should retry", because a hedged description of absent behavior is the same untruth the ordering constraint exists to prevent. Then record the verbatim text you shipped in V-05's evidence, so a later reader can tell which of the two outcomes occurred without re-deriving it.
  ALSO CHECK WHETHER `lqly9m` CHANGED WHAT THE SENTENCE SHOULD SAY. Its review added an emptiness probe: when the hook's fix erases the whole diff, the tooled path returns `nothing-to-commit`, not a commit. The ruled sentence's claim ("costs you no round trip") stays true for the ordinary case, but if you observe the empty-diff case in your reproduction, report it rather than presenting it as the retry succeeding.
  - Depends on: E-04
  - Expected outcome: either the retry is demonstrated and the full five-sentence ruled wording ships, or the retry is absent, the third sentence is DELETED, the other four ship, and the shipped text is recorded verbatim.
  - Execution state: pending

- [ ] E-06 Apply the COMPANION NARROWING the ruling also specifies, to the paragraph immediately above. The target is "RE-VERIFY AFTER A FAILED HOOK" (`agent_workflows/engine.py:1246-1251`, rendered at `AGENTS.md:58`), which currently charges the re-verify after EVERY failed attempt including tooled ones, and never mentions that a mutating hook also REWROTE the file. Retitle it to "RE-VERIFY AFTER A FAILED RAW COMMIT", scope it to raw `git commit`, and add the one specified sentence: "A MUTATING hook (whitespace or eof fixer, formatter) also REWRITES your file and then rejects, so your retry must re-stage the rewritten path." KEEP THE `git reset`/`git stash` PROHIBITION VERBATIM: the ruling is explicit that it "stays VERBATIM; it is about a co-worker's work and is unrelated to this change." That prohibition protects a concurrent agent and must not be softened while narrowing the paragraph's scope.
  - Depends on: E-05
  - Expected outcome: the paragraph is retitled and scoped to raw commits with the specified sentence added, and the reset/stash prohibition is byte-identical.
  - Execution state: pending

### Task group 3: make the regeneration actually land, and guard it

- [ ] E-07 RECONCILE THE MANAGED-SECTION MANIFEST BEFORE REGENERATING, because the drift-preserve rule would otherwise throw E-04's and E-06's edits away with no visible failure. This is a PREREQUISITE of E-04, not cleanup afterwards. The mechanism: `merge_aw_block` calls `_apply_section_consent`, which for each section compares the ON-DISK body against `manifest.recorded_hash(f"{file_key}#aw:{slug}")` and, on a MISMATCH, appends the DISK section and skips ours (the "user drift" rule, so a hand-edit is never clobbered). MEASURED STATE AT REVIEW: the manifest records `AGENTS.md#aw:pointer` = `a9deb5a1d76e603e6c016d5ea0c2f676010bd4910297ffa16aa5b52647897fc4`, the live body hashes `8baff639f14d9139b0f4ac76bf1f60e09751282e202d4801d7e33b43f56af7a8`, `matches_recorded` returns FALSE, and a sentinel-bearing `merge_aw_block` call returned `action='refreshed'` with the sentinel absent. Note also that `AGENTS.md#aw:reporting` has NO recorded hash at all, so that sibling section takes the write path today; the defect is specific to `pointer`, which is the section both E-04 and E-06 edit.
  DIAGNOSE BEFORE YOU ACT, and prefer the least-privileged fix. The stale record means the on-disk body legitimately differs from what the installer last recorded (the file currently EQUALS the generator, verified, so this is a stale RECORD rather than a real hand-edit). Re-record the section hash for what is genuinely ours, or pass `manifest=None` to the narrow writer for this one regeneration and then re-record, whichever you can demonstrate. Do NOT run a full `aw install .` to sidestep it: its declared Target Delta is `.aw/system/`, `.aw/config/project.json` and `.aw/state/durable`, far wider than `Scope-Paths`, and other agents hold lanes in this checkout. If you cannot make the write land without touching a file outside `Scope-Paths`, STOP and report rather than widening scope; that is a genuinely unsafe condition, not a scope question.
  - Depends on: none
  - Expected outcome: a demonstrated regeneration in which a KNOWN-NEW sentence is present in `AGENTS.md` on disk afterwards, proving the drift rule no longer discards the write.
  - Execution state: pending

- [ ] E-08 Add the test file the plan's validation section already assumes, and note that it DOES NOT EXIST TODAY: `tests/test_work_commit.py` is absent from the tree (verified), so this creates it rather than extending it. Cover the behaviors E-01 through E-03 introduce, with the argv shapes E-01 measured as the hazard: a two-or-more-path `--no-plan` commit where every path lands in the commit and none is read as a selector; the flag-after-selector shape refused with exit 2; `--no-plan` without `-m` refused with exit 2; and the no-plan-and-no-flag case still refused as today. FOLLOW THE EXISTING PATTERN rather than inventing a harness: `tests/test_work_primitives.py:189-243` already drives `aw commit` end to end against a scratch repo (`self._run(["commit", "wk0001", "--dir", ...])`) and already asserts the out-of-scope refusal and the shared-helper delegation. Extend that file where a new case belongs beside an existing one; put the plan-less surface in the new file.
  - Depends on: E-03
  - Expected outcome: a new `tests/test_work_commit.py` whose cases fail against the pre-change code and pass after, with each of the four shapes above asserted on EXIT CODE and on the resulting commit's file list.
  - Execution state: pending

## Project conventions discovered (Step 0)

- `AGENTS.md` is GENERATED from `engine.py`'s managed block and must never be hand-edited; the block is installed into every managed repo, which is why a descriptive claim there is a claim about every consumer's tooling, not just this repo's.
- THE GENERATED BLOCK IS SECTIONED and consent-gated per section. The two sections are `aw:pointer` (which holds both paragraphs this plan edits) and `aw:reporting`; `merge_aw_block` will PRESERVE an on-disk section instead of writing yours when the manifest's recorded hash for it disagrees with disk. That rule currently FIRES for `pointer`, which is why E-07 exists.
- `offer_commit` takes NO plan argument and is used plan-lessly at 6 of its 7 in-package call sites (`plans_archive.py:294`, `research_archive.py:399`, `oc_runipd.py:1414`, `status_set.py:1146`, `cli.py:4830`, `specs.py:697`; the seventh, `work_cmd.py:470`, is the plan-bearing one), so a plan-less mode is a CLI-surface gap rather than a helper limitation.
- `aw commit`'s argument surface is REMAINDER-based, so any new flag must be recoverable from the pre-`--` segment the way `-m`, `--no-commit` and `--dir` already are. This is the single largest implementation hazard in the plan; see E-01.
- `tests/test_work_primitives.py` asserts `"git commit"` never appears in `run_commit`'s source, so any new user-facing notice inside that function must avoid the literal string.
- `CONTRIBUTING.md` does NOT duplicate the commit contract: at `:179` it POINTS at the managed block as the canonical home (D69, and GUIDING_PRINCIPLES P8). Checked in review, so it correctly stays out of `Scope-Paths` and needs no edit.
- `run_commit` already degrades gracefully when a plan declares no `Scope-Paths`: it prints a notice and proceeds without scope enforcement. That is the precedent shape for `--no-plan`.
- `offer_commit`'s snapshot-then-intersect is the ONLY structural protection against sweeping a concurrent agent's staged edits into your commit, so no mode may bypass it.
- The maintainer's ruling distinguishes IMPERATIVE from DESCRIPTIVE text: a MUST is a command and its validity does not wait on the tooling being total, while a descriptive sentence must be true when it ships. That distinction drives the E-04/E-05 split.
- The ruling also states the truthfulness constraint applies ONLY to descriptive sentences, which is why the MUST may land even if the retry is delayed.
- `egqt32`'s retry moved layer since the ruling was written: `offer_commit` now delegates the commit to `commit_lock.commit_isolated`, so the retry lands there. Verify by behavior through the tooled path.

## Findings

| # | Finding | Evidence |
|---|---|---|
| F-1 | THE PLAN-LESS GAP IS LIVE: `aw commit` refuses without a plan selector, so a plan-less commit has no tooled path. | `work_cmd.py:138` ("a <plan> selector is required") at `a2e0438a` |
| F-2 | The helper never needed a plan: `offer_commit` takes no plan argument. CORRECTED IN REVIEW: there are 7 in-package call sites, of which 6 are plan-less; the count of 8 was wrong and included the plan-bearing one. | re-counted at `26644c26`; `work_cmd.py:470` is the plan-bearing site |
| F-3 | The plan is used for exactly TWO things, both meaningless without it: the `Scope-Paths` refusal and the plan's engine validation. | `work_cmd.py:436-452`, `:459-465` |
| F-4 | The graceful-degradation precedent exists and is the shape to reuse: a plan with no `Scope-Paths` prints a notice and proceeds. | `work_cmd.py:454-458` |
| F-5 | The paragraph to replace still carries the claim the item shows is half-false. | `engine.py:1252-1256`, rendered `AGENTS.md:60` |
| F-6 | The companion paragraph still charges the re-verify after EVERY failed attempt and never mentions the rewrite. | `engine.py:1246-1251`, rendered `AGENTS.md:58` |
| F-7 | THE ORDERING PREREQUISITE IS NOW GRADUATED, so the constraint is satisfiable: `egqt32` has plan `lqly9m` carrying `From-Backlog: egqt32`. | `.aw/records/plans/pending/20260908-hookretry-01-lqly9m-...ipd.md` |
| F-8 | The retry's LAYER moved since the ruling was drafted, so E-05 must verify by behavior: `offer_commit` delegates the commit to `commit_lock.commit_isolated`, and `lqly9m` places the retry there. | `lqly9m`'s Concern and workflow history |
| F-9 | The MUST imperative carries no dependency and could ship alone, which is the fallback if the retry is absent. | the ruling's own words: "The MUST IMPERATIVE itself carries no such dependency and could ship immediately" |
| F-10 | Making non-compliance the common case is a recorded harm, which is why E-01 precedes E-04: a rule that fires on correct behavior trains agents to ignore it. | the item's citation of `gjadwm:45-49` |
| F-11 | **THE REGENERATION IS SILENTLY DISCARDED TODAY.** `_apply_section_consent` preserves the on-disk body when the manifest's recorded hash for a section disagrees with disk. The manifest records `AGENTS.md#aw:pointer` = `a9deb5a1...`; the live body hashes `8baff639...`; `matches_recorded` is FALSE. A `merge_aw_block` call with a sentinel appended to the pointer section returned `action='refreshed'` with the sentinel ABSENT and only a three-blank-line diff. Addressed by new E-07. | measured in review; `.aw/system/managed-sections.json`, `engine.py:1710-1747` |
| F-12 | **A NAIVE `--no-plan` FLAG SWALLOWS THE FIRST PATH.** With `plan` relaxed to `nargs='?'` beside `path_argv` (`nargs=REMAINDER`), `aw commit --no-plan -m msg -- a.py b.py` parses `plan='a.py'`, `path_argv=['b.py']`; and `aw commit y9vpvv --no-plan -- a.py` leaves `no_plan=False` with the flag stranded in the remainder. Addressed by E-01's recovery requirement. | measured in review over five argv shapes; `cli.py:1570-1588` |
| F-13 | An existing test forbids the literal `git commit` in `run_commit`'s source, so E-02's notice wording is constrained. Verified absent today; verified that adding it fails the assertion. | `tests/test_work_primitives.py:236-243` |
| F-14 | **THE RULED WORDING CONTRADICTS THE NEXT PARAGRAPH OF THE SAME BLOCK**, which says agents MUST commit `path-scoped (git commit -m msg -- <path>)`. The same instruction is repeated at four driver-prompt sites. Landing the MUST beside it ships a block mandating both a tooled commit and a raw one. Raised as blocking OQ-03. | `engine.py:1259` rendered `AGENTS.md:63`; also `oc_runipd.py:2607`, `:4963`, `agy_runipd.py:1794`, `:2506` |
| F-15 | `tests/test_work_commit.py`, named in `Scope-Paths` and in the validation section, DOES NOT EXIST; it is a new file. The existing end-to-end harness for this verb is `tests/test_work_primitives.py:189-243`. | verified absent at `26644c26` |
| F-16 | THE SUITE BASELINE IN THIS PLAN WAS WRONG in both count and named failure. Actual bare run: `2 failed, 5918 passed, 3 skipped, 2 xfailed`. Failure 1 is ENVIRONMENTAL (`test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, tripped by 189 gitignored `opencode-recovery/*.md` files belonging to another party). Failure 2 is a 30-second subprocess TIMEOUT (`test_runner_backlog_close.py::ShutdownReportOnInterrupt`). `test_orchestrator_retirement`, which the plan named, PASSES. | bare `python3 -m pytest` at `26644c26` |
| F-17 | The `Item-Dependencies: executed:lqly9m` edge is UNSATISFIED: `lqly9m` is `- Status: reviewed` with `Readiness: go-pending-approval`, so it is neither approved nor executed. A runner will mark this item `dependency-blocked` and continue, which is designed behavior, not a defect. | `lqly9m`'s front matter, read at review |

## Proposed changes (ordered, validatable)

1. Add `--no-plan`, recovering the flag from the REMAINDER as the verb already does for `-m`/`--dir`, reusing the existing no-Scope-Paths flow (E-01).
2. Name the skipped protections, without the literal `git commit` string, and keep the concurrency protection intact (E-02).
3. Refuse plan-plus-`--no-plan` and `--no-plan` without `-m`, in the handler rather than via argparse (E-03).
4. Reconcile the managed-section manifest so a regeneration is not drift-preserved away (E-07).
5. Replace the PREFER paragraph with the ruled MUST wording verbatim and regenerate AGENTS.md (E-04).
6. Prove the retry exists before shipping the descriptive sentence, or delete that one sentence (E-05).
7. Apply the companion narrowing, keeping the reset/stash prohibition verbatim (E-06).
8. Add `tests/test_work_commit.py` covering the four plan-less argv shapes (E-08).

## Deferred / out of scope (with reason)

- THE AGENT-CONTEXT DETECTOR AND THE NEW GUARD (the item's Finding 1, OQ-1 through OQ-4). A separate design with four open questions about where the guard lives, what accident it catches, how it phrases refusal, and how an override is audited. This plan implements the maintainer's WORDING ruling and the gap that ruling exposed, nothing more.
- WIRING THE FOUR UNWIRED GATES. Sibling Order 01 (`kbqpkn`) owns that; it is independent and needs no contract change.
- `mjx7ne`'s `commit_gateway` / `deny_push` CAPABILITIES. Separate open item.
- ENFORCING the MUST mechanically (intercepting raw `git commit`). The ruling makes the escape hatch a REPORTING obligation deliberately, because that is enforceable where an absolute MUST with no exit is not. Building an interceptor is the guard question, deferred above.
- CHANGING THE `git reset`/`git stash` PROHIBITION. The ruling says it stays verbatim; E-06 preserves it.

## Scope check

- Over-scope: `AGENTS.md` is in `Scope-Paths` although it is GENERATED. It is listed because the regenerated file is committed with the change; the edit itself must be made in `engine.py` and the file regenerated, never hand-edited.
- Under-scope: the guard, the detector and the gate wiring are all out. This plan delivers the contract half plus the tooling gap it depends on.
- ADDED IN REVIEW: `tests/test_work_primitives.py` was NOT declared although E-02's notice interacts with an assertion in it and E-08 may extend it beside an existing case. Now declared. If the executor finds the notice can be worded without touching that file at all, leave it unmodified and ACK it as declared-but-unmodified through `aw ipd finalize --scope-ack`; that is the designed handling, not a defect.
- POSSIBLE OUT-OF-SCOPE EDIT, NAMED IN ADVANCE: E-07 may need to write `.aw/system/managed-sections.json` to reconcile the stale section hash. That path is deliberately NOT added to `Scope-Paths`, because whether it is needed depends on which fix E-07 demonstrates (re-record versus a `manifest=None` narrow write). If it IS written, justify it at finalize with `--scope-reason`; do not pre-declare a write that may not happen.
- DELIBERATELY NOT IN SCOPE: the four driver-prompt sites that repeat the raw-`git commit` instruction (`oc_runipd.py:2607`, `:4963`, `agy_runipd.py:1794`, `:2506`). They are the runner-path twin of the contradiction OQ-03 raises, and editing them is not authorized by the ruling. If OQ-03 resolves toward reconciling the wording, they enter scope THEN, with `Scope-Paths` amended before the work.

## Required tests / validation

- `python3 -m pytest` bare, per the repository contract. Paste the ACTUAL summary line. BASELINE MEASURED IN REVIEW at `26644c26`: `2 failed, 5918 passed, 3 skipped, 2 xfailed`. Both failures are pre-existing and NOT this plan's: `test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` is ENVIRONMENTAL (189 gitignored `opencode-recovery/*.md` files owned by another party contain the contract prose), and `test_runner_backlog_close.py::ShutdownReportOnInterrupt::test_sigint_produces_the_report_and_exits_130` is a 30-second subprocess timeout under load. The earlier figure in this plan (`1 failed, 5648 passed`, blamed on `test_orchestrator_retirement`) was WRONG in both count and cause; `test_orchestrator_retirement` passes. Judge on the DELTA, and expect additional environmental failures inside a lane worktree from tests that read live repo state.
- `python3 -m pytest tests/test_work_commit.py tests/test_work_primitives.py tests/test_git_commit_helper.py` for the focused surface. Note `tests/test_work_commit.py` is CREATED by E-08 and does not exist before this plan runs.
- `python3 -m pytest tests/test_shared_checkout_contract.py` specifically for `NoDriftTests::test_repo_agents_block_equals_generated`, which is the standing proof that the regenerated file equals the generator and therefore the cheapest detector of an E-07 failure.
- A real `aw commit --no-plan` against a fixture repository, committing a file with no plan, with the resulting commit and its file list pasted.
- The concurrency check from E-02: a co-worker's staged file present, a `--no-plan` commit of different paths, and proof the co-worker's file is not in the commit.
- The retry demonstration from E-05 through the tooled path, or the recorded reason the sentence was withheld.
- A diff of the regenerated `AGENTS.md` managed block showing both paragraphs changed as ruled, PLUS the E-07 proof that the write was not drift-preserved away (a near-empty diff is a FAILURE signal here, not idempotency).

## Spec / documentation sync

This plan's PRIMARY DELIVERABLE IS DOCUMENTATION, so the sync is the work rather than an afterthought. `agent_workflows/engine.py`'s managed block is the source of `AGENTS.md` and is installed into every managed repo, so both edits must be made there and the file regenerated; hand-editing `AGENTS.md` would be reverted by the next install and would also violate the repository's own rule that the block is generated. No `.spec.md` file governs the commit contract, so none is declared in `Scope-Paths`. TWO honesty obligations. FIRST, the wording is DICTATED by a recorded maintainer ruling: reproduce it verbatim and do not improve it, because a reworded ruling is no longer the ruling. SECOND, the descriptive retry sentence must be true at ship time (E-05); if it is not, delete that one sentence and record the omission, since the whole reason the item states a hard ordering constraint is to stop the contract describing behavior the code lacks.

`CONTRIBUTING.md` WAS CHECKED IN REVIEW AND NEEDS NO EDIT, so it is deliberately NOT in the fence: at `:179` it does not restate the commit rule but POINTS at the managed block as the canonical home ("lives in the managed `AGENT-WORKFLOWS` block in `AGENTS.md`. That block is the canonical home; this file and the `.aw/records/plans` README point at it (D69)"), which is exactly what GUIDING_PRINCIPLES P8 requires. It contains no "PREFER" wording about the commit path. The executor should NOT re-derive this; if `CONTRIBUTING.md` turns out to need a change, that is a scope finding to report, not a silent widening.

THE UNRESOLVED SYNC PROBLEM IS INSIDE THE BLOCK ITSELF, and it is OQ-03. The paragraph immediately AFTER the one E-04 replaces tells agents they MUST commit `path-scoped (git commit -m msg -- <path>)` (`engine.py:1259`), and four driver-prompt sites repeat that instruction verbatim (`oc_runipd.py:2607`, `:4963`, `agy_runipd.py:1794`, `:2506`). The ruling did not authorize touching any of them, so this plan does not, and the maintainer must say how the two are reconciled before the contract can be internally consistent.

## Open questions

### OQ-01: Should `--no-plan` be a flag on `aw commit` or a sibling verb?

- Blocking: no
- Status: open
- Owner: reviewer
- Resolution or deferral rationale: DECIDED IN THE PLAN as a flag, with the reasoning recorded so a reviewer can overrule cheaply. The item offers both shapes. A flag wins because the two paths differ only in skipping two plan-derived checks: everything else (path scoping, the writer lock, the isolated commit, the outcome reporting, the trailers) is identical, so a sibling verb would duplicate the whole argument surface and create two places for a future commit-path change to land. The counter-argument a reviewer may prefer: a distinct verb makes the reduced-safety mode impossible to reach by accident, whereas a flag can be added to a habitual command without thought. If that matters more, the implementation is equivalent work either way.

### OQ-02: Should a plan-less commit be RECORDED as an exception even after `--no-plan` exists?

- Blocking: no
- Status: open
- Owner: maintainer
- Resolution or deferral rationale: DEFERRED, leaning NO, and it decides how the ruled wording reads in practice. The ruling's escape hatch is a reporting obligation for when "no form of `aw commit` fits your case". Once `--no-plan` exists, a plan-less commit IS a form of `aw commit`, so it is compliant and needs no exception report; that is precisely the gap-closing the ruling asks for. The alternative view: plan-less mode skips scope enforcement, so a maintainer may want its use surfaced in reports anyway to see whether plan-less commits are becoming a habit. If that is wanted, it is a reporting convention rather than a code change, and it should be stated in the wording rather than left implicit.

### OQ-03: The ruled MUST contradicts the next paragraph of the same block, which mandates raw `git commit`. How is that reconciled?

- Blocking: yes
- Status: resolved
- Owner: maintainer
- Finding: PR-904
- Resolution or deferral rationale: RESOLVED BY THE MAINTAINER 2026-09-10 (`/askme`): TAKE SHAPE (a), AND FIX THE FOUR DRIVER-PROMPT SITES IN THIS PLAN. The execution-contract paragraph is reworded to state the REQUIREMENT (commit only files you changed, limited to named paths) WITHOUT showing raw `git commit` as the method, so `aw commit` remains the single stated route; the four driver prompts (`oc_runipd.py:2607`, `:4963`, `agy_runipd.py:1794`, `:2506`) get the same treatment, because that is where a runner-executed agent actually reads its instructions. The maintainer additionally ruled the shipped text must present the tooled command IN BOTH ITS FORMS as required: `aw commit <plan> -- <paths>` as the normal path and the new `--no-plan` form for everything else, leaving no commit without a tooled route and no residual reason to reach for raw git except the September ruling's reporting-obligation escape hatch. That escape hatch STANDS as the last resort and must survive the rewording verbatim in substance.
  THIS EXCEEDS THE 2026-09-01 RULING, WHICH IS WHY IT WAS ASKED, and the authorization is now explicit: that ruling dictated a replacement for ONE paragraph and froze the `git reset`/`git stash` prohibition VERBATIM, saying nothing about the execution-contract paragraph or the driver prompts. This answer extends the mandate to both. The frozen prohibition remains untouched: nothing here authorizes editing it.
  THE MAINTAINER'S OWN QUESTION IS THE SUBSTANCE OF THIS ANSWER, and it is recorded because it nearly went unnoticed: told that `aw commit` requires a plan, they asked "How do we handle non-plan-based commits?" That is the load-bearing objection to an absolute MUST, and it is ALREADY ANSWERED BY THIS PLAN'S OWN E-01/E-02 rather than needing a new decision. Confirmed at HEAD `fdb6d639`: `aw commit` refuses without a selector, and `--no-plan` does not yet exist (`aw commit --no-plan -m x -- README.md` fails argument parsing). E-01 adds that mode and E-02 keeps `offer_commit`'s snapshot-then-intersect (the only structural protection against sweeping a co-worker's staged file into a commit) ACTIVE in plan-less mode while skipping only the two plan-derived checks and naming them out loud. So the ordering constraint is internal: the reworded text may not ship claiming a universal tooled path until E-01 lands in the same change, which it does.
  MY EARLIER FRAMING WAS NOT ACCEPTED AS SUFFICIENT, and the record should not read as if it were. I argued the two MUSTs were reconcilable on the merits (the tooled verb IS a path-scoped commit, so shape (b) loses nothing but clarity); the maintainer went further and required the raw command removed from the instruction rather than merely tolerated, on the reasoning that non-plan commits are precisely the ones most exposed to the shared-checkout hazard, so leaving them on the raw path would leave the riskiest case unprotected.
  A PROMPT-COMPOSITION FAILURE OCCURRED HERE AND IS RECORDED FOR `t156g1`. The first `/askme` prompt said "the toolkit's commit command" without naming `aw commit`, and the maintainer replied "What is the toolkit's 'commit command'?" The question had to be re-asked with the verb named, its actual behavior described, and its plan requirement measured. This is the same class of defect `t156g1` was filed for: the prompt passed every mechanical check and was still unintelligible because it referred to a thing by description instead of by name.
  ORIGINAL REVIEW REASONING RETAINED BELOW, since it establishes the conflict is real rather than stylistic. RAISED IN REVIEW, NOT RESOLVABLE FROM THE REPOSITORY, because the two texts are both live maintainer instructions and choosing between them is the maintainer's call. THE CONFLICT, measured: E-04 replaces the paragraph at `engine.py:1252-1256` with "You MUST commit through `aw commit`, not raw `git commit`." The VERY NEXT paragraph in the same managed block, `### Agent execution contract` at `engine.py:1258-1259` (rendered `AGENTS.md:63`), opens "When you execute a task or plan here you MUST: commit ONLY files you changed, path-scoped (`git commit -m msg -- <path>`)". After E-04 the installed block would carry two MUSTs, one naming the tooled verb and one naming the raw command, three lines apart, in the text installed into EVERY managed repo. The same raw-command instruction is repeated at four driver-prompt sites (`oc_runipd.py:2607`, `:4963`, `agy_runipd.py:1794`, `:2506`), which is where a runner-executed agent actually reads it, so leaving it unaddressed means the runner path keeps instructing the opposite of the contract. WHY THIS PLAN DOES NOT JUST FIX IT: the 2026-09-01 ruling dictates a replacement for ONE paragraph and explicitly rules that the `git reset`/`git stash` prohibition in the neighbouring paragraph "stays VERBATIM". It says nothing about the execution-contract paragraph, and silently rewording a second dictated paragraph (or four driver prompts) would exceed what was ruled, which is the same error as rewording the ruling itself. THE THREE SHAPES A DECISION COULD TAKE, for the maintainer's convenience and not as a preference: (a) narrow the execution-contract paragraph to say path-scoped-commit is the REQUIREMENT and `aw commit` is how you satisfy it, keeping one MUST; (b) leave both and accept the redundancy on the reading that `aw commit` IS a path-scoped commit, so neither is false, only confusing; (c) treat the driver prompts as a separate follow-up item and land only the block change now. Whichever is chosen, this plan should carry it rather than a successor, because it is the plan editing that block.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: paste a real `aw commit --no-plan -m <msg> -- <path>` run in a fixture repository, its UNPIPED exit code, the printed notice, and `git show --stat` of the resulting commit showing exactly the named path. THEN paste the MULTI-PATH case, which is the one that fails on a naive implementation: `aw commit --no-plan -m <msg> -- <pathA> <pathB>` with `git show --stat` proving BOTH paths are in the commit and that neither was consumed as a selector. Paste `aw commit` with NEITHER a plan nor `--no-plan` still refusing, so the relaxation is shown to be opt-in.
  - Observed evidence:
  - Result: pending

- [ ] V-02 validates E-02
  - Required evidence: paste the output naming the skipped protections. Then paste the concurrency demonstration: a co-worker's file staged, a `--no-plan` commit of DIFFERENT paths, `git show --stat` proving the co-worker's file is absent, and `git status --porcelain` proving it is still staged/dirty afterwards. ALSO paste `python3 -m pytest tests/test_work_primitives.py -o addopts=""` showing `test_commit_delegates_to_shared_helper` PASSING and confirm the test body was not modified, since the notice wording must not reintroduce the forbidden string.
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: paste THREE refusals with their UNPIPED exit codes and messages: a plan selector BEFORE `--no-plan`, a plan selector AFTER `--no-plan` (the shape where the flag had to be recovered from the remainder), and `--no-plan` without `-m`. All must be 2, and each message must name the problem.
  - Observed evidence:
  - Result: pending

- [ ] V-04 validates E-04
  - Required evidence: paste the `engine.py` diff and the regenerated `AGENTS.md` diff side by side, showing the ruled wording reproduced VERBATIM including the reporting-obligation sentence. Paste a character-level comparison (or a diff against the quoted text at `wjl471:132-138`) proving it was not paraphrased. Confirm `AGENTS.md` was regenerated, not hand-edited, and paste `python3 -m pytest tests/test_shared_checkout_contract.py -o addopts=""` showing `test_repo_agents_block_equals_generated` passing, which proves file and generator agree.
  - Observed evidence:
  - Result: pending

- [ ] V-05 validates E-05
  - Required evidence: paste the scratch reproduction through the TOOLED path: a hook that rewrites a staged file and exits nonzero, then `aw commit` succeeding on the retry with the rewritten content in the commit. If the retry is ABSENT, paste that finding instead. IN EITHER CASE paste the SHIPPED paragraph verbatim and state explicitly whether it contains five sentences or four, so a reader can tell which outcome occurred without re-deriving it. State which outcome occurred.
  - Observed evidence:
  - Result: pending

- [ ] V-06 validates E-06
  - Required evidence: paste the retitled paragraph as rendered in `AGENTS.md`, showing it is scoped to raw `git commit` and carries the specified mutating-hook sentence. Paste a byte-comparison of the `git reset`/`git stash` prohibition before and after proving it is UNCHANGED. Paste the bare `python3 -m pytest` summary line and compare it to the baseline recorded in the validation section, naming any failure as pre-existing or new.
  - Observed evidence:
  - Result: pending

- [ ] V-07 validates E-07
  - Required evidence: paste the BEFORE state (the manifest's recorded hash for `AGENTS.md#aw:pointer`, the live body hash, and `matches_recorded` returning False), then paste the SENTINEL PROOF: regenerate with a known-new sentence and `grep` it out of the on-disk `AGENTS.md`. A near-empty diff is a FAILURE, so paste the diff stat too and state its line count. Finally paste `git status --porcelain` proving no file outside `Scope-Paths` was written; if the manifest itself had to be updated, name it explicitly and justify it as an out-of-scope edit through `aw ipd finalize --scope-reason`.
  - Observed evidence:
  - Result: pending

- [ ] V-08 validates E-08
  - Required evidence: paste `python3 -m pytest tests/test_work_commit.py -o addopts=""` with the per-test names and counts, and paste the FALSIFICATION: the same file run against the pre-change code (e.g. `git stash` of the `work_cmd.py`/`cli.py` edits, or a targeted revert) showing the new cases FAIL there. A test that passes both before and after proves nothing.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

This plan is `reviewed`. It must not be executed until a human sets it `approved` with `aw ipd set approved <plan>`.

IT ALSO CARRIES A BLOCKING OPEN QUESTION, OQ-03, which is a second and independent hold: `aw ipd lint` refuses a `Blocking: yes` question left `open` at every checkpoint, including `pre-execution`, so `aw ipd begin` will not start this plan until the maintainer answers. That is deliberate. The ruled wording contradicts the paragraph three lines below it in the same installed block, and shipping both would put two conflicting MUSTs into every managed repo. Approval alone does not clear it; the question must be answered and recorded first.

It carries `Item-Dependencies: executed:lqly9m` deliberately, encoding the backlog item's own hard ordering constraint: the ruled wording's retry sentence is DESCRIPTIVE and must not ship before the retry exists. E-05 re-proves that at execution time rather than trusting the edge, and provides the documented fallback (delete that one sentence, ship the other four) if the retry is absent. MEASURED AT REVIEW: `lqly9m` is `reviewed` and awaiting approval, so the edge is currently UNSATISFIED; a runner will mark this item `dependency-blocked` and continue, which is designed behavior.

Execution contract for whoever runs it: commit ONLY the paths listed in `Scope-Paths`, path-scoped (`git commit -m msg -- <paths>`), never `git add -A`, never push. Run the bare suite (`python3 -m pytest`) and paste the ACTUAL summary line; do not claim a pass that was not run. Every `V-*` above demands pasted evidence, and a `V-*` may not be marked complete from the matching `E-*` checkmark. NOTE THREE HAZARDS. FIRST, `AGENTS.md` is GENERATED: edit `engine.py` and regenerate, never hand-edit, or the next install reverts it. SECOND, the regeneration is currently DRIFT-PRESERVED AWAY (F-11), so a clean diff after regenerating means your edit was DISCARDED, not that it was already current; E-07 exists for exactly that and must run before E-04. THIRD, this plan edits the commit contract and the commit verb it will itself use, so verify with `git diff --cached --name-only` before each commit and never reach for `--no-verify` to land a change whose purpose is to make the tooled path mandatory. When all validations carry real observed evidence and `aw ipd lint --phase pre-transition` conforms, move the plan to `.aw/records/plans/executed/` through `aw ipd finalize`, never with a raw `git mv` plus a hand-edited `- Status:`.
