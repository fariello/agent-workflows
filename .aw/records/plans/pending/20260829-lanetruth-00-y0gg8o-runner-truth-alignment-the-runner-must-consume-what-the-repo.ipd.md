# IPD: runner truth alignment: the runner must consume what the repository declares

- Date: 2026-08-29
- Kind: orchestrator
- Concern: Three shipped-runner defects in which `aw <host> run` acts on a DIFFERENT truth than the repository declares: it runs the lane branch's copy of the tooling instead of its own, it measures the wrong tree when deciding execution authority, and it cannot read the `Item-Dependencies` field that 11 pending plans declare.
- Scope: The runner's alignment with declared repository truth, in three independent children: (01) nested `aw` invocations must run the RUNNER's tooling, not the lane branch's checked-out copy; (02) `aw ipd begin`'s in-scope-dirty gate must measure the tree the turn will actually execute in; (03) runner preflight must consume the SHARED `Item-Dependencies` predicate instead of a private legacy regex. Excludes the session/worktree granularity defect (backlog xd9sll), already fixed in `c0e9599`, and excludes any change to the dependency GRAMMAR or the four non-runner surfaces that already consume it correctly.
- Scope-Paths: agent_workflows/oc_runipd.py, agent_workflows/agy_runipd.py, agent_workflows/ipd_lifecycle.py, tests/test_lane_tool_identity.py, tests/test_begin_dirty_gate_scope.py, tests/test_runner_item_dependencies.py
- Item-Dependencies: none
- Status: approved
- Set: lanetruth
- Order: 0
- Highest E allocated: 04
- Author: opencode (its_direct/pt3-claude-opus-5-1m-us)
- Id: y0gg8o
- Approval: 2026-08-29, recorded via aw ipd set: status set to approved
- Blocks-Release: next

## Workflow history
- 2026-08-29 approved (aw set): status set to approved
- 2026-08-29 reviewed (opencode its_direct/pt3-claude-opus-5-1m-us): /plan-review, SECOND independent pass at HEAD 64a97b4; APPROVE WITH REVISIONS APPLIED; PR-101..PR-110. The first pass's SUBSTANTIVE findings all HELD on independent re-verification, its BOOKKEEPING did not. Re-verified and confirmed: the `-P` result reproduced exactly with three fresh decoy packages (plain import -> DECOY; `-P` -> the real installed copy, NOT the designated parent; `-P` + parent root on PYTHONPATH -> PARENT; PYTHONPATH without `-P` -> DECOY, so BOTH halves are required, and `PYTHONSAFEPATH=1` behaves identically, which the 3.9 floor at pyproject.toml:12 + CI matrix tests.yml:31 makes mandatory); `begin` is genuinely never lane-shadowed while `driver_finalize` is (via `finalize_repo` at oc_runipd.py:2217/:2231-2233); the runner genuinely reads `[]` where the shared parser reads two typed edges; the lane is always at HEAD. PR-101 (BLOCKER, FIXED): the child-side blocking gate was never actually cleared. `8guhs0`'s OQ-02 STILL reads `Status: open`/`Owner: maintainer` and `8guhs0` is STILL `to-review`; the first pass answered the question here and wrote a cross-reference into the child's workflow HISTORY, but not into the OQ-02 FIELD its own gate reads, so E-03 was presented as executable when it is not. E-03 now carries two explicit preconditions (field set `resolved`, child reviewed AND approved) and the gate states plainly that this orchestrator being clean does not clear the Set. PR-102 (HIGH, FIXED): EVERY line citation was already wrong when written, by 3-19 lines (`:2214`->2231, `:370`->369, `:2288`->2302, `:495`->494, `:2554/:2592`->2557/2595, `pyproject.toml:123`->122), and NOT from drift: `git log 49096ee..HEAD` over the three cited modules is EMPTY. All citations re-verified at HEAD and a citation-discipline rule added (re-locate by name, never trust a line number, prefer function names). PR-103 (HIGH, FIXED): the graduation provenance was never done - all three source items (`tfx39h`, `l6rh0z`, `y9lcem`) are still `Status: open` despite reviewed children, so `aw attention` reports this release-gating work as untouched `ready`; nothing catches it (`aw attention --check` and `aw check backlog` both pass). Added E-04/V-04 and completion criterion 8 to set them `graduated` via `aw backlog set`, keeping `Blocks-Release: next`. PR-104 (HIGH, FIXED): baselines stale a THIRD time; re-measured fast `2876 passed, 3 skipped, 4 xfailed` (zero failures) and full `4 failed, 3203 passed, 3 skipped, 4 xfailed`, and the first pass's "fifth failure" `test_run_viewer_cli_issues_flag` did NOT recur (`34 passed`), which confirms it oscillates rather than standing - recorded as may-or-may-not-appear, owned by `i79rgh`. PR-105 (MEDIUM, FIXED): the "four surfaces already consume it correctly" list was WRONG - `ipd_set_plan.py` is not an `Item-Dependencies` consumer at all (it handles intra-IPD `Depends on` via `parse_depends_on`); corrected to the real consumers and instructed not to assert a count, since that is the same false claim as spec finding SR-002. PR-106 (MEDIUM, FIXED): criterion 6's hazard was underspecified; `dependency_status` uses each dep as a dict key AND an id6, so an unconverted typed string BLOCKS a satisfied dependent rather than silently admitting it - the test must pin which direction. PR-107 (MEDIUM, FIXED): Scope check understated contention, saying only 01 and 03 overlap when all THREE children touch `oc_runipd.py`. PR-108 (MEDIUM, FIXED): added driver-symmetry as a validation TARGET with the paired citations for all six duplicated constructs, since a one-driver fix passes every oc-only test. PR-109 (LOW, FIXED): recorded that the declared `executed:af7i6p` edges are not mechanically enforced today, precisely because child 03's defect means the runner cannot read them. PR-110 (LOW, FIXED): noted there is no `check_begin` function (it is `begin` at ipd_lifecycle.py:594) and corrected the OQ-01 counts to 28/28 pending, 13 with real edges, 0 legacy across 403 files. aw ipd lint conforming at author and review-finalize; E/V bijection now 4/4.
- 2026-08-29 reviewed (aw set): /plan-review: APPROVE WITH REVISIONS APPLIED; PR-001..PR-008; GO - PENDING HUMAN APPROVAL

- 2026-08-29 draft (opencode (its_direct/pt3-claude-opus-5-1m-us)): created.
- 2026-08-29 to-review (opencode (its_direct/pt3-claude-opus-5-1m-us)): graduated from backlog items `tfx39h` and `l6rh0z` (both filed this session from live evidence in the wtiso runs) plus `y9lcem`, filed from finding SR-001 of the `25kzda` spec review. Sibling item `xd9sll` was closed `done` rather than graduated, because it was already fixed in `c0e9599` with 6 falsifiable tests; it is cited here as prior art, not as a child.
- 2026-08-29 /plan-review (opencode (its_direct/pt3-claude-opus-5-1m-us)): APPROVE WITH REVISIONS APPLIED; PR-001..PR-008. The three defects are REAL and independently verified, but two of the Set's load-bearing claims were false and one acceptance criterion was satisfiable by a fix that does not work. PR-001 (BLOCKER, FIXED): completion criterion 2 ("a nested `aw` runs the RUNNER's tooling") was unachievable by child 01's chosen mechanism. Measured with three distinct packages: from a decoy-containing cwd, plain `-m` imported the DECOY (defect reproduced), and `python3 -P -m agent_workflows` imported the DEFAULT-PATH copy, NOT the parent's; `-P` suppresses the cwd entry but pins nothing. It looks correct here only because the install is editable, so site-packages and the checkout are the same files. `-P` plus the parent's package root on the child `PYTHONPATH` resolved to the parent's copy (verified both directions). Criterion 2 now demands a POSITIVE four-way demonstration and explicitly rejects "not the lane's copy" and a `-P`-only pass taken on this editable checkout. Also verified `requires-python = ">=3.9"` with CI on 3.9-3.14 while `-P` needs 3.11, so the `PYTHONSAFEPATH=1` fallback is REQUIRED, not optional. PR-002 (HIGH, FIXED): the Set's entire sequencing rationale ("until 01 lands, every other lifecycle fix including z2isfg's is void inside a lane") is FALSE. `driver_begin` runs with `cwd=str(repo)` (oc_runipd.py:370; agy_runipd.py:495) and the lane is not allocated until AFTER begin returns (:1958 then :1987), so `aw ipd begin` is never lane-shadowed; only `driver_finalize` (:2214) and the `run_checked` helpers are. The three `executed:af7i6p` edges are retained as EDIT SERIALIZATION (all three children touch `oc_runipd.py`) with that stated plainly, and re-sequencing 02 ahead of 01 is now explicitly legitimate-if-recorded. PR-003 (HIGH, FIXED): child `8guhs0` carries `OQ-02 Blocking: yes / Owner: maintainer` and a gate forbidding execution while open, which would have stranded a release-gating child behind a question the repo already answers. Resolved as OQ-03 from evidence: `config.dependency_cutover_date('.')` is `None` and `config.py:275-281` grandfathers everything when unset, so a refusing runner would be STRICTER than `aw check`/`aw ipd lint` and would recreate the divergence the child exists to remove; `ipd_lint.py:895-902` already encodes the deferral; 27/27 pending IPDs carry the field so nothing queued is affected. E-02 must delegate, not hardcode. PR-004 (HIGH, FIXED): the Cross-IPD end-to-end run demanded "a lane based on a commit that predates the current tooling", which the runner CANNOT produce - `allocate_isolation_worktree` hardcodes `base_commit="HEAD"` (oc_runipd.py:478) and no CLI flag overrides it. An executor would have fabricated it or added an out-of-scope flag; the stale-lane case is now unit-level and the end-to-end evidence must state the lane ran at HEAD. Same correction applied to V-02's "genuinely ambiguous lane base". PR-005 (HIGH, FIXED): the recorded test baseline was ALREADY STALE at review (authored `4 failed, 3198 passed`; re-measured `5 failed, 3202 passed` full, and `1 failed, 2875 passed` fast), and the extra failure `test_run_viewer::test_run_viewer_cli_issues_flag` belongs to plan `i79rgh`. Baselines are now per-child measurements stamped with `git rev-parse HEAD`, pre-existing failures are listed BY NAME rather than by count, and the `i79rgh`-owned test is fenced off. Also corrected the suite invocation to BARE `python3 -m pytest` (the plan's `-n auto` duplicates configured addopts, pyproject.toml:123). PR-006 (MEDIUM, FIXED): no criterion protected the EXISTING ordering behavior child 03 rewrites. `dependency_status` (oc_runipd.py:1424-1450) consumes bare id6 strings today; feeding typed `exists:`/`state:` edges through it unchanged would silently mis-gate, since those kinds have different release semantics. Added criterion 6 and V-03 evidence for per-kind handling plus an unchanged no-edge path. PR-007 (MEDIUM, FIXED): the gate lacked three required execution-contract elements (an explicit scope fence, the paste-actual-output honesty rule, and an all-questions-resolved statement); added, with `cli.py`, `test_run_viewer.py`, a base-commit flag, and spec `25kzda` named as out of bounds. PR-008 (LOW, FIXED): corrected counts (27/27 pending declare the field, 13 declare edges, legacy field 0 across the WHOLE plans tree, not 22/11/pending-only) and recorded that a co-worker committed to `oc_runipd.py` and `tests/` DURING this review with two lanes live, so cited line numbers will drift. Verified clean: all `path:line` citations resolve, `aw ipd lint` conforming at author and review-finalize for all four Set plans, E/V bijection 3/3, `tests/test_lane_session_isolation.py` present with 6 tests. All open questions RESOLVED from repository evidence; no maintainer decision outstanding. Readiness: GO - PENDING HUMAN APPROVAL.

## Goal

Make the runner act on the repository's declared truth. Each child closes one place where it does not: it executes tooling from the tree it is operating ON rather than the tree it IS, it decides execution authority from a tree the turn will never touch, and it silently ignores a declared dependency graph. All three are shipped-code defects in a verb used daily, and all three fail SILENTLY rather than closed, which is the opposite of the posture spec `25kzda` requires.

Precision added at review, because the Set's own sequencing was argued from an overstated version of the first defect: the tool-identity hijack affects the nested `aw` invocations that run with the LANE as cwd. That is `driver_finalize`, whose `cwd=str(repo)` (oc_runipd.py:445; agy_runipd.py:568) is the LANE because its caller passes `finalize_repo` (oc_runipd.py:2231-2233; agy_runipd.py:2302-2304), plus the two `run_checked` helpers. It does NOT affect `aw ipd begin`, which always runs with `cwd=str(repo)` (oc_runipd.py:369; agy_runipd.py:494) before any lane exists. The defect is real and worth fixing on its own terms; it is simply narrower than "every lifecycle fix is void inside a lane", and the children must not be validated against the broader claim.

CITATION DISCIPLINE, added at the second review because it bit this plan specifically: EVERY line number the first review recorded was already wrong when it was written, by 3 to 19 lines (`:2214`->`2231`, `:370`->`369`, `:2288`->`2302`, `:495`->`494`, `:2554/:2592`->`2557/2595`, `pyproject.toml:123`->`122`). This was NOT concurrent drift: `git log 49096ee..HEAD -- oc_runipd.py agy_runipd.py ipd_lifecycle.py` is EMPTY, so the cited files have not changed since that review. They were mis-transcribed. All citations in this document are re-verified at the second review and correct at HEAD `64a97b4`. The executor MUST re-locate every cited construct by NAME (`grep -n "def driver_finalize"`), never by trusting a line number, and MUST NOT treat a line-number mismatch as evidence that the underlying claim is false. Prefer citing a function name over a line for anything load-bearing.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: sequence the three children

- [ ] E-01 Execute child `af7i6p` (Order 01): pin nested `aw` invocations to the runner's own tooling. It runs FIRST because all three children edit `agent_workflows/oc_runipd.py` and this child rewrites how every nested `aw` argv in both drivers is built, so landing it first means the other two edit a settled call-site shape instead of rebasing onto it. CORRECTED AT REVIEW: the original rationale ("until it lands, every other lifecycle fix including `z2isfg`'s is void inside a lane") is FALSE and must not be relied on. `driver_begin` runs with `cwd=str(repo)`, the MAIN repo (oc_runipd.py:369, agy_runipd.py:494), and the lane worktree is not allocated until AFTER begin returns (begin called at oc_runipd.py:1958, allocate at :1987; agy at :2030/:2059), so `begin` is NEVER lane-shadowed and `z2isfg`'s fix cannot be voided by a stale lane copy. `driver_finalize` IS lane-shadowed: its own `cwd=str(repo)` (oc_runipd.py:445) receives `finalize_repo` from the call site (oc_runipd.py:2231-2233), and `finalize_repo` is the lane whenever the turn is isolated (`finalize_repo = Path(work_dir) if (work_dir and wt_handle) else repo`, oc_runipd.py:2217). That indirection is why the parameter name reads `repo` at the definition and must not be misread as the main tree. MECHANISM CONSTRAINT (measured at review, INDEPENDENTLY REPRODUCED at the second review with three distinct packages, see Required tests): `-P` alone does NOT satisfy completion criterion 2. `-P` suppresses the cwd entry, so the child resolves `agent_workflows` from the DEFAULT path (site-packages), which equals the parent only by coincidence of an editable install. This child MUST pin POSITIVELY to the parent's own package root (`-P` plus the parent's `Path(agent_workflows.__file__).parent.parent` on the child `PYTHONPATH`, verified working) so its own E-04 identity assertion cannot fail closed on a legitimate `python3 -m agent_workflows` invocation from a checkout.
  - Depends on: none
  - Expected outcome: child `af7i6p` reaches `executed` with its own validation evidence; a nested `aw` in a lane reports the same module path AND version as the PARENT process (not merely "not the lane's"), demonstrated across the four-way matrix in criterion 2.
  - Execution state: pending

- [ ] E-02 Execute child `z2isfg` (Order 02): scope `begin`'s in-scope-dirty gate to the tree the turn will actually execute in. Ordered after E-01 for EDIT SERIALIZATION only (it also touches `oc_runipd.py`), not because its fix depends on tool identity; per E-01 above, `begin` runs in the main tree and is not lane-shadowed. This is the highest-FREQUENCY live defect of the three, so if E-01 stalls for any reason, re-sequencing this child ahead of it is legitimate and the executor should report that rather than waiting.
  - Depends on: E-01
  - Expected outcome: child `z2isfg` reaches `executed`; an isolated lane is granted execution authority even when a co-worker has dirtied an in-scope path in the MAIN tree.
  - Execution state: pending

- [ ] E-03 Execute child `8guhs0` (Order 03): make runner preflight consume the shared `Item-Dependencies` predicate. Ordered after E-01 for the same edit-serialization reason (both touch the two driver modules). BLOCKING GATE, STILL UNCLEARED AS OF THE SECOND REVIEW: child `8guhs0` carries its own OQ-02 marked `Blocking: yes` with `Owner: maintainer`, and its gate (`8guhs0` line 168) says "Do not execute this plan with OQ-02 open". The first review resolved the QUESTION in this orchestrator's OQ-03 and wrote a cross-reference note into `8guhs0`'s workflow history, but re-verified at the second review: `8guhs0`'s OQ-02 STILL reads `Status: open` / `Owner: maintainer`, and `8guhs0` is STILL `Status: to-review`, not `reviewed` or `approved`. So the intended handoff did NOT actually land in the field the child's own gate reads. TWO PRECONDITIONS the executor MUST satisfy, in this order, before any execution of `8guhs0`: (1) `8guhs0`'s OQ-02 is edited to `Status: resolved` / `Owner: none` citing this OQ-03, so the child's gate is cleared in the field and not merely in prose elsewhere; (2) `8guhs0` reaches human `approved` through the normal lifecycle, since a `to-review` child has not been reviewed, let alone approved, and this orchestrator cannot approve it. If either precondition is unmet, STOP and report rather than executing.
  - Depends on: E-01
  - Expected outcome: child `8guhs0` reaches `executed`; a declared `executed:<id6>` edge appears in the frozen queue and gates launch; and its OQ-02 reads `resolved` BEFORE execution began, not retroactively.
  - Execution state: pending

### Task group 2: make the backlog tell the truth about this Set

- [ ] E-04 Set each source backlog item to `graduated` with `aw backlog set graduated <id6>`, for `tfx39h`, `l6rh0z`, and `y9lcem`. All three are still `- Status: open` (measured at the second review) even though their designs are handed off to reviewed children, so `aw attention` reports this release-gating work as untouched `ready` instead of `active`. Use the verb, never a hand-edit. Do NOT set `done`: the code is not written, `graduated` is the honest state, and each item keeps `- Blocks-Release: next` so it correctly remains an outstanding release blocker. This is a records-only action in `.aw/records/backlog/`, touches no `Scope-Paths` file, and may be performed at any point in the Set (doing it EARLY is better, so the attention view is honest while the children are being executed rather than only afterward).
  - Depends on: none
  - Expected outcome: all three items read `- Status: graduated` with `Blocks-Release: next` intact; `aw attention` classifies them `active`; `aw backlog check --agent` reports no new finding for them.
  - Execution state: pending

## Child IPDs, sequence, and dependencies

| Order | Id | Title | Graduated from | Depends on | Why |
| --- | --- | --- | --- | --- | --- |
| 01 | `af7i6p` | nested `aw` in a lane must run the runner's own tooling | backlog `tfx39h` | none | First because it rewrites how EVERY nested `aw` argv is built in both drivers; the other two then edit a settled call-site shape. |
| 02 | `z2isfg` | scope `begin`'s dirty gate to the executing tree | backlog `l6rh0z` | `af7i6p` | Edit serialization: it also touches `oc_runipd.py`. NOT a semantic dependency (see the correction below). |
| 03 | `8guhs0` | runner preflight consumes the shared dependency predicate | backlog `y9lcem` (spec review SR-001) | `af7i6p` | Edit serialization: it touches both driver modules that 01 rewrites. Independent of 02 in subject matter. |

CORRECTED AT REVIEW: the original "Why" for 02 claimed its fix is only verifiable in a lane once 01 lands, because `ipd_lifecycle.py` is the module a stale lane copy shadows. That is FALSE. Verified: `driver_begin` launches with `cwd=str(repo)` (oc_runipd.py:369; agy_runipd.py:494) and the lane worktree is not allocated until AFTER begin returns (oc_runipd.py:1958 then :1987; agy_runipd.py:2030 then :2059), so `aw ipd begin` ALWAYS runs against the main tree with the main tree's `agent_workflows` and is never lane-shadowed. `driver_finalize` is the invocation that genuinely runs with the lane as cwd, via `finalize_repo` at its call site (oc_runipd.py:2217 computing it, :2231-2233 passing it; agy_runipd.py:2302-2304), so the tool-identity defect's real blast radius is finalize (plus the two `run_checked` helpers), not begin. All three dependency edges are therefore EDIT-SERIALIZATION conveniences, not correctness prerequisites.

SECOND-REVIEW NOTE on the declared edges: children `z2isfg` and `8guhs0` each carry `- Item-Dependencies: executed:af7i6p` in their own front matter (verified). Because those edges are conveniences rather than correctness prerequisites, and because the runner currently cannot even READ them (that is child 03's whole defect: `_read_deps` returns `[]` for a typed `Item-Dependencies` value, reproduced at review), the executor should understand that TODAY nothing mechanically enforces this ordering. The ordering is a human/executor discipline until child 03 lands, at which point the edges become live. Do not mistake the declared edge for an enforced one when planning the run.

Consequence for the executor: the declared `executed:af7i6p` edges are conservative, not load-bearing. 02 and 03 are mutually independent and MAY run in either order or in parallel once 01 is verified. Because the edges are conveniences, if 01 stalls, promoting 02 (the highest-frequency live defect) ahead of it is legitimate; the executor must RECORD that re-sequencing in this orchestrator rather than doing it silently, and must re-run 01's own validation afterward since 01 would then rebase onto 02's call sites.

Each child carries `- From-Backlog:` naming its source item and inherits `- Blocks-Release: next`. Re-verified at the second review: `af7i6p`/`tfx39h`, `z2isfg`/`l6rh0z`, `8guhs0`/`y9lcem`, all three with `- Blocks-Release: next`, all three links resolving.

OPEN PROVENANCE GAP, added at the second review and NOT yet closed. The installed contract's graduation step (5) requires the source item be set to `graduated`, "NOT `done`, because `graduated` means the design is handed off while `done` means the code is written and validated" (AGENTS.md). All THREE source items are still `- Status: open`: `tfx39h`, `l6rh0z`, and `y9lcem` (measured). So the backlog still presents this designed, reviewed, release-gating work as untouched `ready`, and `aw attention` classifies it `ready` rather than `active`. This is not currently caught by tooling (`aw attention --check` and `aw check backlog` both pass; the backlog findings that exist are unrelated name/summary debt), which is precisely why it needs stating here rather than being left to a gate. REQUIRED, and it is a prerequisite of this orchestrator being honestly complete, not an afterthought: set each item with `aw backlog set graduated <id6>` so the handoff is visible in the attention view. Do this BEFORE reporting the Set complete; the gate below repeats it. Note the close-legitimacy rule is not at risk here because `graduated` is not a close: the items keep their `Blocks-Release: next` and stay outstanding blockers, which is the correct representation while the code is unwritten.

## Completion criteria (the whole Set is done only when)

1. All three children are in `.aw/records/plans/executed/` with status `executed`, each finalized by `aw ipd finalize` (never a hand-move).
2. A nested `aw` invoked by the runner inside a lane provably runs the RUNNER's OWN tooling: the child reports the same module path AND version as the PARENT process. This criterion is satisfied only by a POSITIVE four-way demonstration, because "not the lane's copy" is a weaker property that a naive fix passes while still running third code. Paste all four: (a) parent module path/version; (b) child module path/version with `cwd` inside a lane whose checked-out `agent_workflows/` differs; (c) the two are EQUAL; (d) the lane's own differing copy was demonstrably present and was not the one imported. Measured at review and binding: `-P` ALONE FAILS this criterion. From a decoy-containing cwd, `python3 -P -m agent_workflows` resolved to the DEFAULT-path (site-packages) copy, not the parent's; it equals the parent here only because this checkout is an editable install. `-P` plus the parent's package root on the child `PYTHONPATH` resolved correctly to the parent's copy (both directions verified). A child that ships `-P` only, and evidences criterion 2 on this editable checkout, would record a PASS that does not generalize to a non-editable install; that is a validation failure, not a pass.
3. An isolated lane receives execution authority when the only dirty in-scope path belongs to another party in the main tree, and still refuses when the LANE's own frozen base is genuinely ambiguous.
4. A plan declaring `- Item-Dependencies: executed:<id6>` produces a frozen queue containing that edge (not `[]`), and a dependent is not launched while its prerequisite is unverified.
5. The runner no longer defines a private dependency regex; one shared predicate serves all surfaces, asserted by a guard test.
6. NO REGRESSION to today's typed-edge-free ordering guarantee. Child 03 replaces the input to `dependency_status` (oc_runipd.py:1424-1451; agy_runipd.py:1521), which today consumes bare id6 strings and checks in-queue status against `EXECUTION_SUCCESS_STATES` or an out-of-queue `plan_bucket`. Typed edges (`exists:`, `state:`) have DIFFERENT release semantics from `executed:`, so feeding them into the existing predicate unchanged would silently mis-gate. Note the shape of the hazard precisely: `dependency_status` uses each dep as a DICT KEY (`dep in by_id`) and as an id6 for `resolve_plan_path`, so an unconverted typed string like `"executed:af7i6p"` would miss the queue lookup AND fail id6 resolution, landing in `unsatisfied` and blocking a satisfied dependent, not silently admitting it. Either direction is a mis-gate; the test must pin which. The Set is done only when `dependency_status` is shown to handle each edge kind per spec 2.9 and the existing orchestrator-deferral path (`dependency-blocked`, oc_runipd.py:2557/:2595; agy_runipd.py has the symmetric 6 occurrences) still behaves as before for a Set with no declared edges.
7. Full suite green against the executor's OWN re-measured baseline (see Required tests; never against a figure copied from this document), leak scan clean, `aw ipd lint --phase pre-transition` conforming for every child.
8. ADDED AT THE SECOND REVIEW: each source backlog item is `graduated`, not left `open`. `tfx39h`, `l6rh0z`, and `y9lcem` are all still `open` as measured, so the attention view misreports this Set's designed work as untouched. Set each with `aw backlog set graduated <id6>` (they retain `Blocks-Release: next` and remain outstanding blockers, which is correct while the code is unwritten). The Set is not honestly complete while the backlog denies the handoff happened.

## Cross-IPD validation

- After all three land, run ONE end-to-end `aw oc run` over a two-plan Set where B declares `executed:<A-id6>`, with a co-worker's uncommitted edit present on an in-scope path. Paste the run's `state.json` queue (showing the edge and correct gating) and the parent-vs-child module-path/version evidence from criterion 2.
- CONSTRUCTIBILITY CORRECTION (review): the original wording asked for this run "with the lane based on a commit that predates the current tooling", which the runner CANNOT do. `allocate_isolation_worktree` hardcodes `base_commit="HEAD"` (oc_runipd.py:478; agy_runipd.py:600, re-verified at the second review) and no CLI flag overrides it (`aw oc run start --help` exposes none), so a driver-allocated lane is ALWAYS at current HEAD. Do not fabricate this condition and do not add a base-commit flag (out of scope). Demonstrate the stale-lane case at the unit level instead, with the decoy-package construction child 01 already specifies, and state plainly in the evidence that the end-to-end lane ran at HEAD. Equally, do not claim the end-to-end run reproduces the tool-identity defect: at HEAD the lane's copy matches the runner's, so the run exercises the dependency edge and the dirty-gate fix, and the identity fix is proven by 01's own decoy test.
- The decoy construction is CONFIRMED WORKING, reproduced independently at the second review with three separate packages (a decoy in cwd, a distinct parent root, and the real installed package). Results, which child 01's test must reproduce: from a decoy-containing cwd, plain `python3 -c "import agent_workflows"` imported the DECOY (defect reproduced); `python3 -P` imported the REAL installed copy, NOT the parent's designated root (so `-P` alone pins nothing); `-P` WITH the parent root on `PYTHONPATH` imported the PARENT (correct); and `PYTHONPATH` WITHOUT `-P` still imported the DECOY (so the env var alone is insufficient and BOTH halves are required). The `PYTHONSAFEPATH=1` env form behaves identically to `-P` in both directions, which is what makes it a valid fallback for the 3.9/3.10 floor (`requires-python = ">=3.9"`, pyproject.toml:12, CI matrix 3.9-3.14 at tests.yml:31, while `-P` needs 3.11). Child 01 must ship the fallback, not just the flag.
- Also note for honesty: this end-to-end run cannot exercise `begin` being lane-shadowed, because `begin` never runs in a lane (see the child-table correction). Only `finalize` and the `run_checked` helpers do.
- Confirm no regression to the `c0e9599` fix from backlog `xd9sll`: `tests/test_lane_session_isolation.py` must still pass unchanged (6 tests, re-verified at the second review: `6 passed`, and backlog `xd9sll` confirmed `Status: done`), since children 01-03 touch the same two driver modules.
- SYMMETRY IS A VALIDATION TARGET, not just an instruction. Both drivers duplicate every construct this Set touches: `_DEPS_RE` (oc:100, agy:69), `dependency_status` (oc:1424, agy:1521), `driver_begin` (oc:348, agy:473), `driver_finalize` (oc:413, agy:536), `allocate_isolation_worktree` (oc:470, agy:593), and `dependency-blocked` (6 occurrences in each). The Cross-IPD evidence must show the fix present in BOTH modules for each child that touches them, because a one-driver fix would leave `aw agy run` on the old behavior while every test that only exercises `oc` still passes.

## Deferred / out of scope (with reason)

- Backlog `xd9sll` (session reuse pinning a turn to the previous lane's worktree): already fixed in `c0e9599`; the item was closed `done` with evidence rather than graduated. Not a child, but its tests are a regression surface for this Set.
- Backlog `qyaime` (external_directory permission deadlock) and `dh0uno` (state roots resolved relative to the lane): both `graduated` already and owned by the `wtiso` Set. Children here must not duplicate that work; where this Set's fixes reduce the blast radius of those defects, say so without claiming to close them.
- The `Item-Dependencies` GRAMMAR and the surfaces that already consume it correctly. CORRECTED AT THE SECOND REVIEW: the list was wrong. The shared evaluator `check_engine.evaluate_ipd_dependencies` is consumed by `ipd_lint.py:1046` and `hooks/ipd_dependency_statement_gate.py:96`, and `cli.py:434` documents that authority; `ipd_schema.parse_item_dependencies` is the shared parser. `ipd_set_plan.py` is NOT an `Item-Dependencies` consumer at all: it handles the DIFFERENT intra-IPD `Depends on:` E-item edges via `parse_depends_on`/`dependency_errors` (verified: no `Item-Dependencies`/`META_ITEM_DEPENDENCIES` reference in that module). So the honest statement is that the shared predicate has a small number of correct consumers and the runner is the outlier; do NOT assert a specific count, and in particular do not repeat "five surfaces already share the predicate", which is the very spec claim review finding SR-002 flagged as false. Child 03 adds the runner as a consumer; the grammar and the existing consumers stay untouched.
- Spec `25kzda`'s stale preamble and the section 2.10 claim that five surfaces already share the predicate (review findings SR-001/SR-002). Spec text is the maintainer's to amend; this Set makes the code match what section 2.10 asserts.
- The wider runner-consolidation program in `25kzda` (`aw hooks install`, `aw <host> prompt`, the hash-chained ledger with `AW-Run:`/`AW-Item:` trailers, the per-host capability descriptor). All genuinely unbuilt and much larger than this corrective Set.

## Scope check

- Over-scope: none. Each Scope-Path is touched by exactly one child, except `oc_runipd.py`, which ALL THREE children touch, and `agy_runipd.py`, which 01 and 03 touch (01 changes how nested `aw` is launched; 02 also edits `oc_runipd.py`; 03 changes how the queue is built). Corrected at the second review: the previous wording said only 01 and 03 overlap, which understated the contention on `oc_runipd.py`; the gate's own concurrency paragraph already stated the three-way version correctly. The children declare this overlap and both 02 and 03 declare `executed:af7i6p` so the edits serialize. E-04 writes only `.aw/records/backlog/` and no code path.
- Under-scope: `xd9sll`, `qyaime`, `dh0uno`, and the spec-text findings are named under Deferred with reasons rather than silently dropped. The backlog-graduation gap found at the second review is NOT deferred; it is now E-04/V-04 in scope, because leaving it out would let the Set complete while the backlog still claims none of this work was ever handed off.

## Required tests / validation

- INVOKE THE SUITE BARE: `python3 -m pytest` (fast subset) and `python3 -m pytest -m ""` (full), or `make test` / `make test-all`. Do NOT append `-n auto`: `pyproject.toml` `addopts` already supplies `-q -n auto --dist=worksteal -m 'not slow'` (pyproject.toml:122), so the flag is redundant, and a second `-q` compounds to `-qq` and suppresses the very `N passed` summary line this plan requires you to paste. (Aligned at review with the `testinvoke` Set's `uyd3lw`, which is making this the installed contract.)
- BASELINE IS A MEASUREMENT, NOT A CONSTANT, and this plan is now the proof: the figures have moved at every single reading. Authoring said fast `2871 passed` / full `4 failed, 3198 passed`. The first review said fast `1 failed, 2875 passed` / full `5 failed, 3202 passed`. RE-MEASURED at the SECOND review at HEAD `64a97b4`: fast `2876 passed, 3 skipped, 4 xfailed in 25.96s` (ZERO failures) and full `4 failed, 3203 passed, 3 skipped, 4 xfailed in 114.11s`. Three readings, three different numbers, and the fast subset went from 1 failure to 0 with no relevant code change, which is exactly the oscillation described below. RULE: each child MUST record its own before/after baseline WITH the `git rev-parse HEAD` it was measured at, and MUST NOT treat a mismatch against ANY figure in this document as a regression it caused. Do not paste a number from this plan as if you measured it.
- The PRE-EXISTING failures to attribute, not fix, identified by NAME (counts drift, names do not). Confirmed present at the second review, all four in the full suite only: `test_command_surface_declarations::test_zero_undeclared_parser_leaves`, `test_cli_conformance_matrix::test_no_undeclared_parser_leaves`, `test_cli_conformance_matrix::test_every_declared_leaf_gets_a_full_scenario_row_set`, and `test_cli::SubcommandDescriptionTests::test_every_subparser_has_fuller_description` (undeclared CLI parser leaves from concurrent `run_cli` work). The "fifth failure" the first review recorded, `test_run_viewer.py::RunViewerTests::test_run_viewer_cli_issues_flag`, did NOT recur at the second review (`tests/test_run_viewer.py` passed, `34 passed`), which CONFIRMS it is a live-repo-state oscillating test rather than a standing failure; it is owned by plan `i79rgh` (testinvoke-02) E-01 either way. Treat it as may-or-may-not-appear, never as expected-failing, and do not "fix" it: a child that touches `tests/test_run_viewer.py` has left its scope fence. Verify by name which failures are present at your own HEAD before starting; a failure NOT on this list is yours until proven otherwise.
- `tests/test_lane_session_isolation.py` must pass unchanged (the `xd9sll` regression surface).
- Each child adds its own test module and each must demonstrate FALSIFIABILITY: the new assertions must be shown to FAIL against the pre-fix code, not merely pass after it. A guard that cannot fail is not evidence.
- `aw check-local-leaks . --agent` clean; `aw ipd lint --phase pre-transition` conforming per child.

## Open questions

### OQ-01: Should the runner keep reading the legacy `Dependencies:`/`Depends-on:` field at all?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: NO, remove it. Re-measured at the SECOND review over the WHOLE plans tree (not just pending): `grep -rn '^- \(Dependencies\|Depends-on\):' .aw/records/plans/` returns 0 matches across all 403 `.ipd.md` files, while 28 of 28 pending IPDs carry `- Item-Dependencies:` and 13 declare real edges. Nothing tracked depends on the old name. Keeping two accepted names is exactly how the two parsers diverged in the first place. Child 03 removes the private regex (`_DEPS_RE`, oc_runipd.py:100 and agy_runipd.py:69, symmetric in both drivers) and adds a guard asserting the runner does not define one, which is what makes the divergence non-recurring. (Counts have moved twice: authoring said "0 of 22 pending / 11 edges", the first review said 27/13, and the second measured 28/13 across 403 files. The DIRECTION is stable and the conclusion is unaffected; treat these as a measurement to redo, not a constant, and do not paste any of them as freshly observed.)

### OQ-02: Should child 02 fix the main-tree dirty check as well, or only the isolated path?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: ONLY the isolated path. Resolved at review from repository evidence rather than left for the maintainer, because the repo already answers it. Two facts settle it: (1) for a non-isolated turn the main tree genuinely IS the execution tree, so measuring it is correct by construction, and the gate documents that intent in source (the in-scope-dirty gate lives in `begin`, ipd_lifecycle.py:703-712, with the path-overlap rationale in `begin`'s own docstring at :610-613; note there is NO function named `check_begin`, which the first review implied, so locate it as `begin` at ipd_lifecycle.py:594); (2) making the non-isolated check ownership-aware is the DECLARED subject of backlog `077yqc` (re-verified at the second review: `Status: open`, `Blocks-Release: next`, `Priority: high`), whose own text names begin/finalize consistency as its scope and cites `ipd_lifecycle.py:610-613` as the rule finalize should adopt. Expanding child 02 into that work would duplicate a separately-gated release blocker and widen a three-file scope fence. Child 02 therefore states the boundary and stops; `077yqc` remains the owner of the ownership-awareness question at both ends.

### OQ-03: What should runner preflight do about a plan with NO `Item-Dependencies` statement? (resolves child `8guhs0`'s blocking OQ-02)

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: ADMIT with an advisory; do NOT refuse. Child `8guhs0` raised this as `Blocking: yes / Owner: maintainer` and its gate forbids executing while it is open, so leaving it open would strand a release-gating child. It is resolved here on repository evidence, which is decisive in three independent ways. FIRST, the question is very nearly moot: all 27 of 27 pending IPDs already carry the field, so no queued work is affected either way. SECOND, the repository's cutover mechanism is CURRENTLY UNSET and fail-open by design: `config.dependency_cutover_date('.')` returns `None` (measured), and `config.py:275-281` states that an absent marker means "no cutover in effect", so EVERY existing plan is grandfathered and the corpus is never mass-failed. A runner that refused a fieldless plan would therefore be STRICTER than `aw check` and `aw ipd lint`, which is the exact divergence this child exists to eliminate; spec 2.10 requires all surfaces to call one evaluator, not for the runner to invent a harsher rule. THIRD, `ipd_lint.check_item_dependencies` already encodes the intended treatment in code (ipd_lint.py:895-902): the pure lint deliberately emits NOTHING for a missing statement and defers cutover-conditional MISSING enforcement to the repo-aware shared evaluator. So child 03's correct implementation is to DELEGATE the missing-statement decision to the shared evaluator plus the cutover marker, not to hardcode refuse-or-admit. That satisfies spec 2.7 ("absence is never `none`") without mass-failing: absence is reported as `check.ipd-missing-dependency-statement` at whatever severity the shared evaluator assigns for the current cutover state, and the runner does not second-guess it. Consequence to record in child 03: if a maintainer later SETS the cutover marker, fieldless plans begin failing preflight automatically through the shared predicate, with no runner change. Child 03 MUST update its own OQ-02 to `resolved` citing this OQ before it is executed, and its E-02 must not contain an ad-hoc runner-local rule for the missing case.

## Validation and cross-check (verify before reporting the Set complete)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: paste child `af7i6p`'s terminal status and location proving it is `executed` in `.aw/records/plans/executed/`. Then paste its own evidence satisfying completion criterion 2 in the POSITIVE four-way form: (a) the PARENT's module path and version; (b) the child's, launched with `cwd` inside a lane holding a DIFFERENT `agent_workflows/`; (c) an explicit equality assertion between them; (d) proof the lane's differing copy existed and was not imported. Additionally state WHICH pin mechanism shipped, and paste the evidence that it pins POSITIVELY to the parent's package root rather than merely suppressing the cwd. A `-P`-only implementation whose criterion-2 evidence was taken on this editable checkout does NOT satisfy this V-item; if that is what shipped, record `Result: pending` and report, because the pass would not generalize to a non-editable install.
  - Observed evidence:
  - Result: pending

- [ ] V-02 validates E-02
  - Required evidence: paste child `z2isfg`'s terminal status and location, plus its evidence showing (a) an isolated lane granted authority with an in-scope path dirty in the MAIN tree, and (b) a genuinely ambiguous lane base still refused. For (b), state HOW the ambiguous lane was constructed: a driver-allocated lane is always clean at HEAD (`base_commit="HEAD"`, oc_runipd.py:478), so this case must be built by WRITING into a lane before begin, not by choosing an older base. Also paste (c) the non-isolated refusal unchanged, so the fix is shown to rescope the protection rather than remove it.
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: paste child `8guhs0`'s terminal status and location, plus its evidence showing a frozen queue containing a declared `executed:<id6>` edge (contrast against the pre-fix `[]`, re-reproduced at the second review: `oc_runipd._read_deps` on a plan declaring `- Item-Dependencies: executed:af7i6p, executed:z2isfg` returns `[]`, while `ipd_schema.parse_item_dependencies` on the same value returns two typed `ItemDependency` records), a dependent NOT launched while its prerequisite is unverified, a malformed/cyclic statement refused at preflight before any session starts, and the guard proving the runner defines no private dependency regex (both drivers: `_DEPS_RE` gone from oc_runipd.py:100 AND agy_runipd.py:69). ADDED AT REVIEW: also paste evidence for completion criterion 6 (no ordering regression), namely that `dependency_status` (oc_runipd.py:1424-1451; agy_runipd.py:1521) handles each typed edge kind per spec 2.9 rather than treating a typed edge as a bare id6, and that a Set with NO declared edges still gates exactly as before (the orchestrator-deferral `dependency-blocked` path at oc_runipd.py:2557/:2595 unchanged). ADDED AT THE SECOND REVIEW: paste child `8guhs0`'s OQ-02 field itself showing `Status: resolved` / `Owner: none` AND its `- Status:` showing it reached `approved`, both dated BEFORE the execution evidence, because at the second review that child was still `to-review` with OQ-02 still `open` (a workflow-history cross-reference note is NOT the field its own gate reads). Also paste the evidence that E-02 contains no runner-local rule for the missing-statement case. Finally paste the cross-IPD end-to-end run described under Cross-IPD validation and the unchanged `tests/test_lane_session_isolation.py` result.
  - Observed evidence:
  - Result: pending

- [ ] V-04 validates E-04
  - Required evidence: paste `aw backlog check --agent` (or the item files) showing `tfx39h`, `l6rh0z`, and `y9lcem` each at `- Status: graduated` with `- Blocks-Release: next` retained, plus the `aw attention` line for each showing it now classifies as `active` rather than `ready`. State the command used (`aw backlog set graduated <id6>`), since a hand-edit is the wrong mechanism.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

This is a Set rather than one plan because the three defects are in different subsystems (process launch, lifecycle authority, queue construction), have different test surfaces, and are independently valuable: any one can ship alone and improve correctness. They are orchestrated together because they share one root theme and because all three edit the same two driver modules, so their edits must serialize. (Corrected at review: the original rationale said child 01 must precede the others "for their validation to be sound inside a lane". That is false; see the child-table correction. The real reason for the ordering is edit serialization.)

Open questions: ALL RESOLVED IN THIS DOCUMENT. OQ-01, OQ-02 and OQ-03 above carry `Status: resolved` with cited repository evidence.

BUT THE CHILD-SIDE GATE IS STILL CLOSED, re-verified at the second review and unchanged since the first: child `8guhs0`'s own OQ-02 still reads `Status: open` / `Owner: maintainer`, and `8guhs0` is still `Status: to-review`. The first review answered the question here and left a cross-reference in the child's workflow history, but did NOT update the child's OQ-02 field, which is what the child's own gate ("Do not execute this plan with OQ-02 open") actually reads. So this orchestrator is internally clean while one of its three children remains BOTH question-blocked and un-reviewed. Consequence, stated plainly: E-01 and E-02 (children `af7i6p` and `z2isfg`, both `reviewed`) are executable once a human approves them; E-03 is NOT, until `8guhs0`'s OQ-02 field is set `resolved` citing OQ-03 and `8guhs0` itself is reviewed and approved. Do not let this orchestrator's own clean state imply the whole Set is cleared.

Scope fence: the Set touches ONLY the six declared `Scope-Paths`, split per child (01: `oc_runipd.py`, `agy_runipd.py`, `tests/test_lane_tool_identity.py`; 02: `ipd_lifecycle.py`, `oc_runipd.py`, `tests/test_begin_dirty_gate_scope.py`; 03: `oc_runipd.py`, `agy_runipd.py`, `tests/test_runner_item_dependencies.py`) - all three children's declared `Scope-Paths` re-verified at the second review as exactly these, matching this orchestrator's union. The one exception is E-04, which writes only `.aw/records/backlog/` item status through `aw backlog set` and touches no code path. Do NOT expand otherwise. Specifically do not touch `agent_workflows/cli.py` (not in any child's scope, frequently dirty from concurrent work, and the subject of the four pre-existing CLI-surface failures), do not touch `tests/test_run_viewer.py` (owned by plan `i79rgh`), do not add a lane base-commit flag, and do not amend spec `25kzda`. If the work seems to need more, STOP and report rather than silently broadening.

Honesty rule (HARD MUST): when you report tests or validation passed, paste the ACTUAL runner output with the `git rev-parse HEAD` it was measured at. Never claim a pass you did not run, and never reuse this plan's authoring-time counts as if freshly measured (they were already stale at review; see Required tests).

Execution contract: each child commits ONLY the files it changed, path-scoped (`git commit -m msg -- <paths>`), never `git add -A`, and never pushes. Before every commit the executor MUST run `git diff --cached --name-only` and unstage anything it did not modify. This repository is a SHARED CHECKOUT with other agents and humans working concurrently; verified at review, a co-worker committed to `agent_workflows/oc_runipd.py` and `tests/` DURING this review, and two lane worktrees (`aw/lane/qcqhj7`, `aw/lane/rchpms`) are live. A path-scoped commit still commits whatever is already staged for those paths, so verify the staged set every time.

Children 01 and 03 both modify `oc_runipd.py` and `agy_runipd.py`, and child 02 also modifies `oc_runipd.py`. All three therefore contend on that one file. The executor must re-read the driver modules immediately before editing rather than reusing a stale view (line numbers cited in these plans WILL drift: they were re-verified at review and had already survived one concurrent commit), and must keep both drivers symmetric: a one-driver-only fix is a defect, and each child must assert symmetry in its tests the way `tests/test_lane_session_isolation.py` already does.

Post-gate lifecycle: no child may be moved to `executed/` until every one of its `V-*` items is verified with concrete pasted evidence and `aw ipd lint --phase pre-transition` reports conforming; the transition is performed by `aw ipd finalize`, never by hand. This orchestrator is finalized only after all three children are `executed`, E-04's backlog graduation is done, and the Cross-IPD validation above has been performed. Do not create or push a git tag or release.

A NOTE ON THIS PLAN'S OWN REVIEW HISTORY, because it bears on how much of the document to trust: this orchestrator has now been reviewed twice. The first pass corrected two false load-bearing claims and one unachievable acceptance criterion, and its substantive technical findings all HELD on independent re-verification at the second pass (the `-P` mechanism result reproduced exactly with fresh decoy packages; `begin` is genuinely not lane-shadowed; the runner genuinely reads `[]` where the shared parser reads two typed edges; the lane really is always at HEAD). What did NOT hold was its bookkeeping: every line citation was off by a few lines even at the moment it was written, its test baselines were stale within hours and have now moved three times, its "four surfaces" consumer list was wrong, and the child-side blocking gate it believed it had cleared was never actually cleared in the field. Read the technical claims as reliable and the numbers as provisional, and re-measure anything you are about to paste.
