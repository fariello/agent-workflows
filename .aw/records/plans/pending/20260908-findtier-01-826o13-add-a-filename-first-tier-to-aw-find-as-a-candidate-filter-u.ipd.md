# IPD: Pin the aw find matching contract as tests, and do not add the filename filter

- Date: 2026-09-08
- Kind: child
- Concern: `aw find` READS RECORD FILE BODIES TO ANSWER A QUESTION THE FILENAME USUALLY ANSWERS. Re-measured at HEAD: `find .aw/records -iname '*lus9ou*'` returns in 20ms while `aw find plans lus9ou` takes 530ms, so the raw gap the item filed is real and the maintainer's 2026-08-31 ruling that the DIRECTORY LISTING is the cheap path still holds.
  BUT THE "70x GAP" ATTRIBUTES TO THE RESOLVER A COST THAT IS MOSTLY NOT THE RESOLVER, AND REVIEW MEASURED THE DECOMPOSITION THE PLAN ASSUMED (F-12, and it re-scopes this plan). Of a ~450ms subprocess `aw find plans <id6>`: interpreter start plus `import agent_workflows.cli` is ~115ms, the resolver TOTAL is ~42.5ms, and the display layer's `plans_index.scan_plans` is ~113.6ms. Inside that ~42.5ms resolver, TRAVERSAL is ~29.5ms (69%) and is IRREDUCIBLE by a filename filter, so the header reads this plan removes are ~13.4ms, and after the filter's own `parse_clustered` overhead (~0.5ms) the NET saving is ~12.8ms. That is roughly 3% of what an operator waits for, and the display layer alone is 8.9x larger than the prize. OQ-03 is therefore ANSWERED rather than open, and it is answered AGAINST executing this plan for its speed: see OQ-03 and F-12.
  THE MAINTAINER RESOLVED OQ-03 ON 2026-09-11 BY KEEPING THE TESTS AND DROPPING THE FILTER, AND THIS PLAN IS NOW THAT PLAN. The filter is NOT deferred pending a better measurement and NOT waiting on a follow-up: it is declined, because ~12.8ms of a ~450ms wait does not justify changing the ONE resolver every verb and all ten record types route through, and the corpus-wide differential needed to prove such a change safe is expensive precisely BECAUSE the change is dangerous. What survives is the correctness work the review surfaced, which never needed the filter to exist: the artifacts-not-references contract and the per-kind semantics are unpinned, today's backtick-quoting behavior is unpinned, and the `parse_clustered` id6-slot hazard (F-16) is undocumented in code. Those three gaps are real today, independent of any optimization, and this plan closes them. THE REAL COST IS THE DISPLAY LAYER re-reading the 616 records the resolver just read (~113.6ms, ~9x this plan's former prize); it is carried by its own backlog item and is deliberately not attempted here (F-12, F-8).
  AND THE FILTER CAN ONLY EVER HELP ONE QUERY KIND, NOT THE THREE E-03 NAMED (F-13). Measured, EVERY kind reads all 616 headers today, `stem` and `substring` included, because `setid` and `status` are evaluated FIRST and live in front matter. So for a stem or substring query the filter saves NOTHING, since the all-file read has already been paid before the filename rules are reached; only `id6`, which precedence evaluates SECOND, can benefit. The plan's own cited proof implies this and E-03 did not carry the implication.
  THE MANDATORY FALLBACK IS NOT NEGOTIABLE AND BOTH OF ITS REASONS RE-MEASURED TRUE. FIRST, STATUS QUERIES ARE IMPOSSIBLE FROM FILENAMES: `find -iname '*approved*'` returns FIVE files, NONE of which is an approved plan (two `apprvguard` records about the word, one `w6mqc0` backlog item about writing it, and one plan literally named `auto-approved` whose status is `executed`), while SEVENTEEN plans in `pending/` genuinely carry `- Status: approved`. SECOND, RECORDS EXIST WHOSE DECLARED IDENTITY IS ABSENT FROM THEIR FILENAME: measured NINE on a bounded header read at HEAD (the item recorded 8 of 751), and one of them is `25kzda`, a spec this repository cites constantly, so filename-only matching would make it uncitable.
  THE NINTH IS NEW AND IS NOT A LEGACY NAME, which matters for how this plan counts its exception set. Eight are genuine pre-id6-grammar names (`4o5lt9`, `wvlk84`, `lus9ou`, `8q6yr9`, `7qx7ys`, and grandfathered specs `4w7d6s`, `25kzda`, `5tapom`). The ninth is `uyeko5` "declared" by research prompt `27rjro`, which is the QUOTED-EXAMPLE parser artifact owned by `cqytxf`/`76w6mq`. So the exception set is 8 real plus 1 artifact that another plan is removing, and this plan must not count that one as a permanent exception.
  NINE IS THE RIGHT NUMBER FOR THIS PLAN AND TEN IS THE RIGHT NUMBER FOR ORDER 02, WHICH IS NOT A CONTRADICTION (F-14). Order 02's review measured TEN. Re-measured both ways at review: a bounded 4096-byte header scan finds NINE, a FULL-BODY scan finds TEN, the tenth being a second quoted-example artifact (`uyeko5` again, in `takpys`'s research report) that sits beyond the header window. This plan is correct to say nine BECAUSE the resolver only ever reads a bounded header (`_HEADER_BYTES = 4096`, `selectors.py:317`), so nine is exactly the population the resolver can see; Order 02 is correct to say ten because a REPORT scans whole files. State the window whenever citing the count, since a bare "nine" versus "ten" reads as one of the two being wrong.
  THE SEMANTIC CONTRACT THIS MUST NOT BREAK IS MEASURABLE AND WAS TESTED. `find` returns matching ARTIFACTS, never references. Measured: `find .aw/records -iname '*wtiso*'` returns TWELVE files including the DIFFERENT Set `wtisoland`, a `wtisodebt` item, and unrelated docs, while `aw find plans wtiso` correctly returns THREE. That is the exact argument against a naive `find | grep` and is the property a filename tier most endangers.
  THE CORRECTION THIS PLAN INHERITED, WHICH IS WHY THE FILTER WAS NEVER CHEAP. The item records, and `tests/test_selector_zero_open.py::PrecedenceForcesFrontMatterReadsTests` proves, that a stem or substring query CANNOT be made read-free while `_PRECEDENCE` (`selectors.py:71`) stays frozen: `stem` and `substring` sit LAST, so the resolver must first establish that `setid` and `status` did not match, and both live in front matter. A token really can be both a Set id and a filename fragment. That proof is now the thing this plan PINS rather than the constraint it designs around, since precedence is not changing and no filter is being added.
- Scope: PIN THE `aw find` MATCHING CONTRACT AS TESTS AND CHANGE NO MATCHING BEHAVIOR. Add contract tests for artifacts-not-references and the per-kind semantics; add a characterization test pinning today's backtick-quoting behavior; document the `parse_clustered` id6-slot hazard (F-16) and the frozen-precedence rationale in the comment block that already carries the per-kind prose. EXCLUDES, ON THE MAINTAINER'S 2026-09-11 OQ-03 RULING, THE FILENAME CANDIDATE FILTER ITSELF and everything that existed only to serve it: no change to `_hits_for(MATCH_ID6)`, no cost decomposition, no corpus-wide differential, no timing claim. Also excludes, as before, the non-conforming-filename report (Order 02), quoting normalization (Order 02), any precedence change, any display-layer change, and any change to `aw search`. NO PRODUCT BEHAVIOR CHANGES: the only non-test edit is comment prose.
- Scope-Paths: agent_workflows/selectors.py, tests/test_selector_zero_open.py, tests/test_cli_find.py
- Item-Dependencies: none
- Status: reviewed
- Readiness: no-go
- Priority: medium
- Work-Kind: chore
- Set: findtier
- Order: 1
- Highest E allocated: 07
- Author: opencode its_direct/pt3-claude-opus-5-1m-us
- Id: 826o13
- From-Backlog: f8m2z2

## Workflow history

- 2026-09-11 re-scope (opencode its_direct/pt3-claude-opus-5-1m-us): NOT A REVIEW, and deliberately not labelled one: `plan_readiness.is_review_history_entry` reads the label to decide whether a record states a verdict, so labelling a descope `reviewed` makes the gate parse this record's mention of `no-go` as a FRESH negative verdict. Measured: it did exactly that, turning `newest_verdict` negative and failing `tests/test_plan_readiness.py::ApprovalGateRealCorpusTests::test_no_pending_plan_is_refused_on_a_verdict_today`. The round-1 review record remains the newest REVIEW record and its neutral verdict still stands. OQ-03 ANSWERED by the maintainer and the plan RE-SCOPED IN PLACE: keep the tests, DROP the filter. This is a FOURTH option, narrower than the execute-as-narrowed choice review offered and less wasteful than retiring to `not-executed`. Stated basis: ~12.8ms net of a ~450ms wait does not justify changing the ONE resolver every verb and all ten record types route through, and the corpus-wide differential needed to prove such a change safe is expensive precisely BECAUSE the change is dangerous. WHAT CHANGED: E-02, E-03, E-05 and E-06 REMOVED (they existed only to measure, build, prove and time the filter); `Highest E allocated` advanced 06 -> 07 so their ids are RETIRED rather than reused, which is why the surviving numbering is E-01, E-04, E-07; new E-07 records the declined optimization with its measured numbers, the id6-only limitation (F-13), the F-16 slot hazard and the frozen-precedence rationale in the comment block that already owns that prose; title, Concern, Scope and Goal rewritten so the plan no longer claims a speedup; `Work-Kind` `feature` -> `chore` since nothing user-visible changes; V-01 now demands a diff proving the pins required NO product change, so this plan cannot quietly alter matching behavior to make its own tests pass. REVIEW ROUND 2 APPENDED to the review record closing PR-001 as FIXED-by-descope (not `deferred`, which `review_findings.is_resolved` treats as unresolved and which would assert something still outstanding). `aw ipd lint` CONFORMING after the edits. READINESS LEFT AT `no-go` DELIBERATELY: all three refusal predicates now compute clear, but a readiness re-check may only recompute a stale verdict WITHOUT re-critiquing content, and four of six E-items were removed after round 1 read this plan, so the round-1 verdict describes a different document. A fresh /plan-review is required; advancing the field here would assert a review nobody performed. NOT DONE: no code was written and no test was added, so nothing here claims the plan is executed. The ~113.6ms display-layer double read, which is the REAL cost, is carried by its own backlog item so it is not lost when this plan closes.
- 2026-09-10 reviewed (opencode its_direct/pt3-claude-opus-5-1m-us): /plan-review REVIEWED - OPEN QUESTIONS; NO-GO; PR-001..PR-008. `aw ipd lint --phase author` conformed BEFORE semantic review and `--phase review-finalize` after. THE REVIEW'S CENTRAL ACT WAS TO MEASURE THE PLAN'S OWN PREMISE RATHER THAN TRUST IT, and the measurement re-scopes the plan. The "70x gap" is real at the command level (20ms filename listing versus 530ms `aw find`) but attributes to the resolver a cost that is mostly elsewhere: of ~450ms, interpreter start plus `import cli` is ~115ms, the resolver TOTAL is ~42.5ms of which traversal is ~29.5ms and IRREDUCIBLE by a filename filter, and the display layer's `scan_plans` is ~113.6ms re-reading the same 616 records. The NET prize is ~12.8ms, about 3% of the wait, with the display layer 8.9x larger (PR-001/F-12). OQ-03 asked exactly this and was authored non-blocking pending measurement; the measurement now exists, so OQ-03 is ESCALATED TO `Blocking: yes` with three named choices, because whether to change the ONE resolver all ten types and every verb share for 3% is the maintainer's priority call, not an executor's. TWO DESIGN DEFECTS WERE FOUND BY RUNNING THE CODE. First, E-03 claimed three filterable kinds; instrumenting `_read_header` shows EVERY kind reads all 616 headers today, because `setid`/`status` precede `stem`/`substring`, so those two have already paid the read and filtering them saves zero: the filter is narrowed to `id6` alone (PR-002/F-13). Second, E-04's quoting normalization CONTRADICTED E-05: simulated, normalizing `` - Set: `awoptimize` `` flips `aw find research awoptimize` from FOUR files by substring to ONE by setid, because `setid` outranks `substring`, so "normalize" and "resolved sets must be identical" could not both hold; E-04 is INVERTED to a characterization test pinning today's behavior and the normalization is handed to Order 02, where it changes no selector answer (PR-003/F-15). THREE SAFETY CORRECTIONS TO THE FILTER ITSELF: the exception set must be COMPUTED via `parse_clustered` (4 of 616 candidates, 0.6%) and never hardcoded as nine, or a legacy record added later is silently dropped (F-18); the token must be matched against the WHOLE FILENAME, never the parsed identity slot, because `parse_clustered` reports `id6='assess'` for a legacy name whose real Id is `wvlk84` (F-16, Order 02's F-13 reaching Order 01); and E-05's differential must span ALL record types and compare the winning KIND, not only paths, since F-15 is a case where the kind flips. TWO RECORDED FACTS WERE STALE. The suite baseline `1 failed, 5648 passed` blaming `test_orchestrator_retirement.py` is wrong: measured `1 failed, 5958 passed, 3 skipped, 2 xfailed`, the failure being an environmental `test_reporting_contract.py` case caused by an untracked local `opencode-recovery/` tree, while `test_orchestrator_retirement.py` passes (112 passed) (F-17). And the nine-versus-Order-02's-ten discrepancy is NOT an error in either plan: a bounded 4096-byte header scan finds nine, a full-body scan finds ten, so nine is exactly right for a resolver that reads `_HEADER_BYTES` (F-14). Also corrected: five pending plans now declare `selectors.py`, not three. `Priority: medium` and `Work-Kind: feature` added from source item `f8m2z2`. No product code was modified by this review.
- 2026-09-08 to-review (opencode its_direct/pt3-claude-opus-5-1m-us): Graduated from backlog `f8m2z2` as Order 01 of a two-child Set. EVERY MEASUREMENT RE-VERIFIED AT HEAD rather than trusted, and three numbers moved. (1) The timing gap WIDENED: 6ms filename listing versus 429ms `aw find`, against the item's 20ms/357ms, because the corpus grew to 582 indexed plans. (2) The status-query trap re-measured: `-iname '*approved*'` returns 5 files, NONE approved, while 17 pending plans genuinely are; the item recorded 3 and 16. (3) The absent-from-filename set is NINE, not the item's 8, and the ninth is NOT a legacy name: it is `uyeko5` "declared" by research prompt `27rjro`, the quoted-example parser artifact owned by `cqytxf`/`76w6mq`, so it must not be counted as a permanent exception. THE ARTIFACTS-NOT-REFERENCES CONTRACT WAS TESTED, not assumed: `-iname '*wtiso*'` returns 12 files including the different Set `wtisoland`, while `aw find plans wtiso` returns 3. THE DESIGN IS CONSTRAINED BY THE ITEM'S OWN INHERITED CORRECTION: `PrecedenceForcesFrontMatterReadsTests` proves a stem or substring query cannot be read-free while `_PRECEDENCE` is frozen, so tier 1 is authored as a CANDIDATE FILTER rather than a precedence change. NOTED OVERLAP the executor must handle: three pending plans (`76w6mq`, `xo3244`, `paw8so`) already edit `selectors.py`, and `76w6mq`/`xo3244` both touch the same three readers; whichever lands last must re-read the others.

## Goal

Make the matching rules `aw find` already follows impossible to break by accident, and change none of them.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: pin the matching contract

- [ ] E-01 PIN THE ARTIFACTS-NOT-REFERENCES CONTRACT AS TESTS. It was authored as a guard to place BEFORE a resolver change; with the filter declined it is the plan's PRIMARY deliverable, because the contract is a real property of `aw find` that nothing currently pins.
  THE MEASURED CASES TO PIN: `aw find plans wtiso` must return the three `wtiso` Set plans and must NOT return `wtisoland` or `wtisodebt`; `aw find plans <id6>` must return only the artifact that IS that id6, never one that merely mentions it. The item's example is that `y6mfgo` resolves to one record while six plans mention it.
  PIN THE PER-KIND SEMANTICS TOO: `setid` is deliberately MULTI-target, `id6`/`path`/`stem` are UNIQUE_KINDS whose multi-match is a data bug, and `substring` is the explicit last resort. Those are documented at `selectors.py:52-58` and are the rules any future optimization must not blur.
  THESE TESTS MUST PASS UNCHANGED AT HEAD, and that is the whole point rather than a precondition: this plan changes no matching behavior, so a red test here means the test is wrong, NOT the resolver. Do not "fix" `selectors.py` to make one pass; report it as a genuine defect with its own carrier.
  - Depends on: none
  - Expected outcome: tests pinning the artifacts-not-references contract and the per-kind semantics, green at HEAD with no product change.
  - Execution state: pending

### Task group 2: pin today's quoting behavior rather than changing it

- [ ] E-04 DO NOT NORMALIZE QUOTING IN THIS PLAN; PIN THE CURRENT BEHAVIOR AND HAND THE CHANGE TO ORDER 02. THE ORIGINAL E-04 CONTRADICTED THE THEN-PRESENT E-05 AND REVIEW MEASURED THE CONTRADICTION (F-15), so this item was inverted rather than deleted; it now stands on its own as one of the two test pins this plan exists to land.
  WHAT WAS MEASURED. The record is `.aw/records/research/reference/202608/20260821-awoptimize-03-effzzi-...roadmap.md`, whose front matter reads `` - Set: `awoptimize` `` with backticks, so `_read_setid` returns `` '`awoptimize`' `` today. Normalizing it makes that record newly match the `setid` rule. Because `setid` (precedence 3) OUTRANKS `substring` (precedence 6), the winning KIND for the token flips and the answer changes: `aw find research awoptimize` returns FOUR files by substring today, and ONE file by setid after normalization. Run at review, before and after a simulated normalization.
  WHY THAT FORBIDS IT HERE, AND THE REASON OUTLIVED THE DESCOPE. It was originally forbidden because the then-present E-05 required identical corpus-wide before/after resolved path sets. That E-item is gone, but the Scope's own prohibition on changing which record wins any query remains and is now the plan's DEFINING constraint, so the conclusion is unchanged and is in fact stronger: a quoting normalization is a MATCHING-BEHAVIOR change, in the same family as the `_STATUS_RE` parity constraint documented at `selectors.py:112-120` ("do not harmonize the two patterns without owning that contract change").
  WHAT TO DO INSTEAD: add a CHARACTERIZATION test asserting the CURRENT behavior, that `_read_setid` returns the backtick-bearing value verbatim and that `aw find research awoptimize` resolves by `substring` to four files. Cite this item in the test so a future reader knows the behavior is pinned deliberately and where the fix belongs. Order 02 (`3i6rso`) already owns quote normalization at ITS comparison site, where it is a report-only concern that changes no selector answer.
  - Depends on: none
  - Expected outcome: a characterization test pinning today's backtick behavior and the four-file substring resolution, and NO change to `_read_setid`.
  - Execution state: pending

### Task group 3: record why the filter was declined, where the next reader will look

- [ ] E-07 WRITE THE DECLINED-OPTIMIZATION RATIONALE AND THE id6-SLOT HAZARD INTO THE COMMENT BLOCK THAT ALREADY OWNS THIS PROSE (`selectors.py:52-58`), so the next reader does not re-derive a dead end. This is the plan's only non-test edit and it changes no behavior.
  RECORD THE MEASUREMENT, NOT AN OPINION: a filename candidate filter on the `id6` rule was measured at ~12.8ms net of a ~450ms `aw find plans <id6>` (~3%), while interpreter start plus `import cli` is ~115ms and the display layer's `plans_index.scan_plans` re-read is ~113.6ms. State that it was DECLINED on that basis by the maintainer on 2026-09-11, and state where the real cost is, so a future reader optimizes the right thing.
  RECORD WHY ONLY `id6` COULD EVER BENEFIT (F-13): `setid` and `status` precede the filename rules, so a `stem` or `substring` query has already paid the full header read before its own rule runs. Without this, someone will extend a filter to kinds where it is pure overhead.
  RECORD THE F-16 HAZARD, which is a live trap independent of any filter: `parse_clustered("20260817-1357-01-assess-bugs-leftover-remove-dataloss.ipd.md")` returns conformant with `id6='assess'` and `ID6_RE.match('assess')` is True, so the PARSED FILENAME SLOT IS NOT A SAFE id6 DISCRIMINATOR; that record's real declared Id is `wvlk84`. Any future filename-based matching must test the WHOLE filename.
  RECORD THAT `_PRECEDENCE` IS FROZEN AS SEMANTICS, citing `PrecedenceForcesFrontMatterReadsTests`, so a reader does not reorder it and silently change which record wins existing queries.
  NOTE AT `_read_setid` that its backtick-bearing return value is PINNED by E-04's characterization test and that the fix belongs at Order 02's report-only comparison site (F-15).
  - Depends on: E-01
  - Expected outcome: comment prose recording the declined optimization with its measured numbers, the id6-only limitation, the F-16 slot hazard, the frozen-precedence rationale, and the `_read_setid` pin, with NO executable change.
  - Execution state: pending

## Project conventions discovered (Step 0)

- THE RAW TIMING GAP IS REAL (20ms filename listing versus 530ms `aw find` at HEAD) BUT MOSTLY NOT THE RESOLVER: measured at review, the resolver is ~42.5ms of ~450ms, its traversal (~29.5ms) is irreducible by a filename filter, and the net prize is ~12.8ms. Interpreter+import (~115ms) and the display layer (~113.6ms) each dwarf it. See F-12; this is what re-opened OQ-03 as blocking.
- ONLY THE `id6` KIND CAN BENEFIT: every kind reads all 616 headers today because `setid`/`status` precede the filename rules, so `stem` and `substring` have already paid the read (F-13).
- THE FALLBACK IS MANDATORY FOR TWO MEASURED REASONS: status is not in filenames (5 filename hits, 0 approved, versus 17 real), and NINE records carry a declared identity absent from their filename, including `25kzda` which this repo cites constantly.
- THE NINTH IS AN ARTIFACT, NOT A LEGACY NAME: `uyeko5` "declared" by research prompt `27rjro` is the quoted-example parser bug owned by `cqytxf`/`76w6mq`. Do not count it as a permanent exception.
- `_PRECEDENCE` IS FROZEN AND THAT IS SEMANTICS, NOT LAZINESS: `tests/test_selector_zero_open.py::PrecedenceForcesFrontMatterReadsTests` proves a stem or substring query cannot be read-free because `setid`/`status` must be eliminated first, and a token can be both a Set id and a filename fragment.
- THE PER-KIND SEMANTICS ARE DOCUMENTED AT `selectors.py:52-58`: `setid` is deliberately multi-target; `path`/`id6`/`stem` are `UNIQUE_KINDS` whose multi-match is a data bug; `substring` is the explicit last resort.
- ARTIFACTS, NOT REFERENCES, IS A CONTRACT AND IS MEASURABLE: `-iname '*wtiso*'` returns 12 including the different Set `wtisoland`; `aw find plans wtiso` returns 3.
- `e32j35` ALREADY LANDED THE TEXT-FREE ENUMERATION this plan needs (`selectors._iter_paths`, `:397`) sharing ONE traversal with the header-reading view. Consume it.
- THE DISPLAY LAYER IS A SECOND, LARGER READER: `cli._find_type_records` calls `plans_index.scan_plans` independently of the resolver. `e32j35` measured 938 opens across 469 files end to end.
- QUOTING MUST NOT BE NORMALIZED HERE: a backtick-quoted `` - Set: `awoptimize` `` produced a phantom miss in the item's audit, but normalizing it FLIPS `aw find research awoptimize` from four files by substring to one by setid, a matching-behavior change this plan's own E-05 forbids (F-15). Order 02 owns it.
- THE PARSED FILENAME SLOT IS NOT A SAFE id6 DISCRIMINATOR: `parse_clustered` reports `id6='assess'` for a legacy name whose real declared Id is `wvlk84` (F-16). Match the whole filename.
- THE EXCEPTION SET MUST BE COMPUTED AT RUNTIME, not hardcoded: 4 of 616 plan candidates are non-conforming today and the population changes (F-18).
- FIVE PENDING PLANS EDIT `selectors.py` (`6ltz1y`, `lznpv6`, `paw8so`, `76w6mq`, `xo3244`), re-scanned at review, plus this Set's own two; `76w6mq` and `xo3244` touch the same readers. Whichever lands last must re-read the others.
- Shared checkout, concurrent edits, suite runs BARE. Re-locate every symbol by name.

## Findings

| Id | Severity | Area | What | Evidence |
|---|---|---|---|---|
| F-1 | HIGH | the gap is real and widened | 6ms for `find -iname '*lus9ou*'` versus 429ms for `aw find plans lus9ou`, against the item's 20ms/357ms; the corpus grew to 582 indexed plans. | timed at HEAD |
| F-2 | HIGH | status cannot come from filenames | `-iname '*approved*'` returns 5 files, NONE approved (two `apprvguard`, one `w6mqc0`, one literally named `auto-approved` whose status is `executed`); 17 pending plans genuinely are. | measured at HEAD |
| F-3 | HIGH | nine records' identity is absent from their filename | Eight legacy (`4o5lt9`, `wvlk84`, `lus9ou`, `8q6yr9`, `7qx7ys`, `4w7d6s`, `25kzda`, `5tapom`) plus one artifact. Filename-only matching would make `25kzda` uncitable. | scan of all tracked records at HEAD |
| F-4 | HIGH | the ninth is a parser artifact, not a legacy name | `uyeko5` is "declared" by research prompt `27rjro` via a quoted example; owned by `cqytxf`/`76w6mq`, so it is temporary and must not be counted as a permanent exception. | the `27rjro` document; `cqytxf` |
| F-5 | HIGH | precedence cannot be reordered without sign-off | `PrecedenceForcesFrontMatterReadsTests` proves `stem`/`substring` cannot be read-free while `setid`/`status` must be eliminated first; a token can be both. So tier 1 must be a FILTER. | `tests/test_selector_zero_open.py:184`; `selectors.py:71` |
| F-6 | HIGH | the artifacts-not-references contract is measurable | `-iname '*wtiso*'` returns 12 files including the different Set `wtisoland` and a `wtisodebt` item; `aw find plans wtiso` returns 3. | both run at HEAD |
| F-7 | MEDIUM | the filter must be a superset filter | It may only remove files that cannot match. For an `id6` query, naive filename filtering would drop `lus9ou`'s legacy-named plan, which is a correctness bug rather than an optimization. | F-3's exception set |
| F-8 | MEDIUM | the display layer may dominate the cost | `cli._find_type_records` uses `plans_index.scan_plans`, a reader independent of the resolver; `e32j35` measured 938 opens across 469 files end to end. Filtering only the resolver may yield little user-visible gain. | `e32j35`'s recorded measurement |
| F-9 | MEDIUM | the enumeration this needs already exists | `selectors._iter_paths` (`:397`) is text-free and shares one traversal with the header-reading view, landed by `e32j35`. | read at HEAD |
| F-10 | LOW | quoting caused a phantom miss once | A backtick-quoted `` set: `awoptimize` `` was a false positive in the item's audit. | the item's record |
| F-11 | LOW | three pending plans edit this file | `76w6mq`, `xo3244`, `paw8so` all declare `selectors.py`; two touch the same readers. Re-measured at review: FIVE pending plans now declare it (`6ltz1y`, `lznpv6`, `paw8so`, `76w6mq`, `xo3244`), plus this Set's own two. | their `Scope-Paths`, re-scanned at review |
| F-12 | HIGH | THE PRIZE IS ~3% OF THE WAIT, AND THE PLAN ATTRIBUTED TO THE RESOLVER A COST THAT IS MOSTLY ELSEWHERE | Of ~450ms end to end: interpreter+import ~115ms, resolver TOTAL ~42.5ms (traversal ~29.5ms IRREDUCIBLE by a filename filter, header reads ~13.4ms), display `scan_plans` ~113.6ms. Net prize after ~0.5ms filter overhead is ~12.8ms. Display layer is 8.9x the prize. This ANSWERS OQ-03 against the plan's speed rationale. | all timed at review, best-of-N |
| F-13 | HIGH | THE FILTER CAN ONLY HELP `id6`, NOT THE THREE KINDS E-03 NAMED | Instrumented header reads per query at review: `id6` 616, `setid` 616, `status` 616, `stem` 616, `substring` 616. `stem`/`substring` are precedence 5/6, so `setid`/`status` have ALREADY forced the full read before they run; filtering them saves zero and costs a parse. Only `id6` (precedence 2) can benefit. | `_read_header` instrumented at review |
| F-14 | MEDIUM | NINE VERSUS ORDER 02's TEN IS A WINDOW DIFFERENCE, NOT AN ERROR | Bounded 4096-byte header scan finds NINE; FULL-BODY scan finds TEN (a second `uyeko5` quoted example in `takpys`'s report, beyond the header window). Nine is right for a resolver that reads `_HEADER_BYTES`; ten is right for a whole-file report. | both scans run at review; `selectors.py:317` |
| F-15 | HIGH | E-04 CONTRADICTED E-05 AND WOULD HAVE CHANGED A MATCHING ANSWER | Normalizing `` - Set: `awoptimize` `` makes that record match the `setid` rule, and `setid` outranks `substring`: `aw find research awoptimize` returns FOUR files by substring today and ONE by setid after normalization. E-05 demands identical before/after sets, so both could not hold. E-04 is inverted to a characterization test. | simulated normalization run at review |
| F-16 | MEDIUM | THE PARSED FILENAME SLOT IS AN UNSAFE DISCRIMINATOR HERE TOO | `parse_clustered("20260817-1357-01-assess-bugs-leftover-remove-dataloss.ipd.md")` returns conformant with `id6='assess'`, and `ID6_RE.match('assess')` is True, so a slot-comparing filter treats a legacy record (declared `wvlk84`) as modern and may drop it. Match the whole filename instead. This is Order 02's F-13 reaching Order 01. | both calls run at review |
| F-17 | MEDIUM | THE AUTHORED SUITE BASELINE IS STALE AND NAMES THE WRONG FAILURE | Authored baseline `1 failed, 5648 passed` blaming `test_orchestrator_retirement.py`. Measured at review: `1 failed, 5958 passed, 3 skipped, 2 xfailed`, the failure being `test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, environmental from an untracked local `opencode-recovery/` (1746 files); `test_orchestrator_retirement.py` passes `112 passed`. | bare suite + targeted run at review |
| F-18 | MEDIUM | THE EXCEPTION SET MUST BE COMPUTED, NOT HARDCODED | 4 of 616 plan candidates (0.6%) are non-clustered-conformant. A filter unioning a HARDCODED list of nine would silently drop any legacy record added later. Union with a runtime `parse_clustered` test instead. | measured at review |
| F-19 | LOW | THE BLAST RADIUS IS ALL TEN TYPES, NOT PLANS | `resolve` is the ONE resolver for every type and every verb (`selectors.py:5-9`), so an id6-rule change reaches `aw set`/`rename`/`show` as well as `find`. E-05's differential must span types. | `selectors.py:5-9` |

## Proposed changes (ordered, validatable)

1. Pin the artifacts-not-references contract and the per-kind semantics (E-01).
2. Pin today's quoting behavior with a characterization test and route normalization to Order 02, rather than changing a matching answer (E-04).
3. Record in the comment block why the filename filter was declined with its measured numbers, why only `id6` could ever have benefited, the F-16 slot hazard, the frozen-precedence rationale, and the `_read_setid` pin (E-07).

E-02, E-03, E-05 and E-06 were REMOVED when the maintainer resolved OQ-03 on 2026-09-11: they existed only to build, prove and time the filename candidate filter. Their ids are retired and not reused, which is why the surviving numbering is E-01, E-04, E-07.

## Deferred / out of scope (with reason)

- THE FILENAME CANDIDATE FILTER ITSELF, WHICH WAS THIS PLAN'S ORIGINAL POINT. DECLINED by the maintainer on 2026-09-11 resolving OQ-03. Not deferred and not awaiting a better measurement: at ~12.8ms net of a ~450ms wait (~3%, F-12) it does not justify changing the ONE resolver every verb and all ten record types route through, and the corpus-wide differential required to prove such a change safe is expensive precisely BECAUSE the change is dangerous. E-03 built it, E-02 measured for it, E-05 proved it and E-06 timed it; all four are removed. Anyone reviving this must re-open the priority call, not merely re-run the numbers.
- THE NON-CONFORMING-FILENAME REPORT. Order 02 (`3i6rso`) owns the item's second added requirement. Split because it is a reporting surface with its own test bed, and because it is what makes the exception set VISIBLE and shrinking, which is a different deliverable from a lookup optimization.
- QUOTING NORMALIZATION, moved OUT of this plan at review and still out. It flips `aw find research awoptimize` from four substring matches to one setid match (F-15), which this plan's Scope forbids. Order 02 owns it at its own comparison site, where it changes no selector answer. This plan PINS today's behavior instead (E-04).
- UNIFYING THE RESOLVER AND `plans_index` READERS, which F-12 identifies as the ACTUAL dominant cost (~113.6ms, ~9x the declined filter's prize). Deliberately not attempted here: `selectors.py:112-120` records that the two readers disagree on 24 records BY CONTRACT and must not be harmonized without owning that matching change. Carried by its own backlog item (see the Findings F-8/F-12 rows) so the cost is not lost when this plan closes; it is a separate plan with its own sign-off.
- CHANGING `_PRECEDENCE`. Forbidden here: `PrecedenceForcesFrontMatterReadsTests` proves the order is semantics, and reordering would change which record wins existing queries, needing sign-off the item explicitly flags.
- THE DISPLAY LAYER (`cli._find_type_records` / `plans_index.scan_plans`). Not measured and not changed by this plan; with E-02 removed, this plan makes no timing claim at all.
- `aw search`. A content search legitimately returns every mention; only `find` claims to return the artifact that IS the selector.
- THE INDEX-FIRST DESIGN. Displaced by the maintainer in `faf2e18b` and superseded (`e32j35`), and `ila6vl` is untracking the INDEX manifests, which removes the speed argument for them entirely. Do not reintroduce a manifest-freshness primitive.
- THE `uyeko5`/`27rjro` PARSER ARTIFACT. Owned by `cqytxf`/`76w6mq`. With the filter declined this plan no longer has an exception set to tolerate it in; it is listed only so a reader does not think the artifact was fixed here.
- THE RESEARCH YAML DIALECT. `05aqbj`/`xo3244` own teaching the readers both front-matter dialects; this plan consumes whatever reader exists.

## Scope check

- Over-scope: none. One resolver filter on one match kind, one characterization test, two test modules. The original E-04 quoting normalization WAS over-scope (it changed a matching answer, F-15) and has been inverted to a pin.
- Scope-Paths justification: `agent_workflows/selectors.py` holds `_PRECEDENCE` (`:71`), the per-kind semantics comment (`:52-58`), `_iter_paths` (`:397`), `_read_setid` (`:304`) and `resolve`, which is where the id6 candidate filter belongs (E-03) and where E-04's routing comment goes; `tests/test_selector_zero_open.py` holds `PrecedenceForcesFrontMatterReadsTests`, the proof that constrains the design and that must stay green, plus the open-count assertions E-02 and E-06 extend and the synthetic-legacy regression test E-05 adds; `tests/test_cli_find.py` is where the artifacts-not-references contract tests belong (E-01) since it exercises the verb an operator runs. `cli.py` and `plans_index.py` are deliberately NOT declared: the display layer is measured and reported, not changed, even though F-12 shows it is the larger cost.
- Under-scope, stated rather than left as `none`: this plan does not add the filename report, does not change precedence, does not touch the display layer, does not alter `aw search`, does not reintroduce an index-first design, does not fix the `27rjro` parser artifact, does not teach the readers a second dialect, does not normalize quoting, and does not filter the `stem`/`substring`/`setid`/`status` kinds. Each is excluded with a reason above.

## Required tests / validation

- `python3 -m pytest` BARE, before and after, both summary lines pasted and the FAILURE SETS compared (not counts). Baseline measured at review 2026-09-10 at HEAD `985bc263`: `1 failed, 5958 passed, 3 skipped, 2 xfailed in 56.85s`, the failure being the environmental `test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` (untracked local `opencode-recovery/`). Re-measure your own BEFORE; the authored `test_orchestrator_retirement.py` baseline is stale and that module now passes (F-17). Criterion: AFTER minus BEFORE is EMPTY, never an absolute count.
- A CORPUS-WIDE RESOLUTION DIFFERENTIAL (E-05) ACROSS ALL RECORD TYPES: for every tracked id6, every setid, and a sample of stems and substrings, the resolved path set AND the winning `kind` must be IDENTICAL before and after. Compare the kind explicitly (F-15). Hand-picked cases are NOT sufficient for a matching-engine change.
- A SYNTHETIC-LEGACY-RECORD REGRESSION TEST: a record whose declared `- Id:` is absent from its filename must still resolve by id6, so the non-conforming union cannot silently regress (F-18).
- THE NINE EXCEPTION RECORDS named individually and shown resolving, THROUGH THE CLI as well as the resolver: `4o5lt9`, `wvlk84`, `lus9ou`, `8q6yr9`, `7qx7ys`, `4w7d6s`, `25kzda`, `5tapom`, and the `uyeko5`/`27rjro` artifact. Nine is the BOUNDED-HEADER count; a full-body scan finds ten (F-14), so state the window when citing it.
- THE ARTIFACTS-NOT-REFERENCES CONTRACT: `aw find plans wtiso` returns the three `wtiso` plans (`8zgybk`, `1o4eif`, `qcqhj7`) and NOT `wtisoland` or `wtisodebt`, before and after. Verified at review: 17 filename hits versus 3 resolved.
- THE BACKTICK CHARACTERIZATION TEST (E-04): `_read_setid` returns the backtick-bearing value verbatim and `aw find research awoptimize` resolves by `substring` to FOUR files, pinning today's behavior rather than changing it.
- `PrecedenceForcesFrontMatterReadsTests` GREEN, with its own summary line, since it is the proof that constrains this design.
- BEFORE/AFTER TIMINGS AND OPEN COUNTS for an id6 query, a setid query, a status query and a substring query, with the gain expressed as a share of END-TO-END wall clock. ONLY the id6 timing may move; the other three must be UNCHANGED within noise (F-13).
- THE COST DECOMPOSITION STATED IN FOUR PARTS (interpreter+import, resolver traversal, resolver header reads, display layer) and an explicit statement of whether E-02's abort condition fired.
- MEASURE EXIT CODES UNPIPED (`cmd >/dev/null 2>&1; echo $?`).
- `aw sanitize --agent` clean.

## Spec / documentation sync

`selectors.py`'s PRECEDENCE COMMENT BLOCK (`:52-58`) is the authoritative prose on what each match kind means and must gain a statement that tier 1 is a CANDIDATE FILTER which never changes the winner, and WHY (citing the frozen-precedence proof). It must ALSO record why the filter applies to `id6` ALONE: because `setid` and `status` precede the filename rules, a `stem` or `substring` query has already paid the full header read before its rule runs, so filtering those kinds costs a parse and saves nothing (F-13). Without that, the next reader will either reorder `_PRECEDENCE` and silently change which record wins, or extend the filter to kinds where it is pure overhead.

The filter's superset-only property must be stated AT THE CODE as an invariant, not left implicit: it may only remove files that cannot match; the union with the runtime-computed non-conforming-name set is what makes that true; and the records whose declared identity is absent from their bounded header are the standing counterexample. Record that the exception set is COMPUTED and must never become a hardcoded list (F-18), and that the token is matched against the WHOLE FILENAME rather than the parsed identity slot because `parse_clustered` reports `id6='assess'` for a legacy name whose real Id is `wvlk84` (F-16). Those sentences are what stop a future change from turning an optimization into a correctness bug.

`_read_setid` must carry a note that its backtick-bearing return value is PINNED by a characterization test and why: normalizing it flips `aw find research awoptimize` from four substring matches to one setid match, so the fix belongs at Order 02's report-only comparison site, not here (F-15). This mirrors the existing `_STATUS_RE` parity constraint (`:112-120`), which is the precedent for treating a reader's strictness as a matching contract.

`_iter_paths`'s docstring already records that the text-free and text-bearing walks stay identical by construction; extend it to note the filter consumes it, so the relationship is findable from either end.

No spec change is expected: `aw find`'s behavior is defined by its own help text and these comments rather than by a spec. If the executor finds spec text promising read-free lookups, that is a false claim to report rather than to edit.

## Open questions

### OQ-01: Should tier 1 be a candidate filter or a precedence change?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: A CANDIDATE FILTER, decided by the proof the item itself inherited. `PrecedenceForcesFrontMatterReadsTests` demonstrates that `stem` and `substring` cannot be answered without first eliminating `setid` and `status`, both of which live in front matter, because one token can legitimately be both a Set id and a filename fragment. Reordering `_PRECEDENCE` would therefore change which record wins for existing queries, which is a matching-behavior change the item explicitly says needs sign-off. The filter shape captures most of the speed (it reduces how many files are opened) while leaving the winner determined by the frozen order, so it needs no sign-off and cannot silently change an answer. The cost is that the theoretical "zero opens" target is unreachable, which the item already recorded as unreachable anyway.

### OQ-02: May the filter ever remove a file that would have matched?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: NEVER; IT IS A SUPERSET-ONLY FILTER AND THIS IS A CORRECTNESS INVARIANT, not a quality goal. The nine records whose declared identity is absent from their filename are the standing counterexample: for an `id6` query, filtering by filename would drop `lus9ou`'s legacy-named plan and `25kzda`'s grandfathered spec, and the latter is cited constantly, so the tool would start failing on its most-referenced spec. E-03 therefore requires the filter either to be SKIPPED for kinds where a body-only match is possible, or to union filename candidates with a body pass over the exception set. E-05's corpus-wide differential is what proves the invariant held, and a hand-picked sample is explicitly insufficient.

### OQ-03: What if the display layer, not the resolver, dominates the cost?

- Blocking: yes
- Status: resolved
- Owner: maintainer
- Finding: PR-001
- Resolution or deferral rationale: MEASURED AT REVIEW, AND THE ANSWER IS YES: THE DISPLAY LAYER AND INTERPRETER STARTUP BOTH DOMINATE, SO THE FACTUAL QUESTION IS CLOSED AND WHAT REMAINS IS A PRIORITY DECISION ONLY THE MAINTAINER MAY MAKE. Decomposition of a ~450ms `aw find plans <id6>`: interpreter start plus `import agent_workflows.cli` ~115ms; resolver TOTAL ~42.5ms, of which traversal is ~29.5ms and IRREDUCIBLE by a filename filter; display `plans_index.scan_plans` ~113.6ms, re-reading all 616 records the resolver just read. The NET prize this plan can win is ~12.8ms, about 3% of the wait, while the display layer alone is 8.9x that and interpreter startup 9x. F-13 narrows it further: only the `id6` kind can benefit at all.
  WHY THIS IS NOW BLOCKING RATHER THAN AN EXECUTOR'S JUDGEMENT CALL. The plan authored this non-blocking on the premise that "the resolver improvement is real and safe regardless", and the safety half still holds. But the numbers invert the premise's second half: the plan's own Concern and Goal are performance claims, and at ~3% they no longer justify a change to the ONE resolver every verb and all ten record types route through (F-19). Executing it would spend a matching-engine change plus a corpus-wide differential to buy an improvement an operator cannot perceive. That is a scope-and-priority trade the maintainer owns, not the executor, so the plan must stop here rather than let an executor infer consent from a non-blocking flag.
  THE DECISION REQUIRED, in plain terms. Choose one: (A) EXECUTE AS NARROWED, accepting ~12.8ms for the correctness value that comes with it (the E-01 contract pins, the E-04 characterization test, and the F-16 discriminator hazard documented in code); (B) RE-SCOPE onto the real cost, a follow-up plan that stops the display layer re-reading the 616 records the resolver already read, which is ~114ms and roughly 9x this plan's prize; or (C) DEFER to `not-executed`, keeping only the correctness items as a small separate plan. E-02 carries an explicit abort condition so that even under (A) the executor cannot report a user-visible speedup that the measurement does not support.
  A NOTE ON (B), so it is not mistaken for a free win: the two readers are not trivially unifiable. `plans_index` and the resolver DELIBERATELY DISAGREE on 24 records, documented at `selectors.py:112-120`, where `_STATUS_RE`'s single-token `(\S+)` is called a matching-behavior CONTRACT that must not be "harmonized" without owning the change. So (B) is a real plan with its own sign-off, not a cleanup, and this review is not asserting it is cheap.

  ANSWERED BY THE MAINTAINER 2026-09-11: KEEP THE TESTS, DROP THE FILTER. This is a FOURTH option, chosen over all three the review offered, and it is narrower than (A) and less wasteful than (C). The filter is DECLINED, not deferred and not awaiting a better measurement: paying a change to the ONE resolver every verb and all ten record types route through, plus the corpus-wide differential needed to prove it safe, to buy ~3% of a wait an operator cannot perceive, is a bad trade. The maintainer's framing was that the guard is expensive precisely BECAUSE the change is dangerous.
  WHAT THIS PLAN BECOMES, and it is neither (A) nor (C): the plan is re-scoped IN PLACE rather than retired, keeping E-01 (contract pins) and E-04 (quoting characterization), removing E-02, E-03, E-05 and E-06 (which existed only to measure, build, prove and time the filter), and adding E-07 to record the declined optimization with its measured numbers plus the F-16 slot hazard in the comment block that already owns that prose. This differs from (C) in that no new plan file is authored and no history is retired; it differs from (A) in that no filter ships and no timing claim is made. Retiring to `not-executed` was rejected because the two test pins are worth landing and are already authored here.
  CONSEQUENCES A LATER READER MUST NOT MISREAD. The plan's Concern still opens with the ~450ms/20ms gap because that measurement is TRUE and is why the work was attempted; it is no longer a rationale for executing. `Work-Kind` moved `feature` -> `chore`, since nothing user-visible changes. The ~113.6ms display-layer double read is the REAL cost and is carried by its own backlog item so it is not lost when this plan closes. Reviving the filter requires re-opening the PRIORITY call, not merely re-running the numbers.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: paste the ACTUAL passing output of the contract tests. Quote the `wtiso` assertion showing `wtisoland` and `wtisodebt` are EXCLUDED, and the id6 assertion showing a mentioning plan is excluded. Paste the per-kind semantics assertions (setid multi-target, id6 unique, substring last resort). ALSO paste `git diff --stat -- agent_workflows/selectors.py` proving the pins required NO product change; a diff containing anything but comment prose is a FAILED validation, since this plan may not change matching behavior to make its own tests pass.
  - Observed evidence:
  - Result: pending

- [ ] V-04 validates E-04
  - Required evidence: paste the ACTUAL passing output of the characterization test, showing `_read_setid` returns the backtick-bearing value VERBATIM and `aw find research awoptimize` resolves by `substring` to FOUR files. Paste a diff proving `_read_setid` is UNCHANGED. A passing test that normalized the value is a FAILED validation, since the whole point is that this plan does not change that answer.
  - Observed evidence:
  - Result: pending

- [ ] V-07 validates E-07
  - Required evidence: paste the ADDED COMMENT PROSE verbatim and confirm each of the five required records is present: the declined optimization WITH its measured numbers (~12.8ms net, ~3%, against ~115ms interpreter and ~113.6ms display), the id6-only limitation and why (`setid`/`status` precede the filename rules), the F-16 slot hazard naming `assess`/`wvlk84`, the frozen-precedence rationale citing `PrecedenceForcesFrontMatterReadsTests`, and the `_read_setid` pin routing the fix to Order 02. THEN paste `git diff -- agent_workflows/selectors.py` and confirm every changed line is a COMMENT: a single changed executable line is a FAILED validation. THEN paste the BARE `python3 -m pytest` summary line and state the failure-SET delta against a freshly measured BEFORE (not a recorded baseline); criterion is an EMPTY delta.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

THE BLOCKING QUESTION IS ANSWERED AND THIS PLAN WAS RE-SCOPED IN PLACE, SO IT NEEDS A FRESH REVIEW BEFORE APPROVAL. OQ-03 was authored non-blocking on the premise that only a measurement could decide the plan's value; review MADE that measurement (F-12) and it argued against the plan's own performance rationale. The maintainer resolved it on 2026-09-11 by keeping the tests and DECLINING the filter, and review round 2 records PR-001 as `FIXED` by that descope.

`- Readiness:` DELIBERATELY STILL READS `no-go`, AND AN AGENT MUST NOT ADVANCE IT HERE. All three refusal conditions do now compute clear (`has_unresolved_blocking_question` -> False, `subject_gating_blocks` -> empty, `newest_verdict` -> neutral), so a READINESS RE-CHECK would be mechanically satisfied. That is not sufficient, and the distinction is the point: a re-check is defined as recomputing a STALE verdict WITHOUT re-critiquing plan content, but this plan's title, Concern, Scope, Goal and entire checklist were REWRITTEN after round 1 read them. Four of its six E-items are gone. The round-1 verdict therefore is not stale bookkeeping about the same document; it is a verdict about a DIFFERENT document, and recomputing three predicates cannot substitute for reading what the plan now says. Advancing readiness on that basis would assert a review that never happened, which is the forgery `AGENTS.md` forbids.

WHAT IS ACTUALLY REQUIRED: a fresh `/plan-review` pass over the re-scoped plan, which may then set readiness on its own authority, followed by human approval. The cheap alternative (re-check to `go-pending-approval`) is available and is WRONG here for the reason above; do not take it because the predicates happen to pass.

IT CARRIES NO `Blocks-Release`, because backlog `f8m2z2` carries none. Source item `f8m2z2` is `Work-Kind: feature` at `Priority: medium` (verified at review) and the maintainer did not gate it; stated so a reader does not assume a gate was dropped.

EXECUTION CONTRACT. Commit only files this plan changed, path-scoped (`git commit -m msg -- <paths>`); never `git add -A` and never push. Do NOT reorder `_PRECEDENCE`. Do NOT change `_read_setid` (E-04 pins it; changing it flips a real query's answer, F-15). Do NOT filter any kind but `id6` (F-13). Do NOT hardcode the exception set; compute it (F-18). Do NOT compare the token against the parsed filename slot; compare the whole filename (F-16). Do NOT let the filter remove a file that could match; the corpus-wide differential over ALL types, comparing winning KINDS as well as paths, is the proof, and a hand-picked sample is not. RE-READ the other pending `selectors.py` edits before starting (`6ltz1y`, `lznpv6`, `paw8so`, `76w6mq`, `xo3244`, and sibling `3i6rso`): if any has landed, this plan's changes must sit on top of them rather than reverting them. Re-locate every symbol by NAME rather than by the line numbers cited here. Paste ACTUAL timings and counts, expressed against END-TO-END wall clock; never claim a speedup you did not measure. Verify the staged set with `git diff --cached --name-only` before every commit and re-verify after any failed hook.

POST-GATE LIFECYCLE MOVE. Do not claim done or move this plan to `.aw/records/plans/executed/` until `aw ipd lint --phase pre-transition` conforms and every `V-*` above carries concrete pasted evidence, including the corpus-wide differential and the nine named exception records.
