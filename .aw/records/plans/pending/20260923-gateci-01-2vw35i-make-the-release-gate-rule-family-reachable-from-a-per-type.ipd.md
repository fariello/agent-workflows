# IPD: Make the release-gate rule family reachable from a per-type check and enforced by a named CI step instead of only by a full sweep nothing runs

- Date: 2026-09-23
- Kind: child
- Concern: THE RELEASE-GATE RULE FAMILY IS UNENFORCED IN CI, AND I CONFIRMED BOTH HALVES OF THAT AT HEAD `22cf67d9`. Backlog `wu8qjy` (`high`, `Blocks-Release: next`) says no workflow step runs `aw check all` and the one step that could report it is advisory. MEASURED: `grep -c "agent_workflows check all"` across every file in `.github/workflows/` returns ZERO (`tests.yml`, `local-leaks.yml`, `secret-scan.yml` all 0). So the sweep that hosts the family never runs in CI at all.
  THE RULES ARE ALSO UNREACHABLE FROM THE PER-TYPE CHECK, WHICH IS THE OTHER HALF. `aw check backlog --agent` reports 5 findings on this tree (4 at authoring), none of them from the gate family, while `aw check all --agent` reports 26 (49 at authoring) INCLUDING `check.live-bug-ungated` against a real item (`.aw/records/backlog/open/20260918-7l1ggb-...`). One live, ungated `bug` exists right now and no per-type invocation can see it. That matches `wu8qjy`'s own measurement exactly ("`aw check backlog` CANNOT report any of these rules"), and it means the rule family is wired only into the once-per-full-sweep seam inside `check_types`. (Counts re-measured in review; they drift, which is why E-01 re-derives them rather than asserting them.)
  REVIEW CORRECTION, AND IT IS THE DECIDING FACT OF THIS PLAN: THE CURRENT PLACEMENT IS NOT AN OVERSIGHT. `check_types` carries an explicit comment beside `check_live_bug_gate` stating the consequence this plan calls a defect, and REJECTING the remedy E-02 proposes: "IT IS THEREFORE NOT REPORTED BY `aw check backlog`, and that is a known, stated cost, not an oversight ... Wiring it into the backlog content path instead was REJECTED because it would surface this ONE rule while its four siblings stayed invisible on the same command, which is a more confusing contract than 'the family lives on the full sweep'. Consumers must use `aw check` / `aw check all`." The same block records WHY the rule is not folded into `check_release_gate_consistency`: that function is composed by `check_commit_invariants`, the opt-in PRE-COMMIT aggregator, whose every rule is commit- or receipt-scoped, so a whole-tree rule there "would refuse a commit because some OTHER party's bug item elsewhere in the tree is ungated - exactly the shared-checkout failure AGENTS.md warns against." So E-02 must argue against a recorded decision rather than fill a gap, and the plan never acknowledged that decision exists. See F-6.
  AND THE PROPOSED CI STEP WOULD BE RED THE DAY IT LANDS, ON TWO FINDINGS NEITHER OF WHICH THIS PLAN MAY FIX. Measured in review: the family produces exactly 2 findings on this tree and `drift_exit_code` is 1. (a) `check.live-bug-ungated` on `7l1ggb`, which the plan names as its regression target -- but that item's own history records a DELIBERATE maintainer decision that it is not a gate ("Not a release gate: the instance was repaired in zz5yxq's commit ... the residual work is a test-harness hardening, not a shipping defect"), and the plan's own deferral forbids gating it. (b) `check.from-backlog-dangling` on an `executed/` plan whose `- From-Backlog:` value is the literal sentinel `none`; executed plan `rgaasb` already recorded that exact finding as "pre-existing and NOT this plan's to fix", and `AGENTS.md` forbids adding commits to a plan in `executed/`. So E-03 as written reproduces the very red-gate failure the plan cites `4y7nzh` to avoid. See F-7 and F-8.
  SO THE GATE IS DOUBLY DEAD: unreachable from the check a developer or hook would plausibly run, and absent from CI even in the form that can report it. `wu8qjy` was filed because the parent Set's completion criterion ("`aw check` reports a live bug with no gate, and CI fails on it") is only half met; the first clause is true under `check all` and the second is false everywhere.
  AND THE CI STEP THAT DOES RUN `check backlog` IS DELIBERATELY ADVISORY, FOR A REASON THAT MUST BE RESPECTED RATHER THAN OVERRIDDEN. `tests.yml` runs it with `|| true` and an explanatory comment: backlog is "ADVISORY (report-only), NOT fail-closed: the tree carries pre-existing backlog name/summary debt ... that a fail-closed gate would red `main` on. It joins the fail-closed set above once that baseline is cleaned by a separate migration (DECISION 18-r2ks4k-D1)". That debt is REAL and still present: `check backlog` reports 5 findings on this tree today (4 at authoring: `check.name-nonconformant` plus the `info` `check.collisions-not-checked`). So simply deleting the `|| true` would red `main` on unrelated naming debt, which is why this plan must separate the GATE rules from the CONFORMANCE rules rather than flipping one switch.
  ONE MORE MEASURED FACT THE PLAN'S PREMISE OMITS, AND IT WEAKENS THE "CI WOULD CATCH IT" STORY: THE EXISTING FAIL-CLOSED STEP IS ALREADY RED. `aw check plans --agent` exits 1 with 12 findings on this tree (11 `check.ipd-uncarried-obligation` plus the `info` collisions notice), and THIS PLAN is one of them. So the job E-03 wants to join is not a green job gaining a gate; it is a job already failing for an unrelated reason. That does not invalidate the plan, but it does mean E-03 cannot claim its step is what turns the job red, and it means the plan must clear its own carrier finding before it can finalize. See F-9.
  THE SHAPE OF THE FIX IS THEREFORE NOT "MAKE `check backlog` FAIL CLOSED". It is to make the release-gate family reachable per-type AND enforced by its own named step, so a live ungated bug blocks a merge while the pre-existing naming debt stays advisory until its own migration lands. That distinction is the whole design and is what OQ-01 asks the reviewer to confirm.
- Scope: Make a live ungated release-blocking bug VISIBLE to CI as its own named step, without red-lining `main`. IN: (a) make the release-gate rule family reachable outside the full-sweep seam, as a WHOLE family rather than one rule of it, per OQ-01 and consistent with the recorded decision F-6 quotes; (b) add a NAMED CI step running exactly those rules, fail-closed if and only if the family is clean and otherwise advisory with the blocking findings named (REVISED in review per F-7/F-8: the authored scope said "fail-closed" unconditionally, which reds `main` on two findings this plan may not fix), leaving the existing advisory `check backlog` step as it is; (c) tests proving every family member is reachable, that it fires on a synthetic violation, and that it does NOT fire on a clean one or on a legitimately handed-off bug. OUT: flipping the existing `check backlog` step to fail-closed, which `DECISION 18-r2ks4k-D1` defers to a separate baseline-cleaning migration and which the measured 4 findings would immediately red; cleaning that naming debt; and adding `aw check all` wholesale to CI, which would enforce 49 findings' worth of unrelated rules in one step.
- Scope-Paths: agent_workflows/check_engine.py, .github/workflows/tests.yml, tests/test_check_engine_release_gate.py
- Item-Dependencies: none
- Status: reviewed
- Readiness: go-pending-approval
- Set: gateci
- Order: 1
- Highest E allocated: 04
- Author: opencode/its_direct/pt3-claude-opus-5-1m-us
- Id: 2vw35i
- From-Backlog: wu8qjy
- Blocks-Release: next

## Workflow history
- 2026-09-24 reviewed (aw set): status set to reviewed

- 2026-09-24 /plan-review (opencode/its_direct/pt3-claude-opus-5-1m-us): APPROVE WITH REVISIONS APPLIED; PR-001..PR-009, all FIXED. Findings recorded in `.aw/records/reviews/20260923-gateci-01-2vw35i-...review.md` Round 1. Structural lint `conforming` at `author` before review and at `review-finalize` after.
  THE PLAN'S DIAGNOSIS HOLDS AND ITS DELIVERABLE DID NOT (PR-001/PR-002, now F-7/F-8). F-1 and F-2 both re-confirmed: zero `check all` in any workflow, and `check_types(['backlog'])` reports ZERO family members while `['all']` reports them. But E-03's fail-closed step would be RED ON ARRIVAL: the family reports exactly 2 findings, `drift_exit_code` 1, and NEITHER is this plan's to fix. `check.live-bug-ungated` fires on `7l1ggb`, whose own record carries a deliberate maintainer decision that it is NOT a gate and which this plan's own deferral forbids gating; `check.from-backlog-dangling` fires on an `executed/` plan whose value is the literal sentinel `none`, which `AGENTS.md` forbids editing and which executed plan `rgaasb` already recorded as not-to-be-fixed. So the plan would have reproduced the exact `4y7nzh` red-gate failure it cites to avoid. E-03 is now conditional: fail-closed only if the family is clean, otherwise the advisory shape the maintainer already uses, with the blocking findings named and no suppression permitted.
  AND E-02 WAS PROPOSING TO OVERTURN A WRITTEN DECISION WITHOUT KNOWING IT (PR-003, F-6). The comment beside `check_live_bug_gate` in `check_types` states the per-type invisibility is "a known, stated cost, not an oversight" and explicitly REJECTS wiring the rule into the backlog content path, because that surfaces one rule while four siblings stay invisible; it also records why the rule is not folded into `check_release_gate_consistency` (that feeds the commit-scoped pre-commit aggregator). E-02 must now argue against that decision explicitly and expose the WHOLE family, and OQ-01 is RESOLVED to the distinct named target on that evidence.
  THREE SMALLER MEASURED CORRECTIONS: the existing fail-closed `check plans` step is ALREADY red (exit 1, 12 findings) and THIS PLAN is one of them, so the plan would have blocked its own finalize (PR-004, F-9; cleared here by adding durable carriers to six deferral rows and OQ-01); the cited counts had drifted (4/49 at authoring, 5/26 at review) and are now required to be re-derived (PR-005); and the family E-03 would enforce CONTAINS a known false-positive rule, since `4le6yz` and `0cqf33` are live gated `bug` items against `check.from-backlog-gate-mismatch` comparing `next` to a resolved id6 literally (PR-006).
  Readiness `go-pending-approval`: no unfixed BLOCKER or HIGH, no blocking open question, awaiting human sign-off only.
- 2026-09-23 to-review (opencode/its_direct/pt3-claude-opus-5-1m-us): Graduated from backlog `wu8qjy`, whose `- Blocks-Release: next` is INHERITED. Confirmed both halves at HEAD rather than trusting the filing: ZERO occurrences of `agent_workflows check all` in any workflow file, and `check backlog` reporting 4 findings none of which are gate rules while `check all` reports 49 including a real `check.live-bug-ungated`.
  THE FINDING THAT SHAPES THE PLAN is that the obvious fix is wrong. Deleting the `|| true` from the existing advisory step would red `main` immediately, because the pre-existing naming debt `DECISION 18-r2ks4k-D1` defers is still present and measurable (4 findings). So the plan separates the GATE rules from the CONFORMANCE rules and adds a narrow named step, rather than flipping a switch the maintainer deliberately left off.
  I ALSO RECORD THE LIVE VIOLATION the gate would catch today: `7l1ggb` is a live `bug` with no `Blocks-Release`, visible to `check all` and invisible to every per-type invocation. That is the concrete thing this plan makes CI see.

## Goal

Make the release-gate rule family reachable outside the full sweep and VISIBLE in CI as its own named step, fail-closed if and only if the tree is already clean for that family, while leaving the deliberately-advisory backlog conformance step untouched.

REVISED IN REVIEW. The pre-review goal was "make a live ungated bug FAIL a named CI step". That is unachievable today without red-lining `main`, and not because of the naming debt the plan already accounted for: the gate family itself reports two live findings (exit 1), and NEITHER is this plan's to fix. One is `7l1ggb`, whose record carries a deliberate maintainer decision that it is not a gate and which this plan's own deferral forbids gating; the other is a `From-Backlog: none` sentinel inside an `executed/` plan that `AGENTS.md` forbids editing. So the honest deliverable is REACHABILITY plus VISIBILITY now, with the fail-closed flip conditioned on the family actually being clean. See F-7 and F-8.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: confirm the double gap and the constraint

- [ ] E-01 RE-MEASURE THE GAP **AND THE BLAST RADIUS**, because review found the second half decides whether E-03 may land at all. Every count below DRIFTS, so re-derive them; the authoring and review figures are context, never the bar.
  CONFIRM CI NEVER RUNS THE SWEEP: grep every file under `.github/workflows/` for `check all` and paste the counts. At authoring and at review: zero in all three workflow files.
  CONFIRM THE PER-TYPE UNREACHABILITY: run `aw check backlog --agent` and `aw check all --agent` and paste both finding counts and rule sets. At review: 5 versus 26 (authoring said 4 versus 49), with `check.live-bug-ungated` present only in the second.
  CONFIRM THE ADVISORY STEP'S CONSTRAINT IS STILL LIVE, which is what forbids the one-line fix: show that `check backlog` reports findings TODAY, so removing `|| true` would fail the job on debt unrelated to release gating. If that debt has since been cleaned, SAY SO, because then the simpler fix becomes available and OQ-01's answer changes.
  READ THE RECORDED DECISION BEFORE PROPOSING TO OVERTURN IT (F-6). Quote the comment in `check_types` beside `check_live_bug_gate`: it states the per-type invisibility is "a known, stated cost, not an oversight" and explicitly REJECTS wiring the rule into the backlog content path, because that would surface one rule while four siblings stayed invisible. Also quote why it is not folded into `check_release_gate_consistency` (that function feeds `check_commit_invariants`, the pre-commit aggregator, whose rules must be commit-scoped). E-02 must be consistent with both, or must argue against them explicitly.
  MEASURE THE FAMILY'S LIVE FINDING SET AND ITS EXIT CODE, which is the gating measurement for E-03 (F-7/F-8). Run the family's functions directly and paste every finding with its location and severity, plus `artifact_core.drift_exit_code`. At review: exactly 2 findings, exit 1 -- `check.live-bug-ungated` on `7l1ggb` and `check.from-backlog-dangling` on an `executed/` plan.
  THEN CLASSIFY EACH LIVE FINDING AS FIXABLE-BY-THIS-PLAN OR NOT, and this is the item's most important output. At review NEITHER is: `7l1ggb`'s history records a deliberate maintainer decision that it is not a gate, and this plan's own deferral forbids gating it; the other sits in an `executed/` plan (value: the literal sentinel `none`) that `AGENTS.md` forbids editing and that executed plan `rgaasb` already recorded as "NOT this plan's to fix". If that is still true at execution, E-03 CANNOT land a fail-closed step without reproducing the red-gate failure this plan cites `4y7nzh` to avoid, and you must STOP and report rather than landing it. E-03 now carries that stop condition explicitly.
  MEASURE WHETHER THE JOB E-03 WOULD JOIN IS ALREADY RED (F-9): run `aw check plans --agent` and `aw check releases --agent` and paste their exit codes. At review `check plans` exits 1 with 12 findings, and THIS PLAN is one of them.
  - Depends on: none
  - Expected outcome: all measurements pasted and re-derived rather than copied; the recorded decision quoted; the family's live finding set enumerated with each member classified fixable-or-not by this plan; an explicit statement of whether E-03 can land fail-closed today; and the current exit status of the existing fail-closed steps.
  - Execution state: pending

### Task group 2: make the rules reachable

- [ ] E-02 MAKE THE RELEASE-GATE FAMILY REACHABLE OUTSIDE THE FULL SWEEP, per OQ-01. Today the family (`check.live-bug-ungated`, `check.from-backlog-gate-mismatch`, `check.blocking-item-closed-without-gate`, `check.blocks-release-dangling`, `check.from-backlog-dangling`) is wired only into the once-per-sweep seam in `check_types`, so no per-type invocation can report it.
  DO NOT FORK THE PREDICATES. `AGENTS.md` records that ONE shared predicate (`check_engine.evaluate_blocking_close`) backs the setter, the `aw check` rules and the opt-in hook precisely "so they cannot diverge". Whatever makes the family reachable must call the same rules, not a copy.
  THE RULES ARE CROSS-TREE BY NATURE, WHICH IS WHY THEY LIVE IN THE SWEEP. A gate rule joins a backlog item to a release record and sometimes to a plan, so "check only the backlog tree" cannot answer it in general. Decide deliberately whether the reachable form is a per-type invocation that still reads the other trees, or a distinct named target, and record which.
  DO NOT RE-LITIGATE THE REJECTED SHAPE SILENTLY (F-6). The in-code comment E-01 quotes already rejected "teach `check backlog` to report this rule", on the stated ground that it surfaces ONE rule while four siblings stay invisible. So a per-type form is admissible ONLY if it makes the WHOLE family reachable together; a form that exposes `check.live-bug-ungated` alone is the shape already refused, and choosing it would contradict a recorded decision without argument. OQ-01's recommended DISTINCT TARGET is consistent with that comment ("Consumers must use `aw check` / `aw check all`" names commands, not a prohibition on a new one) and is the safer default.
  KEEP THE PRE-COMMIT AGGREGATOR'S SCOPING INTACT. `check_release_gate_consistency` is composed by `check_commit_invariants`, whose rules are commit- or receipt-scoped so a local hook cannot refuse a commit over another party's artifact. Whatever E-02 adds must NOT make a whole-tree rule reachable from that aggregator, or it re-creates the shared-checkout failure the comment documents. State explicitly that you checked this.
  DO NOT CHANGE WHAT THE RULES DECIDE. This plan changes REACHABILITY and ENFORCEMENT only. If you find a rule itself is wrong, report it rather than fixing it here; `4le6yz` and `0cqf33` already record specific gate-rule defects and are owned elsewhere.
  - Depends on: E-01
  - Expected outcome: the release-gate family is reportable from an invocation narrower than the full sweep, calling the SAME shared predicates, with the WHOLE family reachable together rather than one rule of it; the rules' verdicts are unchanged; `check_commit_invariants` still composes only commit-scoped rules; the chosen form is recorded with its reason and with an explicit statement of how it relates to the recorded decision it revisits.
  - Execution state: pending

### Task group 3: enforce it in CI

- [ ] E-03 ADD A NAMED CI STEP FOR THE GATE FAMILY, FAIL-CLOSED **ONLY IF THE TREE IS ALREADY CLEAN FOR THAT FAMILY**, AND LEAVE THE ADVISORY STEP ALONE.
  THE PRECONDITION IS THE WHOLE OF THIS ITEM, and it is what review added (F-7/F-8). A fail-closed step may land ONLY when E-01 shows the family reports ZERO findings on the tree, or reports only findings this plan has legitimately cleared. At review the family reported 2, exit 1, and NEITHER was this plan's to fix. Landing a fail-closed step in that state reds `main` on arrival, which is precisely the `4y7nzh` failure this plan's own reasoning forbids -- and worse than the `|| true` case, because here the plan would be the direct cause.
  IF THE FAMILY IS NOT CLEAN, DO NOT LAND FAIL-CLOSED. Take the ADVISORY form instead: the same named step running the same narrow invocation with the `|| true`-plus-`::warning::` shape the existing backlog step uses, carrying a comment that names the exact blocking findings and the precondition for flipping it. That is strictly better than today (the family becomes VISIBLE in CI, which it currently is not at all, since nothing runs the sweep), it is honest, and it follows the precedent the maintainer already set for exactly this situation. THEN SAY SO PLAINLY in the item's report and in V-03: an advisory step does NOT satisfy `wu8qjy`'s "CI fails on it" clause, so record which half shipped and file the flip as the remaining obligation rather than claiming the criterion met.
  DO NOT MAKE THE TREE CLEAN BY SUPPRESSION. Do not narrow the step's rule set to exclude whichever rule is currently firing, do not add an ignore list, and do not gate `7l1ggb` or edit the `executed/` plan to silence a finding. Each of those converts a visible obligation into a hidden one, which is the opposite of this plan's purpose.
  THE NEW STEP MUST BE NARROW regardless of which form it takes. It enforces the release-gate family only. A step enforcing everything `check all` reports would fail on every unrelated finding and be reverted within a day.
  DO NOT TOUCH THE EXISTING ADVISORY `check backlog` STEP. Its `|| true` is a recorded maintainer decision (`DECISION 18-r2ks4k-D1`) with a stated precondition. Removing it is out of scope and E-01 re-proves why.
  FOLLOW THE EXISTING PRECEDENT in the same job: `check plans` and `check releases` run fail-closed as named steps and `check backlog` runs advisory, so BOTH shapes are established there and should be matched rather than invented.
  THE STEP MUST BE PROVEN TO BEHAVE AS CLAIMED, not merely added. Run the exact command locally and paste it: nonzero on a violating fixture, zero on a clean one. A CI step nobody has seen fail is an assumption.
  - Depends on: E-02
  - Expected outcome: a named, narrow CI step enforcing the release-gate family, fail-closed if and only if E-01 showed the family clean, otherwise advisory with the blocking findings and flip precondition named in a comment; the advisory backlog step unchanged; no rule suppressed, no ignore list, no live record edited to silence a finding; the command demonstrated nonzero on a violating fixture and zero on a clean one; and an explicit statement of whether `wu8qjy`'s "CI fails on it" clause is met or still outstanding.
  - Execution state: pending

- [ ] E-04 TEST THE FAMILY'S REACHABILITY AND ITS POLARITY, from fixtures.
  ASSERT BOTH DIRECTIONS: a fixture with a live `bug` carrying no `Blocks-Release` must produce `check.live-bug-ungated` from the new reachable invocation, and a clean fixture must produce nothing. A one-sided test would pass against a rule that fires always.
  DO NOT PIN THE LIVE CORPUS. `7l1ggb` is the live violation today and this plan's own siblings may gate or close it, so an assertion keyed on it would rot; `jb0sc1`, `caf5ed` and `agrlvw` all record that hazard in this repository.
  COVER THE `done`-WITH-GATE CASE TOO if it is cheap, since `check.blocking-item-closed-without-gate` shares the family's seam and `AGENTS.md` documents its three legitimate fixes (handoff, evidence, de-gate); a test that only covers the ungated-bug rule leaves the rest of the family unproven as reachable.
  ASSERT THE WHOLE FAMILY IS REACHABLE, NOT ONE MEMBER, since that is the property F-6's recorded decision cares about: a form exposing one rule while its siblings stay invisible is the shape already rejected. One assertion per family member that the new invocation CAN report it is enough; it need not fire in every test.
  ASSERT THE HANDOFF EXEMPTION, because it is the difference between a rule people keep and one they route around: a live `bug` with no gate of its own but WITH a `From-Backlog` carrier holding the gate must NOT be flagged. `check_live_bug_gate` implements this deliberately ("a correctly-handed-off bug would be flagged forever and the rule would train people to ignore it"), so an untested exemption is the likeliest regression.
  DO NOT ASSERT THE FAMILY IS CLEAN ON THE LIVE TREE. That is a live-artifact count which drifts, and at review it was NOT clean; such an assertion would both rot and fail.
  - Depends on: E-02
  - Expected outcome: tests proving every family member is reachable from the new invocation, that it fires on genuine violations and not on clean or legitimately-handed-off ones, using synthetic fixtures with no reference to live records and no assertion about the live tree's cleanliness.
  - Execution state: pending

## Project conventions discovered (Step 0)

- ONE SHARED PREDICATE BACKS THE SETTER, THE CHECK RULES AND THE HOOK (`check_engine.evaluate_blocking_close`), explicitly "so they cannot diverge" (`AGENTS.md`). Any reachability change must call it rather than copy it.
- THE FULL-SWEEP PLACEMENT OF THE GATE FAMILY IS A WRITTEN DECISION, NOT AN ACCIDENT, and it names the per-type wiring as REJECTED (the comment beside `check_live_bug_gate` in `check_types`). Read it before proposing to change reachability; the reason it gives (one rule visible, four siblings invisible) constrains what an acceptable per-type form could be.
- THE PRE-COMMIT AGGREGATOR IS COMMIT-SCOPED ON PURPOSE. `check_commit_invariants` composes only commit/receipt-scoped rules so a local hook cannot refuse a commit over another party's artifact elsewhere in a shared checkout. A whole-tree rule must never become reachable from it.
- A RULE'S SEVERITY DECIDES WHETHER IT REDS A GATE: `artifact_core.drift_exit_code` exempts exactly `info`. `check.collisions-not-checked` is `info`, which is why a per-type run can report one finding and still exit 0; every gate-family member is `error`.
- THE ADVISORY BACKLOG STEP IS A DELIBERATE DECISION with a stated precondition (`DECISION 18-r2ks4k-D1`): it becomes fail-closed only once the naming baseline is cleaned. The debt is still measurable, so the decision still holds.
- CI HAS BOTH SHAPES IN THE SAME JOB: fail-closed named steps (`check plans`, `check releases`) and one advisory step (`check backlog`, `|| true` plus a `::warning::`). E-03 may need either, so both are precedents to match rather than invent.
- A KNOWN-FALSE-POSITIVE RULE MUST NOT BE MADE FAIL-CLOSED. `4le6yz` and `0cqf33` record that `check.from-backlog-gate-mismatch` compares gate spellings literally, so `next` versus its resolved id6 reads as a broken handoff. That is a live `bug` with a release gate, and it is inside the family E-03 would enforce.
- A GATE THAT STAYS RED STOPS BEING READ: `4y7nzh` measured a correct fail-closed gate red for two days while 143 commits landed past it. That is the argument for a NARROW new step rather than enforcing everything.
- `aw check` EXITS 0 CLEAN / 1 FINDINGS / 2 CANNOT-RUN, so a CI step's fail-closed behavior follows from not swallowing exit 1.
- Cite code by SYMBOL (`module.function`) or by a quoted content string, with a line number only appended to one of those and never alone: an offset expires before this plan executes (spec `ipd-structure-and-linting` Section 10.2; advisory `IPD-C801`).

## Findings

| Id | Severity | Location | Finding | Evidence |
| --- | --- | --- | --- | --- |
| F-1 | BLOCKER | `.github/workflows/` | No workflow runs `aw check all`, so the sweep hosting the release-gate family never executes in CI. CONFIRMED in review. | `grep -c "check all"` returns 0 in `tests.yml`, `local-leaks.yml`, `secret-scan.yml` |
| F-2 | BLOCKER | `check_engine.check_types` seam | The gate family is unreachable from a per-type check. CONFIRMED in review, with corrected counts: `check_types(['backlog'])` reports 5 findings and ZERO family members; `check_types(['all'])` reports 26 including `check.live-bug-ungated` and `check.from-backlog-dangling`. (Authoring said 4 versus 49; the counts drift, the mechanism does not.) | both invocations driven directly in-process |
| F-3 | HIGH (re-scoped in review) | live tree | A real `check.live-bug-ungated` violation exists and no per-type invocation can see it: `7l1ggb`. BUT it is NOT a usable regression target (F-7): its own history records a deliberate maintainer decision that it is not a gate, and this plan's deferral forbids gating it. So it demonstrates the blind spot and must not be used as the thing CI is made to fail on. | `check_live_bug_gate` -> 1 finding, severity `error`; the item's history line "Not a release gate: ... not a shipping defect" |
| F-4 | HIGH (constraint) | `tests.yml` advisory step | The one-line fix is WRONG: removing `|| true` would red `main` on pre-existing naming debt still present, which `DECISION 18-r2ks4k-D1` defers to a separate migration. CONFIRMED in review (5 findings today). | the step's `|| true` and comment; `check backlog` reporting `check.name-nonconformant` + the `info` collisions notice |
| F-5 | MED | rule ownership | Specific gate-rule DEFECTS are owned elsewhere (`4le6yz`, `0cqf33`), so this plan changes reachability and enforcement only, not verdicts. NOTE the sharpened consequence: both are `open`, `Work-Kind: bug`, `Blocks-Release: next` FALSE-POSITIVE defects in `check.from-backlog-gate-mismatch` (it compares `next` against a resolved release id6 by raw string equality). Making a rule with known false positives fail-closed can red `main` on a correctly-preserved gate, so E-03's advisory fallback matters for this reason too. | both items' front matter and `4le6yz`'s measured two-finding reproduction |
| F-6 | BLOCKER (found in review) | `check_engine.check_types`, the comment beside `check_live_bug_gate` | THE CURRENT PLACEMENT IS A RECORDED DECISION, NOT AN OVERSIGHT, and the plan never acknowledged it. The comment states the per-type invisibility is "a known, stated cost, not an oversight" and explicitly REJECTS E-02's remedy: wiring the rule into the backlog content path "was rejected because it would surface this ONE rule while its four siblings stayed invisible on the same command". It also records why the rule is not folded into `check_release_gate_consistency` (that feeds `check_commit_invariants`, the pre-commit aggregator, where a whole-tree rule "would refuse a commit because some OTHER party's bug item elsewhere in the tree is ungated"). E-02 must therefore argue against a decision, and any per-type form must expose the WHOLE family. | the quoted comment block, points (1) and (2) |
| F-7 | BLOCKER (found in review) | E-03 as authored; `7l1ggb` | THE PROPOSED FAIL-CLOSED STEP IS RED ON ARRIVAL, AND ITS NAMED TARGET IS AN ILLEGITIMATE ONE. The family reports 2 findings with `drift_exit_code` 1. The `check.live-bug-ungated` member fires on `7l1ggb`, whose own record says the maintainer decided it is not a gate, and the plan's own deferral forbids gating it. So E-03 would red `main` on a finding this plan may not clear, reproducing the `4y7nzh` failure it cites to avoid. | family driven directly: 2 findings, exit 1; `7l1ggb` history line; the plan's own "GATING `7l1ggb` ITSELF" deferral |
| F-8 | BLOCKER (found in review) | E-03 as authored; an `executed/` plan | THE SECOND BLOCKING FINDING IS UNFIXABLE BY CONSTRUCTION. `check.from-backlog-dangling` fires on `executed/...mjx7ne...ipd.md`, whose `- From-Backlog:` value is the literal sentinel `none`. `AGENTS.md` forbids adding commits to a plan in `executed/`, and executed plan `rgaasb` already recorded this exact finding as "pre-existing and is NOT this plan's to fix". So no legitimate act available to this plan makes the family clean. | `releases.check_from_backlog` detail: "From-Backlog 'none' does not resolve to a backlog item"; `rgaasb`'s deferral text |
| F-9 | MEDIUM (found in review) | `tests.yml`, existing fail-closed steps | THE JOB E-03 WOULD JOIN IS ALREADY RED, which the plan's premise omits. `aw check plans --agent` exits 1 with 12 findings (11 `check.ipd-uncarried-obligation`), and THIS PLAN is one of them, so the plan would also block its own finalize until its deferral rows carry a durable carrier. `check releases` and `check specs` exit 0. | the four per-type invocations' pasted exit codes; `evaluate_durable_carrier` on this plan -> 6 uncarried obligations |

## Proposed changes (ordered, validatable)

1. E-01 re-measures the CI absence, the per-type unreachability, the still-live naming debt, the RECORDED DECISION that rejected E-02's shape, the family's own live finding set with each member classified fixable-or-not, and whether the job E-03 would join is already red.
2. E-02 makes the WHOLE release-gate family reachable outside the full sweep, calling the shared predicates unchanged, without widening what the pre-commit aggregator composes, and recording how the choice relates to the decision it revisits.
3. E-03 adds a narrow named CI step, fail-closed only if E-01 showed the family clean and otherwise advisory with the blocking findings and flip precondition named, suppressing nothing, and leaves the advisory backlog step untouched.
4. E-04 proves every member's reachability, the rule's polarity, and the handoff exemption from synthetic fixtures, asserting nothing about the live tree.

## Deferred / out of scope (with reason)

- FLIPPING `check backlog` TO FAIL-CLOSED. `DECISION 18-r2ks4k-D1` conditions it on a separate baseline-cleaning migration, and F-4 shows the debt is still live, so flipping it now would red `main` on unrelated findings.
  - Carrier: wu8qjy
- CLEANING THE BACKLOG NAMING DEBT. That is the deferred migration's job; doing it here would bundle an unrelated records sweep into a CI fix.
  - Carrier: wu8qjy
- ADDING `aw check all` TO CI WHOLESALE. It reports 26 findings on this tree (49 at authoring); enforcing all of them in one step is the red-gate failure `4y7nzh` measured.
  - Carrier-Declined: a REJECTED alternative, not an outstanding obligation; nothing remains for a future artifact to carry.
- FIXING INDIVIDUAL GATE RULES. `4le6yz` and `0cqf33` own specific verdict defects; E-02 explicitly changes no verdict. Per F-5 both are live `bug` items with release gates, and both are FALSE POSITIVES in `check.from-backlog-gate-mismatch`, which is an additional reason E-03 must be able to fall back to advisory.
  - Carrier: 4le6yz
- GATING `7l1ggb` ITSELF. It is the live violation this plan makes visible, and deciding its gate is a maintainer judgement about that bug, not a side effect of wiring a check. REINFORCED in review: its record already carries an explicit maintainer decision that it is NOT a gate, so gating it here would overturn a recorded decision. This is also why it cannot be E-03's regression target (F-7).
  - Carrier: 7l1ggb
- CLEARING THE `From-Backlog: none` FINDING IN THE `executed/` PLAN (F-8). `AGENTS.md` forbids adding commits to a plan in `executed/`, and executed plan `rgaasb` already recorded it as not-to-be-fixed. Whether the rule should treat the literal sentinel `none` as ABSENT rather than dangling is a genuine rule-verdict question, which this plan explicitly excludes; it needs its own backlog item or plan, because until it is resolved the family cannot reach zero and E-03 cannot flip to fail-closed.
  - Carrier: wu8qjy
  - Carrier-note: `wu8qjy` is named because it is the live source item for "the bug gate is unenforced in CI" and this finding is exactly what BLOCKS that enforcement, so it is revisited with the item rather than vanishing. It is a HOLDING carrier, not the right long-term owner: no existing item covers the `none`-sentinel verdict question (`4le6yz` and `0cqf33` both concern `from-backlog-gate-mismatch` spelling, not `from-backlog-dangling`). E-01 must file a dedicated item if it confirms the finding still blocks the flip, and V-01 requires that id6.

## Scope check

- Over-scope: `check_engine.py` is in scope ONLY for reachability of the existing family. Do not alter any rule's verdict, severity, or the shared `evaluate_blocking_close` predicate's logic. In particular do NOT "fix" the `From-Backlog: none` sentinel verdict here (F-8) even though it is what blocks the fail-closed flip: that is a verdict change, and this plan's whole discipline is that verdicts are owned elsewhere.
- Under-scope: OQ-01 resolved to a DISTINCT NAMED TARGET, so `agent_workflows/cli.py` is very likely needed and is currently UNDECLARED. Add it to `- Scope-Paths:` deliberately before editing, with the reason recorded, rather than discovering it at finalize. Also do not add the new target to `check_engine.SUPPORTED`, which enumerates record TYPES (eight today); a release-gate target is not a record type.
- Under-scope: F-9 must be discharged before this plan can finalize. `aw check plans` reports `check.ipd-uncarried-obligation` (`error`) against this plan, and `aw ipd lint --phase pre-transition` merges the same finding, so every `## Deferred` row and every open question needs a `- Carrier:` or `- Carrier-Declined:`. The rows above now carry one; any row added later must too.
- Deliberately NOT widened: do not make the family clean by suppression (no ignore list, no narrowed rule set, no editing a live record or an `executed/` plan to silence a finding). E-03's advisory fallback exists precisely so the executor never needs to.

## Required tests / validation

- `python3 -m pytest` bare, per the execution contract, with the actual summary line pasted.
- Targeted: the new release-gate check test module.
- E-03's exact CI command MUST be demonstrated returning NONZERO on a fixture carrying a live ungated bug and ZERO on a clean fixture; a step never seen to fail is an assumption, which is exactly what `wu8qjy` reports about the current state.
- E-04 MUST use synthetic fixtures, never the live corpus (`jb0sc1`, `caf5ed`, `agrlvw`), and MUST NOT assert the live tree is family-clean (it is not; F-7/F-8).
- Confirm `aw check plans`, `aw check releases`, `aw check backlog` and `aw check specs` behave as before, since E-02 touches the shared engine they run through. Paste their exit codes BEFORE and AFTER: `check plans` exits 1 today for an unrelated reason (F-9), so "still exits 1" is the correct expectation there and a naive "still green" check would mislead.
- Confirm `check_commit_invariants` still composes exactly its three commit-scoped rules, so E-02 did not make a whole-tree rule reachable from the pre-commit aggregator (F-6). A test asserting that composition is cheap and pins the property directly.
- Confirm the family's rule set reachable from the new target EQUALS the set the full sweep reports for it, so the two invocations cannot silently diverge.

## Spec / documentation sync

- `AGENTS.md`'s release-gate section states the `aw check` rule plus CI is "the portable authority", which is currently only half true. Amend it to match WHAT ACTUALLY SHIPS: if E-03 lands fail-closed, name the enforcing step; if it lands ADVISORY (the likely case per F-7/F-8), say so plainly and name the precondition for the flip. Do NOT write the fail-closed sentence for an advisory step, which would be the same overclaim the current text makes.
- THE IN-CODE COMMENT IN `check_types` BECOMES STALE IF E-02 LANDS. It currently tells a reader the family is full-sweep-only and that "Consumers must use `aw check` / `aw check all`". If a narrower invocation exists, that text must be corrected in the same change, or the next reader is misled exactly as this plan's author was (F-6).
- `check_live_bug_gate`'s docstring point (2) likewise states the rule "IS THEREFORE NOT REPORTED BY `aw check backlog`" and that wiring it elsewhere was rejected. Update it to describe the new reachability and to preserve the REASON the rejection was recorded, so the constraint survives even though the conclusion changed.
- If the gate family's reachability or the CI gate set is specified in a spec governing `aw check`, declare that `.spec.md` in `- Scope-Paths:` before amending it, per the spec-amendment rule. No spec edit is anticipated otherwise.

## Open questions

### OQ-01: Per-type reachability, or a distinct named check target? (RESOLVED at review: distinct named target)

- Blocking: no
- Status: resolved
- Owner: this plan's executor, escalating to the maintainer if it changes a documented CLI surface
- Carrier: wu8qjy
- Resolution or deferral rationale: RESOLVED AT REVIEW (2026-09-24) IN FAVOUR OF THE DISTINCT NAMED TARGET, on repository evidence rather than preference, so the executor is not left to re-derive it. NOT blocking either way, because E-02 must deliver one and either makes the family reachable.
  THE DECIDING EVIDENCE IS THE COMMENT F-6 QUOTES. `check_types` already REJECTED the per-type shape in writing: wiring the rule into the backlog content path "was rejected because it would surface this ONE rule while its four siblings stayed invisible on the same command, which is a more confusing contract than 'the family lives on the full sweep'". A per-type form is therefore admissible ONLY if it exposes the whole family from a backlog-scoped command, which means a backlog check that reads releases and plans -- muddying exactly the type boundary the per-type commands exist to draw. The same comment's closing line ("Consumers must use `aw check` / `aw check all`") names the commands that exist; it does not forbid adding one.
  TWO FURTHER MEASURED REASONS. First, F-4: the natural CI home for a per-type form is the `check backlog` step, which must stay advisory, so per-type would either need a second invocation of the same type (confusing) or a flip that is forbidden. Second, F-7/F-8: the family currently reports findings the plan cannot fix, so E-03 needs a step whose rule set is narrow BY CONSTRUCTION and whose advisory-versus-fail-closed state can be reasoned about independently of the backlog type's debt. A distinct target gives both.
  THE ACCEPTED COST, recorded because it is real: one more documented surface, and `check_engine.SUPPORTED` currently holds eight record TYPES, so a release-gate target is not a record type and must not be added to that mapping as if it were. E-02 must state where it lives and why that placement does not imply a ninth record type.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: pasted `check all` grep counts for every workflow file; pasted finding counts and rule sets for `check backlog` versus `check all`, re-derived not copied; the RECORDED DECISION quoted verbatim from `check_types` (both points: the rejected per-type wiring, and the pre-commit-aggregator scoping); the family's own finding set pasted with each member's location and severity plus `drift_exit_code`; each member CLASSIFIED fixable-or-not-by-this-plan with the reason; an explicit statement of whether E-03 may land fail-closed today; the id6 of any item filed for the `none`-sentinel verdict question if it still blocks; and the current exit codes of `check plans`/`check releases`.
  - Observed evidence:
  - Result: pending

- [ ] V-02 validates E-02
  - Required evidence: pasted output of the new reachable invocation reporting a gate-family finding on a SYNTHETIC fixture; evidence by inspection that it calls the shared predicates rather than a copy; proof that the WHOLE family is reachable from it (each member named), not one rule; proof that `check_commit_invariants` still composes exactly its three commit-scoped rules; the recorded choice of form with its reason and its relation to the decision it revisits; and, if a new target was added, a statement of where it lives and why it is not a ninth entry in `SUPPORTED`.
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: the CI step's definition quoted; a pasted local run of that EXACT command returning nonzero on a violating fixture and zero on a clean one; a diff or quote proving the advisory `check backlog` step is unchanged; an explicit statement of WHICH form shipped (fail-closed or advisory) with the E-01 measurement that decided it; if advisory, the comment naming the blocking findings and the flip precondition, plus a plain statement that `wu8qjy`'s "CI fails on it" clause is NOT yet met and what carries the remainder; and confirmation that nothing was suppressed (no ignore list, no narrowed rule set, no live record or `executed/` plan edited).
  - Observed evidence:
  - Result: pending

- [ ] V-04 validates E-04
  - Required evidence: tests pasted showing the family fires on a violating fixture, NOT on a clean one, and NOT on a live bug whose `From-Backlog` carrier holds the gate (the handoff exemption); an assertion per family member that it is reachable from the new invocation; confirmation by inspection that no assertion reads live records or asserts the live tree is family-clean; `check plans`/`check releases`/`check backlog`/`check specs` exit codes pasted before and after (noting `check plans` exits 1 for the unrelated F-9 reason); and the bare `python3 -m pytest` summary line.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

This plan is `to-review` and requires explicit human approval before execution. The executor commits only the paths named in `- Scope-Paths:` via `aw commit <plan> -- <paths>`, never `git add -A`, and never pushes. Test claims must paste actual runner output. On completion, `aw ipd lint --phase pre-transition` must conform and every `V-*` must carry observed evidence before the plan moves to `.aw/records/plans/executed/`.
