# IPD: Resolve every pre-existing cross-type setid collision before hard enforcement can be enabled

- Date: 2026-09-08
- Kind: child
- Concern: THE SWEEP'S SIZE DEPENDS ON A FLAG, AND THE TWO SHIPPED SURFACES DISAGREE BY MORE THAN A FACTOR OF TWO. Re-measured at review by calling `check_engine.check_collisions` both ways: `include_retired=False` yields 40 `check.setid-collision` findings across 30 setids, and `include_retired=True` yields 86 across 65. End to end, `aw check all` reports 40 and `aw doctor` reports 81, because `check_engine.py:1760` passes the caller's flag through (default False at `:835`) while `doctor.py:530` hardcodes True. WHICH NUMBER THIS CHILD MUST DRIVE TO ZERO IS THEREFORE A DECISION, NOT A MEASUREMENT, and it is raised as OQ-04 below rather than assumed. Authoring quoted 29 across 21; that figure is stale by one day and is retained only to show the population GROWS.
  THE POPULATION IS NOT UNIFORM, AND THE PLAN'S "ALL BENIGN" PREMISE IS FALSE UNDER THE RETIRED-INCLUSIVE VIEW. Measured: under `include_retired=False` all 40 are cross-type `backlog` versus `plans` and all 40 sit on the BACKLOG side, which is the benign graduation shape. Under `include_retired=True` the 86 decompose into 78 `backlog`/`plans`, 2 `plans`/`research`, 1 `plans`/`walkthroughs`, and FIVE that are not cross-type at all but same-type descriptive mismatches between two `executed` plans (`release-review` twice, `leak-sanitizer`, `assess-documentation`, `assess-bugs`). The five same-type findings CANNOT be cleared by regrouping a source, because both sides are immutable executed plans and there is no source. The `plans`/`walkthroughs` and `plans`/`research` cases have no `From-Backlog` edge and no backlog item to regroup either. So "every collision is a benign source-versus-its-own-plan pair" holds for the 40 and is WRONG for the 86.
  ONE FLAGGED ITEM HAS NO GRADUATION LINK, WHICH IS EXACTLY THE CASE OQ-03 SAYS TO REPORT RATHER THAN SWEEP. Measured across all plans and specs carrying `- From-Backlog:`, 39 of the 40 flagged backlog items are the target of a resolvable back-link; the exception is `.aw/records/backlog/blocked/20260908-idxuntrack-01-adgtqb-finalize-leaves-tracked-index-dirty.backlog.md` (setid `idxuntrack`, id6 `adgtqb`), whose colliding partner `.aw/records/plans/pending/20260906-idxuntrack-02-yvvf98-...ipd.md` carries no `From-Backlog` pointing at it. So the guard the plan wrote as a hypothetical fires on the real tree, in one case, on the first run.
  A BACKLOG ITEM CARRIES NO `- Order:` FIELD AT ALL, so the plan's central procedural mandate is aimed at a field that does not exist. Measured: `grep -rln "^- Order:" .aw/records/backlog/` matches 0 of 174 items. Reproduced in a throwaway repo: `aw group backlog <id6> --set <new> --rename --apply` on a realistic item with no Order field renames the file, rewrites `- Set:`, and injects NO Order; the filename `NN` slot is preserved from the old name. Worse, FOLLOWING this plan's mandate is what causes harm: passing `--order 1` on an item whose front matter has no Order silently renumbers the filename slot (`...-realfresh-01-...` stayed `01` only because it already was), and on an item at slot `02` it MOVED the slot to `01`. The Order hazard `s9p5x5` documents is real, but it is a `aw group plans` defect (`plans_refs.py:432`/`:437` substituting zero), and this child does not touch plans.
  THE `agentadhere` CASE IS INVISIBLE TO THE SURFACE THE PLAN VALIDATES AGAINST. Measured: with `include_retired=False` there are ZERO `agentadhere` findings; with True there is exactly one, on the closed item `3gr7fk`. So if OQ-04 resolves to the `aw check all` number, E-03's finding does not exist to clear and V-03's "paste the finding gone" is unfalsifiable, since it was never there.
  THE SWEEP IS A SPEC REQUIREMENT AND ITS POSITION IS FIXED. Spec `4w7d6s` Section 5: "A one-time sweep MUST identify and resolve any other existing cross-type setid collisions before I2 is turned on fail-closed, so enabling the hard rule does not mass-fail the tree. (Mirror the grandfathering discipline used for Scope-Paths / dependency cutover: find violators, fix, then enforce.)"
  BUT SWEEPING FIRST WOULD BE WASTED WORK, WHICH IS WHY THIS ITEM DEPENDS ON ORDER 03. Every one of the 29 collisions was CREATED by a graduation reusing its source's setid, so until Order 03 mints a fresh child setid, the next graduation recreates one. This was demonstrated at authoring time rather than reasoned about: scaffolding this very Set into `Set: setiduniq` immediately collided with backlog item `sjsoqq`, and the four plans were regrouped to `setidhard` under the spec's own G1 rule before any prose was written. A sweep run before Order 03 would have to be run again.
  THE NAMED MIGRATION CASE IS STILL REAL AND IS OLDER THAN THE OTHERS. The closed backlog item `.aw/records/backlog/done/20260823-agentadhere-01-3gr7fk-...backlog.md` still carries `- Set: agentadhere`, shared with SEVEN executed plans (`3b4f8u`, `gfokao`, `uisjns`, `8dto0g`, `wqj1ne`, `diundn`, `r2ks4k`), measured by `grep -rln "^- Set: agentadhere" .aw/records/` returning 8 files: 7 plans plus the item itself. The plan said eight plans; that is off by one and the item is the cross-type half that makes it a collision at all. This is the collision that originally motivated the whole item, via a confusing cross-type "type mismatch" from `aw ipd set approved agentadhere`. Note the setter half of that symptom is ALREADY FIXED (`9107790`), so what remains here is the DATA.
  THE DANGEROUS WAY TO DO THIS IS TO RENAME THE PLAN SETS, and it looks equally valid from the finding text. A plan Set's setid appears in filenames, in `Item-Dependencies` edges, in review-record filenames, in run records, and in prose across the tree. Renaming the SOURCE touches one file per collision, and once Order 02 exists the `Graduated-To` forward link records the relationship the shared name used to carry. The spec points the same way for `agentadhere`: "re-group the closed backlog item to its own unique setid".
  THE ARGUMENT IS RIGHT AND ITS THREE NAMED EXAMPLES ARE ALL WRONG, so do not reuse them as evidence. Measured at review: `scopeattr`/`h9cn0y` is `executed`, `depreview`/`03ie04` is `executed`, and `integearn`/`32ij2j` is `superseded`. NONE is `approved` or executing. The far stronger and correct form of the argument needs no live plan at all: all 40 findings under the `aw check` view sit on the BACKLOG side (measured, 40/40), so renaming the source touches no plan filename whatsoever, and the plan side is not merely risky to rename but UNNECESSARY. Live Sets do exist under other setids (`durablecapture` has three `to-review` children, `runverdict` four `reviewed`, `integearn` three `reviewed`), which is a reason for the executor to re-measure status rather than trust any list, including this one.
- Scope: Resolve every pre-existing cross-type setid collision that HAS a source to regroup, by regrouping the SOURCE artifact (the backlog item or spec) and never a plan Set, and record the lineage via the `Graduated-To` link Order 02 provides. Data only: this child changes NO code and adds NO check. It drives the AGREED target measure (OQ-04) to zero and REPORTS, without renaming, every collision that has no source or no graduation link, so the hard posture Order 00 verifies becomes reachable for the sweepable population and the residue is a named, human-owned list rather than a silent gap.
- Scope-Paths: .aw/records/backlog
- Item-Dependencies: executed:dw7i3m
- Status: reviewed
- Readiness: no-go
- Set: setidhard
- Order: 1
- Highest E allocated: 05
- Author: opencode its_direct/pt3-claude-opus-5-1m-us
- Id: drzbs9
- From-Backlog: sjsoqq

## Workflow history

- 2026-09-09 reviewed (opencode its_direct/pt3-claude-opus-5-1m-us): APPROVE WITH REVISIONS APPLIED; PR-501..PR-512 all FIXED; readiness `no-go`. SELF-REVIEW disclosure: the same agent/model authored this plan, so its value rests on EXECUTING its claims rather than re-reading them. Nine things were run: `check_collisions` was called on BOTH retired branches and the populations decomposed by shape; the `From-Backlog` edge set was gathered across all plans and specs and matched to every flagged item, which found the one no-link case; `aw check all --agent` and `aw doctor` were both run and their per-rule counts compared; `aw group backlog` was reproduced FOUR ways in two throwaway repos (bare, with `--order`, single-match, multi-match) on both a synthetic item with an Order field and a realistic one without; `grep` proved 0 of 174 backlog items carry `- Order:`; the `agentadhere` carrier count was measured; the named live plans' statuses were read; the full bare suite was run; and the plan's named expected-failure file was run in isolation. SEVEN of those nine contradicted the plan. THE TWO THAT MATTER MOST: the plan's `--order` mandate is aimed at a field backlog items do not have and FOLLOWING it moves the filename slot, so it was inverted; and the "resolve EVERY collision" goal is unachievable because eight findings have no source to regroup, five of them between immutable executed plans, which is now an open question rather than a silent failure. Two new `Blocking: yes` questions were added (OQ-04 the target measure, OQ-05 the sourceless residue) and `.aw/records/specs` and `.aw/records/research` were REMOVED from `Scope-Paths` because a declared-but-unmodified path costs a mandatory `--scope-ack` at finalize.
- 2026-09-08 to-review (opencode its_direct/pt3-claude-opus-5-1m-us): Graduated from backlog `sjsoqq` deliverable #6, RE-SEQUENCED and RE-TARGETED. The item's own claim that "`aw check all` now reports 0 setid-collision findings" is STALE: measured 29 findings across 21 setids at HEAD. Its accompanying claim that the `agentadhere` collision "PHYSICALLY persists" is TRUE and re-verified (`3gr7fk` still carries `- Set: agentadhere`, shared with eight executed plans). TWO AUTHORING DECISIONS that the item left open. FIRST, this child DEPENDS ON Order 03 (`executed:dw7i3m`) even though it has a lower Order digit, because every one of the 29 collisions was created by graduation reusing a source setid, so a sweep before the root-cause fix would be undone by the next graduation; demonstrated at authoring when scaffolding this Set into `Set: setiduniq` instantly collided with `sjsoqq`. SECOND, the sweep renames the SOURCE, not the plan Set, because a plan setid is cited across filenames, dependency edges, review records and run records, and several of the 21 belong to plans that are approved or executing under other agents right now; the spec's own Section 5 prescribes exactly this for `agentadhere`.

## Goal

Drive the AGREED target measure of `check.setid-collision` to zero by fixing every collision that has a source, in the one place that is cheap and safe to change, and hand back a named list of the ones that have no source, so a rule the catalog already calls an error stops firing on the repository's own convention and the residue is visible rather than silent.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: inventory before touching anything

- [ ] E-01 RE-MEASURE THE COLLISION SET BOTH WAYS AND CLASSIFY EVERY MEMBER BEFORE RENAMING ANY FILE. Do not trust this plan's list: the population grew by eleven findings and nine setids in the single day between authoring and review, so it will differ again.
  MEASURE BOTH BRANCHES OF THE RETIRED FLAG AND STATE WHICH ONE IS THE TARGET. Call `check_engine.check_collisions(repo_root, include_retired=False)` AND `(..., include_retired=True)`, filter to `rule == "check.setid-collision"`, and paste both counts. They are NOT the same measure: at review, False gave 40 findings across 30 setids and True gave 86 across 65, because `check_engine.py:1760` passes the caller's flag through (default False at `:835`) while `doctor.py:530` hardcodes True, which is why `aw check all` says 40 and `aw doctor` says 81. The target measure is fixed by OQ-04 and MUST be read from OQ-04's recorded answer, not chosen here.
  FOR EACH COLLIDING SETID RECORD FIVE FACTS, because they decide what may be touched: every artifact carrying it with its type and path; whether a SOURCE side exists at all (a backlog item or spec) and which side is the PLAN; the plan side's current `- Status:` and lifecycle directory; whether a `From-Backlog` edge already joins the two; and the finding's SHAPE from its detail text, `different type: X vs Y` or `descriptive: ... vs ...`.
  PARTITION THE INVENTORY INTO SWEEPABLE AND NOT-SWEEPABLE, since the plan's original premise that every collision is benign is FALSE under the retired-inclusive view. Measured at review: under False all 40 are `backlog` versus `plans` with the flagged side in `backlog/`, which is sweepable. Under True the 86 include 2 `plans`/`research`, 1 `plans`/`walkthroughs`, and FIVE same-type `descriptive` mismatches between two `executed` plans (`release-review` twice, `leak-sanitizer`, `assess-documentation`, `assess-bugs`). A same-type executed-plan pair has NO source to regroup and its bodies are immutable, so it CANNOT be swept by this child by construction.
  EXPECT THE NO-LINK CASE TO FIRE, because it already does. Measured at review, 39 of the 40 flagged items are the target of a resolvable `From-Backlog`; the exception is `.aw/records/backlog/blocked/20260908-idxuntrack-01-adgtqb-...backlog.md` (setid `idxuntrack`), whose partner `.aw/records/plans/pending/20260906-idxuntrack-02-yvvf98-...ipd.md` carries no back-link to it. Per OQ-03 that item is REPORTED and NOT regrouped. Re-measure rather than trusting this one case.
  - Depends on: none
  - Expected outcome: a per-setid inventory with the five facts, BOTH branch counts pasted, the target measure quoted from OQ-04's answer, and an explicit sweepable-versus-not-sweepable partition naming every member of the second group.
  - Execution state: pending

### Task group 2: regroup the sources

- [ ] E-02 REGROUP THE SOURCE ARTIFACT OF EACH SWEEPABLE COLLISION TO A FRESH SETID, AND NEVER THE PLAN SET. This is the item's deliverable #6 and the direction the spec prescribes.
  USE THE TOOLED VERB, NOT A HAND RENAME, AND ONE CALL PER ITEM: `aw group backlog <id6> --set <new> --rename --apply`. Select by id6, never by a filename substring, because the verb's own `--help` warns a substring selector can match several files and `--force` then acts on ALL of them. The verb owns the clustering filename and rewrites references; hand-naming is exactly what the naming authority exists to prevent.
  DO NOT PASS `--order`, WHICH REVERSES THE PRIOR REVISION OF THIS PLAN AND IS THE POINT. A backlog item carries NO `- Order:` field: measured, `grep -rln "^- Order:" .aw/records/backlog/` matches 0 of 174 items, so there is no Order to preserve and nothing for the verb to reset. Reproduced in a throwaway repo on a realistic item with no Order field: a bare `aw group backlog <id6> --set <new> --rename --apply` renames the file, rewrites `- Set:`, injects NO Order, and preserves the filename `NN` slot; whereas passing `--order 1` on an item whose slot was `02` MOVED the slot to `01`, changing the name for no reason. The `s9p5x5` hazard is real but belongs to `aw group plans` (`plans_refs.py:432`/`:437` substituting zero when the flag is absent), and this child touches no plan. Verify the `NN` slot and the absence of an injected Order after each call rather than trusting this paragraph.
  ONE CALL PER ITEM, NEVER A SET-WIDE OR STATUS-WIDE SELECTOR. Reproduced: `run_group_generic` assigns `order_val = start_order + i` across all matched files (`artifact_rename.py:682`), so a multi-match call with `--order` renumbers the whole match sequentially. Single-item calls make that unreachable.
  DO NOT RENAME A PLAN SET AT ALL in this child. The reason is not that live plans are at risk (measured at review, the three the earlier draft named are `executed`, `executed` and `superseded`); it is that renaming a plan is UNNECESSARY, because all 40 findings under the `aw check` view have their flagged side in `backlog/`, so the source side alone clears every one. A plan setid is additionally cited in filenames, `Item-Dependencies` edges, review-record filenames, run records and prose.
  DERIVE A DISTINCT-BUT-RECOGNIZABLE NAME, per the spec's OQ-03 reasoning: only the setid TOKEN must differ, and the human-readable slug may still echo the source, so traceability survives for a human reader as well as for the typed link.
  - Depends on: E-01
  - Expected outcome: every sweepable collision's SOURCE regrouped to a fresh setid via one tooled call per id6 with no `--order`, each regrouped file's filename `NN` slot and Order-absence verified, and no plan Set renamed.
  - Execution state: pending

- [ ] E-03 RESOLVE THE `agentadhere` CASE EXPLICITLY, because it is the collision that motivated the whole item and it differs from the others in two ways that matter.
  ITS SOURCE IS A CLOSED ITEM: `.aw/records/backlog/done/20260823-agentadhere-01-3gr7fk-...backlog.md` carries `- Set: agentadhere`, shared with SEVEN executed plans (`grep -rln "^- Set: agentadhere" .aw/records/` returns 8 files: 7 plans plus this item). Regrouping a `done` item is safe (nothing schedules it) and is what the spec prescribes: "re-group the closed backlog item to its own unique setid ... and (per G3) record the plan Set it graduated into via `Graduated-To`".
  IT IS INVISIBLE TO ONE OF THE TWO MEASURES, WHICH DECIDES WHETHER THIS ITEM HAS ANY WORK IN IT. Measured at review: `include_retired=False` reports ZERO `agentadhere` findings, `include_retired=True` reports exactly one, on `3gr7fk`. Both `agentadhere` plans are in `executed/`, which the False branch filters out. So if OQ-04's answer is the `aw check all` (False) measure, this item's regroup CLEARS NO FINDING on that surface and must be justified as spec compliance rather than as a finding fix; state which case you are in before acting, and do NOT report a finding as "gone" that was never present.
  DO NOT TOUCH THE SEVEN EXECUTED PLANS. They are in `executed/`, their bodies are immutable by policy, and their setid is cited in the historical record. The collision is cleared from the SOURCE side alone.
  STATE WHAT THIS DOES AND DOES NOT FIX. It clears the retired-inclusive finding and unblocks the original `aw ipd set approved agentadhere` symptom's DATA half; the SETTER half was already fixed in `9107790` and must not be rebuilt.
  - Depends on: E-02
  - Expected outcome: `3gr7fk` regrouped to its own setid with a `Graduated-To` recording the `agentadhere` plan Set; the seven executed plans untouched; the finding's presence or absence stated per measure, with no claim that an absent finding was cleared.
  - Execution state: pending

### Task group 3: prove the sweep is complete and cost-free

- [ ] E-04 PROVE THE TARGET MEASURE IS ZERO OVER THE SWEEPABLE POPULATION AND THAT NOTHING ELSE MOVED. The sweep's whole purpose is to make the hard posture reachable, so a partial sweep leaves the same unactionable rule with a smaller number.
  COMPARE PER RULE, NEVER BY TOTAL, AND READ THE PER-RULE COUNTS FROM THE `diagnostics` ARRAY. Measured at review: `aw check all --agent` emits a SINGLE `aw.agent/v1` JSON object whose `diagnostics` array carries one `{location, rule}` per finding, so per-rule counts come from parsing that array, NOT from counting tab-separated lines (there are none) and NOT from `--format json` (which emits no JSON on this surface: it raised `JSONDecodeError` at review). Review baseline, 238 findings total: `check.scope-drift` 174, `check.setid-collision` 40, `check.lifecycle-transition-invalid` 15, `check.name-nonconformant` 3, `check.ipd-dependency-findings-blocked` 2, `check.review-finding-unescalated` 1, `check.review-decision-unescalated` 1, `check.id6-collision` 1, `check.from-backlog-dangling` 1. The plan previously said 98; that was stale. The total will not reach zero.
  THE CRITERION IS THE TARGET MEASURE AT ZERO OVER THE SWEEPABLE SET, NOT AT ZERO ABSOLUTELY, and conflating the two would make this item unpassable. Any collision E-01 partitioned as not-sweepable (a same-type executed-plan pair, a `plans`/`walkthroughs` or `plans`/`research` pair with no backlog source, or the no-link case) REMAINS as a finding by design. State the residual count, enumerate its members, and confirm each is in the not-sweepable partition. A residual member that is NOT in that partition is a FAILED validation.
  WATCH THE RULES A REGROUP CAN MOVE. `check.name-nonconformant` (a regroup rewrites filenames; baseline 3), `check.from-backlog-dangling` (baseline 1; a rename must not break a back-link), and `check.scope-drift` (baseline 174, the largest count and the one most likely to absorb a change unnoticed).
  DO NOT REQUIRE `aw check` AND `aw doctor` TO AGREE NUMERICALLY: they CANNOT, and demanding it makes this item unpassable. Measured at review, `aw check all` reports 40 setid-collision findings and `aw doctor` reports 81, because `doctor.py:530` hardcodes `include_retired=True` while `check_engine.py:1760` passes the caller's flag through with a False default (`:835`). The correct check is that BOTH counts FALL by the number of sources you regrouped, and that each surface's residual set equals the not-sweepable partition under its own flag. Report both numbers side by side and explain the gap by that flag; reporting them as equal is a FAILED validation.
  RUN THE SUITE BARE and judge on the DELTA. Re-measured at review on main: `1 failed, 5919 passed, 3 skipped, 2 xfailed`. The plan's named baseline (`1 failed, 5648 passed`) and its named expected failure were BOTH wrong: `python3 -m pytest tests/test_orchestrator_retirement.py` passes `112 passed`, so that file is NOT the known failure and must not be used to excuse anything. The actual failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, and it is ENVIRONMENTAL: it trips on a gitignored local `opencode-recovery/` dump, so it is machine-specific and may not reproduce for you. MEASURE YOUR OWN BEFORE-BASELINE and compare failing NODE IDS, not counts.
  - Depends on: E-03
  - Expected outcome: target measure at zero over the sweepable set with the residual enumerated and each member confirmed not-sweepable; per-rule counts before and after from the `diagnostics` array with no unexplained rise; both surfaces' counts reported with the flag explaining the gap; bare-suite failing node ids identical to the executor's own before-baseline.
  - Execution state: pending

- [ ] E-05 PROVE NO TRACEABILITY WAS LOST, which is the real risk of this child and the reason the maintainer might reject the whole approach. Before the sweep a reader could see the relationship in the shared NAME; after it, the relationship must be readable from the typed links alone.
  FOR EVERY REGROUPED SOURCE, DEMONSTRATE BOTH DIRECTIONS RESOLVE: the plan's `From-Backlog: <source id6>` resolves to the regrouped item, and the source's `Graduated-To: <plan setid>` resolves to the plan Set (the check Order 02 adds). A regroup that clears a finding while orphaning the relationship has made the tree worse.
  CHECK THE BACK-LINK SURVIVED THE RENAME. `From-Backlog` points by id6, which a regroup does NOT change, so it should survive by construction; verify that rather than assuming it, since `--rename` also rewrites references and could plausibly touch one.
  REPORT THE NO-LINK CASE AS A CONCRETE HANDOFF, NOT A HYPOTHETICAL, BECAUSE IT ALREADY FIRES. Measured at review: `.aw/records/backlog/blocked/20260908-idxuntrack-01-adgtqb-...backlog.md` (setid `idxuntrack`) collides with `.aw/records/plans/pending/20260906-idxuntrack-02-yvvf98-...ipd.md`, which carries no `From-Backlog` pointing at it, so the shared setid is the ONLY record that they are related and regrouping it would destroy that. Leave it UNTOUCHED, name it in the report with both paths, and state the remedy is a human decision (establish the real link, or accept they are unrelated and rename deliberately). Do NOT invent a link. Re-measure: the set may have grown.
  - Depends on: E-04
  - Expected outcome: both link directions demonstrated resolving for every regrouped source; every no-link case named with both paths, left unregrouped, and handed to a human.
  - Execution state: pending

## Project conventions discovered (Step 0)

- THE COUNT DEPENDS ON A FLAG: `check_collisions(root, include_retired=False)` gives 40 findings across 30 setids, `True` gives 86 across 65 (`check_engine.py:832-836`, `:1760`; `doctor.py:530`). `aw check all` reports 40 and `aw doctor` reports 81. Re-measured at review; authoring's 29-across-21 is stale.
- THE POPULATION IS NOT UNIFORM. Under False all 40 are cross-type `backlog`/`plans` with the flagged side in `backlog/`. Under True the 86 add 2 `plans`/`research`, 1 `plans`/`walkthroughs`, and 5 SAME-TYPE `descriptive` mismatches between two `executed` plans. The same-type five have no source and cannot be swept.
- ONE FLAGGED ITEM HAS NO GRADUATION LINK: `idxuntrack`/`adgtqb` (39 of 40 do have one). The guard is a live case, not a hypothetical.
- THE SPEC PRESCRIBES RENAMING THE SOURCE, not the Set: for `agentadhere` it says "re-group the closed backlog item to its own unique setid" (Section 5).
- ALL 40 FINDINGS' FLAGGED SIDE IS IN `backlog/`, so renaming the source clears every one and the plan side never needs touching. The earlier claim that `scopeattr`, `depreview` and `integearn` are live is WRONG: measured `executed`, `executed`, `superseded`.
- A BACKLOG ITEM CARRIES NO `- Order:` FIELD AT ALL (0 of 174). So do NOT pass `--order` to `aw group backlog`; there is nothing to preserve, and passing it MOVES the filename `NN` slot. Reproduced in a throwaway repo.
- THE ORDER-RESET DEFECT IS `aw group plans`, NOT `aw group backlog`: `plans_refs.py:432`/`:437` substitute zero when the flag is absent. Tracked as backlog `s9p5x5` (`graduated`, `Blocks-Release: next`), already carried by plan `e3hzyc`, so it needs no new report from this child.
- `aw group` MULTI-MATCH RENUMBERS SEQUENTIALLY: `order_val = start_order + i` (`artifact_rename.py:682`). Select ONE id6 per call.
- `From-Backlog` POINTS BY id6, which a regroup does not change, so the back-link should survive a rename by construction. Verify rather than assume.
- `aw check all` IS NOT GREEN: 238 findings, led by `check.scope-drift` 174 and `check.setid-collision` 40. Compare per RULE, reading the `diagnostics` array of the single `--agent` JSON object (`--format json` emits nothing parseable on this surface).
- THE TWO CHECK SURFACES CANNOT AGREE NUMERICALLY on this rule by construction (the hardcoded True versus the pass-through default). Explain the gap; do not demand equality.
- THE SUITE BASELINE IS `1 failed, 5919 passed, 3 skipped, 2 xfailed` and the failure is the ENVIRONMENTAL `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` (a gitignored local dump). `tests/test_orchestrator_retirement.py` PASSES (`112 passed`) and is not a known failure.
- Shared checkout, concurrent edits, suite runs BARE. Re-locate every symbol by name.

## Findings

| Id | Severity | Area | What | Evidence |
|---|---|---|---|---|
| F-1 | HIGH | the target measure is a DECISION, not a measurement | The count depends on `include_retired`: 40 findings / 30 setids (False) versus 86 / 65 (True). `aw check all` says 40, `aw doctor` says 81. Which one this child drives to zero changes both its size and whether `agentadhere` is even in scope. Raised as OQ-04. | `check_collisions` called both ways; `check_engine.py:832-836`, `:1760`; `doctor.py:530` |
| F-2 | HIGH | NOT every collision is sweepable | Under True the 86 include 5 SAME-TYPE `descriptive` mismatches between two `executed` plans (`release-review` x2, `leak-sanitizer`, `assess-documentation`, `assess-bugs`), plus 2 `plans`/`research` and 1 `plans`/`walkthroughs`. None has a backlog source to regroup and executed bodies are immutable, so "resolve EVERY collision" is unachievable as written. | shapes parsed from each finding's detail, `include_retired=True` |
| F-3 | HIGH | the no-graduation-link guard FIRES on the real tree | 39 of 40 flagged items are the target of a resolvable `From-Backlog`; `idxuntrack`/`adgtqb` (`backlog/blocked/`) is not, and its partner `yvvf98` has no back-link to it. OQ-03 says report, not sweep, so the sweep is 39 of 40 on the first run. | `From-Backlog` edges gathered across all plans and specs, matched to each flagged item's `- Id:` |
| F-4 | HIGH | the `--order` mandate is aimed at a field that does not exist, and following it causes the harm | A backlog item carries NO `- Order:` (0 of 174). Reproduced: a bare `aw group backlog <id6> --set X --rename --apply` injects no Order and preserves the filename `NN`; passing `--order 1` on an item at slot `02` MOVED it to `01`. The real defect is `aw group plans` (`plans_refs.py:432`/`:437`) and is already tracked. | `grep -rln "^- Order:" .aw/records/backlog/`; throwaway-repo reproduction; `s9p5x5` |
| F-5 | HIGH | sweeping before Order 03 is wasted | Every collision was created by graduation reusing a source setid; the next graduation recreates one. DEMONSTRATED: scaffolding this Set into `setiduniq` instantly collided with `sjsoqq`, and again on `grouporder`/`s9p5x5`. | measured at authoring; `s9p5x5`'s own history line |
| F-6 | MEDIUM | the plan-side argument is right and its evidence is wrong | `scopeattr`/`h9cn0y` is `executed`, `depreview`/`03ie04` is `executed`, `integearn`/`32ij2j` is `superseded`. None is `approved` or executing. The sound form is that all 40 flagged sides are in `backlog/`, so no plan needs renaming at all. | `- Status:` read from each named plan at review |
| F-7 | MEDIUM | the named case is invisible to one surface, and the plan count is off by one | `3gr7fk` shares `- Set: agentadhere` with SEVEN executed plans, not eight (8 files = 7 plans + the item). And `include_retired=False` reports ZERO `agentadhere` findings, so on the `aw check` surface E-03 clears nothing. | `grep -rln "^- Set: agentadhere" .aw/records/`; both-branch counts filtered to `agentadhere` |
| F-8 | MEDIUM | two validation baselines are wrong and one names the wrong test | Suite is `1 failed, 5919 passed, 3 skipped, 2 xfailed`, not `1 failed, 5648 passed`; `aw check all` is 238 findings, not 98. The named expected failure `tests/test_orchestrator_retirement.py` PASSES (`112 passed`); the real failure is the environmental `test_reporting_contract` case. An executor inheriting the wrong expected failure can excuse a real regression. | `python3 -m pytest` bare; `-o addopts=""` on the named file; `aw check all --agent` diagnostics |
| F-9 | MEDIUM | demanding the two surfaces AGREE makes the item unpassable | They cannot agree on this rule: `doctor.py:530` hardcodes `include_retired=True` while `check_engine.py:1760` passes a False-defaulted flag. Measured 40 versus 81. The right criterion is that both FALL by the number swept and each residual equals the not-sweepable partition. | both commands run at review |
| F-10 | MEDIUM | traceability is the real risk | Before the sweep the shared NAME carried the relationship. After it, only the typed links do, so both directions must be proven to resolve or the tree is worse off. | spec G2/G3/G5 |
| F-11 | LOW | the back-link should survive | `From-Backlog` points by id6 and a regroup does not change an id6. | `releases.check_from_backlog` resolves by id6 |
| F-12 | LOW | per-rule counts need the `diagnostics` array | `aw check all --agent` emits ONE JSON object with a `diagnostics` array of `{location, rule}`; there are no tab-separated records to count, and `--format json` raised `JSONDecodeError`. | both invocations run at review |

## Proposed changes (ordered, validatable)

1. Re-measure both retired branches and partition the population into sweepable and not-sweepable, naming every member of the second group (E-01).
2. Regroup each sweepable collision's SOURCE to a fresh setid via one tooled call per id6, with NO `--order` (E-02).
3. Resolve `agentadhere` from the source side, leaving the seven executed plans untouched and stating which measure the finding is visible on (E-03).
4. Prove the target measure is zero over the sweepable set, that the residual equals the not-sweepable partition, and that no other rule rose (E-04).
5. Prove both link directions still resolve for every regrouped source, and hand back every no-link case unregrouped (E-05).

## Deferred / out of scope (with reason)

- ENABLING OR HARDENING THE ENFORCEMENT POSTURE. This child clears the DATA so the posture becomes reachable; Order 00 verifies the posture as a whole-Set criterion. Hardening before the sweep is precisely what the spec forbids.
- THE `Graduated-To` FIELD AND ITS CHECK. Order 02 (`bwgyum`) builds them. This child CONSUMES them in E-03 and E-05, which is why it must not run before Order 02 either (its dependency on Order 03 transitively gives that, since Order 03 depends on Order 02).
- PREVENTION AT CREATION AND THE FRESH-SETID MINT. Order 03 (`dw7i3m`). This child is a one-time data sweep and adds no guard; without Order 03 its result decays.
- RENAMING ANY PLAN SET. Excluded because it is UNNECESSARY (F-6: all 40 flagged sides are in `backlog/`) and by the spec's own prescription. If a collision cannot be resolved from the source side, report it rather than renaming a plan.
- THE NOT-SWEEPABLE RESIDUE (F-2). The 5 same-type `executed`-plan `descriptive` mismatches, and the `plans`/`research` and `plans`/`walkthroughs` pairs with no backlog source, are IDENTIFIED and REPORTED by E-01 and left in place. Clearing them would require either editing an immutable executed plan or regrouping a walkthrough or research report whose setid deliberately mirrors the plan it documents, both of which are decisions above this child's authority. Consequence stated plainly: the retired-inclusive measure will NOT reach zero from this child, so Order 00's hard-posture criterion must account for them or the posture cannot be enabled fail-closed. That is raised for the Set in OQ-05.
- THE NO-LINK CASE (F-3), specifically `idxuntrack`/`adgtqb`. Reported, not swept, per OQ-03.
- DELIVERABLE #3 (setter type-scoping). Landed in `9107790`; Order 00 checks it as a regression only.
- FIXING `aw group plans`'s ORDER RESET (F-4). A real defect on a surface this child does not touch, ALREADY tracked as backlog `s9p5x5` (`graduated`, `Blocks-Release: next`) and carried by plan `e3hzyc`. So it needs no fresh report; this child simply does not pass `--order`.
- THE id6 COLLISION FAMILY. Already hard, out of scope in the spec, and `sk7ggr` (`id6integ-01`) is in flight on it.

## Scope check

- Over-scope: none. Data only, in one records tree, with no code and no check added.
- Scope-Paths justification: `.aw/records/backlog` holds the flagged side of ALL 40 measured collisions under the `aw check` view (measured 40/40) and is where E-02 and E-03 act. `.aw/records/specs` and `.aw/records/research` were declared at authoring and are now REMOVED, because a declared-but-unmodified path is not free: `aw ipd finalize` refuses to complete without a `--scope-ack` for each one, so declaring a path on the speculation that the inventory might reach it converts a hypothetical into a mandatory acknowledgement. Measured: zero of the 40 flagged sides is a spec, and the two `plans`/`research` findings appear only in the retired-inclusive branch and are in the NOT-sweepable partition (E-01 reports them, does not regroup them). If a future re-measure genuinely finds a SPEC source that must be regrouped, that is a scope change to raise before acting, not a silent edit under a pre-declared path.
- Under-scope, stated rather than left as `none`: this child changes no code, adds no check, renames no plan Set, does not build `Graduated-To`, does not add creation-time prevention, does not harden the posture, does not fix `aw group plans`'s Order reset, does not clear the not-sweepable residue, and does not resolve the no-link case. Each is excluded with a reason above.

## Required tests / validation

- `python3 -m pytest` BARE, before and after, both summary lines pasted and counts stated. Re-measured at review on main: `1 failed, 5919 passed, 3 skipped, 2 xfailed`. MEASURE YOUR OWN BEFORE-BASELINE rather than adopting that line: the single failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` and it is ENVIRONMENTAL (a gitignored local `opencode-recovery/` dump), so it may not reproduce for you. Criterion: the AFTER failing NODE-ID set equals the BEFORE failing node-id set. Do NOT excuse a failure as `test_orchestrator_retirement`: that file PASSES (`112 passed` measured with `-o addopts=""`).
- `aw check all --agent` PER-RULE counts before and after, pasted, parsed from the `diagnostics` array of the single emitted JSON object (there are no tab-separated records; `--format json` raised `JSONDecodeError` at review). Review baseline: 238 total, `check.scope-drift` 174, `check.setid-collision` 40, `check.lifecycle-transition-invalid` 15, `check.name-nonconformant` 3, `check.ipd-dependency-findings-blocked` 2, `check.review-finding-unescalated` 1, `check.review-decision-unescalated` 1, `check.id6-collision` 1, `check.from-backlog-dangling` 1. Criterion: the target measure's residual equals the not-sweepable partition, and no other rule's count RISES. Watch `check.name-nonconformant`, `check.from-backlog-dangling` and `check.scope-drift`.
- `aw doctor`'s setid-collision count before and after, pasted BESIDE `aw check all`'s, with the difference EXPLAINED by the `include_retired` asymmetry (`doctor.py:530` hardcodes True; `check_engine.py:1760` passes a False-defaulted flag). Criterion: both counts FALL by the number of sources regrouped. Asserting the two are EQUAL is a failed validation (measured 40 versus 81 at review).
- THE PER-SETID INVENTORY from E-01 pasted in full, including the sweepable-versus-not-sweepable partition, so a reviewer can see what was touched and what was deliberately not.
- BOTH LINK DIRECTIONS resolved for every regrouped source (E-05), with the commands and their output, plus every no-link case named with both paths and confirmed unregrouped.
- EVERY REGROUPED FILE'S filename `NN` SLOT verified unchanged and NO `- Order:` injected, given F-4: a backlog item has no Order field, so an appearing Order is a defect and a moved `NN` means `--order` was passed against instruction.
- MEASURE EXIT CODES UNPIPED (`cmd >/dev/null 2>&1; echo $?`).
- `aw sanitize --agent` clean.

## Spec / documentation sync

No spec change is expected from this child: it EXECUTES spec `4w7d6s` Section 5 rather than amending it. The spec is `- Status: draft` and Order 00's E-02 holds that gate; this child must not begin until it is answered.

`.aw/records/backlog/README.md`'s "Promotion to a plan" section describes the graduation convention and is where the fresh-setid rule belongs once Order 03 lands. That documentation edit is Order 02's or Order 03's, NOT this child's, and this child does not declare the README.

If the executor finds prose ANYWHERE (in a README, a spec, or an AGENTS.md block) that instructs a graduation to REUSE the source's setid, that is the convention this Set changes and it must be REPORTED as a finding for the child that owns the documentation, not edited here.

ONE SPEC AMENDMENT MAY BE OWED AND IS NOT THIS CHILD'S TO MAKE. Section 5 says a sweep "MUST identify and resolve any OTHER existing cross-type setid collisions before I2 is turned on fail-closed", and measurement shows a subset cannot be resolved at all from a source side (F-2: same-type executed-plan pairs, and cross-type pairs with no backlog source). So either the spec's "resolve" needs qualifying, or the hard posture needs an explicit carve-out for the residue. That is a change to a `draft` spec governing the whole Set, so it belongs to Order 00, and this child RAISES it (OQ-05) rather than editing the spec under a path it does not declare.

Write no em or en dashes in user-facing prose.

## Open questions

### OQ-01: Which side of each collision should be renamed?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: THE SOURCE, ALWAYS, and the answer survives review with a CORRECTED basis. The original reasoning cited three Sets as `approved` or executing under other agents; measured at review all three are terminal (`scopeattr`/`h9cn0y` `executed`, `depreview`/`03ie04` `executed`, `integearn`/`32ij2j` `superseded`), so that particular argument was false. The answer is nonetheless unchanged and now rests on something stronger and measurable: ALL 40 findings under the `aw check` view have their flagged side in `backlog/` (40 of 40), so the source side clears every one and renaming a plan is not merely risky but UNNECESSARY. The secondary argument still holds, that a plan setid is cited in plan filenames, `Item-Dependencies` edges, review-record filenames, run records and prose, while the source side is one file. The spec's Section 5 independently prescribes the same direction for `agentadhere`. The residual cost is the at-a-glance name match, which E-05 replaces with proven typed links, and which Order 00's OQ-02 puts to the maintainer as the Set-level question.

### OQ-02: What should a regrouped source's new setid be called?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: DISTINCT TOKEN, RECOGNIZABLE SLUG, per the spec's OQ-03 reasoning that "the human-readable slug can still echo the source (only the setid token must differ)". So a `specvis` item may become, for example, `specvisitem`, keeping the filename readable while making the token unique. This is deliberately left as a naming convention rather than an algorithm, because E-01's inventory may reveal sources whose obvious derived token is ALSO taken, and the executor must then pick another rather than follow a rule off a cliff. What is NOT acceptable is a meaningless token (a hash or a number), since the slug is the only thing a human skims.

### OQ-03: What if a colliding pair has no graduation link at all?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: REPORT IT, DO NOT SWEEP IT. If no `From-Backlog` edge joins the two artifacts, then the shared setid is the ONLY record that they are related, and regrouping the source would destroy that information while making the finding disappear, which is strictly worse than the finding. E-01 must flag such a pair and E-05 must report it; the remedy (establish the real link first, or accept that they are unrelated and rename deliberately) is a human decision because it requires knowing the intent behind two artifacts. CORRECTED AT REVIEW: the plan called this "a guard rather than an expected case" on the basis that all 29 authoring-era collisions were joined pairs. Measured at review, it is a LIVE case: 39 of 40 flagged items are the target of a resolvable `From-Backlog`, and `idxuntrack`/`adgtqb` (`.aw/records/backlog/blocked/20260908-idxuntrack-01-adgtqb-...backlog.md`) is not, its partner `yvvf98` carrying no back-link to it. So the sweep is 39 of 40 on the first run and this question has a named subject.

### OQ-04: Which measure must this child drive to zero, `aw check all`'s 40 or `aw doctor`'s 81?

- Blocking: yes
- Status: open
- Owner: maintainer
- Finding: F-1
- Resolution or deferral rationale: NOT AGENT-RESOLVABLE, because the two answers are different pieces of WORK and one of them decides whether this child's named motivating case exists at all. Measured at review: `check_collisions(root, include_retired=False)` gives 40 findings across 30 setids and is what `aw check all` reports; `include_retired=True` gives 86 across 65 and is what `aw doctor` reports as 81, because `doctor.py:530` hardcodes True while `check_engine.py:1760` passes a flag defaulting to False (`:835`). Under the False measure the `agentadhere` collision that MOTIVATED this whole item has ZERO findings (both its plans are in `executed/`, which the filter drops), so E-03 would clear nothing measurable and the spec's Section 5 case would be satisfied on paper only. Under the True measure the population more than doubles and includes the not-sweepable residue of F-2, so the sweep provably cannot reach zero. An agent must not pick: the choice determines whether Order 00 can enable the hard posture fail-closed at all, and it is the same identity-scope judgement Order 00's OQ-02 already reserves to the maintainer. A `/plan-review` on 2026-09-09 attempted to ask and found no interactive channel in this session, so it is left explicitly OPEN with the decision stated rather than guessed. E-01 must read the recorded answer and quote it before any regroup.

### OQ-05: What happens to the collisions that have no source to regroup?

- Blocking: yes
- Status: open
- Owner: maintainer
- Finding: F-2
- Resolution or deferral rationale: NOT AGENT-RESOLVABLE, because every available answer either edits an immutable artifact or amends an approved-track spec. Measured at review under `include_retired=True`: FIVE findings are same-type `descriptive` mismatches between two `executed` plans (`release-review` twice, `leak-sanitizer`, `assess-documentation`, `assess-bugs`), two are `plans`/`research`, and one is `plans`/`walkthroughs`. None has a backlog item to regroup. The same-type five cannot be fixed from a source side at all, because both sides are plans in `executed/` whose bodies are immutable by policy; the research and walkthrough cases have a renameable file, but their setid deliberately mirrors the plan they document, so regrouping them destroys the association that makes them findable. Spec Section 5 nevertheless says the sweep MUST "resolve any other existing cross-type setid collisions before I2 is turned on fail-closed". Three shapes of answer exist and all three are the maintainer's: amend the spec so `resolve` means "resolve or document as grandfathered"; carve the residue out of the hard posture explicitly; or authorize editing executed plans, which contradicts standing policy. This child REPORTS the residue and does not choose. A `/plan-review` on 2026-09-09 attempted to ask and found no interactive channel, so it is left explicitly OPEN.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: paste the per-setid inventory IN FULL with the five facts per collision (artifacts and types and paths; whether a source side exists; the plan side's `- Status:` and directory; whether a `From-Backlog` edge joins the pair; the finding's SHAPE from its detail text). Paste BOTH branch counts, `include_retired=False` and `=True`, and compare each to the review baseline of 40/30 and 86/65, stating the difference. QUOTE OQ-04's recorded answer and name which measure you targeted; if OQ-04 is still `open`, this validation FAILS and no regroup may have happened. Paste the sweepable-versus-not-sweepable partition with every not-sweepable member named and the reason it cannot be swept.
  - Observed evidence:
  - Result: pending

- [ ] V-02 validates E-02
  - Required evidence: paste the ACTUAL `aw group` command and its output for each regrouped source, showing an id6 selector and NO `--order` flag. Paste each regrouped file's `- Set:` line and its full FILENAME after the regroup, proving the `NN` slot is unchanged, and paste a grep proving NO `- Order:` was injected (F-4: a backlog item has none, so an appearing Order is a defect). Paste NEGATIVE proof that no plan file was renamed: a `git status --porcelain .aw/records/plans/` showing no plan path changes attributable to this child. Confirm the count of regrouped sources equals the sweepable partition's size from V-01.
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: paste `3gr7fk`'s front matter before and after, showing the new setid and the `Graduated-To` entry. Paste proof the SEVEN `agentadhere` executed plans are untouched (`git status` plus their unchanged `- Set:` lines), and state the count you measured rather than repeating seven. Paste the `agentadhere` finding count on BOTH branches before and after; if the `include_retired=False` count was ZERO before, say so explicitly and do NOT claim a finding was cleared on that surface, since none existed (F-7). State in one sentence that the setter half was already fixed in `9107790` and was not rebuilt.
  - Observed evidence:
  - Result: pending

- [ ] V-04 validates E-04
  - Required evidence: paste `aw check all --agent` PER-RULE counts before and after, derived from the `diagnostics` array, naming every rule whose count changed with the reason. Show the target measure's RESIDUAL and prove it equals the not-sweepable partition from V-01 member for member; a residual member absent from that partition FAILS this validation. Paste `aw doctor`'s setid-collision count beside `aw check all`'s BEFORE and AFTER, show both FELL by the number regrouped, and explain the remaining gap by the `include_retired` asymmetry; asserting the two are EQUAL fails this validation. THEN paste your OWN bare `python3 -m pytest` summary line before the sweep and after, and compare failing NODE IDS, not counts. Do NOT cite `test_orchestrator_retirement` as a known failure: it passes.
  - Observed evidence:
  - Result: pending

- [ ] V-05 validates E-05
  - Required evidence: for EVERY regrouped source, paste the resolution of BOTH directions: the plan's `From-Backlog` resolving to the regrouped item, and the source's `Graduated-To` resolving to the plan Set. Paste the output of `check.graduated-to-dangling` over the tree showing zero findings; if that rule does not exist in the tree you are running against, Order 02 has not executed and this validation FAILS rather than being waived. Paste the no-link report naming every such pair with BOTH paths (expect at least `idxuntrack`/`adgtqb` versus `yvvf98`) and confirm each was NOT regrouped and no link was invented.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

THIS CHILD MUST NOT RUN FIRST DESPITE ITS ORDER DIGIT. It carries `- Item-Dependencies: executed:dw7i3m` deliberately: sweeping before Order 03 mints fresh setids means the next graduation recreates a collision, and the sweep would have to be repeated. The runner sorts by dependency depth first and will handle this; a human executing by hand must read the edge.

IT ALSO INHERITS ORDER 00's TWO BLOCKING QUESTIONS, AND CARRIES TWO OF ITS OWN. Order 00's OQ-01 is the spec-approval gate (`4w7d6s` is `draft`), and its OQ-02 asks whether hard cross-type uniqueness is the right answer at all given that every finding is the repository's own working convention; if the maintainer narrows I1 instead, this child's sweep is unnecessary and must not be run. THIS child's OQ-04 (which measure to drive to zero) and OQ-05 (what happens to collisions with no source) are `Blocking: yes` and `open`, and `aw ipd lint` refuses this plan at EVERY checkpoint until they are answered, which is the intended stop. Do NOT read this plan's three RESOLVED questions as evidence that it is clear to execute.

WHAT IS SAFE TO DO UNDER ANY RULING, IF A PARTIAL RUN IS EVER AUTHORIZED: nothing. Unlike the prevention half of Order 03, every one of this child's five E-items depends on the target measure OQ-04 fixes, because E-01 partitions by it, E-02 sweeps that partition, E-03's motivating finding exists only on one branch, and E-04 and E-05 validate against it. So this child is entirely hostage to its two blocking questions and must not be started in part.

ITS DECLARED DEPENDENCY IS GENUINELY UNMET, not merely unordered. E-03 and E-05 consume the `Graduated-To` field and its dangling check, which Order 02 (`bwgyum`) builds; measured at review, `grep -rn "Graduated-To" agent_workflows/` returns NOTHING, so the field exists nowhere in the codebase. `bwgyum` is `reviewed` in `pending/` and `dw7i3m` is `reviewed` in `pending/`, so neither prerequisite has executed.

EXECUTION CONTRACT. Commit only files this plan changed, path-scoped (`git commit -m msg -- <paths>`); never `git add -A` and never push. Use the TOOLED regroup verb, select by id6, one call per item, and do NOT pass `--order`; never hand-rename a records file. Do NOT rename any plan Set, and do NOT edit any file under `.aw/records/plans/`. Do NOT touch a backlog item outside the measured collision set, since other agents are graduating items concurrently. Paste ACTUAL command output and compare `aw check` findings PER RULE from the `diagnostics` array, never by total. Verify the staged set with `git diff --cached --name-only` before every commit and re-verify after any failed hook, since `pre-commit` can leave another agent's paths in the index. If an out-of-scope path must be changed, change it and JUSTIFY it at finalize with `--scope-reason`; `Scope-Paths` is a declaration for reconciliation, not a stop sign.

POST-GATE LIFECYCLE MOVE. Do not claim done or move this plan to `.aw/records/plans/executed/` until `aw ipd lint --phase pre-transition` conforms and every `V-*` above carries concrete pasted evidence, including the full per-setid inventory with its partition, both link directions for every regrouped source, and the named residue this child deliberately did not sweep.
