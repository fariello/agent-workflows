# IPD: Deprecation Warning for Per-Workflow Slash-Command Aliases

- Date: 2026-08-22
- Kind: child
- Concern: The single `/aw <verb>` dispatcher has shipped, but the ~24 per-workflow slash-command aliases give users no signal to migrate before eventual pruning.
- Scope: The shim generator in `agent_workflows/engine.py` and the regenerated command files under `.opencode/commands/` and `.claude/commands/`; the aliases keep working.
- Status: draft
- Set: highpbacklog0822
- Order: 3
- Highest E allocated: 03
- Author: opencode (its_direct/pt3-claude-opus-4.8-1m-us)
- Id: h4e9yi

## Workflow history

- 2026-08-22 draft (opencode (its_direct/pt3-claude-opus-4.8-1m-us)): created for backlog 21ni81; the `/aw <verb>` dispatcher prerequisite is already met (`.opencode/commands/aw.md`, `.claude/commands/aw.md`).

## Goal

Tell users of a per-workflow slash alias (e.g. `/assess`, `/spec`) to migrate to `/aw <verb>`, without breaking the aliases, so a later pruning is low-surprise.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation.

### Material change 1: Emit the notice in the generator

- [ ] E-01 In `agent_workflows/engine.py` `shim_body()` (`engine.py:700-825`), add a short, non-blocking deprecation notice to each generated per-workflow shim that names the equivalent `/aw <verb>` form and states the alias still works for now but will eventually be pruned. Do NOT add the notice to the `/aw` dispatcher shim (`aw_dispatcher_shim()`, `engine.py:828-868`).
  - Depends on: none
  - Expected outcome: every generated per-workflow command file carries a migrate-to-`/aw <verb>` notice; the dispatcher shim does not.
  - Execution state: pending

### Material change 2: Keep the shim grammar valid

- [ ] E-02 Ensure the added notice passes `validate_shim_grammar()` (`engine.py:871+`) and does not change the shim's executable behavior (the alias still dispatches to the same workflow).
  - Depends on: E-01
  - Expected outcome: `validate_shim_grammar()` accepts the annotated shims and the alias behavior is unchanged.
  - Execution state: pending

### Material change 3: Regenerate and verify the installed files

- [ ] E-03 Regenerate the command files into `.opencode/commands/` and `.claude/commands/` (`engine.py:167-168`) and confirm every per-workflow alias file shows the notice while `aw.md` (the dispatcher) does not.
  - Depends on: E-01, E-02
  - Expected outcome: the tracked command files reflect the notice consistently across both agent dirs.
  - Execution state: pending

## Project conventions discovered (Step 0)

- Dispatcher shipped: `.opencode/commands/aw.md` and `.claude/commands/aw.md` read `@.aw/system/workflows/index.md`, take `$ARGUMENTS` first token as the verb, and execute the workflow (`.opencode/commands/aw.md:1-12`).
- Per-workflow shims are generated by `shim_body()` (`engine.py:700-825`); the dispatcher by `aw_dispatcher_shim()` (`engine.py:828-868`); grammar checked by `validate_shim_grammar()` (`engine.py:871+`); output dirs at `engine.py:167-168`.
- The ~24 flat command files (`advise`, `assess`, `verify`, `spec`, `whatnext`, ...) are the deprecation candidates. Follow-on to q19z5t; backlog-medhigh-260819 Order 05 kept the aliases working.

## Findings

The dispatcher prerequisite is satisfied, so this is purely additive: annotate the generated per-workflow shims with a migration notice. Removal/pruning is explicitly a later decision, not part of this item.

## Proposed changes (ordered, validatable)

1. `shim_body()` emits a deprecation notice per per-workflow shim (E-01), excluding the dispatcher.
2. Notice conforms to `validate_shim_grammar()` and preserves behavior (E-02).
3. Regenerated `.opencode/commands/` + `.claude/commands/` files carry the notice consistently (E-03).

## Deferred / out of scope (with reason)

- PRUNING the per-workflow aliases (removing the files or stopping their generation): a later decision; this item only warns.
- Any change to the `/aw <verb>` dispatcher itself: out of scope.

## Scope check

- Over-scope: none.
- Under-scope: confirm the notice wording is agent-facing and does not break command parsing in either OpenCode or Claude Code command formats.

## Required tests / validation

Add/extend a test that generates the shims and asserts every per-workflow shim contains the deprecation notice and the `/aw <verb>` equivalent, that `aw.md` does NOT contain it, and that `validate_shim_grammar()` passes for all generated shims. Regenerate and diff the tracked command files. Paste the actual test output and the diff summary.

## Spec / documentation sync

If a contributor/command doc enumerates the slash aliases, add a one-line "aliases are deprecated; prefer `/aw <verb>`" note pointing at the dispatcher.

## Open questions

### OQ-01: Where should the notice appear in a shim (top comment vs body)?

- Blocking: no
- Status: resolved
- Owner: maintainer
- Resolution or deferral rationale: place it as a leading comment/frontmatter line in the generated shim so it is visible to a user reading the command file but does not interfere with `$ARGUMENTS` dispatch; keep it to one or two lines.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: the generator test shows every per-workflow shim contains the migrate-to-`/aw <verb>` notice and the dispatcher shim does not; paste the test output.
  - Observed evidence:
  - Result: pending
- [ ] V-02 validates E-02
  - Required evidence: `validate_shim_grammar()` passes for all generated shims and an alias still dispatches unchanged (behavior test); paste the output.
  - Observed evidence:
  - Result: pending
- [ ] V-03 validates E-03
  - Required evidence: the regenerated tracked files under `.opencode/commands/` and `.claude/commands/` carry the notice consistently and `aw.md` does not; paste the diff summary.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: exactly three changes (emit, validate, regenerate) around one additive notice; aliases keep working.

Review and explicit approval required.

### Execution contract

1. Open questions RESOLVED: OQ-01 above is resolved. `Depends on: none`.
2. Scope fence: touch only `agent_workflows/engine.py` (the `shim_body` notice + any test hook), the regenerated files under `.opencode/commands/` and `.claude/commands/`, the shim tests under `tests/`, and at most a one-line command-doc note. Do NOT remove or stop generating any alias, and do NOT modify the `/aw` dispatcher. If pruning seems warranted, STOP and report (it is a separate decision).
3. Honesty rule (hard MUST): when you report the generator/grammar tests passed, paste the ACTUAL runner output; never claim a pass you did not run.
4. Commit ONLY this plan's own changed files, path-scoped (`git commit -- <path>`); never `git add -A`/bare/`-a`; never push.
5. Lifecycle move: on completion, after every E item is performed and every V item is verified with pasted evidence, append the `## Workflow history` line, set `Status: executed`, `git mv` this file from `pending/` to `executed/`, make the path-scoped lifecycle commit, and set backlog `21ni81` to `done`.
