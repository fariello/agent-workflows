# IPD: one work-nature field name across backlog, plans, and specs

- Date: 2026-08-29
- Kind: orchestrator
- Concern: Only backlog records the NATURE of work (bug, feature, chore, security, followup). Plans and specs do not, so that classification is lost the moment a backlog item graduates into a plan, and nothing can ask for all bug work across the trees. The maintainer decided the field must carry ONE name everywhere, which makes this two genuinely different jobs: a migration that renames backlog's existing field across 88 tracked items, and a feature that adds the field to two more contracts. This orchestrator exists because those jobs have different failure modes and are independently executable, and because the superseded single plan `a6cej0` bundled them.
- Scope: ORCHESTRATOR - authors NO product code. Its own execution work is (E-01) whole-Set verification only. Child 01 carries the rename plus migration; child 02 carries the field addition. This plan owns the child sequence, the one-vocabulary rule both children inherit, the Set completion criteria, and the cross-plan checks. Excludes adding the field to research, excludes any attention-board wiring or sort change, and excludes deriving the value from `From-Backlog`.
- Scope-Paths: .aw/records/plans/pending/20260829-wkindname-00-c2tvmm-one-work-nature-field-name-across-backlog-plans-and-specs.ipd.md
- Item-Dependencies: none
- Status: reviewed
- Set: wkindname
- Order: 0
- Highest E allocated: 01
- Author: opencode (its_direct/pt3-claude-opus-5-1m-us)
- Id: c2tvmm
- Blocks-Release: next
- From-Backlog: 1ap48y

## Workflow history
- 2026-08-30 reviewed (aw set): /plan-review (opencode its_direct/pt3-claude-opus-5-1m-us): APPROVE WITH REVISIONS APPLIED; PR-001..PR-008 fixed in place. Corrected two would-have-failed validations (backlog REQUIRES the field so the Set unifies name not requiredness; the 88/2 counts are tree-wide greps while the parser reads 87 items and 1 Gate-Kind item) and one unachievable gate (aw backlog check is already red on 3 unrelated violations, now no-worsening). Also: two validation mechanisms not one, command_surface.py added to child 01 because backlog new does declare --kind, orchestrator scope narrowed off the shared pending/ dir, 10 old-spelling fixture modules must pass unedited, and E-01 recorded as a manual obligation the runner rollup does not perform. Baselines re-measured at be49ac4 (2927 passed, 3 skipped, 4 xfailed). All three lint conforming at review-finalize.

- 2026-08-29 draft (opencode (its_direct/pt3-claude-opus-5-1m-us)): created.
- 2026-08-30 /plan-review (opencode (its_direct/pt3-claude-opus-5-1m-us)): APPROVE WITH REVISIONS APPLIED; PR-001..PR-008, all FIXED in place, none deferred. The Set's structure and its split rationale hold up: the concern-mixing criterion is correctly cited, the phase independence is real, and the `xprio` template is genuinely applicable. The defects were in the Set's ASSERTED FACTS, and two of them would have made a child fail its own validation. PR-001 (HIGH, new F7/F11 + OQ-02): the completion criteria promised the field would be "optional on all three so an artifact without it still validates", which is FALSE for backlog - it validates the field on read and an absent value is `backlog.kind-invalid` (exit 1, measured), and child 01 is a pure rename that preserves that. An executor would have had to either paste unobtainable evidence or "fix" the mismatch by weakening a live contract across 87 records; the criteria now state the intended asymmetry (optional on the two new carriers, required on backlog, one shared name and vocabulary) and OQ-02 records why. PR-002 (HIGH, new F11 in the orchestrator, F1b in child 01): the 88/2 counts both children validate against are TREE-WIDE GREP counts, not item counts - the parser reads 87 items (86 `.backlog.md` plus one legacy-named `.md`) and skips `README.md`, and the `Gate-Kind` ITEM count is 1 not 2. Child 01 asserted "88 carry the new spelling, 0 carry the old" as a pass condition it could not satisfy; enumeration is now derived from `backlog._iter_items` and the README is owned as DOCUMENTATION by a new E-06. PR-003 (HIGH, new F12/F7): both children required `aw backlog check` CLEAN, but it is already RED with 3 pre-existing `backlog.summary-unsafe` violations outside either fence - the same unrunnable-requirement defect the prior review caught on `a6cej0`'s CLI-conformance check, recurring on a different command; converted to no-worsening keyed on the absence of any `backlog.kind-invalid` finding. PR-004 (MED, new F8 orchestrator / F12 child 02): "one `aw check` rule flags each type" conflated two mechanisms (backlog's own `backlog.kind-invalid` contract drift versus the new registry rule) and could have invited migrating backlog's validation into the registry, which no child is scoped for. PR-005 (MED, new F10/F6): child 02's measured finding that `--priority` is undeclared in `legacy_flags` does NOT transfer to child 01, because `backlog new` DOES declare `--kind`; added `command_surface.py` to child 01's Scope-Paths with a new E-05 to reconcile that one tuple. PR-006 (MED, new F13): the orchestrator declared `Scope-Paths: .aw/records/plans/pending`, a whole SHARED directory - verified that entry is directory-bounded, so any other agent's uncommitted plan there would refuse `aw ipd begin` and pollute finalize's reconciliation; narrowed to its own file. PR-007 (MED, new F8 in child 01): 10 existing test modules emit the old spelling as fixture data and the plan never said what to do with them; they are now named as the dual-read window's best real-caller proof, must pass UNEDITED, and are fenced against mass-editing. PR-008 (LOW, new F9): an orchestrator's E-01 is not self-executing - the runner finalizes a completed Set's orchestrator via `aw ipd set executed` with no agent turn, so the whole-Set verification is a manual obligation; recorded, with the pre-transition lint (verified refusing on unperformed E-01 and empty evidence) as the backstop. Stale baselines refreshed by measurement: fast suite `2927 passed, 3 skipped, 4 xfailed` at `be49ac4` (the recorded `2880` at `df731f1` was already stale), and F9's `59` undeclared-leaf baseline independently re-measured and unchanged. All three plans lint `conforming` at review-finalize with zero diagnostics and zero advisories. No open questions block; human approval remains the sole execution gate.
- 2026-08-30 to-review (opencode (its_direct/pt3-claude-opus-5-1m-us)): created by SPLITTING the approved plan `a6cej0` at the maintainer's direction. `a6cej0` began as "add an optional field", then absorbed the maintainer's rename-first decision, which turned it into a migration bundled with a feature. It stayed under the size thresholds (8 leaves, 3 groups against limits of 18 and 5), so this split is NOT a size split; it is the spec's other criterion, that work mixing distinct concerns with independently-executable phases should be split. The dependency graph in `a6cej0` proved the phases were already independent: its field-addition items declared `Depends on: none` and nothing in them depended on the rename items. `a6cej0` is retired as superseded by this Set, and the maintainer's four recorded decisions carry over verbatim (one shared vocabulary; store rather than derive; rename first; recognized-but-optional mechanics per the `Priority` precedent).

## Goal

Give plans and specs the same work-nature field backlog already has, under a single field name, without letting a mechanical 88-file migration and a two-contract feature share one blast radius.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

This orchestrator authors NO product code. Its only execution step is the whole-Set verification.

### Task group 1: whole-Set verification

- [ ] E-01 After both children execute, confirm the Set delivered one field name on one vocabulary: backlog, plans, and specs all use `- Work-Kind:`; every consumer reads the SAME vocabulary symbol with no forked copy anywhere; an out-of-vocabulary value is REPORTED on each type; and the distinct `- Gate-Kind:` field is intact. Observe the REQUIREDNESS ASYMMETRY exactly as F7 states it and do NOT try to erase it: the field is OPTIONAL on plans and specs (absent validates) and REQUIRED on backlog (absent is `backlog.kind-invalid` today and must stay so after the rename). If verifying this seems to require making backlog's field optional, that is a contract change no child authorizes: STOP and report. Also observe the two-MECHANISM asymmetry per F8: backlog reports through its own `backlog.kind-invalid` contract drift, plans and specs through the new `check.work-kind-invalid` registry rule. Then confirm the honest limit is stated rather than oversold: nothing yet CONSUMES the field for filtering, so the Set must not be reported as delivering cross-tree search. This item is a MANUAL pass and cannot be produced by the runner's orchestrator rollup (F9); perform it, write the evidence into V-01, and only then let the transition happen.
  - Depends on: none
  - Expected outcome: one field name across three types on one shared vocabulary; absent validates on plans and specs while backlog keeps requiring it; a bad value is reported on each type (via each type's own mechanism); `Gate-Kind` unaffected; the no-filtering limit recorded; suite no worse than a freshly measured baseline.
  - Execution state: pending

## Child IPDs, sequence, and dependencies

| Order | File (id6) | What it does | Depends on |
|---|---|---|---|
| 01 | 9trlc3 | Rename backlog's on-disk `- Kind:` field to `- Work-Kind:` in `backlog.py`, and migrate the 88 existing items behind a dual-read window. A pure migration: mechanical, one module, many tracked files. | none |
| 02 | ng2blv | Add the recognized-but-optional `Work-Kind` field to the IPD schema and the spec contract, with setter support and an `aw check` enum rule, reusing the one shared vocabulary. | 01 executed |

Children are SEQUENTIAL. Child 02 declares `executed:9trlc3` because the field name it recognizes must already be the name backlog writes; adding the field first and renaming after would put two spellings in the tree at once, which is the confusion this Set exists to remove.

Hard constraints both children inherit, stated once here:
- ONE vocabulary. `backlog.KINDS` is the single source and no child may fork, subset, or duplicate it. The `xprio` Set's completion criteria demand exactly this ("no forked copies", provable by grep) and this Set follows it.
- The field is RECOGNIZED-BUT-OPTIONAL ON PLANS AND SPECS. Absent means unclassified there, with no forced default, so no existing artifact is mass-failed. It is NOT optional on backlog and this Set does NOT make it so (F7): backlog REQUIRES it today (absent yields `backlog.kind-invalid`) and child 01 is a pure rename that preserves that requirement. The Set therefore delivers ONE NAME, not one requiredness. Any child that reports "the field is optional on all three" has either mis-measured or silently changed backlog's contract.
- The enum check for the NEW carriers lives in the `aw check` surface, never in a schema or contract layer. This is the layering rule the schema states for `Priority`, `Scope-Paths`, `Blocks-Release`, and `From-Backlog`. Backlog keeps validating through its own existing contract-drift path (`backlog.kind-invalid`); child 01 must NOT relocate backlog's validation into the registry, and no child may claim one rule covers all three types (F8).
- Never anchor a rename or a search on the bare token `Kind`. Four other things collide with it: an IPD's structural `Kind` (orchestrator or child), research's document-type kind, a comms message `Kind` (`ask|reply|task|handoff|fyi`, `comms.KINDS`), and backlog's own `Gate-Kind`.

## Completion criteria (the whole Set is done only when)

- Backlog, plans, and specs all carry the work-nature field under the name `- Work-Kind:`.
- Exactly one vocabulary definition exists, consumed by every reader (provable by grep for the member tokens).
- An artifact with no `Work-Kind` validates ON PLANS AND SPECS. On BACKLOG absence stays an error, unchanged from today (F7); that asymmetry is the intended outcome, not a gap to close.
- An out-of-vocabulary value is reported on a backlog item, a plan, and a spec. Backlog reports it as `backlog.kind-invalid` (its existing contract path) and plans/specs as `check.work-kind-invalid` (the new registry rule); two mechanisms, not one (F8).
- The 86 migrated backlog ITEMS plus the 1 legacy-named item the parser also reads validate, and the `- Gate-Kind:` ITEM count is still exactly 1 and still parses. Counts exclude `README.md`, which the parser skips but a naive `grep -rl` over the tree counts (F6).
- `.aw/records/backlog/README.md` documents the new spelling, since it is the human-facing field list and is NOT an item.
- Suite no worse than a freshly measured baseline, and the record states that filtering is NOT delivered.

## Cross-IPD validation

- CID-1: one vocabulary symbol, no forked member list anywhere in `agent_workflows/`.
- CID-2: one field name across all three types; no module still reads the old backlog spelling once child 01 is executed.
- CID-3: the field is absent from the PLAN and SPEC required-field sets (`ipd_schema.META_REQUIRED` and the spec contract's equivalent), so absence never fails a plan or a spec. Backlog's requiredness is deliberately NOT changed (F7), so this check must be applied per type and never as a blanket "absent is valid everywhere" assertion.
- CID-4: `Gate-Kind` is untouched by the migration, verified by ITEM count (1, not the 2 a tree-wide grep reports, F6) and by parsing the item that carries it.
- CID-5: the `--kind` CLI flag stays accepted. `backlog new` DECLARES `--kind` in `command_surface.COMMAND_INVENTORY`'s `legacy_flags` (F10), unlike the `--priority` precedent child 02 reasons from, so if child 01 adds a `--work-kind` spelling the declaration must be reconciled rather than assumed absent. Verify the declared flag set matches the parser's accepted set for `backlog new`.

## Project conventions discovered (Step 0)

- The pattern for this exact shape of change is ALREADY EXECUTED and should be copied rather than reinvented: the `xprio` Set added a uniform recognized-but-optional `Priority` across plans, specs, and research as an orchestrator plus three per-type children. Its rules are the ones this Set inherits: optional not required, absent means unset, one shared vocabulary.
- Test precedents exist for a field of exactly this kind: `tests/test_ipd_priority.py` and `tests/test_spec_priority.py`. Child 02 mirrors them instead of inventing a test shape.
- The `check.priority-invalid` rule is the template for the enum check: registered as error severity, repository assurance class, deterministic.
- `backlog.py` is the ONLY module that reads backlog's work-nature field, which is what keeps child 01 small in code terms even though it rewrites many files.
- The `Priority` precedent this Set copies is OPTIONAL EVERYWHERE, but the field this Set moves is REQUIRED on backlog. That is the one place the analogy breaks, and it is why the Set's outcome is one NAME rather than one requiredness (F7). Copy `xprio`'s mechanics for the new carriers; do not copy them onto backlog.
- Backlog validates its own fields through contract drift (`backlog.kind-invalid`), NOT through the `aw check` rule registry. The registry is where the new carriers' enum check goes. Two mechanisms coexist by design (F8).
- The backlog tree contains files that are NOT items: a `README.md` documenting the field list, which the parser skips by name, and one legacy-named `.md` item without the `.backlog.md` suffix, which the parser DOES read. Any count of "how many items carry this field" must come from the parser's own iterator, never from a `grep -rl` over the tree (F11).
- The superseded `a6cej0` carries the full evidence base for all four maintainer decisions and its findings remain valid; children should cite it rather than re-deriving.

## Findings

| # | Sev | Location | Finding | Evidence |
| --- | --- | --- | --- | --- |
| F1 | MED | `a6cej0` structure | The split criterion is CONCERN MIXING, not size. `a6cej0` had 8 executable leaves across 3 task groups against thresholds of 18 and 5, so it was never oversized; the spec separately directs splitting work that mixes distinct concerns or has independently-executable phases, and after the rename decision it did both. | `ipd_schema.MAX_E_LEAVES = 18`; the ipd-spec size-threshold paragraph naming "mixes distinct concerns, or has independently-executable phases" |
| F2 | MED | `a6cej0` dependency graph | The phases were ALREADY independent, which is why splitting costs nothing structurally. Its field-addition item declared `Depends on: none` and no field-addition item depended on either rename item, so the rename could ship and be verified entirely on its own. | `a6cej0` E-item `Depends on:` lines |
| F3 | MED | `a6cej0` gate | A REAL DEFECT in the approved plan, which the split resolves: its cohesion rationale still read "6 E-leaves across 2 task groups" after the rename decision took it to 8 across 3. The stale count was introduced when the rename items were added and would have misled a reviewer about the plan's shape. | `a6cej0` "Cohesion rationale" line versus its measured leaf and group counts |
| F4 | HIGH | `backlog.py`; `.aw/records/backlog/` | The migration's blast radius is the reason to isolate it: 88 tracked items carry the field, in a checkout where other agents commit concurrently. The code change is small (one module) but the file count is not, so a conflict in the migration should not be able to block the unrelated feature work. | `grep -rl '^- Kind:' .aw/records/backlog/` = 88; the only work-nature reads live in `backlog.py` |
| F5 | HIGH | `backlog.py` | The corruption hazard child 01 must own: `backlog.py` also parses a DISTINCT `- Gate-Kind:` field that 2 items carry, so any substring-based rename silently produces `Gate-Work-Kind` and breaks the gate contract. | `grep -rl '^- Gate-Kind:' .aw/records/backlog/` = 2 |
| F6 | MED | `attention.py`, `check_engine.py` | The honest limit, carried over from `a6cej0` and still true: NOTHING consumes a work-nature field today, not even on backlog. This Set makes the classification recorded and validated on two more types; it does NOT deliver cross-tree filtering, and no child may claim it does. | the attention item record carries priority and release-blocker fields but no work-nature field; no reader of the vocabulary outside backlog's own validation and creation paths |
| F7 | HIGH | `backlog.py:178`; `ipd_schema.py:129`/`:199` | THE SET'S OWN COMPLETION CRITERIA WERE FALSE AS WRITTEN, and this is the correction that matters most. Both this orchestrator and its parent premise claimed the field would be "optional on all three so an artifact without it still validates". It is NOT optional on backlog and this Set does not make it so: backlog VALIDATES the field on read and an item with no `- Kind:` line is a hard `backlog.kind-invalid` error TODAY. Child 01 is explicitly a pure rename, so after it lands backlog still REQUIRES the field while plans and specs will not. An executor taking the old criterion literally would either paste evidence it cannot obtain or "fix" the mismatch by dropping backlog's requirement, silently weakening a live contract on 87 records. The Set delivers ONE NAME across three types, not one requiredness. | measured at `be49ac4`: an item with no `- Kind:` gives `backlog.kind-invalid: kind not in ['bug','chore','feature','followup','security']: None` (exit 1); `backlog.py:178` validates unconditionally; `ipd_schema.META_PRIORITY` is in `META_RECOGNIZED` and NOT `META_REQUIRED`, which is the OPTIONAL shape only the new carriers get |
| F8 | MED | `backlog.py:178`; `check_engine.py:143` | The Set claimed "`aw check` flags an out-of-vocabulary value on each type" through one rule. Two DIFFERENT mechanisms are involved and the criterion must say so: backlog reports through its own contract-drift path (`backlog.kind-invalid`, surfaced by `aw backlog check` and `aw check backlog`), while plans and specs will report through the new registry rule `check.work-kind-invalid` that child 02 adds. Nothing makes backlog route through the registry, and no child is scoped to change that. Left unstated, an executor could either believe one rule covers all three or migrate backlog's validation into the registry, which is out of both children's fences. | `backlog.py:178` emits `core.Drift(..., "backlog.kind-invalid", ...)`; the registry pattern child 02 copies is `"check.priority-invalid": RuleSpec(...)` at `check_engine.py:143` |
| F9 | MED | `oc_runipd.py:2722-2758` | AN ORCHESTRATOR'S E-01 IS NOT SELF-EXECUTING, and this Set is the shape most likely to hit it. When the runner processes an orchestrator whose children all reached `executed`, it calls `finalize_orchestrator`, which shells `aw ipd set executed` with NO agent turn. So the whole-Set verification E-01 describes gets performed only if a human or agent does it deliberately; a runner-driven Set can transition this plan while E-01's evidence was never gathered. The lint is the backstop (`pre-transition` refuses an unperformed `E-*` and an empty `Observed evidence`, verified), which is exactly why V-01 must stay evidence-hungry and must not be softened. Recorded so the verification is understood as a manual obligation rather than an automatic rollup. | `oc_runipd.py:2722` dispatches `action == "orchestrate"` to `finalize_orchestrator` (`:322-345`, "no agent turn") rather than `execute_item`; `aw ipd lint --phase pre-transition` on this plan returns `IPD-S404 E-01: not 'performed'` plus `V-01: empty Observed evidence` |
| F10 | MED | `command_surface.py:946` | A MEASURED EXCEPTION TO THE PRECEDENT CHILD 02 REASONS FROM. Child 02 correctly establishes that `--priority` is UNDECLARED in `legacy_flags` for `ipd set` and `specs set`, and concludes `command_surface.py` stays out of scope. That holds for child 02. It does NOT transfer to child 01: `backlog new` DOES declare `--kind` in its `legacy_flags` tuple. So if child 01 adds a `--work-kind` spelling (OQ-01 says keep `--kind` as an alias), the declaration and the parser can diverge, and the divergence is on the one command where the flag IS declared. Cheap to handle, invisible if unstated. | `COMMAND_INVENTORY` entry for `backlog new` -> `('--summary','--set','--status','--priority','--kind','--slug','--gate-kind','--gate-ref','--blocks-release','--message','--body','--apply')`; `ipd set` -> no `--priority`; `specs set` -> no `--priority` |
| F11 | HIGH | `.aw/records/backlog/` | THE 88 IS WRONG AND IT IS LOAD-BEARING, because both children make it a pass/fail count. `grep -rl '^- Kind:'` over the backlog tree returns 88 FILES, but 2 of those are not items the parser reads the same way: `README.md` (the human-facing field list, which the parser SKIPS by name) and one legacy-named `...-e06-scenario-token-test-bindings.md` (no `.backlog.md` suffix, which the parser DOES read because it globs `*.md`). So the real figures are 86 `.backlog.md` items + 1 legacy-named item = 87 parsed items carrying the field, plus a README that must be updated as DOCUMENTATION rather than migrated as data. The same error inflates `Gate-Kind` from its true 1 item to 2. A child asserting "88 carry the new spelling and 0 carry the old" will FAIL its own validation, because the honest post-migration state is 87 items migrated and a README edited. | measured at `be49ac4`: `grep -rl '^- Kind:' .aw/records/backlog/` = 88; `find .aw/records/backlog -name '*.backlog.md'` = 86, all carrying the field; the 2 non-`.backlog.md` hits are `README.md` and `done/20260817-awphysical-01-xd78mr-e06-scenario-token-test-bindings.md`; `backlog._iter_items` globs `*.md` and excludes only `README.md`, returning 87; `grep -rl '^- Gate-Kind:'` = 2 of which one is `README.md`, so the ITEM count is 1 |
| F12 | HIGH | `.aw/records/backlog/` | BOTH CHILDREN REQUIRE A GREEN `aw backlog check` THAT IS ALREADY RED, so as written each child's validation is unachievable through no fault of its own. The tree carries 3 pre-existing `backlog.summary-unsafe` violations unrelated to this Set. Child 01 demands a clean run at three points (before, during, after) as the whole justification for its dual-read window; that requirement can never be met and an executor would be pushed either to fabricate a pass or to "fix" three items outside its fence. The requirement must become NO-WORSENING against a freshly measured baseline, with the work-nature findings isolated by rule id. This is the same defect class the earlier review caught in child 02's CLI-conformance requirement (F9 there), recurring on a different command. | measured at `be49ac4`: `aw backlog check` exits 1 with `backlog.summary-unsafe` on `20260819-awagyfalseerror-01-uhbdt1`, `20260820-awhistignore-01-f7w55w`, and `20260820-awinstallfix-01-av9hni`, reporting `3 violation(s)` |
| F13 | MED | `run_evidence.dirty_within`; `ipd_lifecycle._scope_match` | This orchestrator declared `Scope-Paths: .aw/records/plans/pending`, a whole SHARED DIRECTORY in a checkout where other sessions are authoring plans concurrently. That entry is directory-bounded, so it matches EVERY file under `pending/`, which has two consequences: `aw ipd begin` refuses to issue a receipt when ANY other agent has an uncommitted plan in `pending/` (the fence is supposed to ignore disjoint work, and this defeats that), and finalize's scope reconciliation would treat another agent's plan as in-scope. Narrowed to this plan's own file; the plan file's own lifecycle move is implicitly allowed. | `dirty_within('.', ['.aw/records/plans/pending'], _scope_match)` returns another agent's file as offending, while the same call with this plan's own path returns `clean`; `_scope_match` treats a bare directory as covering its tree; `scope_paths_implicit_allowances()` = `('.aw/records/plans/**', '.aw/records/plans/INDEX.md', '.aw/records/**/index.md')` |

## Proposed changes (ordered, validatable)

1. Retire `a6cej0` as superseded by this Set (done as part of creating it).
2. Child 01: rename in code, then migrate 88 items behind a dual-read window.
3. Child 02: add the field to the IPD schema and spec contract, with setter and check rule.
4. Verify the whole Set: one name, one vocabulary, optional everywhere, `Gate-Kind` intact (E-01).

## Deferred / out of scope (with reason)

- Adding the field to RESEARCH. A research document's nature is already carried by its mandatory document-type facet drawn from an 18-member vocabulary, so a second kind axis there would confuse rather than clarify. This is where this Set deliberately differs from `xprio`, which did include research because priority is orthogonal to document type.
- DERIVING the value from a plan's `From-Backlog` link. Decided against in `a6cej0` and unchanged: a derived value is undefined for the majority of artifacts that carry no such link, and would let a plan's classification change silently when someone edits the source item. Because the field is optional, a later derivation pass can populate absent values without contradicting this Set.
- ATTENTION-BOARD wiring, labelling, or sort changes, and cross-tree FILTERING. Per F6 nothing consumes the field yet; delivering search is separate work.
- BACKFILLING the field onto existing plans and specs. Absent means unclassified by design.

## Scope check

- Over-scope: none. This orchestrator writes only its own record and declares no product path, which is correct for an orchestrator that authors no code. Its `Scope-Paths` names ONLY its own file: the previous value was the whole shared `.aw/records/plans/pending` directory, which is directory-bounded and would have made another agent's uncommitted plan block `aw ipd begin` and appear in finalize's scope reconciliation (F13).
- Under-scope: none. Both children exist, are authored, and are listed with their dependency; unlike the `rununify` orchestrator this Set's seams were already known, so nothing is deferred to a measurement step.
- The children declare the product paths: child 01 takes `backlog.py` plus the backlog records tree; child 02 takes the schema, spec contract, check engine, CLI, setter module, and its test file.

## Required tests / validation

- Each child must pass its own validation independently, since the point of the split is that they are separately verifiable. Child 01's evidence stands without child 02 existing.
- `aw check` and `aw backlog check` must be NO WORSE after EACH child than a freshly measured baseline, not only at the end of the Set. NOT "clean": `aw backlog check` is ALREADY RED at 3 pre-existing `backlog.summary-unsafe` violations unrelated to this Set (F12), so a clean-run requirement is unachievable and would push an executor to fabricate a pass or to edit items outside its fence. Measure before, compare after, and isolate work-nature findings by RULE ID (`backlog.kind-invalid`, `check.work-kind-invalid`) rather than by total count.
- INVOKE THE SUITE BARE: `python3 -m pytest` and `python3 -m pytest -m ""` (or `make test` / `make test-all`). Do NOT add `-n auto` or a second `-q`. The two are NOT interchangeable: the bare run carries `-m 'not slow'`, so `slow`-marked modules (where the CLI-surface conformance tests live) are SKIPPED entirely by it.
- BASELINE IS A MEASUREMENT, NOT A CONSTANT. Fast suite at `be49ac4` during this review: `2927 passed, 3 skipped, 4 xfailed`. The earlier `2880` reading at `df731f1` is stale, which is the point: take your own readings with the HEAD they were taken at, because several sessions are committing concurrently.
- `aw check-local-leaks . --agent` clean; `aw ipd lint --phase pre-transition` conforming for this plan and both children.

## Spec / documentation sync

- No spec text change is forced. This Set follows the already-executed `Priority` pattern and changes no contract a spec pins.
- Child 01 owns updating any documentation that names backlog's old field spelling, since leaving a doc describing the old name reintroduces the confusion the rename removes.
- Child 02 should check whether the `ipd-spec` document enumerates recognized metadata fields and whether `Priority` was added there when `xprio` landed, then follow that precedent exactly rather than inventing a new documentation obligation.

## Open questions

### OQ-01: Should child 02 wait for child 01, or can they run in parallel?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: SEQUENTIAL, child 01 first. Child 02 declares `executed:9trlc3`. The reason is not a code dependency, since the two touch disjoint modules, but a naming one: child 02 teaches plans and specs to recognize `Work-Kind`, and if it landed while backlog still wrote `Kind`, the tree would briefly carry two spellings for one concept, which is exactly the confusion this Set removes. Running the rename first also front-loads the riskiest work, so a migration conflict surfaces before any feature work is invested.

### OQ-02: Does this Set unify the field's REQUIREDNESS as well as its name?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: NO, IT UNIFIES THE NAME ONLY, and the requiredness asymmetry is a deliberate outcome rather than an unfinished edge. Raised at review because this plan's original completion criteria asserted the field would be "optional on all three so an artifact without it still validates", which is FALSE for backlog: it validates the field on read and rejects an absent value today (F7). Resolved from repository evidence and from the Set's own stated scope rather than by asking, on three grounds. First, the maintainer's recorded decision and this plan's title are about the NAME ("one work-nature field name"); requiredness was never in question. Second, child 01 is scoped as a PURE RENAME, so preserving backlog's existing requirement is the only behavior consistent with its own fence. Third, the alternative, making the field optional on backlog to force symmetry, would weaken a live validation contract across 87 records to satisfy a sentence in a plan, which inverts the proper order. The corrected criteria therefore state the asymmetry explicitly: OPTIONAL on the two new carriers, REQUIRED on backlog, one shared name and one shared vocabulary throughout. Because the field is optional on plans and specs, a future decision to require it there, or to relax it on backlog, remains open and is not foreclosed by this Set.

## Validation and cross-check (verify before reporting the Set complete)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: paste a backlog item, a plan, and a spec each carrying `- Work-Kind:` and validating. Paste a grep for the vocabulary member tokens showing exactly ONE definition in `agent_workflows/`. For the OPTIONALITY asymmetry (F7), paste BOTH halves and do not conflate them: a PLAN and a SPEC with NO `Work-Kind` each validating, AND a backlog item with no `Work-Kind` still being REJECTED as `backlog.kind-invalid`, which is the unchanged, intended behavior. Paste an out-of-vocabulary value being reported on each of the three types, labeling WHICH mechanism reported it (`backlog.kind-invalid` for backlog, `check.work-kind-invalid` for the plan and the spec, F8). Paste the `- Gate-Kind:` ITEM count still at 1 with that item parsing, and state the README exclusion so the number is not mistaken for the tree-wide grep's 2 (F6/F11). Paste the migrated ITEM count as 87 parsed items (86 `.backlog.md` plus the 1 legacy-named item) and show `.aw/records/backlog/README.md` updated as documentation rather than counted as data. Paste `aw backlog check` and `aw check` before and after with their HEADs, showing NO WORSENING against your own freshly measured baseline, not a clean run (F12: 3 pre-existing `backlog.summary-unsafe` violations at `be49ac4`). Paste the suite result with its HEAD from your own measurement. Confirm in writing that E-01 was performed as a deliberate manual pass and not left to the runner's orchestrator rollup, which performs no agent turn (F9). Finally, state in writing that cross-tree filtering is NOT delivered by this Set; claiming otherwise fails this item.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required. 1 E-leaf in 1 task group, which is correct for an orchestrator that authors no code and verifies the Set. The SET's right-sizing is enforced through the two children, each of which is one concern with its own falsifiable surface.

Open questions: ALL RESOLVED. The four decisions this Set implements were made by the maintainer and are recorded in the superseded `a6cej0` and carried into the children: one shared vocabulary rather than a per-type set; store the value rather than derive it; rename backlog's field first; and recognized-but-optional mechanics following the `Priority` precedent, which apply to the two NEW carriers. OQ-01 fixes the child order. OQ-02 records that this Set unifies the field's NAME only, not its requiredness (backlog keeps requiring it), which corrects a false criterion this plan carried.

Scope fence: this plan writes only its own record. Author NO product code and touch neither child's files. Both children already exist, so if executing this orchestrator seems to require writing one, STOP and report.

Honesty rule (HARD MUST): when you report tests or validation passed, paste the ACTUAL runner output with the `git rev-parse HEAD` it was measured at. Specific to this Set: do NOT describe the outcome as enabling cross-tree filtering or search (F6).

Execution contract: commit ONLY the files changed, path-scoped (`git commit -m msg -- <paths>`), never `git add -A`, and never push. Before every commit run `git diff --cached --name-only` and unstage anything not modified by this plan. Several sessions are committing to this checkout concurrently and at least one had unrelated files STAGED while this Set was authored, so verify the staged set rather than trusting a path-scoped command.

Post-gate lifecycle: do not claim done or move this plan until every `V-*` item is verified with concrete pasted evidence and `aw ipd lint --phase pre-transition` reports conforming; the transition is performed by `aw ipd finalize`, never by hand. Do not create or push a git tag or release.
