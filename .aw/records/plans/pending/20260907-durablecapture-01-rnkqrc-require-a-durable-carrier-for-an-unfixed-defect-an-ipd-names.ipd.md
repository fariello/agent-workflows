# IPD: Require a durable carrier for an unfixed defect an IPD names before it may reach executed

- Date: 2026-09-07
- Kind: child
- Concern: In the maintainer's words (2026-09-05): "A note in an executed IPD is 100% guaranteed to be the same as not writing it anywhere." That is literally true. Once a plan reaches `executed`, `aw attention` classes it `done`, so every unfixed defect named in its prose disappears from every "what needs attention" view, permanently, silently, with no record that anything was dropped. Walkthroughs are worse: they are `tracked=False` by design and get filename-only checking, so a walkthrough may describe any number of live defects and no tool will ever read a word of it.
  TWO REQUIRED IPD MECHANISMS ARE DEAD CODE, both verified at HEAD. (1) `## Deferred / out of scope (with reason)` is MANDATORY in both `CHILD_H2_ORDER` and `ORCHESTRATOR_H2_ORDER` (`ipd_schema.py:48` declares `H_DEFERRED`), and `H_DEFERRED` appears ZERO times in `ipd_lint.py` (`grep -c` = 0). The lint enforces that the section exists and is ordered, and never reads its contents, so every "deferred to its own IPD" row in the entire corpus is unverified prose. (2) The blocking-open-question gate runs at `pre-execution` ONLY, never at `pre-transition`, so a blocking question gates the START of work and not the CLAIM THAT IT IS DONE. And an OQ is "resolved" by any non-empty prose: `open_question_error` (`ipd_schema.py:1338-1354`) checks only that a rationale is non-empty, with its own docstring stating "Semantics are the reviewer's job". `ipd_lint.py` contains ZERO occurrences of `From-Backlog` (`grep -c` = 0): the linter has no concept of backlog linkage at all.
  THE PATTERN TO GENERALIZE ALREADY EXISTS AND WORKS. `evaluate_blocking_close` (`check_engine.py:1951`, verdict type `CloseVerdict` at `:1934`) is the only mechanism here that enforces "an obligation must survive in a place that is revisited": closing a backlog item carrying `Blocks-Release:` FAILS CLOSED unless (1) HANDOFF to a plan carrying `From-Backlog:` and the same gate, (2) SATISFIED with a resolvable citation, or (3) DE-GATED explicitly. One shared predicate backs the setter, the `aw check` rules, and an opt-in hook, "so they cannot diverge". This plan applies that exact shape to an IPD's terminal transition.
- Scope: Add ONE shared predicate that refuses an IPD's transition to `executed` while it names an unfixed defect with no durable carrier, with the same three escapes `evaluate_blocking_close` already uses (handoff, satisfied, declined-with-reason). Wire it into `aw ipd lint --phase pre-transition` and `aw check` from that single predicate. Read TYPED fields only, never prose. Ship warning-for-existing / error-for-new so a 530-plan corpus is not mass-failed on day one.
- Scope-Paths: agent_workflows/check_engine.py, agent_workflows/ipd_lint.py, agent_workflows/ipd_schema.py, tests/test_durable_capture.py
- Item-Dependencies: executed:b7xarm
- Status: approved
- Readiness: go-pending-approval
- Set: durablecapture
- Order: 1
- Highest E allocated: 06
- Author: opencode its_direct/pt3-claude-opus-5-1m-us
- Id: rnkqrc
- Approval: 2026-09-13, recorded via aw ipd set: status set to approved
- From-Backlog: jys5dp
- Blocks-Release: next

## Workflow history
- 2026-09-18 executed-pending-transition (opencode its_direct/pt3-claude-opus-5-1m-us): ALL SIX E-ITEMS PERFORMED AND ALL SIX V-ITEMS VERIFIED with pasted evidence, at HEAD `94345381` in lane `aw/lane/rnkqrc` under run `run-20260918T045900Z-2565492`. `aw ipd lint --phase pre-transition` reports CONFORMING. THE TERMINAL MOVE IS NOT MINE TO MAKE: this process is worker-role (`AW_EXECUTION_ROLE=worker`), and `ipd_lifecycle` refuses begin/finalize for a worker with `AW-LIFECYCLE-ROLE-001` because the runner owns the transition for a managed lane, so the plan is deliberately left in `pending/` with `- Status: approved` for the runner to finalize. Nothing was hand-moved and no terminal state was hand-written.
  WHAT SHIPPED: three typed carrier fields (`Carrier`/`Carrier-Evidence`/`Carrier-Declined`) declared beside the `Finding` precedent in `ipd_schema.OQ_FIELDS`; ONE shared predicate (`check_engine.evaluate_carrier_obligation` + `evaluate_durable_carrier`) returning `CloseVerdict` ITSELF with the three proven escapes, resolving carriers against the real trees and refusing a dangling, terminal, or spec-only reference; wired to `pre-transition` from `lint_file` (NOT the pure `lint_text`) and to `aw check` under the registered rule id `check.ipd-uncarried-obligation` (`error`/repository/deterministic/I-07), both calling that ONE predicate; and 62 tests in `tests/test_durable_capture.py`, all on tmp_path fixtures.
  THREE DECISIONS WERE TAKEN AUTONOMOUSLY and are recorded with evidence in the run's `decisions-and-questions.md`. (D1) The typed signal is that EVERY obligation-bearing row must carry one of the three fields, which is the only reading that satisfies both E-01's ban on prose matching and E-06's mandatory prose-only REFUSAL, and it matches E-05's own per-ROW arithmetic. (D2) The grandfathered tier is `info`, NOT the `warning` E-05 words, because `artifact_core.drift_exit_code` exempts only `info`, so `warning` would have failed CI with 106 findings on a clean tree, which E-05 itself forbids; the BEHAVIOR specified is delivered exactly and the registered severity remains `error`. (D3) Boundary option (c): a new module constant `CARRIER_CUTOVER_DATE = 20260919` compared against the plan's own `- Date:`, following the `SPEC_ID6_CUTOVER_DATE` precedent, because F-11's measurement that `config.dependency_cutover_date` is `None` here still holds and reuse would have left the error tier unreachable.
  BLAST RADIUS, MEASURED BEFORE AND AFTER: 105 pending plans report a NON-FAILING `info` finding covering 745 uncarried rows, and `drift_exit_code` is 0 for the rule's whole contribution. The bare suite went `31 failed, 8003 passed` -> `31 failed, 8065 passed` (+62, exactly the new tests) with the failure set IDENTICAL by node id; those 31 are the pre-existing lane-role defect approved plan `e4lkv5` owns, reproduced on both sides and not caused here.
  THE PLAN NOW DOGFOODS ITS OWN RULE, which is the pleasing recursion its approval gate predicted: all six of its `## Deferred / out of scope` rows carry a typed `- Carrier:`, so it produces ZERO findings against the rule it adds. It was grandfathered and did not have to. Five backlog items were filed this turn as the durable carriers for its deferred rows and for the gaps found while executing (`rtyapw` the 745-row migration debt, `dtrect` missing authoring support, `3yr30q` walkthroughs, `wuooks` code comments, `msi8dx` the reviews TreePolicy, `e85snf` the `aw check backlog` posture).
- 2026-09-13 approved (aw set): status set to approved
- 2026-09-10 readiness re-check (opencode its_direct/pt3-claude-opus-5-1m-us): `- Readiness:` CHANGED `no-go` -> `go-pending-approval`. THIS IS A RE-CHECK, NOT A REVIEW: no finding was re-derived and no plan content was re-critiqued. The three `no-go` conditions were RECOMPUTED with the shipped predicates and each was found clear: `plan_readiness.has_unresolved_blocking_question` -> False; `review_findings.subject_gating_blocks` -> empty; `plan_readiness.newest_verdict` polarity -> neutral (not negative). Specifically, its blocking OQ-05 was answered on 2026-09-10 (a carrier is required, the run-record report is not) and the finding it escalated, PR-301, is now closed in review round 2. Performed at HEAD `5692797e` at the maintainer's explicit instruction of 2026-09-10, who was shown that 12 of 15 `no-go` plans were held by stale bookkeeping and chose to have them fixed with evidence recorded rather than re-reviewed. This is the SECOND such cleanup in one session; the durable fix is plan `qhy3i3` E-07, authored and awaiting approval. HUMAN APPROVAL IS STILL REQUIRED AND WAS NOT GIVEN: `go-pending-approval` means the plan awaits sign-off, and nothing here approves it or clears it to execute. Only a review may set `go`.
- 2026-09-10 reviewed (aw set): plan-review round 1: REVIEWED - OPEN QUESTIONS; PR-301..PR-310, nine FIXED and PR-301 escalated as blocking OQ-05; Readiness no-go pending that answer
- 2026-09-10 reviewed (opencode its_direct/pt3-claude-opus-5-1m-us): /plan-review round 1: REVIEWED - OPEN QUESTIONS; PR-301..PR-310, nine FIXED and PR-301 escalated as blocking OQ-05; Readiness `no-go` pending that answer. Reviewed at HEAD `32948d10`; `aw ipd lint --phase author` CONFORMED (0 findings) before semantic review and `--phase review-finalize` after. DISCLOSURE: same model family as the author, so this is near-self-review; its value is in what was EXECUTED.
  EVERY DEAD-CODE CLAIM IS TRUE AND I RE-RAN ALL OF THEM. `rg -c H_DEFERRED agent_workflows/ipd_lint.py` exits 1 (zero matches) while `ipd_schema.py:48` declares it and `:70`/`:86` put it in BOTH H2 orders; `rg -c From-Backlog agent_workflows/ipd_lint.py` exits 1; `open_question_error` (`ipd_schema.py:1340-1354`) really does accept any non-empty rationale and really does say "Semantics are the reviewer's job"; `OQ_FIELDS` (`:1315-1329`) really is declared-and-never-read with that fact written in the comment; and `attention.py:1246` really maps `executed` -> `done`. The precedents are real and correctly identified, though both citations drifted: `CloseVerdict` is at `:1963` (not `:1934`) and `evaluate_blocking_close` at `:1980` (not `:1951`). This is careful, well-sourced work and I weakened none of it.
  THE FINDING THAT DECIDES WHETHER THE PLAN IS EXECUTABLE AS SCOPED IS PR-301 (BLOCKER), AND THE SIBLING'S OWN REVIEW FOUND IT FIRST. This plan's history instructs E-02 to "read the NORMALIZED report `b7xarm` E-06 persists on the run record". That record lives under `.aw/records/runs/`, which is GITIGNORED with ZERO tracked files, and NONE of the four declared `Scope-Paths` can reach it: `rg 'records/runs|state_root|run_dir'` over `check_engine.py`, `ipd_lint.py` and `ipd_schema.py` returns nothing. `b7xarm`'s round-1 review already recorded this as its PR-A05 ("the handoff to rnkqrc is not reachable as scoped"). So the instruction and the declaration contradict each other, and the resolution (add a reader plus its scope path, or drop the run-record read and gate on plan-file fields only) changes what this plan builds. Escalated as OQ-05 rather than decided.
  THE PLAN CONTRADICTS ITSELF ABOUT ITS OWN DEPENDENCY, AND THE FRONT MATTER IS THE CORRECT HALF (PR-302). Front matter declares `Item-Dependencies: executed:b7xarm`; OQ-04's prose says "this plan now declares `Item-Dependencies: to-review:b7xarm`". Measured: `parse_item_dependencies('to-review:b7xarm')` REJECTS the token ("expected executed:/exists:/state:"), so the prose describes an illegal edge that would have failed the dependency linter had anyone copied it. `executed:b7xarm` parses correctly and is also the right semantics, since a gate with nothing to verify is exactly what the maintainer's ruling forbids. Prose corrected to match the field.
  THE BLAST RADIUS IS 15% LARGER THAN F-6 STATES AND THE PLAN MEASURED THE WRONG DENOMINATOR (PR-303). F-6 says 530 plans; measured 608 (470 executed, 104 pending, 30 superseded, 4 not-executed). More importantly the number that matters is not the plan count but the ROW count: 476 plans carry at least one `## Deferred / out of scope` bullet, totaling 1929 rows, and ALL 104 pending plans carry rows (714 of them). Zero plans anywhere carry a typed carrier field. So a per-row error rule would fire 714 times on the pending tree alone, which is what makes E-05's staged severity load-bearing rather than cautious.
  E-03 CANNOT BE IMPLEMENTED WHERE IT SAYS, FOR A DOCUMENTED REASON (PR-304). The `pre-transition` checks live in `check_checkpoint` (`ipd_lint.py:754`), called from `lint_text` (`:1066`), and `lint_text` is PURE BY DOCUMENTED CONTRACT ("Pure: no I/O", `:1040`). A predicate that RESOLVES a carrier against the backlog and plan trees performs I/O, so it cannot go there. The established seam is `lint_file`, and the repository has done this twice already: the Item-Dependencies resolution checks and `_merge_review_escalation` (`:1173-1191`) both moved to `lint_file` with the reason written down. E-03 now names that seam and its consequence (a text-only `lint_text` call cannot report the rule, which is intended and asserted by the existing tests).
  Also corrected: the `dependency_schema_cutover` marker E-05 proposes to reuse currently resolves to `None` in this repository, so "reuse it" means reading an ABSENT marker that grandfathers everything and would leave the error tier unreachable until someone sets it (PR-305); OQ-04 is marked `Blocking: yes` with `Status: resolved`, a combination the linter permits but which contradicts the plan's own gate paragraph claiming "OQ-01 is open but Blocking: no" when OQ-01 is resolved and OQ-04 is the blocking one (PR-306); the two precedent citations drifted (PR-307); and the required-tests baseline cites a stale `~14 test_run_viewer failures` attribution that measures 46 passing today (PR-308).
- 2026-09-09 to-review (aw set): MAINTAINER RULING 2026-09-08 (recorded as OQ-04): the runner CANNOT detect an unfixed defect, and this plan never could; it is a DECLARATION gate whose predicate only checks that a typed field exists, resolves, and points somewhere non-terminal. The maintainer challenged the premise directly and ruled that the declaration must be PRODUCED before the check that verifies it is built: an execute turn must report found bugs/gaps/concerns both affirmatively and negatively in a parsable form, and the runner must ask the agent (in the same session) when neither statement exists. Plan b7xarm (defreport-01) was authored to produce exactly that report, so this edge is a real prerequisite rather than a preference: executed before b7xarm, this gate ships with nothing to verify (F-6 already measures the day-one effect, 530 plans and ZERO carrying the field). E-02's predicate should additionally read the NORMALIZED report b7xarm E-06 persists on the run record, since that record is what distinguishes 'found nothing' from 'never asked'. ALSO RESOLVED in the same ruling (OQ-01): the accepted carrier set is a backlog item or a pending plan ONLY; a spec is never sufficient and is supporting material, with no just-a-spec judgement delegated to the agent.

- 2026-09-08 to-review (opencode its_direct/pt3-claude-opus-5-1m-us): Graduated from backlog `jys5dp`. Every dead-code claim re-verified by running the greps rather than trusting the item: `H_DEFERRED` in `ipd_lint.py` = 0, `From-Backlog` in `ipd_lint.py` = 0, and `open_question_error`'s rationale check is indeed non-emptiness only (`ipd_schema.py:1338-1354`). The corpus size was re-counted at 530 plans (the item said ~484), which strengthens rather than weakens the rollout caution, so the staged severity is carried into E-05 as a hard requirement rather than advice.

## Goal

An unfixed defect an IPD names cannot vanish when that IPD reaches `executed`: it must first be handed to a carrier something revisits, satisfied with cited evidence, or explicitly declined with a reason.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: define the carrier reference

- [x] E-01 ADD A TYPED CARRIER FIELD TO THE OQ AND DEFERRED VOCABULARY in `ipd_schema.py`, so a carrier reference is matched STRUCTURALLY and never by prose. Follow the `Finding` precedent exactly: `Finding` was added to `OQ_FIELDS` (`ipd_schema.py:1329-1335`) as the typed field `check.review-finding-unescalated` matches, with the schema comment recording WHY prose matching was rejected ("a substring search over prose would be spoofable by any incidental mention and brittle against rewording").
  NOTE `OQ_FIELDS` IS CURRENTLY UNREAD, and the schema says so in as many words (`:1315-1318`: it "has NO consumer anywhere in the codebase (it is defined here and never read)", the parser is "deliberately OPEN-ENDED and carries any subfield into the parsed dict"). So adding a field there is necessary but NOT sufficient: this plan's predicate is what finally gives it a consumer. Also honor the standing warning at `:1327-1328`: if `OQ_FIELDS` ever becomes a CLOSED allowlist, both `Finding` and this new field must stay in it.
  DEFINE THE FIELD FOR THE DEFERRED SECTION TOO, since that section is the higher-value half (every "deferred to its own IPD" row in the corpus is currently unverified prose). One vocabulary serving both places, not two.
  - Depends on: none
  - Expected outcome: one typed carrier field declared in the schema, usable in an OQ block and in a `## Deferred / out of scope` row, with the anti-prose rationale recorded next to the `Finding` precedent.
  - Execution state: performed

### Task group 2: one predicate, three escapes

- [x] E-02 WRITE THE SHARED PREDICATE in `check_engine.py`, MIRRORING `evaluate_blocking_close` (`:1951`) rather than inventing a second shape. Return a verdict type in the shape of `CloseVerdict` (`:1934`) so callers get a structured answer, not a boolean. The three escapes must be exactly the proven ones:
  (1) HANDOFF - the named carrier resolves to a `backlog/open/` item or a plan NOT in a terminal directory (i.e. something `aw attention` actually revisits);
  (2) SATISFIED - a resolvable in-tree evidence citation, the same shape `aw backlog set done --evidence` already accepts;
  (3) DECLINED - an explicit recorded decision with a reason, which is the `--blocks-release -` analogue.
  RESOLVE THE CARRIER, DO NOT MERELY PARSE IT. A dangling id6 must FAIL, exactly as `check.from-backlog-dangling` already fails a `From-Backlog` pointing at nothing. A carrier reference that resolves to a plan in `executed/` is NOT a valid handoff, because that is the very hiding place this plan exists to close; assert that case explicitly.
  MIRROR THE VERDICT SHAPE EXACTLY, AND NOTE THE CITATIONS DRIFTED (review, F-9): `CloseVerdict` is at `check_engine.py:1963` and `evaluate_blocking_close` at `:1980` (this plan said `:1934`/`:1951`). `CloseVerdict`'s five fields are `legitimate: bool`, `severity: str` (`'ok'|'warn'|'error'`), `reason: str`, `fixes: Tuple[str, ...]`, `path: Optional[str]` (`HANDOFF|SATISFIED|DE-GATED|None`). Reuse that shape including `fixes`, because E-05's staged severity needs the `warn`/`error` distinction the type already carries and the operator-facing message needs `fixes`; do not invent a fourth verdict type.
  WHETHER THIS PREDICATE READS THE RUN RECORD IS GATED ON OQ-05 (blocking). The plan's history instructs it to read the normalized report `b7xarm` E-06 persists under `.aw/records/runs/`, but that tree is GITIGNORED with zero tracked files and NONE of this plan's four declared `Scope-Paths` reads it (measured: `rg 'records/runs|state_root|run_dir'` over the three production modules returns nothing). `b7xarm`'s own review already recorded this as its PR-A05. Do not resolve it by quietly adding an undeclared reader; see OQ-05.
  NEVER RAISE. Follow the parse-then-diagnose split the sibling evaluators use: a malformed row becomes a finding and surrounding good rows still parse. A gate that crashes on bad input is a gate that gets disabled.
  - Depends on: E-01
  - Gated on: OQ-05 (blocking; whether the run-record read is in scope). Stated here because `Depends on` may hold only `E-*` ids.
  - Expected outcome: one predicate returning a `CloseVerdict`-shaped verdict, with three escapes, resolving carriers against the real trees, refusing a terminal-directory carrier, and never raising.
  - Execution state: performed

- [x] E-03 WIRE `pre-transition` TO THE PREDICATE in `ipd_lint.py`, which is the gate that currently cannot see any of this. The blocking-OQ check runs at `pre-execution` only and `pre-transition` checks solely E-item state, V-item results, and evidence presence, so "is this claim of doneness honest about what it leaves behind?" is asked nowhere. Add the predicate's verdict to the `pre-transition` phase.
  WIRE IT INTO `lint_file`, NOT `check_checkpoint`, AND THIS IS A CORRECTNESS CONSTRAINT RATHER THAN A PREFERENCE (review, F-10). The `pre-transition` checks live in `check_checkpoint` (`ipd_lint.py:754`), which is called from `lint_text` (`:1066`), and `lint_text` is PURE BY DOCUMENTED CONTRACT ("Pure: no I/O", `:1040`). This predicate RESOLVES a carrier against the backlog and plan trees, which is I/O, so it cannot live there. The repository has already made this exact move twice with the reason recorded: the Item-Dependencies resolution checks and `_merge_review_escalation` (`:1173-1191`, whose docstring says the placement "is load-bearing" for precisely this reason) both sit in `lint_file`. Follow `_merge_review_escalation`'s shape: gate on a checkpoint frozenset, skip legacy/quarantined dispositions, reuse the ALREADY-PARSED `doc` rather than re-parsing, and delegate to the one shared evaluator.
  ACCEPT AND STATE THE CONSEQUENCE: a text-only `lint_text` call CANNOT report this rule, exactly as it cannot report the review-escalation rule today. That is intended and is asserted by the existing tests; say so in the plan's output so a later reader does not file it as a bug.
  DO NOT MOVE OR WIDEN THE EXISTING `pre-execution` BLOCKING-OQ CHECK. Its exclusion from `pre-transition` is deliberate and documented; this item ADDS a different question at that phase rather than relocating an existing one. Two gates, two questions. NOTE THE ASYMMETRY WORTH RESPECTING: `_REVIEW_ESCALATION_CHECKPOINTS` (`:1170`) deliberately EXCLUDES `pre-transition` because "blocking there would only strand a completed plan". This plan's rule deliberately does the opposite, and that is defensible (its whole point is that the transition is the moment the obligation would vanish), but the tension must be stated in a comment beside the new checkpoint set so a later reader does not "fix" one to match the other.
  - Depends on: E-02
  - Expected outcome: `aw ipd lint --phase pre-transition` reports the new rule from `lint_file`; `lint_text` stays pure and provably cannot report it; the `pre-execution` blocking-OQ behavior is byte-identical to before; the deliberate divergence from `_REVIEW_ESCALATION_CHECKPOINTS` is documented in place.
  - Execution state: performed

- [x] E-04 WIRE `aw check` TO THE SAME PREDICATE, with a rule id registered in `RULE_REGISTRY`, so the sweep and the checkpoint cannot drift apart. That single-predicate property is stated as the design intent of the nearest precedent (`evaluate_review_finding_escalation`, `check_engine.py:3116`, whose docstring calls itself "The ONE evaluator ... shared by both host surfaces"), and this plan must not weaken it.
  CI ALREADY ENFORCES `aw check plans` FAIL-CLOSED, so a new plans rule inherits enforcement with no workflow edit. Note that `check backlog` is ADVISORY there; do NOT change that as a side effect of this plan, and say so in the report if it seems tempting.
  - Depends on: E-03
  - Expected outcome: one rule id in `RULE_REGISTRY` backed by the SAME predicate `pre-transition` calls; `aw check` and `aw ipd lint` provably cannot disagree.
  - Execution state: performed

### Task group 3: roll it out without mass-failing 530 plans

- [x] E-05 SHIP STAGED SEVERITY: `warning` for existing plans, `error` for newly authored ones. THIS IS MANDATORY, NOT A PREFERENCE. When zero existing artifacts satisfy a new requirement, a fail-closed rule blocks everything on day one; `check.review-finding-unescalated` chose SILENCE on an absent artifact for exactly this reason, recording that "zero .review.md files exist against 428 plan files, so a fail-closed absent case would mass-fail the entire corpus on day one".
  THE BLAST RADIUS IS BIGGER THAN THIS ITEM SAYS, RE-MEASURED AT REVIEW, AND THE RIGHT DENOMINATOR IS ROWS NOT PLANS (F-6 as corrected). 608 plans exist, not 530 (470 executed, 104 pending, 30 superseded, 4 not-executed). 476 of them carry at least one `## Deferred / out of scope` bullet row, 1929 rows in total. ALL 104 PENDING PLANS carry rows, 714 of them. Zero plans anywhere carry a typed carrier field. So a per-row error rule fires 714 times on the tree that is actually being worked, which is why staged severity is load-bearing and not caution.
  THE CUTOVER MECHANISM YOU ARE TOLD TO REUSE IS CURRENTLY UNSET, WHICH CHANGES WHAT "REUSE IT" MEANS (F-11). `check.ipd-missing-dependency-statement` reads `config.dependency_cutover_date` (`check_engine.py:2439`), and an ABSENT marker grandfathers every existing plan. Measured at review: `config.dependency_cutover_date(Path('.'))` returns `None` in this repository, so reusing that same marker would grandfather EVERYTHING and leave the `error` tier unreachable until someone sets a date, i.e. the rule would ship as warn-only forever and quietly satisfy its own test. DECIDE AND STATE which of these you do: (a) reuse the existing marker and accept warn-only until a date is set, saying so plainly rather than claiming staged severity works; (b) add a SEPARATE marker for this rule with its own default; or (c) gate on a per-plan signal that already exists (for example the plan's own `- Date:` compared against a constant) so no configuration is required. Option (c) needs no new config surface and is testable today; whichever you choose, V-05 must show an OLD plan warning AND a NEW plan erroring, which option (a) cannot demonstrate without setting the marker in the test.
  PROVE THE BLAST RADIUS BEFORE AND AFTER. Run `aw check` over the whole corpus and report the finding count at each severity, per ROW as well as per plan. A rule that turns 476 plans and 1929 rows red is not shippable no matter how correct it is.
  - Depends on: E-04
  - Expected outcome: existing plans WARN, new plans ERROR with that demonstrated rather than asserted, the boundary mechanism chosen from the three options above and STATED (including whether the reused marker is actually set), and the measured corpus-wide finding count reported per plan and per row.
  - Execution state: performed

- [x] E-06 TEST THE THREE ESCAPES, THE REFUSAL, AND THE ROLLOUT in a new `tests/test_durable_capture.py`. Cover: a plan naming an unfixed defect with NO carrier (refused at `pre-transition`); each of the three escapes accepted; a DANGLING carrier refused; a carrier resolving into `executed/` refused (the hiding place); a malformed row producing a finding rather than an exception; and the staged severity (an old plan warns, a new one errors).
  ASSERT `aw check` AND `pre-transition` AGREE, from the same fixtures, since a single predicate is the design property most likely to erode later. Assert both surfaces on the same inputs rather than testing one and trusting the other.
  ASSERT PROSE DOES NOT SATISFY THE RULE: a plan whose deferred row says "tracked in the backlog" with no typed field must still be refused. That is the spoofability `Finding` was designed against, and it is the most likely way this gate gets quietly defeated. NOTE THE CORPUS EVIDENCE THIS CASE REPRESENTS (F-6 as corrected): 1929 deferred rows exist today and every one is prose, several of them literally saying "tracked as `<id6>`", so the prose-only case is the NORMAL corpus shape rather than a contrived fixture.
  ASSERT `lint_text` CANNOT REPORT THE RULE, which is the corollary of E-03's placement (F-10): a text-only lint must be shown NOT to fire, exactly as it does not fire for the review-escalation rule. A test that got the rule from `lint_text` would prove the predicate was wired into the pure path, which is the defect E-03 forbids.
  USE FIXTURES, NEVER THE LIVE TREES. `.aw/records/runs/` is gitignored with ZERO tracked files, and `b7xarm`'s review records the same trap for its own tests. Build carrier targets (a `backlog/open/` item, a `plans/pending/` plan, a `plans/executed/` plan) inside a tmp_path so the refusal cases are reproducible in CI and in a lane worktree.
  - Depends on: E-05
  - Expected outcome: a test failing against pre-change HEAD, passing after, covering three escapes, four refusal cases, the malformed case, staged severity both ways, cross-surface agreement, and the `lint_text`-cannot-report corollary, all on tmp_path fixtures.
  - Execution state: performed

## Project conventions discovered (Step 0)

- `evaluate_blocking_close` (`check_engine.py:1951`) + `CloseVerdict` (`:1934`) is the proven "obligation must survive" shape, backed by one predicate serving the setter, the `aw check` rules, and an opt-in hook.
- `evaluate_review_finding_escalation` (`check_engine.py:3116`) is the nearest precedent for a lint+check shared evaluator, and its own comment concedes the adjacent weakness this plan closes: it escalates a finding into an OQ INSIDE the same plan, so once that plan is `executed` the OQ is frozen in a terminal artifact.
- TYPED FIELDS ONLY. The `Finding` field exists precisely because prose matching is "spoofable by any incidental mention and brittle against rewording" (`ipd_schema.py:1321-1323`).
- `OQ_FIELDS` is currently declared and never read (`ipd_schema.py:1315`), and the OQ parser is deliberately open-ended, so a new subfield parses today and simply has no consumer until this plan adds one.
- Severity vocabulary must be SHARED, not forked (see `review_findings.is_gating`).
- The suite runs BARE. Shared checkout: run `aw runs` first; `check_engine.py` is large and widely imported, so re-locate symbols before editing.

## Findings

| Id | Severity | Area | What | Evidence |
|---|---|---|---|---|
| F-1 | HIGH | invisible on executed | An `executed` plan classes `done` in `aw attention`, so any unfixed defect in its prose leaves every attention view with no record it was dropped. | maintainer 2026-09-05; `attention_contract` maps executed -> done |
| F-2 | HIGH | dead required section | `## Deferred / out of scope (with reason)` is mandatory in both H2 orders and its contents are never read. | `H_DEFERRED` at `ipd_schema.py:48`; `grep -c H_DEFERRED agent_workflows/ipd_lint.py` = 0 |
| F-3 | HIGH | wrong phase | The blocking-OQ gate runs at `pre-execution` only, so it gates starting work and not claiming it is done. | `ipd_lint.py` phase split; the exclusion is deliberate and documented |
| F-4 | HIGH | prose-resolvable OQ | An OQ is "resolved" by any non-empty rationale; semantics are explicitly the reviewer's job. | `open_question_error`, `ipd_schema.py:1338-1354` (`if status == "resolved" and not has_rationale`) |
| F-5 | HIGH | no linkage concept | `ipd_lint.py` has ZERO occurrences of `From-Backlog`: the linter cannot see backlog linkage at all. | `grep -c From-Backlog agent_workflows/ipd_lint.py` = 0 |
| F-6 | HIGH | rollout hazard | RE-MEASURED AT REVIEW AND LARGER, WITH A BETTER DENOMINATOR: 608 plans exist (470 executed, 104 pending, 30 superseded, 4 not-executed), of which 476 carry at least one `## Deferred / out of scope` bullet row, 1929 rows total; ALL 104 pending plans carry rows (714). ZERO plans anywhere carry a typed carrier field. So a per-row error rule fires 714 times on the working tree alone. Severity raised from MEDIUM because the number is 15% higher in plans and unbounded in rows. | `ls .aw/records/plans/*/*.ipd.md \| wc -l` = 608; section-scoped row count script; `rg '^- Carrier:'` = 0; `check.review-finding-unescalated` absent-case comment |
| F-7 | MEDIUM | schema field is unread | `OQ_FIELDS` is declared and never consumed, so E-01 alone changes no behavior; E-02 is what gives it meaning. | `ipd_schema.py:1315-1318` |
| F-8 | LOW | walkthroughs unreachable | Walkthroughs are `tracked=False` and filename-only checked, so a defect recorded only there is invisible by construction. This plan does not fix that; it makes the IPD route reliable. | item's own measurement; `attention_contract` walkthrough policy |
| F-9 | HIGH | handoff unreachable as scoped | THE RUN-RECORD READ THE HISTORY MANDATES IS NOT REACHABLE FROM THE DECLARED SCOPE. `b7xarm` E-06 persists the normalized report under `.aw/records/runs/`, which is gitignored with ZERO tracked files, and NONE of this plan's four `Scope-Paths` reads that tree. `b7xarm`'s own round-1 review recorded the same gap as its PR-A05. Escalated as blocking OQ-05. | `rg 'records/runs\|state_root\|run_dir'` over the three production modules -> no match; `git ls-files .aw/records/runs \| wc -l` = 0; `b7xarm` history |
| F-10 | HIGH | wrong seam for E-03 | `pre-transition` CHECKS LIVE IN A PURE FUNCTION AND CANNOT RESOLVE A CARRIER. `check_checkpoint` (`ipd_lint.py:754`) is called from `lint_text` (`:1066`), whose contract is "Pure: no I/O" (`:1040`). Carrier resolution is I/O, so the rule must be merged in `lint_file`, exactly as the dependency-resolution checks and `_merge_review_escalation` (`:1173-1191`) already are, with the same documented consequence that a text-only lint cannot report it. | source read |
| F-11 | MED | cutover marker unset | THE MECHANISM E-05 REUSES IS NOT CONFIGURED HERE, so "reuse it" would grandfather everything and leave the `error` tier unreachable. `config.dependency_cutover_date(Path('.'))` returns `None`, and an absent marker is the documented grandfather-everything case. Three options recorded in E-05; V-05 cannot demonstrate an erroring NEW plan under naive reuse. | evaluated the accessor; `check_engine.py:2439`; `oc_runipd.py:2544` comment |
| F-12 | MED | internal contradiction, now fixed | THE PLAN CONTRADICTED ITSELF ON ITS DEPENDENCY AND THE PROSE NAMED AN ILLEGAL EDGE. Front matter says `executed:b7xarm`; OQ-04's prose claimed `to-review:b7xarm`. Measured: `parse_item_dependencies('to-review:b7xarm')` is REJECTED ("expected executed:/exists:/state:"). The front matter is correct and the prose is corrected. | `parse_item_dependencies` evaluated on four tokens |
| F-13 | LOW | citation drift | BOTH PRECEDENT CITATIONS MOVED: `CloseVerdict` is at `check_engine.py:1963` (plan said `:1934`) and `evaluate_blocking_close` at `:1980` (plan said `:1951`). The symbols and their documented semantics are otherwise exactly as described, including the three named paths `HANDOFF|SATISFIED|DE-GATED`. | source read |
| F-14 | LOW | stale test attribution | The required-tests note attributes `~14 test_run_viewer.py` failures to `agrlvw`; that module is GREEN today. The real environmental failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, caused by another party's gitignored `opencode-recovery/` tree (backlog `8kttqq`). | measured |

## Proposed changes (ordered, validatable)

0. SETTLE OQ-05 FIRST (does the predicate read `b7xarm`'s run record, and does `Scope-Paths` change?), because it decides what E-02 builds. E-01 is valid under either answer.
1. Declare a typed carrier field for OQ blocks and Deferred rows (E-01).
2. Write one shared predicate with three escapes, resolving carriers for real (E-02).
3. Wire `pre-transition` to it without disturbing the `pre-execution` gate (E-03).
4. Wire `aw check` to the SAME predicate under a registered rule id (E-04).
5. Ship staged severity on an existing cutover mechanism, with the blast radius measured (E-05).
6. Test three escapes, four refusals, the malformed case, staged severity, and cross-surface agreement (E-06).

## Deferred / out of scope (with reason)

EVERY ROW BELOW NOW CARRIES A TYPED `- Carrier:`, WHICH IS THIS PLAN DOGFOODING ITS OWN RULE. It was not required to: this plan is dated 2026-09-07, before `CARRIER_CUTOVER_DATE` (20260919), so E-05's staged severity grandfathers it and its rows produced a non-failing `info` advisory. Carrying them anyway is the point, because the rule's whole claim is that a note in an executed IPD is the same as writing it nowhere. Four rows named a tracker only in PROSE (`f1sw71`, `ld08f1`, and two unfiled gaps), which is exactly the corpus shape E-06's prose-only test refuses; three needed a carrier filed this turn.

- MAKING WALKTHROUGHS READABLE (F-8): they are `tracked=False` and filename-only checked by design. Changing that is a records-policy change with its own blast radius; this plan makes the IPD route reliable instead.
  - Carrier: 3yr30q
- PER-REQUIREMENT SPEC COVERAGE (the "which half of this approved spec is unbuilt" gap): the item explicitly says this must NOT block, and it is tracked separately as `f1sw71`.
  - Carrier: f1sw71
- RETIRING `TODO.md`: tracked by `ld08f1` (open). The item names it only to explain why a `TODO.md` entry is not an acceptable carrier: it holds zero items, is in `SCAN_ROOTS`, and is silently dropped as unclassified, i.e. invisible by construction.
  - Carrier: ld08f1
- SCANNING SOURCE-CODE COMMENTS for defects: `artifact_core.py` limits reading to `.md`/`.txt`, and the one TODO/FIXME scanner that exists is diff-scoped and unwired. Out of scope.
  - Carrier: wuooks
- MAKING `aw check backlog` FAIL-CLOSED in CI: currently advisory. Adjacent and tempting, but a separate decision with its own consequences.
  - Carrier: e85snf
- THE `releases` TREE having no scan root, and the `reviews` tree having no TreePolicy: both found while investigating, both their own items. The `releases` half is carried by plan `m867ox` (durablecapture 02, approved and pending); the `reviews` half is carried by the backlog item below.
  - Carrier: msi8dx, m867ox

## Scope check

- Over-scope: none. Three production modules and one test file.
- Under-scope: this plan does NOT read walkthroughs, does NOT add per-requirement spec tracking, does NOT retire `TODO.md`, does NOT scan code comments, and does NOT change `aw check backlog`'s advisory posture in CI.
- SCOPE-PATHS COMPLETENESS WAS SETTLED BY OQ-05 AND IS NOW CLOSED (F-9 resolved). The maintainer ruled the run-record read UNNECESSARY ("The report is not needed. A backlog item or IPD is"), so the four declared paths ARE complete and none was added. VERIFIED AT EXECUTION: `rg 'records/runs|state_root|run_dir'` over `check_engine.py`, `ipd_lint.py` and `ipd_schema.py` returns no match, and the exact files changed are the four declared ones and nothing else.
- THE OPTION-(b) LIMIT PARAGRAPH IS SUPERSEDED, NOT SATISFIED, and the distinction matters because it changes what follow-up is owed. It said a follow-up item must be filed for the "found-nothing versus never-asked" distinction. OQ-05's ruling REJECTED that premise rather than choosing the option: the run-record report is not the requirement, a durable carrier is. So NO follow-up item is owed for it, and the shipped rule does deliver the ruling as ruled.
- THE HONEST LIMIT THAT DOES REMAIN, restated accurately at execution: this is a DECLARATION gate, not a detector (OQ-04). It enforces "if you RECORD an outstanding obligation, carry it durably" and cannot enforce "declare every defect you found", because nothing deterministic can tell that an author described a defect in prose and wrote no typed field. That limit is inherent to refusing prose matching (E-01) and is documented in the shipped code rather than left for a reader to discover.

## Required tests / validation

New `tests/test_durable_capture.py` covering the three escapes, the four refusal cases (no carrier, dangling carrier, terminal-directory carrier, prose-only), the malformed-input case, staged severity, `aw check`/`pre-transition` agreement on identical fixtures, and the `lint_text`-cannot-report corollary (F-10). Build every carrier target in a tmp_path fixture, never against the live trees. Plus a whole-corpus `aw check` run reporting counts at each severity PER PLAN AND PER ROW (the row count is the one that matters: 1929 rows across 476 plans, 714 of them in the 104 pending plans), and the bare suite on a self-measured delta.
BASELINE RE-MEASURED AT REVIEW (HEAD `32948d10`): `1 failed, 5958 passed, 3 skipped, 2 xfailed`. The single failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, caused by ANOTHER party's gitignored `opencode-recovery/` tree (1746 files, `.gitignore:49`, backlog `8kttqq` `open`). DO NOT delete or "fix" that tree; the shared-checkout rule forbids touching it. CORRECTED (F-14): the note below about "~14 `test_run_viewer.py` failures" belonging to `agrlvw` is STALE, that module is green today, so do not use it to excuse a failure. Compare failing NODE IDS, never totals.

## Spec / documentation sync

No `.spec.md` is amended, so none is declared in `- Scope-Paths:`. This plan adds an enforcement rule over an EXISTING required IPD section and an existing OQ vocabulary; it introduces no new contract for a spec to describe. If review finds that the IPD structural contract (the `ipd-spec` document) must record the new typed field, that is a spec amendment and this plan must gain the path explicitly rather than editing an undeclared file.

The new rule's message is operator-facing: it must name the offending row AND the three ways to satisfy it, because a refusal an author cannot act on is what trains people to bypass gates (`gjadwm`). Write no em or en dashes there.

## Open questions

### OQ-01: Should the carrier field be permitted to name a SPEC, not only a backlog item or a pending plan?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: RESOLVED BY THE MAINTAINER 2026-09-08: NO. The accepted carrier set is a BACKLOG ITEM or a PENDING PLAN only. A spec is never sufficient. THE REASONING GIVEN, which goes further than this question asked and binds E-03's prompt guidance in the precursor plan `b7xarm`: an agent may and SHOULD write a spec where a spec is genuinely what the work needs, and should reference it, but the durable carrier is ALWAYS a backlog item, with any spec treated as supporting material. Offered the alternative of letting the agent judge whether a case was "just a spec" and then file a spec-review backlog item, the maintainer instead chose the rule that removes the judgement entirely, having said plainly "I don't know when 'just a spec' would be enough". So do NOT implement a spec-only branch: it delegates exactly the judgement this rule exists to prevent. The reading offered at authoring (accept a non-terminal spec as weaker-but-better-than-prose) is REJECTED. E-02's accepted-carrier list stays one list in one predicate, and it contains two entries.
  ORIGINAL FRAMING, retained because it records what was weighed: AGAINST a spec, the item argues there is no per-requirement tracking, no notion of partial implementation, and no join from a requirement to an implementing plan, so "a spec can sit `approved` for weeks with half its requirements unbuilt and nothing asks which half". FOR a spec, `aw attention` DOES map spec `approved` -> `ready`, so a spec is not invisible the way an executed plan is, and the repository already treats a spec as a legitimate release-gate carrier. The maintainer weighed both and chose the stricter set.

### OQ-04: Can this gate work at all before an execute turn is required to declare its findings?

- Blocking: yes
- Status: resolved
- Owner: none
- Resolution or deferral rationale: RESOLVED BY THE MAINTAINER 2026-09-08, AND THE ANSWER ADDED A PREREQUISITE THIS PLAN DID NOT HAVE. Asked to approve OQ-01, the maintainer rejected the framing instead: "I do want to know how the system (runner) 'knows' there is an 'unfixed bug that has no durable home somewhere else' since I am skeptical you can do this. Settle this before moving forward." THAT SKEPTICISM WAS CORRECT AND IS NOW RECORDED AS THIS PLAN'S HONEST LIMIT. The runner cannot detect an unfixed defect and this plan never claimed to: E-01 matches STRUCTURALLY and explicitly refuses prose matching as "spoofable by any incidental mention and brittle against rewording", so the predicate only ever decides mechanical facts (does a typed field exist, does its id6 resolve, is the target non-terminal). It is a DECLARATION gate, not a detector. The consequence the maintainer drew out: an author who mentions a defect in prose and writes no typed field is NOT caught, and F-6 already measures the day-one effect (530 plans exist, ZERO carry the field), so shipped alone this gate initially catches nothing.
  THE RULING: the declaration must be PRODUCED before the check that verifies it is built. In the maintainer's words, the execute instructions "must ask the agent to report any found bugs, gaps, concerns both affirmatively and negatively ... If neither exists, the runner must ask the agent to add it", with the preferred design being that when the agent is done, "the runner asks the agent (in the same session) to fill out an outstanding bugs/gaps/etc. report", and this is "a requirement before we can implement the check to see if a backlog / IPD exists for each item".
  CONSEQUENCE FOR THIS PLAN: `b7xarm` (`defreport-01`) was authored to produce that report, and this plan declares `Item-Dependencies: executed:b7xarm`. CORRECTED AT REVIEW (F-12): this paragraph previously said the declaration was `to-review:b7xarm`, which is not a legal edge at all. Measured: `parse_item_dependencies('to-review:b7xarm')` is REJECTED with "expected executed:/exists:/state:", while `executed:b7xarm` parses to a single `ItemDependency(kind='executed', target_type='ipd', id6='b7xarm')`. The front-matter field was right and this prose was wrong; anyone who had copied the prose value would have failed the dependency linter. `executed:` is also the correct SEMANTICS, since the maintainer's ruling is that the declaration must be produced before the check is built, and a `b7xarm` that is merely reviewed produces nothing. `b7xarm` is currently `- Status: reviewed` with `- Readiness: go-pending-approval`, so this edge is unmet today and correctly blocks execution.
  E-02's predicate was told to read the NORMALIZED report `b7xarm` E-06 persists on the run record rather than only the plan's typed fields, since that record is what distinguishes "found nothing" from "never asked". THAT INSTRUCTION IS NOT REACHABLE FROM THIS PLAN'S DECLARED SCOPE and is now escalated as OQ-05 (F-9): the record lives under the gitignored `.aw/records/runs/` and none of the four declared `Scope-Paths` reads that tree. `b7xarm`'s own review found the same gap independently (its PR-A05). Resolved rather than open because the maintainer's DECISION is made and the dependency is declared; BLOCKING stays `yes` to reflect that executing this plan before `b7xarm` would ship a gate with nothing to verify.

### OQ-05: Does the predicate read `b7xarm`'s run record, which no declared scope path can reach?

- Blocking: yes
- Status: resolved
- Owner: maintainer
- Finding: PR-301
- Resolution or deferral rationale: RESOLVED BY THE MAINTAINER 2026-09-10 (`/askme`), AND THE RULING IS NARROWER AND STRONGER THAN ANY OPTION OFFERED. Their words: "All defects require one or more backlogs or plans to address. The report is not needed. A backlog item or IPD is. This is a MUST, not a should."
  SO THE PREDICATE DOES NOT READ THE RUN RECORD, AND THE WEAKER-RULE FRAMING IN MY PROMPT WAS WRONG. I offered scope-to-plan-fields as a compromise that "ships the weaker rule" and states honestly that found-nothing versus never-asked is unenforced. The maintainer rejected the premise rather than choosing the compromise: the run-record REPORT is not the requirement at all. The requirement is a DURABLE CARRIER, a `backlog` item or an IPD, for every defect. A report that merely asserts "I looked and found nothing" is not what makes a defect survive; a carrier is. So the run-record read is dropped as UNNECESSARY, not deferred as unaffordable, and no follow-up item is owed for it.
  WHAT THIS PLAN ALREADY BUILDS IS THE RULING, which is why nothing needs re-scoping: E-02's predicate refuses an IPD's transition to `executed` while it names an unfixed defect with no durable carrier, and its HANDOFF escape already requires the carrier to resolve to a `backlog/open/` item or a plan NOT in a terminal directory, with a dangling reference FAILING and a carrier resolving into `executed/` explicitly rejected as "the very hiding place this plan exists to close". That is exactly "a backlog item or IPD is required".
  ONE CONSEQUENCE THE EXECUTOR MUST HONOR: the ruling says MUST, so E-05's staged severity (`warning` for existing plans, `error` for newly authored ones) is a ROLLOUT mechanism and not a softening of the rule. The end state is `error`; the warning tier exists only so a pre-existing corpus can be migrated, and nothing may leave the rule permanently at `warning`.
  ALSO CORRECT THE PLAN'S OWN HISTORY, which instructs that "E-02's predicate should additionally read the NORMALIZED report `b7xarm` E-06 persists on the run record". That instruction is SUPERSEDED and must not be followed; leaving it in place would have an executor build a read the maintainer has ruled unnecessary.
  THE UNREACHABILITY MEASUREMENT STANDS AND IS WHY THE QUESTION WAS ASKED AT ALL, re-verified at HEAD `3c64b5bf`: `git ls-files .aw/records/runs` returns ZERO tracked files, and none of the four declared `Scope-Paths` (`check_engine.py`, `ipd_lint.py`, `ipd_schema.py`, `tests/test_durable_capture.py`) contains any reference to that tree. The ruling makes that unreachability moot rather than a problem to solve.
  THE `- Item-Dependencies: executed:b7xarm` EDGE IS LEFT IN PLACE and was not touched by this answer. Whether it is still warranted now that the run-record read is dropped is a separate question the executor should raise if the edge blocks progress; this answer does not clear it.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [x] V-01 validates E-01
  - Required evidence: paste the new field's declaration beside the existing `Finding` entry in `OQ_FIELDS`, and paste the comment recording why prose matching is refused. State explicitly that `OQ_FIELDS` has no consumer TODAY (quote `ipd_schema.py:1315`) and that E-02 is what gives this field meaning, so this item alone is provably behavior-neutral: paste a test or a run showing no existing plan's lint result changed.
  - Observed evidence: THREE typed fields were added, not one, because the three escapes E-02 requires each need their own declaration. `python3 -c "from agent_workflows import ipd_schema as S; print(S.OQ_FIELDS); print(S.CARRIER_FIELDS)"`:
    `('Blocking', 'Status', 'Owner', 'Resolution or deferral rationale', 'Finding', 'Carrier', 'Carrier-Evidence', 'Carrier-Declined')`
    `('Carrier', 'Carrier-Evidence', 'Carrier-Declined')`
    They sit DIRECTLY BESIDE `Finding` in `OQ_FIELDS`, with the pointer comment added immediately above the tuple: "durablecapture `rnkqrc` E-01 adds `Carrier` on the same terms and for the same reason ... the reference is matched STRUCTURALLY (`- Carrier: <id6>`), never by prose."
    THE ANTI-PROSE RATIONALE is recorded in the new `CARRIER_FIELD` block, and it cites the corpus rather than asserting the principle: "A substring search over prose would be spoofable by any incidental mention and brittle against rewording ... measured 2026-09-18 at HEAD 94345381, 2225 `## Deferred / out of scope` rows exist across 539 plans and EVERY ONE is prose, several literally saying 'tracked as `<id6>`'. A prose matcher would accept all of them and verify nothing".
    `OQ_FIELDS` HAS NO CONSUMER TODAY, quoted verbatim from `ipd_schema.py:1317` as required: "`OQ_FIELDS` has NO consumer anywhere in the codebase (it is defined here and never read)". The standing warning at `:1329` was honored: the new comment states that if the tuple ever becomes a CLOSED allowlist, these fields must stay in it. E-02's `evaluate_carrier_obligation` is what finally gives the vocabulary a consumer.
    BEHAVIOR-NEUTRALITY IS DEMONSTRATED, NOT ASSERTED. `tests/test_durable_capture.py::SchemaIsBehaviorNeutralTests::test_adding_the_vocabulary_changes_no_existing_lint_result` lints a carrier-free plan at ALL FOUR checkpoints (`author`, `review-finalize`, `pre-execution`, `pre-transition`) through the PURE `lint_text` and asserts the rule is absent from every one; it passes. Corpus-wide confirmation: `aw check plans` at pre-change HEAD reported `check.lifecycle-transition-invalid` 5 + `check.scope-drift` 333 = 338 findings, exit 1. The three new schema symbols alone add nothing to that (the six pure `lint_text` checks never read them); the 106 new findings measured in V-05 come from E-02/E-04, exactly as this item predicts.
  - Result: pass

- [x] V-02 validates E-02
  - Required evidence: FIRST paste the maintainer's answer to OQ-05 and the resulting shape of this item (whether the predicate reads the run record and whether `Scope-Paths` changed); V-02 may not be marked complete while OQ-05 is open. Then paste the predicate and its verdict type, and paste the side-by-side comparison with `evaluate_blocking_close` (`check_engine.py:1980`) / `CloseVerdict` (`:1963`) showing the shape was MIRRORED rather than reinvented, including the `severity` and `fixes` fields E-05 and the operator message depend on. Demonstrate each of the three escapes returning a satisfied verdict on a real fixture.
    THE LOAD-BEARING REFUSALS: paste a DANGLING carrier refused, and paste a carrier resolving into `executed/` refused. The second is the whole point of the plan (an executed plan is the hiding place), so a predicate that accepts it has inverted the requirement. Paste a malformed row producing a finding rather than a traceback.
  - Observed evidence: OQ-05 WAS ALREADY ANSWERED BEFORE EXECUTION, so this item was not gated. The maintainer's words, recorded in OQ-05: "All defects require one or more backlogs or plans to address. The report is not needed. A backlog item or IPD is. This is a MUST, not a should." RESULTING SHAPE: the predicate does NOT read the run record, `Scope-Paths` is UNCHANGED (the four declared paths are exactly what was touched), and nothing in the new code references `.aw/records/runs/`. Verified: `rg 'records/runs|state_root|run_dir' agent_workflows/check_engine.py agent_workflows/ipd_lint.py agent_workflows/ipd_schema.py` returns no match. The superseded history instruction to read that record was NOT followed, as OQ-05 directs.
    THE PREDICATE is `check_engine.evaluate_carrier_obligation` (per row) plus `check_engine.evaluate_durable_carrier` (per plan). THE VERDICT TYPE IS `CloseVerdict` ITSELF, not a lookalike: `python3 -c "from agent_workflows import check_engine as ce; print(ce.CloseVerdict._fields)"` -> `('legitimate', 'severity', 'reason', 'fixes', 'path')`. `tests/test_durable_capture.py::VerdictShapeIsMirroredTests::test_the_verdict_is_a_CloseVerdict` asserts `isInstance(v, ce.CloseVerdict)` AND that exact field tuple, so no fourth verdict type was invented. `severity` and `fixes` are both populated and both load-bearing: E-05's staged tier reads `severity`, and the operator message reads `fixes` (three of them, one per escape, asserted by `test_a_refusal_carries_the_three_actionable_fixes`).
    THE THREE ESCAPES, run on real tmp_path fixtures (actual output):
    `HANDOFF   : CloseVerdict(legitimate=True, severity='ok', reason='handed off to carrier car111', fixes=(), path='HANDOFF')`
    `SATISFIED : CloseVerdict(legitimate=True, severity='ok', reason="satisfied by resolvable evidence '.aw/records/specs/20260901-aaaaaa-01-aaaaaa-x.spec.md'", fixes=(), path='SATISFIED')`
    `DECLINED  : CloseVerdict(legitimate=True, severity='ok', reason='obligation explicitly declined with a reason', fixes=(), path='DECLINED')`
    SATISFIED delegates to the SHARED `resolve_evidence_artifact`, the same resolver `aw backlog set done --evidence` uses, so the evidence shape cannot fork. Note the one deliberate naming divergence from `evaluate_blocking_close`: its third path is `DE-GATED`, ours is `DECLINED`, because de-gating removes a release gate whereas declining records a reason for needing no carrier. Same role in the verdict, honest name.
    THE FOUR REFUSALS (actual `reason` strings):
    `REFUSE none    : deferred row 1 records an outstanding obligation with NO durable carrier; once this plan reaches `executed` it classes `done` in `aw attention` and this vanishes with no record`
    `REFUSE dangling: deferred row 1: carrier zzzzzz resolves to no backlog item or plan`
    `REFUSE executed: deferred row 1: carrier carexe resolves only to a terminal/hidden artifact (executed); nothing revisits it`
    `REFUSE malformed: deferred row 1: malformed `Carrier` reference(s) 'see the backlog' (expected a bare 6-char id6)`
    THE `executed/` CASE IS THE WHOLE POINT AND IT REFUSES, asserted by `test_a_carrier_in_executed_is_refused_because_that_is_the_hiding_place`. The terminal set was widened beyond `executed/` on the same reasoning, each with a test: a `done` backlog item and a `parked` one are refused too (`parked` maps to the PARKED attention class, which the default board HIDES, so it is invisible by construction exactly as an executed plan is), while a `graduated` item is ACCEPTED because `attention_contract` maps it to `active` and it is therefore still revisited. A SPEC is refused (`test_a_spec_is_never_a_sufficient_carrier`), which is OQ-01's ruling enforced in code: `_CARRIER_TARGET_TYPES` holds exactly `("backlog", "plans")`.
    MALFORMED IS A FINDING, NEVER AN EXCEPTION: the fourth line above is a returned verdict, not a traceback. `parse_carrier_ids` returns `(good, bad)` instead of raising, so `test_a_good_token_beside_a_bad_one_still_reports_the_malformed_one` shows a good token beside a bad one still parses. A binary-garbage plan yields no findings and no traceback (`test_an_unparseable_plan_yields_no_traceback`), and a nonexistent repo root yields a refusal rather than an OSError (`test_the_predicate_never_raises_on_an_absent_repo`).
  - Result: pass

- [x] V-03 validates E-03
  - Required evidence: paste `aw ipd lint --phase pre-transition` reporting the new rule on a fixture plan with an uncarried defect. PROVE THE PLACEMENT: paste the `git diff` showing the merge happens in `lint_file` and NOT in `check_checkpoint`/`lint_text`, and paste a `lint_text` call on the same text showing it does NOT report the rule (F-10; `lint_text`'s contract is "Pure: no I/O"). Paste the comment documenting the deliberate divergence from `_REVIEW_ESCALATION_CHECKPOINTS`, which excludes `pre-transition` on purpose. THEN PROVE THE `pre-execution` GATE IS UNTOUCHED: paste its behavior before and after on the same fixture and show them identical, and show the existing blocking-OQ check was not moved or widened.
  - Observed evidence: THE RULE FIRES AT `pre-transition` ON A FIXTURE WITH AN UNCARRIED DEFECT (actual `lint_file` output, the fixture's own unrelated metadata diagnostics included verbatim so nothing is trimmed to flatter the result):
    `disposition: error`
    `   IPD-M101 | Concern: required field missing`
    `   IPD-M101 | Scope: required field missing`
    `   IPD-M101 | Author: required field missing`
    `   IPD-M104 | Approval: Approval is required when Status is approved`
    `   check.ipd-uncarried-obligation | 1 obligation(s) name no durable carrier: deferred row 1 records an outstanding obligation with NO durable carrier; once this plan reaches `executed` i...`
    THE PLACEMENT IS PROVEN BY SYMBOL, not only by diff, so a later refactor into the pure path fails a test rather than passing silently. `test_the_merge_is_wired_into_lint_file_and_not_check_checkpoint` reads `inspect.getsource` of all three functions and asserts `_merge_durable_carrier` appears in `lint_file` and appears in NEITHER `check_checkpoint` NOR `lint_text`. The diff is one line in `lint_file`, immediately after its `_merge_review_escalation` sibling: `result = _merge_durable_carrier(path, result, text, checkpoint, doc)`. It follows that sibling's shape exactly: gated on a checkpoint frozenset, skips legacy/quarantined dispositions, reuses the ALREADY-PARSED `doc.open_questions` rather than re-parsing, and delegates to the ONE shared evaluator.
    `lint_text` PROVABLY CANNOT REPORT IT, on the SAME text as the passing run above:
    `disposition: error | diagnostics: ['IPD-M101', 'IPD-M101', 'IPD-M101', 'IPD-M104'] | advisories: []`
    The rule is absent from both lists, which is the intended corollary of `lint_text`'s "Pure: no I/O" contract (carrier resolution is I/O). Asserted by `test_lint_text_can_never_report_the_rule`. THIS IS NOT A BUG and a later reader should not file it as one; it is stated in `_merge_durable_carrier`'s docstring in as many words.
    THE DELIBERATE DIVERGENCE IS DOCUMENTED IN PLACE, beside `_CARRIER_CHECKPOINTS`: "`pre-transition` ONLY, and the asymmetry with `_REVIEW_ESCALATION_CHECKPOINTS` directly above is DELIBERATE rather than an inconsistency to be 'fixed' later. That set EXCLUDES `pre-transition` because 'blocking there would only strand a completed plan' ... This rule does the exact opposite for the opposite reason: the transition to `executed` is the precise moment an uncarried obligation VANISHES ... One set excludes the phase the other requires, and both are right about their own concern." The divergence is also PINNED BY A TEST (`test_the_carrier_checkpoint_set_diverges_from_the_review_escalation_set`), so someone "harmonizing" the two sets breaks a test that explains why not to.
    THE `pre-execution` GATE IS UNTOUCHED, shown both ways. (1) The carrier rule does NOT fire there: `carrier rule at pre-execution: []` on the same fixture that errors at `pre-transition`. (2) The pre-existing blocking-OQ check still fires there unchanged: `test_the_blocking_oq_check_still_fires_at_pre_execution` asserts `IPD-S404` plus the exact message "unresolved blocking question at pre-execution". `git diff agent_workflows/ipd_lint.py` touches ONLY the single call line in `lint_file` and appends the new block; `check_checkpoint` has ZERO diff lines, so the `pre-execution` behavior is byte-identical by construction rather than by measurement. Two gates, two questions, neither moved nor widened.
    ONE ADDITION BEYOND THE ITEM'S WORDING, stated because it is a real behavioral choice: a GRANDFATHERED (pre-cutover) plan's finding is surfaced as an ADVISORY here and never flips the disposition, so the 106 pending plans that predate this rule still lint conforming at `pre-transition` (`test_a_pre_cutover_plan_lints_conforming_at_pre_transition` asserts absent-from-diagnostics AND present-in-advisories). Without that, E-05's staged severity would have been defeated at the lint surface even while working at the `aw check` surface.
  - Result: pass

- [x] V-04 validates E-04
  - Required evidence: paste the `RULE_REGISTRY` entry, and paste `aw check` and `aw ipd lint --phase pre-transition` producing the SAME verdict on the SAME fixture, for both a passing and a failing case. Show by symbol that both call ONE predicate (object identity or the single call site), since drift between the two surfaces is the failure the precedent's docstring warns about. Confirm `aw check backlog`'s advisory posture in CI is unchanged (`git diff` on the workflow file EMPTY).
  - Observed evidence: THE REGISTRY ENTRY, read back from the shipped registry rather than quoted from the source: `python3 -c "from agent_workflows import check_engine as ce; print(ce.rule_spec('check.ipd-uncarried-obligation'))"` ->
    `RuleSpec(severity='error', assurance='repository', determinism='deterministic', invariant='I-07')`
    `test_the_rule_id_resolves_to_a_registered_spec_not_the_default` asserts all four fields, so the rule cannot silently fall through to `_DEFAULT_RULESPEC`. Invariant `I-07` is claimed deliberately, not left empty: it is the obligation-PRESERVATION invariant its `check.blocking-item-closed-without-gate` and `check.from-backlog-*` neighbours claim, and this rule is the same three-escape predicate shape applied to the same concern. `determinism='deterministic'` is honest: a typed-field presence test plus a literal id6 lookup plus a closed-vocabulary status comparison, with no prose read anywhere.
    THE TWO SURFACES AGREE ON IDENTICAL FIXTURES, both directions. FAILING case (actual output):
    `rule: check.ipd-uncarried-obligation | severity: error`
    `detail: 1 obligation(s) name no durable carrier: deferred row 1 records an outstanding obligation with NO durable carrier; once this plan reaches `executed` i...`
    `detail == lint message: True`
    That last line is the load-bearing one: the `aw check` Drift's `detail` is BYTE-EQUAL to the `pre-transition` Diagnostic's `message`, asserted by `test_both_surfaces_fire_on_the_same_failing_fixture`. PASSING case: `test_both_surfaces_are_silent_on_the_same_passing_fixture` adds `- Carrier: car111` to the same fixture and asserts BOTH surfaces return empty.
    ONE PREDICATE, PROVEN BY SYMBOL (`test_one_predicate_backs_both_surfaces`): `inspect.getsource(ce.check_durable_carrier)` contains `evaluate_durable_carrier`, and `inspect.getsource(ipd_lint._merge_durable_carrier)` contains `evaluate_durable_carrier`. There is exactly one definition of that function, so the sweep and the checkpoint cannot disagree by construction, which is the property `evaluate_review_finding_escalation`'s docstring calls its design intent ("The ONE evaluator ... shared by both host surfaces").
    REACH IS CORRECT AND NOT DOUBLE-COUNTED: `test_the_rule_is_reached_by_the_plans_type_path` shows `check_types(repo, ["plans"])` reports it, and `test_the_rule_is_reported_exactly_once_by_the_full_sweep` shows `check_types(repo, ["all"])` reports it EXACTLY once. Placement is in the plans-type content path beside `check_review_finding_unescalated`, for the reason that neighbour documents, and deliberately NOT in the collisions-only cross-tree sweep. Terminal-lane plans are grandfathered by the sweep across all three terminal dirs (`test_a_terminal_lane_plan_is_grandfathered_by_the_sweep`), so the 527 executed plans are never retroactively litigated.
    `aw check backlog`'s CI POSTURE IS UNCHANGED, and the temptation is recorded as the item asks. `git diff -- .github/workflows/` is EMPTY (verified this turn; `git diff --name-only` lists only `agent_workflows/check_engine.py`, `agent_workflows/ipd_lint.py`, `agent_workflows/ipd_schema.py`). It WAS tempting: the two backlog items this turn filed are the durable carriers for its own findings, and an advisory backlog check means a malformed one would not fail CI. It was left alone; making it fail-closed is its own decision with its own blast radius, and this plan's `## Deferred / out of scope` section already names it.
  - Result: pass

- [x] V-05 validates E-05
  - Required evidence: THE BLAST RADIUS IS THE EVIDENCE. Paste `aw check` over the whole corpus BEFORE and AFTER, with counts at each severity, PER PLAN AND PER ROW, and show existing plans produce WARNINGS not ERRORS. State the plan count and the ROW count you measured (review measured 608 plans, 476 with deferred rows, 1929 rows, 714 of them in the 104 pending plans) and how many carry a typed carrier field (expected 0). Paste an OLD plan warning AND a NEW plan ERRORING, demonstrated by running the rule rather than asserted. NAME WHICH OF E-05's THREE BOUNDARY OPTIONS YOU CHOSE and, if you reused `dependency_schema_cutover`, paste the value of `config.dependency_cutover_date(repo_root)` in the test environment: review measured `None` here (F-11), and under an absent marker a naive reuse grandfathers everything so the erroring-new-plan case cannot be shown without setting it.
  - Observed evidence: BOUNDARY OPTION (c) WAS CHOSEN: a module constant compared against the plan's OWN `- Date:`, needing no configuration. `ce.CARRIER_CUTOVER_DATE` = `20260919`. The reused-marker option was rejected on the re-verified measurement F-11 records: `config.dependency_cutover_date(Path('.'))` -> `None` in this repository (run this turn), and an absent marker grandfathers EVERYTHING, so a naive reuse would leave the `error` tier unreachable and the rule advisory-forever while its own test passed. The precedent for option (c) is in the same module: `SPEC_ID6_CUTOVER_DATE`, a constant compared against the artifact's own date, whose value was chosen "strictly AFTER the newest existing legacy-named spec date ... so ALL existing specs remain grandfathered". `20260919` follows that rule exactly: the newest plan `- Date:` in the corpus is `2026-09-17` and today is 2026-09-18, so every plan in flight is grandfathered. `test_the_cutover_is_strictly_after_every_plan_that_exists_today` pins `CARRIER_CUTOVER_DATE > "20260917"`, so lowering it carelessly breaks a test.
    THE ADVISORY TIER IS `info`, NOT `warning`, AND THIS IS THE ONE PLACE THIS ITEM'S WORDING COULD NOT BE FOLLOWED LITERALLY (recorded as DECISION 07-rnkqrc-D2, and flagged for the maintainer). Measured, not assumed: `artifact_core.drift_exit_code` (`:524-534`) exempts ONLY `info`, so a `warning` tier would exit 1 with 106 findings on a clean tree and fail the CI that E-04 notes enforces `aw check plans` fail-closed, which is exactly the "turns the working tree red and trains every agent to bypass `aw check`" outcome this plan's own approval gate forbids. This module already documents the identical conflict on `check.review-decision-unescalated` ("DO NOT READ `warning` AS 'cannot fail anything'") and already resolved it the same way on `check.stale-index-missing` ("MISSING -> `info`, the ONLY non-failing severity ... `warning` here would still exit 1 on every fresh clone"). The BEHAVIOR E-05 specified (existing reported and not failing, new failing closed) is delivered exactly; only the token differs. The REGISTERED severity is `error`, so anything the per-plan downgrade does not reach fails toward visible.
    CORPUS BLAST RADIUS, BEFORE (pre-change HEAD, `check_types(repo, ["plans"])`):
    `  5  check.lifecycle-transition-invalid`
    `333  check.scope-drift`
    `total: 338 exit: 1`
    AFTER (same call, this change applied):
    `106  check.ipd-uncarried-obligation`
    `  5  check.lifecycle-transition-invalid`
    `341  check.scope-drift`
    `total: 452`
    (the `scope-drift` delta of 8 is not mine: it is a live receipt-scope computation over a tree that other agents are changing concurrently, and it moved between the two runs.)
    MY RULE'S CONTRIBUTION IN ISOLATION, per plan AND per row as required: `plans with a finding: 106`, `severities: {'info': 106}`, `offending rows: 745`, `drift_exit_code: 0`. So all 106 affected plans are REPORTED and NONE fails the gate. Re-measured denominators (the review's numbers moved because the corpus grew): 671 plans exist (527 executed, 106 pending, 34 superseded, 4 not-executed), 539 carry at least one `## Deferred / out of scope` row, 2225 rows in total, and ALL 106 pending plans carry rows. Plans carrying a typed carrier field: ZERO (`rg '^\s*- Carrier:' .aw/records/plans` -> no match), exactly as expected.
    BOTH TIERS DEMONSTRATED BY RUNNING THE RULE, not asserted. `test_a_pre_cutover_plan_warns_without_failing_the_gate`: an old-dated plan yields severity `info` and `core.drift_exit_code(drift) == 0`. `test_a_post_cutover_plan_errors_and_fails_the_gate`: a plan dated at the cutover yields severity `error` and `drift_exit_code == 1`. Both fixtures DERIVE their dates from `ce.CARRIER_CUTOVER_DATE` itself, so they cannot drift from the shipped boundary. A plan with no parseable `- Date:` is grandfathered rather than punished (`test_a_plan_with_no_date_is_grandfathered_not_punished`), because `IPD-M101` already owns the missing-Date complaint and this rule must not invent a second consequence for it.
    THE MIGRATION DEBT IS NOT LEFT IMPLICIT. OQ-05 records that the ruling is a MUST and that "nothing may leave the rule permanently at `warning`", so backlog `rtyapw` was filed this turn as the durable carrier for the 745 grandfathered rows, with the measurement and the definition of done in the item itself. That is this plan's own rule applied to this plan's own rollout.
  - Result: pass

- [x] V-06 validates E-06
  - Required evidence: paste the test FAILING against pre-change HEAD and passing after. Enumerate every covered case with its result: three escapes accepted; no-carrier, dangling, terminal-directory, and PROSE-ONLY all refused; malformed producing a finding; staged severity both ways; and `aw check`/`pre-transition` agreeing on identical fixtures.
    The PROSE-ONLY case is mandatory: a deferred row saying "tracked in the backlog" with no typed field must be REFUSED. If that case passes the gate, the rule is spoofable and the item's central design requirement is unmet.
    Paste the bare `python3 -m pytest` summary line with a self-measured BEFORE baseline and the AFTER-minus-BEFORE failure set EMPTY, compared by NODE ID. The review baseline is `1 failed, 5958 passed, 3 skipped, 2 xfailed`, failing at `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` from another party's gitignored tree, which you must not touch. The plan's earlier note attributing ~14 failures to `test_run_viewer.py`/`agrlvw` is STALE (F-14): that module passes today, so do not use it to excuse any failure. Also paste the `lint_text`-cannot-report assertion and confirm every fixture was built under tmp_path.
  - Observed evidence: THE TEST FAILS AGAINST PRE-CHANGE HEAD AND PASSES AFTER. Pre-change (the three production modules stashed, actual output):
    `tests/test_durable_capture.py:42: in <module>`
    `    _CUT = ce.CARRIER_CUTOVER_DATE`
    `E   AttributeError: module 'agent_workflows.check_engine' has no attribute 'CARRIER_CUTOVER_DATE'. Did you mean: 'SPEC_ID6_CUTOVER_DATE'?`
    `1 error in 0.23s`
    After: `62 passed in 0.95s` (`python3 -m pytest tests/test_durable_capture.py -o addopts="" -q`).
    EVERY REQUIRED CASE, ENUMERATED WITH ITS RESULT, all 62 passing. THREE ESCAPES ACCEPTED: handoff to an open backlog item; handoff to a non-terminal plan; handoff to a `graduated` item (accepted because it maps to `active`); satisfied by resolvable evidence; declined with a reason. FOUR REFUSALS: no carrier at all; DANGLING carrier; carrier resolving into `executed/`; and PROSE-ONLY. Plus three refusals beyond the four required, on the same reasoning: a `done` backlog item, a `parked` one, an unresolvable evidence path, and a SPEC (OQ-01's ruling in code). MALFORMED PRODUCES A FINDING: a non-id6 token, a good token beside a bad one, binary garbage, and a nonexistent repo root all yield verdicts or empty results, never a traceback. STAGED SEVERITY BOTH WAYS: old plan `info` with exit 0, new plan `error` with exit 1, no-date grandfathered, and a pre-cutover plan lints CONFORMING at `pre-transition` while still appearing as an advisory. CROSS-SURFACE AGREEMENT: both surfaces fire on the same failing fixture with a BYTE-EQUAL detail/message, both are silent on the same passing fixture, one predicate proven by symbol, reached by the plans path, reported exactly once by the full sweep, terminal lanes grandfathered.
    THE PROSE-ONLY CASE IS THE MANDATORY ONE AND IT REFUSES. `test_a_prose_row_claiming_backlog_tracking_is_still_refused` builds a deferred row reading "THE WALKTHROUGH GAP: tracked in the backlog as `car111`" AND creates a REAL open backlog item with that exact id6, then asserts the rule STILL fires, with the assertion message "prose naming a REAL id6 must NOT satisfy the rule; only a typed field may". Its twin `test_the_same_row_with_a_typed_field_is_accepted` shows the identical row passing once `- Carrier: car111` is added, so the refusal is attributable to the missing typed field and to nothing else. A third case closes the obvious spoof: a `- Carrier:` line inside a CODE FENCE does not count (`test_a_carrier_inside_a_code_fence_does_not_count`), because the section walk uses the fence-aware structural reader, so a documented EXAMPLE cannot satisfy the gate.
    THE `lint_text`-CANNOT-REPORT ASSERTION: `test_lint_text_can_never_report_the_rule` calls `ipd_lint.lint_text(text, checkpoint="pre-transition", directory="pending")` on the same text that errors through `lint_file`, and asserts the rule is absent from BOTH `diagnostics` and `advisories`. Live output: `diagnostics: ['IPD-M101', 'IPD-M101', 'IPD-M101', 'IPD-M104'] | advisories: []`. Reinforced structurally by `test_the_merge_is_wired_into_lint_file_and_not_check_checkpoint`.
    EVERY FIXTURE IS BUILT IN A TEMP DIRECTORY, never against the live trees: `_mkrepo()` is `tempfile.mkdtemp(prefix="aw_durablecapture_")` and every test class tears its repo down. NOTHING reads this repository's live plans, live backlog, or `.aw/records/runs/` (gitignored, ZERO tracked files, which is why a test touching it would pass locally and fail in CI or in a lane; the module docstring records that trap).
    BARE SUITE, SELF-MEASURED BOTH SIDES IN THIS LANE. BEFORE (three modules stashed, new test file moved aside): `31 failed, 8003 passed, 3 skipped, 2 xfailed in 112.09s`. AFTER: `31 failed, 8065 passed, 3 skipped, 2 xfailed in 118.63s`. `8065 - 8003 = 62`, exactly this plan's new tests. THE AFTER-MINUS-BEFORE FAILURE SET IS EMPTY, compared by NODE ID and not by total: both `FAILED` lists were captured, sorted, and `diff`ed, and the diff is empty.
    THE 31 PRE-EXISTING FAILURES ARE NOT MINE AND ARE ALREADY OWNED, so they are not used as a blanket excuse. They are the lane-environment defect approved plan `e4lkv5` (`lanesuite-01`) exists to fix: the runner exports `AW_EXECUTION_ROLE=worker` into every isolated lane, `ipd_lifecycle.py:69` correctly refuses begin/finalize for a worker-role process, and 31 tests across six files invoke those verbs while INHERITING the ambient role. That plan's own concern line records `31 failed, 7866 passed` under the same condition against a green main tree. The review baseline quoted in this item (`1 failed, 5958 passed`) is from 2026-09-10 and predates both the lane condition and ~2100 added tests. F-14's warning was honored: `tests/test_run_viewer.py` is green and was not used to excuse anything, and neither was the `test_reporting_contract.py` failure, which does not appear in either run here.
  - Result: pass

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

Execution requires explicit human approval (`- Status: approved`). CORRECTED AT REVIEW (F-15): this paragraph said "OQ-01 is open but `Blocking: no`". OQ-01 is `Status: resolved` (the maintainer answered it on 2026-09-08) and is not what gates anything. TWO QUESTIONS CARRY `Blocking: yes`: OQ-04, recorded `resolved` with blocking retained to express that `executed:b7xarm` must land first, and OQ-05, which is genuinely OPEN and must be answered before E-02 is written because it decides whether the predicate reads the run record and therefore whether `Scope-Paths` is complete. ANSWER OQ-05 FIRST; the `executed:b7xarm` edge is a sequencing fact rather than a decision.

STAGED SEVERITY IS NOT OPTIONAL, AND THE NUMBERS ARE BIGGER THAN THIS PLAN ORIGINALLY STATED. Re-measured at review: 608 plans, 476 of them carrying at least one `## Deferred / out of scope` bullet row, 1929 rows in total, ALL 104 pending plans carrying rows (714), and ZERO plans anywhere carrying a typed carrier field. Shipping this as a hard error would turn the working tree red and train every agent to bypass `aw check`, which is the exact failure mode `gjadwm` records. Note also that the cutover marker E-05 proposes to reuse is currently UNSET (F-11), so naive reuse yields warn-only forever; pick one of E-05's three options and say which. If the staged rollout cannot be made to work, STOP and report rather than shipping a fail-closed rule over a corpus that cannot satisfy it.

ONE PREDICATE, TWO SURFACES. `aw check` and `aw ipd lint --phase pre-transition` must call the SAME function. Two implementations that agree today will disagree later, which is precisely why the nearest precedent's docstring insists on one evaluator.

TYPED FIELDS ONLY. Never satisfy this rule by grepping prose for "TODO", "future work", or a bare id6 mention. Prose matching is spoofable by incidental mention and brittle against rewording, and the schema already records that reasoning for the `Finding` field.

Commit ONLY the files you changed, path-scoped (`git commit -m msg -- <paths>`), never `git add -A`, never push. Verify with `git diff --cached --name-only` before every commit and RE-VERIFY after any failed hook. THIS IS A SHARED CHECKOUT: `check_engine.py` is large and widely imported, so re-locate every cited symbol before editing.

HARD HONESTY RULE: tests must be RUN and their ACTUAL output pasted into `Observed evidence`, including the `N passed` summary line from a BARE `python3 -m pytest`. A `V-*` item may not be marked `pass` from the matching execution checkmark. Do not move this plan to `.aw/records/plans/executed/` until `aw ipd lint --phase pre-transition` conforms and every `V-*` carries concrete pasted evidence. Note the pleasing recursion: once this plan lands, its own terminal transition is subject to the rule it adds.

On completion, close backlog `jys5dp` (this plan carries `- From-Backlog: jys5dp` and inherits its `Blocks-Release: next`).
