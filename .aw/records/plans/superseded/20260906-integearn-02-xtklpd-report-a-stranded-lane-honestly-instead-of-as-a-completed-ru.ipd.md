# IPD: Report a stranded lane honestly instead of as a completed run

- Date: 2026-09-06
- Kind: child
- Concern: When a lane's integration is not earned, every report the operator sees is technically true and practically misleading. MEASURED on run `run-20260907T010730Z-3199043` (`mm6wuz`), which left 2164 insertions across 10 files on a branch and reached main only through a hand recovery: the exit summary printed `Outcome: COMPLETED   Duration: 1h 36m 25s   Spend: $39.42` and `Progress: 1/1 [##########] 100% (1 substantially-complete)`. A run whose entire product was stranded reported COMPLETED at 100 percent, in green.
   THE MECHANISM IS A MISSING QUESTION, NOT A WRONG ANSWER. `render_run_summary_table` derives its outcome purely from per-item STATUS (find by symbol; `render_stream.py:1869-1877` at review HEAD): if every item is in `("executed", "reviewed", "approved", "substantially-complete")` the outcome is `COMPLETED`, and `substantially-complete` is in that tuple. Nothing in that computation asks whether the work INTEGRATED, so a stranded lane and a landed one are indistinguishable to it. RE-VERIFIED at review, including the green color at `:1891-1893`.
   THREE INDEPENDENT MISREPORTS, each separately fixable, all from the same incident. (1) The disposition downgrade is SILENT: the outcome file says `'disposition': 'executed'` and `reconcile_disposition` overrides it to `substantially-complete` because `plan_bucket() != "executed"` (find by symbol; `oc_runipd.py:5892-5902` at review HEAD). That override is CORRECT as an anti-fabrication guard and must stay, but a reader comparing the two records finds a contradiction with no explanation. (2) ~~The integration verdict is DISCARDED~~ **CORRECTED AT REVIEW: THE VERDICT IS ALREADY PERSISTED, AND THE REAL GAP IS THAT NO REPORT READS IT.** See F-10. (3) `Outcome: COMPLETED` contradicts `aw runs <id>`, which DOES surface the truth in its artifact-audit table (`Expected executed/ | Actual pending/`) and is how the maintainer caught this at all.
   THE PLAN'S BEST FINDING SURVIVES AND ITS MECHANISM CHANGES. Measured on the incident's own `state.json`: `attempt["integration_signal"]` is `"suite-failed"`, `attempt["integration_detail"]` is the full verdict reason ("no verifier ran (validation off) and the driver-run suite did not pass: suite FAILED with exit 1 ..."), `item["integration_signal"]` is `"suite-failed"`, and `item["preserved_branch"]` is `"aw/lane/mm6wuz_attempt3"`. Both drivers write all three (`oc_runipd.py:6475-6477`, `agy_runipd.py:3742-3744`). So the verdict, its reason AND the recovery route were all durably recorded at the time; what is missing is a READER. `grep` finds ZERO occurrences of `integration_signal` in `render_stream.py` and ZERO in `run_viewer.py`. That makes this plan SMALLER and STRONGER than authored: the summary can tell the truth from the run's OWN STATE, with no new persistence, no cross-module audit, and no filesystem lookup.
   AND THAT MATTERS MORE THAN A SIMPLIFICATION, BECAUSE THE AUTHORED APPROACH CANNOT REPRODUCE ITS OWN INCIDENT. E-04 proposes deriving the outcome from `run_viewer.audit_step_artifact`, which reads the LIVE FILESYSTEM. Measured: auditing `mm6wuz` TODAY returns `expected=executed actual=executed location_mismatch=False`, because the hand recovery moved the plan. So a filesystem-derived verdict silently REWRITES HISTORY: re-render that run's summary now and it reports COMPLETED again, which is the exact defect this plan exists to remove. A run summary must be a statement about WHAT THAT RUN DID, and only the run's own state is immutable in that way. See F-11 and OQ-04.
  WHY THIS MATTERS MORE THAN THE GATE BUG IT REPORTED ON. Sibling child `32ij2j` fixes WHY integration was not earned. The gate will be wrong again for some new reason; what must not recur is a report that hides it. An operator or agent reading only the summary would reasonably conclude the work had landed, and the honest signal lives in a different command that one must already suspect a problem to run.
- Scope: Make the run's own reporting tell the truth about integration: persist the integration verdict and its reason, record the disposition downgrade with its cause instead of silently replacing the value, emit an event for a not-earned decision, and give the exit summary a distinct outcome for "verified but NOT integrated" that names the recovery route. Do NOT weaken the anti-fabrication override; only its silence is the defect.
- Scope-Paths: agent_workflows/oc_runipd.py, agent_workflows/agy_runipd.py, agent_workflows/render_stream.py, agent_workflows/run_viewer.py, tests/test_render_stream.py, tests/test_run_viewer.py, tests/test_oc_runipd.py
- Item-Dependencies: none
- Status: superseded
- Readiness: no-go
- Set: integearn
- Order: 2
- Highest E allocated: 06
- Author: opencode its_direct/pt3-claude-opus-5-1m-us
- Id: xtklpd
- From-Backlog: 7m0aro
- Blocks-Release: next

## Workflow history
- 2026-09-08 superseded (opencode/its_direct/pt3-claude-opus-5-1m-us): SUPERSEDED by ys1dor. The diagnosis was sound; both prescribed MECHANISMS were measured structurally unavailable at review (verdict REJECT - NEEDS REPLAN, Readiness no-go), so a replacement was authored on the maintainer's 2026-09-08 rulings rather than revising this plan in place. Its evidence is carried forward into ys1dor.
- 2026-09-08 reviewed (opencode/its_direct/pt3-claude-opus-5-1m-us): /plan-review REVIEWED - OPEN QUESTIONS; readiness NO-GO. PR-023..PR-030, seven FIXED and one OPEN. THE DIAGNOSIS IS CORRECT AND RE-VERIFIED AT EVERY POINT; ONE DATA SOURCE IS WRONG. THE BLOCKER (PR-023, new F-11): E-04 derives the outcome from `run_viewer.audit_step_artifact`, which reads the LIVE FILESYSTEM (it globs the plans tree and parses the plan's `- Status:`). Measured, auditing this plan's own incident `mm6wuz` TODAY returns `expected=executed actual=executed location_mismatch=False`, because the hand recovery moved the plan, so re-rendering that run's summary would report COMPLETED again: the exact defect the plan exists to remove. A verdict about a FINISHED run that changes as the tree moves is worse than the bug being fixed, and it makes E-06's fixture unbuildable without pinning to live plan files, the anti-pattern behind this suite's other failure. Two further measured obstacles: `audit_step_artifact` takes a 21-field `StepSummary` while the renderer holds raw queue dicts, and `render_run_summary_table`'s docstring declares it a PURE renderer that 'must not import a runner'. Escalated as OQ-04, recommending derivation from run state. THE BEST FINDING IS THAT THE PLAN UNDERSTATES ITS OWN POSITION (PR-024, new F-10, F-5 WITHDRAWN AS FALSE): the verdict is NOT discarded. Measured on the incident's state.json, `attempt['integration_signal']` is 'suite-failed', `attempt['integration_detail']` holds the full reason, `item['integration_signal']` mirrors it, and `item['preserved_branch']` is 'aw/lane/mm6wuz_attempt3'; both drivers write all of them (`oc_runipd.py:6475-6477`, `agy_runipd.py:3742-3744`). The field the plan inspected, `attempt['integration']`, is a DIFFERENT one written only when integration was ATTEMPTED. And `grep` finds ZERO readers of `integration_signal` in `render_stream.py` or `run_viewer.py`. So the defect is a missing READER, not missing persistence, which makes this plan SMALLER and STRONGER: no new persistence, no cross-module coupling, no filesystem access. E-01 shrinks to exposing a machine-readable `earned` boolean so the consumer need not hardcode a signal-name list. ALSO FIXED: an ABSENT verdict must not read as stranded (PR-025), since the gate records one only when relevant (`self_finalize` on, not review, success disposition), so otherwise every review sweep would report the new label, the pessimistic failure this plan's own gate forbids; the precedence list omitted the TWO branches that come FIRST, `exit_reason` (short-circuits everything) and INTERRUPTED, and did not note that `integration-blocked`/`merge-conflict` ALREADY yield FAILED or that an unknown label falls through to CYAN (PR-026); `format_discrepancy_table` DOES NOT EXIST (the renderer is `format_artifact_audit_summary`) and `yocdq4` is a BACKLOG id with no plan, so the summary must offer only the preserved branch (PR-027); citations drifted ~73 lines in `render_stream.py` and ~78 in `oc_runipd.py` though substance re-verified at each coordinate (PR-029); and the renderer has SIX call sites across both drivers while E-02's event emission is per-driver and must be done twice (PR-030). DISCLOSURE (PR-028, F-15): the baseline is now `2 failed, 5650 passed` versus the authored `1 failed, 5612 passed`, and THIS REVIEWER CAUSED the second failure: `test_plan_readiness::ApprovalGateRealCorpusTests::test_no_pending_plan_is_refused_on_a_verdict_today` asserts no pending plan carries a negative verdict, and sibling `32ij2j` now does after the REJECT verdict from this same session. Same test-design flaw as the long-standing `RealRepositorySets` failure: pinned to the live mutable plan corpus. Neither failure is this plan's and neither is fixed here. E-02, E-03 and the reporting-honesty goal survive unchanged and were re-verified, which is why this is one blocking question rather than a replan.

- 2026-09-06 to-review (opencode its_direct/pt3-claude-opus-5-1m-us): Graduated from backlog `7m0aro`, filed from the same measured incident as its sibling: a $39.42 lane reported COMPLETED at 100 percent while all of its work sat on a branch. Every claim re-verified against code at HEAD `6f62419b` rather than trusted. CONFIRMED: the outcome string is computed from per-item status alone (`render_stream.py:1795-1803`) with `substantially-complete` inside the COMPLETED tuple and no integration term anywhere in the expression; `COMPLETED` is additionally rendered GREEN (`:1817-1819`), so the visual signal agrees with the wrong verdict. FOUND WHILE AUTHORING, and it makes the fix much smaller than the backlog item assumed: `run_viewer.audit_step_artifact` (`:465`) ALREADY computes exactly the missing fact, returning a `StepArtifactAudit` (`:425-435`) carrying `location_mismatch`, `expected_dir` and `actual_dir`, and `format_discrepancy_table` (`:1341`) already renders it. So the truth is already computed and already displayed in ONE view; the defect is that the exit summary does not consume it. That reframes E-04 from "compute integration state" to "consume the existing audit", which is why this plan does not add a second source of truth. DELIBERATELY NOT IN SCOPE: why integration was not earned is `32ij2j` (integearn-01); this child assumes the gate may refuse for ANY reason and only insists the refusal be visible.

## Goal

Make a run that strands its work say so, in the place the operator actually looks, so a near-total loss can never again read as a completed run at 100 percent.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: stop discarding what the runner already decided

- [ ] E-01 ESTABLISH WHAT IS ALREADY PERSISTED BEFORE WRITING ANYTHING, then add ONLY the missing piece: an explicit `earned` boolean.
  THE PREMISE THIS ITEM WAS AUTHORED ON IS FALSE, MEASURED (F-10). It claimed `attempt["integration"]` is `None` so "the record keeps the decision's INPUT and drops the decision itself". Measured on the incident's own `state.json`: `attempt["integration_signal"] == "suite-failed"`, `attempt["integration_detail"]` holds the FULL verdict reason, `item["integration_signal"] == "suite-failed"`, and `item["preserved_branch"] == "aw/lane/mm6wuz_attempt3"`. Both drivers write all three unconditionally when the gate is relevant (`oc_runipd.py:6475-6477`, `agy_runipd.py:3742-3744`). The key the plan inspected (`attempt["integration"]`) is a DIFFERENT field, written only on the integrate-attempted path (`:6636`) and legitimately absent when integration was never attempted. So the verdict and its reason are ALREADY DURABLE on both paths.
  WHAT IS GENUINELY MISSING is a machine-readable `earned` boolean: today a reader must infer it by comparing `integration_signal` against the `INTEGRATION_EARNED_BY_*` / `INTEGRATION_REFUSED_*` constants. Add that boolean (and nothing else), so E-04's consumer does not hardcode a signal-name list that drifts when a new refusal reason is added. If the maintainer prefers a documented predicate over a stored field, that is equally acceptable and cheaper; say which was chosen.
  KEEP THE EXISTING KEYS AND THEIR MEANINGS. `suite_check`, `integration_signal`, `integration_detail`, `integration_deferral` and `preserved_branch` are already written and read; add rather than repurpose. Do NOT rename `integration_signal` to make room for a new field: it is written by both drivers and persisted in 135 existing run records.
  - Depends on: none
  - Expected outcome: the already-persisted fields are documented in the plan's own evidence and left unchanged; a single `earned` boolean (or an equivalent shared predicate) makes the verdict machine-readable without a signal-name list at the call site; no existing key changes meaning.
  - Execution state: pending

- [ ] E-02 Emit a DURABLE EVENT for a not-earned integration decision, so the EVENT LOG shows the fork rather than falling silent. RE-VERIFIED at review: the incident's `events.jsonl` holds exactly ten events, ending `lane-submissions-collected` / `worktree-preserved` / `ipd-finished`, and none names an integration decision. This finding STANDS (unlike E-01's premise): the decision is in `state.json` but not in the event stream, and the event stream is what a reader tails.
  FOLLOW THE EXISTING EVENT SHAPE, including a `detail` field. Every neighbouring event type carries one; the `dependency-blocked` event's lack of a detail is the same defect one layer over (backlog `phawyy`), so do not reproduce it here.
  NAME THE LANE AND THE RECOVERY ROUTE in the detail, because an event a reader cannot act on is only marginally better than silence: state the preserved branch and that the work is recoverable from it. Both inputs already exist in state (`item["preserved_branch"]`, `attempt["integration_detail"]`), so this is composition rather than new computation.
  EMIT IT ON BOTH HOSTS. The event emission sites are per-driver, so this is one of the items that must be done twice; `agy_runipd.py:3742-3744` is the mirror of `oc_runipd.py:6475-6477`.
  - Depends on: E-01
  - Expected outcome: a `not-earned` integration decision emits an event carrying the id6, the reason, and the preserved lane branch, on BOTH drivers; the event log no longer goes silent at the fork.
  - Execution state: pending

- [ ] E-03 Record the DISPOSITION DOWNGRADE explicitly instead of silently replacing the value. `reconcile_disposition` (find by symbol; `oc_runipd.py:5892-5902` at review HEAD) reads `plan_bucket(current_plan)` and, when the plan is not in `executed/`, overrides an outcome file claiming `disposition: executed` down to `substantially-complete`. RE-VERIFIED at review, including that the `bucket == "executed"` early return precedes it.
  THE OVERRIDE IS CORRECT AND MUST NOT BE WEAKENED. The runner must not take an agent's word that a plan was finalized; that is the anti-fabrication guard, and this repository's history of agents over-claiming completion is exactly why it exists. This item changes only its SILENCE.
  WRITE THE FACT AND THE CAUSE, for example an `attempt["disposition_downgraded"]` naming the claimed value, the recorded value, and the observed reason (the plan's actual bucket). A reader comparing the outcome file against the queue entry must find an explanation rather than a bare contradiction.
  DO NOT DERIVE THE CAUSE FROM A GUESS. The bucket is the observed fact the override actually used; record that, not an inference about why finalize did not run.
  - Depends on: E-02
  - Expected outcome: a downgrade is recorded with the claimed value, the recorded value and the observed bucket; the override's behavior is byte-identical to today.
  - Execution state: pending

### Task group 2: make the summary agree with the truth

- [ ] E-04 Give `render_run_summary_table` a DISTINCT OUTCOME for "verified but NOT integrated", derived FROM THE RUN'S OWN STATE (the integration verdict E-01 exposes), NOT from a live filesystem audit. Today the outcome is computed from per-item status alone (find by symbol; `render_stream.py:1869-1877` at review HEAD) and `substantially-complete` sits inside the COMPLETED tuple, so a stranded lane is indistinguishable from a landed one.
  THE AUTHORED DATA SOURCE IS WRONG AND WOULD REWRITE HISTORY (F-11, OQ-04). This item said to consume `run_viewer.audit_step_artifact`, which reads the LIVE FILESYSTEM (it globs the plans tree via `find_artifact_file` and reads the plan's `- Status:`). Measured: auditing the incident's `mm6wuz` TODAY returns `expected=executed actual=executed location_mismatch=False`, because the recovery moved the plan. So re-rendering that run's summary would report COMPLETED again, and a summary that changes its verdict about a FINISHED run as the tree moves under it is a worse defect than the one being fixed. The run's own state is the immutable record of what the run did, and it ALREADY contains the answer (`integration_signal`, `integration_detail`, `preserved_branch`).
  TWO MORE REASONS THE AUDIT IS THE WRONG SOURCE HERE, both measured. It is SHAPE-INCOMPATIBLE: `audit_step_artifact` takes a `StepSummary` dataclass (21 fields) while the renderer holds raw `state["queue"]` dicts, so consuming it means constructing `StepSummary` objects inside a renderer. And it would BREAK A STATED INVARIANT: `render_run_summary_table`'s own docstring says it is deliberately "a PURE renderer: it must not import a runner", which is why it reads `state["run_order"]` instead of calling `queue_sort_key`. Importing `run_viewer` to reach the filesystem is the same violation in a new direction. Deriving from state preserves purity by construction.
  FIX THE COLOR TOO. `COMPLETED` is rendered green (`:1891-1893`), so today the visual signal reinforces the wrong verdict. A stranded outcome must not be green. Note the color expression keys on the outcome STRING (`"INTERRUPT" in`, `"STOP" in`, `== "PARTIAL"`, `"FAIL" in`, else cyan), so a new label falls through to CYAN automatically; choose the label and the color deliberately rather than relying on that fallthrough, and do not pick a label containing `FAIL` unless you intend red.
  NAME THE RECOVERY ROUTE in the summary, since a bare label leaves the operator stuck: state that the lane branch is preserved (it is in `item["preserved_branch"]`) and how to recover. Backlog `yocdq4` (the `integrate` verb) is GRADUATED but has no plan yet, so the summary must read correctly with only the preserved branch to offer, and must not reference a verb that does not exist.
  BE CAREFUL WITH THE PRECEDENCE ORDER, AND NOTE THE PLAN'S LIST WAS INCOMPLETE. Measured, the real order is: `exit_reason` (an explicit override that SHORT-CIRCUITS everything, used for STOPPED/wind-down), then INTERRUPTED, then FAILED (`failed-safely`/`integration-blocked`/`merge-conflict`), then BLOCKED, then COMPLETED, then PARTIAL, then QUEUED. The plan omitted both `exit_reason` and INTERRUPTED. A stranded run must not mask any of them, and note `integration-blocked` and `merge-conflict` ALREADY report FAILED, so this new label is specifically for the case where integration was never ATTEMPTED.
  - Depends on: E-03
  - Expected outcome: a run whose items are `substantially-complete` with a not-earned integration verdict IN ITS OWN STATE reports a distinct non-green outcome naming the stranded state and the recovery route; `exit_reason`, INTERRUPTED, FAILED and BLOCKED all still take precedence; no filesystem lookup and no `run_viewer` import is added to the renderer; re-rendering an OLD run record yields the SAME verdict it did at the time.
  - Execution state: pending

### Task group 3: prove it

- [ ] E-05 Test the RECORD, deterministically and without running a real lane. Assert: the verdict is machine-readable on BOTH paths per E-01 (whether via the new `earned` boolean or the shared predicate, and asserting the ALREADY-PERSISTED `integration_signal`/`integration_detail` are untouched); the not-earned event exists and carries a `detail` naming the lane branch, ON BOTH DRIVERS; a downgrade writes the claimed value, the recorded value and the observed bucket; and `reconcile_disposition`'s RETURN VALUE is unchanged for every input it handles today.
  THE LAST ASSERTION IS THE SAFETY ONE. This item touches the anti-fabrication override, so a test must prove the override still downgrades exactly as before. Drive it with an outcome file claiming `executed` and a plan in `pending/`, and assert `substantially-complete` is still returned.
  ASSERT BACKWARD COMPATIBILITY: an OLDER run record lacking every new key must still render without raising, which is the rule the `launch_profile` record already follows.
  - Depends on: E-04
  - Expected outcome: the verdict, the event, and the downgrade record are each pinned; the override's return values are unchanged; an older record still renders.
  - Execution state: pending

- [ ] E-06 Test the SUMMARY against the real incident, so the test documents the bug it prevents. Reconstruct `mm6wuz`'s shape FROM ITS RUN STATE, which is the shape that is stable: one item, status `substantially-complete`, `integration_signal: "suite-failed"`, `integration_detail` naming the failed suite, `preserved_branch: "aw/lane/mm6wuz_attempt3"`. Assert the summary does NOT say `COMPLETED`, does NOT render green, names the stranded state, and names the recovery route.
  DO NOT PIN THE FIXTURE TO A PLAN'S LOCATION ON DISK. The authored version said to place the plan in `pending/` "while the audit expects `executed/`", which is unbuildable as a durable test for the reason F-11 records: the real `mm6wuz` plan is in `executed/` today, so a filesystem-shaped fixture must either fabricate a tree or pin to live plan locations. Pinning to live plan files is exactly what broke `tests/test_orchestrator_retirement.py::RealRepositorySets`, this suite's long-standing failure, which asserts a sibling plan's mutable status. Build the fixture from a synthesized state dict.
  ASSERT THE CONTRAST, which is what makes the test meaningful: the SAME item with an EARNED verdict must still report `COMPLETED` and stay green. A test that only checks the stranded case would pass if the outcome were hardcoded to the new label.
  ASSERT PRECEDENCE with three cases, not one: a mixed queue with a genuinely failed item must report FAILED; a run carrying an `exit_reason` must still report that reason (it short-circuits everything); and an `interrupted` item must still report INTERRUPTED. The plan's original precedence list omitted the latter two.
  ASSERT THE HISTORY PROPERTY, which is this item's most important assertion and the one the authored design would have failed: render a summary from a stored state whose plan has SINCE been moved to `executed/` (the post-recovery `mm6wuz` situation) and assert the verdict is STILL the stranded one. That is the difference between reporting what the run did and reporting what the tree looks like now.
  Run the suite BARE (`python3 -m pytest`) and state before/after counts. VALIDATE IN THE REAL CHECKOUT: `tests/test_run_viewer.py` reads the gitignored `.aw/records/runs/`, so it fails in a bare worktree and passes in the real checkout; green elsewhere proves nothing, and that file is in this plan's Scope-Paths.
  - Depends on: E-05
  - Expected outcome: the incident's state-shaped fixture reports honestly and non-green; an earned verdict still reports COMPLETED; `exit_reason`, INTERRUPTED and FAILED all still win; a since-moved plan does NOT change the stored run's verdict; bare suite delta empty with counts stated.
  - Execution state: pending

## Project conventions discovered (Step 0)

- THE OUTCOME STRING IS COMPUTED FROM STATUS ALONE. `render_stream.py:1869-1877` (at review HEAD) tests membership in `("executed", "reviewed", "approved", "substantially-complete")` and has no integration term, which is the entire defect. `COMPLETED` is then colored green at `:1891-1893`. The full precedence is `exit_reason` -> INTERRUPTED -> FAILED -> BLOCKED -> COMPLETED -> PARTIAL -> QUEUED; the color expression keys on the outcome STRING and falls through to CYAN for an unknown label.
- **THE VERDICT IS ALREADY PERSISTED (CORRECTED AT REVIEW).** Both drivers write `attempt["integration_signal"]`, `attempt["integration_detail"]` and `item["integration_signal"]` (`oc_runipd.py:6475-6477`, `agy_runipd.py:3742-3744`), and `item["preserved_branch"]` carries the recovery route. Verified present in the incident's own `state.json`. `grep` finds ZERO readers of `integration_signal` in `render_stream.py` or `run_viewer.py`, so the defect is a missing READER, not missing persistence. `attempt["integration"]` is a DIFFERENT field, written only when integration was ATTEMPTED (`:6636`).
- **DO NOT DERIVE A RUN'S VERDICT FROM THE LIVE FILESYSTEM.** `run_viewer.audit_step_artifact` globs the plans tree and reads the plan's `- Status:`, so its answer CHANGES as the tree moves. Measured: auditing the incident's `mm6wuz` today returns `location_mismatch=False` because the recovery moved the plan, so a filesystem-derived summary would report COMPLETED for the very run this plan exists to expose. A run summary must be a statement about what THAT RUN did; only the run's own state is immutable in that way. The audit remains correct for `aw runs`, which answers a different question ("is the tree consistent NOW").
- `render_run_summary_table` IS DELIBERATELY A PURE RENDERER. Its docstring states it "must not import a runner", which is why it reads `state["run_order"]` rather than calling `queue_sort_key`. It also takes raw `state["queue"]` dicts, while `audit_step_artifact` takes a 21-field `StepSummary`. Deriving from state respects both constraints; importing `run_viewer` violates the first.
- IT IS ONE RENDERER WITH SIX CALL SITES, three per driver (`oc_runipd.py:7267`, `:8124`, `:8170`; `agy_runipd.py:4489`, `:5078`, `:5124`). The outcome logic is shared, so E-04 is a single edit, but any change to the FUNCTION SIGNATURE would touch all six.
- THE AUDIT'S REAL RENDERER IS `format_artifact_audit_summary` (`run_viewer.py:1337`), NOT `format_discrepancy_table`, which does not exist anywhere in the package.
- `substantially-complete` IS IN `EXECUTION_SUCCESS_STATES` (`oc_runipd.py:321`), so it legitimately reads as a success variant. The fix is not to reclassify the status but to stop treating success-of-the-turn as success-of-the-run.
- THE ANTI-FABRICATION OVERRIDE IS DELIBERATE. `reconcile_disposition` trusts the DIRECTORY over the agent's outcome file. Keep it; record it.
- THE ATTEMPT RECORD ALREADY PERSISTS DECISION INPUTS (`suite_check`) and already has a place for the decision (`attempt["integration"]`, currently `None`), so persistence needs no new structure.
- EVENTS CARRY A `detail` FIELD BY CONVENTION, and the `dependency-blocked` event's lack of one is a known defect (`phawyy`). Do not add a second detail-less event.
- BACKWARD COMPATIBILITY RULE: `run_viewer` must read a new field defensively so an older record still renders, as the `launch_profile` record already requires.
- `tests/test_run_viewer.py` READS THE GITIGNORED `.aw/records/runs/`, so it must be validated in the REAL checkout, not a bare worktree.
- Run the suite BARE: `python3 -m pytest`. The configured `addopts` already supply quiet, parallel, and the fast subset.

## Findings

| # | Finding | Evidence |
|---|---|---|
| F-1 | **THE MISREPORT, MEASURED.** A run that stranded 2164 insertions across 10 files printed `Outcome: COMPLETED`, `Progress: 1/1 [##########] 100% (1 substantially-complete)`, `Spend: $39.42`. | `run-20260907T010730Z-3199043` exit summary |
| F-2 | The outcome is derived from per-item STATUS with no integration term, and `substantially-complete` is inside the COMPLETED tuple. RE-VERIFIED at review. | `render_stream.py:1869-1877` |
| F-3 | `COMPLETED` is rendered GREEN, so the visual signal reinforces the wrong verdict. RE-VERIFIED at review; note the color keys on the outcome STRING and an unknown label falls through to CYAN. | `render_stream.py:1891-1893` |
| F-4 | **THE MISSING FACT IS ALREADY COMPUTED AND ALREADY DISPLAYED IN ONE VIEW**: `audit_step_artifact` returns `location_mismatch`/`expected_dir`/`actual_dir`, and the renderer is `format_artifact_audit_summary` (NOT `format_discrepancy_table`, which does not exist). That table is how the maintainer caught the incident. CORRECTED AT REVIEW: the audit is the right answer for `aw runs` and the WRONG source for a run summary, because it reads the live filesystem (F-11). | `run_viewer.py:465`, `:425-435`, `:1337`; `grep -rn format_discrepancy_table` -> no hits |
| F-5 | ~~The integration verdict is DISCARDED~~ **WITHDRAWN AT REVIEW: FALSE.** The verdict, its reason and the recovery route are all already persisted; the field the plan inspected (`attempt["integration"]`) is a different one, written only when integration was attempted. Superseded by F-10. | `mm6wuz` state.json: `attempt["integration_signal"]="suite-failed"`, `integration_detail` full reason, `item["preserved_branch"]="aw/lane/mm6wuz_attempt3"` |
| F-10 | **THE VERDICT IS ALREADY DURABLE AND NOTHING READS IT, WHICH MAKES THIS PLAN SMALLER AND STRONGER.** Both drivers write `attempt["integration_signal"]`, `attempt["integration_detail"]` and `item["integration_signal"]` whenever the gate is relevant, and `item["preserved_branch"]` holds the lane. All four verified present in the incident's own state. Yet `grep` finds ZERO occurrences of `integration_signal` in `render_stream.py` and ZERO in `run_viewer.py`. So no new persistence is needed: the summary can tell the truth from the run's own state, and E-01 shrinks to exposing a machine-readable `earned` boolean so the consumer need not hardcode a signal-name list. | `oc_runipd.py:6475-6477`; `agy_runipd.py:3742-3744`; `.aw/records/runs/run-20260907T010730Z-3199043/state.json`; `grep -rn integration_signal agent_workflows/render_stream.py agent_workflows/run_viewer.py` -> no hits |
| F-11 | **THE AUTHORED DATA SOURCE WOULD REWRITE HISTORY AND CANNOT REPRODUCE THIS PLAN'S OWN INCIDENT.** E-04 said to derive the outcome from `audit_step_artifact`, which globs the plans tree and reads the plan's `- Status:` from disk. Measured: auditing `mm6wuz` TODAY returns `expected=executed actual=executed location_mismatch=False`, because the hand recovery moved the plan, so re-rendering that run's summary would say COMPLETED again. A verdict about a FINISHED run that changes as the tree moves is a worse defect than the one being fixed. Two further measured obstacles: the audit takes a 21-field `StepSummary` while the renderer holds raw queue dicts, and `render_run_summary_table`'s docstring declares it a PURE renderer that "must not import a runner". | `run_viewer.audit_step_artifact(StepSummary(id6="mm6wuz", status="substantially-complete"), Path("."))` -> `location_mismatch=False`; `run_viewer.py:465`, `find_artifact_file:439`; `render_stream.py:1656` docstring; `StepSummary` field list |
| F-12 | THE PRECEDENCE LIST IN E-04 WAS INCOMPLETE, omitting the two branches that come FIRST. The real order is `exit_reason` (an explicit override that short-circuits everything, used for STOPPED/wind-down), then INTERRUPTED, then FAILED, then BLOCKED, then COMPLETED, then PARTIAL, then QUEUED. Also `integration-blocked` and `merge-conflict` ALREADY yield FAILED, so the new label is specifically for integration never ATTEMPTED. | `render_stream.py:1857-1881` |
| F-13 | THE `integrate` VERB THE SUMMARY WOULD POINT AT HAS NO PLAN. `yocdq4` is a BACKLOG id (`backlog/graduated/...-yocdq4-reintegrate-verb-and-resume.backlog.md`), not a plan in an "integpath Set"; no plan carries `- Id: yocdq4`. The summary must therefore read correctly offering only the preserved branch, and must not name a verb that does not exist. | `grep -rn '^- Id: yocdq4' .aw/records/plans/` -> empty; the backlog file is `graduated` |
| F-14 | EVERY LINE CITATION HAS DRIFTED, by roughly 73 lines in `render_stream.py` and 78 in `oc_runipd.py`: the outcome expression is `:1869-1877` (plan `:1795-1803`), the green color `:1891-1893` (`:1817-1819`), `reconcile_disposition` `:5892-5902` (`:5814-5822`), `EXECUTION_SUCCESS_STATES` `:328` (`:321`). Substance re-verified correct at each new coordinate; the plan already instructs re-location by symbol. | `grep -n` per symbol at review HEAD `8dc4e0bc` |
| F-15 | BASELINE MOVED AND A SECOND FAILURE APPEARED, DISCLOSED HONESTLY BECAUSE THIS REVIEW CAUSED IT. Measured `2 failed, 5650 passed` versus the authored `1 failed, 5612 passed`. The second failure is `tests/test_plan_readiness.py::ApprovalGateRealCorpusTests::test_no_pending_plan_is_refused_on_a_verdict_today`, which asserts NO pending plan carries a negative verdict; sibling `32ij2j` now does, because THIS reviewer's immediately preceding `/plan-review` gave it a legitimate `REJECT - NEEDS REPLAN`. It is the same test-design flaw as the long-standing `RealRepositorySets` failure: a test pinned to the live mutable plan corpus. Neither failure is this plan's, and neither is to be fixed here. | bare `python3 -m pytest` at `8dc4e0bc`; `plan_readiness.newest_verdict(32ij2j)` -> `negative`; `tests/test_plan_readiness.py:789-798` |
| F-6 | NO EVENT RECORDS THE FORK: the log ends at `worktree-preserved` / `ipd-finished` and `grep -c integrat events.jsonl` returns 0. | `run-20260907T010730Z-3199043` events.jsonl |
| F-7 | THE DOWNGRADE IS SILENT AND CONTRADICTORY: the outcome file says `disposition: executed`, the queue entry says `substantially-complete`, and nothing records that an override occurred or why. | `oc_runipd.py:5814-5822`; `mm6wuz` outcome vs queue entry |
| F-8 | The override reads the DIRECTORY, which is the same bucket-versus-status confusion filed as `yf9fj9` for the dependency gate. Recording the observed bucket keeps this plan honest about what the override actually used. | `oc_runipd.py:5815`; backlog `yf9fj9` |
| F-9 | `substantially-complete` is a legitimate success variant (`EXECUTION_SUCCESS_STATES`), so the fix must not reclassify the status; only the run-level verdict is wrong. RE-VERIFIED at `:328`. | `oc_runipd.py:328` |

## Proposed changes (ordered, validatable)

1. Persist the integration verdict and reason on both paths (E-01).
2. Emit a not-earned integration event carrying a `detail` that names the lane and the recovery route (E-02).
3. Record the disposition downgrade with its claimed value, recorded value and observed bucket, leaving the override's behavior unchanged (E-03).
4. Give the summary a distinct non-green outcome for a stranded run by consuming the existing audit, preserving FAILED/BLOCKED precedence (E-04).
5. Pin the record: verdict, event, downgrade, unchanged override return values, older-record rendering (E-05).
6. Pin the summary against the real incident plus the landed contrast and the precedence case (E-06).

## Deferred / out of scope (with reason)

- WHY INTEGRATION WAS NOT EARNED. That is sibling `32ij2j` (integearn-01), which changes the gate from absolute-green to a delta against the frozen base. This child deliberately assumes the gate may refuse for ANY reason and only insists the refusal be visible, so the two can be reviewed and verified independently.
- WEAKENING OR REMOVING THE ANTI-FABRICATION OVERRIDE. Explicitly counter to the goal: the override is right, its silence is the defect.
- RECLASSIFYING `substantially-complete`. It is in `EXECUTION_SUCCESS_STATES` and legitimately describes the TURN's outcome; changing its membership would ripple through dependency satisfaction and retry eligibility for no gain here.
- THE `dependency-blocked` EVENT'S MISSING REASON. Same class of defect, different code path, already filed as `phawyy`. Named here so the executor does not fix half of it incidentally and leave the record inconsistent.
- THE `integrate` VERB the summary will point at. `yocdq4` is a BACKLOG item (`graduated`), NOT a plan in an "integpath Set": no plan carries that id (F-13). So the summary must name only what exists today, the preserved lane branch, and must NOT reference a verb that does not exist. Corrected at review.
- THE STALE-BASE SCOPE MISATTRIBUTION (`hyx1dg`). Same incident, unrelated mechanism.

## Scope check

- Over-scope: none. Two drivers, the summary renderer, the viewer, and three test modules.
- Scope-Paths justification: `oc_runipd.py` holds `reconcile_disposition` and the attempt record (E-01, E-03) plus the event emission (E-02); `agy_runipd.py` needs the same three changes at its own sites; `render_stream.py` holds the outcome computation and its color (E-04); `run_viewer.py` holds the audit being consumed and must read new fields defensively; `tests/test_render_stream.py` covers the summary, `tests/test_run_viewer.py` the audit consumption, `tests/test_oc_runipd.py` the record and the override.
- BOTH DRIVER MODULES ARE THE HIGHEST-CONTENTION FILES IN THE REPOSITORY. Expect drift and re-locate by symbol.
- Under-scope, stated rather than left as `none`: this child does not change WHEN integration is earned, does not touch the override's behavior, does not reclassify any status, does not fix the `dependency-blocked` event, and does not build the `integrate` verb. Each is excluded with a reason above.

## Required tests / validation

- `python3 -m pytest` BARE, before and after, with the `N passed` summary line pasted and counts stated. MEASURE YOUR OWN BEFORE-BASELINE: at authoring it was `1 failed, 5612 passed, 3 skipped, 2 xfailed` at HEAD `6f62419b`; RE-MEASURED at review it is `2 failed, 5650 passed, 3 skipped, 2 xfailed` at HEAD `8dc4e0bc`. TWO pre-existing failures now, both pinned to the live mutable plan corpus and NEITHER this plan's: `test_orchestrator_retirement::RealRepositorySets` (asserts a sibling plan's status) and `test_plan_readiness::ApprovalGateRealCorpusTests::test_no_pending_plan_is_refused_on_a_verdict_today` (asserts no pending plan carries a negative verdict, which sibling `32ij2j` now legitimately does after its review). Trust neither total. The criterion is that the AFTER failure set minus the BEFORE failure set is EMPTY.
- Targeted: `tests/test_render_stream.py`, `tests/test_run_viewer.py`, `tests/test_oc_runipd.py`.
- VALIDATE `tests/test_run_viewer.py` IN THE REAL CHECKOUT. It reads the gitignored `.aw/records/runs/` and fails in a bare worktree; it is in this plan's Scope-Paths, so a bare-worktree run would report a false failure.
- A RENDERED-OUTPUT PASTE of the summary for the stranded case AND the landed case, side by side, with ANSI stripped so the color difference is visible as a code rather than as a claim.
- MEASURE EXIT CODES UNPIPED (`cmd >/dev/null 2>&1; echo $?`).
- `aw sanitize --agent` clean.

## Spec / documentation sync

Spec `25kzda` governs the run's reporting surface. This plan ADDS an outcome label and record fields; if the spec enumerates run outcomes, the new one must be reflected there, and if it does not, no spec change is needed. Verify by reading rather than assuming, and do NOT edit the spec's §4.2 finding-code table under any circumstances: it is transcribed verbatim into `run_evidence.RUN_FINDING_CODES` with a byte-equality test, so editing a cell IS a code change.

`render_run_summary_table`'s docstring must state that the outcome now depends on integration state and not on item status alone, since that docstring is where a future reader will look to understand the verdict.

Any user-facing text describing the new outcome must name the recovery route plainly and must not imply the work is lost: the lane branch is preserved and the commits are recoverable. Write no em or en dashes in user-facing prose.

## Open questions

### OQ-01: What should the new outcome be called?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: A LABEL THAT NAMES THE STATE, not a severity word. The operator's question is "did my work land", so the label must answer it: something of the form `NOT INTEGRATED` (optionally qualified, e.g. `VERIFIED, NOT INTEGRATED`) rather than `WARNING` or `PARTIAL`. `PARTIAL` is already taken by a different meaning in the same expression (some items completed, others did not) and reusing it would collapse two distinct facts. The exact string is the executor's choice within that constraint; what E-06 pins is that it is NOT `COMPLETED`, is NOT green, and names both the stranded state and the recovery route.

### OQ-02: Should a stranded run exit nonzero?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: OUT OF SCOPE FOR THIS PLAN, and deliberately not changed. Exit codes are a contract other tooling and CI may already depend on, and spec `25kzda` owns the run's exit-code table; changing one is a separate, wider decision with its own compatibility surface. This plan fixes what the run SAYS, which is the defect that cost a near-total loss its visibility. If the maintainer wants a nonzero exit for a stranded run, that is a follow-up item against the exit-code table, and it should be decided with the spec in hand rather than as a side effect of a reporting fix.

### OQ-03: Should the summary consume `run_viewer`'s audit directly, given `render_stream` does not import it today?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: SUPERSEDED AT REVIEW BY OQ-04. The question assumed the audit is the only existing source of truth, so consuming it was the only alternative to a second implementation. Measured, that assumption is false: the run's OWN STATE already carries `integration_signal`, `integration_detail` and `preserved_branch` (F-10), so there is a third option the question did not consider, and it is neither a `run_viewer` import nor a reimplementation of the location check. The reasoning that MATTERS here survives intact and is worth keeping: ONE source of truth, and never a second independent location check. It simply points at state rather than at the audit. See OQ-04 for the live decision.

### OQ-04: Should the summary's verdict come from the run's OWN STATE or from the live filesystem audit?

- Blocking: yes
- Status: open
- Owner: maintainer
- Finding: PR-023
- Question: E-04 as authored derives the outcome from `run_viewer.audit_step_artifact`, which globs the plans tree and reads the plan's `- Status:` from disk. Measured, that cannot reproduce this plan's own incident: auditing `mm6wuz` TODAY returns `location_mismatch=False` because the hand recovery moved the plan, so re-rendering that run's summary would report COMPLETED again, which is the exact defect the plan exists to remove. Meanwhile the run's own state ALREADY records the verdict (`attempt["integration_signal"]="suite-failed"`, `integration_detail` with the full reason, `item["preserved_branch"]="aw/lane/mm6wuz_attempt3"`), and nothing reads it. Options: (a) derive the outcome FROM RUN STATE, so a summary is an immutable statement about what that run did, requiring no `run_viewer` import (preserving the renderer's stated purity), no `StepSummary` construction, and no filesystem access; (b) keep the audit as authored, accepting that a stranded run's verdict silently flips to COMPLETED once the work is recovered, and that the renderer gains a filesystem dependency and a runner import against its docstring; (c) use BOTH, state for the run verdict and the audit for a supplementary "and the tree currently disagrees" note, which is more honest but is two mechanisms and two failure modes. Recommendation: (a), with (c) as a later enhancement if the maintainer wants tree drift surfaced in the summary too. This changes E-04's data source, E-06's fixture shape, and shrinks E-01, which is why it is blocking rather than an executor's detail.
- Resolution or deferral rationale: OPEN. It decides where the run's verdict comes from and whether a finished run's summary is allowed to change its answer later, which is a contract question rather than an implementation detail.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: FIRST paste the fields that ALREADY exist, from a real run state, so the item's premise is on the record: `attempt["integration_signal"]`, `attempt["integration_detail"]`, `item["integration_signal"]` and `item["preserved_branch"]` for a not-earned decision (F-10). THEN paste the machine-readable `earned` verdict for BOTH an earned and a not-earned decision, and state which form was chosen (a stored boolean or a shared predicate). Confirm `suite_check`, `integration_signal`, `integration_detail` and `integration_deferral` are unchanged in shape and meaning by pasting them alongside. State in one sentence why the reason text is not asserted verbatim (sibling `32ij2j` enriches it).
  - Observed evidence:
  - Result: pending

- [ ] V-02 validates E-02
  - Required evidence: paste the new event line from a real `events.jsonl` for a not-earned decision, showing the id6, the reason, and the preserved lane branch in its `detail`. Paste `grep -c integrat events.jsonl` returning nonzero for that run, contrasted with the measured 0 from `run-20260907T010730Z-3199043`. Confirm the event carries a `detail` field, naming the `phawyy` defect this avoids repeating.
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: paste the downgrade record showing the claimed value (`executed`), the recorded value (`substantially-complete`), and the OBSERVED BUCKET. THEN paste the safety proof: `reconcile_disposition` driven with an outcome file claiming `executed` and a plan in `pending/` still returns `substantially-complete`, byte-identical to today. A paste that does not demonstrate the override still firing is a FAILED validation.
  - Observed evidence:
  - Result: pending

- [ ] V-04 validates E-04
  - Required evidence: paste the rendered summary (ANSI STRIPPED, and separately the raw escape codes) for a stranded run, showing the outcome is NOT `COMPLETED`, is NOT green, and names both the stranded state and the recovery route. State which OQ-04 option was implemented. If (a), paste an AST or grep proof that `render_stream.py` gained NO `run_viewer` import and performs NO filesystem access in the outcome computation, since the renderer's docstring declares it pure; if (b) or (c), paste the audit consumption and quote the line proving no second location check was written. Paste FOUR precedence cases, not one: a stranded item plus a failed item reports FAILED; a run with an `exit_reason` still reports that reason; an `interrupted` item still reports INTERRUPTED; and the label's color was chosen deliberately rather than falling through to cyan (quote the color branch it takes).
  - Observed evidence:
  - Result: pending

- [ ] V-05 validates E-05
  - Required evidence: paste the ACTUAL output of the record tests: verdict on both paths, the event with its detail, the downgrade record, and the unchanged-override assertion. Paste the backward-compatibility test rendering an OLDER run record that lacks every new key, still passing.
  - Observed evidence:
  - Result: pending

- [ ] V-06 validates E-06
  - Required evidence: paste the incident-shaped test built from a SYNTHESIZED STATE (one item `substantially-complete`, `integration_signal: "suite-failed"`, `preserved_branch` set) and its assertions; a fixture pinned to a plan's live on-disk location does NOT satisfy this item, because the real `mm6wuz` plan is in `executed/` today and pinning to live plan files is what broke `RealRepositorySets` (F-11, F-15). Paste the CONTRAST case with an EARNED verdict still reporting `COMPLETED` and still green; without that contrast the test would pass on a hardcoded label. PASTE THE HISTORY-PROPERTY CASE: render a stored state whose plan has SINCE been moved to `executed/` and show the verdict is STILL the stranded one; if OQ-04 resolved to (b), state plainly that this assertion cannot hold and what was done instead. Paste the BARE `python3 -m pytest` summary with before/after counts and show the AFTER-minus-BEFORE failure set is empty, comparing NODE IDS not totals (two pre-existing failures at review, neither this plan's). State that `tests/test_run_viewer.py` was validated in the REAL checkout and why that matters here.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

EXECUTION IS BLOCKED ON OQ-04, and human approval alone does not clear it. It carries `- Blocking: yes`, so the pre-execution checkpoint refuses while it is open (verified at review that `plan_readiness.has_unresolved_blocking_question` returns True and names it). The question is whether the summary's verdict comes from the run's own state or from a live filesystem audit, and it is not an executor's call because option (b) means a finished run's verdict can silently flip to COMPLETED once the work is recovered, which is the defect this plan exists to remove. It changes E-04's data source, E-06's fixture shape, and shrinks E-01.

THE PLAN'S DIAGNOSIS AND ITS THREE OTHER ITEMS ARE SOUND AND UNAFFECTED. E-02 (the missing event) was re-verified as a genuine gap, E-03's override analysis re-verified correct, and the summary defect re-verified at its drifted coordinates. Only the DATA SOURCE for E-04 is unsettled, which is why this is a blocking question on an otherwise good plan rather than a replan.

Scope fence: touch ONLY the seven paths in `Scope-Paths`. Note that if OQ-04 resolves to (a), `run_viewer.py` may end up UNMODIFIED; that is fine and `aw ipd finalize` accepts it with a `--scope-ack` for that path rather than requiring a token edit. Do NOT change WHEN integration is earned (that is `32ij2j`). Do NOT weaken, bypass, or remove the `reconcile_disposition` override. Do NOT reclassify `substantially-complete` or alter `EXECUTION_SUCCESS_STATES`. Do NOT change any run exit code (OQ-02). Do NOT write a second implementation of the artifact-location check. Do NOT edit spec `25kzda`, and never its finding-code table. If the work GENUINELY requires a path outside the fence, MAKE THE EDIT AND JUSTIFY IT, since `aw ipd finalize` refuses to complete until every out-of-scope path carries a `--scope-reason` and every declared-but-unmodified path carries a `--scope-ack`.

Commit ONLY the files you changed, path-scoped (`git commit -m msg -- <paths>`), never `git add -A`, never push. Verify with `git diff --cached --name-only` before every commit and RE-VERIFY after any failed hook. THIS IS A SHARED CHECKOUT and both driver modules are the highest-contention files in it: run `aw runs` before starting, and if a driver file is being changed under you and the two sets of changes cannot be safely combined, STOP and report rather than overwriting.

HARD HONESTY RULE: tests must be RUN and their ACTUAL output pasted into `Observed evidence`, including the `N passed` summary line from a BARE `python3 -m pytest`. A `V-*` item may not be marked `pass` from the matching execution checkmark. Do not move this plan to `.aw/records/plans/executed/` until `aw ipd lint --phase pre-transition` conforms and every `V-*` carries concrete pasted evidence.

BASELINE HONESTY: the suite is NOT green at HEAD and this plan does not make it green. Judge yourself on the DELTA by comparing NODE IDS, never totals. There are TWO pre-existing failures as of review, and NEITHER is yours: `test_orchestrator_retirement::RealRepositorySets` and `test_plan_readiness::ApprovalGateRealCorpusTests::test_no_pending_plan_is_refused_on_a_verdict_today`. Both are pinned to the live mutable plan corpus, so both can appear or vanish from ordinary plan-status changes; the second appeared because sibling `32ij2j` legitimately received a negative review verdict. Do not fix either here, and do not report either as caused by this change.

RE-LOCATE BY SYMBOL, NEVER BY THE LINE NUMBERS IN THIS PLAN, which have all drifted (F-14: roughly 73 lines in `render_stream.py`, 78 in `oc_runipd.py`). Find `render_run_summary_table`'s outcome expression, `reconcile_disposition`, `audit_step_artifact`, `StepArtifactAudit`, and `format_artifact_audit_summary` by name. NOTE `format_discrepancy_table` DOES NOT EXIST anywhere in the package; the real renderer is `format_artifact_audit_summary`, and searching for the wrong name would send an executor looking for something to consume that is not there.

THE ITEM THAT MATTERS MOST IS V-06's CONTRAST PASTE. This plan's claim is that a stranded run and a landed run now report DIFFERENTLY. A test that only exercises the stranded case would pass if the label were hardcoded, and a test that only exercises the landed case would pass if nothing changed. Both must be shown, from the same code path.

DO NOT SOLVE THIS BY MAKING THE SUMMARY PESSIMISTIC. A label that says NOT INTEGRATED whenever it is unsure would be as useless as one that always says COMPLETED, and it would train operators to ignore it. The verdict must follow an OBSERVED FACT the run itself recorded, which is why E-04 reads the integration verdict rather than guessing. (As authored this sentence said "the observed artifact location"; that source was measured to be time-varying, F-11, and OQ-04 settles the replacement.)

AND DO NOT LET A CONVENIENT LABEL HIDE A MISSING SIGNAL. The gate only records an integration verdict when it is RELEVANT (`self_finalize` on, not a review action, disposition in the success set). For every other item that field is legitimately absent, and absent MUST NOT be read as stranded, or `--no-self-finalize` runs and review sweeps would all report the new label. Treat a missing verdict as "not applicable" and fall through to today's behavior; E-06's precedence cases are where that is pinned.

On completion, close backlog `7m0aro`, which this plan carries as `- From-Backlog:` and whose `- Blocks-Release: next` gate it inherits.
