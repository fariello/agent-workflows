# IPD: Earn integration from a suite-failure delta against the frozen base instead of absolute green

- Date: 2026-09-06
- Kind: child
- Concern: ONE pre-existing red test anywhere in the repository silently disables automatic lane integration for EVERY plan. With `--validate` defaulting FALSE, `integration_is_earned` falls back to the driver-run suite as the trust signal (`oc_runipd.py:3733-3744` at review HEAD), and `run_suite_check` reports a BINARY pass/fail on the whole suite (`passing` is True only on an observed exit 0, `:3566`). So the gate cannot distinguish "this lane broke something" from "this repository was already red before the lane existed", and any unrelated failure closes it for every item in every run.
  THE DIAGNOSIS ABOVE IS CORRECT AND THE PRESCRIBED FIX IS NOT IMPLEMENTABLE AS WRITTEN. Two independent blockers were MEASURED at review, either one of which is fatal to the plan as authored; see F-12 and F-13, and OQ-04/OQ-05 which now carry them.
  FIRST, THERE IS NO FAILING-TEST TEXT TO PARSE. E-01's whole premise is reading `FAILED <nodeid>` lines out of the captured stdout, and F-8 asserted the substrate is already captured because `max_output_bytes=512_000`. Measured: `run_suite_check` reads `tool_event.get("stdout_excerpt")` (`:3615`), and `run_evidence.build_tool_event` NEVER WRITES THAT KEY. It stores `stdout_sha256`, `stderr_sha256`, `stdout_len`, `stderr_len` and `truncated` only, deliberately, and `max_output_bytes` merely TRUNCATES the bytes before hashing them. So `stdout` is unconditionally `""` at that line, and the existing `_SUITE_SUMMARY_RE` search over it can never match. This is not a theory: the real incident's own record shows `summary: ""` even though the suite printed a summary line, and `grep` finds `stdout_excerpt` at exactly ONE site in the whole package, the reader. E-01 as written parses a string that is always empty, so the failing set would always be empty, so EVERY lane would earn integration unconditionally. That is the precise inversion the gate at the bottom of this plan warns against.
  SECOND, THE DELTA WOULD NOT MEASURE THE LANE. `run_suite_check` runs in the PRIMARY checkout by explicit design (`dh0uno`), and its docstring already states the honest limit: "this proves THE TREE is green, not that the lane's uncommitted state is". But with `isolate_worktree` defaulting TRUE the agent commits ONLY on `aw/lane/<id6>`, so the primary tree does not contain the lane's work at gate time. A delta of primary-tree-now against `base_head` therefore measures what OTHER agents did to main during the run, and is empty for a lane that broke everything. The absolute check tolerated this because it was merely conservative; a SUBSET check converts it into a false PASS. Fixing this means either revalidating the merged result (which `execute_merge_and_revalidate_gate` already exists to do, and whose `full_validation_runner` is currently a hardcoded `return True`, `:2077-2092`) or running the delta somewhere the lane's commits exist. Both are different plans than this one.
  MEASURED COST, ONE INCIDENT. Run `run-20260907T010730Z-3199043` executed `mm6wuz` for 1h35m at $39.42, committed 2164 insertions across 10 files on its lane, verified all 12 E-items and all 12 V-items, and reported `aw ipd lint --phase pre-transition` conforming. Its `suite_check` recorded `{'passing': False, 'exit_code': 1, 'elapsed_seconds': 78.0}`, so `integration.earned` was False, so the branch at `oc_runipd.py:6435` never ran, and that branch is the ONLY caller of both `driver_finalize` and `integrate_lane_branch` (`:6468`). No finalize meant the plan stayed in `pending/`, which made `reconcile_disposition` downgrade the agent's own `disposition: executed` to `substantially-complete`. Zero integration events were emitted. The work was recovered by hand (`0f8abfab`, `b2bda903`).
  THE AGENT HAD ALREADY PROVEN NO REGRESSION AND THE GATE COULD NOT SEE IT. Its outcome record states the bare suite ended `31 failed, 5563 passed`, that a clean clone of the frozen base `6091014c` was built and run bare giving `31 failed, 5505 passed`, and that the sorted FAILED sets were BYTE-IDENTICAL, so zero regressions and +58 passing tests. That is exactly the delta comparison this plan makes the gate perform. The information existed; only the gate's question was wrong.
  NOTE WHAT THAT AGENT DID THAT THIS PLAN DOES NOT: it ran the lane's suite IN THE LANE (that is where `5563 passed` came from) and compared it against a clean clone of the base. This plan instead keeps the lane-side measurement in the PRIMARY checkout, where the lane's commits are absent. So the incident does not validate this plan's design; it validates a design where BOTH sides of the comparison see the code they claim to describe. See F-13.
  THIS IS THE SAME CLASS AS THE BUG `evgi9n` ALREADY FIXED, which is why it is a release blocker rather than tuning. `integration_is_earned`'s own docstring records that the gate used to require `verify_disp == "verified"`, unreachable in the default configuration, and that the measured cost was "~$528 across five overnight runs, 21 plans stranded in lanes, then a full session hand-merging 24 lanes". That fix made an unreachable condition reachable. This is the next layer: reachable, but gated on a signal that is red for reasons unrelated to the lane. Today's baseline is ONE failure (`test_orchestrator_retirement::RealRepositorySets`, which asserts against a sibling plan's live status), so the trigger threshold is a single unrelated red test.
- Scope: Teach the driver's suite check to answer a DELTA question instead of an absolute one: capture the failing-test set at the frozen base HEAD, compare the lane-time failing set against it, and earn integration when the lane's failures are a SUBSET of the base's. Keep every existing refusal that is genuinely about this lane, keep the fail-closed posture for a suite that cannot run, and change no default the operator must remember.
- Scope-Paths: agent_workflows/oc_runipd.py, agent_workflows/agy_runipd.py, tests/test_novalnomerge_integration.py, tests/test_oc_runipd.py, tests/test_agy_runipd_cli.py
- Item-Dependencies: none
- Status: superseded
- Readiness: no-go
- Set: integearn
- Order: 1
- Highest E allocated: 06
- Author: opencode its_direct/pt3-claude-opus-5-1m-us
- Id: 32ij2j
- From-Backlog: ciesaj
- Blocks-Release: next

## Workflow history
- 2026-09-08 superseded (opencode/its_direct/pt3-claude-opus-5-1m-us): SUPERSEDED by daexj1. The diagnosis was sound; both prescribed MECHANISMS were measured structurally unavailable at review (verdict REJECT - NEEDS REPLAN, Readiness no-go), so a replacement was authored on the maintainer's 2026-09-08 rulings rather than revising this plan in place. Its evidence is carried forward into daexj1.
- 2026-09-08 reviewed (opencode/its_direct/pt3-claude-opus-5-1m-us): /plan-review REJECT - NEEDS REPLAN; readiness NO-GO. PR-016..PR-022, five FIXED and two OPEN. THE DIAGNOSIS IS SOUND AND THE PRESCRIPTION IS NOT IMPLEMENTABLE. The plan states its feasibility was measured before authoring, so both feasibility claims were RE-RUN rather than trusted, and both failed. FIRST BLOCKER (PR-016, new F-12, F-8 WITHDRAWN AS FALSE): there is no output to parse. E-01 parses `FAILED <nodeid>` lines out of `tool_event['stdout_excerpt']`, and `run_evidence.build_tool_event` NEVER WRITES THAT KEY: it stores `stdout_sha256`/`stderr_sha256`/`stdout_len`/`stderr_len`/`truncated`, and `max_output_bytes` truncates the bytes BEFORE HASHING them and persists no text. Reproduced by calling `capture_command` on a command printing two FAILED lines (no such key returned); corroborated twice, since `grep` finds `stdout_excerpt` at exactly ONE site in the package (the reader) and the cited incident's own record has `summary: ''` despite its suite printing a summary line, meaning the EXISTING summary regex is already dead code. The consequence is an inversion, not a gap: a parser over an always-empty string returns `()`, and `() subset of anything` holds, so E-03's subset test would EARN integration for EVERY lane including one that broke everything. That is precisely what this plan's own gate paragraph says to stop for. The existing tests cannot catch it because they stub `capture_command` and fabricate the key (`tests/test_novalnomerge_integration.py:262,288,312`). SECOND BLOCKER (PR-017, new F-13): the delta would not measure the lane. `run_suite_check` runs in the PRIMARY checkout by design (`dh0uno`) and its docstring already concedes it proves THE TREE is green, not the lane; but `isolate_worktree` defaults TRUE, so the agent commits only on `aw/lane/<id6>` and the measured tree lacks the work being judged. Absolute-green tolerated that as merely conservative, which is this plan's complaint; a SUBSET test inverts it into a FALSE PASS, because a lane that broke every test it touched leaves the primary failing set identical to base. The incident the plan cites as proof measured the lane IN the lane (`5563 passed`) against a clean base clone, so it validates a DIFFERENT design than the one authored. Also found: the repository ALREADY has a combined-result revalidation gate (`execute_merge_and_revalidate_gate`, 'per-lane green never implies integrated green') whose `full_validation_runner` is a hardcoded `return True`, which is the more natural home for a no-new-failures check. OPEN: OQ-04 (`Blocking: yes`, PR-016) on how suite output becomes reachable, since one option mutates a schema-versioned evidence record and writes pytest output carrying absolute machine paths (D92) into a durable artifact; OQ-05 (`Blocking: yes`, PR-017) on which tree supplies the lane side, since every option relocates the trust signal. Both are PREREQUISITES of the existing E-items rather than gaps inside them, which is why the verdict is REPLAN: the plan was HARDENED in place rather than rewritten so its correct diagnosis and evidence survive, and the gate now states the minimum shape of a sound replacement (make output reachable and prove it without stubbing; then put the comparison where the merged code exists; then the subset predicate). ALSO FIXED: the sibling cited by a BACKLOG id three times (`7m0aro` is the backlog item; the plan is `xtklpd`), with the third occurrence deliberately left verbatim inside `## Workflow history`; every line citation drifted by roughly 49 lines in `oc_runipd.py` though the substance re-verified correct at the new coordinates; the cited `mm6wuz` receipt is GONE, consumed by its own recovery finalize, though the mechanism holds and the gate provably runs before finalize; a stale baseline (`5612` authored versus `5651` measured, same failing node id); and an unquantified cost now stated as roughly doubling gate wall time per distinct base. V-items tightened to demand the evidence that would have caught each blocker: V-01 now requires a NON-EMPTY failing set against a genuinely red suite obtained WITHOUT stubbing, and V-06 requires the refusal case run with `isolate_worktree` at its shipped default with the breaking commit lane-only.

- 2026-09-06 to-review (opencode its_direct/pt3-claude-opus-5-1m-us): Graduated from backlog `ciesaj`, which was itself filed from a measured incident earlier in this session: a $39.42 lane left unintegrated with zero integration events. Every claim was re-verified against code at HEAD `15445857` rather than trusted. CONFIRMED: `SuiteCheckResult.passing` is True only on exit 0 (`oc_runipd.py:3517`); `run_suite_check` is called from exactly two sites, one per driver (`oc_runipd.py:6397`, `agy_runipd.py:3711`), and agy BINDS the shared definition rather than forking it (`agy_runipd.py:306`), so a fix in the shared function reaches both hosts by construction; the earned-integration branch at `:6435` is the sole caller of `driver_finalize` and `integrate_lane_branch`. FEASIBILITY MEASURED BEFORE AUTHORING, so the design is not speculative: `capture_command` is invoked with `max_output_bytes=512_000` (`:3563`) while a real run's `FAILED` lines total 125 bytes, so the substrate for parsing a failing-test set is already captured and no new plumbing is needed; and the begin receipt already persists `base_head` (verified in `.aw/state/ipd-lifecycle/mm6wuz.receipt.json`) with `ipd_lifecycle.read_receipt` (`:816`) as the reader, so the baseline commit is already knowable at gate time. TWO THINGS DELIBERATELY LEFT TO SIBLINGS: reporting the verdict honestly is `7m0aro` (integearn-02), and the stale-base scope misattribution the same recovery exposed is `hyx1dg`.

## Goal

Make the integration gate ask "did this lane make the suite worse", which is the question that actually protects main, instead of "is the whole repository green", which no busy repository ever is.

## Detailed Implementation Checklist (TODO)

Execution-state rule: mark an `E-*` item complete only after performing the action. That mark is not validation. Right-sizing rule: each E-item must address one concern and be executable in one focused pass; split when an E-item names multiple distinct deliverables or independent test-surfaces.

### Task group 1: make a failing-test SET observable

- [ ] E-01 FIRST MAKE THE OUTPUT REACH THE READER, THEN extend `SuiteCheckResult` (find it by symbol; `oc_runipd.py:3558-3572` at review HEAD) with the FAILING TEST SET and populate it in `run_suite_check`. Store it as a SORTED TUPLE so two results are comparable by equality and a subset test is cheap and order-independent.
  THE SUBSTRATE IS **NOT** CAPTURED, WHICH F-8 GOT WRONG AND WHICH BLOCKS THIS ITEM (F-12, OQ-04). Measured at review: `run_suite_check` reads `tool_event.get("stdout_excerpt")` (`:3615`), and `run_evidence.build_tool_event` (`:277-332`) writes NO such key. It records `stdout_sha256`, `stderr_sha256`, `stdout_len`, `stderr_len`, `truncated` and (when given) `max_bytes`; `max_output_bytes` truncates the bytes BEFORE hashing and stores none of them. Reproduced by calling `capture_command` on a command printing two `FAILED` lines: the returned tool_event has no `stdout_excerpt`, so the value `run_suite_check` binds is `""`. Corroborated by the incident's own record, whose `summary` is `""` despite the suite having printed a summary line. So today's `_SUITE_SUMMARY_RE` search is already dead code, and parsing `FAILED` lines out of that same empty string would yield an ALWAYS-EMPTY failing set, which under E-03's subset test earns integration for EVERY lane unconditionally.
  DO NOT "FIX" THIS BY ADDING AN EXCERPT FIELD ON YOUR OWN AUTHORITY. `build_tool_event` deliberately stores hashes rather than output, `validate_evidence` reasons about `stdout_sha256`/`stdout_len`, and the ledger record is schema-versioned (`schema.LEDGER_SCHEMA_VERSION`), so persisting raw suite output into a run ledger is a provenance and leak-surface decision (D92 territory: pytest output carries absolute machine paths), not a detail of a cache. OQ-04 must be answered first. The cheapest shape that avoids the ledger entirely is to have `run_suite_check` obtain the output DIRECTLY (its own `subprocess.run`, or a `capture_command` variant that RETURNS the text to its caller without writing it to the ledger) and keep only the parsed node-id set. That is a design choice with a security dimension, so it is the maintainer's.
  Follow the existing `_SUITE_SUMMARY_RE` precedent (`:3553`, searched against stdout then stderr at `:3635`) for STYLE, but understand it is currently non-functional for the same reason; if this item makes output available, that regex starts working too, which is a behavior change to report rather than a silent side effect.
  KEEP `passing` EXACTLY AS IT IS. It is documented as "an OBSERVED FACT, not a claim ... True only on an observed exit 0" and other readers depend on that meaning. This item ADDS a field; it must not redefine one.
  RECORD WHEN THE SET IS UNKNOWABLE, and distinguish that from an empty set. A truncated capture, an unparseable summary, or exit 124/127 means "I could not determine the failing set", which is NOT the same as "nothing failed". Use `None` for unknown and an empty tuple for genuinely no failures, because E-03 must fail closed on the former and pass on the latter. THIS IS THE ITEM'S MOST IMPORTANT PROPERTY GIVEN F-12: a nonzero exit with NO parseable `FAILED` line is precisely the always-empty case above, so it MUST map to `None`, never to `()`. A `passing=False` result whose failing set is empty is a CONTRADICTION and must be treated as unknown.
  - Depends on: none
  - Expected outcome: the suite's failing-test text is actually reachable by the parser (per OQ-04's answer, with no raw output added to the ledger unless that answer says so); `SuiteCheckResult` carries a sorted failing-test tuple, or `None` when undeterminable INCLUDING the nonzero-exit-with-no-parsed-failures case; `passing` is unchanged in meaning and value.
  - Execution state: pending

- [ ] E-02 Capture the BASE-HEAD baseline failing set, and cache it per commit so the cost is paid once per run rather than once per item. The comparison target is the frozen base, which the begin receipt already records: `base_head` is persisted in the receipt and `ipd_lifecycle.read_receipt` (`:816`) reads it. Re-verified at review that the receipt is consumed ONLY on a successful finalize (`ipd_lifecycle.py:2652`), and that the suite check runs BEFORE finalize (`oc_runipd.py:6456` versus `:6493`), so the receipt is present at gate time. NOTE the specific receipt this plan cited as evidence (`mm6wuz`) no longer exists on disk, exactly because its recovery finalized it; cite a LIVE receipt when validating, not that one.
  A BASELINE ALONE DOES NOT MAKE A DELTA HONEST. Whatever this item measures at `base_head` is only half the comparison, and the OTHER half is currently measured in a tree that does not contain the lane's work (F-13, OQ-05). Do not execute this item as if the lane side were settled.
  RUN THE BASELINE IN A DETACHED CLONE OR SCRATCH WORKTREE OF `base_head`, NEVER BY MUTATING THE PRIMARY CHECKOUT. This is not optional: the primary tree is what other agents are working in, and this repository's policy for un-owned state is to leave it strictly alone. A `git stash` or `git checkout` to reach the base would be exactly the co-worker-clobbering `AGENTS.md` forbids. The agent whose incident motivated this plan did it correctly by hand, building "a clean clone of HEAD `6091014c` in a gitignored scratch dir".
  CACHE UNDER `.aw/state/`, keyed by the base commit sha. That tree is gitignored and box-local, which is the correct home for an ephemeral measurement, and a sha key makes the cache self-invalidating: a different base is a different key, so a stale baseline cannot be reused.
  DO NOT RUN THE BASELINE IN A LANE WORKTREE. `run_suite_check`'s docstring records the measured reason: a linked worktree resolves `.aw/state` relative to cwd (`dh0uno`), giving `36 passed` in the primary checkout versus `15 failed, 20 passed` in a lane, "every failure being the `run_viewer`/state-resolution family". A baseline measured in a lane would be permanently and wrongly red, reproducing this plan's own bug with a new cause.
  - Depends on: E-01
  - Expected outcome: the base-head failing set is measured in an isolated checkout of that commit, cached under `.aw/state/` keyed by sha, reused within a run, and never obtained by mutating the primary tree.
  - Execution state: pending

### Task group 2: change the question the gate asks

- [ ] E-03 Change `integration_is_earned`'s validation-OFF branch from "the suite passed" to "the LANE'S failing set is a SUBSET of the base's". Earn integration when every failing test also failed at the base; refuse when the lane introduces a failure the base did not have.
  THE WORD "LANE'S" IS THE UNRESOLVED PROBLEM, NOT A DETAIL (F-13, OQ-05). The result this branch receives comes from `run_suite_check(repo, ...)` (`:6456`), which runs in the PRIMARY checkout BY DESIGN and whose own docstring says it "proves THE TREE is green, not that the lane's uncommitted state is". With `isolate_worktree` defaulting TRUE the agent's commits live only on `aw/lane/<id6>`, so at gate time the primary tree does NOT contain the work being judged. Under the old ABSOLUTE test that mismatch was merely conservative (an unrelated red test refused a good lane, which is this plan's complaint). Under a SUBSET test it inverts into a false PASS: a lane that broke every test it touched shows a primary-tree failing set unchanged from base, so the subset holds and integration is EARNED. Do NOT implement the subset comparison until OQ-05 says which tree supplies the lane side.
  FAIL CLOSED ON UNKNOWN, which is the case E-01 made distinguishable. If either failing set is `None` (undeterminable), REFUSE. The existing posture is explicit that "a suite that cannot be run is a FAILURE, never a pass" and that "a gate that crashes is a gate that is OFF"; an unknown delta must inherit that, or this change becomes an escape hatch rather than a better question. Given F-12, also refuse when `passing` is False and the failing set is EMPTY: that combination means the parser saw nothing, not that nothing failed, and treating it as an empty set is exactly the always-earn bug.
  DO NOT TOUCH THE VALIDATION-ON BRANCH. The docstring states the two modes are ALTERNATIVES and that "a verifier that DECLINED is a stronger and more specific signal than a green suite, so a passing suite must NOT override it; otherwise `--validate` would be weaker than the default, which is absurd". That reasoning is unaffected by this change and must survive it verbatim.
  CARRY THE REASON. `IntegrationVerdict` already exists to explain the decision; populate it with the delta ("3 failures, all present at base `<sha>`" or "1 NEW failure not at base: `<nodeid>`"). Sibling child `xtklpd` (integearn-02) owns PERSISTING and REPORTING that reason; this item's obligation is to produce it rather than discard it.
  - Depends on: E-02
  - Expected outcome: a lane whose failures are all pre-existing earns integration; a lane introducing any new failure does not; an undeterminable set refuses; the validation-ON branch is byte-unchanged.
  - Execution state: pending

- [ ] E-04 Bound the cost and state the honest limits, because a baseline suite run is not free and a plan that pretends otherwise will be switched off.
  BOUND THE BASELINE RUN with the same timeout discipline `run_suite_check` already uses (`SUITE_CHECK_TIMEOUT_SECONDS = 900.0`, `:3545` at review HEAD), and treat a baseline timeout as UNKNOWN (refuse per E-03), never as an empty failing set. Reuse the constant; do not introduce a second timeout.
  STATE THE COST HONESTLY, WITH THE MEASURED NUMBER. The bare suite takes 165s at review (and the incident's own gate run took 78s), so adding a baseline run roughly DOUBLES the gate's wall time per distinct base, before any lane-side run OQ-05 may require. Say that plainly rather than calling it "not free".
  RECORD THE HONEST LIMITS in the function's docstring, extending the existing "HONEST LIMIT" paragraph rather than replacing it. FOUR are known and each should be stated: (a) the comparison proves the lane added no failure THE SUITE CAN SEE, not that the lane is correct; (b) a FLAKY test can appear in one set and not the other, so a single new failure may be noise rather than a regression, which is an argument for reporting it rather than for ignoring it; (c) the baseline is measured at `base_head`, so a failure introduced on main by another agent AFTER that commit will look like this lane's fault (narrowing it is `hyx1dg`'s territory); and (d) ADDED AT REVIEW, and only removable by OQ-05: whichever tree supplies the lane side, say WHICH, because the pre-existing docstring already concedes the primary-checkout run "proves THE TREE is green, not that the lane's uncommitted state is", and a subset test over that tree proves nothing about the lane at all.
  - Depends on: E-03
  - Expected outcome: the baseline run is timeout-bounded with the existing constant; a timeout refuses rather than passes; all three honest limits are documented at the decision site.
  - Execution state: pending

### Task group 3: prove it

- [ ] E-05 Test the DELTA LOGIC deterministically, without running a real suite. Drive `integration_is_earned` directly with synthesized `SuiteCheckResult` pairs and assert: identical failing sets earn; a lane set that is a strict subset earns (a lane that FIXED something must not be punished); a lane set containing one node absent from the base REFUSES and names that node; both-empty earns; `None` on either side refuses; `passing=False` with an EMPTY failing set refuses (the F-12 case, which is what a broken parser actually produces); and validation-ON still defers entirely to `verify_disp`.
  ONE TEST MUST NOT STUB `capture_command`. Every existing test of this path patches it and fabricates a `stdout_excerpt` key that production never sets, which is exactly why F-12 survived to review. Add at least one test that drives the REAL `capture_command` against a trivial command printing known `FAILED` lines and asserts the parsed set is non-empty. A suite that only ever sees the stub cannot tell a working parser from a dead one.
  ASSERT THE REGRESSION CASE NAMES THE OFFENDER. A refusal that says only "suite not green" reproduces the diagnostic gap that made the original incident hard to read. The verdict must identify which test is new.
  ADD THE INCIDENT AS A REGRESSION FIXTURE, using its real measured numbers so the test documents the bug it prevents: base `31 failed, 5505 passed` and lane `31 failed, 5563 passed` with byte-identical FAILED sets must EARN integration. That is the exact case that stranded $39.42.
  - Depends on: E-04
  - Expected outcome: seven delta cases pinned including the incident fixture; the new-failure refusal names the offending node; the validation-ON path is proven untouched.
  - Execution state: pending

- [ ] E-06 Prove the END-TO-END path on BOTH hosts, which is where a shared-predicate fix can still be wired wrong. The predicate is shared by construction (`agy_runipd.py:311-313` binds it with the `as <same-name>` re-export form, and its docstring says "ONE predicate, consumed by BOTH drivers, so a one-runner fix cannot leave the other silently broken"), but the CALL SITES are per-driver (`oc_runipd.py:6456`, `agy_runipd.py:3724`) and each must pass the baseline through. Both re-verified at review at these coordinates.
  DEMONSTRATE INTEGRATION ACTUALLY HAPPENING with a red pre-existing test present. Build a throwaway repository whose suite has one permanently failing test, run a lane that changes something unrelated, and show the lane INTEGRATES to main. Under today's code it would strand; that contrast is the proof.
  DEMONSTRATE THE REFUSAL TOO: a lane that breaks a test which passed at base must NOT integrate, and its lane branch must be preserved. THIS IS THE ITEM THAT WOULD HAVE CAUGHT F-13, and it must be run with `isolate_worktree` at its DEFAULT (TRUE), because the false pass exists only when the lane's commits are absent from the tree the gate measures. A refusal case demonstrated with `--no-isolate-worktree`, or with the breaking change applied to the primary tree, proves nothing about the shipped configuration and would pass over the bug.
  Run the suite BARE (`python3 -m pytest`) and state before/after counts. Note `tests/test_run_viewer.py` reads the gitignored `.aw/records/runs/`, so validate in the REAL checkout; green in a bare worktree proves nothing.
  - Depends on: E-05
  - Expected outcome: on both hosts, a lane integrates despite an unrelated red test and refuses on a genuine new failure; bare suite green with counts stated.
  - Execution state: pending

## Project conventions discovered (Step 0)

- THE PREDICATE IS ALREADY SHARED AND SAYS WHY: `integration_is_earned` is "ONE predicate, consumed by BOTH drivers, so a one-runner fix cannot leave the other silently broken", and `agy_runipd.py:306` binds it with the `as <same-name>` re-export form. Fix it there and both hosts inherit the fix.
- `run_suite_check` MUST RUN IN THE PRIMARY CHECKOUT, never a lane. Its docstring records the measured reason (`dh0uno`): a lane resolves a different `.aw/state`, giving `15 failed, 20 passed` where the primary checkout gives `36 passed`. A lane-measured baseline would be permanently red.
- FAIL-CLOSED IS THE ESTABLISHED POSTURE, stated twice at the site: "a suite that cannot be run is a FAILURE, never a pass", and "a gate that crashes is a gate that is OFF, so an unexpected exception here must still be a REFUSAL". The unknown-delta case must inherit it.
- `passing` IS DOCUMENTED AS AN OBSERVED FACT, not a claim, and is True only on exit 0. Add fields; do not redefine it.
- **THE OUTPUT SUBSTRATE IS NOT CAPTURED (CORRECTED AT REVIEW).** `max_output_bytes` truncates bytes before HASHING them; `build_tool_event` persists `stdout_sha256`/`stdout_len` and no text. `run_suite_check` reads a `stdout_excerpt` key that nothing writes, so its `stdout` is always `""` and its summary regex is already dead. Any parsing item must FIRST make output reachable, and that is a provenance/leak decision (pytest output carries absolute machine paths), not a detail.
- **THE EXISTING TESTS CANNOT SEE THAT BUG**, because they patch `run_evidence.capture_command` and hand back a fabricated `stdout_excerpt`. A new test that stubs the same way would also pass while the real path stayed broken; at least one test must exercise the REAL `capture_command`.
- **THE PRIMARY-CHECKOUT SUITE DOES NOT CONTAIN THE LANE.** `isolate_worktree` defaults TRUE, so lane commits live on `aw/lane/<id6>`. A comparison whose lane side is measured in the primary tree is not measuring the lane, and turning an absolute check into a subset check converts that from conservative into a false pass.
- **THERE IS ALREADY A SECOND, COMBINED-RESULT GATE**: `integrate_lane_branch` calls `orchestrate_isolation.execute_merge_and_revalidate_gate`, whose `full_validation_runner` is currently `lambda: True` (`make_integration_validation_runner`). Any plan about "does the merged result regress" should consider that seam before adding delta logic to the pre-merge gate.
- `base_head` IS ALREADY FROZEN AND READABLE: the begin receipt persists it and `ipd_lifecycle.read_receipt` reads it, so the comparison target requires no new state. The receipt is consumed only at successful finalize and the gate runs before finalize, so it is present at gate time. Do NOT cite `mm6wuz`'s receipt: it is gone, consumed by its recovery finalize.
- RE-LOCATE EVERY SYMBOL BY NAME: every line number in this plan has drifted by roughly 49 lines in `oc_runipd.py` (F-15).
- `.aw/state/` IS GITIGNORED AND BOX-LOCAL, the correct home for a cached measurement, and a sha-keyed cache is self-invalidating.
- THE TWO VALIDATION MODES ARE ALTERNATIVES, NOT AN OR. A verifier that declined must not be overridden by a green suite.
- Run the suite BARE: `python3 -m pytest`. The configured `addopts` already supply quiet, parallel, and the fast subset; do not add `-n0` or a second `-q`.

## Findings

| # | Finding | Evidence |
|---|---|---|
| F-1 | The gate is BINARY on the whole suite: `passing` is True only on an observed exit 0, so any unrelated red test closes it for every item. RE-VERIFIED at review at the drifted coordinates. | `oc_runipd.py:3558-3572`, `:3566` |
| F-2 | **THE MEASURED INCIDENT.** `mm6wuz` ran 1h35m at $39.42, committed 2164 insertions across 10 files, verified 12 E and 12 V items, and stranded everything: `suite_check` recorded `{'passing': False, 'exit_code': 1}`, `attempt['integration']` was `None`, and `grep -c integrat events.jsonl` returned 0. | `run-20260907T010730Z-3199043` state.json and events.jsonl |
| F-3 | The earned branch is the SOLE caller of both finalize and integration, so a False verdict skips both silently. | `oc_runipd.py:6435` (the `if integration_gate_relevant and integration.earned:` guard), `:6468` |
| F-4 | **THE AGENT HAD ALREADY DONE THE DELTA COMPARISON BY HAND** and the gate could not consume it: base clone of `6091014c` gave `31 failed, 5505 passed`, the lane gave `31 failed, 5563 passed`, and the sorted FAILED sets were byte-identical, so zero regressions and +58 passing tests. | `mm6wuz` outcome record, `regression_analysis` field |
| F-5 | The downgrade to `substantially-complete` is a CONSEQUENCE, not the cause: with no finalize the plan stayed in `pending/`, so `reconcile_disposition` read `plan_bucket() != "executed"` and overrode the agent's `disposition: executed`. | `oc_runipd.py:5814-5822` |
| F-6 | **SAME CLASS AS A BUG ALREADY FIXED AT MEASURED COST.** `integration_is_earned`'s docstring records the prior version required `verify_disp == "verified"`, unreachable with `--validate` defaulting False, costing "~$528 across five overnight runs, 21 plans stranded in lanes, then a full session hand-merging 24 lanes". | `oc_runipd.py:3646-3651` |
| F-7 | TODAY'S THRESHOLD IS ONE TEST. The current bare baseline is `1 failed, 5612 passed`, the failure being `test_orchestrator_retirement::RealRepositorySets`, which asserts against a sibling plan's live status and will recur whenever a plan's status changes. So the gate is closed right now. | measured 2026-09-06 at HEAD `15445857` |
| F-8 | ~~**THE PARSING SUBSTRATE ALREADY EXISTS**~~ **WITHDRAWN AT REVIEW: FALSE.** `max_output_bytes` truncates the bytes before HASHING them and stores no text. The 125-byte measurement was of a terminal pipeline, not of anything the driver can read. Superseded by F-12. | `oc_runipd.py:3612`; `run_evidence.build_tool_event:277-332` |
| F-9 | **THE COMPARISON TARGET ALREADY EXISTS**: the begin receipt persists `base_head` and `read_receipt` is its reader. Re-verified at review, with one correction: the cited `mm6wuz` receipt is GONE (consumed by the successful recovery finalize, `ipd_lifecycle.py:2652`), so it is not usable as evidence; the mechanism holds and the gate does run before finalize (`oc_runipd.py:6456` vs `:6493`). | `ipd_lifecycle.py:816`, `:1084`, `:2652`; `ls .aw/state/ipd-lifecycle/` (30 live receipts, no `mm6wuz`) |
| F-12 | **BLOCKER, MEASURED: THERE IS NO OUTPUT TO PARSE, SO E-01's DELTA WOULD ALWAYS BE EMPTY AND EVERY LANE WOULD EARN INTEGRATION.** `run_suite_check` reads `tool_event.get("stdout_excerpt")`, and `build_tool_event` never writes that key (it writes `stdout_sha256`/`stderr_sha256`/`stdout_len`/`stderr_len`/`truncated`/`max_bytes`). `stdout_excerpt` appears at exactly ONE site in the package: the reader. So `stdout` is always `""`, today's `_SUITE_SUMMARY_RE` is already dead, and a `FAILED`-line parser over it yields `()` for a red suite. Under E-03's subset test `() ⊆ anything` holds, so integration is earned unconditionally: the exact inversion this plan's own gate says to stop for. The existing tests do not catch it because they STUB `capture_command` and fabricate a `stdout_excerpt` key. | called `capture_command` on a command printing two `FAILED` lines: returned keys contain no `stdout_excerpt`, `stdout_len=62`; `grep -rn stdout_excerpt agent_workflows/` -> 1 hit (`oc_runipd.py:3615`); incident record `summary: ""`; `tests/test_novalnomerge_integration.py:262`, `:288`, `:312` fabricate the key |
| F-13 | **BLOCKER, MEASURED: THE LANE SIDE OF THE DELTA DOES NOT CONTAIN THE LANE.** `run_suite_check(repo, ...)` runs in the PRIMARY checkout by explicit design and its docstring already concedes it "proves THE TREE is green, not that the lane's uncommitted state is". With `isolate_worktree` defaulting TRUE the agent commits only on `aw/lane/<id6>`, so at gate time the primary tree lacks the work under judgement. Absolute-green tolerated this (conservative); a SUBSET test inverts it into a FALSE PASS, since a lane that broke everything leaves the primary failing set equal to base. The incident this plan cites got it right by measuring the lane IN the lane (`5563 passed`), which is what this plan does not do. Note `execute_merge_and_revalidate_gate` already exists to revalidate the COMBINED result and its `full_validation_runner` is a hardcoded `return True`, so the honest fix may belong there. | `oc_runipd.py:3593-3594` (docstring limit), `:5985` (`isolate_worktree` default True), `:6456` (call passes `repo`, not `work_dir`), `:2077-2092` (`_runner` returns True) |
| F-14 | **THE SIBLING IS CITED BY THE WRONG ID, THREE TIMES.** The plan names `7m0aro` as "sibling child (integearn-02)". Measured: no plan carries `- Id: 7m0aro`; it is the BACKLOG item (`backlog/graduated/...-7m0aro-stranded-run-reports-completed.backlog.md`) that integearn-02 graduated FROM. The actual sibling plan is `xtklpd`, which carries `- From-Backlog: 7m0aro`. An executor told to consult `7m0aro` as a plan finds nothing, and `aw find plans 7m0aro` returns no match. | `aw find plans 7m0aro` -> "no matching plans"; `grep -rln '^- Id: 7m0aro' .aw/records/plans/` -> empty; `integearn-02` front matter `- Id: xtklpd`, `- From-Backlog: 7m0aro` |
| F-15 | **EVERY LINE CITATION IN THIS PLAN HAS DRIFTED, BY ABOUT 49 LINES IN THE HOT FILE.** Measured: `SuiteCheckResult` is `:3558-3572` (plan says `:3509-3524`), `passing` is `:3566` (`:3517`), `_SUITE_SUMMARY_RE` `:3553` (`:3504`), `SUITE_CHECK_TIMEOUT_SECONDS` `:3545` (`:3496`), `max_output_bytes` `:3612` (`:3563`), the oc call site `:6456` (`:6397`), the earned branch `:6493` (`:6435`), agy's call site `:3724` (`:3711`), agy's binding `:311-313` (`:306`). The plan's own gate already says to re-locate by symbol, which is why this is LOW: the substance was verified correct at the new coordinates. | `grep -n` per symbol at review HEAD `8b4e1570` |
| F-16 | Baseline drift: the plan's before-baseline (`1 failed, 5612 passed` at `15445857`) is stale; measured at review `1 failed, 5651 passed, 3 skipped, 2 xfailed`, SAME failing node id (`test_orchestrator_retirement::RealRepositorySets`). The plan's own delta criterion is correct and this is why it must be re-measured in the executing worktree rather than compared to a recorded total. | bare `python3 -m pytest` at HEAD `8b4e1570` |
| F-10 | The fix reaches both hosts by construction: `run_suite_check` is called once per driver and agy BINDS the shared definition rather than forking it. | `oc_runipd.py:6397`; `agy_runipd.py:3711`, `:306` |
| F-11 | A LANE-MEASURED BASELINE WOULD REPRODUCE THIS BUG with a new cause: `dh0uno` makes a lane resolve a different `.aw/state`, giving `15 failed, 20 passed` where the primary checkout gives `36 passed`. Hence E-02's isolated-clone requirement. | `run_suite_check` docstring, `oc_runipd.py:3536-3542` |

## Proposed changes (ordered, validatable)

1. Add a sorted failing-test set to `SuiteCheckResult`, parsed from already-captured stdout, with `None` for undeterminable (E-01).
2. Measure the `base_head` baseline in an isolated checkout, cached under `.aw/state/` by sha, never by mutating the primary tree (E-02).
3. Change the validation-OFF branch to a subset test, failing closed on unknown, leaving validation-ON untouched (E-03).
4. Bound the baseline with the existing timeout and document the three honest limits (E-04).
5. Pin seven delta cases including the real incident as a fixture (E-05).
6. Prove end to end on both hosts that an unrelated red test no longer strands a lane (E-06).

## Deferred / out of scope (with reason)

- REPORTING THE VERDICT HONESTLY. Sibling child `xtklpd` (integearn-02) owns persisting the integration verdict, recording the disposition downgrade, and fixing `Outcome: COMPLETED` for a stranded run. This child PRODUCES the reason; that one PERSISTS and DISPLAYS it. Split deliberately: the gate's logic and the report's honesty have different test surfaces and can be verified independently. (`7m0aro` is the BACKLOG item that plan graduated from, not the plan; F-14.)
- THE STALE-BASE SCOPE MISATTRIBUTION (`hyx1dg`). Same incident, different mechanism: finalize's scope audit attributing other agents' commits to the finalizing plan. It shares the `base_head` value but nothing else, and mixing them would put two unrelated fixes behind one verdict.
- NARROWING HONEST LIMIT (c), that a failure introduced on main by another agent after `base_head` looks like this lane's fault. Real residual, documented in E-04, and its fix needs commit-level attribution which is `a8eufb`/`hyx1dg` territory.
- FIXING THE PRE-EXISTING FAILURES THEMSELVES. `test_orchestrator_retirement::RealRepositorySets` asserts against a sibling plan's live status and is out of scope; this plan makes such a failure stop blocking integration, which is the durable answer rather than chasing each one.
- MAKING THE DELTA CHECK OPTIONAL VIA A FLAG. Rejected: `vju5ba` measured that a default which silently disables the mechanism is exactly how five overnight runs were lost. The correct behavior must be the default.
- THE DEFERRAL LADDER AND THE `integrate` VERB. `5wdoze` / `yocdq4` in the `integpath` Set handle a lane whose integration was ATTEMPTED and refused. This plan is the case where it was never attempted, so those do not cover it and this does not cover them.

## Scope check

- Over-scope: none. The shared suite-check and predicate, both drivers' call sites, and three test modules.
- Scope-Paths justification: `oc_runipd.py` holds `SuiteCheckResult`, `run_suite_check`, `integration_is_earned` and the oc call site (E-01..E-04); `agy_runipd.py` holds the agy call site that must pass the baseline through (E-06); `tests/test_novalnomerge_integration.py` is the suite that pins THIS gate's bug class and is where the incident fixture belongs; each driver's suite covers its own end-to-end path.
- BOTH DRIVER MODULES ARE THE HIGHEST-CONTENTION FILES IN THE REPOSITORY. Expect drift, re-locate by symbol, expect to rebase and re-run the full suite after any merge.
- Under-scope, stated rather than left as `none`: this child does not persist or display the verdict (`xtklpd`), does not fix the scope misattribution (`hyx1dg`), does not narrow honest limit (c), does not repair the pre-existing failures, and does not add the ladder or the `integrate` verb. Each is excluded with a reason above.
- UNDER-SCOPE FOUND AT REVIEW AND NOT ADDED HERE, because each is a maintainer decision rather than an omission to patch: making the suite output reachable at all (OQ-04, F-12) and deciding which tree supplies the lane side of the delta (OQ-05, F-13). Both are PREREQUISITES of this plan's own E-items rather than extensions of them, which is why the verdict is REPLAN rather than a set of added items. Note `Scope-Paths` does NOT include `agent_workflows/run_evidence.py` or `agent_workflows/orchestrate_isolation.py`, and depending on how OQ-04/OQ-05 resolve, one or both may be required; declare them then.

## Required tests / validation

- `python3 -m pytest` BARE, before and after, with the `N passed` summary line pasted and counts stated. MEASURE YOUR OWN BEFORE-BASELINE: at authoring it was `1 failed, 5612 passed, 3 skipped, 2 xfailed` at HEAD `15445857`; RE-MEASURED at review it is `1 failed, 5651 passed, 3 skipped, 2 xfailed` at HEAD `8b4e1570`, with the SAME failing node id. Trust neither total (F-16). The criterion is that the AFTER failure set minus the BEFORE failure set is EMPTY, not that the suite is green.
- Targeted: `tests/test_novalnomerge_integration.py`, `tests/test_oc_runipd.py`, `tests/test_agy_runipd_cli.py`.
- A REAL END-TO-END DEMONSTRATION on BOTH hosts in a throwaway repository: a permanently red unrelated test present, a lane that changes something else, and the lane INTEGRATING. Plus the refusal case: a lane that breaks a previously-passing test does not integrate and its branch is preserved.
- VALIDATE IN THE REAL CHECKOUT for `tests/test_run_viewer.py` (reads the gitignored `.aw/records/runs/`).
- MEASURE EXIT CODES UNPIPED (`cmd >/dev/null 2>&1; echo $?`); a pipe through `head` reports the pipe's status, which has already produced one false finding in this repository.
- `aw sanitize --agent` clean.

## Spec / documentation sync

Spec `25kzda` governs the run's verification and integration semantics. This plan changes WHICH OBSERVATION earns integration, not whether verification is required, so no requirement changes. If the executor finds spec text asserting that integration requires a GREEN suite (rather than no-new-failures), NOTE IT for a spec amendment and do NOT edit the spec here: its §4.2 finding-code table is transcribed verbatim into `run_evidence.RUN_FINDING_CODES` with a byte-equality test, so editing a cell IS a code change.

`integration_is_earned`'s docstring is the authoritative prose statement of the gate and MUST be updated: it currently says the driver-run suite decides when validation is off, which becomes "the driver-run suite's DELTA against the frozen base decides". Extend its existing "THE TWO MODES ARE ALTERNATIVES" and "HONEST LIMIT" paragraphs rather than replacing them, and add E-04's three limits.

Any `--help` or CHANGELOG text describing the integration gate must state the new behavior plainly, including that a pre-existing failure no longer blocks integration. Write no em or en dashes in user-facing prose.

## Open questions

### OQ-01: Should the baseline be measured at `base_head` or at main's CURRENT head at integration time?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: AT `base_head`, the frozen begin-time commit. Three reasons. It is ALREADY RECORDED in the begin receipt, so it needs no new state and cannot drift. It is the commit the lane was actually built on, so it is the honest counterfactual for "what did this lane change". And it is STABLE: main's current head moves under a long run as other agents commit, so a moving target would make the same lane earn or refuse depending on when it happened to finish, which is precisely the timing-dependent unfairness this plan exists to remove. The cost is honest limit (c) in E-04: a failure another agent introduces on main after `base_head` will look like this lane's fault. That is a real residual, is documented, and is narrower than the bug being fixed.

### OQ-02: Should a lane that FIXES a pre-existing failure be treated specially?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: NO SPECIAL CASE; the subset test already handles it correctly. A lane whose failing set is a strict subset of the base's earns integration, which is the right outcome for a lane that repaired something incidentally. E-05 pins this explicitly because the naive implementation (requiring set EQUALITY) would REFUSE such a lane, punishing an improvement, and that error is easy to make and invisible without a test. No credit, no reward, no separate reporting: it simply must not be blocked.

### OQ-03: What if the baseline suite run is itself flaky, so the same base yields different failing sets?

- Blocking: no
- Status: resolved
- Owner: none
- Resolution or deferral rationale: ACCEPT IT AND REPORT, do not retry or vote. A flaky test can appear in the lane's set and not the base's, producing a spurious refusal; the correct response is that the refusal NAMES the test (E-03, E-05), so an operator sees "1 NEW failure: <nodeid>" and can recognize a known flake immediately. Retrying or best-of-N would hide genuine regressions behind noise and would multiply the already-real cost of a baseline run. The sha-keyed cache also reduces exposure by measuring a given base once. This is stated as honest limit (b) in E-04 rather than engineered around, because the alternative trades a visible false refusal for an invisible false pass.

### OQ-04: How does the suite's failing-test text become reachable at all, given nothing writes `stdout_excerpt`?

- Blocking: yes
- Status: open
- Owner: maintainer
- Finding: PR-016
- Question: E-01 parses `FAILED` lines out of `tool_event["stdout_excerpt"]`, and measured, `run_evidence.build_tool_event` never writes that key: it stores `stdout_sha256`, `stderr_sha256`, `stdout_len`, `stderr_len` and `truncated`, and `max_output_bytes` truncates the bytes before hashing. So `run_suite_check`'s `stdout` is always `""` (reproduced), its existing summary regex is already dead, and a `FAILED` parser over it returns `()` for a red suite, which under E-03's subset test earns integration for EVERY lane. Options: (a) have `run_suite_check` capture the output ITSELF (its own `subprocess.run`) and keep only the parsed node-id set in memory, persisting no raw text, which touches no ledger contract and adds no leak surface; (b) add a `capture_command` variant that RETURNS the decoded output to its caller without writing it to the ledger record; (c) add a real `stdout_excerpt`/tail field to `build_tool_event` and persist bounded suite output into the run ledger, which changes a schema-versioned provenance record and puts pytest output (carrying absolute machine paths, D92) into a durable artifact; (d) fix only the DEAD READ (so the summary line works) and abandon set-level parsing. Recommendation: (a), smallest blast radius, no schema or leak-surface change, and it keeps the failing set an in-memory gate input rather than a persisted artifact. Whichever is chosen, `agent_workflows/run_evidence.py` may need to enter `Scope-Paths`, and the fix MUST come with a test that does not stub `capture_command` (every existing one does, which is why this survived to review).
- Resolution or deferral rationale: OPEN. Option (c) changes a schema-versioned evidence record and its leak surface, so the choice is not an executor's.

### OQ-05: Which tree supplies the LANE side of the delta, given the primary checkout does not contain the lane's commits?

- Blocking: yes
- Status: open
- Owner: maintainer
- Finding: PR-017
- Question: `run_suite_check` runs in the PRIMARY checkout by explicit design (`dh0uno`) and already concedes it "proves THE TREE is green, not that the lane's uncommitted state is". With `isolate_worktree` defaulting TRUE the agent commits only on `aw/lane/<id6>`, so the tree the gate measures does not contain the work being judged. Under the ABSOLUTE test that was merely conservative; under a SUBSET test it becomes a FALSE PASS, because a lane that broke every test it touched leaves the primary failing set identical to base and therefore earns integration. Note the incident this plan cites did it correctly by measuring the lane IN the lane (`5563 passed`) against a clean base clone. Options: (a) measure the lane side in an isolated checkout of the LANE BRANCH (mirrors E-02's base handling, avoids the `dh0uno` in-lane `.aw/state` problem by using a detached clone rather than the live worktree, and costs a second suite run); (b) move the delta to the ALREADY EXISTING combined-result gate by implementing `make_integration_validation_runner`, which today is a hardcoded `return True`, so the revalidation seam exists and is inert; (c) keep the primary-tree measurement and DROP the subset change, using the delta only to explain a refusal rather than to earn integration; (d) run the lane's suite in the lane worktree, REJECTED because `run_suite_check`'s docstring records the measured `dh0uno` reason a lane suite is permanently red. Recommendation: (b) as the architecturally correct home (it is the gate that actually guards main, and it is currently a no-op), with (a) as the smaller step if the maintainer wants this Set's shape preserved. This decides whether the plan is repairable in place or must be re-scoped, and may pull `agent_workflows/orchestrate_isolation.py` into `Scope-Paths`.
- Resolution or deferral rationale: OPEN. Every option changes WHERE the trust signal comes from, which is an architecture decision and the reason this review's verdict is REPLAN rather than revisions-applied.

## Validation and cross-check (verify before reporting done)

Validation-state rule: inspect evidence in a separate pass. Do not mark a `V-*` item complete from memory or from the matching execution checkmark.

- [ ] V-01 validates E-01
  - Required evidence: paste the extended `SuiteCheckResult` and the parsing code. Paste a real `run_suite_check` result AGAINST A GENUINELY RED SUITE showing the failing set populated, SORTED, and NON-EMPTY, obtained WITHOUT stubbing `capture_command`: that non-emptiness is the whole proof that F-12 was fixed rather than reproduced, since a dead parser yields `()` and looks identical in every other respect. State which mechanism made the output reachable and which OQ-04 option it implements, and if any raw output is now persisted anywhere, paste the record and confirm `aw sanitize --agent` is clean on it. Paste a case where the set is `None` (undeterminable) and state which condition produced it, confirming it is distinguishable from an empty tuple, INCLUDING the `passing=False`-with-no-parsed-failures case. Confirm by quoting the field that `passing` still means "observed exit 0" and its value is unchanged.
  - Observed evidence:
  - Result: pending

- [ ] V-02 validates E-02
  - Required evidence: paste the baseline capture code and the cache path, showing the key is the base commit sha. PROVE THE PRIMARY TREE WAS NOT MUTATED: paste `git status --short` and `git rev-parse HEAD` in the primary checkout immediately before and after a baseline measurement, both unchanged. State where the baseline ran (an isolated clone or scratch worktree of `base_head`) and confirm it was NOT a lane worktree, naming the `dh0uno` reason. Paste evidence the cache is reused within a run rather than re-measured per item.
  - Observed evidence:
  - Result: pending

- [ ] V-03 validates E-03
  - Required evidence: paste the changed branch of `integration_is_earned`. Show FIVE verdicts with their reasons: identical sets (earn), strict subset (earn), one new node (REFUSE, and the reason NAMES that node), `None` on either side (refuse), and `passing=False` with an empty failing set (refuse, per F-12). State explicitly WHICH TREE produced the lane-side result and how OQ-05 was resolved; if the answer is still the primary checkout, this V-item FAILS, because a subset test over a tree that lacks the lane's commits earns integration for a lane that broke everything. Paste the validation-ON branch and confirm by diff that it is byte-unchanged, plus one probe showing a declined verifier still refuses even with a green suite.
  - Observed evidence:
  - Result: pending

- [ ] V-04 validates E-04
  - Required evidence: paste the baseline timeout wiring showing it reuses `SUITE_CHECK_TIMEOUT_SECONDS` rather than a new constant. Paste a simulated baseline timeout showing the verdict REFUSES (not passes) and the reason says so. Quote all three honest limits from the updated docstring and confirm the pre-existing "HONEST LIMIT" text was extended rather than replaced.
  - Observed evidence:
  - Result: pending

- [ ] V-05 validates E-05
  - Required evidence: paste the ACTUAL output of the eight delta cases (the original seven plus the `passing=False`-with-empty-set refusal). Quote the assertion for the strict-subset case and state in one sentence why requiring set EQUALITY would have been wrong (OQ-02). Paste the incident fixture using the real numbers (base `31 failed, 5505 passed`, lane `31 failed, 5563 passed`, identical FAILED sets) and show it EARNS. Paste the refusal message for the new-failure case showing it names the offending node. Paste the NON-STUBBED test (real `capture_command`) and state which assertion in it would fail if the `stdout_excerpt` bug were reintroduced; a suite in which every test stubs that function does not satisfy this item.
  - Observed evidence:
  - Result: pending

- [ ] V-06 validates E-06
  - Required evidence: THE PROOF THAT MATTERS. For BOTH hosts, in a throwaway repository with one permanently failing unrelated test: paste the lane INTEGRATING to main (show the merge commit on main). Then paste the same scenario against the PRE-FIX code showing it strands, so the contrast is demonstrated rather than asserted. Then paste the refusal case: a lane that breaks a previously-passing test does not integrate and `git branch --list` shows its lane preserved. THE REFUSAL CASE MUST RUN WITH `isolate_worktree` AT ITS DEFAULT (TRUE) and the breaking change committed ONLY on the lane branch; state that explicitly and paste the evidence (e.g. `git log --oneline main..aw/lane/<id6>` showing the breaking commit is lane-only). A refusal reproduced with `--no-isolate-worktree`, or with the break applied to the primary tree, does NOT satisfy this item: F-13's false pass exists precisely in the shipped isolated configuration, so that variant is the only one that tests it. Paste the BARE `python3 -m pytest` summary with before/after counts (re-measure your own before-baseline; the recorded one is stale, F-16) and show the AFTER-minus-BEFORE failure set is empty. If the agy host could not be demonstrated, SAY SO PLAINLY rather than inferring from the oc result.
  - Observed evidence:
  - Result: pending

## Approval and execution gate

- Size assessment: standard
- Cohesion rationale: not required

EXECUTION IS BLOCKED ON OQ-04 AND OQ-05, and human approval alone clears neither. Both carry `- Blocking: yes`, so the pre-execution checkpoint refuses while either is open (verified at review that `plan_readiness.has_unresolved_blocking_question` now returns True and names them). Each is a measured impossibility in the plan as authored, not a preference: OQ-04 because the text E-01 parses is always empty, so the delta would be empty and every lane would earn integration; OQ-05 because the tree supplying the lane side does not contain the lane's commits, so a subset test over it is a false pass rather than a better question. THE DIAGNOSIS IN THIS PLAN IS SOUND AND WORTH KEEPING; the prescription needs re-scoping around whichever answers the maintainer gives, which is why the review verdict is REJECT - NEEDS REPLAN rather than revisions-applied.

THE MINIMUM SHAPE OF A SOUND REPLACEMENT, so the rework is not started from nothing: (1) a first plan that makes the suite's own output reachable to its caller and proves it with a test that does NOT stub `capture_command`, which also repairs the already-dead summary regex and is independently valuable; then (2) a plan that puts the no-new-failures comparison where the code being judged actually exists, most plausibly by implementing the inert `make_integration_validation_runner` so the combined-result gate revalidates rather than returning True; and only then (3) the subset predicate, which is a handful of lines once its two inputs are real. Steps (1) and (2) are each independently testable and neither depends on the other.

Execution requires explicit human approval (`- Status: approved`) IN ADDITION to resolving both blocking questions.

Scope fence: touch ONLY the five paths in `Scope-Paths`. Do NOT change the meaning of `passing`. Do NOT touch the validation-ON branch of `integration_is_earned`. Do NOT weaken the fail-closed posture for a suite that cannot run. Do NOT reach the base by `git stash`, `git checkout`, or any mutation of the primary checkout. Do NOT measure the baseline in a lane worktree. Do NOT add a flag that lets an operator disable the delta check. Do NOT repair the pre-existing failing tests. Do NOT edit spec `25kzda`. If the work GENUINELY requires a path outside the fence, MAKE THE EDIT AND JUSTIFY IT, since `aw ipd finalize` refuses to complete until every out-of-scope path carries a `--scope-reason` and every declared-but-unmodified path carries a `--scope-ack`.

Commit ONLY the files you changed, path-scoped (`git commit -m msg -- <paths>`), never `git add -A`, never push. Verify with `git diff --cached --name-only` before every commit and RE-VERIFY after any failed hook. THIS IS A SHARED CHECKOUT and both driver modules are the highest-contention files in it: run `aw runs` before starting, and if a driver file is being changed under you and the two sets of changes cannot be safely combined, STOP and report rather than overwriting.

HARD HONESTY RULE: tests must be RUN and their ACTUAL output pasted into `Observed evidence`, including the `N passed` summary line from a BARE `python3 -m pytest`. A `V-*` item may not be marked `pass` from the matching execution checkmark. Do not move this plan to `.aw/records/plans/executed/` until `aw ipd lint --phase pre-transition` conforms and every `V-*` carries concrete pasted evidence.

BASELINE HONESTY: the suite is NOT green at HEAD and this plan does not make it green. The pass criterion is a DELTA (after-minus-before failure set is empty), not an absolute count. Do not report the pre-existing `test_orchestrator_retirement` failure as caused by this change, and do not fix it here.

RE-LOCATE BY SYMBOL, NEVER BY THE LINE NUMBERS IN THIS PLAN. Find `SuiteCheckResult`, `run_suite_check`, `integration_is_earned`, `IntegrationVerdict`, both `run_suite_check` call sites, and `ipd_lifecycle.read_receipt` by name.

THE ITEM THAT MATTERS MOST IS V-06's PRE-FIX CONTRAST. This plan's whole claim is that a lane no longer strands because of an unrelated red test. A test suite that only ever runs against the FIXED code cannot demonstrate that; it would pass identically if the delta logic were subtly inverted. Show the same scenario failing before and succeeding after.

THE SECOND-MOST IMPORTANT IS THE FAIL-CLOSED PATH IN V-03. This change makes a previously-refusing gate pass in more cases, which is exactly the direction in which a mistake becomes dangerous: a bug that makes the delta LOOK empty would auto-integrate unverified work into main. An undeterminable set MUST refuse. If you find yourself defaulting an unknown to "no failures", stop.

AND NOTE THAT REVIEW MEASURED BOTH OF THOSE DANGERS ALREADY PRESENT IN THE PLAN AS AUTHORED, which is the strongest argument for its own gate. The `stdout_excerpt` bug (F-12) is exactly "a bug that makes the delta LOOK empty", and the primary-tree measurement (F-13) is exactly an auto-integration of work the gate never examined. Neither was hypothetical and neither would have been caught by the plan's own V-items as first written, because V-01 asked only that a failing set be "populated" without requiring it be non-empty against a genuinely red suite, and V-06's refusal case did not pin `isolate_worktree` to its shipped default. Both V-items are now tightened accordingly.

On completion, close backlog `ciesaj`, which this plan carries as `- From-Backlog:` and whose `- Blocks-Release: next` gate it inherits.
