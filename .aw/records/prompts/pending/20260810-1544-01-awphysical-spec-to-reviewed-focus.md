<!-- aw-prompt: Kind: run-once | Status: pending | Created: 2026-08-10 | Author: opencode (Opus 4.8, its_direct/pt3-claude-opus-4.8-1m-us) | Targets: GPT-5.6 High (Codex), author of the physical-layout spec | Concerns: .agents/docs/specs/20260810-1447-01-physical-aw-hierarchy-placement-and-migration.spec.md + tools/awphysical/migration-scenarios.json | Relation: a focused subset of .agents/prompts/pending/20260810-1530-01-awphysical-residual-reconciliation.md | Goal: close the SPEC-level findings so the spec can advance to reviewed . This HTML comment is pipeline metadata only; it is invisible when pasted into a chat and is not part of the prompt. -->
You are GPT-5.6 High (Codex), author of `.agents/docs/specs/20260810-1447-01-physical-aw-hierarchy-placement-and-migration.spec.md`. I am Opus 4.8 (opencode). You are already working the full residual reconciliation in `.agents/prompts/pending/20260810-1530-01-awphysical-residual-reconciliation.md` (R1-R5 across the spec, 13 plans, tools, and schema). This is NOT a new work item: it is a PRIORITIZATION note that carves out the SUBSET of `1530-01` that gates the spec's own advancement from `to-review` to `reviewed`, so the maintainer can advance the spec without waiting on the plan/tool work (R2-R5).

Please address these SPEC-level items first and hand the spec back for an independent re-review. The per-plan/tool residuals (R2 V-evidence discrimination, R3 durability enum wiring in Order 05/code, R4 postcheck implementation, R5 Order-11 plan gates) may follow separately; they do not block the spec's review. Keep the spec `Status: to-review`; do not set `reviewed` or `approved` (an agent cannot; the maintainer approves, and I re-review before `reviewed`). Commit path-scoped; do not execute, migrate, push, tag, or release. Keep `aw specs check` clean.

# S1 (blocking the spec's advance): close the Section 12 crosswalk evidence gap

This is the one HIGH-severity spec finding. Section 12 maps the old 25 scenarios onto the 42-row catalog `tools/awphysical/migration-scenarios.json`, but two spec-mandated behaviors are mapped to AWP scenarios whose `expected` sets do not encode them, so the "scenario set equals evidence set" gate (Order 12) can pass while the behaviors ship untested. Verified against the file:

- Old #6 "incomplete/first-install fails closed" -> mapped to AWP-041, but AWP-041 is `malformed-or-future-policy` with `expected: [fail-closed, no-policy-overwrite, source-specific-diagnostic]` (malformed/future POLICY CONTENT), not closed-stdin/EOF/missing-choices at first install. The tokens `eof`/`stdin` do not appear anywhere in the catalog. Spec Section 8 ("A closed stdin or incomplete noninteractive first install fails before writes and prints the missing choices") and Section 13 ("first-install EOF fails closed") make this a MUST/acceptance criterion.
- Old #22 (color/terminal modes) and #23 (screen-reader without color) -> mapped to AWP-001, but AWP-001 (`fresh-private-target`) has `expected: [durable-material-target-tracked, local-config-ignored, runtime-ignored, no-migration-required]` - no color/`NO_COLOR`/`FORCE_COLOR`/`TERM=dumb`/screen-reader assertion. The token `color` does not appear anywhere in the catalog (`accessib` appears only inside `inaccessible-external-root`). Spec Section 8 mandates the accessibility surface; Section 13 requires it.

Fix: add real coverage so each of old #6/#22/#23 binds to a concrete executable assertion - either add `expected` tokens to an existing scenario or, cleaner, add dedicated AWP scenarios (for example a first-install-EOF-fail-closed scenario with `expected` including `fail-before-write`/`missing-choices-printed`/`no-policy-written`, and a color/screen-reader-accessibility scenario with `expected` including `no-color-honored`/`term-dumb-safe`/`screen-reader-linear`). Then strengthen Section 12 so the machine-checkable crosswalk Order 12 builds asserts PER-BEHAVIOR coverage: each retained/paired old scenario must name the concrete assertion/test that proves its behavior, not merely that a parent AWP id exists. Update the Section 12 disposition rows for #6/#22/#23 to point at the real coverage.

# S2 (minor spec clarifications, cheap; fold in to reach a clean review)

1. Durability-enum redefinition note. Section 10 lists observable states `unversioned, local-git, acknowledged-durable, repository-managed, unreachable, unknown`, but the shipped `agent_workflows/project_schema.py` `DurabilityState` enum has five values (`unversioned, local-git, durable-private, repository-managed, unknown`) - no `unreachable`, and `durable-private` rather than `acknowledged-durable`. Add one sentence (Section 7 or 10) stating this cutover redefines the durability enum: `durable-private` maps to `acknowledged-durable`, `unreachable` is added, and the schema owner updates it (naming the owning Order). (The actual code/enum change stays in the R3 plan/tool work; the spec just needs to acknowledge the redefinition so it is not an unstated contradiction.)
2. Postcheck independence definition. Section 11.2 step 10 and acceptance line 212 require an "independent" compare/postcheck but never define independence, so it is satisfiable by re-running the migration's own code. Add a one-line criterion, e.g.: the postcheck MUST re-derive expected content from the frozen inventory and the destination filesystem/Git, not from the migration transaction's own in-memory map, and MUST emit deterministic (sorted, digested) output.
3. All-preset scenario counting. Section 11.3/13 say "all 42 scenarios pass," but some catalog rows carry `"preset": "all"`. Clarify that an `all`-preset scenario must pass for every applicable preset (so the "42" count is not satisfied by a single preset run of an `all` scenario).

# Deliverable and boundary

Edit only the spec `20260810-1447-01` (kept `to-review`) and `tools/awphysical/migration-scenarios.json` (plus, if you choose, the Section 12 table). Keep `aw specs check` clean. Do NOT set the spec to `reviewed`/`approved`, do not execute any IPD, do not migrate/push/tag/release. When done, report exactly what changed in the spec and the catalog; I will re-review the spec, and if the crosswalk gap is closed and the minors folded in, it advances to `reviewed` and then to the maintainer for approval.

# ADDENDUM (2026-08-10, after independent re-review of the 1530-01 pass, commit cc2d184)

Opus 4.8 ran a full independent re-review of your 1530-01 reconciliation. Most residuals are verified CLOSED (R1 crosswalk with real AWP-043/044, R3 durability enum in code, R4/R5 postcheck independence in the tool, Orders 04/07/08/10/11/12). Full suite 825 OK; gates conform. Status of THIS prompt's items after that pass:

- S1 (crosswalk): the catalog fix is DONE (AWP-043 EOF-fail-closed and AWP-044 color/screen-reader exist with real `expected` tokens; Section 12 requires per-behavior tokens and a machine check enforces token-in-`expected`). One residual (S-01): Section 12 also says each assertion must "map to a named automated test assertion," but nothing yet binds a token to a real test - fold that binding into Order 12 or note it as an Order-12 acceptance gate.
- S2.1 (durability-enum redefinition NOTE in the spec): still OPEN in the spec text - please add it (Section 7 or 10).
- S2.2 (postcheck independence DEFINITION in the spec): still OPEN in the spec text (the tool already behaves this way; the spec just needs the one-line normative definition).
- S2.3 (all-preset scenario counting): still OPEN in the spec text (note: catalog is now 44 scenarios, not 42 - phrase the clause as "scenarios x applicable presets").

Also close these NON-spec residuals from the re-review (they do not block the spec's advance to `reviewed`, but should land before execution):

- L07-01 (MEDIUM): Order 07's evidence-matrix test module still targets the SUPERSEDED `tests/test_layout_migration.py` (awlayout-owned). You moved Orders 06 and 08 off it but missed Order 07 - repoint Order 07's rows to a new physical-layout module (e.g. `tests.test_awphysical_migration`).
- L04-01 (HIGH-severity, LOW-risk): Order 04 E-05 execution item says "reusing the existing `engine.py` `is_self` mechanism," but `is_self` (engine.py ~1930) is path-equality only, which spec Section 9 declares insufficient (needs package-metadata + Git-common-dir) and which E-05's own spoof/ambiguous matrix row requires to reject. Reword E-05 to EXTEND `is_self` with positive-identity evidence, and add "path-equality-only detection is retained" as a failure condition.
- S-02 (LOW code wart): `agent_workflows/project_schema.py` declares `DURABLE_PRIVATE = "acknowledged-durable"`, which Python collapses into an ALIAS of `ACKNOWLEDGED_DURABLE` (same value) - it does not carry the legacy string and is dead/misleading. The working legacy mapping is `LEGACY_DURABILITY_STATE_ALIASES`. Remove the enum member (keep the dict), or the spec note should say the historical spelling lives only in the normalization table.
- R2 (MEDIUM, Set-wide): the V-evidence discrimination upgrade (named negative/planted-violation fixtures + per-condition assertions instead of the generic "command is nonzero, fixture is absent, or the stated positive assertion is not observed") was applied only to the specifically-named rows (O3 E-01/E-04, O4 E-05/E-07, O8 E-03/E-07). Orders 01, 02, 05 got little/none, and O7's non-kill fault classes assert only exit+phase. Apply the same discrimination to the safety-critical rows across the Set: O1 never-track config_local/state_runtime; O2 six-class separate-resolution + public-output leak-sanitizer canary; O5 leakage/identity/per-durability-state + revoke->downgrade; O6 E-05/E-07; O7 switch-failure/rollback/resume/source-protection with the kill-case's per-hash/single-authority depth.
- NEW-01 (Order 09): `agent_workflows/clean_delta.py` is still the fabricated hardcoded-`target_writes: 0` module (untouched by cc2d184). Order 09 E-01 correctly schedules its replacement, but add a named negative assertion to E-01/E-05 that `install_clean_delta` fails / does not report `0` when a planted AW-owned target write exists, so the literal cannot survive execution.

Boundary unchanged: keep the spec `to-review`, do not approve/execute/migrate/push/tag/release; commit path-scoped. When done, hand back for a final re-verify.
