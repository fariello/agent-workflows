- Id: hhr83h
- Status: done
- Set: hhr83h
- Priority: low
- Work-Kind: followup
- Summary: Confirm the fix shape for spec 25kzda Section 7's false oc-descriptor capability claim

## Workflow history
- 2026-09-25 done (aw set): OBSOLETE at 877545fc, closed during graduate-top10 triage: fixed: 25kzda worked example reads ASSUME FOR THIS EXAMPLE; open question tracked by b7tlsh
- 2026-09-21 created (aw backlog): Filed by plan 4h7tt0 (Set nopush) as defect finding F-A while retiring RUN-NO-PUSH from spec 25kzda 4.2. THE DEFECT IS ALREADY FIXED; this item exists only so a maintainer can confirm or change the SHAPE of the fix, which is a judgement call about an approved spec's worked example rather than a fact. THE DEFECT: Section 7 asserted that 'The current oc capability descriptor positively proves standard isolated-worktree, commit-gateway, hook, fresh-session, and no-push enforcement'. That is FALSE and was false independently of RUN-NO-PUSH's retirement: supports_commit_gateway and supports_deny_push both default False and are mapped to None in host_sandbox_profile._RUNNER_SAFETY_PROBES, i.e. DECLARED AND NEVER PROBED, which the same spec's Section 1 preamble already records; measured detect_host_capabilities('opencode').supports_deny_push is False. So the spec asserted proof of an enforcement it elsewhere records as unprobed, which is the fail-open reading the spec exists to refuse. THE FIX APPLIED (plan 4h7tt0 E-05, decision D-3): the sentence was converted from an assertion about the CURRENT descriptor into an explicit ASSUMPTION for the example, with the correction stated and attributed inline. The two capability names were deliberately KEPT, because the same example's action packet declares required_host_capabilities ['isolated_worktree','commit_gateway','deny_push'] and its queue trace shows draft2 and free07 reaching verified, so deleting them would make the example contradict itself. Section 7 already opens with 'Assume the repository contains these items', so the mood is the section's own. THE ALTERNATIVE A MAINTAINER MAY PREFER: delete commit-gateway and no-push from the sentence AND rewrite the example's action packet and queue trace so no item requires a capability the real descriptor refuses. That is a larger, more honest edit which would make the worked example demonstrate the fail-closed path for those two capabilities as well as for controlled_network_allowlist. It was not chosen here because it rewrites teaching material in an approved spec well beyond the retirement's scope. No code or test depends on this sentence; the change is prose and reversible.
