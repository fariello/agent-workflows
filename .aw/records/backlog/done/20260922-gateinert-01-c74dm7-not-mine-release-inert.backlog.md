- Id: c74dm7
- Status: done
- Blocks-Release: next
- Set: gateinert
- Priority: medium
- Work-Kind: bug
- Summary: A 'not-mine' gate answer is recorded as releasing integration and then the item is still refused terminally, so the override the runner documents has no effect

## Workflow history
- 2026-09-23 set (aw backlog): closed by aw oc run: IPD n9na1c executed (every IPD carrier is executed and this run executed .aw/records/plans/executed/20260922-gateinert-01-n9na1c-make-a-usable-not-mine-gate-answer-actually-release-the-inte.ipd.md); evidence .aw/records/plans/executed/20260922-gateinert-01-n9na1c-make-a-usable-not-mine-gate-answer-actually-release-the-inte.ipd.md
- 2026-09-22 graduated (aw set): Graduated to plan n9na1c (.aw/records/plans/pending/20260922-gateinert-01-n9na1c-make-a-usable-not-mine-gate-answer-actually-release-the-inte.ipd.md). The plan carries '- From-Backlog: c74dm7' and inherits this item's 'Blocks-Release: next' gate, so the release gate is provably handed off. Design handed off; code not yet written, hence graduated rather than done.
- 2026-09-22 open (aw set): CORRECTION TO THIS ITEM'S OWN FILING, measured more carefully. The original text claimed the 'not-mine' release 'has no effect' and that the release 'either never reached the ladder or the ladder overrode it'. THAT IS WRONG and is retracted. Re-reading ld8lb3's attempt record: attempt['integration_detail'] ends 'RELEASED by the agent's gate answer (not-mine)', attempt['finalized'] is True, and the plan DID reach executed/ on the lane branch (commit 78891bde). So the release worked exactly as documented: it set integration.earned, which gates SELF-FINALIZE, and self-finalize ran. THE REAL SHAPE IS THAT THERE ARE TWO INDEPENDENT GATES AND THE ANSWER ONLY GOVERNS THE FIRST. Gate 1 is the per-lane suite signal (integration_is_earned / integration.earned), which the 'not-mine' answer legitimately releases. Gate 2 is the POST-MERGE revalidation inside make_integration_validation_runner, reached later through integrate_lane_branch, which re-runs the suite on the merged tree and refuses on its own, consulting neither the answer nor the baseline. So the agent's well-founded 'not-mine' is honored once and then silently re-litigated by a second gate that never saw it. THE DEFECT IS THEREFORE 'the gate answer does not propagate to the post-merge gate', NOT 'the release is inert'. That is narrower and it changes the fix: the answer (or at least its attributed failing-id set) must reach gate 2, so a failure the agent attributed away is not re-blamed after the merge. This overlaps fuk1mr (baseline subtraction) but is not the same: baseline subtraction handles ids that were ALREADY red, whereas the answer covers ids the agent attributed away for other reasons (landed on main mid-turn, flake, order-dependence) that no baseline can catch. STILL OPEN AND STILL WORTH FIXING; priority lowered high -> medium because the primary strand cause is fuk1mr and this is the second line of defense rather than the trigger. The observation about 65cuw0 (integration_gate_answer null, never asked) stands and is unaffected.
- 2026-09-22 created (aw backlog): MEASURED in run-20260922T024054Z-2245533 on item ld8lb3. The runner's own documented contract (runner_shared.py:14865) says: 'not-mine -> RELEASE this attempt's integration.earned, so self-finalize and integration run.' On ld8lb3 the agent WAS asked, answered 'not-mine', and the run recorded integration_gate_answer.answer='not-mine', usable=True, AND integration_released_by_answer='not-mine' -- i.e. the release was recorded as having happened. The item's final status is nevertheless 'merge-refused', and its integration_ladder records deferrable=false with the verdict 'integration refusal kind merge-refused is terminal on its first attempt: ... so it asserts a real failure of the work'. So the release either never reached the ladder or the ladder overrode it. The agent's recorded reason was well-founded and independently confirmed: it named 'except Exception: pass' at runner_shared.py:16881 introduced by commit 894d7924 INSIDE the region tests/test_defect_report.py fences, proved it failing at its own base commit 301a1d8fbc15, re-ran with its four source changes stashed out to show 2 failed in 0.59s, and filed it as backlog p9ag41 rather than fixing another agent's code. That is exactly the good-faith 'not-mine' the feature exists to honor, and it was honored in the RECORD and ignored in the OUTCOME. WHY THIS IS A SEPARATE DEFECT from the baseline gap (backlog fuk1mr): fixing the baseline subtraction would stop the question from being ASKED here, but this path must still work, because 'not-mine' is the designed escape hatch for every case the baseline cannot cover (a failure that landed on main while the lane worked, a flake, an order-dependence). Note 65cuw0 hit the SAME combined-red refusal with integration_gate_answer=null, i.e. it was never asked at all, so the ask itself is also not reliably reached; a fix should determine why one item was asked and the other was not. A FIX MUST make a usable 'not-mine' actually release the refusal (or, if release is deliberately narrower than the docstring says, correct that docstring and the operator-facing verdict text so they stop promising an override that does not occur). Regression test should assert an item with a usable 'not-mine' answer reaches integration rather than a terminal 'merge-refused'.
