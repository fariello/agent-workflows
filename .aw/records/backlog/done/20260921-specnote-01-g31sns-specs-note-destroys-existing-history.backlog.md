- Id: g31sns
- Status: done
- Blocks-Release: next
- Set: specnote
- Priority: high
- Work-Kind: bug
- Summary: aw specs note truncates a spec's inline workflow history by design, but the sidecar it defers to is gitignored, so tracked amendment provenance is permanently lost

## Workflow history
- 2026-09-23 done (aw set): Already FIXED by executed plan vhbvwz (setterguard Order 02, commit fbf85068), which reversed the slimming in specs._append_history and backlog._reattach_history so both PREPEND and PRESERVE prior rounds. Verified live at HEAD 22cf67d9 by probing specs._append_history on a 3-round fixture: 4 rounds out, all 3 priors intact. The maintainer's 2026-09-10 ruling (vhbvwz OQ-01) made inline history the DURABLE home for specs and backlog items, matching plans, so this item's fix-option (a) (re-track the gitignored sidecar) is settled against. Closed via the SATISFIED path. What SURVIVED this family is tracked by IPD 7jqev2 (histresid): the migration slimmer, the oldest-first legacy reader, and the missing specs dedup.
- 2026-09-21 set (aw backlog): CORRECTION TO THE FILING ABOVE, and it changes the diagnosis rather than softening it. The truncation is NOT a regression and NOT an accident: it is DELIBERATE, documented behavior. agent_workflows/specs.py:342 _append_history carries the comment 'awhistory Order 02: the inline Workflow history section keeps only the LATEST record; the full chronological log lives in the global .aw/records/history.jsonl sidecar' and its body literally does out[i+1:end] = ['', record]. So 'aw specs note is buggy' is the WRONG framing and I withdraw it. THE REAL DEFECT IS THAT THE DESIGN'S DURABILITY PREMISE IS FALSE. Truncating the inline section is only safe if the sidecar preserves what was dropped, and the sidecar is GITIGNORED: '.aw/.gitignore:11: records/history.jsonl', confirmed with git check-ignore -v, and git ls-files reports the path is not tracked at all. MEASURED CONSEQUENCE on this very file: .aw/records/history.jsonl on this machine holds exactly TWO records (my own note plus this backlog filing, both written minutes ago), while spec 25kzda's inline history held FIVE records spanning 2026-09-13 to 2026-09-20 that the sidecar has NEVER seen, because they predate this checkout's sidecar. Had I committed the tool's output, those five amendment records (the 2026-09-20 factual-status correction, the 2026-09-19 orchprobe-03 2.5b record, both 2026-09-14 flag-declaration records, and the 2026-09-13 5.3a telemetry record) would have been deleted from TRACKED history with no tracked copy anywhere, and the only surviving copy would be an untracked, per-machine, gitignored file that a fresh clone does not have and that no other agent or CI can read. That is permanent loss of a tracked audit trail on every 'aw specs note' against any spec whose inline history predates the local sidecar. WHY IT IS STILL SEVERE AND STILL BLOCKS RELEASE: the managed AGENTS.md block instructs agents to record spec amendments through the tooled path, so following the documented instruction silently destroys provenance, and the loss is invisible to a normal review because the rendered file still shows a plausible history containing the newest record. I restored the five records by hand from git show HEAD:<path>, so my own committed diff is a pure 50-line insertion with zero deletions. WHAT A FIX MUST DECIDE, since this is a design question and not a one-line patch: either (a) track the sidecar (stop gitignoring it) so the premise becomes true, or (b) stop truncating the inline section, or (c) truncate only records the sidecar provably already contains. ALSO CORRECTED: the sibling verbs are NOT all affected. agent_workflows/backlog.py:871 is explicitly documented as 'Preserve prior Workflow history records and append one transition record', and status_set.py:710 and :1077 state records are PREPENDED newest-first and 'NOT appended'. So plans and backlog items preserve their history and only the SPECS writer truncates, which also means this repository is internally inconsistent about a contract it states in three places.
