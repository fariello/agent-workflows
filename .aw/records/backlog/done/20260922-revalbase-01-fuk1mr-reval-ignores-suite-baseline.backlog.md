- Id: fuk1mr
- Status: done
- Blocks-Release: next
- Set: revalbase
- Priority: high
- Work-Kind: bug
- Summary: Post-merge revalidation ignores the pre-work suite baseline it already measured, so a pre-existing failure refuses a verified lane as combined-red

## Workflow history
- 2026-09-22 set (aw backlog): closed by aw oc run: IPD tgyfs2 executed (every IPD carrier is executed and this run executed .aw/records/plans/executed/20260922-revalbase-01-tgyfs2-subtract-the-measured-suite-baseline-before-declaring-combin.ipd.md); evidence .aw/records/plans/executed/20260922-revalbase-01-tgyfs2-subtract-the-measured-suite-baseline-before-declaring-combin.ipd.md
- 2026-09-22 graduated (aw set): Graduated to plan tgyfs2 (.aw/records/plans/pending/20260922-revalbase-01-tgyfs2-subtract-the-measured-suite-baseline-before-declaring-combin.ipd.md). The plan carries '- From-Backlog: fuk1mr' and inherits this item's 'Blocks-Release: next' gate, so the release gate is provably handed off. Design handed off; code not yet written, hence graduated rather than done.
- 2026-09-22 created (aw backlog): MEASURED in run-20260922T024054Z-2245533: lanes ld8lb3 and 65cuw0 both ended 'merge-refused' with integration_deferral 'integration_failed_combined_red', and in BOTH cases the post-merge revalidation's failure list is EXACTLY the single test the run's OWN per-attempt suite_baseline already recorded as failing before the work existed: 'FAILED tests/test_defect_report.py::ValidatorTests::test_no_bare_except_was_introduced_around_the_new_code'. ld8lb3 post_merge_revalidation.failures == that one id, and its attempt's suite_baseline.failures == that same one id (baseline summary '1 failed, 8042 passed'; post-merge '1 failed, 8064 passed'). 65cuw0 identical (baseline '1 failed, 8063 passed'; post-merge '1 failed, 8084 passed'). In both the passed count ROSE by exactly the tests the lane adds and the failing SET is unchanged, which is the textbook definition of 'no new failures'. ROOT CAUSE, located: make_integration_validation_runner (runner_shared.py:15415) decides pass/fail purely on 'passed = bool(getattr(result, "passing", False))' from the merged-tree suite run, and the whole function body contains ZERO references to any baseline (verified: 'sed -n 15415,15660p | grep -c baseline' -> 0). So the gate asks the ABSOLUTE question 'is the merged tree green?' when the only sound question is the RELATIVE one 'did this lane introduce a failure that was not already there?'. The machinery to answer it ALREADY EXISTS and is already populated on the same item: SuiteBaseline, start_suite_baseline, and suite_baseline_context (:14513), which the per-lane gate-answer path consumes to let an agent answer 'not-mine'. The post-merge gate simply never consults it. THE ASYMMETRY IS THE BUG: one half of the system knows what was already red and the other half re-discovers it and blames the lane. CONSEQUENCE: two fully verified lanes were stranded for an 8.5-hour unattended run and cascaded 3 more items to dependency-blocked (ut0vzr, k311gw via 65cuw0; lkexaw via 8u6770); a human then had to re-merge them by hand, and the suite was green on the first try (8474 passed) because there was never anything wrong with the work. A FIX MUST subtract the known-failing baseline set before declaring combined-red: refuse only on failing ids ABSENT from the baseline, and when the baseline is not known (baseline.known False) keep today's fail-closed behavior rather than assuming green, exactly as suite_baseline_context already states absence as UNKNOWN and never as an empty set. Regression test should pin that a merged tree whose failing set EQUALS the baseline's integrates, and that one NEW failing id still refuses.
