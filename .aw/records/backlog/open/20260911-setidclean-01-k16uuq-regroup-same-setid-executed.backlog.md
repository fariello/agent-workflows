- Id: k16uuq
- Status: open
- Set: setidclean
- Priority: low
- Work-Kind: chore
- Summary: Give the six same-setid executed plans distinct setids so the within-type descriptive rule reports nothing

## Workflow history
- 2026-09-11 created (aw backlog): Filed 2026-09-10 from an /askme round on 216rgg OQ-01, per the rule that no defect may be raised without a carrier. THE DEFECT: six plans in executed/ share a setid with a DIFFERENT descriptive, which is the one within-type case the re-scoped check.setid-collision rule legitimately keeps. Measured with aw check all --all (they are ABSENT from the default scan): ez65jl, 6cdker and s3axqd all carry setid release-review with different descriptives; kq6akq carries leak-sanitizer; g2payb assess-documentation; hq4p3a assess-bugs. THE MAINTAINER'S OWN SUGGESTION, chosen over widening the rule's scope: rename them with aw group so the names are correct, after which the rule reports zero because nothing is wrong rather than because it is looking away. Previewed clean: aw group plans ez65jl --set relrev01 --rename yields 20260711-relrev01-00-ez65jl-..., touching one file. Citation cost is small: each id6 appears in only 2-4 TRACKED files. STATUS: the maintainer RAN all six --apply commands on 2026-09-10; the on-disk renames and index rewrite SUCCEEDED and were committed manually after the verb's own self-commit was refused (that refusal is a separate defect, backlog mqmlug). TWO TRAPS FOR ANY FUTURE SIMILAR WORK. FIRST, these setids are also LIVE WORKFLOW NAMES: .aw/system/workflows/release-review/ exists and the token appears in 201 tracked files, leak-sanitizer in 58, and assess is also a workflow, so a rename must touch only plan filenames and their citations, never the workflow or prose about it. SECOND, an open design question this cleanup did not settle: all three release-review plans were Order 00, i.e. three orchestrators claiming one Set, so an arguably better fix was ONE shared setid with distinct Orders, which is what the D153 shared-topic ruling endorses; splitting them into relrev01/02/03 was chosen for the smaller blast radius on finished records. If a similar case arises on ACTIVE plans, prefer the shared-setid-plus-Order shape.
