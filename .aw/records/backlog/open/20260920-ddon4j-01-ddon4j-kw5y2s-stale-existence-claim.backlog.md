- Id: ddon4j
- Status: open
- Set: ddon4j
- Priority: medium
- Work-Kind: chore
- Summary: Spec kw5y2s states aw check reviews fails with unknown artifact type reviews, and that verb now succeeds

## Workflow history
- 2026-09-20 created (aw backlog): Found and RE-VERIFIED at HEAD 007d05e1 while executing plan wenmg4, which corrected the same defect class in spec 25kzda. This is the follow-up OQ-04 of that plan recommends, filed because the same audit-and-correct shape has now recurred twice and an item is the cheapest way to stop it being rediscovered a third time. THE FACT: kw5y2s:124 (Status approved) says 'aw check reviews currently fails with unknown artifact type reviews. This is intended, and the...', presenting it as current behavior an implementing plan must change. MEASURED at HEAD: that verb SUCCEEDS. 'aw check reviews >/dev/null 2>&1; echo $?' -> 0, and '--agent' reports outcome conforms, exit 0, target reviews, findings 0; separately ''reviews' in artifact_types.ARTIFACT_TYPES' -> True. WHY IT MATTERS, which is the same harm 25kzda's infrastructure paragraph exists to prevent: a Set graduating from kw5y2s could set out to build a CLI type noun that already works, which is the duplication that destroyed a54m79 and forced hostcap-01 to be written. WHY plan wenmg4 did NOT fix it: kw5y2s is not in that plan's Scope-Paths, the runners announce declared spec edits before a run starts and the finalize scope gate reconciles them, so an undeclared second approved-spec edit is exactly the drift that machinery exists to catch. Reporting it was in scope; fixing it was not. SHAPE OF THE FIX: a factual-status correction to kw5y2s's line, on the a59f2c53 precedent that such an edit is not a design change and leaves the spec approved, ideally adopting the point-in-time snapshot convention plan wenmg4 added to 25kzda's preamble (state the measurement date and commit, and instruct the reader to re-measure) so the corrected line cannot decay invisibly the same way. Filed chore rather than bug: no shipped behavior is broken, the defect is documentation accuracy in an approved spec. NOT gated on a release for that reason; a maintainer may disagree, since the failure mode is wasted duplicate implementation work rather than a wrong answer.
