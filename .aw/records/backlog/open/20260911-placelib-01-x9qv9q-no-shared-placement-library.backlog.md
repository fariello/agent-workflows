- Id: x9qv9q
- Status: open
- Set: placelib
- Priority: medium
- Work-Kind: chore
- Summary: No shared placement library: four scattered sources of truth for which record types have status subdirs

## Workflow history
- 2026-09-11 created (aw backlog): Filed 2026-09-10 from an /askme round, on the maintainer's ruling that no defect may be raised without a carrier. THE DEFECT, measured at HEAD cf922221: there is NO relocation module. The only real relocation code is an if/elif chain at status_set.py:822-864 whose first branch covers ('plans','prompts') and whose second covers backlog. Each type stores its status-to-directory knowledge differently: backlog.STATUS_DIRS is a tuple at backlog.py:67, plans' dispositions are derived inline from a hardcoded 5-tuple, specs have none, and specs.py:945 places a new file with a bare _specs_root(repo_root)/filename. Nothing anywhere answers 'which types have status subdirs and what are they'. THE PATTERN HAS ALREADY FAILED THIS WAY ONCE: the comment at status_set.py:856-860 records the backlog branch being repaired after a hardcoded list silently DECLINED TO MOVE a file whose source dir was the new status, leaving directory and Status disagreeing. The same fork has bitten twice more in specs.py:550-553 and :563-568, both resolved by consuming ONE SHARED PREDICATE. Placement is the third instance. RELATIONSHIP TO EXISTING WORK: the maintainer chose 'build one placement library, then adopt it per type' when answering wfjsp4 OQ-04 and 1bdxcp OQ-04 on 2026-09-10, but those answers were recorded in two plans that must be RE-SCOPED and no carrier holds the library work itself, so this item exists so the decision cannot evaporate if either plan is retired. Scope: one component owning which types have status subdirs, what they are, and where any artifact belongs; every status-changing AND creation verb calls it; existing plans/prompts/backlog branches replaced by calls to it with regression evidence that their behavior is unchanged. Creation verbs are in scope: aw specs new currently ignores subdirs entirely.
