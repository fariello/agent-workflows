- Id: 0jxknk
- Status: open
- Set: 0jxknk
- Priority: low
- Work-Kind: chore
- Summary: Enforce the run finding table's abort tri-state counts so a prose comment cannot rot unnoticed

## Workflow history
- 2026-09-21 created (aw backlog): Filed by plan 4h7tt0 (Set nopush) as defect finding F-B while retiring RUN-NO-PUSH from spec 25kzda 4.2. THE STALE PROSE IS ALREADY CORRECTED; what this item carries is the UNENFORCED-NUMBER problem that let it rot, which the plan had no mandate to solve. THE DEFECT: run_evidence.py's abort-semantics comment read 'Two of the 13 codes abort UNCONDITIONALLY; eight abort ONLY under a named 4.1 class; three never abort', and may_abort_run's docstring said 'spec 4.1 licenses eight of the 13 codes to abort only under a named abort class'. MEASURED at HEAD 803d10f6 BEFORE any edit, Counter(r.abort for r in RUN_FINDING_CODES) was {'conditional': 6, 'never': 5, 'always': 2}, so the split was 2/6/5 and the comment claimed 2/8/3. Both sum to 13, which is why the error survived every reader who checked only the total. Corrected in plan 4h7tt0 to the post-retirement measured split 2/5/5, with the prior error stated inline so the retirement is not blamed for it. WHY IT ROTTED AND WHAT WOULD FIX IT: nothing asserts these three numbers. test_abort_tristate_agrees_with_the_specs_action_text enforces the MAPPING from each spec action string to each row's tri-state, which is the load-bearing property and is sound; and test_conditional_abort_is_not_reported_as_unconditional pins the two ABORT_ALWAYS codes by name. Neither pins the conditional or never COUNTS, so a prose summary of them can drift silently, exactly as it did. A small case asserting the measured partition (as test_measured_binding_partition_is_recorded already does for the BINDING partition, deliberately, so a change is reviewed rather than drive-by) would close it. CONSIDER ALSO deriving the numbers rather than restating them, or dropping them from the comment entirely and pointing at the Counter one-liner, since the enforced mapping test is what a reader should trust. Precedent for the tension: spec 25kzda's own preamble now carries an explicit point-in-time convention because its infrastructure paragraph decayed three times.
