- Id: 21fykf
- Status: graduated
- Blocks-Release: next
- Set: laneraceplan
- Priority: medium
- Work-Kind: bug
- Summary: A records-only plan that edits front matter across many pending plans conflicts with every plan the same run concurrently moves to executed/, stranding it

## Workflow history
- 2026-09-22 graduated (aw set): Graduated to plan kl18sz (.aw/records/plans/pending/20260922-laneraceplan-01-kl18sz-stop-a-bulk-front-matter-backfill-racing-its-own-run-by-re-d.ipd.md). The plan carries '- From-Backlog: 21fykf' and inherits this item's 'Blocks-Release: next' gate, so the release gate is provably handed off. Design handed off; code not yet written, hence graduated rather than done.
- 2026-09-22 created (aw backlog): MEASURED in run-20260922T024054Z-2245533 on item 8u6770 (planprio Order 02), which ended 'merge-refused' with a real git conflict in 13 files, ALL of them .aw/records/plans/*.ipd.md and NONE of them code. The shape is a pure lifecycle race, not a disagreement about content. 8u6770's job is to backfill '- Work-Kind:' and '- Priority:' onto pending plans by inheritance, so it read 44 plans in .aw/records/plans/pending/ and added two adjacent front-matter lines to each. While it ran, THE SAME RUN executed 13 of those very plans, which moves each file pending/ -> executed/ AND rewrites its '- Status:' line from 'approved' to 'executed' plus appends a history line. Git therefore sees both sides touching adjacent lines of a renamed file and conflicts. The two edits are not in opposition: the lane's own appended history line says in as many words 'status unchanged (no lifecycle transition)', so it never intended to touch Status at all. Resolution is mechanical (keep main's Status, keep the lane's two new fields, keep both history lines) and was verified green by hand, but it needed a human. WHY THIS RECURS RATHER THAN BEING A ONE-OFF: any plan whose scope is 'edit front matter across the whole pending/ population' is by construction contending with every execute item in the same queue, and the more successful the run, the more conflicts it causes. planprio has two more such plans (lkexaw, and lc4unl which hit the same class), so this is a plan ARCHETYPE, not an incident. POSSIBLE FIXES, for a human to choose between: (a) SEQUENCE it -- give a bulk-records plan an Item-Dependencies edge on, or an explicit ordering after, every execute item in its own run, so it runs last against a settled tree; (b) make the runner AWARE that a records-only lane touching only front-matter keys can be re-derived rather than merged, i.e. re-apply the field backfill to the post-merge tree instead of merging a stale snapshot of it; (c) at minimum, TEACH THE MERGE this narrow case, since 'one side rewrote Status and moved the file, the other added orthogonal keys' is losslessly resolvable and is the only conflict class 13 of 13 files exhibited here. Do NOT resolve it by taking the lane's Status, which would revert real executions (the trap: the lane's snapshot says 'approved' for 13 plans that had genuinely reached executed/).
