- Id: qztbeq
- Status: graduated
- Blocks-Release: next
- Set: stalemerge
- Priority: high
- Work-Kind: bug
- Summary: A content conflict against a stale lane base is terminal on first attempt and nothing ever re-merges the lane onto current main, so a trivially resolvable additive conflict permanently strands verified work

## Workflow history
- 2026-09-22 graduated (aw set): Graduated to plan 87apfx (.aw/records/plans/pending/20260922-stalemerge-01-87apfx-tell-a-cross-run-adjacency-conflict-from-a-real-failure-of-t.ipd.md). The plan carries '- From-Backlog: qztbeq' and inherits this item's 'Blocks-Release: next' gate, so the release gate is provably handed off. SCOPE NARROWED FROM THIS ITEM'S ORIGINAL PROPOSAL, per the correction already recorded above: the plan delivers HONEST REPORTING AND ROUTING (record which of six gate causes fired, stop asserting 'a real failure of the work' for a conflict that is not one, and hand a resolver the conflicting paths plus the peer commit and run) and explicitly EXCLUDES automatic resolution and base refreshing, both of which this item's own measurement refuted. It also excludes making a plain conflict deferrable, pinned as an anti-regression control, because no retry can clear a same-location double insertion. Design handed off; code not yet written, hence graduated rather than done.
- 2026-09-22 open (aw set): CORRECTION, MEASURED, AND IT REFUTES THIS ITEM'S OWN PROPOSED FIX (ii). The original text claimed '(i)+(ii) would have integrated both lanes WITHOUT any content resolution, because against the fresher base git merges both additive changes cleanly'. THAT IS FALSE and is retracted. I tested it directly with git merge-tree --write-tree on the real commits. For 92u0v9: merging the recovered lane tip 97255449 against f9a37808 (a base that ALREADY CONTAINS the peer commit 4y95tp) still reports 'CONFLICT (content): Merge conflict in agent_workflows/completion.py' (exit 1); merging the other direction (main INTO the lane, i.e. what a refresh or rebase performs) ALSO conflicts in the same file. For xipfy1: merge-tree of 908db905 against lane tip d1cc184f likewise conflicts in agent_workflows/runner_shared.py. So refreshing the base does NOT make these merge cleanly, and re-merging onto current main would NOT have integrated either lane unattended. WHY I WAS WRONG: I reasoned that because both sides only ADD code, git must be able to combine them. That is false when both additions land at the SAME insertion point. Git has no basis to order two unrelated insertions at one location, so it conflicts regardless of which base it computes against and regardless of merge direction. 'Purely additive' describes the SEMANTICS (no line either side wrote is modified, which is why a human resolution is trivial and lossless) and says nothing about MERGEABILITY. Those are different properties and I conflated them. WHAT SURVIVES, unchanged: DEFECT 1 is real and independently verified (classify_integration_refusal('merge-refused') returns False, so the refusal is terminal on first attempt). DEFECT 2 is real as a FACT (no rebase/refresh machinery exists) but is NO LONGER SUPPORTED AS A FIX for these two cases, because the measurement above shows a fresher base does not clear them. The stale-base race diagnosis also survives and is unaffected: neither peer commit is an ancestor of the lane base, so these lanes genuinely raced across runs. WHAT THIS MEANS FOR THE FIX, and it is a weaker and more honest claim: there is NO unattended mechanism that would have landed these two lanes, because a same-location double insertion REQUIRES a judgement about ordering that only a human or an agent reading both sides can make. The defensible remaining scope is therefore (a) STOP CALLING IT A FAILURE OF THE WORK: the verdict text asserts the refusal 'asserts a real failure of the work', which is false for a cross-run adjacency conflict, and that wording is what made me and would make any operator misread a resolvable conflict as broken code; (b) ROUTE IT TO A HUMAN OR AN AGENT TURN rather than stranding it, i.e. make this class reach merge-needs-human with a message naming the peer commit, the peer run, and the fact that both sides only add, so the resolver knows it is a keep-both in minutes; and (c) NAME THE CAUSE, since the gate collapses four distinct outcomes onto one kind and an operator cannot tell a cross-run adjacency conflict from a scope violation. Do NOT build automatic base refreshing on the strength of this item; it is measurably insufficient for the only two cases this item documents. Priority kept high because two verified lanes were stranded and 3 more cascaded to dependency-blocked, but the deliverable is now honest reporting and routing rather than automatic resolution.
- 2026-09-22 created (aw backlog): MEASURED across two runs on 2026-09-22. TWO COUPLED DEFECTS, both verified in source and in recorded run state. DEFECT 1, THE REFUSAL IS TERMINAL: classify_integration_refusal('merge-refused') returns False (driven directly), so decide_integration_deferral takes its first branch and records deferrable=false with the verdict 'terminal on its first attempt ... repetition alone cannot clear it'. That verdict's reasoning is sound for a SEMANTIC conflict and FALSE for a STALE-BASE one: repetition alone indeed cannot clear it, but re-merging against a DIFFERENT (fresher) base can, and the code conflates the two. DEFECT 2, NOTHING REFRESHES A LANE'S BASE: grep for rebase across agent_workflows/ finds only engine.py's 'git pull --rebase' operator flow, lifecycle_style/taxonomy label strings, and comments; there is NO machinery anywhere that re-merges or rebases a lane onto current main. reattempt_deferred_integrations re-runs integrate (hence the gate and revalidation) but the lane branch and its base are untouched, so even a permitted re-attempt would re-compute the SAME conflict from the SAME two commits. So the two defects compose: the one class of refusal a fresher base would fix is exactly the class forbidden from retrying, and the retry that is permitted would not refresh the base anyway. MEASURED HARM, two verified lanes permanently stranded, both conflicts PURELY ADDITIVE and resolved by hand in minutes with the suite green afterwards. (a) 92u0v9 (compinert) conflicted in agent_workflows/completion.py with 4y95tp, which belongs to a DIFFERENT run (run-20260922T023434Z-2057475). Proof it is a stale-base race and not a real disagreement: 4y95tp's commit f9a37808 is NOT an ancestor of 92u0v9's lane base f763be8c (git merge-base --is-ancestor exits nonzero). Both sides merely ADD a new top-level function at the same insertion point (main's installed_completion_state, the lane's completion_framework_status block); resolution was to keep both verbatim. (b) xipfy1 (retrywire) conflicted in agent_workflows/runner_shared.py with 8b9ufm, from a THIRD run (run-20260922T023526Z-2065001); commit 908db905 is likewise not an ancestor of base d1d6b6eb. Both sides declare a DIFFERENT local at the same point in the execute-prompt builder (role_block vs correction_notice) and the f-string below already merged cleanly consuming BOTH, so dropping either side would have left an undefined name. Neither conflict was a disagreement about intent; both were adjacency against a base nobody refreshed. WHY A MERGE LOCK DOES NOT FIX THIS, recorded because it was my own first recommendation and it was wrong: the peer commits landed 4.5 and 5 hours BEFORE the merge was attempted (4y95tp 23:09Z vs merge 03:37Z; 8b9ufm 04:05Z vs merge 09:07Z). A publish-serializing lock is held for milliseconds and would have been uncontended and granted instantly in both cases, producing the identical conflict. The contention is STALENESS measured in hours, not simultaneity. A FIX MUST decide, and the two halves can ship independently: (i) SPLIT THE KIND so a stale-base or cleanly-remergeable conflict is deferrable while a genuine semantic conflict stays terminal, rather than collapsing all four gate outcomes (integration_failed_stale_base, _conflict, _scope_violation, _combined_red) onto one terminal kind; and (ii) ON RE-ATTEMPT, RE-MERGE THE LANE ONTO CURRENT MAIN before re-running the gate, so a retry tests a genuinely different question from the one that failed. Do NOT auto-resolve conflict content: (i)+(ii) would have integrated both lanes WITHOUT any content resolution, because against the fresher base git merges both additive changes cleanly, which is the measured evidence that refreshing the base is sufficient here. Note also that a merge-refused item CASCADES: 3 further items reached dependency-blocked behind these refusals in one run.
