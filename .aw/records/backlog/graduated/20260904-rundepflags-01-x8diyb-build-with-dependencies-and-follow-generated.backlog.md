- Id: x8diyb
- Status: graduated
- Blocks-Release: next
- Set: rundepflags
- Priority: high
- Work-Kind: feature
- Summary: Build --with-dependencies closure expansion and --follow-generated graph joining (runflags E-05 registers them as refusals only)

## Workflow history
- 2026-09-08 graduated (aw set): PARTIALLY graduated to plan dhycim (Set depclosure, .aw/records/plans/pending/20260908-depclosure-01-dhycim-...ipd.md), which carries From-Backlog: x8diyb and inherits this item's Blocks-Release: next. Status graduated (design handed off), NOT done. THE SPLIT IS DELIBERATE AND IS THE MAIN FINDING: only --with-dependencies was graduated. --follow-generated WAS NOT, because the mechanism it needs DOES NOT EXIST. Searched for it: recommended_next_action and files_changed appear ONLY inside the prompt text the runner SENDS the agent (oc_runipd.py:4898, :4904; agy_runipd.py:2395, :2401) and are never parsed back for a new-IPD claim; generated_manifest_paths (runner_shared.py:309) is about INDEX.json/INDEX.md merge conflicts and is unrelated; and the queue is appended to at exactly ONE place, build time (oc_runipd.py:2954, agy_runipd.py:1944), with nothing re-discovering plans mid-run. So --follow-generated is not 'wire a flag' but 'invent detection, then mutate a deliberately frozen queue, then make the generated IPD resolve its own Item-Dependencies before review-readiness' (spec 25kzda:945). Graduating it here would have produced a plan whose first E-item is 'invent the missing half', and THE ONE PREVIOUS ATTEMPT TO CARRY BOTH FLAGS TOGETHER WAS REJECTED: superseded kaygwo E-07 (:136) promised both and drew NEEDS REPLAN. The three questions a future --follow-generated plan must answer first are recorded as dhycim OQ-01: how a generated IPD is DETECTED (the outcome file is agent-authored, so an unverified claim would let a turn inject work into its own run), what adding to a FROZEN queue means for reproducibility given that --resume refuses several flags precisely to preserve it, and how the generated IPD resolves its own Item-Dependencies before review-readiness. A maintainer should decide whether the behavior is wanted at all before anyone plans it. THE --with-dependencies HALF IS FULLY ALIVE AND UNOWNED: searched all five plan dispositions for with-dependencies, follow-generated, rundepflags, x8diyb, 'dependency closure', RUN-MIXED-TYPES; hits are uyeko5 (executed, registered the refusals), 8guhs0/ovbnyq/r7xku3 (executed, explicitly DEFERRED the closure), 6lu3rq (executed, built the mixed-type gate) and superseded kaygwo. VERIFIED AT HEAD fac69fbd: the flag is a shared-table row with implemented=False and owner='backlog x8diyb (rundepflags-01)' (runner_shared.py:1877-1888), registered by register_run_policy_flags (:1927) on both hosts and refused by refuse_unimplemented_run_flags (:2011, message :2027-2031) before the run directory exists; it has ZERO behavioral consumption sites, the only other references being two comments recording the absence (oc_runipd.py:3385, :3870). A STRONGER JUSTIFICATION THAN THIS ITEM STATES was found: THREE error catalogue rows already tell operators to run the flag as their remedy (IPD-DEP-SATISFIED spec :667, IPD-DEP-CASCADE :668, IPD-EXEC-READY :708, each ending 'then run: aw <host> run <id6> --with-dependencies'), so the tool currently recommends a flag that refuses. TWO OF THIS ITEM'S CLAIMS ARE NOW STALE: 'the flags do not exist until uyeko5 E-05 lands' is obsolete (uyeko5 is executed, they exist and refuse), and 'doing it first would mean two sessions in the same runner code' no longer holds because the flag surface moved to runner_shared.py rather than the runners. wenmg4 (specfresh-01) DOES NOT WEAKEN THE REQUIREMENT: its Scope-Paths is the spec file alone, its scope is 25kzda's INFRASTRUCTURE PARAGRAPH (:21-33) only, and it explicitly excludes 'CHANGING ANY DESIGN TEXT IN 25kzda. Factual-status only' (:116) and building the five enumerated items (:115); neither flag is among the five, so 2.1's declarations stand. ONE CONSTRAINT THIS ITEM DID NOT KNOW, and it is the real hidden cost: discover_plans (runner_shared.py:1147) walks only the two plans trees, so a spec or backlog dependency target has NO manifest entry at all, which is both why the mixed-type re-trigger is currently untestable on a live invocation (recorded at runner_shared.py:2134-2141) and why the plan has to DECIDE manifest admission per target type rather than assume it. That may be a spec-versus-implementation gap, since spec :160 presupposes a non-plan target can join the queue; dhycim OQ-03 owns it and forbids a silent plans-only narrowing. ALSO: dependency_depth (oc_runipd.py:3803) cannot be reused for expansion because :3820 skips targets not already in the queue, so the closure is new code; and two shipped contract tests pin the present state and must change with the flip (tests/test_run_flag_surface.py:680-684 asserts EXACTLY TWO unimplemented flags and names them; :280-286 requires NOT YET IMPLEMENTED and x8diyb in an unimplemented row's help).
- 2026-09-08 open (aw set): status set to open
- 2026-09-04 blocked (aw set): status set to blocked
- 2026-09-04 created (aw backlog): Build --with-dependencies closure expansion and --follow-generated graph joining (runflags E-05 registers them as refusals only)

Spec `25kzda` 2.1 declares both flags; `runflags-01` (`uyeko5`) E-05 REGISTERS them but deliberately makes each REFUSE with 'not yet implemented', because the behavior is real graph work rather than a parser edit. Nothing owns that behavior, and `uyeko5`'s Deferred section promises a follow-up that did not exist until this item.

WHAT IS ACTUALLY MISSING:

- `--with-dependencies`: expand the selection to the TRANSITIVE declared dependency closure BEFORE the queue is frozen, and subject any newly introduced TYPE to the same mixed-type gate (so pulling in a spec alongside plans must re-trigger the `RUN-MIXED-TYPES` confirmation). Without the flag, dependencies outside the selection are checked against repository state but never silently enqueued.
- `--follow-generated`: a run that GENERATES a new IPD mid-flight joins it to the active graph. Without the flag it is reported as a next action only. Spec 2.1 also requires a generated IPD to resolve its own `Item-Dependencies` before review-readiness.

WHY IT IS GATED RATHER THAN OPEN: the flags do not exist until `uyeko5` E-05 lands, and the closure expansion has to run inside the queue-build path `uyeko5` is already editing. Doing it first would mean two sessions in the same runner code.

WHY IT IS NOT MERELY A UX GAP: a silent no-op here is a CORRECTNESS failure - an operator who passes `--with-dependencies` and gets no expansion believes prerequisites were queued when they were not. That is why E-05 refuses instead of accepting, and why this item exists rather than leaving the flags half-built.
