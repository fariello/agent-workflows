# Review: extract the integration refusal and dirty-overlap check into one shared module, child 6sb3yu (Set integpath)

- Subject-Id: 6sb3yu
- Subject-Type: ipd
- Reviewed-At: 2026-09-07
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `86581595`. `aw ipd lint --phase author` conforming BEFORE semantic review and
`--phase review-finalize` conforming after every revision, so nothing here is structural.

DISCLOSURE: same agent/model authored this plan, so this is a SELF-REVIEW. Its value rests on
re-deriving every load-bearing measurement mechanically rather than reading the plan's account of it.
That re-derivation both STRENGTHENED the plan's central claim (a statement-level AST comparison proves
"exactly one behavioral difference" more tightly than the line diff the plan cited) and falsified two
of its premises (PR-201, PR-203), which no amount of re-reading would have surfaced.

SCOPE: only this child was a candidate. `cczotj`, `29wvmj`, `51vw4y`, `rl67b0` were read as evidence.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-201 | HIGH | IN-SCOPE | A. Correctness; C. Architecture | `runner_shared.py:159`; `oc_runipd.py:106-110`, `:169`; `agy_runipd.py:174`, `:278`; runtime `is` probe | **E-03 AND OQ-02 REST ON A PROBLEM ALREADY FIXED.** Both assert "the two `DriverError` classes are distinct" and build a translation-layer requirement on it. MEASURED FALSE: exactly ONE `DriverError` exists (`runner_shared.py:159`), both drivers re-export it, and `oc.DriverError is agy.DriverError is runner_shared.DriverError` is True. Neither moved body raises or catches it at all. The plan was reading a STALE comment (`agy_runipd.py:278`) that still describes the pre-fix world, while `oc_runipd.py:106-110` records the fix ("There is now ONE class"). An executor following E-03 would have added an unnecessary translation layer, i.e. new unjustified code inside a plan whose entire claim is that it changes nothing. | C:Low; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | E-03 rewritten with the measured collaborator analysis and an explicit "do not invent the `DriverError` problem, it is already fixed" instruction naming the stale comment. OQ-02 rewritten to record the question as MOOT with the runtime proof. V-03 now forbids pasting a `DriverError` translation demonstration as evidence. |
| PR-202 | HIGH | UNDER-SCOPE | A. Correctness; G. Plan executability | AST external-name extraction 2026-09-07; `grep -c "^def build_lane_outcome"` | **A FOURTH DUPLICATED SYMBOL THE PLAN NEVER NAMED, which the moved code calls.** The two bodies' complete external-name set is `_run_git`, `build_lane_outcome`, `conflicted_paths`, `format_merge_conflict_reason`. `build_lane_outcome` is defined in BOTH drivers, ABSENT from `runner_shared.py`, and the shared `integrate_lane_branch` calls it. Unnamed, the executor would hit it mid-move and either invent an injection parameter to route around a duplicate or silently extract a third symbol the scope fence forbade. | C:Low; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | Added F-10 with the measurement (0.887 similarity, EXECUTABLE AST IDENTICAL, docstring-only diff). E-03 now names it as the only real collaborator question with two options and a recommendation (move it, same evidence as `dirty_tree_overlap`). The scope fence carries a NARROW explicit exception authorizing that third symbol so the executor need not guess, and E-04/V-04 extend to cover it. |
| PR-203 | MEDIUM | IN-SCOPE | A. Correctness (false premise) | fresh `git init` has no non-sample hooks; `--no-ff` merge succeeded with no bypass; child 01's F-14 | **THE DEPENDENCY RATIONALE IS FALSE.** The gate said E-05's integrations "today trip the executed-transition gate and would need `--no-verify`". They do not: E-05 works in a THROWAWAY repository, which has no `pre-commit` hook installed at all. Compounding it, git runs `pre-merge-commit` (not `pre-commit`) for an automated merge and this repo installs only `pre-commit`. A false dependency rationale invites an executor to either wait unnecessarily or discard a correct edge for the wrong reason. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Rationale rewritten: the EDGE IS KEPT on honest grounds (both children edit code this Set builds on; serializing protects the two highest-contention driver files; child 01 is the Set's declared first slice), with the false technical claim retracted and an explicit instruction not to reorder the Set without the maintainer. |
| PR-204 | MEDIUM | IN-SCOPE | E. Testing; honesty | measured `1 failed, 5613 passed` at HEAD `86581595` | Stated baseline (`5536 passed, 3 skipped, 2 xfailed` at HEAD `3d239cfa`) is stale by ~77 tests and asserts a GREEN suite when HEAD carries a pre-existing failure (`test_orchestrator_retirement::RealRepositorySets`). An executor comparing against it would report a false regression or be tempted to claim green. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Replaced with the review-measured baseline, judge-on-DELTA instruction, and the named pre-existing failure confirmed present with unrelated changes stashed. |
| PR-205 | MEDIUM | IN-SCOPE | A. Correctness (stale anchors) | `oc_runipd.py:1944`, `:1976`; `agy_runipd.py:1232`, `:1259`; `runner_shared.py:312`, `:327`, `:331` | Four line citations are stale: the agy anchors in the Concern and F-1 (`:1109`/`:1156`, actually `:1232`/`:1259`), the oc `integrate_lane_branch` "`:2006`-region" (actually `:1976`), and all three F-7 injection-precedent lines (`:224/:239/:243`, actually `:312/:327/:331`, which pointed at unrelated code). In a plan about two of the repo's highest-churn files, stale anchors send the executor to the wrong place. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All corrected with a re-locate-by-symbol instruction added to the Concern and F-1. |
| PR-206 | LOW | IN-SCOPE | C. Architecture (misapplied precedent) | `runner_shared.py:55`, `:66-68`, `:312-331`; runtime `is` probes | E-03 told the executor to expect `run_checked` to need injection and to follow the `git_head(..., run_checked=)` precedent. Neither moved body references `run_checked`, and THREE of the four real collaborators are already shared SINGLETONS (`_run_git`, `conflicted_paths`, `format_merge_conflict_reason` are all `is`-identical across both drivers). The precedent exists because `run_checked` is host-specific (opencode-only `env_builder`), so applying it here would add parameters for no benefit. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-9 with the runtime probes; E-03 and the conventions section now state the precedent's actual reason and that no injection is warranted for an already-shared collaborator; V-03 requires the `is` probe and an explicit statement that no injection parameter was added. |
| PR-207 | LOW | IN-SCOPE | A. Correctness (stale figures) | research `tvnq50` (rununify E-01, HEAD `769989ce`) | The out-of-scope bullet cites "67 symbols shared and 36 diverge below 0.80". `rununify`'s own executed inventory measured 88 shared and 52 diverged. Quoting superseded numbers from a sibling Set weakens the boundary argument. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Corrected to 88/52 with the artifact and HEAD cited. |

CONFIRMED SOUND, and this is the important half. The plan's central safety claim is TRUE and now proven
more strongly than it claimed: `dirty_tree_overlap`'s EXECUTABLE AST is byte-identical across hosts,
and `integrate_lane_branch` has 13 top-level statements on each side with IDENTICAL signatures
(`repo, handle, id6, validation_runner`) of which EXACTLY ONE differs, the `--no-ff` merge subject's
`aw oc run` versus `aw agy run` label (added as F-11). Similarities re-measured at 0.717 and 0.651,
matching the plan's 0.72/0.65. `runner_shared.py` imports neither driver, so the cycle risk E-03 guards
is real and currently absent. F-8's `render_stream` precedent is corroborated in `rununify`'s review
history. The plan's decision to split the move from the behavior change, so the identity assertion is
falsifiable, is the right shape and the reason this child is cheap to verify.

No finding was DEFERRED, left OPEN, or marked REPLAN, so no `- Blocking: yes` escalation was required.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | `build_lane_outcome` must be resolved, but moving it extracts a THIRD symbol the scope fence forbids. Widen the fence, or require injection? | Authorized a NARROW named exception in the fence permitting that one symbol to move, recommended option (i) (move), and required the executor to record which option it took. | (a) Require injection to keep the fence at two symbols, rejected because it adds a parameter whose only purpose is to route around a duplicate, leaving the duplicate in place, which is contrary to the child's own goal. (b) Leave it unmentioned, rejected because that is how the defect arose. (c) Escalate to the maintainer, rejected as disproportionate: the symbol's executable AST is IDENTICAL across hosts, so it is the same pure-move class the plan already justifies, and the exception is auditable. | measured identical executable AST + docstring-only diff; the plan's own pure-move criterion for `dirty_tree_overlap` | yes |
| D-2 | PR-203 shows the `Item-Dependencies` edge rests on a false claim. Drop the edge or keep it? | KEPT the edge, replaced the rationale. | Dropping it, rejected because the Set's ordering is the maintainer's explicit ruling and there are real reasons to serialize (both children edit code the Set builds on; the two driver files are the repo's highest-contention). Removing a correct edge because its stated reason was wrong would be the wrong correction. | AGENTS.md on the runner owning ordering; the maintainer's one-Set-with-hook-first ruling recorded in the orchestrator | yes |
| D-3 | PR-201 falsifies an OQ the plan marked `resolved`. Re-open it as a question, or record it as moot? | Recorded it as MOOT with the runtime proof, keeping `Status: resolved`. | Re-opening as `Status: open`, rejected because there is no decision left to make: the premise is gone, so an open question would imply the executor must choose something. Marking it moot with evidence tells the executor to do nothing, which is the actual correct action. | `oc.DriverError is agy.DriverError` True at runtime; neither body raises or catches it | yes |
