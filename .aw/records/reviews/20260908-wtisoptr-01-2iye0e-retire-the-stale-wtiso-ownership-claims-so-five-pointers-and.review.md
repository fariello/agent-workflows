# Review: retire the stale wtiso ownership claims so five pointers and one spec OQ stop naming a plan that will never run, child 2iye0e (Set wtisoptr)

- Subject-Id: 2iye0e
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `4c063c8a`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value therefore
rests on EXECUTING the plan's claims rather than re-reading them. Five things were run rather than
inspected: every one of the six target sites was read and compared against its quoted text; the AST
fingerprint harness was driven in-process to establish which comparison set `describe_lane` falls in
and what each candidate remedy does to the drift guard; the `y6mfgo` supersession sentences were
extracted verbatim; the successor backlog item was located and read; and both suite baselines were
re-measured. The second produced the BLOCKER, which reverses this plan's own prescribed remedy for its
own best finding.

SCOPE: only this child was a candidate; the Set has exactly one member. Read as evidence:
`runner_stop.py`, `runner_shutdown.py`, `runner_shared.py`, `wtiso_gate.py`, `cli.py`,
`ipd_lifecycle.py`, `tests/test_runner_shared.py`, `tests/fixtures/runner_shared_premove_fingerprints.json`,
`tests/test_lane_allocation_idempotent.py`, `tests/test_wtiso_taxonomy_freeze.py`,
`docs/wtiso-state-taxonomy.md`, spec `c4gd2h`, backlog `ol8iyx` and `e820ka`, executed plans `y6mfgo`
and `604wra`, and the retirement commit `70b5338a`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-A01 | BLOCKER | IN-SCOPE | D. Anti-regression; E. Testing (the fix defeats the guard it trips) | drove the harness in-process; `tests/test_runner_shared.py:150`, `:396`; `_strip_docstring` docstring | **THE PLAN'S BEST FINDING CARRIES THE WRONG REMEDY, AND ACTING ON IT WOULD DESTROY THE GUARD IT CORRECTLY IDENTIFIED.** F-3 is real and reproduced: `describe_lane` sits in the STRICT comparison set (present in `symbols`, absent from `UNMOVABLE`, `INJECTED`, `HOST_NAMING_ONLY` and `SUPERSEDED_SINCE_MOVE`), it matches the pre-move capture exactly today, and re-dumping it with an altered docstring DIFFERS. But E-04 said to REGENERATE the fingerprint entry, which re-baselines recorded pre-move truth to a post-move value in the one file whose entire value is that a change to it is suspicious. The harness ALREADY ships the correct route: `DOCUMENTED_SINCE_MOVE = ("plan_bucket",)` compares a named symbol with `drop_docstring=True`, and `_strip_docstring`'s own docstring argues precisely this plan's case ("a docstring is a string constant that no caller can observe through behavior, so adding one cannot change what the function does") and records that the strict-only alternative "was measured and rejected: a moved symbol could then never be DOCUMENTED, which penalizes exactly the improvement this repository wants". MEASURED, and it decides the shape of the fix: adding a name to that tuple does NOT remove it from `clean`, so the `assertEqual(len(clean), 24, "the clean-move count must not drift silently")` guard at `:396` still holds; had the remedy instead EXCLUDED the symbol, that assertion would have failed and invited a second re-baseline. | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added F-11. E-04 inverted: add `describe_lane` to `DOCUMENTED_SINCE_MOVE` with a recorded reason, leave the fixture BYTE-UNCHANGED, and prove no executable change via the docstring-subtracted fingerprint. `tests/test_runner_shared.py` added to `Scope-Paths`; the fixture stays declared but is `--scope-ack`ed as deliberately unmodified. V-04 rewritten to require an EMPTY fixture diff and to fail a pasted regeneration explicitly. Concern, Step 0, Scope check and the gate all carry the reversal so it is not re-attempted. |
| PR-A02 | HIGH | UNDER-SCOPE | G. Plan executability (an unciteable citation) | `grep -c e820ka` on the plan returned 0; the item read at `.aw/records/backlog/open/...e820ka...` | **THREE E-ITEMS REQUIRE CITING A SUCCESSOR ITEM THE PLAN NEVER NAMES.** E-03 says to "NAME THE SUCCESSOR TRACKER", E-05 says the relocation "is tracked by the successor backlog item", and the deferred section says it "survives as its own backlog item, filed with this plan", yet no id6 appears anywhere in the plan. So an executor asked to replace a precise-but-stale pointer (`2c122z`) would have to either hunt for the target or write something vaguer than what it replaces, which is the exact defect this plan exists to remove. The item DOES exist and was verified: `e820ka`, `- Status: open`, `- Set: wtisoreloc`, `- Priority: low`, `- Work-Kind: followup`, no release gate, and its own history names `2iye0e` as the sibling that took the pointer half. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-13. `e820ka` recorded with its full path in the Scope block, and E-03, E-05, V-03 and V-05 now require it BY ID6, with V-03 failing a pointer that omits it. The post-gate paragraph also names it and notes `ol8iyx` already reads `graduated`, so no transition may be owed. |
| PR-A03 | MEDIUM | IN-SCOPE | B. Security/safety of a gate; A. Correctness | spec `c4gd2h:160` | **OQ-03 CARRIES `- Blocking: yes` AND THE PLAN NEVER SAYS SO, which understates exactly the risk E-05 is built to avoid.** Its full line is `- Blocking: yes (it interacts directly with `wtiso` Phase 4, which relocates machine state out of the repo)`. A `Blocking: yes` question left `open` is refused by `aw ipd lint` at EVERY checkpoint, and this spec carries `- Blocks-Release: next`, so wording that reads as re-opening the question would gate every plan the spec governs. The plan correctly says ADD-do-not-revise but grounds it only in the maintainer's authorship, missing the mechanical consequence that makes it urgent. Verified the other three OQs (OQ-01 resolved/blocking-yes, OQ-02 open/blocking-no, OQ-04 open/blocking-no) sit outside this plan's reach. | C:Low; U:Low; S:Medium; F:Medium; Overall:Low | FIXED | Added F-12. Concern gained a paragraph with the mechanism and the full line quoted. E-05 now requires the note be purely DECLARATIVE, containing no question, no "needs revisiting", and no new question heading. V-05 requires confirming that and pins `- Blocking: yes` among the unchanged lines. Gate forbids changing it. |
| PR-A04 | MEDIUM | IN-SCOPE | G. Plan executability (a question the repository answers) | `tests/test_lane_allocation_idempotent.py:339`, `:656` read in full | OQ-02 was left `open` with `- Owner: the maintainer for the scope call`, but reading the two sites answers it without troubling the maintainer, and they are DIFFERENT IN KIND from what this plan fixes. `:339` ("Plan `2c122z` E-06 DEPENDS on this") is a historically true statement that also explains WHY `worktree_lease` must stay stdlib-only, so rewriting it would replace a true statement with a vaguer one; it is the same class as the `docs/` table OQ-01 already resolved to leave. `:656` is the rationale comment above `test_no_new_cli_verb_or_flag_was_added`, whose `assertNotIn("--lanes", cli)` / `assertNotIn('"recover"', cli)` assertions remain valid regardless of who owned the verbs. Neither asserts that unbuilt work has a LIVE owner, which is the specific falsehood E-01 through E-03 remove. Leaving it open would have asked the maintainer to adjudicate a scope call the code settles. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | OQ-02 rewritten `resolved` (Owner `none`) with both sites characterized and the original deferral reasoning retained. The deferred section carries the same reasoning so the judgement is visible where a reader looks for exclusions. |
| PR-A05 | LOW | IN-SCOPE | A. Correctness (a mis-described artifact) | read the fixture and its three integrity tests | The plan describes the fixture as holding "`ast.dump(...)` for 34 symbols", implying a flat map, and E-04's original "regenerate only the one symbol's entry" rested on that shape. It is actually a four-key dict (`captured_at_head`, `symbols`, `symbol_count`, `fingerprints`) whose `fingerprints` is keyed by RUNNER (`oc_runipd`, `agy_runipd`), each carrying all 34 symbols, with separate tests pinning `symbol_count == 34`, symbol/fingerprint key agreement, and a 40-hex head. So a hand-regeneration would have had to touch two runner blocks and would trip more guards than described. Immaterial now that E-04 does not edit it, but it shows the abandoned route was less contained than the plan believed. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-14 and a Step 0 convention recording the real shape and the three guards. |
| PR-A06 | LOW | IN-SCOPE | E. Testing (stale baselines, one used as a criterion) | measured at HEAD `4c063c8a` | Both baselines are stale and one is a validation criterion. The targeted suite is `70 passed`, not the `58 passed` asserted in F-3, E-04 and V-04, so an executor measuring 70 could read its own correct number as a regression. The bare run is `1 failed, 5919 passed, 3 skipped, 2 xfailed` with the failure being `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` (ENVIRONMENTAL, a gitignored local `opencode-recovery/` dump), not the `tests/test_orchestrator_retirement.py` case named, which now gives `112 passed`. The plan's own instruction to compare NODE IDS is what keeps this from being harmful. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added a Step 0 convention with both measured figures. Required-tests and V-04 now state the measured `70 passed` and instruct the executor to measure its own and not treat 70 as a regression. |
| PR-A07 | LOW | IN-SCOPE | E. Testing (a verification step that over-collects) | ran the phrase grep the plan mandates | V-03 mandates a phrase grep for "Phase 4", "Phase 5" and "wtiso" over `agent_workflows/`, which is correct and necessary (F-4), but the plan does not say what the extra hits WILL be, so an executor faces an unclassified pile. Measured: the run surfaces a large body of `runstop` Phase 5 (`71vjbn`) prose in `oc_runipd.py`, `agy_runipd.py` and `runner_stop.py` about a DIFFERENT, LIVE Set whose references are correct and must not be touched, plus four `__pycache__` binary matches that are build artifacts. Without that expectation stated, a diligent executor could "fix" correct prose about a live plan. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | V-03 now requires excluding `__pycache__` and adds `runstop`-Phase-5 prose about `71vjbn` as an explicit classification bucket. The gate forbids touching it, naming it as a different live Set. |

CONFIRMED SOUND, AND THE DIAGNOSTIC WORK HERE IS THE STRONGEST PART OF THE PLAN. Every site claim was
verified against the live text rather than trusted, and all six match as quoted: `runner_stop.py`'s
module-docstring Windows-kill note, its level-5 restatement, its Phase 4 relocation paragraph, and
`resolve_stop_request_path`'s docstring; `runner_shutdown.py`'s `RunLockHandle` `platform_lock` note
and its `observe_tree` never-auto-stash note; and `describe_lane`'s owner clause in `runner_shared.py`.
F-2 is exactly right and is the plan's best independent catch: `agent_workflows/platform_lock.py`
exists, and `y6mfgo`'s record contains both the sentence "SUPERSEDES the `platform_lock` portion of
approved plan `2c122z`, which planned to hand-roll the same thing (18 references there)" and the
deliberate-naming sentence "to match what `2c122z` already refers to (18 references), so that plan's
prose still resolves", so the generic no-successor note really would have written a false claim. F-4 is
right: neither `runner_stop.py` Phase 4 site contains any id6, so the item's own VERIFY-WITH grep
reports success while both survive. F-5 holds: no `platform_state.py`, and the ONLY occurrence of
either accessor anywhere in `agent_workflows/` is the stale prose E-03 rewrites. F-6 holds
(`checkout_control_root` exists at `ipd_lifecycle.py:190`). F-8's distinction is correct and worth the
separate treatment: `observe_tree`'s paragraph independently grounds the never-auto-stash rule in house
policy and GUIDING_PRINCIPLES 10, so re-attribution rather than a no-owner note is right. F-1, F-9 and
F-10 all verified, including that `WTISO_CHILDREN` deliberately enumerates the retired ids and that the
docs table names them 16 times. `aw doctor --lanes` and `aw recover` genuinely do not exist. All three
retired plans resolve under `superseded/`, `604wra` under `executed/`, and `70b5338a` is the retirement
commit it claims to be. `aw specs check` is clean at 29 checked.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Almost nothing about WHAT to fix, and one important
thing about HOW. The plan found the fingerprint trap that its source item missed, which is exactly the
kind of catch review hopes for, and then prescribed the one remedy that would have quietly converted a
falsifiable guard into a decorative one. Inverting E-04 is the whole substance of this round: the
fixture becomes a declared-but-unmodified path, the test file joins the fence, and the harness's own
enumerated exemption does the work it was built for. The second theme is smaller but recurs three
times: this plan is about replacing vague pointers with precise ones, and it three times instructed
the executor to write a pointer it had not itself made precise (the unnamed successor item), plus one
verification step that over-collects. Both are now specific.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | How should the `describe_lane` docstring edit be reconciled with the AST freeze: regenerate the fingerprint (as the plan said), exclude the symbol, or use the harness's `DOCUMENTED_SINCE_MOVE` exemption? | The `DOCUMENTED_SINCE_MOVE` exemption, with the fixture left byte-unchanged. | (a) Regenerate the one entry, the plan's own instruction; rejected because it re-baselines recorded pre-move truth in the one file whose value is that changes to it are suspicious, and the harness explicitly exists to be falsifiable. (b) Add the symbol to `SUPERSEDED_SINCE_MOVE`; rejected because that set asserts a body DELIBERATELY no longer matches and is guarded by `test_a_superseded_symbol_is_accounted_for`, which would then require the symbol to differ strictly, a false claim for a docstring-only change. (c) Exclude it from comparison entirely; rejected because it would drop `len(clean)` below the asserted 24 and invite a second re-baseline. | `tests/test_runner_shared.py:150` and `_strip_docstring`'s docstring ("ONLY the leading string expression is removed, and every remaining token must then match the pre-move capture ... Change one executable line as well and the comparison still fails"); measured in-process that adding a name keeps `len(clean) == 24` and that `describe_lane` is currently a strict match. | yes |
| D-2 | Should `tests/fixtures/runner_shared_premove_fingerprints.json` be dropped from `Scope-Paths` now that it is not edited? | Keep it DECLARED and `--scope-ack` it as deliberately unmodified. | Dropping it; rejected because the plan's central hazard concerns that file and a reader of the fence should see it named, and because `aw ipd finalize` already has a first-class mechanism (`--scope-ack`) for a declared-but-unmodified path, which records the decision rather than hiding it. | `aw ipd finalize`'s scope gate requires a `--scope-ack` per declared-but-unmodified path, so the declaration costs one acknowledgement and buys the fence's completeness; the alternative silently removes the plan's own warning from its scope statement. | yes |
| D-3 | Does OQ-02 (the two test-prose sites) need the maintainer, as the plan asserted? | No: resolve NO from the code. | Leaving it open for the maintainer; rejected because reading both sites answers it, and asking a human to adjudicate what the repository already settles is exactly what the workflow forbids. | `tests/test_lane_allocation_idempotent.py:339` is a historically true statement that also carries the stdlib-only rationale; `:656` is the rationale for `assertNotIn("--lanes")` / `assertNotIn('"recover"')`, assertions still valid after the owner's retirement. Neither claims a live owner for unbuilt work, which is the falsehood this plan removes. Same class as OQ-01, already resolved NO. | yes |
| D-4 | The plan's post-gate step says to set `ol8iyx` to `graduated`; is that transition owed? | Record that it ALREADY reads `graduated` and require verifying before acting. | Leaving the instruction as written; rejected because an executor following it literally would attempt a no-op transition and might, on finding it already graduated, wonder whether a `done` transition was intended instead, which would be wrong while `e820ka` is open. | `.aw/records/backlog/graduated/20260905-wtisodebt-01-ol8iyx-...backlog.md` reads `- Status: graduated`; `e820ka` reads `- Status: open` and carries no release gate, so no gate is inherited and no `done` close is legitimate yet. | yes |
