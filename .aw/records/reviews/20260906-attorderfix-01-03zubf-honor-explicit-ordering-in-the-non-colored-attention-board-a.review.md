# Review: Honor explicit ordering in the non-colored attention board and de-flake the live-repo alias tests

- Subject-Id: 03zubf
- Subject-Type: ipd
- Reviewed-At: 2026-09-06
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED

## Round 1

SELF-REVIEW DISCLOSED: I authored this plan earlier in the same session, so this round was run adversarially by construction, hunting for what the plan gets WRONG rather than confirming it. That posture found three findings, one of them a functional gap that would have left the plan's own stated goal only partly delivered. This is the same failure mode the plan exists to correct in `8ldrlx`, which makes an independent re-review by another agent or model worthwhile before execution even though this round conforms.

Structural preflight `aw ipd lint --phase author` returned `conforming`, exit 0, zero diagnostics, and the `review-finalize` checkpoint returned the same after all revisions. The pre-review snapshot was SKIPPED because the target was already committed and unchanged at `d6caf176`.

EVERY MATERIAL CLAIM WAS RE-VERIFIED FROM SOURCE RATHER THAN TRUSTED. `render_board` does accept `order_by` (`attention.py:1731`) and both `cmd_attention` call sites do pass it (`:2179-2180`, `:2212-2213`), so the plan's "already plumbed" premise holds. The non-colored branch does rebuild `by_class` and ignore it (`:1765-1768`). The multi-key tuple does sit above the class rank in the sort key (`:656-660`), so a globally correct order does reach the renderer. The defect was INDEPENDENTLY REPRODUCED through the real CLI on a purpose-built fixture repo: under `-o priority`, `--format json` returns `bbb222(high), ddd444(high), aaa111(medium), ccc333(low)` while the non-colored board prints `## ready` with `bbb222, aaa111, ccc333` then `## blocked` with `ddd444`, placing a `high` item after two lower-priority ones, matching F-1's evidence string exactly. F-3 verified: `8ldrlx:73` carries the dropped F-2 finding, `:88` defers only the direction-modifier question, `:93` says `Under-scope: none`. F-4 verified: `grep -c "F-2\|render_board\|non-color"` over the `8ldrlx` review returns 0.

THREE FINDINGS, ALL FIXED IN PLACE. PR-001 (HIGH, the substantive one): the plan would NOT have fully delivered its own goal, because a SECOND fragmentation source sits UPSTREAM of the renderer where E-01 cannot reach it. `cmd_attention` extracts `release_blockers` from the item list BEFORE calling `render_board` and appends them as a trailing section (`:2162-2202`), so a `Blocks-Release` item stays outside the ordering even after the renderer emits one flat list; measured on a fixture with a `low`-priority release blocker, it printed LAST, after `## blocked`. Fixed by adding E-06 plus V-06, gated on the same shared predicate. PR-002 (MEDIUM): E-02 as authored MANDATED a `[hidden; use --all]` notice in the flat form, which would have invented a third behavior, because that notice is a property of the SECTIONED form only (`:1782-1785`) while `render_table` filters the same two classes SILENTLY (`:1675-1676`); requiring it would also put a `## `-prefixed line into a form whose defining property is having no section headers, breaking the `^- ` row-grep shape. Fixed by requiring NO notice and recording the asymmetry. PR-003 (LOW): `Under-scope: none` was incomplete given two other test modules call `render_board`; measured that neither passes `order_by`, so both stay on the untouched default path, which makes them a REGRESSION ORACLE rather than a gap. Fixed by recording that and forbidding their edit in the fence.

WHAT I DELIBERATELY DID NOT FLAG. The plan's scope fence uses the DECLARE-then-JUSTIFY form and correctly does NOT tell the executor to stop over an out-of-scope edit, per the 2026-09-01 maintainer ruling; its two stop directives are for genuinely unsafe conditions (an unfalsifiable fixture, a broken regression oracle), which remain correct. `- Readiness:` was correctly ABSENT at authoring time and is written by THIS review, not by the author. Right-sizing was assessed per E-item against conceptual density, not by the passing count lint: each of the six addresses one concern with its own evidence kind, and E-06 was added as a separate item rather than folded into E-01 precisely because it edits a different function.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | HIGH | UNDER-SCOPE | A. Correctness / G. Plan executability | `agent_workflows/attention.py:2162-2202` | **A second fragmentation source sits UPSTREAM of the renderer, so the plan would not have delivered its own goal.** `cmd_attention`'s non-colored branch extracts `release_blockers` BEFORE calling `render_board` and appends them as a trailing `## release-blockers for <version> (<id6>)` section. E-01 changes only the renderer, so a release-blocking item would still print outside the ordering. MEASURED on a fixture repo: a `Blocks-Release: next` item at `low` priority printed LAST under `-o priority`, after `## blocked`, leaving the order split three ways instead of one flat list. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | FIXED 2026-09-06. Added E-06 (stop extracting under an explicit order, using E-01's shared predicate; keep the default section byte-identical) and V-06 (before/after CLI paste plus the no-`-o` default check). E-05(e) now covers the case and requires a `planned` release record in the fixture so `next` resolves. |
| PR-002 | MEDIUM | IN-SCOPE | F. KISS, principles, and UX | `agent_workflows/attention.py:1675-1676` vs `:1782-1785` | **E-02 mandated a notice line that would invent a third behavior.** The `[hidden; use --all]` line is emitted per class group and ONLY when not colored, whereas `render_table` filters `done`/`parked` SILENTLY with no notice. Requiring the notice in the flat form would make it differ from the flat COLORED form it now parallels, and would put a `## `-prefixed line into an output whose defining property is having no section headers, breaking the `^- ` row-grep shape agents rely on. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | FIXED 2026-09-06. E-02 now requires the suppression WITHOUT a notice, records the measured asymmetry, and states that adding one would have to change BOTH flat paths (outside the fence) with a preference for emitting none. Recorded as F-12. |
| PR-003 | LOW | UNDER-SCOPE | D. Anti-regression and domain invariants | `tests/test_attention_compact.py`, `tests/test_attention_priority_blocker.py` | **`Under-scope: none` was incomplete: two other test modules call `render_board` and are not in Scope-Paths.** Verified neither passes `order_by` (`grep -c` returns 0 for both), so both exercise the default sectioned path E-01 must leave byte-identical. That makes them a regression oracle for this change, and the risk is not that they are missing but that an executor might EDIT one to clear a failure that actually signals a broken default view. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | FIXED 2026-09-06. Recorded as F-13, stated in Scope check with the measured greps, added to V-05's evidence bar (`git diff --name-only` must not list them), and forbidden in the scope fence with "report, do not silence". |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Should the flat non-colored form emit a `[hidden; use --all]` notice for suppressed `done`/`parked` items? | NO notice. Match the flat colored form, which filters silently. | (a) Emit it, as the plan originally required (rejected: invents a third behavior, and injects a `## `-prefixed line into a form defined by having no section headers, breaking the `^- ` grep shape). (b) Add it to BOTH flat paths for consistency (rejected here: that changes `render_table`'s shipped output, outside this plan's fence, and belongs to a deliberate separate decision). | `attention.py:1675-1676` (colored filters silently) versus `:1782-1785` (notice is per-group and non-colored only) | yes |
| D-2 | Should release-blocker extraction be fixed in this plan or filed separately? | In this plan, as E-06. | File a follow-up (rejected: the plan's stated goal is that `aw next -o priority \| head` returns the right items, and shipping E-01 alone would leave that goal unmet while appearing complete, which is exactly the `8ldrlx` failure mode this plan exists to correct). | `attention.py:2162-2202`; measured fixture reproduction showing a `low`-priority blocker printing last under `-o priority` | yes |
| D-3 | Are the two out-of-scope `render_board` test callers a coverage gap needing Scope-Paths expansion? | No. They are a regression oracle; leave them unmodified and forbid editing them. | Add them to Scope-Paths (rejected: nothing in them needs to change, and declaring a path invites an edit; `aw ipd finalize` would then also demand a `--scope-ack` for an unmodified declared path). | measured `grep -c order_by` returns 0 in both files, so both stay on the default path | yes |

No `Reversible: no` decision was taken in this round, so no escalation to a `- Blocking: yes` open question was required. All three findings are `FIXED`, so nothing reaches the repository's `HIGH` gate threshold (`review_findings_gate.block_at` absent from `.aw/config/project.json`, so the default `HIGH` applies) and `check.review-finding-unescalated` has nothing to report.
