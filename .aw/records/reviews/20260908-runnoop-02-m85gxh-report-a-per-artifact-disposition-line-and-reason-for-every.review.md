# Review: report a per-artifact disposition line and reason for every artifact the selector matched, child m85gxh (Set runnoop)

- Subject-Id: m85gxh
- Subject-Type: ipd
- Reviewed-At: 2026-09-08
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `08aabdd4`. `aw ipd lint --phase author` conforming BEFORE semantic review and
`--phase review-finalize` conforming after every revision, so nothing here is structural.

DISCLOSURE: same agent/model authored this plan, so this is a SELF-REVIEW. Its value rests on
EXECUTING the claims rather than re-reading them. Four things were run rather than inspected: the
end-of-run summary was RENDERED with a zero-attempt `reviewed` item to see what an operator actually
gets today; the order announcement was rendered with an expanded selector; the re-fork guard's table
was loaded and its rows counted; and the oc-to-agy import edge was AST-counted. The first produced
the BLOCKER (PR-601), which corrects the plan's central premise, and the third produced PR-603, which
invalidates an enforcement claim the plan relies on.

SCOPE: only this child was a candidate. Read as evidence: `run_selection_policy.py`, `render_stream.py`,
`oc_runipd.py`, `agy_runipd.py`, `runner_shared.py`, `tests/test_runner_refork_guard.py`,
`tests/test_run_selection_policy.py`, spec `25kzda` §5.4/§5.6, backlog `em0z50`, and sibling plans
`zz5yxq`, `r2i1b1`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-601 | BLOCKER | IN-SCOPE | A. Correctness (false premise); F. UX; G. Plan executability | measured `render_run_summary_table` and `format_run_order_announcement`; `render_stream.py:2152-2173` | **THE PLAN'S CENTRAL PREMISE IS OVERSTATED, AND THE CORRECTED VERSION CHANGES WHAT THE WORK IS.** The Concern asserts a matched-but-unacted artifact "produces NO output". MEASURED: TWO shipped surfaces already name it. `format_run_order_announcement` prints every matched id6 unconditionally at queue-freeze (`Run order (2 item(s)): 01 abc123  02 def456`), and the END-OF-RUN SUMMARY TABLE renders ONE ROW PER MATCHED ARTIFACT with id6, set, action and disposition INCLUDING for a zero-attempt item (rendered: `01 │ 01 │ abc123 │ wtiso │ execute │ reviewed`, progress `1/1 [██████████] 100% (1 reviewed)`). What is genuinely absent is the REASON: in that same render the word `approval` appears ZERO times and no diagnostic line is emitted at all, because the diagnostics block keys on a five-status allowlist that excludes `reviewed`. So the defect is an UNEXPLAINED DISPOSITION on a surface that already explains five other statuses, not a missing line. This is not a wording quibble: an executor holding the stronger belief would reasonably build a third listing of the queue, satisfy every word of the plan and the letter of V-03, and fix nothing an operator cares about. | C:Low; U:Medium-High; S:Low; F:High; Overall:Medium | FIXED | Added F-8 with both measurements. The Concern now states the corrected premise explicitly and forbids re-asserting "produces NO output". E-03 gained a DO-NOT-BUILD-A-SECOND-SUMMARY-TABLE paragraph naming what already prints, plus the diagnostics shape as precedent and its fence. V-03's load-bearing paste is now that the line CARRIES A REASON, with an explicit statement that a reason-free line proves nothing. A Step 0 convention and a gate paragraph carry the same correction so a reviewer meets it first. |
| PR-602 | HIGH | IN-SCOPE | A. Correctness (stale anchors) | measured at HEAD `08aabdd4` | **EVERY DRIVER ANCHOR IN THE PLAN IS WRONG ONE DAY AFTER AUTHORING, AND TWO ARE WRONG IN A WAY THAT MISDIRECTS THE WORK.** F-1 and E-03 cite `oc_runipd.py:6743-6757` for the per-item finish line; that range is INTEGRATION code (the `integrate_lane_branch` / fail-closed block) and the finish line is at `:6956-6970` (agy `:4019`, whose `:4013` was close). E-02 and `zz5yxq` both cite `oc_runipd.py:3011` for the queue-builder admission tuple; `:3011` is inside the frozen launch-profile block and the tuple is at `:2977-2980` (agy `:2011-2013`, not `:2026`). Also `action_for` is at `runner_shared.py:3074`, not `:2735`; `announce_run_order` is at `:4178`, not `:4122`; `unsatisfied_dependency_reasons` is WRITTEN at `runner_shared.py:3382`, which the plan never names. The plan's own gate warns that these files move, which makes carrying unverified anchors self-contradictory. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | All anchors corrected with the wrong ones called out inline so the executor does not re-follow them. The re-locate-by-symbol paragraph upgraded from "moved roughly 70 and 95 lines" to the measured fact that every anchor was wrong within a day, naming the two that misdirect. The admission-tuple correction is noted as applying to `zz5yxq`'s citation too. |
| PR-603 | HIGH | IN-SCOPE | D. Anti-regression; E. Testing | `tests/test_runner_refork_guard.py:336-344`; measured `REFORK_TABLE` | **E-04's ENFORCEMENT CLAIM IS FALSE, so its guarantee rests on nothing.** It states `test_the_table_covers_both_runners` "already fails a one-sided table" and concludes "a table row naming only `oc_runipd` is a failed E-04". Measured: that test asserts only that the table's AGGREGATE `runners` set equals `BOTH` and that MORE THAN ONE row names agy. It cannot fail a one-sided ROW, and 4 of the 46 shipped rows are legitimately one-sided (`StreamTracker`, `format_tokens`, `format_statusline`, `render_event`) and pass. So an executor could add a one-sided row for this plan's symbol, cite that test as satisfied, and ship exactly the asymmetry the item exists to prevent. Separately measured: `run_selection_policy` owns ZERO rows today, so this will be the first. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-9 with the row count and the four one-sided examples. E-04 rewritten: the false claim is corrected in place, the symmetric row is stated as a REQUIREMENT of this plan rather than something the aggregate test enforces, and the per-row identity assertions (`:270`, `:290`, `:318`) are named as the real mechanism. V-04 now declares the MUTATION CHECK the only real proof and forbids citing the aggregate test as the enforcer. |
| PR-604 | HIGH | UNDER-SCOPE | G. Plan executability (scope fence) | plan `Scope-Paths` versus E-04 | E-04 MANDATES a new `REFORK_TABLE` row, but `tests/test_runner_refork_guard.py` was not declared in `Scope-Paths`, so the plan's own required edit was outside its own fence. `aw ipd finalize` would then refuse until the executor wrote a `--scope-reason` for work the plan commands, which trains exactly the habit the scope gate exists to discourage. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Path added to `Scope-Paths` (now seven). Added F-10. E-04 states the addition and why; the gate's fence sentence updated from "six paths" to seven; the Required tests section now names the file as a declared path that must be run and pasted. |
| PR-605 | MEDIUM | IN-SCOPE | E. Testing (unsatisfiable acceptance criterion) | AST walk of `agy_runipd.py` | E-04's expected outcome requires "the AST-measured oc-to-agy import count unchanged at 47 or lower", but the measured count is **48**. As written the criterion is unsatisfiable without an unrelated reduction, so a conforming executor would either fail its own gate or quietly reinterpret it. (The DIRECTION claim is sound and was re-verified: `oc_runipd` imports zero names from agy.) | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-11. E-04 and V-03 now read "not increased above the measured baseline of 48", with the plan's original 47 called out as wrong so the executor does not treat its own correct measurement as a regression. |
| PR-606 | MEDIUM | IN-SCOPE | E. Testing (stale baseline) | measured bare run and targeted runs | The Step 0 suite baseline is stale in BOTH halves and in its lane figure. It declares `1 failed, 5648 passed` attributed to `tests/test_orchestrator_retirement.py`; measured, the bare run gives `1 failed, 5919 passed, 3 skipped, 2 xfailed` with the failure being `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` (ENVIRONMENTAL, from a gitignored local `opencode-recovery/` dump), while `test_orchestrator_retirement.py` now gives `112 passed`. The claimed "roughly 32 environmental failures in a lane worktree" is also stale: a fresh linked worktree measures `5920 passed, 3 skipped, 2 xfailed`, CLEANER than the primary checkout. The plan's instruction to compare node ids rather than totals is what keeps this from being harmful. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-12. Step 0 baseline replaced with the measured values, the failure labelled environmental and possibly absent elsewhere, the retired failure recorded as now-passing, the lane figure corrected, and the executor told to measure its own and quote none of these. |
| PR-607 | MEDIUM | IN-SCOPE | G. Plan executability; A. Correctness (vocabulary) | spec `25kzda:948-958`, `:672`; measured greps | The spec-sync section leaves the executor to determine mid-implementation whether the spec's `reason_code` vocabulary constrains E-02's names. It does, and review answered it: §5.4 carries an explicit "Stable dependency reason codes" table naming `dependency_not_met`, `dependency_not_met_external`, `needs_human_approval` (precisely this plan's needs-approval case), `host_capability_unavailable`, `dependency_graph_invalid` and `dependency_cycle`. Leaving it open risked a second vocabulary for a fact the approved contract already names. Worth pairing with the honest limit, also measured: `needs_human_approval` greps to ZERO under `agent_workflows/` and `dependency_not_met` exists only as an `AggregatedItem` boolean (`run_evidence.py:1977`), so nothing goes red if the executor diverges. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Spec-sync section rewritten with the reconciliation done and the §5.4 table quoted by name. E-02 now requires the spec's names where one exists, permits minting only for the two cases §5.4 does not cover (already-executed, status-not-runnable), and requires citing §5.4 in the code. V-02 requires showing each spec-named reason uses its spec name and justifying any minted name against a checked row. The §4.2 byte-equality warning is retained and distinguished from §5.4. |
| PR-608 | LOW | IN-SCOPE | A. Correctness (an answerable open question) | `oc_runipd.py:2800-2812`, `:2891`, `:2895` | OQ-02 left a measurement to the executor that review could extend, and the extension has a design consequence the question did not state. Re-verified: the draft gate DOES rebind rather than raise (its own comment says so), AND it runs at `:2800` while the run directory is not created until `:2891`. So a draft-excluded artifact never enters run state at all, which means a per-artifact line for that case cannot be emitted from the queue: it must come from the exclusion site (which `render_drafts_exclusion` already serves) or be excluded from this plan's line with that measurement as the reason. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | OQ-02's rationale extended with the re-verified ordering, the consequence stated, and both resolutions declared defensible. Also flagged that the plan's unverified claim about the mixed-type gate and dependency preflight raising early must be MEASURED rather than trusted, given every other anchor drifted. |
| PR-609 | LOW | IN-SCOPE | G. Plan executability | `zz5yxq` front matter; its E-03 | The dependency on `zz5yxq` is correct but its state and its SUBSTANCE were unstated, leaving a reader unable to judge how far off execution is. Read at review: `zz5yxq` is `- Status: reviewed` / `- Readiness: go-pending-approval`, still in `pending/`, so the edge is live and correctly gating. Its E-03 is what makes the needs-approval fact an explicit durable token, which is the value E-02 reads for the most important of the six reasons, so the edge is substantive rather than administrative. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | The gate's post-lifecycle paragraph now records `zz5yxq`'s current status and location and states why the dependency is substantive. |

CONFIRMED SOUND, and it is most of the plan. The SITING ARGUMENT is the strongest thing here and
verified exactly: `run_selection_policy.py` really does hold `render_action_preview` (`:589`),
`render_counts_inline` (`:634`), `render_refusal` (`:693`), `render_drafts_preview` (`:937`),
`render_drafts_exclusion` (`:966`) and `decide_draft_admission` (`:1004`) at the precise lines cited,
and an independent AST walk re-confirmed its only first-party imports are `selectors` and `status_set`
(`:39-40`). The cycle argument against `render_stream` is real. F-3 verified including its docstring
quote ("the order must be auditable in the log even when nothing was reordered"), and
`announce_run_order` is genuinely SHARED rather than forked (defined once in `oc_runipd`, re-exported
at `agy_runipd.py:377`, called by both). F-4 verified precisely, including that the reasons reach only
the diagnostics block and that the block keys on a five-status allowlist. F-5 verified. The
one-way import direction verified. The overlap fence with `r2i1b1` is correctly drawn and E-05's
decision to read that plan's status AT EXECUTION TIME rather than guess is the right instrument. The
anti-second-renderer discipline is genuinely the module's own recorded convention, not an imposition,
and E-01's insistence on ONE renderer for both the acted-on and skipped forms follows from it
correctly. The targeted suites pass unmodified (`94 passed` for the two declared test modules).

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. The BLOCKER makes the plan's TARGET more precise
rather than smaller: the work is to explain a disposition, not to display one. That distinction was
invisible in the prose and only appeared by rendering the summary an operator actually sees, which is
the same lesson PR-603 teaches one layer down (a guard the plan trusted turned out not to guard the
property it claimed). Both defects share a root cause worth naming: the plan reasoned from what the
code LOOKS like it does, at two places where measurement disagrees.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-601: the premise is overstated, since two surfaces already name every matched artifact. Is the plan still needed, or is this a REPLAN? | KEEP IT AND RE-AIM IT at the REASON. Correct the premise in the Concern, E-03, Step 0, the gate and V-03, and make the reason the load-bearing validation. | (a) REPLAN, rejected: the operator's real problem (an unexplained `reviewed` on eight artifacts) is genuine, unaddressed, and exactly what the backlog item was filed about; the plan's siting, renderer discipline and fences are all sound and would be re-authored unchanged. (b) Narrow to "add `reviewed` to the diagnostics allowlist", rejected: `render_stream.py` and that allowlist belong to `r2i1b1`, and this plan's fence forbids the edit, so it would trade a false premise for a scope violation. (c) Leave the premise and let the executor discover it, rejected as the measured hazard: a third queue listing satisfies every word of the plan and fixes nothing. | measured `render_run_summary_table` with a zero-attempt `reviewed` item (row present, `approval` absent); measured `format_run_order_announcement`; `render_stream.py:2152-2173` | yes |
| D-2 | PR-603: E-04's cited enforcer does not enforce per-row symmetry. Strengthen the test, or strengthen the plan's own proof? | STRENGTHEN THE PLAN'S PROOF. State the symmetric row as this plan's requirement and make V-04's mutation check the evidence. | (a) Change `test_the_table_covers_both_runners` to demand per-row symmetry, rejected on evidence: 4 shipped rows are legitimately one-sided (`StreamTracker` and three `render_stream` formatters genuinely have one caller), so the stricter test would fail on correct code and the fix belongs to whoever owns that guard, not to this plan. (b) Drop the symmetry requirement since nothing enforces it, rejected: both hosts import this renderer, so asymmetry here would be a real re-fork risk rather than a stylistic one. | measured `REFORK_TABLE` (46 rows, 4 single-runner, all passing); `tests/test_runner_refork_guard.py:336-344`, `:270`, `:290`, `:318` | yes |
| D-3 | PR-607: does spec §5.4's reason-code vocabulary bind E-02's names, and should review decide it? | DECIDE IT AT REVIEW: YES for the four reasons §5.4 names, with minting allowed only for the two it does not cover. Record the honest limit that no test enforces it. | (a) Leave it to the executor as authored, rejected: it is a question about an `approved` contract's vocabulary, which an implementer should not be settling mid-task, and getting it wrong mints a second name for a fact the contract already has. (b) Require spec names for all six, rejected as measurably impossible: §5.4 is dependency-scoped and has no row for already-executed or status-not-runnable. (c) Ignore the spec because nothing binds the names in code, rejected: the spec is the contract every other plan is reviewed against, and "no test catches it" is not a reason to diverge from it. | spec `25kzda:948-958` (the table), `:672`; measured zero `needs_human_approval` occurrences under `agent_workflows/`; `run_evidence.py:1977` | yes |
| D-4 | PR-604: E-04 mandates editing an undeclared file. Add the path, or drop the mandate? | ADD `tests/test_runner_refork_guard.py` TO `Scope-Paths`. | (a) Drop the guard row, rejected: the object-identity pin is the plan's defense against the re-fork class this repository has already suffered, and E-04 is the item that supplies it. (b) Leave it undeclared and let the executor justify it with `--scope-reason`, rejected: a plan that commands an out-of-fence edit teaches an executor to treat the fence as advisory, which is the opposite of what the finalize gate is for. | plan `Scope-Paths` as authored versus E-04's mandate; `aw ipd finalize`'s scope reconciliation | yes |
