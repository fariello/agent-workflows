# Review: split reviewed out of the run success bar, child zz5yxq (Set runnoop)

- Subject-Id: zz5yxq
- Subject-Type: ipd
- Reviewed-At: 2026-09-08
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `6cd66049`. Structural preflight `aw ipd lint --phase author` conformed CLEAN with no
advisory, and `--phase review-finalize` conforms clean after the revisions.

DISCLOSURE: authored in the same session as its parent `7ewc74`, reviewed immediately before it, so this
is a self-review. The cost shows in PR-901 and PR-902: the plan asserted an agy call site that does not
exist and then built a siting prohibition on top of that misreading, and both were findable by one
identity check.

THE DIAGNOSIS HOLDS AND SO DOES ITS SHARPEST SELF-IMPOSED CONSTRAINT, both re-executed rather than
re-read. `'reviewed' in SUCCESS_STATES` is True; `action_for('child','reviewed')` is `'execute'`;
`deliberate_stop_exit_code(["reviewed"], success_states=SUCCESS_STATES, stopped=False)` is `0`;
`oc.SUCCESS_STATES is agy.SUCCESS_STATES` is False while `==` is True. F-5 is real and must not be
weakened: the `cascade_dependency_blocked` docstring still records run `run-20260904T042705Z-1025943`
killing Orders 02-05 of an all-review `wslayout` run the instant Order 01 reached `reviewed`, and the
call site carries its own warning not to hardcode the execution bar there. The plan's refusal to take
the tempting one-line fix is the single best judgement in it, and review preserved it verbatim.

WHAT ROUND 1 CHANGED. Nine findings, all FIXED in place; none deferred, none left open, none REPLAN. No
E-item was added or removed. Both open questions were RESOLVED from shipped code rather than handed to
the executor, which is the most useful thing this review did: OQ-01's own instruction was that a grep
"must be run, not guessed", so I ran it and the answer closes the question's escape hatch outright; and
OQ-02's premise (an unreconciled exit table) is obsolete, because the reconciliation already exists in
code and the real gap is elsewhere and larger.

THE FINDING THAT CHANGES THE MOST WORK IS PR-901, and it cascades into PR-902. `agy_runipd` does not
define `edge_satisfied` or `cascade_dependency_blocked`; it RE-EXPORTS oc's objects. So agy has no
dependency call sites at all, the plan's "agy twin at `:2275`" points at a docstring about argv, and
agy's real `SUCCESS_STATES` uses are four rather than five. That in turn inverts E-02's siting rule:
the plan forbade siting anything in `oc_runipd` on layering grounds, but re-export from `oc_runipd` with
an object-identity pin is precisely this family's established, test-enforced convention, with an in-file
comment recording that `ruff` once stripped six of those re-exports and the symmetry test caught it.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-901 | HIGH | IN-SCOPE | Evidence accuracy; C. architecture | `agy_runipd.py:350`, `:368`; `agy.edge_satisfied is oc.edge_satisfied` -> True | **AGY DEFINES NEITHER DEPENDENCY FUNCTION; IT RE-EXPORTS OC'S OBJECTS, so E-01's site inventory is wrong in a way that would send the executor hunting for code that does not exist.** The plan lists an agy `edge_satisfied` twin at `:2275`; that line is a docstring about argv handling. Measured, both functions resolve to the same objects with `__module__` `agent_workflows.oc_runipd`, so question (1) has ONE implementation already shared, agy has ZERO dependency call sites, and agy's real `SUCCESS_STATES` uses are FOUR not five | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-01 rewritten with the corrected inventory (six real oc reads excluding the definition and one in-comment mention; four agy reads), an explicit "agy has no dependency call sites, do not add one" paragraph, and a note that a classification implying a per-host pair for question (1) would be wrong. New F-9. V-01 now states the expected counts and demands the identity check |
| PR-902 | HIGH | IN-SCOPE | C. architecture; G. executability | `agy_runipd.py:295-303`; `tests/test_runner_item_dependencies.py:1633-1664` | **E-02's "SITE IT IN `runner_shared`, NEVER IN EITHER RUNNER" is wrong as an absolute and would fork a family this repository deliberately keeps unforked.** The existing action-aware bar lives in `oc_runipd` and is bound into agy with the `as <same-name>` re-export form, pinned by `assertIs` in `_SHARED_NAMES`; the in-file comment records that `ruff` removed 6 of those re-exports on one commit attempt and the cross-driver symmetry test caught it immediately. So the invariant actually enforced is ONE DEFINITION plus an IDENTITY PIN, not a particular file, and forbidding `oc_runipd` could force the predicate away from the code it must agree with or into a circular-import risk | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-02 rewritten: `runner_shared` PREFERRED and still the default, `oc_runipd` plus a pinned re-export explicitly legitimate if the predicate must sit beside `edge_satisfied`, a second agy copy the only genuine prohibition, and the executor required to state which and why. New F-10. A Step 0 convention records the pattern. The gate's flat ban replaced with the single-definition rule |
| PR-903 | HIGH | IN-SCOPE | A. correctness; G. executability | `run_gates.py:31`, `:157`, `:214`; `run_evidence.py:1839`, `:1966` | **OQ-01 was left open pending a grep that decides it, and the grep's answer is stronger than either branch the question offered.** `needs_input` is not merely available in spec §5.6; it is ALREADY THE SHIPPED TOKEN FOR THIS EXACT MEANING, as `run_gates.GATE_STATUS_NEEDS_INPUT` and `run_evidence.AGGREGATE_NEEDS_INPUT` ("Human input or explicit acknowledgement is required (spec 5.6 exit 3)", item flag glossed "True when a human gate stopped the item"). The question's escape hatch, that a new token is defensible if `needs_input` is load-bearing for a DIFFERENT case, is therefore closed: it is load-bearing for the SAME case | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | OQ-01 flipped to `resolved` with the two shipped constants cited and an escalate-do-not-choose instruction. E-03 rewritten to state the token and to prefer wiring the existing `AggregatedItem.needs_input` carrier over inventing a field, with the honest note that the drivers do not construct it today. F-8 strengthened. V-03 now demands byte-equality with `GATE_STATUS_NEEDS_INPUT`. Spec-sync and the gate updated: no spec edit at all |
| PR-904 | MEDIUM | IN-SCOPE | A. correctness; Evidence accuracy | `run_evidence.py:1847-1853`, `:2173-2175`; zero driver call sites | **OQ-02's stated obstacle does not exist, and the real obstacle is different and larger.** The question feared an "unreconciled conflict" between the spec's exit table and `run_cli.py`'s, and told the executor to prefer 1 for that reason. Measured: `_CLASSIFICATION_EXITS` already transcribes the spec table with `needs_input: 3`, its comment records that `run_cli.py`'s constants were DELIBERATELY kept separate rather than left in conflict, `_CLASSIFICATION_PRIORITY` already makes 3 outrank 1, and `aggregate_run_exit` already raises the candidate from an item flag. The actual gap is that neither driver calls `aggregate_run_exit` at all | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | OQ-02 flipped to `resolved`: emit 1 (which fixes the measured silent 0), record 3 as the spec-and-code-intended value reachable only by wiring the aggregator, and forbid editing `_CLASSIFICATION_EXITS`. New F-11. Spec-sync rewritten. A new under-scope entry names the unwired aggregator so it is not mistaken for an oversight, and the gate forbids the wiring |
| PR-905 | MEDIUM | UNDER-SCOPE | A. correctness; spec obligation | spec `25kzda:484` | The plan argues the defect from first principles and never cites the spec row that makes it a VIOLATION: §3.2 requires a `reviewed` IPD unattended to "Stop `needs_input`. Exact recovery names the human approval command" and forbids "treating model approval as human approval". Counting such an item a success and exiting 0 is the direct opposite. Omitting this understates the warrant and leaves a reviewer of the implementation without the contract to check against | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Spec-sync gains a third obligation stating the violation with the citation; new F-12; V-02 now requires citing that row as the requirement satisfied |
| PR-906 | MEDIUM | IN-SCOPE | Evidence accuracy | `tests/test_runner_shared.py:70-84` | The validation section describes `test_no_call_site_was_rewritten` as counting `save_state` call sites and implies a five-symbol `INJECTED` map; the file's own comment says "THE COUNT IS 8, NOT THE PLAN'S 5" and names the three additions (`git_head`/`git_status`/`git_common_dir`, which call `run_checked` inside the same seam). A plan mis-describing the guard it must respect invites working around it | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Validation section corrected with the count of eight, the three additions and their reason, and the instruction to reflect a moved count rather than work around it |
| PR-907 | MEDIUM | UNDER-SCOPE | E. testing; scope fence | `REFORK_TABLE` contract; `_SHARED_NAMES` | E-04 sends the identity pin to `REFORK_TABLE`, but that table asserts BOTH no top-level definition in the listed runner AND attribute identity, so it cannot describe a symbol DEFINED in `oc_runipd` and re-exported (a row naming `oc_runipd` as a runner forbidden to define it is self-contradictory). Under PR-902's correction the likely home is `_SHARED_NAMES`, which is NOT in `Scope-Paths` | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-04 now names both candidate homes with the contract that decides between them and requires the choice be stated; the validation section explains the same; the gate records that `tests/test_runner_item_dependencies.py` and the two stop-level modules are undeclared on purpose and must be edited with a `--scope-reason` if E-02's siting requires it |
| PR-908 | MEDIUM | IN-SCOPE | Evidence accuracy | measured bare suite; `tests/test_run_viewer.py` -> 69 passed | The baseline is wrong in both halves and its lane figure is stale: the plan says `1 failed, 5648 passed` with the `test_orchestrator_retirement` failure, but the suite reports `2 failed, 5864 passed, 3 skipped, 2 xfailed` and that test PASSES; the "roughly 32 tests fail in a lane worktree" claim is also stale since `test_run_viewer.py` now passes 69/69. An executor told to expect a different failure might try to fix one, and one of the two tempts deleting another agent's untracked files | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Step 0 baseline rewritten with the re-measured numbers, both failures named with mechanisms, the flaky-under-load one characterized, the stale lane figure corrected, and the deletion prohibited. A focused command list added including the stop-level modules this plan's exit-code edit touches |
| PR-909 | LOW | IN-SCOPE | Evidence accuracy; G. executability | every coordinate re-resolved | EVERY oc line number in E-01 had drifted (`:327`->`:336`, `:3373`->`:3377`, `:4219`->`:4275`, `:6743`/`:6746`->`:6943`/`:6946`, `:7296`->`:7496`, `:7377`->`:7577`), as had `runner_shared.py:2727`/`:2735`->`:2969`/`:2977` (the plan's coordinates now land inside `find_unauthored_child_rows`) and the agy exit code. Also the oc-to-agy import count is 43 top-level / 48 with nested, not 47, and the plan used that wrong number as an ARGUMENT for E-02's siting | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All coordinates re-resolved throughout. The import count corrected in both places, with an explicit instruction not to let a wrong number drive the siting. The RE-LOCATE paragraph now names the measured drifts as its justification rather than asserting the rule generically |

No finding was DEFERRED, left OPEN, or marked REPLAN, so no escalation to a `- Blocking: yes` question was
required. The two HIGH findings other than PR-903 are both about the plan describing the codebase
incorrectly rather than about the change being wrong, and both were fixable by measurement.

Both of the plan's open questions are now `resolved` and no new question was raised: every gap review found
was answerable from the repository, and none required a maintainer decision about scope, priority or risk.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-902: E-02 forbids siting the predicate in `oc_runipd`. Enforce that, or allow the re-export pattern? | ALLOW BOTH, with `runner_shared` preferred and a single definition plus an object-identity pin as the actual requirement; the executor states which home and why. | (a) Keep the flat prohibition, rejected because it contradicts the pinned convention for this exact family and could force the predicate away from the `is_exec` selection it must agree with, or into a circular-import risk. (b) Mandate `oc_runipd` plus re-export to match the neighbours, rejected as over-specifying: `runner_shared` genuinely is cleaner when no coupling forces the move, and the identity pin makes either safe. (c) Escalate to the maintainer, rejected because the repository answers it: the convention is documented in-file and enforced by a test. | `agy_runipd.py:295-303` (the `as <same-name>` comment and the `ruff` incident), `:350`, `:368`; `tests/test_runner_item_dependencies.py:1633-1664` | yes |
| D-2 | OQ-01: does the disposition take `needs_input` or a new token? The plan left this to the executor pending a grep. | `needs_input`, resolved at review by RUNNING the grep the question specified. | (a) Leave it open as authored, rejected because the question named a deciding measurement and leaving it unrun invites a third vocabulary, which the parent's CID-3/CID-6 forbid. (b) Mint a new token and amend §5.6, rejected outright now that the grep shows the token is already shipped FOR THIS SAME MEANING in two modules, which is the strongest case for reuse rather than the exception the question imagined. | `run_gates.py:31` (`GATE_STATUS_NEEDS_INPUT`), `run_evidence.py:1839` (`AGGREGATE_NEEDS_INPUT`, commented as spec 5.6 exit 3), `:1966`; spec `25kzda:484`, `:1035-1042`, `:1099` | yes |
| D-3 | OQ-02: exit 1 or the spec's 3, given the plan's belief that the exit tables are unreconciled? | EMIT 1 in this plan, and RECORD 3 as the spec-and-code-intended value reachable only by wiring the drivers to `aggregate_run_exit`. | (a) Emit 3 directly from the driver site, rejected because it would bypass the shipped classifier and hand-bind an integer the code deliberately keeps in one table, recreating the divergence that table exists to prevent. (b) Wire the drivers to `aggregate_run_exit` in this plan, rejected as far outside a `reviewed`-item fix: it would change every run's exit classification. (c) Leave the question open as authored, rejected because its stated premise (an unreconciled conflict) is measurably false and would have misled the executor into a wrong reason for a right answer. | `run_evidence.py:1847-1853` (the table and its "ONLY PLACE INTEGERS APPEAR" comment), `:2173-2175` and `_CLASSIFICATION_PRIORITY`; zero `aggregate_run_exit` call sites in either driver | yes |

Nothing in the plan's core reasoning was rejected. Its diagnosis, its insistence on classifying call sites
before substituting, its explicit refusal to remove `reviewed` from `SUCCESS_STATES` with a cited measured
failure, its demand for a review-mode CONTROL test as the load-bearing half of E-05, and its mutation-check
item are all correct and were preserved. What review supplied was an accurate map of which host defines
what, a siting rule that matches the convention the tests already enforce, both open questions answered
from shipped code, the spec row that makes this a compliance fix, and current baselines and coordinates.
