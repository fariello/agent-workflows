# Review: print an end-of-run disposition summary with per-disposition counts and the exact remedy command, child bsc457 (Set runnoop)

- Subject-Id: bsc457
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `bf8a431a`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value therefore
rests on EXECUTING the plan's claims rather than re-reading them. Six things were run rather than
inspected: `render_run_summary_table` was rendered with a `reviewed`/zero-attempt queue (which
produced the BLOCKER); `render_continuation_hint` was read in BOTH hosts and their sentences compared;
the `REFORK_TABLE` was imported and its rows counted; the oc-to-agy import edge was AST-measured; the
`aw ipd set approved --help` remedy was run; and the full bare suite was measured. Four of those six
produced findings, and the first two are cases where following the plan literally builds the wrong
thing or writes the wrong product name.

A NOTE ON THIS SET'S SHAPE THAT MATTERS FOR READING THE FINDINGS: five of this plan's ten findings are
the SAME defects child 02's review (`m85gxh`, PR-601/603/604/605/606) already recorded and fixed in its
own plan. They were not propagated to this sibling, which is why an overstated premise, a false
enforcement claim, an undeclared test path, an off-by-one import count and a stale baseline all
survived here after being corrected next door.

SCOPE: only this child was a candidate. Read as evidence: `oc_runipd.py` and `agy_runipd.py`
(`render_continuation_hint`, `announce_run_order`, `SUCCESS_STATES`, the auto-approval provenance
string), `render_stream.py` (`render_run_summary_table`, its COMPLETED assignment, its diagnostics
block, `format_run_order_announcement`), `run_selection_policy.py`, `tests/test_runner_refork_guard.py`,
spec `25kzda` §5.6, backlog `em0z50`, siblings `7ewc74`/`zz5yxq`/`m85gxh`, plan `r2i1b1`, superseded
`xtklpd` and its successor `ys1dor`, and child 02's review record.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-301 | BLOCKER | IN-SCOPE | F. KISS (an overstated premise that misdirects the build); D. Anti-regression | rendered the real `render_run_summary_table` with one `reviewed`/zero-attempt item | **THE CONCERN'S CENTRAL FACTUAL CLAIM IS FALSE, AND BELIEVING IT PRODUCES A THIRD QUEUE LISTING THAT FIXES NOTHING.** The plan states a zero-action run's "ONLY closing output" was the session sentence. Measured: that run ALREADY prints a full bordered table containing a per-artifact row (`01 / 01 / abc123 / wtiso / execute / reviewed`), a `Progress: 1/1 [##########] 100% (1 reviewed)` count line, and a totals row. So a per-artifact listing and a count line both already exist for exactly the case the plan says prints nothing. What is genuinely absent is the REASON beside the disposition, the REMEDY, and an honest verdict. This is a BLOCKER rather than a correction because E-01's deliverable is specified as "a line per matched artifact and a per-disposition count line", which is a DESCRIPTION OF THE EXISTING TABLE: an executor can satisfy every word of E-01 and V-01, pass validation, and ship a duplicate. Child 02's review recorded this identical overstatement as its own PR-601/F-8 and re-aimed that plan; the correction was never carried to this sibling. | C:Low; U:Medium; S:Low; F:High; Overall:Medium | FIXED | Added F-8. Concern now states the measured table output and says plainly that an executor believing the stronger claim will build a second queue table. E-01 gained "BUILD WHAT THE TABLE IS NOT" (reason-bearing, remedy-bearing, honest about zero work). V-01 now requires pasting the existing table's render BESIDE the new block and stating in one sentence what the block adds, with "the same rows again" declared a FAILED validation. |
| PR-302 | HIGH | IN-SCOPE | A. Correctness (a cross-host instruction that writes the wrong product name) | `oc_runipd.py:7575` vs `agy_runipd.py:4574`; both `render_continuation_hint` bodies read | **E-04 CALLS THE TWO HOSTS' SENTENCES TWINS AND DEMANDS THEY CHANGE "IDENTICALLY", BUT THEY DIFFER BY PRODUCT NAME BY DESIGN.** The plan quotes `No OpenCode session was captured for this run.` and calls `agy_runipd.py:4568` its "agy twin". Measured: agy prints `No Antigravity session was captured for this run.`, and oc's block header is `--- OpenCode Session Continuity ---` while agy's is its own. An executor told to change both hosts identically, holding oc's string as the canonical text, would put "OpenCode" into the Antigravity runner's operator-facing output. That is a user-visible correctness defect in the exact surface this plan exists to make trustworthy. | C:Low; U:Medium; S:Low; F:Medium; Overall:Low | FIXED | Concern gained a paragraph with both measured sentences and the rule that "identically" means the same STRUCTURE, not the same literal string. E-04's expected outcome rewritten accordingly. V-04 now requires showing agy's output naming Antigravity and declares an agy footer carrying oc's product name a FAILED validation. A Step 0 convention records the difference. |
| PR-303 | HIGH | UNDER-SCOPE | E. Testing (a false enforcement claim); G. Executability (an out-of-fence edit) | imported `REFORK_TABLE`: 46 rows, 4 one-sided; `test_the_table_covers_both_runners` body read; the authored `- Scope-Paths:` | **E-05 RESTS ITS SYMMETRY GUARANTEE ON A TEST THAT DOES NOT ENFORCE IT, AND COMMANDS AN EDIT TO AN UNDECLARED FILE.** The item claims `test_the_table_covers_both_runners` "already fails a one-sided table". Measured: it asserts only that the table's AGGREGATE `runners` set equals BOTH and that more than one row names agy. 4 of the 46 shipped rows name a single runner (`StreamTracker`, `format_tokens`, `format_statusline`, `render_event`) and the suite passes, so a one-sided row for this plan's symbol would slip through silently. Separately, E-05 mandates editing `tests/test_runner_refork_guard.py`, which the authored `- Scope-Paths:` does not declare, so the plan instructed an out-of-fence edit against its own gate. Child 02's review recorded both defects (its PR-603/F-9 and PR-604) and neither was carried here. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-9 and F-10. E-05 now states the measured non-enforcement and rests the requirement on its own mutation check; `tests/test_runner_refork_guard.py` added to `Scope-Paths`. V-05 requires a SECOND mutation check that drops one runner from the row, with an honest "nothing caught it" permitted rather than citing the aggregate test. E-05's expected outcome updated. |
| PR-304 | MEDIUM | IN-SCOPE | C. Architecture (a reusable API that does not exist) | grep for `status_summary_str`: exactly two occurrences, both inside `render_run_summary_table`; no `def` | **THE PLAN TREATS A LOCAL VARIABLE AS A CALLABLE AGGREGATION HELPER.** Both the Concern and the history line say `status_summary_str` (`render_stream.py:1855`) "already builds count strings" and conclude "the aggregation substrate is present", framing it as something E-01 need not invent. Measured: it is a LOCAL VARIABLE assigned inside `render_run_summary_table` and consumed once, in the `Progress:` line. There is no function to import, call, or reuse. Nothing in the design breaks (E-01 deliberately uses a different denominator anyway), but an executor could waste a pass looking for an importable helper, or worse, refactor `render_stream.py` to extract one, which this plan's fence forbids outright. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Concern gained an explicit correction paragraph; F-3 rewritten to say it is a local, not a function, so E-01 writes its own counting. E-01's counting instruction updated to say there is nothing to reuse. |
| PR-305 | MEDIUM | IN-SCOPE | D. Anti-regression (a wrong named baseline) | `python3 -m pytest` -> `1 failed, 5919 passed, 3 skipped, 2 xfailed`; `pytest tests/test_orchestrator_retirement.py` -> `112 passed` | **BOTH HALVES OF THE STATED BASELINE ARE WRONG, AND ONE IS AN EXPECTED-FAILURE NAME.** The plan records `1 failed, 5648 passed` with "the known `tests/test_orchestrator_retirement.py` failure". Measured: the bare run is `1 failed, 5919 passed`, and the retirement suite is fully GREEN at `112 passed`, so the named failure does not exist. The actual single failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, environmental (it trips on a gitignored local `opencode-recovery/` dump) and therefore machine-specific. The harm is directional: an executor told to expect a retirement failure could excuse a REAL regression there as known. Child 02's review recorded the same stale baseline (its PR-606). | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | The Step 0 baseline bullet rewritten with both measurements, the environmental cause, and the explicit warning that a wrong-named expected failure is worse than none. The node-id comparison rule retained. |
| PR-306 | MEDIUM | IN-SCOPE | G. Executability (every code anchor stale) | re-measured each: `:7362`->`:7575`, `:4568`->`:4574`, `:7377`->`:7589`, `:4583`->`:4589`, `:4122`->`:4178`, `:722`->`:731`, `:1872`->`:1871-1877`, `:2152`->`:2153-2179` | **EVERY LINE NUMBER IN THE PLAN HAD DRIFTED ONE DAY AFTER AUTHORING, INCLUDING ALL EIGHT IN THE FINDINGS TABLE.** The plan's own gate already says to re-locate by symbol and states these are the highest-contention files, which is correct and is why this is MEDIUM rather than HIGH; but the findings table presents the stale coordinates as "measured at HEAD `44d4950d`" without marking them superseded, and F-1's `:7362` in particular points into a different part of the function. A reader checking the evidence column would not reproduce it. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Findings table header now says every anchor was re-measured and to locate by symbol; each row carries the authored anchor beside the measured one. Concern and E-04 switched to symbol references. A Step 0 convention lists all eight drifts. |
| PR-307 | MEDIUM | IN-SCOPE | G. Executability (a resolvable question left to the executor) | `r2i1b1` OQ-01's maintainer resolution; `announce_run_order`'s docstring | OQ-01 (end-only, or start-and-end?) was left `open` with the executor as owner, but the repository already answers it and the plan had itself assembled every input without drawing the conclusion. The maintainer's ruling in `r2i1b1` OQ-01 requires four places; of those, the START requirement is ALREADY satisfied by shipped code (`announce_run_order` prints the matched list unconditionally, and a start-side print cannot carry dispositions that do not yet exist), and the `aw runs`/log requirement is explicitly OUT of this plan's fence. What remains for this child is the end-of-run summary, which is one block. Leaving this open asked an executor to re-derive a settled ruling mid-implementation. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | OQ-01 rewritten `resolved` (Owner `none`) with the four-place ruling quoted and the two facts that decide its application here. E-03 now instructs end-placement and self-containment directly instead of asking for a decision; its expected outcome updated. |
| PR-308 | MEDIUM | IN-SCOPE | C. Architecture (a coordination premise that shrank) | `xtklpd` is `- Status: superseded`; `ys1dor` is its successor; `r2i1b1` is `approved`, `Item-Dependencies: none` | OQ-02 was left `open` and escalated to the maintainer on the premise that "THREE plans converging on one expression is a coordination question". Measured: only TWO now converge, because `xtklpd` is superseded (replaced by `ys1dor`), and only ONE of the two (`r2i1b1`) declares `render_stream.py`. That makes ownership unambiguous rather than a judgement call, so the plan's own stated default (do not edit, report the disagreement) is correct and needs no maintainer round trip. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | OQ-02 rewritten `resolved` (Owner `none`) recording the superseded third plan, naming `r2i1b1` as the tuple's owner, and requiring a reported finding rather than a quiet reconciliation if the two surfaces disagree. The deferred-scope bullet cross-references it. |
| PR-309 | LOW | IN-SCOPE | A. Correctness (an unsatisfiable numeric criterion) | AST walk: agy imports 48 names from `oc_runipd`; oc imports 0 from agy | E-03 and V-03 state the oc-to-agy import count as 47 and make "did not increase from 47" the criterion. Measured: it is 48, so an executor that adds nothing would still read its own correct measurement as an increase. The DIRECTION claim (one-way, zero flowing back) is verified and correct. Child 02's review recorded the same off-by-one (its PR-605). | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-03 and the Step 0 bullet corrected to 48; V-03 now requires before/after against a self-measured count rather than a number from the plan. |
| PR-310 | LOW | IN-SCOPE | C. Architecture (a sequencing assumption that inverted) | `r2i1b1`: `- Status: approved`, `- Readiness: go-pending-approval`, `- Item-Dependencies: none`, both OQs resolved | The plan treats `r2i1b1` as a pending plan whose landing is checked at the END (E-06), describing it as owning surfaces this plan must avoid. Measured: it is APPROVED with no dependencies, so it is runnable NOW while this Set is not approved, making it the LIKELY first lander; its declared `Scope-Paths` also overlap both drivers. So the remedy-sourcing question is an AUTHORING-TIME input, not a closing formality: building a parallel remedy table in E-02 and discovering the record at E-06 means discarding that work. Child 00's review recorded this same ordering correction for the Set. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-06 gained an "EXPECT THE EXECUTED BRANCH" paragraph requiring the status be read BEFORE E-02 authors remedy data, with the measured front matter. F-6 updated with the current status. A Step 0 convention records it. |

CONFIRMED SOUND, AND THE PLAN'S SITING AND FENCING ARE ITS STRONGEST PARTS. Every architectural claim
verified: `run_selection_policy.py`'s only first-party imports really are `selectors` and `status_set`
(AST-confirmed), and `render_action_preview` (`:589`) and `render_refusal` (`:693`) are exactly where
the plan says, so the pure-renderer siting is right. The unconditional-reporting precedent is real and
its rationale is quoted verbatim from `announce_run_order`'s docstring ("the order must be auditable in
the log even when nothing was reordered"). F-2 re-confirmed by rendering the real function: a
`reviewed`-only queue does print `Outcome: COMPLETED` at 100% with no diagnostics block, so the
COMPLETED-tuple and allowlist analysis is exactly right. F-4's coupling is real: `all_success` and the
session sentence are in one function in both hosts, and `SUCCESS_STATES` is what child 01 changes, so
the re-measure instruction is correct and load-bearing. The `--full-auto` caution is right and the
provenance string says what the plan quotes. The remedy command verified: `aw ipd set approved --help`
shows `--by-human  Attest human approval`. Spec §5.6 exists, is `approved`, and its sentence is quoted
correctly, including that `next command` is this plan's remedy; the spec-sync section's instruction to
check whether a separate block satisfies "the final TABLE" is a legitimate open check rather than a
defect, and its warning never to edit §4.2's byte-tested finding-code table is correct. The scope fence
is unusually precise and the "make the edit and justify it" wording matches the 2026-09-01 ruling
rather than the retired "STOP and report" form.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. One thing fundamentally, and a cluster of inherited
staleness. The fundamental change is PR-301: this plan believed it was adding the first per-artifact
listing and count line to a run that printed neither, when both already ship. That belief is what made
E-01's deliverable indistinguishable from the existing table, and the fix re-aims the item onto the
three things the table genuinely lacks (reason, remedy, honest verdict) and makes V-01 prove the
difference rather than presence. The cluster is more troubling than any individual item: five findings
here are defects child 02's review already found and fixed NEXT DOOR, in the same Set, days earlier.
A sibling's corrections were not propagated, so the same overstated premise, the same false
`REFORK_TABLE` enforcement claim, the same undeclared test path, the same 47-vs-48 count and the same
stale baseline all had to be re-measured from scratch. Worth naming for whoever reviews child 00 or
authors the next Set: when a review corrects a claim that appears in sibling plans verbatim, the
correction belongs in all of them.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | The Concern's "only closing output" claim is false. Should the plan be re-aimed in place, or marked REPLAN because E-01's deliverable duplicates the shipped table? | Re-aim in place: keep E-01's renderer and re-specify it as the reason-and-remedy-bearing block the table is not, and make V-01 prove the difference. | REPLAN; rejected because the plan's VALUE survives the corrected premise intact. The three genuinely missing things (reason, remedy, honest zero-work verdict) are real, are what the backlog item actually asked for, and are not delivered by the table; only the JUSTIFICATION was overstated. Child 02's review reached the same conclusion on the identical claim and re-aimed rather than replanned, so this also keeps the two siblings consistent. | Rendered `render_run_summary_table` with a `reviewed`/zero-attempt queue (row + progress line + totals present, `approval` absent, no diagnostics block); child 02's review record PR-601/F-8; backlog `em0z50`'s ask is a remedy-bearing answer, not a listing. | yes |
| D-2 | Does OQ-01 (end-only versus start-and-end) need the executor or the maintainer, as the plan asserted? | Resolve it NOW as end-only-plus-self-contained, from the maintainer's existing ruling. | Leaving it open for the executor; rejected because the repository answers it and the plan had already gathered every input without concluding. Also rejected asking the maintainer again: `r2i1b1` OQ-01 IS the maintainer's ruling on exactly this, so re-asking would re-litigate a settled decision. | `r2i1b1` OQ-01 (maintainer, 2026-09-07) requires four places; `announce_run_order` already satisfies the START place unconditionally and cannot carry not-yet-existing dispositions; places (4) are explicitly outside this plan's fence, leaving the end block as the whole of this child's obligation. | yes |
| D-3 | Does OQ-02 (correct the table's COMPLETED verdict here?) need the maintainer, given it crosses a fence? | No: resolve NO from the artifacts, and require a reported finding if the surfaces visibly disagree. | Escalating to the maintainer as the plan directed; rejected because the coordination premise that motivated escalation has shrunk from three converging plans to two, and only one of those declares `render_stream.py`, so ownership is a fact rather than a judgement. The plan's own default was already this answer. | `xtklpd` is `- Status: superseded` (successor `ys1dor`); `r2i1b1` is `approved` with `- Item-Dependencies: none` and declares `render_stream.py`; this plan's fence forbids editing that file. | yes |
| D-4 | E-05 mandates editing `tests/test_runner_refork_guard.py`, which is undeclared. Add the path, or drop the instruction? | Add the path to `Scope-Paths`. | Dropping the `REFORK_TABLE` registration; rejected because the sharing pin is the only thing standing between this Set and a one-sided re-fork, which is the documented reason that guard exists, and because child 02's review resolved the identical defect the same way (adding the path), so dropping it here would leave the two siblings inconsistent about a shared guard. | The authored `- Scope-Paths:` line versus E-05's explicit instruction; child 02's review PR-604 and its amended scope; `aw ipd finalize` would otherwise demand a `--scope-reason` for a path the plan itself commanded. | yes |
