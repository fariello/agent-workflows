# Review: distinguish not-ready-yet from can-never-be-ready so a transiently blocked item is retried instead of parked forever, child akzy45 (Set depblock)

- Subject-Id: akzy45
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `921bf409`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value therefore
rests on EXECUTING the plan's claims rather than re-reading them. Four things were run rather than
inspected: `cascade_dependency_blocked` was driven in-process across five sibling statuses; the drain
arm's reachable causes were enumerated by construction and each candidate tested; the measured
incident's `state.json` was re-read item by item rather than taken from the plan's summary; and the
oc-to-agy import edge was AST-counted. The first produced the inverted-premise finding, the second
produced the vacuity finding that reshaped E-05 and E-06, and the third showed the plan's own evidence
covers only half of what it claims.

SCOPE: only this child was a candidate; the Set has exactly one member. Read as evidence:
`oc_runipd.py`, `agy_runipd.py`, `runner_shared.py`, `tests/test_runner_item_dependencies.py`,
`tests/test_runner_shared.py`, spec `25kzda` §4.3 and §5.7, backlog `nueip1`, executed plan `7nkcgp`,
pending plans `51vw4y`, `rl67b0`, `9kmbr0`, `1f7xno`, and run `run-20260905T050043Z-639569`'s
`state.json`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-901 | BLOCKER | IN-SCOPE | C. Architecture; G. Plan executability (undeclared collision) | `51vw4y` E-04 and its front matter; `oc_runipd.py:7336`, `agy_runipd.py:4350` | **TWO PLANS REWRITE ONE CODE ARM, AND THIS PLAN DECLARED NO DEPENDENCY.** OQ-02 asserts `51vw4y` "does not touch" the drain-time path and concludes the two are "complementary in either order". MEASURED: `51vw4y` E-04 rewrites the SAME `runnable is None` arm, using that exact condition as the trigger for its bounded integration poll, and its Step 0 conventions name the arm twice ("`runnable is None` IS ALREADY COMPUTED ... and is exactly rung 2's trigger"). It has also ADVANCED since this plan was authored, to `- Status: reviewed` / `- Readiness: go-pending-approval`, while this plan sat at `to-review`. OQ-02's fallback argument is wrong in a generalizable way worth naming: per-item worktrees resolve TEXTUAL conflicts through the merge-and-revalidate gate, but they cannot reconcile two DESIGNS for one code path, and whichever landed second would be reviewed against a base its author never read. With no declared edge, nothing sequenced them. | C:Medium; U:Low; S:Low; F:Medium-High; Overall:Medium | FIXED | Added F-11. Declared `- Item-Dependencies: executed:51vw4y` (verified it resolves: `evaluate_ipd_dependencies` reports zero findings for this plan). E-02 re-scoped to add its classification INSIDE that plan's post-landing shape, with an explicit instruction to STOP and report if it has not landed rather than write against the old shape. OQ-02 rewritten `resolved` with the measurement and the isolation correction. A dependency-gate paragraph added at the head of the gate. |
| PR-902 | HIGH | IN-SCOPE | A. Correctness (inverted premise); D. Anti-regression | drove `cascade_dependency_blocked` across five sibling statuses; `oc_runipd.py:4277` | **E-03's PREMISE IS INVERTED, AND ACTING ON IT AS WRITTEN WOULD REOPEN THE CASES E-05 EXISTS TO PROTECT.** E-03 treats the cascade-of-a-cascade as the thing to fix and asks for an item blocked on a sibling's label to become runnable again. MEASURED: the cascade's terminal test is CORRECT. It labels a dependent when the sibling is `dependency-blocked` or `integration-blocked`, and does NOT when the sibling is `queued`, `interrupted` or `executed`. So the cascade-of-a-cascade is correct behavior applied to a label that E-02 should never have written, and the fix belongs UPSTREAM: once the sibling stays `queued`, driving the cascade produces no cascade and the dependent survives with no un-blocking machinery at all. A mechanism that RESURRECTS an already-labelled item cannot distinguish the transient label from the permanent one at the point of resurrection, so it would reopen exactly the genuinely-dead prerequisites case (a) requires to keep cascading, and it duplicates `--retry-incomplete`. | C:Medium; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added F-12. E-03 rewritten as PREVENTION-not-resurrection: the deliverable is a demonstration against the loop's existing per-iteration re-test, with new code only if the demonstration proves it necessary and then with a stated reason and bound. V-03 now requires stating whether any new code was written and pasting a diff proving no re-queue path was added. E-05 case (c) re-specified to assert the sibling never ACQUIRES the label, with an explicit note that asserting the cascade tolerates a `dependency-blocked` prerequisite would contradict case (a). Gate forbids modifying the cascade's terminal test. |
| PR-903 | HIGH | UNDER-SCOPE | A. Correctness; E. Testing (an uncharacterized target condition) | enumerated and tested in-process; `'interrupted' in TERMINAL_STATES` -> `False` | **THE TRANSIENT DRAIN CASE IS NEVER CHARACTERIZED, AND WITHOUT THE CHARACTERIZATION E-02's NARROWING IS UNSAFE.** Because `cascade_dependency_blocked` runs FIRST in each loop iteration, any terminal-non-success prerequisite is already labelled before the drain is reached, so the drain arm is reachable only with all blockers non-terminal. That leaves exactly three causes: a CYCLE among queued items (structurally permanent), an unsatisfiable EXTERNAL edge (permanent in-run, there being no `--with-dependencies` closure), and a prerequisite in a non-terminal non-queued state, which measured is `interrupted`. Only the third is transient. The plan asks E-02 to "label only what is permanently blocked" without saying which causes those are, so a reasonable executor could narrow the label off a CYCLE, converting a clean dead end into an infinite wait, which is the exact regression `7nkcgp` was guarding. It also means the practical benefit is narrower than the Concern implies, which the plan should say rather than oversell. | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added F-13. Concern gained a paragraph characterizing the drain condition. E-02 now names the `interrupted` case as the target and forbids relabelling a cycle or dangling edge, requiring the executor to re-verify the enumeration and report a fourth cause if found. E-05 gained cases (d) cycle and (e) dangling external edge that must STILL terminate. E-06 gained mutation 3, since mutations 1 and 2 both exercise the cascade and neither touched the drain's classification. Under-scope now states the narrowness plainly. |
| PR-904 | HIGH | IN-SCOPE | A. Correctness (evidence does not cover the claim) | re-read that run's `state.json`; status census 4 blocked / 18 queued; `runner_shared.py:3267` | **THE MEASURED INCIDENT PROVES PART 1 ONLY, AND CONTAINS A FOURTH BLOCKED ITEM THE PLAN MISSED.** The plan presents one run as the evidence for a two-part defect. MEASURED from that `state.json`: 18 items remain `queued` beside the 4 blocked ones, and the drain arm labels EVERY remaining queued item, so had it fired zero would remain `queued`. Therefore all labels came from the cascade and the drain path NEVER RAN in the incident; Part 2 rests on code reading alone. Separately, the run holds FOUR `dependency-blocked` items, not three: `5e4sb6` is an `orchestrate` item carrying `unsatisfied_dependencies: []`, a terminal label with no nameable unmet dependency, which is precisely the defect `pgq326` already fixed and whose `dispatch_orchestrator_item` docstring names `5e4sb6` as its motivating case. Counting it as evidence here would argue for work already done. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-15. Concern gained two paragraphs: one stating plainly that the incident does not exercise Part 2 and that Part 2 rests on code reading, one identifying the fourth item as the already-fixed case. Step 0 conventions record both. E-01 now treats the orchestrator path as already-correct PRECEDENT (its RECONSIDER outcome writing no status is this plan's own transient handling, worked) rather than as a site to classify with an open disposition. |
| PR-905 | MEDIUM | UNDER-SCOPE | F. UX; A. Correctness (a regression the plan asks to avoid but does not name) | `oc_runipd.py:7350-7367` | **AN ITEM YOU DECLINE TO LABEL LOSES THE RECOVERY HINT ENTIRELY, which makes the fixed state LESS informative than the bug.** E-02 says "do not remove the recovery hint", but `item["dependency_block_recovery"] = DEPENDENCY_BLOCK_RECOVERY_HINT` is assigned PER ITEM INSIDE the labelling loop being narrowed. So an item newly left `queued` exits the run with no status explanation and no recovery text at all, where today it at least gets a terminal label plus the exact recovery command. The plan treats hint preservation as a caution when it is a concrete regression requiring its own report line. | C:Low; U:Medium; S:Low; F:Medium; Overall:Low | FIXED | Added F-14. E-02 now states the mechanism and requires an explicit report line for the transient path, framed as a regression risk rather than a caution. E-05 case (b) must assert the item is not silently statusless. V-02 and V-05 require the report line pasted. OQ-03's resolution carries the reporting half as the substance of the answer rather than bookkeeping. |
| PR-906 | MEDIUM | IN-SCOPE | G. Plan executability (a resolvable question deferred into implementation) | spec `25kzda:678`, `:924` read in full | The spec-sync section asks the executor to "determine and report" mid-implementation whether this plan is a COMPLIANCE fix, which is a question review can and should answer, and which materially affects scope (a compliance reading would put an approved spec in `Scope-Paths`). ANSWERED: both passages mandating re-evaluation scope it to RESUME. §4.3's sentence begins "On a later explicit resume, the engine re-evaluates every dependency-not-met item", and §5.7 item 8 sits inside the numbered list of what `aw <host> run resume <run-id>` does. The spec therefore does NOT require same-run re-evaluation, the runner already satisfies the resume requirement under `--retry-incomplete`, and this plan is an ENHANCEMENT. Reading it as a compliance gap would have been a misreading in the plan's own favor, which is the direction a self-authored plan is most likely to drift. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-17. Spec-sync section rewritten: question one answered with both citations quoted, the enhancement-not-compliance conclusion recorded, and the spec explicitly kept OUT of `Scope-Paths` with an instruction to declare it BEFORE editing should execution overturn the reading. Question two (vocabulary) retained as binding. Gate updated to forbid the spec edit. |
| PR-907 | MEDIUM | UNDER-SCOPE | A. Correctness (an incomplete audit) | grep of status assignments across both drivers and `runner_shared.py:3267` | E-01 says the write sites are "at least two ... plus the orchestrator dispatch path", which leaves the audit's boundary undefined for an item whose whole deliverable is completeness. ENUMERATED: exactly three, and the third is the one an executor will miss, because in `runner_shared.dispatch_orchestrator_item` the value appears as the `terminal_status="dependency-blocked"` DEFAULT PARAMETER rather than as a literal assignment in a loop, so a grep confined to the two drivers does not surface it. Also unstated: the `TERMINAL_STATES` definition ALREADY carries a `7nkcgp`-authored comment describing Part 2's behavior, so E-01's instruction to record the classification there would produce two descriptions of one arm unless it updates that comment. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-01 now enumerates all three sites with anchors and flags the default-parameter site as the easily-missed one; it requires UPDATING the existing limitation comment rather than adding a second. V-01 fails an incomplete classification explicitly. Step 0 conventions record both facts. |
| PR-908 | MEDIUM | IN-SCOPE | E. Testing (a wrong number used as a pass criterion) | AST walk of `agy_runipd`'s `ImportFrom` nodes; `_SHARED_NAMES` counted | Two counts are wrong and one is load-bearing. The oc-to-agy import count is **48**, not the 47 the plan asserts in E-04 and again in V-04, where it appears as the PASS CRITERION "showing it did not increase from 47". A correct implementation measuring 48 would therefore fail its own validation, and the likely response is to "fix" a number that was never wrong. `_SHARED_NAMES` holds **twelve** names, not eleven; the twelfth (`dependency_status_detailed`) was added by `03ie04` E-04 precisely because its absence let the guard pass over agy's real copy, which is the failure mode E-04 must not repeat. The DIRECTION claim (zero reverse imports) was re-verified and holds. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-16. E-04 states the measured 48 with the reason the stale figure is dangerous, notes plan `9kmbr0`'s title carries the same stale number so it is not corroboration, and corrects the guard to twelve names. V-04 now requires a self-measured baseline and explicitly says a count of 48 is not a regression to fix. Required-tests section carries the same instruction. |
| PR-909 | LOW | IN-SCOPE | E. Testing (stale baseline) | bare run and targeted run at HEAD `921bf409` | The Step 0 suite baseline is stale in both halves: it declares `1 failed, 5648 passed` attributed to `tests/test_orchestrator_retirement.py`, while the measured bare run is `1 failed, 5919 passed, 3 skipped, 2 xfailed` with the failure being `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` (ENVIRONMENTAL, caused by a gitignored local `opencode-recovery/` dump), and the named module now gives `112 passed`. Separately, the `cnwy8g` re-homing plans are described as "graduated plans" but `9kmbr0` and `1f7xno` both read `- Status: to-review`, so nothing may rely on the re-homing having happened. The plan's own instruction to compare NODE IDS rather than totals is what keeps this from being harmful. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-18 and F-19. Step 0 baseline replaced with measured values, the failure labelled environmental with its cause, the misattributed module recorded as now-passing, and the re-homing plans' real statuses stated. Required-tests section tells the executor to measure its own and quote none of these. |

CONFIRMED SOUND, AND IT IS THE PLAN'S CORE DIAGNOSIS. Both headline defects are real and were
re-verified by import rather than by reading: `'dependency-blocked' in TERMINAL_STATES` returns `True`,
the selection filter admits only `queued`, and the cascade only ever moves `queued ->
dependency-blocked` with no path back; and the drain-time arm does label EVERY remaining queued item
and `break` out of the run. The cascade's action-aware bar is exactly as described
(`EXECUTION_SUCCESS_STATES` for execute, `SUCCESS_STATES` for review) and is correctly identified as
the model. The wind-down branch really does make this plan's own argument in the adjacent lines,
declining to relabel the remainder because doing so "would be exactly the fabricated disposition spec
R22 forbids", and it is the right shape to follow. `7nkcgp` really did preserve the `--retry-incomplete`
default deliberately, and the re-queue really does sit under `if retry_incomplete:` in the RESUME path
with `dependency-blocked` in its status set, so a bare `resume` genuinely leaves the item blocked. The
spin-hazard citation is genuine and is the orchestrator path's own recorded reasoning. The measured
incident's three named shapes reproduce exactly, all five prerequisites now read `executed` in
`executed/`, confirming F-4's transience claim. The one-way import direction holds at zero reverse
imports. The drain path really is duplicated per driver while the cascade really is shared, so E-04's
distinction is correct. And the instruction to re-locate by symbol is vindicated twice over: every
anchor this plan wrote had itself drifted within a day, which is now recorded in the gate.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. The diagnosis survived intact; the REMEDY moved. Two
of the three fix items were pointed at the wrong place. E-03 was aimed at the cascade, which measurement
showed is correct, so it became a demonstration that prevention suffices rather than a new mechanism,
which also removed the plan's largest regression risk. E-02 was aimed at a condition the plan never
characterized, and characterizing it both narrowed the target to a single cause and revealed two
permanent causes that must NOT be narrowed, which is where the new test and mutation cases came from.
The plan is now smaller in code and larger in proof, which is the right direction for a change whose
failure mode is an infinite wait. The one genuinely new obligation is the declared dependency on
`51vw4y`: this plan is about items stranded by dependency labels and it now carries a dependency edge
of its own, which the gate states plainly along with the correct response if it is ever stranded.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | How should the collision with `51vw4y` over the `runnable is None` arm be resolved: a declared dependency, a prose coordination note, or a scope split? | A declared `- Item-Dependencies: executed:51vw4y` edge, with E-02 re-scoped to build on that plan's post-landing shape. | (a) Keep it as a prose note, which is what the plan had; rejected because nothing enforces prose and both plans are queueable today, so the second to land would be written against a base its author never read. (b) Split the arm so each plan owns part of it; rejected as inventing a boundary inside a fifteen-line block, which is harder to review than an ordering constraint. (c) Merge the two plans; rejected as outside a reviewer's authority and because their concerns are genuinely different. | `51vw4y` E-04 and its Step 0 both name `runnable is None` as their trigger; its front matter reads `- Status: reviewed` / `- Readiness: go-pending-approval` versus this plan's `to-review`, so it is ahead; the edge was verified to resolve (`evaluate_ipd_dependencies` reports zero findings for `akzy45`) and `aw check plans` shows no dependency finding against it. | yes |
| D-2 | Given the cascade's terminal test is correct, should E-03 keep asking for an un-blocking mechanism? | No: re-specify E-03 as PREVENTION plus a demonstration, adding code only if the demonstration proves it necessary. | (a) Keep the resurrection mechanism; rejected because at the point of resurrection the transient label and the permanent label are indistinguishable, so it would reopen the genuinely-dead prerequisites E-05 case (a) requires to keep cascading, and it duplicates `--retry-incomplete`. (b) Delete E-03 entirely; rejected because the same-run re-testability claim still needs proving, and the demonstration is exactly that proof. | Drove `cascade_dependency_blocked` across five sibling statuses: it fires on `dependency-blocked`/`integration-blocked` and not on `queued`/`interrupted`/`executed`, so a prevented label produces no cascade; the loop already reloads state and re-tests every `queued` item each iteration, which the plan's own Concern records as working. | yes |
| D-3 | What exactly counts as the transient drain case that E-02 must stop labelling? | Only a prerequisite in a non-terminal NON-QUEUED state, measured as `interrupted`. Cycles and unsatisfiable external edges stay permanent and keep the terminal label. | (a) Leave it as "prerequisites merely unfinished", the plan's wording; rejected as admitting a cycle, which would convert a clean dead end into an infinite wait, the precise regression `7nkcgp` guarded. (b) Treat every non-terminal blocker as transient including cycles; rejected for the same reason, and because the static evaluator already reports a cycle through `preflight_dependency_findings`, so downgrading it here would contradict a finding the run emitted. | Because the cascade runs FIRST each iteration, the drain is reachable only with all blockers non-terminal; enumerating that set by construction and testing each candidate yields cycle (permanent), dangling external edge (permanent, no `--with-dependencies` closure exists), and `interrupted` (`'interrupted' in TERMINAL_STATES` -> `False`, the only transient one). | yes |
| D-4 | Is this plan a spec-compliance fix (requiring an amendment and the spec in `Scope-Paths`) or an enhancement? | An ENHANCEMENT; no amendment, spec stays out of `Scope-Paths`. | Reading it as a compliance fix, which the plan's spec-sync section floated first and which would have justified editing an approved spec; rejected on the text. | Spec `25kzda:678` scopes re-evaluation with "On a later explicit resume, the engine re-evaluates every dependency-not-met item", and `:924` (§5.7 item 8) sits inside the numbered list of what `resume` does. Neither requires same-run re-evaluation; the runner already satisfies the resume requirement under `--retry-incomplete`. | yes |
