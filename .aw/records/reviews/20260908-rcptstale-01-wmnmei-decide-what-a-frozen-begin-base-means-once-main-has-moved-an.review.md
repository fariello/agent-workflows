# Review: decide what a frozen begin base means once main has moved, child wmnmei (Set rcptstale)

- Subject-Id: wmnmei
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `3a3eaa79`. `aw ipd lint --phase author` returned EXIT 1 with a single `IPD-Q501`
(OQ-01 is `Blocking: yes` and `open`) BEFORE semantic review, and `--phase review-finalize` returns the
SAME single finding after every revision. That is not a defect and was not repaired: the backlog item's
first line declares itself "AN EXPLORATION, NOT A FIX" and forbids graduating it into a plan that
implements expiry, so the plan's deliverable IS a maintainer decision, and the lint gate refusing it at
every checkpoint is the mechanism doing its job. Nothing else structural fired.

DISCLOSURE: the same agent/model authored this plan yesterday, so this is a SELF-REVIEW. Its value rests
entirely on RE-MEASURING rather than re-reading, and the measurements inverted the plan's own diagnosis
three times. Eleven things were RUN rather than inspected: `check_scope_drift` was called in-process and
counted per location; every contributing receipt was read and its base age computed with
`git log --no-merges`; `_receipt_is_live` was CALLED on each; `_changed_path_sources` was called per
receipt and the out-of-scope set split by source under the rule's own fence; the shipped
`_execution_cohesive_committed_paths` was called over the same three receipts to see what ownership
attribution would actually yield; `_iter_type_files` was walked and every plan id6 collected to
membership-test the item's six named plans; the receipt directory was listed and counted; both
`45f8156c` and `06b6c72e` timestamps were read; `git worktree list` plus per-lane
`git status --porcelain` established which contributors are actively executing; `aw check plans --agent`
was tallied by rule code and its unpiped exit code measured; and the bare suite was run. Every scope-drift
line number cited in the plan was re-located by symbol, and four were wrong.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | HIGH | IN-SCOPE | Evidence accuracy (the diagnosis, not just a number) | `_changed_path_sources` per receipt: committed-only/worktree-only 81/0, 75/0, 18/0; `check_engine.py:1330` docstring | **THE NOISE IS 100 PERCENT COMMITTED HISTORY AND 0 PERCENT WORKING TREE, WHICH THE PLAN NEVER MEASURED AND WHICH CHANGES WHAT THE FIX IS.** `check_scope_drift`'s own docstring calls itself an advisory about paths changed "since the frozen base" in "the current working tree", and the plan inherited that framing. Splitting the union per contributor shows the working-tree half contributes NOTHING today: every offending path comes from `base..HEAD`. That is precisely the half `h9cn0y` shipped `_execution_cohesive_committed_paths` to attribute, so the defect is misattribution of other agents' commits and a collapse-only fix would compress a wrong answer rather than correct it. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-3b (BLOCKER). Concern rewritten to lead with the split; E-01 now REQUIRES the per-contributor committed/worktree split under the same fence with the measured baseline stated; V-01 demands it pasted plus a one-sentence verdict; a new Validation bullet and a conventions bullet carry it. |
| PR-002 | HIGH | IN-SCOPE | A. Correctness; D. Domain invariants | receipt timestamp 2026-09-08T21:42; `git -C .aw/worktrees/hp9rot status --porcelain` = 8 modified files; 41 commits since its base | **"STALE" MISNAMES EVERY CURRENT CASE, AND THE MISNAMING POINTS THE REMEDY AT THE WRONG THING.** The plan's title, concern, E-06 and OQ-02 all frame the surviving case as a stale baseline. Measured, `hp9rot` holds a receipt written hours earlier whose lane worktree is dirty RIGHT NOW, with 41 commits landed on main since; `xdr83v` and `yvvf98` are the same shape a day older. Nothing about these bases is wrong and no plan drifted: all 174 findings report other agents' commits as this plan's drift. So age is a proxy for "main moved", and every age-keyed remedy fires HARDEST on the most active execution, inverting the intent. | C:Low; U:Medium; S:Low; F:High; Overall:Medium | FIXED | Added F-5b (BLOCKER). Concern gained the reframing paragraph; E-01 gained a lane-dirtiness column; E-03 must now state per candidate whether it addresses misattribution or only volume; E-04 gained reason (5); E-06 retitled to "the live-but-misattributed case" and told to name the state for the defect not the age; OQ-01 and OQ-02 both carry the sharpened question. |
| PR-003 | HIGH | IN-SCOPE | C. Architecture (work commissioned that already ships) | `h9cn0y` Status `executed`; `_execution_cohesive_committed_paths(repo, base, sp)` called over all three receipts, same fence: 21, 7, 9 vs 81, 75, 18 | **THE PLAN CALLS `h9cn0y` "APPROVED" AND TREATS ITS OWNERSHIP PREDICATE AS FUTURE WORK; IT IS EXECUTED AND CALLABLE, AND CALLING IT CUTS THE FINDING SET BY 76 PERCENT.** E-03 said the advisory "may be able to inherit the result rather than inventing anything" and left it at that, and the Deferred section named `h9cn0y` as owning an unfinished half. Running the shipped predicate over the same three receipts under the same fence yields 37 out-of-scope paths instead of 174, entirely from correcting attribution. A candidate that is already implementable and measurable must not be presented to the maintainer as a maybe. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-3c (BLOCKER) and F-11 rewritten. E-03's candidate list went from four to FIVE with the measured option (e) and its honest heuristic cost quoted from the predicate's own docstring; V-03 requires the re-measured figures; OQ-01 records that the leading candidate is partly in-tree; Deferred and conventions corrected to `executed`. |
| PR-004 | HIGH | IN-SCOPE | G. Plan executability (a blocked deliverable) | `- Depends on: E-04` on E-05; the gate's own claim that E-05 is "deliberately independent of OQ-01" | **E-05, THE ONE CHANGE THE PLAN SAYS IS CORRECT UNDER EVERY OUTCOME, WAS CHAINED BEHIND THE THREE ANALYSIS ITEMS THAT FEED THE BLOCKING QUESTION.** The plan asserts twice that the collapse is independent of OQ-01 and puts it in its own task group for that reason, then makes it depend on E-04. Since `aw ipd lint` refuses the plan while OQ-01 is open, and E-02 to E-04 exist only to inform that question, the declared dependency strands the plan's only unconditional deliverable behind the very decision it was designed to survive. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-05 now `Depends on: E-01` with an explicit paragraph stating why, and E-06 correctly depends on E-04 AND E-05. Proposed changes reordered to put the collapse second. |
| PR-005 | HIGH | IN-SCOPE | Evidence accuracy (every quantity, including its own) | `check_scope_drift` at `3a3eaa79`: 174 findings from three plans (81/75/18) | **THE PLAN REPLACED THE ITEM'S DEAD NUMBER WITH ITS OWN, ONE DAY OLD AND ALREADY DEAD.** It measured 110 findings with 72 from `xdr83v` at base `3d55a2d6` frozen 2026-09-06T06:19:17Z, and "38 across seven plans at 5 to 7 each". At review: 174 from exactly three, `xdr83v`'s receipt re-begun at base `5fe992aa` on 2026-09-08T08:06:20Z, and the alleged seven small contributors are actually one at 75 and one at 18. The receipt field is `timestamp`, not `frozen_at`. Also wrong: 60 plan files iterated (104), and the suite baseline `1 failed, 5648 passed` (`1 failed, 5919 passed, 3 skipped, 2 xfailed`, with a DIFFERENT failing node, `test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, environmental, caused by 189 untracked `opencode-recovery/*.md` files belonging to another party). | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | F-1 rewritten to record all three rounds and to forbid citing any of them; Concern, E-01, F-2, F-3 and the Validation baselines re-measured; the field name corrected; the suite baseline replaced with the measured figures and its environmental cause named. |
| PR-006 | HIGH | IN-SCOPE | G. Plan executability (a stale argument for waiting) | count trajectory 1013 -> 110 -> 174 in nine days; `git log --no-merges` per day: 99, 57, 134, 23 for 2026-09-06..09 | **THE PLAN CELEBRATES THE SELF-HEAL WITHOUT NOTICING THE COUNT WENT BACK UP, WHICH IS THE ACTUAL ARGUMENT FOR ACTING NOW.** F-2 and the Concern present the item's prediction as vindicated and "spent", which invites the reading that the problem drains itself. It does not: the count falls as plans reach terminal directories and refills on every fresh `aw ipd begin`, in a tree absorbing 57 to 134 non-merge commits per day. The volume problem is structural and regenerating, not a backlog to wait out. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-2b (HIGH) with the trajectory and the per-day commit rate; the Concern's self-heal paragraph now states that self-healing is not convergence. |
| PR-007 | MEDIUM | IN-SCOPE | A. Correctness (four wrong citations in a plan that warns about citations) | symbols re-located by name | **FOUR OF THE PLAN'S LOAD-BEARING LINE NUMBERS ARE WRONG, INCLUDING ONE ATTRIBUTED TO THE WRONG FUNCTION.** `_receipt_is_live` is at `:1266`, not `:1237` (cited five times); the per-path Drift loop is at `:1386`, not `:1357`; `isolate_worktree`'s default is at `oc_runipd.py:7888-7891`, not `:7661-7665`; and the frozen-base constraint the plan quotes three times is in the docstring of `begin` at `ipd_lifecycle.py:946-950`, NOT `run_begin` (which is the thin CLI wrapper at `:2933`). The plan's own gate tells the executor to re-locate by name, so its citations were the natural place to check. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Every citation corrected throughout, `begin` vs `run_begin` disambiguated at all three sites, and the gate's re-locate paragraph now cites the plan's OWN corrected citations as the evidence for its own instruction. |
| PR-008 | MEDIUM | IN-SCOPE | E. Testing (a declared file that does not exist, and two that do) | `ls tests/`: no `test_check_engine_scope_drift.py`; coverage lives in `test_check_engine_receipt_liveness.py` and `test_event_derived_lifecycle.py` | **`Scope-Paths` DECLARES A TEST FILE THAT DOES NOT EXIST WHILE OMITTING THE TWO THAT HOLD THE EXISTING COVERAGE.** An executor reading the declaration would reasonably conclude scope-drift is untested and rebuild fixtures that already exist (seven liveness cases a-g, plus four rule-behavior tests). Worse, `test_event_derived_lifecycle.py::test_flags_out_of_scope_change` asserts on finding DETAIL, so a collapse could legitimately require a one-line change there, which would have been an undeclared out-of-scope edit. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-16. `Scope-Paths` now also declares `tests/test_check_engine_receipt_liveness.py` and `tests/test_event_derived_lifecycle.py`, and the Scope check explains that declaring them permits a needed adjustment without licensing a rewrite. |
| PR-009 | MEDIUM | IN-SCOPE | E. Testing (an unmeasured CI risk, and a missing case) | every scope-drift assertion is emptiness/truthiness; `tests/test_ci_check_parity.py:87` compares `(rule, location)` sets; `aw check plans` = 193 findings across 5 codes, exit 1 | **THE PLAN TREATS THE COLLAPSE AS A CI RISK TO VERIFY WHEN IT IS MEASURABLY NOT ONE, AND MEANWHILE OMITS THE ONE CASE ITS FIXTURES WOULD NOT CATCH.** No existing test asserts a scope-drift count, and the parity test compares `(rule, location)` pairs, which a per-plan collapse preserves by construction. Separately, `aw check plans` exits 1 on four OTHER rule codes regardless, so no reduction here moves the gate. The real gap is the opposite: nothing would catch a collapse that merged ALL plans into one finding instead of one per plan, and nothing checks that the `recovery` field the pre-commit hook prints verbatim survives. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-14 and F-15. E-05 gained the location/rule-id invariant, the `recovery` requirement and the measured gate statement; Validation gained a MULTI-PLAN fixture case, a `(rule, location)` set comparison, and a hook-surface check against `test_phase4_hooks.py`'s three assertions; V-05 demands all of it. |
| PR-010 | MEDIUM | IN-SCOPE | B. Security-adjacent; shared-checkout safety | `git worktree list`; `.aw/worktrees/hp9rot` with 8 modified files; 189 untracked `opencode-recovery/*.md` | **THE PLAN ENUMERATES OTHER AGENTS' IN-FLIGHT PLANS AS MEASUREMENT SUBJECTS WITH NO FENCE AGAINST ACTING ON THEM.** Two of the three contributors have live lane worktrees, one actively dirty. The plan forbids deleting receipts but says nothing about lanes or branches, and an executor told to investigate "stale" receipts could plausibly inspect, check out, or tidy a lane. The untracked recovery files that cause the one suite failure are likewise another party's. | C:Low; U:Low; S:Medium; F:Medium; Overall:Medium | FIXED | Added a CONCURRENCY FENCE paragraph to the gate (no touching `.aw/worktrees/`, no `aw/lane/*` checkout, no tidying an apparently abandoned lane or receipt, and the explicit rule that a plan appearing in the distribution is evidence about the RULE and never a licence to act on that plan), plus a conventions bullet and the untracked-file warning. |
| PR-011 | MEDIUM | IN-SCOPE | A. Correctness (a hazard reported as lapsed while it has an instance) | `hp9rot` live receipt + dirty lane + 41 commits distance | **E-04 TOLD THE EXECUTOR THAT THE STRANDING HAZARD MAY HAVE NO INSTANCE, AND ONE EXISTS.** The plan noted `6knsrx` is now superseded and instructed the executor to consider saying "no live receipt would be revoked". Measured, a distance-keyed expiry would revoke `hp9rot`'s authority today, mid-execution, with uncommitted lane work. Reason (1) against expiry is therefore not weakened but currently demonstrable. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | F-9 rewritten from "may be gone" to "has an instance again"; E-04 now requires the list WITH lane dirtiness and states the `hp9rot` instance; V-04 demands the list or an evidenced none. |
| PR-012 | MEDIUM | IN-SCOPE | A. Correctness; F. Prevent silent failure | `_DEFAULT_RULESPEC` fallback (`error`, empty invariant); `check.scope-drift` registered `error`/`I-01` | E-06 said not to mint a new rule code without authorization but never said what minting one entails. An unregistered id silently inherits `error` with an EMPTY invariant trace, so adding a code without a deliberate `RULE_REGISTRY` entry would change a CI-consumed contract by omission, which is exactly the failure mode the surrounding comments in that registry warn about twice. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-06 now requires registration with an explicit severity and invariant trace; V-06 requires the registry entry pasted; a conventions bullet records the fallback. |
| PR-013 | LOW | IN-SCOPE | E. Testing (an under-specified regression) | `tests/test_check_engine_receipt_liveness.py` cases a-g; `tests/test_phase4_hooks.py:75-96` | The liveness-regression bullet said only "paste the tests covering `_receipt_is_live`" without naming them, and nothing covered the hook surface at all, even though the hook asserts on the finding's rule name, its `fix:` prefix and the string `Scope-Paths`, all of which a detail-string rewrite could break. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Both test modules named explicitly in Validation and in V-06; a hook-surface bullet added with its three assertions. |
| PR-014 | LOW | IN-SCOPE | G. Plan executability (an expected refusal read as a defect) | `aw ipd lint --phase author` exit 1, one `IPD-Q501`; `ipd_lint.py:637` | The gate explained WHY OQ-01 is blocking but never told the executor what the linter will actually do, so a conscientious agent meeting `IPD-Q501` could read it as a plan defect and "fix" it by flipping `Blocking: no`, which would silently unblock an exploration the item insisted must not be decided for the maintainer. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | The gate now states that `IPD-Q501` is expected until the maintainer answers, that it is the gate working rather than a defect, and that it is why the plan cannot be approved or begun. |

CONFIRMED SOUND, AND THE PLAN'S CENTRAL JUDGEMENT IS THE RIGHT ONE. Its refusal to graduate the item
into an expiry implementation is correct and now better supported than when written: reasons (2), (3)
and (4) all hold, (4) is stronger since isolation defaults on, and review added a fifth. Carrying the
REJECTED FRAMING forward whole rather than dropping it is the single most valuable structural choice in
the plan, because expiry is the obvious-looking fix that the measurement shows would fire hardest on
healthy work. The overload finding is real (five reads of the receipt's `base_head`, three with
different needs), the frozen-value constraint it identified is genuinely documented in code and must
not be broken, OQ-03's resolution to keep the rule is correct and now rests on the rule being the only
in-tree check on a plan's declared fence between `begin` and `finalize`, and the deferrals of the
resume-semantics question and the `aw doctor` surfacing are both properly reasoned. The six named
plans' receipts were verified present and untouched, exactly as the item demanded.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. No E-item was added or removed; the E/V bijection is
unchanged at six. What changed is the DIAGNOSIS the maintainer will decide against: from "a stale base
produces too many findings" to "a correctly frozen base attributes other agents' commits to this plan,
and the ownership fix for that already ships unconsumed". One dependency edge was corrected so the
plan's unconditional deliverable is reachable while OQ-01 stays open. OQ-01 remains OPEN and unresolved
because it is genuinely the maintainer's: candidate (e) buys correctness by adopting a documented
two-sided heuristic and makes a CI-gating advisory quieter in the case where it is currently the only
warning, which is a risk-appetite judgement no measurement settles.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Should the `IPD-Q501` finding on OQ-01 be repaired so the plan lints clean? | No. Left OPEN and recorded as the expected, correct behavior of the gate; readiness set `no-go` because of it. | Flipping `Blocking: no` (would silently unblock an exploration the item insisted must not be decided for the maintainer); resolving OQ-01 myself (forbidden: plan-review may not invent a human decision). | Backlog `v880xk` line 58 ("THIS ITEM IS AN EXPLORATION, NOT A FIX"), lines 92-102 (the maintainer "declined to accept that as the answer"); `ipd_lint.py:637` | yes |
| D-2 | E-05 declared `Depends on: E-04` while the plan asserts twice that the collapse is independent of OQ-01. Which is authoritative? | The independence claim. Re-pointed E-05 to `Depends on: E-01`, and E-06 to `E-04, E-05`. | Leaving the edge and softening the independence claim (would strand the plan's only unconditional deliverable behind a blocking question); dropping E-05's dependencies entirely (it genuinely needs E-01's distribution to justify an unconditional collapse). | The plan's own task-group 3 heading and gate paragraph; `IPD-Q501` refusing the plan while OQ-01 is open | yes |
| D-3 | Should `Scope-Paths` gain the two existing test modules, given a plan may not widen its own scope silently? | Yes, declared explicitly, with the Scope check stating that declaration permits a needed adjustment and does not license a rewrite. | Leaving them undeclared (a one-line detail-assertion fix in `test_event_derived_lifecycle.py` would then be an undeclared out-of-scope edit that finalize would demand a `--scope-reason` for); adding a prohibition on touching them (would make a legitimate consequence of E-05 unimplementable). | `tests/test_event_derived_lifecycle.py:250-254` asserts on finding detail; AGENTS.md scope-declaration rule; `ipd_lifecycle.py:2496-2499` | yes |
| D-4 | E-03's candidate list: add the already-shipped ownership option as a fifth candidate, or leave the plan's four? | Added as candidate (e), WITH its measured effect and its documented heuristic cost, and still explicitly unselected. | Leaving four (would hide from the maintainer that the leading answer is already implementable and measurable); selecting it (would decide OQ-01, which is reserved). | `h9cn0y` Status `executed`; `_execution_cohesive_committed_paths` called over the three live receipts; its own docstring's two-sided-error admission | yes |

## Commit result

- Pre-review snapshot: skipped, target plan committed and unchanged at `e179bd0b`
- Hardened result: see the `plan-review: harden rcptstale-01 wmnmei (revisions applied)` commit
- Push: not performed
