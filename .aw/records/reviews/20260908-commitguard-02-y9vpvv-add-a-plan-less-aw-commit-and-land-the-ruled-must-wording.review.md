# Review: add a plan-less aw commit and land the ruled MUST wording, child y9vpvv (Set commitguard)

- Subject-Id: y9vpvv
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `26644c26`. `aw ipd lint --phase author` CONFORMING before semantic review; at
`--phase review-finalize` after revision it reports exactly one `IPD-Q501`, which is the newly raised
blocking OQ-03 firing as designed and is not a structural defect.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value therefore
rests on RUNNING the plan's mechanisms rather than re-reading its prose. Six things were executed
rather than inspected, and four of them produced findings: `merge_aw_block` was CALLED with a sentinel
appended to the pointer section (which produced the BLOCKER); five `--no-plan` argv shapes were parsed
against a faithful copy of the verb's real parser declaration (which produced the second BLOCKER);
`aw commit --no-plan` was invoked end to end at HEAD to confirm the gap is live; the manifest hashes
were compared programmatically; `inspect.getsource(run_commit)` was probed against the existing
forbidden-string assertion; and the bare suite was measured.

READINESS IS `no-go` ON ONE NEWLY RAISED BLOCKING QUESTION, NOT ON THE PLAN'S QUALITY. Every finding
in this round is FIXED in place except PR-904, which is not a repairable plan defect: it is a conflict
between two maintainer-authored instructions in the same installed block, and resolving it by editing
either one would exceed the 2026-09-01 ruling's authority. It is therefore escalated as OQ-03 with
`Blocking: yes` and `Finding: PR-904`, which makes `aw ipd lint` refuse the plan at every checkpoint
until the maintainer answers. Had that one question not existed, the verdict would have been APPROVE
WITH REVISIONS APPLIED and the readiness `go-pending-approval`.

SCOPE: only this child was a candidate. Read as evidence: `work_cmd.py` (`_resolve_plan` `:133-145`,
`_split_remainder` `:77-124`, `run_commit` `:387-486`, the notice at `:454-458`, the `offer_commit`
call at `:470`), `cli.py` (`_add_commit_flags` `:726-746`, the `commit` subparser `:1559-1588`),
`git_commit_helper.py` (`offer_commit` `:381-580`, the `commit_isolated` delegation `:559`),
`engine.py` (the two target paragraphs `:1246-1256`, the execution contract `:1257-1262`,
`agents_managed_sections` `:1536-1560`, `merge_aw_block` `:1621-1690`, `_apply_section_consent`),
`AGENTS.md:58/:60/:63`, `.aw/system/managed-sections.json`, `tests/test_work_primitives.py:189-243`,
`tests/test_shared_checkout_contract.py:129-152`, `CONTRIBUTING.md:170-184`, backlog `wjl471`
(especially the ruled wording at `:132-138` and the ordering constraint at `:151-155`), and the Set
siblings `ao1rb7` / `kbqpkn` plus the dependency target `lqly9m`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-901 | BLOCKER | UNDER-SCOPE | A. Correctness; G. Executability (a step that silently does nothing) | called `merge_aw_block(AGENTS.md_text, sections_with_sentinel, manifest=live, file_key='AGENTS.md')` -> `action='refreshed'`, sentinel ABSENT, diff = 3 blank lines; manifest records `AGENTS.md#aw:pointer`=`a9deb5a1...`, live body hashes `8baff639...`, `matches_recorded` False; `engine.py:1710-1747` | **E-04's AND E-06's REGENERATION IS DISCARDED TODAY, AND THE FAILURE LOOKS LIKE SUCCESS.** `_apply_section_consent` preserves the ON-DISK section body instead of writing ours whenever the manifest holds a recorded hash for that section that disagrees with disk (the user-drift rule, which exists to protect a hand-edit). That rule FIRES for `aw:pointer`, the section holding BOTH paragraphs this plan edits. Measured with a sentinel: the writer returns `refreshed`, the sentinel never reaches the file, and the only diff is whitespace. An executor would read a near-empty diff as idempotency and mark E-04 performed having shipped nothing, then V-04's "confirm AGENTS.md was regenerated" would pass on a file that was not changed. This is a BLOCKER rather than HIGH because the plan's PRIMARY deliverable is the contract text, and this makes delivering it a silent no-op. Note the precedent plan `uyd3lw` E-02 verified `recorded_hash` returned NONE and explicitly told its executor the write WOULD land; that verification is now stale and inverted. | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added E-07 (reconcile the manifest before regenerating, with the measured hashes, the least-privileged fix options, and an explicit refusal to sidestep it with a full `aw install .`) and V-07 (demands the before-state hashes, a sentinel proof grepped out of the on-disk file, a diff line count, and a `git status` scope proof). E-04 now `Depends on: E-07`, carries the measurement, states its completion criterion is presence in the FILE not the generator, and cites `test_repo_agents_block_equals_generated` as the standing detector. F-11 added. The gate's hazard list now names it as hazard two. |
| PR-902 | BLOCKER | IN-SCOPE | A. Correctness; F. UX (silent data loss on a normal invocation) | parsed 5 argv shapes against the verb's real declaration shape: `--no-plan -m msg -- a.py b.py` -> `plan='a.py'`, `path_argv=['b.py']`; `y9vpvv --no-plan -- a.py` -> `no_plan=False` with the flag stranded in the remainder; `cli.py:1570-1588`; existing recovery at `work_cmd.py:398-414` and `:77-124` | **THE OBVIOUS IMPLEMENTATION OF `--no-plan` SILENTLY EATS THE FIRST PATH.** `p_commit` declares a REQUIRED `plan` positional followed by `path_argv` with `nargs=argparse.REMAINDER`. Relaxing `plan` to `nargs='?'` and adding a `store_true` (which is what "add `--no-plan`" means without further instruction) produces two wrong parses, both reproduced: the first path after `--` is consumed as the SELECTOR and only the remainder is committed, and a flag placed after a selector is swallowed by REMAINDER and IGNORED rather than refused. The first case is the dangerous one, because it commits a strict subset of the requested paths and reports success. The plan's E-01 says only "the selector requirement to relax is at `work_cmd.py:138`", which is where the refusal lives but not where the hazard is. | C:Medium; U:Low; S:Low; F:High; Overall:Medium | FIXED | E-01 gained the two measured wrong parses verbatim and the fix, which is the pattern the verb ALREADY uses: recover `--no-plan` in the same pre-`--` scan that already recovers `-m`/`--message`/`--no-commit`, and take the path list only from after the `--` marker. It also states the behavioral acceptance test (every path after `--` must reach `offer_commit`; none may be read as a selector). V-01 now requires the MULTI-PATH case with `git show --stat` proving both paths landed. F-12 added. |
| PR-903 | HIGH | IN-SCOPE | D. Anti-regression (a step that breaks an existing guard) | `tests/test_work_primitives.py:236-243` asserts `"git commit" not in inspect.getsource(work_cmd.run_commit)`; verified absent today; verified that appending a notice containing the string makes the assertion fail | **E-02's "print which protections were skipped" CAN BREAK AN EXISTING TEST, AND THAT TEST IS LOAD-BEARING.** `test_commit_delegates_to_shared_helper` reads the function's source and forbids the literal `git commit`, precisely to prove the verb never forks its own commit path. The natural wording for a plan-less notice (telling the agent what NOT to do instead) contains that string. Worse, the obvious repair when it fails is to weaken the assertion, which would remove the guard that keeps a future editor from reintroducing a raw commit inside the very function this plan is making mandatory. The file was also NOT declared in `Scope-Paths`, so an executor editing it would trip the finalize scope gate. | C:Low; U:Low; S:Medium; F:Medium; Overall:Low | FIXED | E-02 gained the citation, the measurement, an example of compliant wording, and an explicit prohibition on weakening or deleting the test. V-02 now requires the test pasted PASSING with the body unmodified. `tests/test_work_primitives.py` added to `Scope-Paths`, with the scope-check note that leaving it unmodified is handled by `--scope-ack` rather than being a defect. F-13 added. |
| PR-904 | HIGH | UNDER-SCOPE | B/G. Honest documentation (two contradictory MUSTs in one installed block) | ruled text at `wjl471:132-138`; the neighbouring paragraph `engine.py:1259` rendered `AGENTS.md:63`; the same instruction repeated at `oc_runipd.py:2607`, `:4963`, `agy_runipd.py:1794`, `:2506` | **THE RULED WORDING CONTRADICTS THE PARAGRAPH THREE LINES BELOW IT, WHICH THIS PLAN DOES NOT MENTION.** E-04 installs "You MUST commit through `aw commit`, not raw `git commit`." The very next section, `### Agent execution contract`, opens "When you execute a task or plan here you MUST: commit ONLY files you changed, path-scoped (`git commit -m msg -- <path>`)". After E-04 the block installed into EVERY managed repo carries two MUSTs, one naming the tooled verb and one naming the raw command, adjacent. The same raw-command instruction is repeated at four driver-prompt sites, which is where a runner-executed agent actually reads its contract, so the runner path would keep instructing the opposite. This is not a plan defect I can repair: the ruling dictates a replacement for ONE paragraph and explicitly rules that the neighbouring `git reset`/`git stash` prohibition stays VERBATIM, so it plainly contemplated adjacent text and did not authorize changing this one. Rewording it, or the four prompts, on reviewer authority would be the same error as rewording the ruling. | C:Medium; U:Medium; S:Low; F:Medium; Overall:Medium-High | OPEN | ESCALATED, not fixed. New OQ-03 (`Blocking: yes`, `Owner: maintainer`, `Finding: PR-904`) states the conflict with citations, explains why the plan does not resolve it unilaterally, and offers three shapes a ruling could take without expressing a preference. The spec-sync section now carries the conflict as its unresolved sync problem. The four driver sites are named in the scope check as DELIBERATELY out of scope, entering scope only if OQ-03 resolves toward reconciliation. The gate states OQ-03 is a second, independent hold that approval alone does not clear. Readiness set `no-go` on this alone. |
| PR-905 | MEDIUM | IN-SCOPE | G. Executability (an unspecified fallback on the plan's own critical path) | the ruled paragraph is 5 sentences, the descriptive one is the 3rd (`wjl471:132-138`); `lqly9m`'s reviewed E-02 adds an `ISO_NOTHING` empty-diff outcome | **E-05's FALLBACK HAD NO MECHANICS, ON THE ONE STEP WHOSE WHOLE PURPOSE IS AN HONESTY GATE.** It says to "land the MUST imperative and hold the retry sentence" without saying what holding means. Deleting the sentence, softening it to "will retry", or footnoting it are materially different outcomes, and two of the three ship a hedged description of behavior that does not exist, which is exactly the untruth the item's hard ordering constraint was written to prevent. Separately, `lqly9m`'s own review added an emptiness probe under which the tooled path returns `nothing-to-commit` rather than committing when a hook's fix erases the whole diff, so an executor could observe that shape and mis-report it as the retry succeeding. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-05 now specifies: DELETE the third sentence entirely, ship the other four unchanged, never soften it, and record the verbatim shipped text. It also warns about the empty-diff shape and requires reporting it rather than presenting it as success. V-05 now demands the shipped paragraph verbatim plus an explicit statement of whether it has five sentences or four, so the outcome is readable without re-derivation. |
| PR-906 | MEDIUM | IN-SCOPE | D. Anti-regression (a wrong baseline used as a pass criterion) | bare `python3 -m pytest` at `26644c26` -> `2 failed, 5918 passed, 3 skipped, 2 xfailed`; failures are `test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` (environmental: 189 gitignored `opencode-recovery/*.md` files owned by another party) and `test_runner_backlog_close.py::ShutdownReportOnInterrupt::test_sigint_produces_the_report_and_exits_130` (30s subprocess timeout); `test_orchestrator_retirement` PASSES | The plan states the baseline as "1 failed, 5648 passed (the known `test_orchestrator_retirement` failure)" and uses it as a validation criterion. It is wrong in the pass count, the failure count, and the named failure. An executor inheriting a wrong expected-failure can excuse a real regression as the known one, and the actual failures are machine-specific (one caused by another party's gitignored dump) so they will not reproduce elsewhere. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | The required-tests section now carries the measured figures, both node ids, the cause of each, an explicit note that the earlier figure was wrong, and the instruction to judge on the delta. V-06 now requires naming any failure as pre-existing or new. F-16 added. |
| PR-907 | MEDIUM | UNDER-SCOPE | E. Testing (a validation command naming a file that does not exist) | `ls tests/test_work_commit.py` -> absent at `26644c26`; the existing harness is `tests/test_work_primitives.py:189-243` | `tests/test_work_commit.py` appears in `Scope-Paths` and in the required-tests list as though it were an existing file to run, but it does not exist, and NO E-item said to create it. So the plan's own focused-suite command would fail with a collection error, and the four behaviors E-01 through E-03 introduce had no test coverage assigned to any step. The plan also did not name the existing end-to-end harness for this verb, so an executor would likely build a second one. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added E-08 (create the file, cover the four argv shapes including the multi-path hazard, and follow the existing `test_work_primitives.py:189-243` pattern rather than inventing a harness) and V-08 (per-test output plus a FALSIFICATION run against the pre-change code, since a test that passes both before and after proves nothing). The required-tests entry now says the file is created by E-08. F-15 added. |
| PR-908 | LOW | IN-SCOPE | A. Correctness (an overstated inventory figure) | `grep -rn "offer_commit(" agent_workflows/*.py` -> 7 call sites, of which `work_cmd.py:470` is the plan-bearing one | F-2 and the Step 0 conventions both claim "8 call sites across the package" use `offer_commit` plan-lessly. There are 7 in-package call sites and only 6 are plan-less. The argument (the helper never needed a plan, so this is a CLI-surface gap) is unaffected and remains correct, but the figure appears as concrete evidence an executor may reproduce, and a wrong count invites a false negative. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | F-2 corrected with the recount and the note that the count was wrong because it included the plan-bearing site. The Step 0 convention now enumerates all six plan-less sites by `path:line` and names the seventh. |
| PR-909 | LOW | IN-SCOPE | A. Correctness (a cited precedent that is not the right one) | `cli.py:733-746` registers `--commit`/`--no-commit` via `add_mutually_exclusive_group`; `--no-color` is a bare `store_true` on the shared parent at `cli.py:806` | E-03 justifies refusing plan-plus-`--no-plan` by analogy to "the sibling `--color`/`--no-color` pair is mutually exclusive". There is no `--color` flag; `--no-color` is a lone boolean. The real in-repo precedent for a mutually exclusive pair on a commit-adjacent verb is `--commit`/`--no-commit`. Separately, argparse's own mutual exclusion CANNOT enforce this case, because one side is a positional that REMAINDER may have swallowed, so citing the group mechanism without that caveat would send an executor down a path that cannot work. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-03 now cites `_add_commit_flags` at `cli.py:733-746` as the precedent, and adds a paragraph stating that argparse cannot enforce it here and the refusal must live in the handler after E-01's recovery scan. V-03 expanded from two refusals to three, adding the flag-after-selector shape. |
| PR-910 | LOW | IN-SCOPE | G. Executability (a check left to the executor that the reviewer could resolve) | `CONTRIBUTING.md:179-184` points at the managed block as canonical (D69) and contains no PREFER wording; `GUIDING_PRINCIPLES.md:85` (P8) | The spec-sync section ends by telling the executor to check `CONTRIBUTING.md` for consistency and add it to the fence "if it says PREFER". That is a question the repository answers, so it should not consume an executor turn or risk an unnecessary scope widening. Checked: `CONTRIBUTING.md` does not restate the rule, it POINTS at the managed block as the canonical home per D69 and P8, and contains no PREFER wording about the commit path. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Resolved in the plan rather than left open: the spec-sync section now records the finding with the citation, states the file needs no edit and correctly stays out of `Scope-Paths`, and adds that if it DOES turn out to need a change that is a scope finding to report rather than a silent widening. Recorded as decision D-1. |
| PR-911 | LOW | IN-SCOPE | C. Operability (an unstated dependency state) | `lqly9m` front matter: `- Status: reviewed`, `- Readiness: go-pending-approval`; `edge_satisfied` `oc_runipd.py:3345-3380` | The plan's gate presents `Item-Dependencies: executed:lqly9m` as satisfiable ("is now satisfiable") without stating that it is currently UNSATISFIED. `lqly9m` is `reviewed` and awaiting human approval, so under a runner this item is marked `dependency-blocked` and skipped. That is designed behavior rather than a defect, but an operator planning a `commitguard` run should know the Set cannot finish in one pass, and the sibling orchestrator `ao1rb7` already documents exactly that for its own E-02 while this child does not. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | The gate now states the measured status of `lqly9m` and that a runner will correctly mark this item `dependency-blocked` and continue. F-17 added. |
| PR-912 | LOW | UNDER-SCOPE | G. Executability (an unnamed possible out-of-scope write) | E-07's fix may write `.aw/system/managed-sections.json`; `aw install . --dry-run` declares a Target Delta of `.aw/system/`, `.aw/config/project.json`, `.aw/state/durable` | The new E-07 may need to write the manifest, which is outside `Scope-Paths`, and the tempting shortcut (a full `aw install .`) writes three shared paths while other agents hold lanes in this checkout. Neither hazard was stated anywhere. Pre-declaring the manifest in `Scope-Paths` would be wrong too, since whether it is written depends on which fix E-07 demonstrates. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-07 forbids the full-install shortcut with the measured Target Delta and the shared-checkout reason, and instructs STOP-and-report if the write cannot land without leaving `Scope-Paths` (a genuinely unsafe condition, not a scope question). The scope check names the manifest as a possible out-of-scope edit to justify at finalize with `--scope-reason` rather than pre-declaring. V-07 requires the `git status` scope proof. |

CONFIRMED SOUND AND LEFT ALONE. The plan's central diagnosis is correct and every load-bearing claim
about the CURRENT state reproduces. The plan-less gap is live: `aw commit` refuses without a selector
(`work_cmd.py:138`) and `aw commit --no-plan -m x -- f.py` exits 2 today with "unrecognized arguments:
--no-plan", so there is no plan-less tooled path at all. The two-plan-derived-protections analysis is
exactly right: `run_commit` uses the plan only for the `Scope-Paths` refusal (`:439-453`) and the
engine validation (`:461-466`), and nothing else. The graceful-degradation precedent is real and is
the correct shape to reuse (`:454-458`). `offer_commit` genuinely takes no plan argument and its
snapshot-then-intersect really is the structural protection described (`git_commit_helper.py:490-500`,
`:541-543`). The PREFER paragraph still carries "immune to this by construction" (`engine.py:1252`).
The ruled wording is quoted verbatim in the item and the plan reproduces it faithfully. The decision
to implement a FLAG rather than a sibling verb is well argued and I would have made the same call;
OQ-01 records the counter-argument honestly enough that a maintainer can overrule it cheaply. The
imperative-versus-descriptive distinction driving the E-04/E-05 split is the ruling's own and is
applied correctly. And the plan is right that `lqly9m` moved the retry's layer, so E-05's insistence
on verifying by BEHAVIOR through the tooled path rather than by grepping one function is correct.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Does `CONTRIBUTING.md` describe the commit path in a way the new MUST contradicts, so does it enter `Scope-Paths`? | NO. It needs no edit and correctly stays out of the fence. Recorded in the plan so the executor does not re-derive it. | Leaving it as the executor task the plan wrote; adding the file to `Scope-Paths` defensively (rejected: an undeclared-but-unmodified path costs a `--scope-ack`, and declaring a file nobody needs to touch invites an unnecessary edit). | `CONTRIBUTING.md:179-184` states the contract "lives in the managed `AGENT-WORKFLOWS` block in `AGENTS.md`. That block is the canonical home; this file and the `.aw/records/plans` README point at it (D69)", and contains no PREFER wording. This is the P8 single-canonical-place pattern (`GUIDING_PRINCIPLES.md:85`). | yes |
| D-2 | Should `--no-plan` be implemented as a flag or a sibling verb, given PR-902 shows the flag has a real parsing hazard? | KEEP the flag, and fix the parse. The hazard is a recovery detail the verb already solves three times over, not an argument for a second verb. | A sibling verb (`aw commit-free` or similar), which sidesteps REMAINDER entirely because it needs no selector positional. Genuinely cleaner on this one axis, and a reviewer may prefer it. Rejected because it duplicates the whole argument surface, the writer lock, the isolated commit and the outcome reporting, creating two places for a future commit-path change to land. | The verb already recovers `-m`/`--message`/`--no-commit` from the pre-`--` segment (`work_cmd.py:398-414`) and `--dir` from the remainder (`:77-124`), so the mechanism exists and is tested; and the plan's own OQ-01 already recorded the trade-off for a maintainer to overrule. | yes |
| D-3 | Should the reviewer reconcile the contradiction PR-904 found by narrowing the execution-contract paragraph? | NO. Escalate as a blocking question instead. | Narrowing the neighbouring paragraph myself (rejected: exceeds the ruling); leaving the contradiction unmentioned (rejected: ships two conflicting MUSTs to every managed repo); deleting the ruled sentence that conflicts (rejected: reworded ruling is no longer the ruling). | The 2026-09-01 ruling dictates a replacement for ONE paragraph and explicitly rules the neighbouring `git reset`/`git stash` prohibition "stays VERBATIM" (`wjl471:144-149`), so it contemplated adjacent text and authorized no change to the execution-contract paragraph. The plan-review memory kernel also forbids inventing a decision that requires the human. ESCALATED, not merely recorded: raised in the plan as OQ-03 carrying `Blocking: yes` and `Finding: PR-904`, which the lint gate refuses at every checkpoint. | no |
| D-4 | Is the drift-preserve behavior (PR-901) a bug to fix in `engine.py`, or a state to reconcile in this plan? | Reconcile the STATE in E-07. Do not change the drift rule. | Changing `_apply_section_consent` so a mismatch writes ours anyway (rejected: that rule exists to protect a user's hand-edit and removing it would clobber adopters' customizations); adding `engine.py`'s consent logic to this plan's work (rejected: out of scope and far riskier than the plan's own subject). | The file currently EQUALS the generator (verified byte-for-byte), so the mismatch is a STALE RECORD rather than a real hand-edit, which makes re-recording the correct and least-privileged fix. The precedent plan `uyd3lw` E-02 already treated this as a per-run hazard to check rather than a defect to fix. | yes |
