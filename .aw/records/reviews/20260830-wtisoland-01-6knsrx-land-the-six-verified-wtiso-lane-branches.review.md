# Review: Land the six verified wtiso lane branches (stack conflict resolution and receipt re-issue)

- Plan-Id: 6knsrx
- Reviewed-At: 2026-08-30
- Reviewer: opencode/its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS

## Round 1

The plan's factual basis was established at HEAD `2b10ae7`. Main has since advanced 136 commits to
`144f3347`, which falsified or materially corrected six of its twelve findings, including both
findings that size the work. The plan is structurally sound and unusually well evidenced for its
original basis commit; the problem is staleness, not craft.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | blocker | IN-SCOPE | G. Plan executability / A. Correctness | plan:114 (`verified in this repo at HEAD 2b10ae7`); `git rev-list --count 2b10ae7..HEAD` -> 136 | The whole Findings table, and therefore the execution checklist derived from it, rests on a HEAD that is 136 commits stale. Two load-bearing rows are outright falsified: F-7 undercounts the conflict surface by ~8x (3 code hunks -> 26, 4 paths -> 6) and F-10 directs a merge of `1o4eif` that already landed as `b08de37d`. An executor following the plan literally would resolve 3 hunks, hit 23 more it was never told about, and attempt a redundant merge. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added a BASIS-COMMIT WARNING at the head of Findings; re-verified all 12 rows at `144f3347` and annotated each HOLDS/CORRECTED/FALSIFIED with reproducible commands; added F-13..F-15. Added E-01 as a mandatory re-measure-before-acting gate with a STOP condition. |
| PR-002 | blocker | IN-SCOPE | G. Plan executability / right-sizing | `git merge-tree --write-tree --name-only main aw/lane/2c122z` -> 6 conflicted paths, tree `80a9557`; per-path marker counts 9/9/5/1/1/1; `git diff --stat main...aw/lane/2c122z` -> 55 files, +16902/-837 | The plan is scoped as a mechanical 3-hunk resolution but the real work is 26 semantic hunks spanning lock primitives (`run_lock`), process reaping (`terminate_process`), worktree allocation, lifecycle role enforcement, and disposition reconciliation. OQ-02 (one merge vs per-phase replay) was classified `Blocking: no` on the strength of the undercount, so the plan would have proceeded to a single unauditable 55-file integration commit without a maintainer ruling. | C:Medium-High; U:Low; S:Medium; F:Medium-High; Overall:Medium-High | fixed | Split the driver work into E-02 (turn loop), E-03 (8 remaining oc hunks), E-04 (9 agy hunks), E-05 (4 out-of-scope paths), each demanding per-hunk rationale. Promoted OQ-02 to `Blocking: yes` with `- Finding: PR-002`. The one-vs-many integration decision is the maintainer's; deferred to them rather than chosen here. RESOLVED 2026-08-31 by maintainer ruling, recorded in the plan's OQ-02 (now `Blocking: no`, `Status: resolved`): land the stack PHASE BY PHASE in stack order with the full suite green before each next merge, which is what this finding's own analysis recommended. Re-measured read-only with `git merge-tree` at the ruling: phase 1 alone conflicts in 3 files / 9 hunks versus 6 files / 26 hunks for the whole stack, so per-phase genuinely REDUCES the work at each step rather than merely spreading it. Also corrected here: the stack is a STRICT LINEAR chain (each lane an ancestor of the next) of 26 UNIQUE commits; the per-lane counts 3/10/16/22/26 are cumulative, so the '~79 commits' figure double-counts and must not be repeated. Executor must re-measure before EACH merge, since main moves under a five-step sequence. |
| PR-003 | high | UNDER-SCOPE | D. Anti-regression / F. Prevent silent failure | main's second unconditional reset: `oc_runipd.py:3452-3454` (`def _subagent_progress(): watchdog.touch()`); the in-loop one at `:3532` | E-01 as written gates only the in-loop `watchdog.touch()`, but main gained a SECOND unconditional watchdog reset after the lanes were authored: the `stallfp kaga7s` poller touches from a background thread. Gating one path leaves the wedged-turn defect partially open while the plan's expected outcome claims it closed, i.e. it would land a partial fix reported as complete. Gating both risks re-breaking `kaga7s`, whose purpose is keeping healthy subagent turns alive. | C:Medium; U:Medium; S:Low; F:Medium-High; Overall:Medium-High | FIXED | RESOLVED 2026-08-31 by maintainer ruling on measured evidence, recorded in the plan's OQ-03 (now `Blocking: no`, `Status: resolved`): gate ONLY the in-loop `watchdog.touch()`, leave the poller UNCHANGED. This review's framing of the poller as resetting on "best-effort log evidence" understated it: `stall_progress.classify_progress` already counts only `PROGRESS_MESSAGE_KINDS` agent-loop lines and explicitly discards housekeeping so a "permission-deadlocked (but still chatty) process is NOT mistaken for a progressing one", and the poller additionally counts only lines two-hop-attributed to a proven child of THIS turn. So the second reset path is NOT unconditional, and the wedged-turn property this finding feared was missing is already enforced there. Applying `is_meaningful_event` to it would be a category error (that predicate JSON-parses each line and returns False on anything unparseable; the poller reads plain-text log lines), which would reject nearly every poller line and re-break the `kaga7s` keepalive this finding warned about. The dilemma was therefore false: the two mechanisms implement one policy against two data sources. Residual risk recorded in OQ-03 rather than hidden: a stuck turn whose only output is agent-loop-shaped lines from a live child can still be held open, which is arguably correct by the poller's definition and would need its own plan if it ever proves real. |
| PR-004 | high | UNDER-SCOPE | G. Plan executability / scope fence | plan `- Scope-Paths:` line 7 vs the 6 conflicted paths in PR-002's evidence | Four paths that MUST be edited to resolve the merge (`worktree_lease.py`, `cli.py`, `ipd_lifecycle.py`, `tests/test_wtiso_adversarial.py`) are absent from `Scope-Paths`, while the plan's own gate forbids editing outside it. The plan is self-contradictory: it cannot complete without breaching its own fence, and the likely execution-time outcome is silent broadening, which is exactly what the scope fence exists to prevent. | C:Low; U:Low; S:Medium; F:Low; Overall:Medium | fixed | Added F-13; rewrote Scope check to name the gap as UNRESOLVED; added E-05 gated on the answer; raised OQ-04 with `Blocking: yes` and `- Finding: PR-004`. Widening a fence to `ipd_lifecycle.py` is a risk decision for the maintainer, not bookkeeping. RESOLVED 2026-08-31 by maintainer ruling, recorded in the plan's OQ-04 (now `Blocking: no`, `Status: resolved`): option (a), WIDEN `Scope-Paths` to EXACTLY the four measured paths and no more. The plan's `Scope-Paths` now declares `worktree_lease.py`, `cli.py`, `ipd_lifecycle.py` and `tests/test_wtiso_adversarial.py`, so the self-contradiction this finding identified is gone. The fence was widened by ENUMERATION against a measurement, not converted into a general licence; anything beyond those four remains a STOP-and-report. Option (b) (split into a child IPD) was rejected on evidence: a git merge is ATOMIC PER FILE and these conflicts belong to the same single merge as the in-scope ones, so two plans would have to coordinate inside one uncommittable intermediate state. Risk accepted knowingly and measured: the two sensitive files carry the SMALLEST conflicts of the six (`cli.py` 1 hunk, `ipd_lifecycle.py` 1 hunk, versus 9 each in the runners), the work is conflict resolution rather than new lifecycle design, and per-hunk justification plus a green suite is still required. |
| PR-005 | high | IN-SCOPE | E. Testing and verification | measured bare run at `144f3347`: `3654 passed, 3 skipped, 4 xfailed in 48.30s`, exit 0, vs plan:163 `2874 passed` | The stated validation baseline is 780 tests stale, and the two "known live-state deselections" it tells the executor to expect did not occur. A stale baseline defeats the plan's own delta-must-be-explained rule: an executor reconciling against 2874 would see ~780 unexplained extra passes and could rationalize a real regression as noise. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Replaced with the measured baseline as a pasted code block plus exit code (F-14), and added an instruction to re-measure immediately before merging since main is under concurrent development. |
| PR-006 | medium | IN-SCOPE | A. Correctness (arithmetic) | per-lane counts 3/10/16/22/26 are cumulative along the F-2 chain; `git rev-list --count main..aw/lane/2c122z` -> 26 | The "~79 commits" figure in the Goal and E-07 double-counts: it sums cumulative counts along a linear stack, so the same commits are counted up to five times. It also still says "six" unmerged lanes after `1o4eif` landed. Inflated stakes in a plan whose gate turns on how much verified work is at risk. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Corrected to 26 unique commits in the Goal (with the double-count explained), in F-1, and in E-11; corrected six lanes to five unmerged. |
| PR-007 | medium | IN-SCOPE | A. Correctness / lifecycle | recomputed digests: qcqhj7/rchpms/58ha43/2c122z `match`, `1o4eif` `STALE`; `git ls-tree aw/lane/7p9n2v .aw/records/plans/executed/` lists the 7p9n2v plan | E-05 told the executor to finalize "each of the six plans", but `7p9n2v` is already `executed` on its lane and arrives that way through the merge, so finalizing it again is wrong; and `1o4eif`'s receipt is genuinely stale (not spuriously, as F-3 implied) because `e5b0f939` edited its plan post-`begin`. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Corrected F-3 per-lane; rewrote as E-09 naming the FIVE plans to finalize and requiring `7p9n2v` be reconciled rather than re-finalized, with V-09 demanding that be stated explicitly. |
| PR-008 | medium | UNDER-SCOPE | E. Testing and verification | original V-05 bundled `executed/` counts, receipt listing, AND the full-suite reconciliation into one item | The full-suite validation was a clause inside a lifecycle-bookkeeping V-item, and it sat AFTER the finalize step in sequence, so the plan permitted marking plans executed before proving the merged tree green. Ordering defect, not just a packaging one. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Extracted the suite run into its own E-08/V-08 placed BEFORE any lifecycle transition, requiring the pasted summary line, exit code, and a change-by-change reconciliation, with unexplained failure forced to `Result: pending`. |
| PR-009 | medium | IN-SCOPE | E. Testing / anti-greenwash | original V-01/V-02 accepted `grep -c '^<<<<<<<'` -> 0 as sufficient | A zero marker count cannot distinguish a reasoned combine-both resolution from `git checkout --ours` discarding a whole side. Since PR-002 shows 26 hunks each carrying an independent semantic decision, marker-count-only evidence is a greenwash hole precisely where the risk concentrates. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | V-02/V-03/V-04 now additionally require the resolved source showing all four mechanisms coexisting, a per-hunk table naming the side chosen and why, and a negative grep proving no stray `heartbeat = Heartbeat(` was reintroduced. |
| PR-010 | low | IN-SCOPE | C. Architecture and operability | `oc_runipd.py:57` imports `Heartbeat` from `render_stream`; `grep -c 'class Heartbeat'` -> 0 in both main and lane `oc_runipd.py`; `agy_runipd.py:44` re-exports it, `:2697` builds `Statusline` | E-02's citations were drifted or wrong: it claimed main "deleted `Heartbeat` from `oc_runipd.py`" (neither side ever defined it locally; both import it), cited `Statusline` at `oc_runipd.py:46` (actually `:59`), and cited `agy_runipd.py:244` defining `Heartbeat` and `:1957` building `Statusline` (actually `:44` re-export and `:2697`). | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Corrected every citation in E-02/E-04 and F-8/F-9, and reframed the `Heartbeat` instruction as "do not restore the lane's `heartbeat = Heartbeat(...)` line" rather than a false claim about deletion. |
| PR-011 | low | IN-SCOPE | E. Testing / method | this review used `git merge-tree --write-tree` (pure tree op, working tree untouched, confirmed by `git status --porcelain`) | The plan's F-7/F-10 method was a throwaway detached-worktree merge, while its own F-11 says a scratch worktree is an unreliable venue. `git merge-tree` gets the same conflict inventory without touching main, any worktree, or the index, which also removes the abort-cleanup risk in a shared checkout. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-01/V-01 now specify `git merge-tree` and require a `git status --porcelain` paste proving the tree was not modified; F-7/F-11 updated to record the method. |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Should the review re-verify all 12 findings at current HEAD, or review the plan as written against its stated basis commit? | Re-verify every row at `144f3347` and annotate HOLDS/CORRECTED/FALSIFIED | Review as-written and note staleness generically, which would have left the 3-hunk-vs-26-hunk and already-landed-`1o4eif` errors undiscovered | plan-review Step 1 items 4-5 require verifying material claims with evidence and recording stale evidence; `git rev-list --count 2b10ae7..HEAD` -> 136 made staleness the dominant risk | yes |
| D-2 | Is a single 55-file / 26-hunk integration commit acceptable, or must the maintainer rule? | Escalate to the maintainer via OQ-02 promoted to blocking; do not choose | Choosing "one merge" per the plan's own recommendation; choosing per-phase replay on reviewer authority | The plan's recommendation rested on a 3-hunk survey that F-7 falsified, so its basis is void; `909eb007` shows even a clean lane merge needed post-hoc fixes (F-15). Integration granularity for 16902 changed lines is a maintainer risk decision | yes |
| D-3 | Should the reviewer decide whether the `kaga7s` background poller must be gated by `is_meaningful_event`? | Escalate via new OQ-03; do not decide | Gate it (risks killing healthy subagent turns, re-breaking `kaga7s`); leave it ungated (lands a partial fix while claiming the defect closed) | Two shipped features with opposed requirements: `oc_runipd.py:3444-3450` documents the poller exists so subagent turns are NOT killed, while `wtiso-02` E-05(b) exists so wedged turns ARE killed. No repo artifact resolves the conflict | yes |
| D-4 | May the reviewer widen the plan's `Scope-Paths` to the four extra conflicting paths? | Escalate via new OQ-04; document the contradiction, do not widen | Widen it silently to make the plan executable; split the out-of-scope work into a child IPD on reviewer authority | The plan's gate forbids editing outside `Scope-Paths` and AGENTS.md requires STOP-and-report over silent broadening; `ipd_lifecycle.py` is core lifecycle code, so widening is a risk decision | yes |
| D-5 | Did the review's own merge inspection risk disturbing a shared checkout with active concurrent work? | Use `git merge-tree --write-tree` only; never a real merge or scratch worktree | A detached-worktree trial merge as the plan's F-7 did | `git status --porcelain` before and after showed the same 5 pre-existing entries from other parties, none authored by this review; AGENTS.md forbids disturbing co-workers' uncommitted work | yes |
| D-6 | `Size assessment` was set to `large` to convey conceptual density; the linter rejected it. | Revert to `standard` (11 leaves is under the 18 threshold) and carry the density warning in the `Cohesion rationale` prose instead | Keep `exception`, which the spec reserves for exceeding a threshold and would misreport the structural facts | `IPD-Z601` allows only `standard`/`exception`; the ipd-spec ties `exception` to exceeding >18 leaves / >5 groups, which this plan does not | yes |
