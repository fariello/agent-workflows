# Review: resolve every pre-existing cross-type setid collision before hard enforcement can be enabled, child drzbs9 (Set setidhard)

- Subject-Id: drzbs9
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: no-go

## Round 1

Reviewed at HEAD `7a999386`. `aw ipd lint --phase author` CONFORMING before semantic review. After
revision, `--phase review-finalize` reports exit 1 with exactly two `IPD-Q501`, both being the two NEW
`Blocking: yes` questions this round added; that is the gate working as designed, not a structural
defect, and it is the identical disposition sibling `yku4ga` carries.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value therefore rests
on EXECUTING the plan's claims rather than re-reading them. NINE things were run rather than inspected:
`check_collisions` was called on BOTH retired branches and each population decomposed by finding SHAPE;
the `From-Backlog` edge set was gathered across every plan and spec and matched to each flagged item's
`- Id:`; `aw check all --agent` and `aw doctor` were both run and their per-rule counts compared;
`aw group backlog` was reproduced FOUR ways across two throwaway repositories (bare, with `--order`,
single-match, multi-match) against both a synthetic item carrying an Order field and a realistic one
without; `grep` measured how many backlog items carry `- Order:` at all; the `agentadhere` carrier set
was counted by type; the three plans the plan names as live had their `- Status:` read; the full bare
suite was run; and the plan's named expected-failure file was run in isolation. SEVEN of the nine
contradicted the plan.

SCOPE: only this child was a candidate. Read as evidence: `check_engine.check_collisions`
(`:832-919`, the `include_retired` default at `:835`, the `seen_sets`/`prev_type` comparison at
`:893-916`, the caller at `:1760`), `doctor.py:527-531`, `artifact_rename.py` (`compute_target_name:91`,
`:142`, `:153`, `run_group_generic`'s `order_val` at `:682` and the metadata write at `:741`),
`cli.py:1345`/`:1486` and `ipd_lifecycle.py:1724-1737` for the finalize scope gate, spec `4w7d6s`
Section 5, backlog `s9p5x5` and `sjsoqq`, and siblings `yku4ga`/`bwgyum`/`dw7i3m`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-501 | BLOCKER | IN-SCOPE | A. Correctness; G. Executability (a mandated action that causes the harm it cites) | `grep -rln "^- Order:" .aw/records/backlog/` -> 0 of 174; reproduced in two throwaway repos; `artifact_rename.py:142`/`:153` preserve `NN` when `new_order is None`; `plans_refs.py:432`/`:437` per `s9p5x5` | **THE PLAN MANDATES `--order` ON A FIELD BACKLOG ITEMS DO NOT HAVE, AND OBEYING THE MANDATE IS WHAT CORRUPTS THE FILENAME.** E-02 said to pass `--order` explicitly and V-02 required proving "the Order was not reset". Measured: ZERO of 174 backlog items carry a `- Order:` line, so there is nothing to preserve and nothing for the verb to reset. Reproduced on a realistic sourceless-Order item: a BARE `aw group backlog <id6> --set X --rename --apply` renames the file, rewrites `- Set:`, injects NO Order, and preserves the filename `NN` slot; whereas passing `--order 1` on an item whose slot was `02` MOVED the slot to `01`, changing the name for no reason. So the plan's protective instruction is the only thing in it that damages a filename. The hazard it cited is real but belongs to a DIFFERENT verb: `aw group plans` substitutes zero when the flag is absent (`plans_refs.py:432`/`:437`), and this child touches no plan. BLOCKER because it is a mandated action, applied 39 times, that silently rewrites the identity slot of records other artifacts cite. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-02 now says DO NOT pass `--order`, with the measurement, the reproduction, and the correct attribution to `aw group plans`. F-4 raised HIGH and rewritten. V-02 now demands the filename `NN` slot be proven unchanged and NO injected Order, inverting what it previously asked for. The Concern and Step 0 carry the correction, and the deferral entry now records that `s9p5x5` already tracks the real defect so no fresh report is owed. |
| PR-502 | HIGH | IN-SCOPE | A. Correctness (a false premise the whole method rests on) | `check_collisions(..., include_retired=True)` shapes parsed: 78 `backlog`/`plans`, 2 `plans`/`research`, 1 `plans`/`walkthroughs`, and 5 SAME-TYPE `descriptive` (`release-review` x2, `leak-sanitizer`, `assess-documentation`, `assess-bugs`), all five between two `executed/` plans | **"EVERY COLLISION IS A BENIGN SOURCE-VERSUS-ITS-OWN-PLAN PAIR" IS TRUE OF 40 AND FALSE OF 86, SO THE SCOPE SENTENCE "RESOLVE EVERY PRE-EXISTING CROSS-TYPE SETID COLLISION" IS UNACHIEVABLE AS WRITTEN.** Eight findings have no source to regroup. Five are not even cross-type: they are `descriptive` mismatches between two plans in `executed/`, whose bodies are immutable by policy, so neither side can be touched and there is no backlog item in the pair. Two more are `plans`/`research` and one `plans`/`walkthroughs`, where a renameable file exists but its setid deliberately mirrors the plan it documents. The plan asserted a uniform population and wrote a goal that assumes one, so an executor reaching the residue has no instruction except to fail. | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Scope, Goal, E-01 and E-04 now partition the population into SWEEPABLE and NOT-SWEEPABLE with the residue named and enumerated; E-04's criterion became "target measure zero over the sweepable set with the residual equal to the partition" rather than zero absolutely. F-2 added HIGH. A new `Blocking: yes` OQ-05 puts the residue's disposition to the maintainer, since every answer either amends a spec, carves out the hard posture, or authorizes editing executed plans. The spec-sync section records the amendment that may be owed and attributes it to Order 00. |
| PR-503 | HIGH | IN-SCOPE | B. Security of a gate (an unattested measure choice that changes the deliverable) | `check_collisions(Path('.'), include_retired=False)` -> 40 findings / 30 setids; `=True` -> 86 / 65; `aw check all` 40 versus `aw doctor` 81; `check_engine.py:835` default False, `:1760` pass-through, `doctor.py:530` hardcoded True | **WHICH NUMBER THIS CHILD MUST DRIVE TO ZERO IS A DECISION THE PLAN NEVER MAKES, AND THE TWO ANSWERS ARE DIFFERENT PIECES OF WORK.** The plan says "`check.setid-collision` must be ZERO" as though one measure existed. Two do, differing by more than a factor of two, and the choice is not cosmetic: under the False measure the `agentadhere` collision that MOTIVATED the entire item has ZERO findings, because both its plans sit in `executed/` which the filter drops, so E-03 would clear nothing measurable and V-03's "paste the finding gone" would be unfalsifiable. Under the True measure the population doubles and provably includes PR-502's unsweepable residue. An agent picking either would be choosing the item's size and its acceptance criterion on its own authority. | C:Low; U:Low; S:Medium; F:High; Overall:Medium-High | FIXED | The plan's HANDLING of the ambiguity is fixed; the DECISION itself is escalated as blocking OQ-04. F-1 added HIGH with both branch counts and both call sites. E-01 now requires measuring BOTH branches, pasting both counts, and QUOTING OQ-04's recorded answer before any regroup; V-01 FAILS if OQ-04 is still open. A new `Blocking: yes` OQ-04 puts the choice to the maintainer, so the lint refuses the plan until it is answered. Not resolved from evidence: the repository cannot say which identity scope the maintainer intends, and it is the same judgement Order 00's OQ-02 already reserves. |
| PR-504 | HIGH | IN-SCOPE | E. Testing (a validation criterion that cannot be satisfied) | `aw check all` -> 40 setid-collision findings; `aw doctor` -> 81; `doctor.py:530` hardcodes `include_retired=True`, `check_engine.py:1760` passes a False-defaulted flag | **THE PLAN REQUIRES THE TWO CHECK SURFACES TO AGREE, WHICH THEY CANNOT, SO V-04 AS WRITTEN IS UNPASSABLE.** It says to "assert `aw doctor` agrees" and V-04 demanded confirming its set "equals `aw check all`'s". Measured 40 versus 81. The divergence is structural, not drift: one caller hardcodes the retired-inclusive flag and the other defaults it off. An honest executor would be stuck; a careless one would write the sentence anyway, which is exactly the false attestation the repo's contract forbids. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-04 and the required-tests list now forbid demanding equality and require instead that BOTH counts FALL by the number of sources regrouped, with the gap EXPLAINED by the flag asymmetry and both call sites cited. V-04 now states that asserting equality is a FAILED validation. F-9 added. |
| PR-505 | HIGH | IN-SCOPE | A. Correctness (a guard described as hypothetical that fires on the real tree) | `From-Backlog` edges gathered across all plans+specs (146 carriers, 102 distinct targets) matched to each flagged item's `- Id:`: 39 of 40 linked; `.aw/records/backlog/blocked/20260908-idxuntrack-01-adgtqb-...backlog.md` unlinked, partner `.aw/records/plans/pending/20260906-idxuntrack-02-yvvf98-...ipd.md` | **THE NO-GRADUATION-LINK CASE IS NOT A GUARD, IT IS A LIVE CASE THAT FIRES ON THE FIRST RUN.** OQ-03 concluded "all 29 collisions measured at authoring were joined pairs, so this is a guard rather than an expected case", and E-01 treated a non-benign shape as hypothetical. Measured: 39 of the 40 flagged items ARE the target of a resolvable back-link and exactly one is not, so the sweep is 39 of 40 and one item must be reported rather than swept on the very first execution. An executor primed to treat the case as improbable is the one most likely to sweep it anyway and destroy the only record that two artifacts are related. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | F-3 added HIGH with the method and the named pair. E-01 now says to EXPECT the case with the measured example, E-05 requires naming every such pair with BOTH paths and confirming none was regrouped, V-05 expects at least that pair, and OQ-03's rationale is corrected from "guard" to a live case with its subject named. |
| PR-506 | MEDIUM | IN-SCOPE | D. Anti-regression (three wrong baselines, one naming the wrong test) | `python3 -m pytest` -> `1 failed, 5919 passed, 3 skipped, 2 xfailed`; `python3 -m pytest tests/test_orchestrator_retirement.py -o addopts=""` -> `112 passed`; `aw check all --agent` -> 238 findings | **THE PLAN NAMES A SUITE BASELINE, A FINDING TOTAL, AND AN EXPECTED FAILURE, AND ALL THREE ARE WRONG, INCLUDING THE TEST FILE IT TELLS THE EXECUTOR TO EXCUSE.** It states `1 failed, 5648 passed` (actual `5919 passed`), `aw check all` at 98 findings (actual 238), and identifies the known failure as `tests/test_orchestrator_retirement.py`, reasoning that it "asserts against SIBLING PLANS' live status and is therefore exactly the kind of test a mass regroup could perturb". That file PASSES outright. The real failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, and it is ENVIRONMENTAL, tripping on a gitignored local `opencode-recovery/` dump, so it is machine-specific. An executor handed a wrong expected-failure can excuse a genuine regression as the known one, and here the named file is precisely the one a mass regroup would plausibly break. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-04, the required-tests list and Step 0 all carry the re-measured figures, the real failing node id, its environmental cause, and the instruction to measure the executor's OWN before-baseline and compare NODE IDS rather than counts. V-04 explicitly forbids citing `test_orchestrator_retirement` as a known failure. F-8 added. |
| PR-507 | MEDIUM | IN-SCOPE | A. Correctness (a sound argument resting on three false facts) | `- Status:` read at review: `scopeattr`/`h9cn0y` `executed`, `depreview`/`03ie04` `executed`, `integearn`/`32ij2j` `superseded`; all 40 flagged sides in `backlog/` | **THE "DO NOT RENAME A LIVE PLAN SET" ARGUMENT IS RIGHT AND ALL THREE OF ITS NAMED EXAMPLES ARE WRONG.** The Concern, E-02, Step 0 and OQ-01 each assert that `scopeattr`, `depreview` and `integearn` belong to plans "approved or executing RIGHT NOW under other agents". Measured, they are `executed`, `executed` and `superseded`; none is live. The conclusion survives on a far stronger measured basis the plan never states: ALL 40 findings have their flagged side in `backlog/` (40 of 40), so renaming a plan is not merely risky but UNNECESSARY. Leaving the false version in place invites a reviewer who checks one status to discard the whole argument. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | The Concern, E-02, Step 0 and OQ-01 now carry the corrected statuses, the 40-of-40 measurement as the primary argument, and a note that live Sets do exist under OTHER setids (`durablecapture` three `to-review`, `runverdict` four `reviewed`, `integearn` three `reviewed`) as a reason to re-measure rather than trust any list. F-6 added. |
| PR-508 | MEDIUM | OVER-SCOPE | G. Executability (a declared path that costs a mandatory acknowledgement) | `Scope-Paths` declared `.aw/records/specs` and `.aw/records/research`; `cli.py:1486-1492`, `ipd_lifecycle.py:1724-1737`; zero of 40 flagged sides is a spec | **TWO OF THE THREE DECLARED SCOPE PATHS ARE SPECULATIVE, AND A SPECULATIVE DECLARATION IS NOT FREE.** The plan declares `.aw/records/specs` and `.aw/records/research` "because the inventory MAY find one", and its own scope-check says they "may finish UNCHANGED, which is the expected outcome". But `aw ipd finalize` refuses to complete without a `--scope-ack` per declared-but-unmodified path, so each speculative declaration converts into a mandatory acknowledgement the executor must produce for work that never happened. Measured: zero of the 40 flagged sides is a spec, and the two `plans`/`research` findings appear only in the retired-inclusive branch and land in PR-502's NOT-sweepable partition, so neither path is reachable by a regroup. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | `Scope-Paths` reduced to `.aw/records/backlog`. The scope-check now explains why a declared-but-unmodified path costs an ack, cites the measurement, and says that a genuine future spec source is a scope change to RAISE before acting rather than a silent edit under a pre-declared path. |
| PR-509 | MEDIUM | IN-SCOPE | A. Correctness (an inventory count off by one, used as reproduction evidence) | `grep -rln "^- Set: agentadhere" .aw/records/` -> 8 files: 7 in `plans/executed/` plus `backlog/done/...3gr7fk...` | The `agentadhere` case is cited three times as "EIGHT executed plans"; it is SEVEN executed plans plus the backlog item itself, the item being the cross-type half that makes it a collision at all. The argument is unaffected, but the number appears in E-03 and V-03 as concrete evidence the executor is told to reproduce, so a wrong count invites a false negative when they measure seven. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Corrected to seven in the Concern, E-03, F-7 and V-03, with the 8-files-equals-7-plans-plus-the-item decomposition stated so the discrepancy cannot recur. V-03 now asks the executor to state the count they measured rather than repeating the plan's. |
| PR-510 | MEDIUM | UNDER-SCOPE | G. Executability (an unmet dependency stated only as sequencing) | `grep -rn "Graduated-To" agent_workflows/` returns nothing; `bwgyum` `- Status: reviewed` in `pending/`; `dw7i3m` `- Status: reviewed` in `pending/` | **THE PLAN CONSUMES A FIELD THAT DOES NOT EXIST IN THE CODEBASE AND TREATS THAT AS AN ORDERING NOTE.** E-03 and E-05 require a `Graduated-To` link and V-05 requires a `check.graduated-to-dangling` rule, all of which Order 02 builds. `Graduated-To` appears nowhere in `agent_workflows/`, and neither `bwgyum` nor `dw7i3m` has executed. The plan's deferred section mentions Order 02 "builds them", but the gate frames the dependency purely as sequencing, so an executor could reasonably attempt E-03. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | The gate gained a paragraph recording that the dependency is GENUINELY UNMET with the grep result and both siblings' statuses, plus a paragraph stating that unlike Order 03 this child has NO half that is safe under any ruling, so it must not be started in part. V-05 now FAILS rather than waives if the dangling rule does not exist in the tree. |
| PR-511 | LOW | IN-SCOPE | E. Testing (an unparseable measurement instruction) | `aw check all --agent` emits ONE `aw.agent/v1` object with a 238-entry `diagnostics` array; `--format json` raised `JSONDecodeError` | The plan requires "`aw check all --agent` PER-RULE counts" without saying where they come from. The `--agent` surface emits a single JSON object, not tab-separated records, so a line-oriented count yields zero rules and `--format json` emits nothing parseable at all. An executor following the instruction literally gets no per-rule data and may fall back to the total the plan correctly forbids comparing. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-04, the required-tests list and Step 0 now name the `diagnostics` array as the source, record that `--format json` fails on this surface, and paste the full 238-finding per-rule baseline so the executor has a concrete comparison. F-12 added. V-04 requires the counts be derived from that array. |
| PR-512 | LOW | IN-SCOPE | G. Executability (a selector that can act on more than intended) | `aw group --help` on `--force`; `artifact_rename.py:682` `order_val = start_order + i`; reproduced: `aw group backlog multiset --set fresh --rename --apply` matched and moved one file, while a `--order` multi-match renumbers sequentially | The plan's command form is `aw group backlog <file> ...`, a filename selector, and the verb's own help warns a substring selector can be ambiguous with `--force` acting on ALL matches. Combined with the sequential renumbering at `:682`, a set-wide or status-wide selector plus `--order` would renumber an entire match. Low because the plan does not instruct a bulk selector, but it does not forbid one either, across 39 repetitions. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-02 now requires selecting by id6, ONE call per item, never a set-wide or status-wide selector, citing the renumbering mechanism at `:682` and the reproduction. Step 0 records both facts. V-02 requires pasting each command so the selector form is auditable. |

CONFIRMED SOUND, AND THIS PLAN'S SEQUENCING JUDGEMENT AND SPEC READING ARE ITS STRONGEST PARTS. The
counter-intuitive dependency is correct and well argued: `- Item-Dependencies: executed:dw7i3m` on an
Order-01 child looks wrong and is right, because every collision was created by a graduation reusing a
source setid, so a sweep before the mint decays immediately. That is not reasoned about but DEMONSTRATED
twice on the tree itself, once when scaffolding this Set into `setiduniq` collided with `sjsoqq`, and
independently in `s9p5x5`'s own history, which records scaffolding into `grouporder` colliding with the
item it was filed against. The spec citation is accurate: Section 5 really does say "A one-time sweep
MUST identify and resolve any other existing cross-type setid collisions before I2 is turned on
fail-closed" and really does prescribe "re-group the closed backlog item to its own unique setid" for
`agentadhere`, so the direction-of-rename decision follows the spec rather than inventing a policy. The
refusal to rename a plan Set is correct on the measurement even though its stated evidence was wrong
(PR-507). `check_collisions`'s internals are as described, with the `seen_sets` map and the
`prev_type != record_type` comparison at `:893-916`, so E-01's "call the predicate, do not parse report
text" has a real target. The `From-Backlog`-points-by-id6 claim is right and the back-link does survive a
regroup by construction. OQ-02's naming answer is well judged, deliberately a convention rather than an
algorithm because a derived token can itself be taken. And the plan's own instruction to compare PER RULE
rather than by total is exactly right, which is what kept its stale 98 from becoming a false criterion.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing about the direction of the sweep, and three
things about whether it can be executed at all. FIRST, the plan's one protective instruction was
backwards: it mandated `--order` to guard against a reset that cannot happen to a backlog item, and
obeying it is the only way to damage a filename. That inversion is PR-501 and it would have been applied
39 times. SECOND, the goal was unachievable, because eight findings have no source to regroup and five of
those are between immutable executed plans, so "resolve every collision" had no completion state; the
plan now partitions the population and hands the residue to a human. THIRD, the plan assumed one
measurement where two exist, and the choice between them decides whether the item's own motivating case
is even visible; that is now a blocking question rather than a silent assumption. The rest is measurement
hygiene, and there was a lot of it: three baselines wrong, one expected-failure naming a test that
passes, three live-plan claims false, one count off by one, and a guard described as hypothetical that
fires on the first run. For a plan whose entire content is a mass data edit driven by measurements, that
density of stale figures is itself the finding worth remembering.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | The plan mandates `--order` on `aw group backlog`. Keep it, drop it, or forbid it? | FORBID it, with the measurement and the reproduction, and invert V-02 to prove the filename `NN` slot did not move. | (a) Keep the mandate and merely soften it to "verify afterwards", rejected because the mandate is not merely useless here but actively harmful: passing `--order 1` on an item at slot `02` MOVED the slot, so the instruction causes the damage it was written to prevent. (b) Keep it as cheap insurance in case `aw group backlog` shares the plans defect, rejected because it was TESTED both ways on both a synthetic item with an Order field and a realistic one without, and the bare call preserved everything in every case. | `grep -rln "^- Order:" .aw/records/backlog/` -> 0 of 174; four reproductions in two throwaway repos; `artifact_rename.py:142`/`:153` preserve `NN` when `new_order is None`; `s9p5x5` localizes the real defect to `plans_refs.py:432`/`:437`. | yes |
| D-2 | Eight findings have no source to regroup, five between immutable executed plans. Narrow the plan's goal, widen its authority, or ask? | ASK, as a new `Blocking: yes` OQ-05, and narrow the goal to the sweepable partition in the meantime with the residue named. | (a) Narrow the goal silently and treat the residue as out of scope, rejected because spec Section 5 requires the sweep to resolve collisions "before I2 is turned on fail-closed", so an undocumented residue would leave Order 00 unable to enable the posture with no record of why. (b) Regroup the walkthrough and research files, which ARE renameable, rejected because their setid deliberately mirrors the plan they document and no `From-Backlog` joins them, which is precisely the information-destroying rename OQ-03 forbids. (c) Edit the five executed plans, rejected outright: `AGENTS.md` makes executed plan bodies immutable and directs a post-execution gap to a new corrective IPD. | Shapes parsed from all 86 retired-inclusive findings; all five same-type pairs resolved to files under `plans/executed/`; spec `4w7d6s` Section 5; `AGENTS.md` on not adding commits to an executed plan; ESCALATED as OQ-05 with `Blocking: yes`, so `aw ipd lint` refuses the plan at every checkpoint until a human answers. | no |
| D-3 | `aw check` says 40 and `aw doctor` says 81. Pick the target measure from evidence, or ask? | ASK, as a new `Blocking: yes` OQ-04, and require E-01 to quote the recorded answer before any regroup. | (a) Target the `aw check` (False) measure as the "shipped default", rejected because under it the `agentadhere` collision that motivated the entire backlog item has ZERO findings, so the item's own named deliverable would be satisfied on paper while the data persists. (b) Target the retired-inclusive measure as the safer superset, rejected because it provably cannot reach zero given D-2's residue, so it would make the item unpassable by construction rather than by a human decision. (c) Change `check_collisions`'s default, rejected as out of scope for a data-only child and behavior-changing for every existing caller. | `check_collisions` called both ways: 40/30 versus 86/65; `agentadhere` 0 versus 1; `check_engine.py:835` default False and `:1760` pass-through against `doctor.py:530` hardcoded True; Order 00's OQ-02 already reserves the identity-scope judgement to the maintainer; ESCALATED as OQ-04 with `Blocking: yes`, and the same answer is required by Order 00's own OQ-02. | no |
| D-4 | V-04 demanded the two check surfaces agree. Make them agree, or change the criterion? | Change the criterion: require both counts to FALL by the number swept and require the gap be EXPLAINED by the flag, and declare an equality assertion a failed validation. | (a) Require the executor to reconcile them by passing `include_retired=True` everywhere, rejected because that means editing `check_engine.py` or `doctor.py`, which is code work in a data-only child and would change `aw check`'s reported totals that this very sweep is measured against. (b) Drop the `aw doctor` cross-check, rejected because it is the only thing that would catch a sweep clearing the visible half while leaving the retired half, which is exactly PR-503's hazard. | `aw check all` 40 versus `aw doctor` 81 measured; `doctor.py:530` hardcodes True, `check_engine.py:1760` passes a False-defaulted flag; the divergence is structural, not drift. | yes |
| D-5 | Should `.aw/records/specs` and `.aw/records/research` stay in `Scope-Paths` as speculative declarations? | Remove both; declare only `.aw/records/backlog`. | (a) Keep them in case the inventory reaches a spec, rejected because `aw ipd finalize` requires a `--scope-ack` per declared-but-unmodified path, so the speculation is paid for with a mandatory acknowledgement for work that never happened, and measurement shows zero of the 40 flagged sides is a spec. (b) Keep `.aw/records/research` because two `plans`/`research` findings exist, rejected because both appear only in the retired-inclusive branch and both land in the NOT-sweepable partition, so E-01 reports them and never regroups them. | `cli.py:1486-1492` and `ipd_lifecycle.py:1724-1737` on the two-way scope reconciliation; all 40 `include_retired=False` flagged sides measured in `backlog/`; the two research findings' shape and partition. | yes |
| D-6 | `s9p5x5` already tracks the `aw group plans` Order defect. Should this child still "report it to the maintainer" as its deferral said? | No. Record that it is already tracked and carried by plan `e3hzyc`, and drop the report obligation. | Keeping the instruction to report it, rejected because `s9p5x5` exists precisely BECAUSE four artifacts each said the defect was "reported separately" when no durable carrier existed; adding a fifth prose mention would repeat the failure that item was filed to correct. | `s9p5x5` is `graduated` with `Blocks-Release: next` and its history line records the graduation to plan `e3hzyc`; its body names the four artifacts that had only prose. | yes |
| D-7 | Readiness: `go-pending-approval` (clean verdict, every finding fixed) or `no-go`? | `no-go`. | `go-pending-approval`; rejected because the vocabulary reserves `no-go` for a genuine not-ready condition and this plan has four: its own OQ-04 and OQ-05 are `Blocking: yes` and open, Order 00 carries two more still open, spec `4w7d6s` is `draft`, and the `Graduated-To` field this child consumes exists nowhere in the codebase. Order 00's OQ-02 can also cancel the sweep entirely by narrowing I1. | `aw ipd lint --phase review-finalize` exit 1 with two `IPD-Q501`; Order 00 OQ-01/OQ-02 `Blocking: yes` + `open`; spec `- Status: draft`; `grep -rn "Graduated-To" agent_workflows/` empty; the workflow's definition of NO-GO as "any open question". | yes |
| D-8 | Two new blocking questions cannot be asked: no interactive channel exists in this session. Leave open, or downgrade to `Blocking: no` so the lint passes? | Leave both OPEN and `Blocking: yes`, verdict `APPROVE WITH REVISIONS APPLIED` with readiness `no-go`, recording that an ask was attempted. | (a) Set `Blocking: no` so `aw ipd lint` conforms, rejected as the worst option available: it clears a hard gate by relabelling it, and the lint's own message offers that route, which is exactly why taking it would be a forged clearance. (b) Answer them from evidence, rejected because OQ-04 is an identity-scope judgement Order 00 already reserves to the maintainer and OQ-05's every answer either amends an approved-track spec or contradicts the immutability policy. (c) Write `Readiness: go-pending-approval` so a runner picks the plan up, rejected as forging another role's attestation. | `plan-review.md` Step 3.3 non-interactive exception and memory-kernel rule 5; `AGENTS.md` on never writing another role's attestation field; sibling `yku4ga` carries the identical disposition for the same reason. | yes |
