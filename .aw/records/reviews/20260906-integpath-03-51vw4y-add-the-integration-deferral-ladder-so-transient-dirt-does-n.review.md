# Review: add the integration deferral ladder so transient dirt does not permanently strand a verified lane, child 51vw4y (Set integpath)

- Subject-Id: 51vw4y
- Subject-Type: ipd
- Reviewed-At: 2026-09-07
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `ec475372`. `aw ipd lint --phase author` conforming BEFORE semantic review and
`--phase review-finalize` conforming after every revision, so nothing here is structural.

DISCLOSURE: same agent/model authored this plan, so this is a SELF-REVIEW. Its value rests on
independently AUDITING the `TERMINAL_STATES` consumer set instead of trusting the plan's list, and on
running the flag-surface contract test's logic against the spec. Both produced defects the plan could
not have found by re-reading itself: PR-301 (an unresolvable contradiction that would have failed a
shipped test mid-implementation) and PR-302 (a silent-downgrade path that would have destroyed the
ladder behind a green suite).

SCOPE: only this child was a candidate. `cczotj`, `29wvmj`, `6sb3yu`, `rl67b0` were read as evidence.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-301 | BLOCKER | IN-SCOPE | C. Architecture; E. Testing; spec `25kzda` | `tests/test_run_flag_surface.py:150-158`; spec grep returns no match; spec `Status: approved` | **THE PLAN CANNOT BE EXECUTED AS WRITTEN: E-02/E-05 CONTRADICT A SHIPPED CONTRACT TEST.** E-02 says register `--integration-retry-limit` in `runner_shared.RUN_POLICY_FLAGS` and E-05 adds `--on-integration-blocked`. That table is bound to spec `25kzda` 2.1 by a test reading the SPEC FILE in BOTH directions, and `test_no_owned_flag_is_absent_from_the_spec` FAILS on a registered flag the spec does not declare. Spec 2.1 declares NEITHER flag, and the plan's own `Spec / documentation sync` disclaims spec-edit authority. So following E-02 breaks the suite and every fix requires an act the plan forbids. An executor would discover this only after implementing both flags. | C:Medium; U:Low; S:Low; F:High; Overall:High | OPEN | ESCALATED to new OQ-04 (`Blocking: yes`, `Finding: PR-301`), which mechanically blocks approval. E-02 now carries the measurement and an explicit prohibition on the three tempting workarounds (edit the spec unilaterally, misuse `DECLARED_BUT_NOT_OWNED_HERE`, weaken the test). Three options recorded with costs; recommend (a) amend spec 2.1 in a separate maintainer-approved change. NOT agent-resolvable: it is a decision about a public flag surface and an approved spec. |
| PR-302 | HIGH | IN-SCOPE | A. Correctness; D. Anti-regression | `oc_runipd.py:5840-5846`; `agy_runipd.py:3181-3187`; set-membership probe | **A SILENT-DOWNGRADE PATH THE AUDIT LIST MISCHARACTERIZED AS A NUANCE.** `reconcile_disposition` does `if disposition in TERMINAL_STATES - {"dependency-blocked","not-attempted"}: return disposition`. Because `integration-deferred` is deliberately NOT in `TERMINAL_STATES`, that branch is SKIPPED and control falls through to `return ("partial" if exit_code == 0 else "failed-safely")`. A deferred item would be silently relabelled `partial`, which IS terminal, destroying the deferral and reproducing today's permanent loss WHILE EVERY LADDER UNIT TEST PASSED. The plan described these sites only as "a set-difference expression can silently include or exclude a new member", which understates a defect that would nullify the plan's entire substance. | C:Medium; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added F-11 with the probe. E-01 now names this as "THE TRAP" and "the single highest-risk edit in this E-item", requiring the consumer be taught to pass the status through. V-01 now makes the load-bearing paste `reconcile_disposition` returning `integration-deferred` UNCHANGED on both hosts, and states that pasting only the `TERMINAL_STATES` definitions is a FAILED validation. |
| PR-303 | MEDIUM | UNDER-SCOPE | A. Correctness; G. Plan executability | `oc_runipd.py:3454`, `:4162`, `:5844`, `:7120`; `agy_runipd.py:3185`, `:4394`; `run_state.py:65` | **THE CONSUMER AUDIT LIST WAS INCOMPLETE AND ITS ANCHORS WERE ALL STALE.** The plan named three sites (`oc:3451`, `agy:3052`, `agy:4246`); none of those line numbers is a consumer today, and the real set is SIX. It also omitted `cascade_dependency_blocked` (`oc:4162`), the consumer whose behavior IS the seven-of-34 cascade the plan exists to prevent, and did not warn about the unrelated `run_state.TERMINAL_STATES` (`complete`/`cancelled`) that a grep-driven audit would hit. E-01's whole safety argument is "audit every consumer", so an incomplete list undermines the item it belongs to. | C:Low; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | E-01 now enumerates all six with what each must do for a deferred item, records that `cascade_dependency_blocked` is ONE shared implementation agy re-exports (F-12), and explicitly excludes `run_state.TERMINAL_STATES` (F-14). V-01 requires the six-site enumeration plus confirmation the unrelated set was untouched. |
| PR-304 | MEDIUM | IN-SCOPE | A. Correctness (stale anchors) | measured 2026-09-07 | Six further citations stale: `TERMINAL_STATES` at `oc:301-313`/`agy:355` (actually `:301-319`/`:362-380`), the diagnostic-artifact sites `oc:6473`/`agy:3671` (actually `:6493-6495`/`:3804-3806`), and `runnable is None` at `oc:7024` (actually `:7064`, plus `agy:4340` which was never cited). In a plan about the repo's two highest-churn files, stale anchors send the executor to the wrong place. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All corrected and the agy counterpart added where missing; the `oc:6970`-region dispatch-loop pointer replaced with a find-by-condition instruction. `is_interactive_run:1790` and `run_recovery.py:67` verified EXACT and left as-is. |
| PR-305 | MEDIUM | IN-SCOPE | A. Correctness (false measurement) | `aw runs run-20260905T050043Z-639569` | The headline cost `$165.90`, inherited from backlog `5wdoze`, is wrong: the run record says `$183.95`, and the cost actually lost to the refused lanes was `$88.23` (the three cascaded items spent nothing). Same defect corrected in the orchestrator; correcting it here too keeps the Set consistent. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Concern and F-4 corrected with the commands cited in F-4's Evidence column. |
| PR-306 | LOW | IN-SCOPE | E. Testing; honesty | measured `1 failed, 5613 passed` at HEAD `ec475372` | Baseline (`5536 passed` at HEAD `3d239cfa`) stale by ~77 tests and asserts a green suite when HEAD carries a pre-existing failure. `tests/test_run_flag_surface.py` was also absent from the targeted list despite being the test PR-301 collides with. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Replaced with the measured baseline plus judge-on-DELTA and the named pre-existing failure; added the flag-surface test to the targeted list with an instruction to paste its result as OQ-04 compliance evidence. |

CONFIRMED SOUND. `integration-blocked` IS still terminal on both hosts and `integration-deferred` does
not exist, so the defect is real. F-3's warning is accurate and valuable: the diagnostic
`integration_deferred` reason string genuinely exists in both runners while the status still goes
terminal, which is exactly the kind of thing that gets mistaken for finished work. The design's
strongest feature survives review unchanged: rung 2's SECOND bound (main's activity staleness) is what
makes the wait evidence-based, and the gate paragraph already flags it as the item most likely to be
skipped. The `is_interactive_run` reuse is correct and its citation exact. OQ-01 through OQ-03 are
well-reasoned, and OQ-03's rule (never end a run on a non-terminal status) is the right invariant.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-301 has three possible fixes, one of which (amend spec 2.1) I could have drafted. Fix it or escalate? | ESCALATED as OQ-04 (`Blocking: yes`) with three options, costs, and a recommendation. | (a) Draft the spec amendment myself, rejected: the spec is `Status: approved`, a flag surface is a public contract, and `plan-review` forbids inventing decisions that require the human. (b) Silently move the flags off the shared table, rejected because that abandons the anti-divergence property `RUN_POLICY_FLAGS` exists for (the shipped `--full-auto` had already diverged between hosts) without the maintainer choosing that tradeoff. (c) Leave it as a note, rejected because it fails OPEN: an executor would hit a red test with no authorized fix. | `plan-review.md:228-229`; `tests/test_run_flag_surface.py:150-158`; spec `Status: approved` | yes |
| D-2 | PR-302 is a code-level trap, not a plan defect per se. Is hardening the plan's prose sufficient? | YES, sufficient AND necessary: made it the named highest-risk edit in E-01 and made V-01's load-bearing evidence the pass-through proof. | Filing it as separate backlog, rejected because it is not a live defect today (no `integration-deferred` status exists yet); it becomes one only when this plan lands, so the plan is exactly the right place to carry it. Leaving it in the finding table only, rejected because the executor reads the plan, not the review record. | `oc_runipd.py:5844` fall-through; `'integration-deferred' in (TERMINAL_STATES - {...})` is False | yes |
| D-3 | The Set's other children had their `Item-Dependencies` rationales falsified. Does this one's survive? | YES, kept and strengthened. This edge is genuine: the ladder must be written once in the shared module child 02 creates, or it is written twice into two drifted copies. | Reflexively re-examining it as false, rejected on evidence: unlike children 02 and 04's hook-based rationales, this one rests on code placement rather than on hook behavior, and the 0.651 drift is re-measured. Added a warning that child 02 may have moved a third symbol. | re-measured similarity 0.651; child 02's authorized `build_lane_outcome` exception | yes |
