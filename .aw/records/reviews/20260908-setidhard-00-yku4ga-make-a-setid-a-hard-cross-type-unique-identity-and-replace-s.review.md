# Review: make a setid a hard cross-type-unique identity and replace shared-setid graduation coupling with typed links, orchestrator yku4ga (Set setidhard)

- Subject-Id: yku4ga
- Subject-Type: ipd
- Reviewed-At: 2026-09-08
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `15dba2d7`. STRUCTURAL PREFLIGHT DOES NOT CLEAR, and that is the correct outcome
rather than a defect to repair: `aw ipd lint --phase author` returns EXIT 1 with two `IPD-Q501`
diagnostics, one per `Blocking: yes` open question. Both questions are the MAINTAINER's by their own
`- Owner:` field, so the reviewer may not resolve them, and the lint holds the plan at every
checkpoint from `author` onward. Per the workflow's Step 3.3 non-interactive exception the questions
are left explicitly OPEN, the verdict is `REVIEWED - OPEN QUESTIONS`, and readiness is `NO-GO`.

DISCLOSURE: same agent/model authored this plan, so this is a SELF-REVIEW. Its value rests on
re-measuring the plan's own numbers rather than re-reading them, and that is where every finding came
from: the two headline counts, the total, the suite baseline, and the spec's location were each checked
against the tree and four of the five had moved or were wrong.

I TRIED TO ASK AND COULD NOT. This review attempted to put OQ-01 and OQ-02 to the maintainer through
the interactive question tool and no such channel was available in this session. That is recorded here
and in the plan rather than being silently converted into an agent decision, which is the specific
failure the `askme` workflow exists to prevent.

SCOPE: only this orchestrator was a candidate. Read as evidence: spec `4w7d6s`
(`.aw/records/specs/20260827-1514-01-setid-uniqueness-across-types-and-graduation-links.spec.md`), the
three child plans `drzbs9`/`bwgyum`/`dw7i3m`, backlog `sjsoqq`/`3gr7fk`/`5ev6lh`, the executed plan
`84j8d7`, `check_engine.py`, `releases.py`, `ipd_authoring.py`, and `oc_runipd.py`'s queue sort.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-701 | BLOCKER | IN-SCOPE | G. Plan executability (structural gate) | `aw ipd lint --phase author` exit 1, two `IPD-Q501` | **THE PLAN CANNOT PASS ITS OWN STRUCTURAL GATE, BY DESIGN, AND ONLY A HUMAN CAN CLEAR IT.** OQ-01 (may this Set execute against a `draft` spec) and OQ-02 (does hard cross-type uniqueness break the repository's actual backlog-to-plan naming convention) are both `- Blocking: yes` and both `- Owner: maintainer`. The lint refuses the plan from the `author` checkpoint onward, so it cannot reach `approved` or be executed. This is NOT a plan defect to fix: it is the plan correctly holding a gate on a decision that changes a convention every future graduation follows and would rename artifacts across 30 setids, several belonging to plans other agents are executing now. The reviewer may not resolve it (`plan-review` forbids inventing a human decision) and may not set the spec `approved` (that would forge an attestation). | C:Low; U:Low; S:Low; F:High; Overall:High | OPEN | Left OPEN deliberately. Review ATTEMPTED to ask the maintainer and found no interactive channel in this session, so the non-interactive exception applies: verdict `REVIEWED - OPEN QUESTIONS`, readiness `NO-GO`, both questions annotated with the required decision and with review's new measurements. Remediation Risk is High on the functionality axis because the fix IS the human decision; no bounded edit can substitute for it. |
| PR-702 | HIGH | IN-SCOPE | A. Correctness (stale central measurement) | `aw check all --agent` at HEAD, grouped by rule and setid | **THE PLAN'S TWO HEADLINE NUMBERS ARE BOTH STALE, AND THE DIRECTION OF THE ERROR MATTERS.** It asserts 29 `check.setid-collision` findings across 21 distinct setids as the measurement that shaped the whole sequence. Measured at review, ONE DAY after authoring: **40 findings across 30 distinct setids**. The `aw check all` total is likewise 238, not 98 (174 of those being `check.scope-drift`). The shape claim, which is the load-bearing part, VERIFIED EXACTLY: all 40 locations are in `.aw/records/backlog/`, zero on the plan side, so "every finding is a backlog item sharing its graduated Set's setid" holds. Since these numbers appear in the Concern, Step 0, F-1, F-2, the child table, the completion criteria and the Required tests, a stale count propagates into an executor's acceptance criteria. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | All twelve occurrences corrected across the Concern, Step 0, F-1, F-2, the child table, the completion criteria, the Deferred section, the Required tests and V-03. Each now states the review measurement and marks the authoring figure stale, and Order 01 is told to measure at ITS OWN execution time rather than against any number in the plan. |
| PR-703 | HIGH | IN-SCOPE | A. Correctness; F. KISS (the argument gets stronger) | authoring 29/21 versus review 40/30 | **THE COLLISION POPULATION IS GROWING AT A MEASURABLE RATE, WHICH THE PLAN DOES NOT KNOW AND WHICH IS ITS OWN BEST EVIDENCE.** Eleven new findings and nine new setids appeared in the single day between authoring and review, every one produced by an ordinary graduation reusing its source's setid. That converts the plan's root-cause claim from a prediction about what would happen after a sweep into an observed rate, and it has two consequences the plan should carry: the sweep's cost RISES while the gate is unanswered (an argument for asking promptly, not for proceeding), and Order 01's evidence must be measured at its own execution time. It also sharpens OQ-02 in both directions, which the maintainer deserves to see: it strengthens the case for acting, and it raises the cost of the answer that preserves the name match, since an exemption would now have to cover 30 setids and counting. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-13. A new Concern paragraph states the rate and both consequences. OQ-02's rationale now carries the measurement with both directions and names the concrete narrowing option (exempt a backlog-to-plan pair joined by a resolvable `From-Backlog`, which would take all 40 to zero without renaming anything). The gate section records the cost of waiting. |
| PR-704 | HIGH | UNDER-SCOPE | G. Plan executability | `rg -l 4w7d6s .aw/records/specs/`; `ls .aw/records/specs/*4w7d6s*` returns nothing | **THE SET'S SINGLE SOURCE OF TRUTH IS NOT FINDABLE BY THE OBVIOUS LOOKUP.** The plan cites spec `4w7d6s` throughout, and it does exist with `- Status: draft` and all three OQs open, but its filename is the LEGACY pre-cutover form `20260827-1514-01-setid-uniqueness-across-types-and-graduation-links.spec.md`, carrying no id6. So an id6 glob under `specs/` matches NOTHING. An executor at E-02, whose whole job is to read that spec's status, would either report its source of truth missing (stopping for the wrong reason) or conclude the gate is vacuous. The plan never mentions the naming discrepancy. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-14. E-02 now opens with the lookup instruction and the exact path, and V-02 requires the executor to paste the command it used and show it searched by id6 rather than by a filename glob that cannot match. A Step 0 convention carries the same warning. |
| PR-705 | MEDIUM | IN-SCOPE | A. Correctness (where the gate lives) | spec `:137`, `:146`, `:155` | The plan repeatedly describes the spec's three open questions in terms that read as though the SPEC were blocked ("carries three open questions, which is the gate on this whole Set"). Measured: all three of the spec's OQs carry `- Blocking: no`. The real gate is this plan's OWN two `Blocking: yes` questions, enforced by the lint. The distinction is practical rather than pedantic: an executor who checks the spec, sees three non-blocking questions, and concludes the gate is soft would draw exactly the wrong conclusion, when the actual mechanism is a hard lint refusal on this plan. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-15. E-02 now states where the block lives and forbids reporting "the spec's questions are non-blocking, therefore we may proceed". V-02 requires pasting THIS plan's OQ status lines too and declares reporting the spec's three as the gate a FAILED validation. Step 0 and OQ-01's rationale carry the correction. |
| PR-706 | MEDIUM | IN-SCOPE | E. Testing (stale baseline) | measured bare run at HEAD | The suite baseline is stale in both halves and in its lane figure. It declares `1 failed, 5648 passed` with the failure being `tests/test_orchestrator_retirement.py`; measured, the bare run gives `1 failed, 5919 passed, 3 skipped, 2 xfailed in 49.87s` and the failing node is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` (ENVIRONMENTAL, from a gitignored local dump), while `test_orchestrator_retirement.py` now PASSES. The claimed "roughly 32 environmental failures in a lane worktree" is also stale; a fresh linked worktree measures cleaner than the primary checkout. The plan's delta-not-totals rule is what keeps this from being harmful. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Corrected in E-03 and the Required tests section, with the failure labelled environmental and possibly absent elsewhere, the retired failure recorded as now-passing, the lane figure corrected, and the executor told to measure its own and quote none of these. |
| PR-707 | LOW | IN-SCOPE | A. Correctness (verification of sound claims) | see resolution | Five load-bearing claims were re-verified rather than trusted, and all five HOLD, which is worth recording so a later reader need not redo it: the severity really is already `error` (`check_engine.py:95`); `check_from_backlog` really is the mirror to copy (`releases.py:580`, rglob over three trees, resolves against a known-id set); `Graduated-To` really has ZERO occurrences in the package; `ipd_authoring` really consults no setid predicate; and the `agentadhere` migration target really persists (backlog `3gr7fk` in `done/` still carrying `Set: agentadhere`, shared with executed plans). | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | F-6, F-9, F-10, F-11 and F-8 confirmed in place; F-16 added recording that the entire child table (three ids, three Orders, three dependency edges, all `Kind: child`, all `to-review`) verifies exactly, along with `queue_sort_key`'s dependency-depth-first guarantee. |

CONFIRMED SOUND, and it is the large majority of this plan. The SEQUENCING ARGUMENT is the strongest
thing here and every link in it verified: the severity is already `error` so deliverable #1 really is
about posture rather than a flip; the spec's Section 5 really does require the sweep before hard
enforcement; the sweep really is not durable alone; and the Order-versus-dependency inversion (declared
01/02/03, dependency 02/03/01) is CORRECTLY ENCODED in the children's `Item-Dependencies` and will be
dispatched correctly, since `queue_sort_key`'s docstring states that dependency depth is the first sort
key and that "DECLARED EDGES WIN". The three exclusions are right: deliverable #3 really did land, the
id6 invariant really is out of scope, and `aw check all` really cannot be made green here. CID-1
through CID-6 are unusually good cross-item checks, and CID-2's insistence that a partial sweep
reproduces the same unactionable rule "only with a smaller number" is exactly the right standard. The
gate paragraph's warning about orchestrator retirement discharging parent-only work UNPERFORMED is real
and was verified: `84j8d7` still sits in `executed/` carrying `Execution state: pending` on E-01 and
`Result: pending` on V-01, so E-03's whole-Set verification genuinely is the class of work a runner
retirement would mark complete without performing. That warning belongs in every orchestrator.

WHAT THIS ROUND CHANGED. Nothing about the plan's design, and that is the honest summary: six of seven
findings are measurement drift and one lookup hazard, all fixed in place. The seventh is the plan
holding a gate it is right to hold. The single most useful thing review added is PR-703's rate: the
plan argued that a sweep without the root-cause fix would be undone by the next graduation, and the
tree supplied eleven fresh collisions in a day to prove it. That both vindicates the sequence and makes
the unanswered gate more expensive the longer it stands, which is the one new fact the maintainer
should have when they answer.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-701: the lint refuses this plan on two blocking maintainer questions and no interactive channel exists. Resolve them, downgrade them, or leave them open? | LEAVE BOTH OPEN, verdict `REVIEWED - OPEN QUESTIONS`, readiness `NO-GO`, with the required decision stated and review's new measurements added to each. Record that an ask was attempted and no channel existed. | (a) Answer them from evidence, rejected as the exact prohibition `plan-review` names: OQ-02 is a workflow-ergonomics judgement about how the maintainer reads their own tree, and OQ-01 asks whether to implement a `draft` spec, neither of which the repository can settle. (b) Set `Blocking: no` so the lint passes, rejected as the worst option available: it would clear a hard gate by relabelling it, and the lint's own message even offers that route, which is precisely why taking it dishonestly would be a forged clearance. (c) Set the spec `approved` to discharge OQ-01, rejected outright: that is an attested human act and writing it would forge the attestation `AGENTS.md` forbids by name. | `aw ipd lint` exit 1 with two `IPD-Q501`; both questions' `- Owner: maintainer`; `plan-review.md` Step 3.3 and memory-kernel rule 5; `AGENTS.md` on never writing another role's attestation | no |
| D-2 | PR-702/PR-703: the plan's central counts are stale and the population is growing. Correct the numbers, or re-author the plan around the new figures? | CORRECT IN PLACE and add the RATE as a finding, since the growth strengthens the existing argument rather than changing it. | (a) Re-author, rejected: the sequence, the exclusions and the cross-item checks are all unaffected by the counts; only the acceptance criteria and the prose needed the true numbers. (b) Drop the counts entirely so they cannot go stale, rejected: the count is what makes the rule's unactionability concrete, and OQ-02 cannot be answered without knowing how many artifacts an exemption or a sweep would cover. Instead every count is now dated and paired with an instruction to re-measure. | measured 40/30 versus authoring 29/21; 238 versus 98 total; all 40 locations backlog-side | yes |
| D-3 | PR-704: the spec is unfindable by id6 glob. Fix the plan's citations, or rename the spec to the id6 form? | FIX THE PLAN (state the real path and require an id6 search), and do NOT rename the spec. | (a) Rename the spec to the id6 filename form, rejected on three grounds: it is out of this orchestrator's fence (`Scope-Paths` is `.aw/records/plans/pending`), pre-cutover legacy spec names are explicitly grandfathered by repository convention, and renaming a `draft` spec mid-review would invalidate every citation already written against it, including three child plans'. (b) Say nothing, rejected as the measured hazard: E-02's entire job is reading that file's status, and the obvious lookup returns nothing. | `ls .aw/records/specs/*4w7d6s*` empty versus `rg -l` finding it; `AGENTS.md` on grandfathered legacy spec names; the plan's own Scope-Paths | yes |
| D-4 | PR-705: the plan implies the spec's own questions are the gate, but all three are non-blocking. Correct the framing, or make the spec's questions blocking? | CORRECT THE FRAMING in this plan only, and name the lint refusal as the real mechanism. | (a) Edit the spec to mark its OQs blocking, rejected: out of fence, not this plan's artifact, and unnecessary since this plan's own two blocking questions already hold the Set hard. (b) Leave the framing, rejected: an executor could read the spec, find three non-blocking questions, and conclude the gate is advisory when it is a hard lint refusal. | spec `:137`, `:146`, `:155` all `- Blocking: no`; measured `IPD-Q501` refusal on this plan | yes |
