# Review: stop the plan-gate execution contract from prescribing a hand-rolled lifecycle move

- Subject-Id: dcri4s
- Subject-Type: ipd
- Reviewed-At: 2026-09-17
- Reviewer: opencode/its_direct-pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED

## Round 1

Reviewed at HEAD `edb9ba85`. `aw ipd lint --phase author` reported `conforming` BEFORE semantic
review and `--phase review-finalize` `conforming` after every revision, so nothing below is
structural. DISCLOSURE: the same agent/model authored the plan, so this is a SELF-REVIEW, and its
value therefore rests on RE-MEASURING rather than re-reading. Every finding below was found by
running something, not by re-reading the prose.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | BLOCKER | IN-SCOPE | A. Correctness / G. executability | measured: `AW_EXECUTION_ROLE=worker python3 -m agent_workflows ipd finalize dcri4s --actor x/y --message probe --apply` -> `AW-LIFECYCLE-ROLE-001`, exit 2; `ipd_lifecycle.py:68-71`, `:74-81`, `:84-100`, `:4285`, `:4105`; `oc_runipd.py:5876`; `agy_runipd.py:2779` | THE PLAN'S PRESCRIBED FIX IS ITSELF REFUSED BY THE TOOLKIT. E-02 required element 5 of every gate to direct the executor to run `aw ipd finalize --actor ... --apply`. A managed lane MAY NOT run that verb: both runners export `AW_EXECUTION_ROLE=worker` for every ISOLATED turn (the default), and the role refusal is checked before any selector resolution. So the fix would have moved the wasted turn from `IPD-M104`/`IPD-S406` to `AW-LIFECYCLE-ROLE-001` while appearing to resolve it. That exact substitution is backlog `i452hf` ("driver refinalizes a lane the agent already finalized"), closed by commit `cdef9c90` whose purpose was to STOP an in-lane agent finalizing. Root cause restated: the contract names a COMMAND for a step whose OWNER is a run option (`--no-self-finalize` help: "the agent must move the plan itself", `oc_runipd.py:9078-9083`; `driver_finalize` only inside `if self_finalize and not is_review:`, `oc_runipd.py:6715`). | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Goal gained a "WHAT REVIEW CHANGED ABOUT THE DIAGNOSIS" section with the pasted refusal. E-02 rewritten to separate the UNCONDITIONAL obligation from the CONDITIONAL owner, with an attempt-and-treat-refusal-as-success rule so a wrong guess costs nothing. New E-09/V-09 REPRODUCE the refusal before any text is written, with a STOP condition if it does not reproduce. G-07 added. Gate clause 10 now dogfoods the conditional form. |
| PR-002 | HIGH | OVER-SCOPE | F. KISS / duplicate paths | `.aw/records/plans/pending/20260908-roleadv-01-8b9ufm-...ipd.md` (`- Status: approved`, `- Blocks-Release: next`) and its `- Scope-Paths:`; `runner_shared.build_prompt:9832` | G-05 treated the silent runner prompt as an unowned gap "recorded for the driver's owner". It is OWNED by an APPROVED, release-blocking plan. `8b9ufm` states the runner-owns-begin/finalize role at TURN START in all four prompt builders, gated on the run's frozen `self_finalize`, and declares three of the relevant source files. Its own review PR-001 rejected an UNCONDITIONAL statement for the same reason this plan's E-02 must be conditional, so the two designs already agree. Left as written, an executor could reasonably have "improved" the prompt and collided with an approved plan. Also corrected: the prompt is now ONE shared definition for both hosts (`runner_shared.build_prompt`), not per-host as the plan implied. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | G-05 rewritten to name the owner; Deferred section states the ownership and the complementarity; new gate clause 7 forbids touching any prompt builder or adding those three files to the changed set; scope check records the avoided collision. |
| PR-003 | HIGH | IN-SCOPE | E-07 premise / D. root-cause honesty | measured over gate SECTIONS: 15 of 110 pending gates carry `git mv`+`executed/` and ALL 15 already carry the corrective wording; 156 of 516 executed gates carry it without finalize; `ipd_lint.py:1067-1068` + `_is_terminal_dir` `:1043`; all 516 executed plans lint `legacy/not evaluated` | THE MIGRATION ITEM RESTED ON A FALSE PREMISE. E-07 asserted "every plan scaffolded before this fix has it, including plans already in `executed/`" and required choosing among a migration fixer or a lint grandfathering cutoff. Neither is needed: ZERO pending gates prescribe the hand-rolled move (all 15 that mention it already forbid it), and the executed tree is exempt by construction because `lint_text` short-circuits every terminal-directory plan to the `legacy` disposition before any rule runs. Building a fixer or a cutoff on that premise is speculative work with a real regression surface. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-07 INVERTED from build-a-migration to verify-none-is-needed, retaining a remedy branch if a measurement differs at execution HEAD. G-06 rewritten with the real counts. OQ-01 resolved from the measurement. V-07 rewritten to demand both re-run measurements. The propagation paragraph in the Goal now carries the numbers and notes that 15 hand-written corrections are the signature of a template nobody trusts. |
| PR-004 | MEDIUM | IN-SCOPE | E-05 design / A. correctness (false positives) | this plan quotes the defective clause 13 in its own Goal; pending plans `i4c0c3` and `y9vpvv` discuss the executed-transition gate as subject matter; `ipd_lint.py:141-144`, `:157-167`, `:379`; `ipd_schema.py:57` | E-05 specified the new rule as firing on a plan whose gate "instructs a terminal move by hand" but never bounded the SCAN to the gate section. A whole-file scan fires on this very plan and on any plan whose subject matter is the transition gate, which would make the rule's first victim the fix itself. The plan also did not name the mechanism, though the parser already provides it (`doc.h2` with line numbers, `H_APPROVAL_GATE`, already used for section-scoped field parsing). | C:Low; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | E-05 now REQUIRES gate-section scoping and names the parser affordances to use. E-06 gained a fifth test asserting the rule does NOT fire when the clause appears outside the gate. V-05 demands a four-part reproduction plus a clean lint of this plan's own file. |
| PR-005 | MEDIUM | IN-SCOPE | Step 1 evidence integrity | `ipd_lint.py:63-65` (S406) vs `:68` (`C_READINESS_UNATTESTED`/IPD-M107); `ipd-lifecycle.md:105-107`, `:109-114`, `:114-116`; `plan-review.md:351-354`; `review-rubric.md:17-19` | Load-bearing citations were off. `IPD-S406` was cited at `ipd_lint.py:68`, which is `IPD-M107`. Five `ipd-lifecycle.md` ranges were each shifted by 2 to 4 lines (`:110-113` -> `:109-114`, `:115` -> `:114-116`, `:113` -> `:118-119`, `:126` -> the hook paragraph). An executor following them reads the wrong code or the wrong paragraph for facts the plan rests on. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All citations corrected in E-01, the conventions section, the findings table and the Goal, with the S406 correction called out explicitly so a reader can tell a fix from a transcription error. E-01 now also cites `ipd_schema.py:398-407` for M104's actual semantics. |
| PR-006 | MEDIUM | IN-SCOPE | D. anti-regression / accuracy of the diagnosis | `ipd-lifecycle.md:105-107` ("This is the ONLY supported terminal path; the manual ordered steps below are the contract it implements") vs the plan's G-03 | G-03 claimed `ipd-lifecycle.md`'s two passages "contradict each other" and that the file "reads as authorizing a hand-performed terminal move". It does not: the file ALREADY states the manual steps are the contract finalize implements. The residual defect is only that the framing sits ~90 lines above the steps. Writing E-04 as the repair of a contradiction invites an executor to rewrite a passage that is substantively correct. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | G-03 downgraded Medium -> Low and rewritten with the quote. E-04 restated as a LOCAL clarification with an explicit no-edit-needed branch if E-01 finds the framing already local. V-04 accepts either outcome but demands the quote for both. |
| PR-007 | MEDIUM | UNDER-SCOPE | G. executability (spec sync) | `20260726-1340-01-ipd-spec.spec.md:14`, `:44`; `20260802-1904-01-ipd-structure-and-linting.spec.md:505-515`, `:509` | The spec-sync section deferred to the executor the question of whether a spec governs the gate's contract elements ("E-01 should discover whether one exists"), risking an approved spec being amended mid-execution or a required amendment being missed. Checked at review instead: `ipd-spec:14` delegates the execution contract to `AGENTS.md`/`CONTRIBUTING.md`, and both specs describe the transaction rather than prescribing its invoker, so neither needs amending. The one live possibility is the new lint id joining a rule catalog. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Spec-sync section now records the check with citations, states neither spec needs amending and therefore neither is declared, and keeps the declare-first rule for the narrow lint-catalog case. |
| PR-008 | LOW | IN-SCOPE | E. testing / repository convention | `AGENTS.md` suite-running contract; `pyproject.toml` `addopts` | "Required tests" said to run the suite bare but hedged with "if the toolkit configures `addopts`, a bare run is already correct", leaving an executor free to add flags. The repository contract is specific about which flags actively harm (`-n0` several-fold slower, a second `-q` suppressing the summary line the plan requires pasted, `-p no:randomly` disabling order randomization). | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Validation section now names the exact command and the three forbidden flags with their consequences; gate clause 8 repeats the rule at the point of use. |
| PR-009 | LOW | UNDER-SCOPE | B. security lens (honest limits) | `oc_runipd.py:5869-5871` ("an environment SELECTOR, not a hardened boundary ... A same-user worker with shell access can unset it"); backlog `c4yixg` | The corrected design leans on the worker-role marking, which is a selector rather than a boundary and has a known live bypass. Silent reliance would let a reader think the conditional contract is enforced when it is advisory. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Deferred section now states the honest limit, cites the runners' own admission and backlog `c4yixg`, and says plainly that this plan describes the normal case in prose and does not attempt enforcement. |
| PR-010 | LOW | IN-SCOPE | G. executability (self-consistency) | plan gate clause 9 (pre-revision) | The plan's own gate carried the UNCONDITIONAL "run `aw ipd finalize ... --apply`; do NOT `git mv`" wording, i.e. exactly the instruction PR-001 shows is refused in a managed lane. Since the plan is executed by a runner, its own gate would have reproduced the failure class one layer down, and clause 3 advertised the wording as exemplary. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Gate clause 11 rewritten to the conditional form (lane -> runner owns it, do not run the verb; otherwise run it; unsure -> attempt and accept the refusal as success). Clause 4 now frames the plan's own gate as its smoke test and directs a mismatch to be fixed in E-02 rather than in the clause. |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | The plan's central fix (make every gate say "run `aw ipd finalize`") is refused in a managed lane. Rewrite the fix, or stop and ask the maintainer which owner the contract should name? | Rewrote it as a CONDITIONAL contract: element 5 states the unconditional OBLIGATION (only the gated finalize transaction may reach `executed`) separately from the OWNER (runner in a managed lane, executor otherwise), plus an attempt-and-treat-refusal-as-success rule so a wrong guess is free. | Naming the RUNNER unconditionally (rejected: strands a hand-executed plan in `pending/`, transitioned by nobody, which is worse than a wasted turn); naming the AGENT unconditionally (rejected: this is the measured defect); leaving the plan NO-GO pending a maintainer ruling (rejected: the repository already answers it, and approved plan `8b9ufm` reached this same conditional design through its own review) | `oc_runipd.py:5876` + `agy_runipd.py:2779` (worker marking keyed on `work_dir`); `ipd_lifecycle.py:4285` (refusal); `oc_runipd.py:6715` (`if self_finalize and not is_review:`); `oc_runipd.py:9078-9083` (`--no-self-finalize`: "the agent must move the plan itself"); `8b9ufm` and its PR-001 | yes |
| D-2 | E-07's premise (every existing plan carries the defective clause) is false. Delete the item, or repoint it? | Repointed to VERIFY-none-is-needed, keeping a remedy branch if either measurement differs at execution HEAD. | Deleting E-07 (rejected: the two facts that make migration unnecessary are then asserted nowhere and unverified at execution time, and E-05 silently depends on one of them); leaving it as build-a-migration (rejected: speculative work on a false premise, with a real regression surface in a fixer) | pending-gate scan (15 of 110, all already corrective); `ipd_lint.py:1067-1068` + `:1043`; all 516 executed plans linting `legacy/not evaluated` | yes |
| D-3 | Should this plan also fix the silent runner prompt (its own G-05)? | No. Recorded the existing owner and fenced this plan off the prompt builders and the three files `8b9ufm` declares. | Fixing it here (rejected: merge collision with an approved, release-blocking plan whose review already settled the wording); leaving G-05 as an unowned "for the driver's owner" note (rejected: that invites an executor to helpfully do it) | `8b9ufm` front matter (`approved`, `Blocks-Release: next`) and its `Scope-Paths`; `runner_shared.build_prompt:9832` | yes |
| D-4 | Does the new lint rule need a grandfathering cutoff for the 156 executed gates carrying the old wording? | No. Relied on the existing terminal-directory short-circuit instead of adding a cutoff mechanism. | Implementing an `oorry1`-style `Scope-Paths` cutoff (rejected: a second grandfathering mechanism for a case the linter already exempts by construction); date-keyed grandfathering (rejected: same, plus a date is a worse key than the disposition the linter already computes) | `ipd_lint.py:1067-1068`, `_is_terminal_dir` `:1043`, verified by linting the whole executed tree | yes |
| D-5 | Both open questions were addressed to the maintainer. Ask, or resolve from evidence? | Resolved BOTH from repository evidence and set them `resolved`, per the workflow rule not to ask what the repository answers. | Leaving them open and recommending NO-GO (rejected: both are answered in-tree, and an open question here would gate the plan at every lint checkpoint for no decision the human actually needs to make) | OQ-01: the two propagation measurements plus `ipd_lint.py:1067-1068`. OQ-02: `8b9ufm` (`approved`) owning the prompt surface | yes |

No `Reversible: no` decision was taken, so no escalation to a `Blocking: yes` question was required.
Every finding is FIXED, so no finding meets the `HIGH` escalation threshold for an unfixed-finding
open question either.
