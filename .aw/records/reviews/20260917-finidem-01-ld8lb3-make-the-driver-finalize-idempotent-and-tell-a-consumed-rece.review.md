# Review: make the driver finalize idempotent and tell a consumed receipt from a never-issued one

- Subject-Id: ld8lb3
- Subject-Type: ipd
- Reviewed-At: 2026-09-17
- Reviewer: opencode/its_direct-pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED

## Round 1

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | BLOCKER | IN-SCOPE | A. Correctness / B. Security (fail-closed gate) | `run_selection_policy.py:506-511`, `:134`; `.aw/records/plans/reusable/README.md:5` | The predicate the plan PRESCRIBES for ALREADY-FINALIZED is fail-open. `is_in_terminal_directory` admits FOUR segments including `/reusable/`, and `_IPD_ACTIONS["reusable"]` is `ACTION_EXECUTE`, so a reusable plan is one the runner re-dispatches. Measured: the predicate returns True for a `reusable/` path. Keying E-04 on it makes every reusable-plan run read a never-issued receipt as success, the exact inversion the plan's own OQ-02 forbids, delivered by the predicate the plan called safe. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Conventions bullet rewritten to forbid the predicate and prescribe `plan_bucket(path) == "executed"`, preferably with the plan-bound `lifecycle(<id6>): finalize` commit subject. E-02, E-07, V-02, V-07 and the gate now demand a reusable-plan refusal control as pasted evidence. |
| PR-002 | MEDIUM | IN-SCOPE | Step 1 evidence integrity | plan F-1 and conventions vs `ipd_lifecycle.py:3989-3993`; plan F-6 vs `:1905-1913`; plan F-9 vs `:3984-3988` | Three load-bearing `path:line` citations point at unrelated docstring text. `:3570-3573` lands in `_perform_terminal_transaction`'s docstring, not the receipt consumption; `:1602-1611` lands in `_run_record_committed_paths`' docstring, not the `receipt is None` branch. An executor following them would read the wrong code for the two facts the plan rests on. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All three corrected in place with the correction noted, so a reader can tell a fix from a transcription error. |
| PR-003 | HIGH | IN-SCOPE | Step 1 evidence integrity / D. root-cause honesty | `git show 575f0b32:<63425h plan>` line 711 (`aw ipd finalize` x5 in that plan); run `events.jsonl` `lane-inputs-materialized` | F-10 is FALSE and E-06's whole premise rests on it. The plan concludes "the agent finalized on its OWN initiative" from the absence of `aw ipd finalize` in the RUNBOOK. But the lane also received the ASSIGNED PLAN, whose own execution gate says "move this plan to `.aw/records/plans/executed/` via `aw ipd finalize`". The agent was following its contract, not improvising, so "documentation is part of the fix" does not follow. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | F-10 struck through and marked WITHDRAWN AS FALSE with the counter-evidence; F-10a and F-10b added carrying the real root cause; Goal gained a paragraph stating what review changed about the diagnosis. |
| PR-004 | HIGH | IN-SCOPE | G. Plan executability | `oc_runipd.py:1294`, `agy_runipd.py:1037`, `runner_shared.py:8440`; `oc_runipd.py:7651-7945` vs `agy_runipd.py:4266-4490` | Two counterfactual claims make E-05 unexecutable as written. The plan says "Both hosts call the shared `driver_finalize`" and offers E-05 the branch "or prove it already shares the site". `driver_finalize` is NOT shared: it is defined twice, once per host, and the entire finalize result-handling block is duplicated too. Only `driver_begin` is shared, which is what `:8440` actually is. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Conventions bullet corrected with both definition sites; E-05's dead branch removed and the item restated as two changed sites; V-05 updated to demand the two sites. Coordination note added for the in-flight `hostdedup`/`rununify` lifts of these same symbols. |
| PR-005 | HIGH | OVER-SCOPE | F. KISS / duplicate paths | `.aw/records/plans/pending/20260908-roleadv-01-8b9ufm-...ipd.md` (`- Status: approved`, `Readiness: go-pending-approval`), its `- Scope-Paths:` | E-06 duplicates an APPROVED plan. `8b9ufm` (roleadv-01) already states the runner-owns-begin/finalize role at TURN START in all four prompt builders, already gates it on the run's frozen `self_finalize`, and declares three of the four source files this plan declares. Two approved plans writing the same prompt text is a merge collision. `8b9ufm` also already resolved this plan's OQ-01. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-06 rewritten to the genuinely unowned gap (PR-006) and explicitly forbidden from touching the prompt builders; F-12 records the ownership; scope-check now names the collision it avoided. |
| PR-006 | HIGH | UNDER-SCOPE | B. Security (default-deny) / A. Correctness | measured: `worker_role_active` present in `run_finalize`/`run_begin`, ABSENT from `finalize()` and from all of `status_set.py`; delegation at `status_set.py:1144`; precedent at `ipd_lifecycle.py:3177` + `ROLLUP_SHARED_GATES` "worker-role-refusal" | The role guard has an unbypassed delegation hole the plan never noticed. `worker_role_active` is checked in the CLI wrappers only, so `aw set executed <plan>` (which delegates straight into `_life.finalize`) performs a full terminal transaction from a worker lane with NO role refusal. `retire_orchestrator` hit the identical gap and fixed it by checking the predicate itself, recording that it is "NOT inherited". | C:Low; U:Medium; S:Low; F:Low; Overall:Medium | FIXED | Became the new E-06, citing the `retire_orchestrator` precedent; V-06 rewritten to demand the refusal plus an unmoved plan plus a coordinator control, all pasted. |
| PR-007 | HIGH | IN-SCOPE | E. Testing / G. executability | `oc_runipd.py:5843`, `agy_runipd.py:2750`, `ipd_lifecycle.py:4285`; backlog `770fkp` (high), `s0303g` (medium) | V-07 mandates "a REAL driver execution" as non-negotiable evidence but the plan never checks that it is reachable. An isolated lane exports `AW_EXECUTION_ROLE=worker`, which refuses the lifecycle verbs the demonstration needs, and 31 lifecycle tests already fail in that environment. As written the item is either unsatisfiable or invites the executor to relabel a unit test as end-to-end, which is the greenwash the plan's own gate warns against. | C:Medium; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | V-07 now names two acceptable forms in preference order (a run outside any lane; or a harness driving the real `execute_item` path), names what is NOT acceptable, and requires a DEFERRED question with evidence rather than a pass if neither is reachable. |
| PR-008 | MEDIUM | IN-SCOPE | A. Correctness / G. executability | `oc_runipd.py:1341`, `agy_runipd.py:1082`; `cli.py:1519-1571` | E-04's instruction "Consume E-02's finding id; do NOT re-derive the condition" is not satisfiable through the channel that exists. `driver_finalize` returns a bare `(returncode, stderr_text)` pair and `aw ipd finalize` registers no `--agent`/`--json` on that path, so the driver's only discriminator today is the message STRING, which is the fragile coupling E-07 exists to ban. | C:Medium; U:Low; S:Low; F:Low; Overall:Medium | FIXED | Conventions bullet added describing the channel; E-04 now requires an explicit recorded choice between calling the predicate in-process or adding structured output and declaring `cli.py` in `Scope-Paths` first; substring-matching explicitly banned. |
| PR-009 | LOW | UNDER-SCOPE | G. executability (spec sync) | `25kzda` 1.1, 1.2 step 11, `:1059` | The spec-sync section left it to the executor to discover whether a spec assigns the transition owner, which risks an approved spec being edited mid-execution. Checked at review instead: `25kzda` already says the checker "alone authorizes ... terminal transitions", that the executor "cannot mark the action verified", and lists `finalize` as a runner action. No amendment is needed. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Spec-sync section now records the check and its citations, and keeps the declare-first rule if the executor still concludes otherwise. |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Who owns the terminal transition, the agent or the driver? (the plan's OQ-01, addressed to the maintainer) | Resolved from evidence WITHOUT asking: the DRIVER owns it whenever `self_finalize` is true and the AGENT owns it under `--no-self-finalize`; ownership is a run option, not a constant. OQ-01 set to `resolved`. | Leaving it OPEN and recommending NO-GO (rejected: the repository answers it, and the workflow forbids asking what the repo already answers); picking "agent-owned" per the plan's F-10 (rejected: F-10 is false, see PR-003) | `oc_runipd.py:5843` + `agy_runipd.py:2750` (worker marking), `ipd_lifecycle.py:4285` (the refusal), commit `cdef9c90` "fix(i452hf): stop stranding a lane the agent already finalized", `oc_runipd.py:9013-9018` (`--no-self-finalize` help: "the agent must move the plan itself"), `oc_runipd.py:6682` (both driver verbs inside `if self_finalize`), and approved plan `8b9ufm` which states exactly this | yes |
| D-2 | E-06 duplicates approved plan `8b9ufm`. Delete it, defer it, or repoint it? | Repointed to the genuinely unowned delegation hole (PR-006) rather than deleted, and explicitly fenced off the prompt builders. | Deleting E-06 outright (rejected: it would drop the real unguarded `aw set executed` path with nothing owning it); leaving it and accepting the collision (rejected: two approved plans editing the same prompt text) | `8b9ufm` front matter `- Status: approved` and its `- Scope-Paths:`; the measured absence of `worker_role_active` from `finalize()` and `status_set.py`; the `retire_orchestrator` precedent at `ipd_lifecycle.py:3177` | yes |
| D-3 | Which positive signal should ALREADY-FINALIZED key on, now that `is_in_terminal_directory` is disqualified? | `plan_bucket(path) == "executed"` as the floor, with the plan-bound `artifact_core.finalize_commit_subject` commit preferred or additionally required. Left as a preference rather than mandated, since the commit lookup's cost in the driver's context is an implementation question. | Mandating the commit signal only (rejected: may not be cheaply reachable at every call site, and would risk the executor inventing a worse workaround); keeping the directory test with `/reusable/` special-cased (rejected: a second directory test is what the conventions section rightly forbids) | `runner_shared.py:3719` (`plan_bucket` and its bucket-vs-readiness contract), `artifact_core.py:150-158`, `hooks/executed_transition_gate.py:261-293` (the same subject already used as finalize proof), `oc_runipd.py:6371-6373` (the driver already does this bucket test) | yes |
| D-4 | Should the `env -u AW_EXECUTION_ROLE` bypass that actually caused the incident be fixed in this plan? | No. Recorded as an accepted UNDER-SCOPE follow-on, with the reason and the two backlog items that remove the incentive. | Widening this plan to harden the boundary (rejected: needs an OS sandbox or separate principal, far outside a finalize-idempotence plan); staying silent (rejected: it is the true proximate cause and a reader must not think this plan closes it) | `oc_runipd.py:5836-5838` ("this is an environment SELECTOR, not a hardened boundary ... A same-user worker with shell access can unset it"); the incident's own outcome JSON recording `env -u AW_EXECUTION_ROLE python3 -m agent_workflows ipd finalize 63425h --apply`; backlog `770fkp` (high), `s0303g` (medium) | yes |
