# Review: point a driver run at the verb that can help it, child d91i3e (Set runrecon)

- Subject-Id: d91i3e
- Subject-Type: ipd
- Reviewed-At: 2026-09-08
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `ef1e1fbe`. Structural preflight `aw ipd lint --phase author` conformed with three
`IPD-Z602` density advisories (E-02, E-05, E-06); `--phase review-finalize` conforms after revisions with
two advisories remaining (E-05, E-07), each examined below.

DISCLOSURE: authored in the same session, so this is a self-review and worth less than an independent one.
What that cost is visible in PR-601 and PR-604: the plan enumerated five affected leaves when ten emit the
refusal, and reasoned about a machine-record schema that this module does not implement. Both were
findable by running the commands the plan itself cites.

THE PREMISE IS CONFIRMED LIVE, not accepted from the narrative. Measured against driver run
`run-20260908T213552Z-3724920` (holding `events.jsonl`, `state.json`, `outcomes/`, `manifest.json`,
`prompts/`, `sessions/`, `collections/`, `lane-reports/`, and no `ledger.jsonl`): every ledger-model leaf
prints `error: ledger file not found for target '<id>'` and exits 2 unpiped, mentioning `repair` zero
times, while `aw runs repair <id>` on that same run answers `nothing to repair (no running steps)` at
exit 0 and `aw runs <id>` renders the run's full table at exit 0. So the dead end is real, the suggested
action is real, and the plan's chosen approach (option 4 of the backlog item, make the error actionable)
is the right one. The plan's narrowing is also confirmed: `1273806c` did land the discoverability fix
(`aw runs repair --help` prints `REPAIR_HELP` at exit 0; `aw runs --help` mentions `repair` three times),
and `resolve_ledger_path` still deliberately returns `None` for a driver run id, so the `e6b9kt` fence is
intact and must stay.

WHAT ROUND 1 CHANGED. Every finding was fixed in place; none was deferred, left open, or marked REPLAN.
Two new E-items were added (E-07 splitting the three-way return out of the detector, E-08 covering the
four writer leaves the plan never listed) with their V-twins, and the watermark was raised to 08. The two
most serious findings are cases where the plan as written would have produced a WRONG message: PR-601
(four leaves entirely unlisted) and PR-603 (a suggestion that fails, because the plan told the executor to
probe the wrong resolver's roots).

ON THE TWO REMAINING `IPD-Z602` ADVISORIES, examined rather than waved past. E-05 is one concern (preserve
`resolve_ledger_path`'s behavior) whose clause count comes from citing the five existing pins it must keep
green; splitting it would separate a constraint from its evidence. E-07 is one concern (a three-valued
return type) whose clauses are the three values. Neither names multiple deliverables nor needs multiple
V-items, so neither is a real density violation. The one advisory that WAS real, on the original E-02, is
now split into E-02 (ask `repair`'s resolver) and E-07 (three-way answer).

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-601 | HIGH | UNDER-SCOPE | G. executability; A. correctness | `run_cli.py:808`, `:912`, `:1050`, `:1126`; measured ten leaves at `ef1e1fbe` | **TEN leaves emit this refusal, not five, and the four the plan never listed are the `aw run` WRITERS.** `aw run start`, `record`, `cancel` and `finalize` each call `_resolve_or_error` as their first act and return its code, so E-01's consolidation reaches them WHETHER OR NOT the plan acknowledges them. That is what makes the omission serious rather than cosmetic: the plan would have shipped a new message to four unexamined leaves, with no test covering them and no one having asked whether the wording is even true there. The plan's own F-4 called five leaves "the spread" and its V-01 said "five separate outputs; a single leaf does not validate this item", so the under-count was load-bearing in the validation too | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | F-4 rewritten to ten leaves with the four writer call sites cited. New E-08 owns the writer-side verification and any wording fix, with new V-08 demanding four separate outputs. E-01's text, its expected outcome, V-01, E-06's case (a) and the scope check all corrected from five to ten/six. Over-scope declared explicitly with the reason (the consolidation reaches them anyway) |
| PR-602 | HIGH | IN-SCOPE | A. correctness; F. UX | `run_cli.py:806`, `:910`, `:1048`, `:1120`; `aw run --help` | **The message the plan specifies is FALSE on four of the ten leaves.** E-03 said to state "that these readers serve the ledger run model", but `aw run start/record/cancel/finalize` are WRITERS that mutate a ledger (`_run_start`: "Release + start a runnable step"). A refusal that mis-describes the very leaf that printed it is precisely the class of defect this plan exists to remove, and it would have been introduced BY the fix. Compounding it, the plan had no item that would have caught this, because it did not know those leaves existed (PR-601) | C:Low; U:Medium; S:Low; F:Medium; Overall:Medium | FIXED | E-03 gains a paragraph forbidding the "these readers" framing, requiring either wording true of both directions (name the RUN MODEL, not the leaf's direction) or a direction parameter on the builder, and requiring the executor to state which was chosen and why. V-03 now demands the same message rendered from a WRITER leaf with a justification of why the wording is true there. New F-14 records the mechanism |
| PR-603 | HIGH | IN-SCOPE | A. correctness; C. architecture | `run_cli.py:251-259` vs `run_viewer.py:1117-1133`, `:2584-2585`, `:2645-2649`; `project_context.py:314-327`; three measured cases | **The plan told the executor to detect a driver run by probing `resolve_ledger_path`'s OWN roots, which measurably produces suggestions that fail.** `repair` resolves through `discover_run_dirs`, and the two resolvers disagree on three axes, each reproduced: (a) ROOTS, the reader searches `.aw/state/runs/` but `repair` does not, so a run there gets told to run `repair` and `repair` answers `error: no run matched target` at exit 2; (b) PREDICATE, `repair_run` requires `state.json` and answers `not a run directory` at exit 2 for an `events.jsonl`-only directory, which the plan's stated detector (`state.json` **or** `events.jsonl`) would have called a driver run; (c) CWD, the readers use `Path.cwd()` while the viewer CLIMBS to the project root, so from `tests/` the reader refuses while `repair` succeeds. A plan whose entire product is a trustworthy signpost cannot afford a signpost that points at a refusal | C:Medium; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | E-02 rewritten: ask `resolve_target_runs_detailed`, the function the `repair` branch itself calls, and additionally require the `state.json` `repair_run` requires; do not re-derive the rules. New F-15 records all three axes with citations and measurements. V-02 now demands the detector's answer be pasted NEXT TO the actual `repair` result and exit code for each of the three axes. A Step 0 convention states the two resolvers are not the same resolver |
| PR-604 | HIGH | IN-SCOPE | Evidence accuracy; C. architecture | measured `validate_agent_record` -> 3 violations; `run_cli.py` imports `agent_schema` zero times; `run_viewer.py:22`, `:2551-2566` | **E-04 reasoned about a schema this module does not implement, and its instruction was unbuildable as written.** It said to carry the suggestion in "the same `NextAction`-style `next` field the rest of the CLI uses" and to "CHECK THE AGENT SCHEMA ... so this case is compliant as-is". Measured: `run_cli`'s payloads are bare dicts (`{"ok","error","exit_code"}`) and `agent_schema.validate_agent_record` returns THREE violations against one (no `schema`, no `kind`, no `cmd`); the module imports `agent_schema` zero times. So there is no `next` field convention here to follow, and the compliance claim was about a contract that does not govern these payloads. An executor following it literally would either invent a shape or, worse, convert the payloads to `aw.agent/v1`, silently changing what every existing `--agent` consumer of `aw run`/`aw runs` parses across 24 `_emit_machine` and 33 `_emit_error` sites | C:Low; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | New F-16 records the measurement, the import counts, and the contrast with `run_viewer` (which DOES emit conformant records). E-04 rewritten: extend the EXISTING payload shape, do NOT convert to `aw.agent/v1`, and STOP and raise if the conversion seems required, since it is a public-surface decision. F-10's schema half corrected, keeping exit 2 justified by the module's own vocabulary instead. V-04 now demands the payload key set before/after and explicitly forbids pasting a validator call as evidence of success (it would report a pre-existing nonconformance). The exclusion is declared in the scope check and the gate |
| PR-605 | HIGH | UNDER-SCOPE | B. security and privacy; D92 | `agent_schema.py:63-65` (`_HOME_PATH_RE`); E-02 requires the detector to know a directory path | **Nothing in the plan stopped the new message from printing an absolute path, and the design pushes toward exactly that.** The detector must FIND a run directory to classify the target, so it holds an absolute path; the natural way to make a suggestion unambiguous (and the natural way to explain the subdirectory discrepancy of OQ-02) is to print it. That path contains the maintainer's home directory and username, the refusal is on the most-copied surface here, and `agent_schema._HOME_PATH_RE` exists precisely because this class of leak is a known repository hazard. The plan's original E-03 and V-03 asked for the message text and never for the absence of a path | C:Low; U:Low; S:Medium-High; F:Low; Overall:Medium | FIXED | E-03 now forbids printing the resolved directory, requires `aw runs repair <target-as-given>`, and says to describe an unreachable-run case in words rather than by pasting a path. V-03 demands a grep or assertion proving no absolute path and no `/home/` segment in any of the three renderings. E-06 gains case (g) asserting the same in the tests. OQ-02 repeats the prohibition. The validation section now states the sanitizer check is load-bearing here rather than a formality |
| PR-606 | MEDIUM | IN-SCOPE | Evidence accuracy | measured bare suite at `ef1e1fbe`; `tests/test_runner_backlog_close.py` alone -> 47 passed | The stated baseline is wrong in BOTH halves: the plan says "1 failed, 5648 passed (the known `test_orchestrator_retirement` failure)", but re-measured bare the suite reports `2 failed, 5864 passed, 3 skipped, 2 xfailed in 88.84s` and `test_orchestrator_retirement` now PASSES. The two current failures are `test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` (environmental, an `rglob` ignoring `.gitignore`, tracked as backlog `8kttqq`) and `test_runner_backlog_close.py::ShutdownReportOnInterrupt::test_sigint_produces_the_report_and_exits_130` (a `TimeoutExpired` under parallel load; that file passes 47/47 alone). An executor told to expect a different failure might try to "fix" one of these, and the first tempts deleting another party's untracked directory | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Validation section rewritten to a delta rule with the re-measured numbers, both failures named with their mechanisms, the flaky-under-load one characterized so it is not mistaken for a regression, and an explicit prohibition on deleting the untracked directory. The gate repeats the measure-your-own-baseline instruction |
| PR-607 | MEDIUM | UNDER-SCOPE | C. architecture; E. testing | measured import graph; `run_cli.py:190-197`; `run_viewer` import cost ~168 ms | Two gaps the corrected E-02 exposes. FIRST, the plan never addressed that `run_cli` imports `run_viewer` ZERO times today, so the fix introduces a NEW cross-module dependency with no cycle analysis and no statement of where the import goes; measured, `run_viewer` imports `run_cli` zero times so there is no cycle, but a module-scope import would put a ~168 ms import graph on every ledger-reader invocation. SECOND, the plan's original E-02 bundled the detection mechanism and the three-valued return type into one item, which is what `IPD-Z602` flagged and which would have needed two unrelated V-items | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-02 now requires a FUNCTION-LOCAL import following the `_projection_dir` precedent, with the cycle measurement and the cost stated; V-02 demands proof of no cycle. The three-way return split into new E-07 with new V-07, clearing the advisory on that item. A Step 0 convention records the import direction, and the over-scope of the new dependency is declared in the scope check |
| PR-608 | MEDIUM | UNDER-SCOPE | E. testing | `tests/test_run_recovery_cli.py:1052-1059` | **E-06's chosen fixture is not a driver run under the corrected detector, so case (a) could not pass honestly.** The class's `setUp` creates `.aw/records/runs/<id>/events.jsonl` and NO `state.json`, but `repair_run` requires `state.json` and refuses that shape at exit 2 (measured). A case-(a) test written against the fixture as-is would assert a suggestion the detector must NOT make, and the likeliest way to make it pass would be to weaken the detector, defeating PR-603's fix. The plan asserted the fixture was ready ("E-05's pin is largely written") without checking it against the behavior it now depends on | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-06 now requires adding `state.json` to the fixture and says WHY, keeps the events-only directory as new case (h) asserting NO suggestion, and warns against "simplifying" the fixture root to `.aw/state/runs/` where `repair` cannot see it. V-05 additionally requires proof every pre-existing assertion in the class still passes with the modified `setUp`. Case count raised from six to eight |
| PR-609 | LOW | IN-SCOPE | Evidence accuracy; A. correctness | `run_cli.py:284` vs `:613`; measured `show --agent` vs `resume --agent`; `run_viewer.py` citations re-resolved | Two accuracy items. FIRST, the human and machine streams already disagree on the `error:` prefix: the three inline copies bake `error: ` into the payload string while `_emit_error` adds it only on the human path, so `show --agent` emits `"error: ledger file not found..."` and `resume --agent` emits `"ledger file not found..."`. Consolidation must pick one, and the plan did not notice there was a choice. SECOND, every `run_viewer.py` citation in the plan had drifted by roughly 120 lines (two commits since `44d4950d`), including the three in the Step 0 conventions; the constructs all exist and the claims are true, so this is citation rot, but the plan leans on those coordinates to tell the executor `repair` is safe to suggest | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | New F-17 records the prefix split with both measured payloads. E-01 now requires reconciling it and stating the chosen convention; V-01 demands the human and agent renderings side by side. All `run_viewer.py` citations re-resolved at `ef1e1fbe` and the drift named in the gate as the reason the RE-LOCATE rule exists. The sibling duplicated string `no run id or ledger path given` (same four sites) folded into E-01 and F-5 |

No finding was DEFERRED, left OPEN, or marked REPLAN, so no escalation to a `- Blocking: yes` question was
required. PR-605 is the one with real potential for durable harm and was fixed in place because the fix is
bounded and mechanically checkable (suggest the target as given, assert no `/home/` segment) rather than
needing a decision.

OQ-01 was RESOLVED at review from measurement, as the question itself directed: `aw runs <id>` does render
a driver run at exit 0, so the message names both the read and the repair action. ONE NEW QUESTION was
raised, OQ-02, NON-BLOCKING and owned by the maintainer: from a subdirectory with no `--dir`, the ledger
readers refuse while `aw runs repair` succeeds, because only the viewer path climbs to the project root.
The plan is correct without deciding it (the detector asks the climbing resolver), but whether the ten
reader leaves should also climb is a resolution-contract change well outside a message fix.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-603: how should the detector decide "this is a driver run", given the two resolvers disagree on three measured axes? | ASK `repair`'s OWN resolver (`resolve_target_runs_detailed`) and additionally require the `state.json` that `repair_run` requires, so the detector cannot suggest a command that then refuses. | (a) Probe `resolve_ledger_path`'s own roots as the plan originally said, rejected because it is measurably wrong on all three axes and produces a suggestion that fails. (b) UNIFY the two resolvers, rejected as far outside a message fix: it changes target resolution for ten leaves plus the viewer and carries its own regression surface. (c) Suggest `repair` unconditionally whenever no ledger resolves, rejected because it reintroduces the confidently-wrong message this plan exists to remove. | measured all three axes at `ef1e1fbe`; `run_viewer.py:2645-2649` (what the `repair` branch actually calls), `:2584-2585`, `:1117-1133`; `project_context.py:314-327` | yes |
| D-2 | PR-604: E-04 asked for a `NextAction`-style field and asserted schema compliance. Add the field how? | EXTEND the module's existing bare-dict payload (`ok`/`error`/`exit_code`) with a suggestion field, and explicitly do NOT convert these payloads to `aw.agent/v1`. | (a) Convert to conformant `aw.agent/v1` records as the plan's wording implied, rejected as a public machine-contract change across 24 `_emit_machine` and 33 `_emit_error` sites that would silently change what existing consumers parse, and not what the backlog item asked for. (b) Put the suggestion only in the message string, rejected because the machine path is what automation reads and prose-only is the gap that does the most damage. Recorded as a STOP condition rather than silently permitted, so an executor who thinks (a) is necessary raises it. | measured 3 schema violations on a current payload; `run_cli.py` imports `agent_schema` zero times; contrast `run_viewer.py:2551-2566` | yes |
| D-3 | PR-602: the shared message must serve six readers and four writers. One wording or two? | Require the wording be TRUE of both directions (name the run model, not the leaf's direction), with a direction parameter permitted, and make the executor state which and why. | (a) Keep "these readers serve a different run model", rejected outright as false on four leaves. (b) Mandate the direction parameter, rejected as over-specifying an implementation the executor can judge better with the code in front of them; what matters is the truth condition, which V-03 now checks. | `run_cli.py:806`, `:910`, `:1048`, `:1120`; `aw run --help` advertises exactly the four writers | yes |
| D-4 | OQ-01 was open with `Owner: executor`: suggest `repair` alone, or also name the read verbs? | RESOLVED: name both `aw runs <id>` (read) and `aw runs repair <id>` (action), nothing else. | (a) `repair` alone, rejected because `repair` MUTATES and an operator who was just refused usually wants to SEE the run first; offering only the mutating verb pushes them toward a write when a read would do. (b) A fuller list of working verbs, rejected on the question's own reasoning (a menu nobody reads). Verified the precondition the question set rather than assuming it. | measured `aw runs <driver-run-id>` rendering the full table at exit 0 at `ef1e1fbe` | yes |
| D-5 | PR-608: the existing fixture lacks `state.json`, which the corrected detector requires. Modify the shared fixture or add a separate one? | MODIFY the class's `setUp` to add `state.json`, and keep the events-only shape as its own new case (h). | (a) Leave `setUp` alone and build a second fixture, rejected because two nearly-identical driver fixtures in one class invite drift and the class is already the documented home for this surface. (b) Weaken the detector to accept `events.jsonl` alone so the current fixture passes, rejected outright: that is defeating PR-603's fix to make a test green, and `repair` would still refuse. Guarded by requiring V-05 to prove every pre-existing assertion still passes with the modified `setUp`. | `tests/test_run_recovery_cli.py:1052-1059`; measured `repair_run` answering `not a run directory` at exit 2 for an events-only directory | yes |

Nothing was rejected in the plan's core reasoning. Its diagnosis, its choice of the backlog item's option 4
over a second resume engine, its refusal to undo `e6b9kt`, and its instinct to consolidate rather than patch
one leaf are all sound and were preserved. What review supplied was four leaves the plan did not know it was
changing, a message wording that was false on those four, the resolver the detector must actually ask, a
privacy constraint on what the suggestion may contain, a machine-contract boundary the plan had mis-stated,
a fixture that would not have supported its own test, and a current map of the suite.
