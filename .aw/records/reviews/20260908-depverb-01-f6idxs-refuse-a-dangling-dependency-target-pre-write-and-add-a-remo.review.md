# Review: refuse a dangling dependency target pre-write and add a remove verb, child f6idxs (Set depverb)

- Subject-Id: f6idxs
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `0a2626c9`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value rests on
RE-RUNNING rather than re-reading. EIGHT things were EXECUTED rather than inspected: a fresh probe repo
was built and the dangling setter was run in all THREE edge forms (not just the plan's one); the same
invocation was run WITHOUT `--dry-run` to see whether it actually writes; `aw ipd dependencies remove`
was invoked to confirm the argparse refusal; the setid-shaped grammar refusal was reproduced verbatim;
BOTH candidate resolvers (`match_selector` and `build_dependency_index` + `_resolve_edge`) were CALLED
in-process against the live tree and compared; the multi-owner id6 census was computed; the setter was
run against `approved` and `reviewed` plans to isolate its side effects; and the bare suite was run.
FOUR of those eight produced findings the plan did not contain, and ONE of them inverts an E-item.

SCOPE: only this child was a candidate. Backlog `rxoazt`, `2k42zu`, `sjsoqq`, the four `setidhard`
plans, and spec `25kzda` were read as EVIDENCE, not reviewed, and are not in the ledger. Also read:
`status_set.py`, `check_engine.py`, `ipd_schema.py`, `cli.py`, `ipd_lint.py`,
`tests/test_runner_item_dependencies.py`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | LOW | IN-SCOPE | Evidence accuracy (the defect is worse than recorded) | probe: three edge forms, with and without `--dry-run` | The reproduction UNDERSTATES the defect. The plan cites `--dry-run` reporting `unchanged (dry-run)` and exiting 0. Measured: the NON-dry-run invocation also exits 0 and genuinely WRITES `- Item-Dependencies: exists:backlog:zzzzzz` into the file, and all THREE edge forms behave identically (`executed:zzzzzz`, `exists:backlog:zzzzzz`, `state:spec:approved:zzzzzz`), each exit 0, each persisted. A dry-run-only citation invites an executor to write a dry-run-only failing test, which would prove less than the defect deserves. Several line citations had also drifted (`run_dependencies_set_command` `:1515` -> `:1489`, its grammar refusal `:1558` -> `:1527-1534`, `evaluate_ipd_dependencies` `:2365` -> `:2394` and its `aw check` call site `:2609` -> `:2638`, `ipd_lint.py:1119` -> `:1147`, `oc_runipd.py:2651` -> `:2562`). | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | F-1 and F-7/F-8 updated with the stronger reproduction and corrected citations; E-01 and E-06 now require asserting the failing-first case against the real WRITE rather than a dry run; the gate lists ten symbols to find by name. |
| PR-002 | HIGH | IN-SCOPE | C. Architecture; A. Correctness (two E-items contradicted each other) | called both resolvers in-process; `check_engine.py:2347`, `:2368`; id6 `uyeko5` | **E-01's INSTRUCTION WOULD HAVE CREATED THE SECOND AUTHORITY E-05 EXISTS TO FORBID.** E-01 says to resolve targets "through the shared selector resolver so the setter agrees with `aw find` and with `aw check`". Measured, `aw check` does NOT use the selector resolver for edges: `evaluate_ipd_dependencies` calls `build_dependency_index` and `_resolve_edge`, a purpose-built edge resolver that (a) enforces the edge's TYPE through `ipd_schema.ITEM_DEP_TYPE_TO_RECORD_TYPE` and (b) returns a THREE-WAY verdict `ok`/`dangling`/`ambiguous`. `match_selector` has neither property. The divergence is REAL: id6 `uyeko5` is owned by one `plans` record and TWO `research` records in this repository, so `match_selector("uyeko5", scoped_type="plans")` returns a single hit (a naive existence check says OK) while the shared index records three owners. So following E-01 would make the setter disagree with `aw check` about the exact class of edge it exists to validate, and E-05 would then have to fail its own plan. The corrected instruction also GAINS behavior the plan never had: an `ambiguous` verdict that must refuse and must not be admitted by the escape hatch. | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added F-11 and a Step 0 convention distinguishing the edge resolver from the selector resolver, with the measured `uyeko5` divergence. E-01 rewritten to use `_resolve_edge` + `build_dependency_index` and to refuse on `ambiguous` too; E-02 scoped to `dangling` only with that boundary required in the flag's help; E-05 states the two instructions previously conflicted and that reusing `_resolve_edge` satisfies both; E-06 gains the ambiguous case; V-01 FAILS a `match_selector`-based implementation and requires a setter-versus-checker agreement paste; Required tests add that agreement proof. |
| PR-003 | MEDIUM | IN-SCOPE | E. Testing (a guard cited as proof cannot see the code) | `tests/test_runner_item_dependencies.py:46-49`, `:1552` | **THE ANTI-DIVERGENCE GUARD E-05 CITES SCANS ONLY THE TWO RUNNER FILES.** `AntiDivergenceGuardTests` iterates `_DRIVER_SOURCES`, which is exactly `oc_runipd.py` and `agy_runipd.py`. Neither `status_set.py` nor `cli.py` is in that tuple, so the guard passes UNCHANGED even if this work introduced a private dependency regex, and E-05's plan to "verify by grep ... mirroring the anti-divergence discipline" conflates mirroring a discipline with being covered by it. Extending the guard also means editing a file NOT in `Scope-Paths`. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-12 and a Step 0 convention. E-05 now requires either extending `_DRIVER_SOURCES` (with the `--scope-reason` named) or writing an equivalent guard in the new test module, and requires the choice be recorded; V-05 is incomplete without one of the two; the Scope check foresees the out-of-fence edit. |
| PR-004 | MEDIUM | UNDER-SCOPE | A. Correctness (three unspecified `remove` behaviors) | `ipd_schema.py:693-698`; `status_set.py:1555` | **THREE BEHAVIORS `remove` NEEDS WERE LEFT UNSPECIFIED, AND ONE WOULD BE A FUNCTIONAL DEFECT.** (a) `remove` must NOT inherit E-01's existence validation: removing an edge whose target has since been deleted is the NORMAL repair case, so a shared validating code path would make a dangling edge unremovable, inverting the item's intent. (b) Edges must be matched in CANONICAL form, because the grammar redirects `state:ipd:executed:<id6>` to canonical `executed:<id6>`, so a raw-string comparison would report a correctly-spelled removal request as an absent edge and E-04 would refuse it. (c) The setter LOOPS over every matched plan and accumulates `rc_final` rather than aborting, so E-04's absent-edge error fires PER PLAN and the all-or-nothing question is inherited by accident rather than decided. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-14 and a Step 0 convention on the multi-match loop. E-03 now states all three: canonical matching, no validation inheritance, and a required decision on multi-match semantics; E-06 gains the dangling-removal and canonical-matching cases; V-03 requires all three pasted. |
| PR-005 | LOW | IN-SCOPE | E. Testing (side effects that break naive assertions) | probe with `approved` and `reviewed` controls | The setter has TWO side effects the plan never mentions: it appends a `## Workflow history` receipt, and on an `approved` plan it REWRITES the `- Approval:` line to "recorded via aw ipd set: set Item-Dependencies to ...". Measured against a control, a direct `aw ipd set approved` rewrites the same line, so this is `apply_status_change`'s shared behavior for every setter rather than a defect of this verb, and it is correctly out of scope. But V-01 as authored offers "`git diff` empty" as proof nothing was written, which would be wrong on any successful path, and a whole-file equality assertion in E-06 would fail on unrelated churn. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-13 and a Step 0 convention; recorded in Deferred as neither caused nor fixed here. E-06 and V-01/V-06 now require assertions scoped to the `- Item-Dependencies:` line, and V-01's "`git diff` empty" option removed. |
| PR-006 | LOW | IN-SCOPE | G. Plan executability (a stale deferral reason) | backlog and `setidhard` front matter | F-6 says GAP 3's prerequisite "`sjsoqq` is `open`". It is now `graduated`, into a live FOUR-PLAN Set (`yku4ga`, `drzbs9`, `bwgyum`, `dw7i3m`; two `reviewed`, two `to-review`) whose Order 03 owns the setid-collision refusal. The deferral still holds, but its reason is now stronger and different: the prerequisite is not merely unlanded, it is actively being designed, so a setid EDGE would be built against a setid IDENTITY four in-flight plans are still defining. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-15; F-6 marked superseded; the GAP 3 deferral and the gate paragraph rewritten with the measured status and the stronger reason. |
| PR-007 | LOW | IN-SCOPE | G. Plan executability (two answerable questions left open) | `ipd_schema.py:739-745`; `check_engine.py:211` | Both open questions were answerable and one was owned by `reviewer`, which is this review. OQ-01 (flag name and whether to require a reason) is a judgement the plan already argued correctly; leaving it open would defer to a reviewer who is here. OQ-02 (`none` versus `unresolved`) is decidable from the parser and the plan told the executor to "confirm" it, which is work a review can simply do: `unresolved` returns `ready=False` while `none` returns `ready=True`, so writing `unresolved` on removal would flip the plan into a not-ready state as a side effect of dropping one edge. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Both marked `resolved` with owners named. OQ-01 keeps the name, requires no reason (with the `--scope-reason` analogy rebutted: two records already exist), and gains the ambiguous-verdict scope correction. OQ-02 confirmed from the parser with the `ready` flag as the decisive evidence. |
| PR-008 | LOW | IN-SCOPE | E. Testing (wrong baseline) | bare suite at review | The baseline cites `1 failed, 5648 passed` with the failure in `test_orchestrator_retirement`. Measured: `1 failed, 5919 passed, 3 skipped, 2 xfailed`, and the failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, caused by 189 untracked `opencode-recovery/*.md` files belonging to another party. An executor expecting the wrong failure could chase a phantom regression or "fix" another party's files. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Baseline corrected in the conventions and Required tests with the environmental cause, a no-added-flags note, and an instruction that it cancels in the delta; the gate forbids touching those files. |

CONFIRMED SOUND, AND THE NARROWING IS THE PLAN'S BEST DECISION. All four gap claims reproduce exactly as
described. GAP 4 fires in all three edge forms. GAP 2's argparse refusal is verbatim as recorded. GAP 3's
refusal is CORRECT behavior with its exact message ("exists target 'worksequence' is not a 6-char base36
id6. Refusing before making changes.", exit 2, nothing written), which is precisely why deferring it is
right rather than lazy. `2k42zu` really is `done`, so the item's own open question really is answered by
events and there is no sibling to merge into. The single-evaluator discipline is real and already spans
four surfaces. The `none`-not-blank reasoning is right and now has a second supporting fact. The
inherited no-op write path is real. And the plan's care in distinguishing `Item-Dependencies` from the
intra-plan `Depends on:`, from `Gate-Kind`/`Gate-Ref`, and from `Blocks-Release` is the kind of
disambiguation that prevents a whole class of executor error.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing about the direction: validate pre-write, add a
loud escape hatch, add a remove verb, keep one authority. The pattern in the revisions is that this plan
identified the right SEAM and the right PRINCIPLE but not the right MECHANISM. It knew the setter must
agree with `aw check` and named the wrong resolver to achieve it; it knew a second parser must not appear
and cited a guard that cannot see the files it was protecting; it knew removal should reuse the existing
write and did not ask what removal must NOT reuse. Each is the same shape: a correct requirement paired
with a mechanism that has not been executed. The second, smaller theme is that a plan whose subject is a
MUTATING verb must characterize everything that verb writes, not only the field it cares about, or its
own tests will fail on the churn.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-002: E-01 names the selector resolver and E-05 forbids a second authority. Which resolver must the pre-write check use? | `check_engine._resolve_edge` plus `build_dependency_index`, the same pair `aw check` uses, and refuse on `ambiguous` as well as `dangling`. | (a) Keep `match_selector` as authored; rejected because measured it diverges from `aw check` on a real id6 (`uyeko5`, one `plans` and two `research` owners), has no type enforcement for edges, and has no ambiguity verdict, so it would make the setter and the checker disagree about the exact class of edge the check exists to validate. (b) Extract a new shared helper both could call; rejected as a refactor of a shipped four-consumer evaluator inside a CLI bug fix, and unnecessary since `_resolve_edge` already is that helper. (c) Refuse only on `dangling` and ignore `ambiguous`; rejected because `_resolve_edge` returns it, `check.ipd-dependency-ambiguous` is a real rule, and silently writing an edge that can never resolve is the same falsehood as writing a dangling one. | `evaluate_ipd_dependencies` calls `build_dependency_index` (`check_engine.py:2347`) and `_resolve_edge` (`:2368`); `_resolve_edge` filters by `ITEM_DEP_TYPE_TO_RECORD_TYPE` and returns `ok`/`dangling`/`ambiguous`; called in-process, `match_selector("uyeko5", scoped_type="plans")` returns 1 hit while the index records 3 owners across 2 record types. | yes |
| D-2 | PR-002/OQ-01: should `--allow-dangling` cover an ambiguous target too? | NO. Scope the hatch to the `dangling` verdict only, and say so in the flag's help text. | (a) Cover both under one flag; rejected because a dangling edge becomes valid when the target is authored (the forward-reference case the flag exists for) while an ambiguous edge cannot become valid by waiting, so one flag would conflate a temporary state with a permanent one. (b) Add a second flag for ambiguity; rejected as inventing an override for a case with no legitimate use, and the plan's fence is a validation fix. | `_resolve_edge` returns the two verdicts distinctly; the forward-reference justification in E-02 applies only to a target that does not exist YET. | yes |
| D-3 | PR-004: must `remove` inherit E-01's existence validation? | NO, explicitly, and test it. | (a) Share one validating path for both verbs; rejected because it would make a dangling edge UNREMOVABLE, which inverts the item's stated purpose (removing an edge whose target was deleted is the normal repair) and would turn a validation improvement into a functional regression. (b) Leave it unstated; rejected because "reuse the same machinery `set` uses" reads as reusing the validation too, which is the likeliest wrong reading. | The item's own framing of `remove` as the alternative to hand-rewriting a statement; E-01's refusal sits in the shared `run_dependencies_set_command` path that E-03 is told to reuse. | yes |
| D-4 | PR-007/OQ-01: require a written reason with `--allow-dangling`, as `--scope-reason` does? | NO reason required; keep the name. | (a) Require a reason by analogy to `--scope-reason`; rejected because that precedent governs a finalize-time reconciliation where the prose is the ONLY record, whereas here two independent records already exist (E-02 requires naming every dangling target in the output, and `check.ipd-dependency-dangling` stays `error` at the gate), so a third would be filler on a routine action. (b) Rename the flag; rejected as churn, and `dangling` is the exact verdict token the resolver returns, which makes the name accurate rather than merely conventional. | `check_engine.py:211` (rule severity); E-02's naming requirement; the repository's own guidance against prose requirements on routine actions. | yes |
| D-5 | PR-007/OQ-02: `none` or `unresolved` when removal empties a statement? | `none`, confirmed from the parser rather than left for the executor to confirm. | (a) `unresolved`; rejected on measurement: `parse_item_dependencies` returns `ready=False` for `unresolved` and `ready=True` for `none`, so writing `unresolved` would flip the plan into a not-ready state as a SIDE EFFECT of dropping one edge, a behavior change nobody requested. (b) An empty value; rejected because the parser tolerates it as an implicit `none` but it is not the canonical rendering, so it would write a non-canonical statement. | `ipd_schema.py:739-745`. | yes |
