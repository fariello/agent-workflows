# Review: add an integrate verb and make resume merge finished lanes instead of re-dispatching them, child rl67b0 (Set integpath)

- Subject-Id: rl67b0
- Subject-Type: ipd
- Reviewed-At: 2026-09-07
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `784f5a31`. `aw ipd lint --phase author` conforming BEFORE semantic review and
`--phase review-finalize` conforming after every revision, so nothing here is structural.

DISCLOSURE: same agent/model authored this plan, so this is a SELF-REVIEW. Its value rests on
EXECUTING the claims rather than re-reading them: calling the integration gate directly with the
arguments the shipped code passes, running the plan's own declared test command against its own
declared test file, and parsing the shim subcommand set out of all three files that hold a copy. Each
of those produced a defect the plan could not have found by re-reading itself, and two of them
(PR-401, PR-402) invalidate evidence the plan treats as its central safety proof.

SCOPE: only this child was a candidate. `cczotj`, `29wvmj`, `6sb3yu`, `51vw4y` were read as evidence,
as were spec `25kzda` and backlog `yocdq4`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-401 | BLOCKER | IN-SCOPE | A. Correctness; B. Security (unvalidated code reaching main); E. Testing | `oc_runipd.py:2070-2085`, `agy_runipd.py:1218-1229`; `oc_runipd.py:2017-2022`; `orchestrate_isolation.py:1054-1075`, `:1160`; direct gate call at review | **THE PLAN'S CENTRAL SAFETY MECHANISM IS INERT, so "route it through the revalidate gate" as written re-verifies NOTHING.** `make_integration_validation_runner` returns `def _runner(...): return True` on BOTH hosts, and `integrate_lane_branch` passes `integration_base_commit=handle.base_commit` for a single-lane `merge_order`, so the gate's stale-base check compares the lane's base to ITSELF and the revalidation step is a constant pass. MEASURED by calling the gate with exactly those arguments and a fabricated diff: `passed: True, status: integrated_passed, revalidation_passed: True`. In the RUN the real correctness signal is `run_suite_check` in the primary checkout before finalize, which a verb calling only `integrate_lane_branch` never reaches. So the plan's own gate paragraph, V-01's "name the gate function and show it was invoked", and the gate section's "if you find yourself calling `git merge` without the gate, stop" would ALL be satisfied by an implementation that lands a stale lane on main having validated nothing, and the executor would paste a green gate result as proof of verification. This is the exact failure the plan calls its second-most-important item, reachable while obeying its letter. | C:Medium; U:Low; S:Medium-High; F:High; Overall:Medium-High | FIXED | Added F-15 with the measurement. E-01 now requires a REAL validation runner plus the suite in the primary checkout and main's CURRENT head as the integration base, and names the consequence of not doing so. V-01's load-bearing paste is now the NEGATIVE (a failing runner makes the verb refuse), with "the gate ran" declared insufficient on its own. E-05/V-05 require a case in which the gate CAN refuse, using the patch point the existing suites already use. The gate section rewritten to name the subtle failure, not just the bare-`git merge` one. Deferral considered and rejected: the fix is bounded (supply a runner and a base the code already accepts as parameters) and the risk of NOT fixing it is unvalidated code on main. |
| PR-402 | HIGH | UNDER-SCOPE | E. Testing; G. Plan executability | `tests/test_cli_conformance_matrix.py:46`; `tests/test_command_surface_declarations.py:37`; `pyproject.toml:169`; `Makefile:29-30`; measured `no tests ran in 1.83s` | **THE PLAN'S VALIDATION COMMAND CANNOT REACH THE TEST FILE IT DECLARES.** `tests/test_cli_conformance_matrix.py` is in Scope-Paths and in the targeted-test list, but it is `pytestmark = pytest.mark.slow` while `addopts` is `-m 'not slow'`, so the BARE `python3 -m pytest` the plan mandates DESELECTS it entirely. Measured: running that file bare reports `no tests ran`. `tests/test_command_surface_declarations.py`, which holds the undeclared-leaf gate a new cli leaf would trip, is `slow` too. So an executor could add an undeclared leaf, run the plan's prescribed validation, see a clean `N passed`, and ship a broken declaration surface. | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added F-13. E-07 and the Required tests section now mandate a SEPARATE `-m ''` run for both files with its result pasted separately, and state that a bare `N passed` is not evidence about the declaration surface. V-02 and V-07 require that paste. Also recorded the SECOND pre-existing failure this exposes (`test_zero_undeclared_parser_leaves`, five `oc profile *` leaves) so the executor does not claim it. |
| PR-403 | BLOCKER | IN-SCOPE | C. Architecture; F. UX; G. Plan executability | plan `Scope` vs E-02's justification; `agent_workflows/cli.py:3481-3541`; `oc_runipd.py:7878-7886`; measured `aw oc stop --help` exit 2 vs `aw oc run stop --help` exit 0 | **THE PLAN SPECIFIES TWO INCOMPATIBLE COMMAND SPELLINGS AND CANNOT BE EXECUTED AS WRITTEN.** `Scope`, E-02, and V-02 all say `aw <host> integrate <id6>` and V-02 demands `aw oc integrate --help` output, which is a HOST-NOUN leaf in `cli.py`. But E-02's entire justification is the implicit-start shim, which lives in the DRIVERS and is only reachable at `aw oc run <token>`, and its cited precedent `stop` is a driver subcommand with NO cli leaf (measured: `aw oc stop` exits 2). The two spellings need different files, different tests, and different amounts of work, and the shim guard E-02 makes mandatory is MOOT under the host-noun reading. An executor choosing the driver spelling would either fabricate the `aw oc integrate --help` evidence V-02 demands or fail its own validation. | C:Medium; U:Medium-High; S:Low; F:High; Overall:Medium-High | OPEN | ESCALATED to new OQ-04 (`Blocking: yes`, `Finding: PR-403`), which mechanically blocks approval. Three options recorded with their real costs and the files each touches; recommend the alias shape (c) if the verb is operator-facing, (a) if the smallest change is wanted. NOT agent-resolvable: it is a naming decision on a shipped public CLI surface, which `plan-review` forbids inventing. E-02, V-02, and the Scope-Paths note now branch explicitly on the answer instead of assuming one. |
| PR-404 | HIGH | UNDER-SCOPE | A. Correctness; G. Plan executability | `oc_runipd.py:1976-1977`, `:2018`, `:6620-6640`, `:4293-4340`; `worktree_lease.py:100-107` | **THE SHARED FUNCTION'S INPUT DOES NOT EXIST IN THE VERB'S PROCESS, and the plan never says how to obtain it.** `integrate_lane_branch` takes a `WorktreeHandle` and reads `handle.base_commit`/`branch`/`path`; in the run that is a LIVE object from `allocate_isolation_worktree`. A fresh `aw ... integrate` process has none. The plan says "locate that lane" with no mechanism, and the obvious guess (`aw/lane/<id6>`) is EXPLICITLY FORBIDDEN by `lane_branch_name`'s docstring because allocation may have attempt-scoped the name, which `mm6wuz`'s three lanes prove happens. So the most likely implementation integrates the WRONG lane, and OQ-02's own premise ("the lane branch name is derived from the id6") is the forbidden assumption. | C:Medium; U:Low; S:Medium; F:High; Overall:Medium | FIXED | Added F-14. E-01 now names `resolve_prior_lane` and the durable fields it reads (`preserved_lane_id`/`preserved_base`/`preserved_branch`, written at `:6620-6640`) plus `inspect_lane`, states the prohibition and why, and requires the executor to say which fields it consumed. E-05/V-05 add an attempt-scoped-lane case asserting the branch was NOT reconstructed, so an implementation that guesses fails a test rather than shipping. |
| PR-405 | HIGH | UNDER-SCOPE | A. Correctness (concurrency); C. Architecture | `tests/test_runner_stop_triggers.py:1019-1027`, `:1042-1067`; `oc_runipd.py:6977-6996`; `worktree_lease.py:182`, `:286-287`, `:433` | **THREE OMISSIONS THAT EACH BREAK SOMETHING SHIPPED, sharing the root cause that the plan reasoned from the two driver files alone.** (i) The implicit-start subcommand set has a THIRD copy, inline in `tests/test_runner_stop_triggers.py`, and `test_the_inline_copy_matches_both_drivers` asserts all three are equal by regexing both driver sources; editing only the drivers FAILS that test, and the file was in neither Scope-Paths nor E-02. (ii) E-03 says integrate "before the requeue", but immediately AFTER the requeue `run_queue` REFUSES the whole resume when any item is indeterminate, returning 1 having started nothing; integrating there would mutate main during a resume the driver is about to decline. (iii) The verb is out-of-band and holds NO run lock, so it can integrate a lane a LIVE driver is still working in; `inspect_lane` already reports `owner_live` and the plan never consumes it. | C:Medium; U:Low; S:Medium; F:High; Overall:Medium | FIXED | Added F-11 (with the measured set value and the do-not-hoist warning both drivers record) and folded (ii)/(iii) into E-01 and E-03. `tests/test_runner_stop_triggers.py` added to Scope-Paths. E-01 now requires refusing a live-owned lane via the existing `owner_live` field with no second liveness probe. E-03 now pins the placement precisely (after `requeue_interrupted`, before `if retry_incomplete:`, skipping indeterminate items) and requires the attempt to be unable to abort the resume. E-06/V-06 assert the indeterminate-suppression case. |
| PR-406 | MEDIUM | UNDER-SCOPE | A. Correctness; G. Plan executability | `worktree_lease.py:146-164`, `:320-336`; `agent_workflows/command_surface.py:1810-1814`; `oc_runipd.py:6554-6580` | Three smaller under-specifications. (a) E-01 says to consume `worktree_lease`'s classification for four refusal cases, but the classifier has FIVE states (the plan omits `STALE`) and does not separate the cases the plan wants: "no commits" is `EMPTY` OR `STALE`, a merely-DIRTY lane with zero commits classifies `HOLDS-WORK`, and "plan not finalized on the lane" is not a lane state at all. (b) A new `cli.py` leaf requires a `CommandDeclaration` in `command_surface.py`, which the plan never names, and two tests fail without it. (c) E-03 says a success must reach "the same state an in-run integration produces" without naming what that path also DOES after integrating: re-resolve `last_plan_path` and attempt `process_backlog_close`. Omitting (c) would strand the backlog close this plan's own final line requires. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-16 and F-12. E-01 now enumerates the five states and says explicitly which cases the classifier cannot answer, including the `commits_ahead > 0` check. E-02 and Scope-Paths now carry `command_surface.py`, conditional on OQ-04. E-03 now names the re-resolve and the backlog close with their anchor, and V-03 requires that paste. |
| PR-407 | MEDIUM | IN-SCOPE | A. Correctness; D. Anti-regression | `oc_runipd.py:6962-6975`; `agy_runipd.py:4250-4264`; `txc9l1` (executed) | **E-04 CONFLATES THE CASE E-03 ALREADY SOLVES WITH THE ONE ACTUALLY LEFT, and its narrowing is over-broad.** A SUCCESSFUL E-03 integration leaves the item `executed`, which is not in the `--retry-incomplete` status set at all, so no narrowing is needed for it; the case E-04 genuinely owns is the REFUSED integration, still `integration-blocked` with its lane intact. Separately, the flag's status set also covers `interrupted`, and a snapshot-only `interrupted` item whose lane holds work is exactly what `txc9l1` (executed) routes to verify-and-continue, so a narrowing keyed on "lane holds work" would silently capture and break it. E-04 also left the held-back item's fate unstated, which would reproduce the "bare resume does nothing" trap one level up. | C:Low; U:Medium; S:Low; F:Medium; Overall:Medium | FIXED | E-04 rewritten to scope itself to the refused case, to key the narrowing on `commits_ahead > 0` rather than status alone, to enumerate the flag's other statuses and forbid capturing the `txc9l1` case, and to require an explicit operator-facing message naming the held-back item and the operator's options. E-06/V-06 add the snapshot-only interrupted item as a second negative. |
| PR-408 | LOW | IN-SCOPE | A. Correctness (stale anchors, unreachable evidence) | measured 2026-09-07 at HEAD `784f5a31` | Five citations stale or imprecise, and one test design flaw. `oc_runipd.py:6913-6936` for the requeue is actually `:6962-6975` (agy's `:4250-4264` never cited); `TERMINAL_STATES` at `:301-313` is `:301-319` (agy's `:362-380` never cited); the `grep '"integrate"'` claim omits the one real repo hit (`ipd_schema.py:1174`, unrelated); the `$39.42` figure was uncorroborated in the plan (now verified against `aw runs --ipd mm6wuz`, which reports `complete Total $39.42`); and the baseline `1 failed, 5612 passed` is stale. SEPARATELY, E-06 as written resumes with NO flag, which cannot distinguish the fix from unchanged code, since a bare resume does not requeue a terminal item today either. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All anchors corrected with the agy counterpart added, the `ipd_schema` hit disclosed so a future grep is not misread as a regression, the cost figure given its corroborating command, and the baseline replaced with the measured `1 failed, 5613 passed, 3 skipped, 2 xfailed in 78.97s`. E-06/V-06 now require the ordering assertion to be made with `--retry-incomplete` PASSED, and the no-turn absence proven by a launcher that fails if invoked rather than inferred. |

CONFIRMED SOUND, and worth stating because most of the plan survives. The core defect is real and
re-verified: no `integrate` verb exists, `integration-blocked` IS terminal on both hosts, the
`--retry-incomplete` branch does flip those items to `queued` with `recovery_next=True`, and
`allocate_worktree` does attempt-scope a `HOLDS-WORK` lane. The orphaning is not hypothetical:
`aw/lane/mm6wuz`, `_attempt2`, and `_attempt3` all still exist in this checkout with registered
worktrees, which is the plan's prediction standing in the tree. The build-A-then-B ordering is right,
the refusal to delete or reclaim a lane is right and consistent with repository policy, the insistence
that the resume path NOT be behind a flag is right, and V-06's "two absences" framing is the sharpest
thing in the plan: it correctly identifies that a test asserting only the integrated outcome would
pass for a paid re-dispatch. OQ-01 and OQ-03 are well-reasoned; OQ-03's rule that the verb must not be
a softer door into main than the runner is exactly the right invariant, which is why PR-401 matters so
much (the door was already softer than the plan believed). OQ-02's conclusion (id6 alone, refuse when
several lanes match) is right, though its stated PREMISE about deriving the branch name is the
forbidden assumption PR-404 corrects.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-401: the gate is inert for a single lane. Fix the shared in-run `make_integration_validation_runner` (the root cause, affecting every integration in every run) or scope the fix to this verb? | SCOPE IT TO THE VERB, and record the in-run defect as out of scope needing its own item. E-01 supplies a real runner and the current main head for its OWN call; the in-run path is left byte-identical. | (a) Fix the shared runner here, rejected: it changes what EVERY integration in EVERY run requires, on both hosts, inside a plan whose stated claim is a new verb; that is a behavior change to the shipped integration gate smuggled into a recovery feature, and it would invalidate this child's own scope argument. (b) Leave the plan's "route through the gate" wording alone and trust the executor to notice, rejected as the measured failure mode: every assertion the plan demands would pass. | `oc_runipd.py:2070-2085` and `agy_runipd.py:1218-1229` (constant `True`); direct gate call returning `passed: True` with a fabricated diff; `oc_runipd.py:3526-3548` (`run_suite_check` is the real in-run signal) | yes |
| D-2 | PR-403 has three viable spellings and I could have picked one and rewritten the plan consistently. Fix or escalate? | ESCALATED as OQ-04 (`Blocking: yes`) with three options, the files each touches, and a recommendation. | (a) Pick the host-noun leaf to match the plan's literal `Scope` wording, rejected: it silently makes E-02's whole shim justification and its mandated guard test moot, so I would be deleting the plan's stated rationale to preserve its phrasing. (b) Pick the driver subcommand to match the cited `stop` precedent, rejected: V-02 demands `aw oc integrate --help`, which that spelling can never emit, so I would be choosing which of the author's two statements to overrule on a PUBLIC CLI surface. (c) Note it without escalating, rejected because it fails OPEN: the executor hits the contradiction only after implementing one side. | `plan-review.md:228-229` (do not invent decisions requiring the human); measured `aw oc stop --help` exit 2 vs `aw oc run stop --help` exit 0; `cli.py:3481-3541` (host-noun leaves) | yes |
| D-3 | PR-402: the slow-marker gap means the plan's declared validation cannot see two of its declared test files. Widen the mandated command for the whole suite, or add a targeted second run? | ADD A TARGETED `-m ''` RUN for the two files, pasted separately, and keep the BARE run as the primary. | Switching the plan's mandate to `make test-all` wholesale, rejected: `AGENTS.md` is emphatic that the suite is run BARE and enumerates the flags not to add; replacing that with `-m ''` everywhere would contradict the repository contract and slow every future validation for a gap that affects two files. | `pyproject.toml:169`; `AGENTS.md` (run the suite bare); measured `no tests ran in 1.83s` | yes |
| D-4 | Does this child's `Item-Dependencies: executed:51vw4y` survive, given `51vw4y` is itself `no-go` on its own blocking OQ-04? | KEPT. The edge is genuine (the verb reuses the re-attempt path the ladder establishes in the shared module) and the runner re-checks dependencies at dispatch, so a blocked prerequisite blocks this child correctly rather than silently. | Dropping the edge so this child can land first, rejected: it would mean writing the re-attempt logic twice into the two highest-contention files, which is the exact cost child 02's extraction exists to avoid. Noting it as a risk only, rejected as unnecessary: the mechanism already fails closed. | `51vw4y` `- Status: reviewed` / `- Readiness: no-go` with its own `Blocking: yes` OQ-04; `AGENTS.md` (dependencies re-checked at dispatch) | yes |
| D-5 | PR-401 also revealed that the IN-RUN integration gate's revalidation is a no-op, which is a live defect in shipped code, not a plan defect. Where does it go? | RAISED TO THE MAINTAINER IN THIS REVIEW and recorded in the plan's Deferred section as needing its own item; NOT filed as a backlog record, since `.aw/records/backlog/` is outside this plan's fence and this workflow edits planning documents only. | Filing the backlog item myself, rejected: `plan-review` is scoped to the reviewed plan and its record, and creating an unrelated tracked artifact mid-review is a side effect the maintainer did not ask for. Saying nothing, rejected: it is a real weakening of a shipped safety gate and the measurement is in hand. | `plan-review.md:5-7` (planning documents only); `oc_runipd.py:2070-2085`; grep of `.aw/records/backlog/` finds no existing item | yes |

## Round 2

Finding-disposition round recording a MAINTAINER RULING, not a re-review. PR-403 was escalated to the plan's OQ-04 and answered on 2026-09-07; the answer landed in the PLAN (`dec9c629`) while this record still said `OPEN`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-403 | BLOCKER | IN-SCOPE | C. Architecture; F. UX; G. Plan executability | plan OQ-04 `- Status: resolved`; commit `dec9c629`; measured `aw oc stop --help` exit 2 vs `aw oc run stop --help` exit 0 | SUPERSEDES ROUND 1's PR-403. The plan really did specify two incompatible command spellings: Scope/E-02/V-02 said `aw <host> integrate` (a `cli.py` host-noun leaf, and V-02 demanded `aw oc integrate --help` output) while E-02's justification rested on the implicit-start shim reachable only at `aw <host> run <token>`. The maintainer chose OPTION (c), BOTH: the driver subcommand is the implementation and a THIN `cli.py` alias delegates to it. This resolves rather than picks a side, since V-02 can now paste real leaf help output AND the shim guard test stays meaningful. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Resolved by maintainer ruling, option (c), committed in `dec9c629`. Every conditional the answer settled was debranched in the same change: the `cli.py` leaf plus its `CommandDeclaration` are now unconditional, the driver-only `--scope-ack` branch is marked DEAD, V-02 demands four help outputs and four working invocations, and a thin-alias proof requires lane resolution, the gate call, and the backlog close to appear exactly once. |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-5 | Option (c) is the most work of the three. Record it as `FIXED` when no code is written yet? | YES, `FIXED` refers to the PLAN DEFECT (two contradictory spellings), which is fully repaired. | (a) Leave it `OPEN` until the code lands, rejected: it conflates a plan defect with an unimplemented plan, and would block the Set's review sweep forever since every unexecuted plan is trivially 'not yet done'. (b) Mark `DEFERRED`, rejected: nothing was postponed, the spelling question was decided. | plan Scope/E-02/V-02 now consistent at both spellings; `dec9c629` | yes |

## Round 3

Reviewed at HEAD `b42bc05c`, 2026-09-08. `aw ipd lint --phase author` conforming BEFORE semantic review
and `--phase review-finalize` conforming after every revision, so nothing here is structural.

DISCLOSURE: same agent/model authored this plan and its two earlier review rounds, so this is a
SELF-REVIEW of a self-review. Its value rests entirely on EXECUTION rather than re-reading: this round
ran the gate in a real throwaway repository with main advanced past the lane base, ran the plan's own
declared test commands, AST-checked the module the plan writes into, and evaluated the shipped
declaration classifier. Two of this round's findings (PR-410, PR-412) invalidate INSTRUCTIONS THAT
ROUND 1 ITSELF ADDED, which is the strongest available argument that re-reading a review is not a
review.

SCOPE: only this child was a candidate. Read as evidence: `runner_shared.py`, `oc_runipd.py`,
`agy_runipd.py`, `worktree_lease.py`, `orchestrate_isolation.py`, `command_surface.py`, `cli.py`,
`tests/test_runner_shared.py`, `tests/test_runner_stop_triggers.py`,
`tests/test_command_surface_declarations.py`, `tests/test_cli_conformance_matrix.py`,
`tests/conformance_matrix.py`, spec `25kzda`, backlog `yocdq4`/`ciesaj`, and sibling plans `51vw4y`,
`daexj1`, `pr5b0t`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-409 | BLOCKER | IN-SCOPE | A. Correctness; C. Architecture; G. Plan executability | `tests/test_runner_shared.py:926-970`; `oc_runipd.py:3631`; `agy_runipd.py:320`; `runner_shared.py:107`, `:959-996` | **E-01's MANDATED SUITE RUN IS UNREACHABLE FROM THE MODULE E-01 PUTS IT IN, and a SHIPPED TEST ENFORCES THAT.** E-01 places the shared re-integration function in `runner_shared.py` and requires it to run the suite in the primary checkout, but `run_suite_check` is defined in `oc_runipd.py` (agy only re-exports it) and `NoRunnerImportTests::test_runner_shared_imports_neither_runner` AST-walks the shared module and FAILS on any `import`/`from ... import` whose name contains `runipd`, at module level OR lazily inside a function; a sibling test re-imports the module standalone in a fresh interpreter. So the obvious implementation fails a test, copying the body forks the fail-closed exit-124/127 reading, and moving the symbol is out of fence and collides with `daexj1` E-03. The plan named neither the constraint nor the mechanism, and the mechanism it needs is the one THIS VERY FUNCTION already uses for `run_checked`/`host_label`. An executor would discover this only after writing the whole function. | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added F-17. E-01 now requires the suite check to be an INJECTED parameter with each host's thin wrapper binding its own, forbids the import and the copy, and names the AST test. V-01 requires the parameter name, both wrappers, and `NoRunnerImportTests` pasted passing; E-05/V-05 add that test to the targeted run; the scope fence forbids the import explicitly. |
| PR-410 | BLOCKER | IN-SCOPE | A. Correctness; B. Security (a diff that reverts main) | `orchestrate_isolation.py:1054-1075`; `worktree_lease.py:565-572`; measured gate calls in a throwaway repo | **ROUND 1's OWN INSTRUCTION "PASS THE CURRENT MAIN HEAD AS THE INTEGRATION BASE" IS UNBUILDABLE IN BOTH DIRECTIONS, and one direction silently proposes reverting main.** `stale_base_check` compares the FIRST lane outcome's own `base_commit` to whatever `integration_base_commit` the caller passed: it is a consistency assertion on two caller-supplied arguments, not a freshness test against main, and no argument makes it into one. MEASURED in a real repo with main one commit ahead of the lane base: (a) passing main's head with the outcome built at the lane base returns `False / integration_failed_stale_base`, so the verb refuses EVERY recovered lane and is useless; (b) rebuilding the `LaneOutcome` against main's head passes `True / integrated_passed`, but its combined diff contains main's own intervening file as a DELETION (`m.txt deleted`), i.e. the revalidated diff REVERTS main. Arm (b) is exactly the mis-attribution `worktree_lease`'s `LANE_STALE` rationale forbids by name. Round 1 hedged ("if the check now fires for every recovered lane the verb is useless, so state what you chose") and the measured answer is that it does. | C:Medium; U:Low; S:Medium-High; F:High; Overall:Medium-High | FIXED | Added F-19 with both measured arms. E-01's instruction REVERSED: pass the lane's own declared base exactly as the in-run path does, and get freshness from the suite run plus the real validation runner. F-15 amended to record that its prescribed remedy is superseded while its load-bearing half survives. V-01 now forbids pasting main's head as the base. The gate section carries an explicit "do not try to fix this by moving the integration base" paragraph naming the self-contradiction, and the scope fence forbids both arms. A real base-freshness gate is deferred with a reason. |
| PR-411 | HIGH | IN-SCOPE | C. Architecture; F. KISS/UX; G. Plan executability | `command_surface.py:1545-1566` vs `:1523-1531`; `tests/test_command_surface_declarations.py:117-123`; spec `25kzda:156`; measured `required_scenarios` | **THE PRESCRIBED `CommandDeclaration` SHAPE IS WRONG FOR A THIN ALIAS AND CONTRADICTS E-02's OWN NEXT PARAGRAPH.** E-02 said `command_class="mutation"` "following the `oc runipd` row", but the shipped precedent for a host-noun alias forwarding to the driver is `oc review`/`agy review`: `command_class="alias"`, `empty_error_renderer="delegated"`, `canonical_command="<host> runipd"`. That pairing is TEST-ENFORCED (`test_empty_error_renderer_classification_consistency` requires every alias to declare `delegated`) and the row's own comment calls the class "load-bearing rather than cosmetic". A `mutation` row is also strictly more work: measured with the shipped `required_scenarios`, it demands the extra `success_preview` scenario (7 vs 6). So the prescription asserted an independent contract for a pure delegation, contradicting E-02's "KEEP THE ALIAS THIN" two paragraphs later and spec 2.1's rule that an operator-visible difference in an alias is a defect. | C:Low; U:Medium; S:Low; F:Medium; Overall:Medium | FIXED | Added F-18. E-02 now prescribes `command_class="alias"` + `empty_error_renderer="delegated"` + `canonical_command`, one row per host, following the review rows, with the measured scenario-count difference stated. V-02 requires the pasted declaration to read `alias` and declares a `mutation` row a FAILED validation even under a green suite, and requires the classification-consistency test pasted. Also recorded that the live `AliasEquivalenceTests` gate covers a curated set of READ-ONLY leaves and must NOT gain a main-mutating verb. |
| PR-412 | HIGH | IN-SCOPE | A. Correctness (ordering, concurrency); G. Plan executability | `oc_runipd.py:7223`, `:7225-7247`, `:7254-7266`; `agy_runipd.py:4249`, `:4251-4272`, `:4278-4290` | **ROUND 1 ADDED TWO PLACEMENT CONSTRAINTS TO E-03 THAT ARE MUTUALLY EXCLUSIVE.** It required the integration attempt to sit "after `requeue_interrupted`, before the `if retry_incomplete:` block" AND "after the indeterminate refusal" -- but the refusal is emitted AFTER the requeue block, not before it (measured on both hosts). An executor obeying the literal placement puts the attempt BEFORE the refusal, which the SAME paragraph forbids as mutating main during a resume the driver is about to decline. Worse, the correct placement (after the refusal) has two consequences the plan never states: `--retry-incomplete` has by then already rewritten the item's `status` to `queued` with `recovery_next=True`, so selecting on `status in {integration-blocked, merge-conflict}` finds nothing, and a successful integration must UNDO that flip or the requeue re-dispatches work that just landed. Separately, "place it inside the run lock" is already unconditionally true (`run_queue` is only entered under `locked_run`), so as written it invites a redundant second acquisition. | C:Medium; U:Low; S:Medium; F:High; Overall:Medium | FIXED | Added F-20 with the measured order on both hosts. E-03 rewritten: placement is AFTER the refusal, the conflict is named as a correction to an earlier round, selection must read DURABLE LANE FACTS rather than a status the flag may have erased, success must clear `recovery_next`, and the alternative arrangement (narrow the requeue first, integrate before it) is permitted but must be chosen once and stated. The run-lock paragraph now says the constraint is satisfied automatically and forbids a second acquisition. E-04 gained a cross-reference so the two items cannot assume different orders. V-03 now forbids pasting the non-existent three-way ordering claim. |
| PR-413 | MEDIUM | UNDER-SCOPE | C. Architecture; D. Anti-regression | `.aw/records/plans/pending/20260908-integearn-03-daexj1-...ipd.md:7`, `:50`; backlog `ciesaj` | **THE PLAN'S DEFERRED SECTION SAYS THE CONSTANT-`True` RUNNER "NEEDS ITS OWN ITEM", BUT THAT ITEM EXISTS AND IS ALREADY GRADUATED INTO A REVIEWED PLAN.** `daexj1` (`integearn-03`, from backlog `ciesaj`, `reviewed`/`no-go`) declares `runner_shared.py`, `orchestrate_isolation.py` AND both drivers in `Scope-Paths`, and its E-03 is verbatim the fix this plan defers: "Fill the revalidation stub ... Supply a real runner that executes the bare suite against the merged tree." That is simultaneously file contention and SEMANTIC overlap with E-01's injected runner: whichever lands second must consume rather than fork, and the plan gave the executor no warning at all. Round 1's D-5 decision ("raised to the maintainer, not filed, since backlog is outside the fence") was reasonable at the time but is now simply out of date. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-21. The Deferred entry rewritten to name `daexj1` as the owner, instruct the executor to check which landed first and CONSUME rather than fork, and mark the "needs its OWN item" note superseded. A Step-0 convention and a Scope-check note carry the same warning; the scope fence forbids changing the in-run runner here. |
| PR-414 | MEDIUM | IN-SCOPE | A. Correctness (stale anchors); E. Testing (stale baseline) | measured 2026-09-08 at HEAD `b42bc05c` | **THIRTY-THREE LINE ANCHORS WENT STALE IN ONE DAY, and the plan's stated pre-existing failure is no longer the one that fails.** Round 1 re-measured every anchor at HEAD `784f5a31`; at `b42bc05c` (one day later) 33 of them had moved, including `TERMINAL_STATES` (`:301-319` -> `:317-335`), the requeue branch (`:6962-6975` -> `:7225-7247`), the shim set literal (`:7878-7886` -> `:8181-8189`), `run_suite_check` (`:3526-3548` -> `:3631`), `resolve_prior_lane` (`:4293-4340` -> `:4408-4455`) and the preserved-field writes (`:6620-6640` -> `:6890-6900`). Separately the declared BASELINE is wrong in both halves: the bare run now gives `1 failed, 5919 passed, 3 skipped, 2 xfailed` and the failure is `test_reporting_contract::ParityTests::test_only_expected_files_contain_the_full_contract_prose` (189 files under the gitignored local `opencode-recovery/`, environmental and possibly absent elsewhere), while the round-2 failure `test_orchestrator_retirement::...test_runprofile_refuses_for_R2...` now PASSES (`112 passed` for that file). An executor trusting the plan would report a passing test as a known-red and be surprised by a different failure. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All 33 anchors corrected. The baseline replaced with the round-3 measurement, the environmental nature of the new failure stated, and the round-2 failure explicitly retired as now-passing. The RE-LOCATE-BY-SYMBOL paragraph upgraded from prudence to a measured fact (33 anchors in one day, with four examples). Required tests and V-07 now instruct the executor to measure its OWN baseline rather than quote the plan's. |
| PR-415 | LOW | IN-SCOPE | G. Plan executability (a stale gate that contradicts its own resolved question) | plan gate section vs OQ-04 `- Status: resolved` | The approval gate still read "**OQ-04 is OPEN and `Blocking: yes`**, so the pre-execution gate stops this plan until the maintainer settles the verb's spelling", while OQ-04 has read `- Status: resolved` since the 2026-09-07 maintainer ruling that the same section's E-02 already implements. A gate paragraph that asserts a blocking question the plan no longer has is the kind of stale text an approver reads first. The dependency note was also stale in the reader's favor: it described `51vw4y` as gating, without saying that `51vw4y` has since moved from `no-go` to `go-pending-approval`. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Gate rewritten: all four questions resolved, nothing awaits a human decision except approval itself, and the earlier OPEN statement marked superseded. The dependency note now records `51vw4y`'s current `reviewed`/`go-pending-approval` state and that the edge still correctly gates because it is not yet `executed`. Also refreshed the Concern's re-verification date and added the round-3 lane measurement (29 branches, four attempt-scoped clusters) so the defect is not read as historical. |

CONFIRMED SOUND, and stated because the plan's core survives a third round intact. Re-verified at
`b42bc05c`: no `integrate` verb exists on either host or in the CLI (`aw oc integrate --help` exits 2),
`integration-blocked` IS in both hosts' `TERMINAL_STATES`, the `--retry-incomplete` branch DOES flip it
to `queued` with `recovery_next=True`, `allocate_worktree` DOES attempt-scope a non-`ABSENT` lane, and
the orphaning is still accumulating in this checkout (29 `aw/lane/*` branches, four attempt-scoped
clusters). The build-A-then-B ordering remains right; the refusal to delete or reclaim a lane remains
right; the insistence that the resume path not be flagged remains right; V-06's "two absences" framing
remains the sharpest thing in the plan. F-11 (the third copy of the shim set), F-12 (the declaration
requirement), F-13 (the slow-marker gap), F-14 (the lane identity is persisted, not derivable) and F-16
(the classifier's five states) were each re-verified this round and all five hold exactly as written.
The constant-`True` runner also holds, and the negative arrangement V-01 demands is measurably
reachable (`lambda: False` -> `integration_failed_combined_red`).

WHAT THIS ROUND CHANGES ABOUT HOW TO READ THIS PLAN. Two of the six substantive findings are defects in
REVIEW OUTPUT, not in the author's original text: PR-410 and PR-412 both correct instructions round 1
ADDED. Both were caught the same way, by executing the claim instead of re-reading it, and neither was
visible to inspection: the stale-base instruction reads entirely plausible until you call the gate with
both argument shapes, and the placement instruction reads entirely plausible until you note which of
two code blocks comes first. That is the case for the workflow's "verify claims from repository
evidence" rule applying to a reviewer's own prior rounds as much as to the author's prose.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-6 | PR-409: the suite run must happen but `runner_shared` may not import a driver. Inject it, move `run_suite_check` into the shared module, or relocate the new function into the drivers? | INJECT it as a parameter, with each host's thin wrapper binding its own, exactly as `integrate_lane_branch` already binds `run_checked` and `host_label`. | (a) Move `run_suite_check` into `runner_shared`, rejected twice over: it is outside this plan's declared intent (a new verb, not a re-home) and `daexj1` E-03 is already changing that exact surface, so two plans would move and rewrite one function. (b) Define the new function in each driver instead of the shared module, rejected: that is the two-implementations drift child 02's extraction exists to end, and it is what `cnwy8g` tracks as the 40-symbol coupling. (c) Copy the body into the shared module, rejected: it forks the fail-closed reading of exit 124/127, which is the part of `run_suite_check` that makes a gate that cannot run into a refusal rather than a pass. | `tests/test_runner_shared.py:926-970` (the AST guard); `runner_shared.py:107` ("no import of a caller"), `:959-996` (the shipped injection pattern); `daexj1` `Scope-Paths` + E-03 | yes |
| D-7 | PR-410: round 1's own instruction is measurably wrong. Reverse it on my own authority, or escalate the base question to the maintainer? | REVERSE IT IN PLACE and record the measurement. Pass the lane's declared base, exactly as the shipped in-run path already does. | (a) Escalate as a blocking question, rejected: there is nothing for a human to decide. Both alternatives were EXECUTED and both fail on their own terms (one refuses every lane, the other revalidates a diff that deletes main's files), and the correct behavior is simply what the shipped code already does, so this is a measurement, not a judgement call. (b) Leave the instruction and note the risk, rejected as the measured failure mode: an executor obeying it either ships a verb that always refuses or one that proposes reverting main, and every assertion the plan demands would still be pasteable. (c) Also fix `stale_base_check` to be a real freshness test, rejected: that changes what EVERY in-run integration requires, on both hosts, inside a plan whose claim is a new recovery verb -- the same reason round 1's D-1 scoped the runner fix to this verb. | measured gate results (a) `integration_failed_stale_base`, (b) `integrated_passed` over an `m.txt deleted` diff; `orchestrate_isolation.py:1054-1075`; `worktree_lease.py:565-572` (the mis-attribution rationale) | yes |
| D-8 | PR-413: `daexj1` overlaps E-01 semantically and on four files. Add an `Item-Dependencies` edge to it, or record the convergence as guidance? | RECORD IT AS GUIDANCE (check which landed first, consume rather than fork) and do NOT add a dependency edge. | (a) Declare `Item-Dependencies: executed:daexj1`, rejected: `daexj1` is `reviewed`/`no-go` with its own open question, so the edge would gate this plan behind an artifact that cannot currently execute, and this plan does NOT need `daexj1`'s outcome -- it needs only to not fork it, which is a convergence obligation rather than a prerequisite. (b) Say nothing, rejected: the executor would build a second real validation runner in the same module, which is precisely the drift this Set exists to end. | `daexj1` `- Status: reviewed` / `- Readiness: no-go` with `- Blocking: yes` OQ-01 open; its `Scope-Paths` and E-03; `AGENTS.md` (the runner isolates worktrees and revalidates on merge, so file overlap is not a runtime hazard) | yes |
| D-9 | PR-414: the round-3 bare-run failure is environmental (a gitignored local dump this machine happens to hold). Record it as the baseline, or omit it? | RECORD IT, explicitly LABELLED environmental and possibly absent elsewhere, and instruct the executor to measure its own baseline rather than quote the plan's. | (a) Record it as a flat "pre-existing failure, not yours" like round 2 did, rejected: round 2 did exactly that for a test that now PASSES, and the executor would then carry a stale known-red for a second time. (b) Omit it and say only "measure your own", rejected: an executor who sees an unexplained red in `test_reporting_contract` deserves to know it has been seen and diagnosed. | measured `1 failed, 5919 passed, 3 skipped, 2 xfailed`; `tests/test_reporting_contract.py:639-666` (skip list omits `opencode-recovery/`); `.gitignore:49`; `tests/test_orchestrator_retirement.py` now `112 passed` | yes |
