# Review: make a spec or backlog selector reachable for the plan action, child iuxtjy (Set graduate)

- Subject-Id: iuxtjy
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: no-go

## Round 1

Reviewed at HEAD `b07058b7`. `aw ipd lint --phase author` CONFORMING before semantic review. After
revision, `--phase review-finalize` reports exit 1 with exactly two `IPD-Q501`, both being the two NEW
`Blocking: yes` questions this round added; that is the gate working as designed, not a structural
defect, and it is the same disposition sibling `drzbs9` carries for the same reason.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value therefore rests
on EXECUTING the plan's claims rather than re-reading them. TWELVE things were run rather than inspected:
`ACTION_IMPLEMENTED` and the live `--action plan` refusal on both hosts; `discover_specs`' coverage and
its full consumer CHAIN (both hops); `action_for`/`determine_action` over every Kind x status pair;
`enforce_requested_action` with `ACTION_IMPLEMENTED` mutated to include `plan`; `expand_selectors` over
every discoverable spec id6 and all 174 backlog id6s; `build_dynamic_manifest` on a throwaway repo
holding one approved spec and zero plans; the ordering of the pre-flight seams inside `initialize_run`;
both hosts' `--full-auto` parser defaults; the oc-to-agy import set by AST; the `From-*` cluster census
across plans and specs; the four pinning tests under a widened frozenset; and the full bare suite. NINE
of the twelve contradicted the plan.

SCOPE: only this child was a candidate. Read as evidence: `runner_shared.discover_specs` (`:1321-1379`),
`sweep_review_candidates_for_type` (`:1381-1431`), `describe_unresolved_plan_selector` (`:1505`),
`determine_action`/`action_for` (`:3066-3093`), `enforce_mixed_type_gate` (`:2209`),
`refuse_unimplemented_run_flags` (`:2108`); `oc_runipd.expand_selectors` (`:2254`),
`build_dynamic_manifest` (`:2224`), `ACTION_CHOICES`/`ACTION_IMPLEMENTED` (`:2425`),
`enforce_requested_action` (`:2428`), `initialize_run` (`:2732`) and its queue loop;
`agy_runipd` `:1716-1767` and `:1890-1910`; `run_selection_policy` `_SPEC_ACTIONS`/`_BACKLOG_ACTIONS`
(`:140-160`) and `_action_for` (`:323`); `check_engine._iter_spec_records` (`:1900`),
`find_from_backlog_artifacts` (`:1947`), `_type_dirs` (`:426`); spec `25kzda` §1.3, §2.1, §2.3, §2.6,
§3.3, §3.4, §4.8, §4.9; sibling `jxxec8`; and pending plan `lyo1tz` on the import count.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-701 | BLOCKER | IN-SCOPE | G. Executability; C. Architecture (the plan's central instruction misdescribes the work) | `discover_specs(repo)` -> 10 of 29; `grep -rn sweep_review_candidates_for_type agent_workflows/` -> definition only; `build_dynamic_manifest` on a throwaway repo with one spec and no plans -> `plans == {}`, `m['plans']['aaa111']` raises `KeyError`; `expand_selectors`/queue-loop sources read | **"CONSUME `discover_specs`" IS NOT THE MECHANISM, AND FOLLOWING E-02 LITERALLY PRODUCES A `KeyError` RATHER THAN A FEATURE.** `discover_specs` returns a bare id6 -> `SpecRecord` mapping; it is not a selector resolver, it does not feed `build_dynamic_manifest`, and it has no relation to `expand_selectors`. Its ONE consumer (`sweep_review_candidates_for_type`'s `spec` branch) has ZERO non-test callers in the package, so the enumeration this plan was built to consume feeds a helper nothing calls, and "wire into the existing consumer" reaches no operator-visible surface. The real path crosses FOUR seams: `expand_selectors` returns id6 strings whose only meaning is a key into `manifest["plans"]`; the `--action` preflight reads `manifest["plans"].get(id6, {})` and silently defaults a missing status to `approved`; `enforce_requested_action` compares against `action_for`; and the queue loop subscripts `manifest["plans"][id6]` BARE. BLOCKER because the plan's Scope, Goal, E-02 and OQ-01 all rest on a size claim that is wrong by roughly a factor of four, so the plan as written cannot be executed to completion and its Set was sized on the same error. | C:Medium; U:Low; S:Low; F:High; Overall:Medium | FIXED | E-02 rewritten around the four measured seams with each one named and its measurement pasted; the "`discover_specs` is an input, not the mechanism" correction leads the item. Scope narrowed to say so explicitly. E-01 now requires measuring all four seams and the TWO-HOP consumer chain before any code. V-02 requires an end-to-end resolution through the real entry point with no traceback, and E-06/V-06 add an eighth case asserting the queue seam raises no `KeyError`. F-11 added BLOCKER. The residual scope question is escalated as blocking OQ-03, since whether this is still one plan is the maintainer's call. |
| PR-702 | BLOCKER | UNDER-SCOPE | A. Correctness; B. Safety of a legality gate | `action_for`/`determine_action` over every Kind x status pair -> `{execute, review, orchestrate}`; `'plan' in getsource(...)` False for both; both frozensets mutated to `{"review","plan"}` then `enforce_requested_action("plan", [("25kzda","approved","execute")])` | **THE LEGALITY DERIVATION CANNOT RETURN `plan`, SO WIDENING `ACTION_IMPLEMENTED` ALONE REPLACES A CLEAR REFUSAL WITH AN INCOHERENT ONE.** For an `approved` spec `action_for` derives `execute`, and `enforce_requested_action` refuses on `derived != action`. Reproduced: the message reads "--action review is illegal for 1 selected item(s): 25kzda (status 'approved' -> action 'execute')", naming `review` when `plan` was requested, because the refusal text hardcodes `review` as a literal. The plan's own scope fence ("Do NOT change `determine_action`, `action_for`, or any other legality rule") therefore forbade the one change that makes the feature possible, so the plan was internally unsatisfiable. The table spec 2.6 describes ALREADY EXISTS as data in `run_selection_policy` (`_SPEC_ACTIONS: approved -> ACTION_PLAN`, `_BACKLOG_ACTIONS: open -> ACTION_PLAN`, read by the private `_action_for`), which the plan never cites. | C:Medium; U:Low; S:Medium; F:High; Overall:Medium-High | FIXED | The unsatisfiable fence is REPAIRED and narrowly relaxed: a per-type derivation may be ADDED consuming `run_selection_policy`'s existing table, never forking a third, and `action_for`/`determine_action` must keep their current return value for every input they handle today, proven with a before/after table. `run_selection_policy.py` added to `- Scope-Paths:` with an explicit prohibition on editing the two spec-transcribed dicts. E-05 now owns the hardcoded-`review` fix. F-12 added BLOCKER. The API decision (making `_action_for` public) is NOT mine and is escalated as blocking OQ-04. |
| PR-703 | HIGH | UNDER-SCOPE | A. Correctness (a live mis-resolution on the plan's own headline example) | `expand_selectors(m, ['25kzda'])` -> `['wenmg4']`; run over all discoverable spec id6s and all 174 backlog id6s | **FOUR SPEC id6s AND TWO BACKLOG id6s ALREADY RESOLVE TO A PLAN TODAY, SO A SPEC BRANCH APPENDED AFTER THE SUBSTRING FALLBACK PLANS ABOUT THE WRONG ARTIFACT.** Because the convention slugs a plan `...adopt-spec-<id6>`, `expand_selectors`' last-resort filename-substring match wins: `25kzda` -> `wenmg4`, `77tr3o` -> `84j8d7`, `7ckptx` -> `h0zljh`, `c4gd2h` -> `zpbx7o`; backlog `8t5ghs` and `ackme8` likewise, and `vwios6` matches four plans. The plan says nothing about precedence, so the most natural implementation is silently wrong on `25kzda`, the very example the plan and its backlog item both lead with, and every test the plan specifies would pass. | C:Low; U:Medium; S:Low; F:High; Overall:Medium | FIXED | E-02 now requires the spec branch to PRECEDE the substring fallback, with the six colliding tokens enumerated. E-06 adds a seventh required case (a fixture in the live tree's exact shape) and V-02 FAILS the plan if `25kzda` resolves to `wenmg4`. F-13 added HIGH. OQ-05 records the blast-radius question and the escalation condition (any in-tree invocation that DEPENDS on the current answer). |
| PR-704 | HIGH | UNDER-SCOPE | E. Testing (undeclared paths that go red on the change) | `ACTION_IMPLEMENTED` mutated on both hosts -> `3 failed, 14 passed`; nodes at `tests/test_oc_runipd.py:3673`, `:3700`, `tests/test_agy_runipd_cli.py:958`, `:972` | FOUR TESTS PIN THE CURRENT REFUSAL AND NEITHER FILE WAS DECLARED. `test_action_choices_are_the_spec_vocabulary` asserts `ACTION_IMPLEMENTED == frozenset({"review"})` exactly, its agy twin asserts the two hosts agree, and both `plan and execute refuse honestly` tests assert the `not implemented` string. So the first correct step of this plan turns the suite red in files the fence forbids touching, and an executor obeying the fence would either abandon the change or edit undeclared paths and discover it only at `aw ipd finalize`'s scope reconciliation. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Both test files added to `- Scope-Paths:` with scope narrowed to those four assertions only. E-01 requires naming the pinning nodes up front, E-06 requires updating them deliberately, and the required-tests section now says to expect exactly these failures and to distinguish them from the environmental one BY NODE ID. |
| PR-705 | MEDIUM | IN-SCOPE | F. UX / prevent silent failure | both drivers' `f"--action review is illegal for ..."`; reproduced at review | THE REFUSAL TEXT HARDCODES `review` WHILE INTERPOLATING THE DERIVED ACTION, so the moment any other action is requestable the message misreports what the operator asked for. This is the exact failure the existing "refuse honestly rather than silently accepting" design was written to avoid, reintroduced one layer down. | C:Low; U:Medium; S:Low; F:Low; Overall:Low | FIXED | E-05 now owns the fix explicitly ("with the widening, not after it"), its expected outcome names it, and V-05 requires pasting both hosts' refusal texts naming the requested action. |
| PR-706 | MEDIUM | IN-SCOPE | A. Correctness (a host-divergence claim that is false) | both preflight call-site lines diffed; both `build_parser().parse_args(['start','xxxxxx']).full_auto` -> False; `agy_runipd.py:1730` docstring | THIS PLAN'S HOST-DIVERGENCE PREMISE IS FALSE AND THE REAL DIVERGENCE IS IN THE COMMENTS. E-05 said `oc` derives via `action_for` and `agy` via `determine_action`. Measured, both call sites are the byte-identical `preflight_items.append((id6, st, action_for(plan_info.get("kind"), st)))`, so the drivers AGREE. What diverges is prose: agy's `enforce_requested_action` docstring still asserts `--full-auto` "DEFAULTS TO TRUE on this host" and still names `determine_action`, while the parser says False on both hosts per the 2026-09-04 ruling. An executor trusting the docstring would re-derive a difference that does not exist. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-05 corrected with the byte-identical measurement, the stale docstring named as such, and the instruction to trust the parser over the comment. Its expected outcome now includes correcting or reporting the stale docstring. F-15 added. The Step 0 note and F-6's row carry the same correction. |
| PR-707 | MEDIUM | IN-SCOPE | G. Executability (an item written against an interface that does not exist) | `jxxec8` `- Status: reviewed` in `pending/`; its OQ-01 `open`; `evaluate_backlog_close` re-run | E-04 TELLS THE EXECUTOR TO CALL AN INTERFACE THAT IS NOT DECIDED YET, AND THE PLAN DOES NOT SAY SO. Child 01 is `reviewed`, not executed, and its own OQ-01 (a `check` rule versus a read surface versus both) is still open, so whether E-04's call site invokes a rule, a CLI verb, or a Python helper is undetermined by anything in the tree. Worse, if `jxxec8` ships a `check` rule only, a rule cannot be "called" from a driver pre-flight at all, and the plan gives no instruction for that case. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-04 now states plainly that the interface does not exist yet, forbids guessing, and requires reporting a shape mismatch rather than inventing a wrapper; V-04 permits marking the item `blocked` with that finding as its evidence. E-01 records `jxxec8`'s measured status. The gate records that the declared dependency is GENUINELY UNMET, which is correct sequencing rather than a defect. |
| PR-708 | MEDIUM | IN-SCOPE | C. Architecture (a sizing argument resting on a false asymmetry) | `_type_dirs(repo,"backlog")` read; `selectors.resolve(repo,'backlog','6h7y2y')` and `'ackme8'` -> match | THE SPEC/BACKLOG ASYMMETRY OQ-01 RESTS ON IS SMALLER THAN CLAIMED, SO ITS RECOMMENDATION IS ARGUED FROM THE WRONG PREMISE. `check_engine._type_dirs` already resolves the backlog tree with the same no-new-path-literal discipline, and `selectors.resolve` already resolves a backlog id6 to its file, so `discover_backlog` is a thin function over existing authorities rather than new infrastructure. Combined with PR-701, the true cost of BOTH halves is the four seams, which each must cross identically, so the enumeration was never the expensive part. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-03 and OQ-01 both re-argued: the recommendation to defer the backlog half SURVIVES but on the opposite ground (the spec half is far larger than budgeted, so adding a second type to an under-specified dispatch multiplies risk). V-03 now FAILS a deferral justified by "no enumeration exists". F-14 added. E-03 also requires an explicit choice of enumeration route, since no `_iter_backlog_records` sibling exists. |
| PR-709 | MEDIUM | IN-SCOPE | E. Testing; G. Executability (six stale figures, one a pass criterion) | `discover_specs` -> 10/29; AST import count -> 48; cluster census -> 24 clusters / 106 sources / `25kzda` at 10; bare suite -> `1 failed, 5919 passed, 3 skipped, 2 xfailed` | SIX MEASUREMENTS ARE STALE AND ONE IS USED AS A PASS CRITERION. `discover_specs` finds 10 of 29 not 9 of 28, and V-01 asked the executor to record "its discoverable-spec count", inviting a comparison against a wrong figure; the oc-to-agy import count is 48 not 47 (V-05 demanded "did not increase from 47", which a correct measurement of 48 would fail, the identical defect `lyo1tz`'s review found in its own Set); the cluster census is 24 multi-artifact clusters over 106 sources with `25kzda` at 10 artifacts, not "17 legitimate clusters" and "nine plans"; and the suite baseline figure was carried forward correctly but is worth re-pinning. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Every figure re-measured in place with DERIVE-never-pin instructions attached. V-01 now says explicitly not to compare against either the plan's or the review's spec count and to state its own. V-05 now forbids asserting a literal import baseline and requires only that it not have RISEN, since both 47 and 48 appear in this Set's prose. F-16 added. |
| PR-710 | LOW | IN-SCOPE | G. Executability; spec sync (an open question the repository already answers) | spec `25kzda` §2.6, §3.3, §3.4 read; `_SPEC_ACTIONS`/`_BACKLOG_ACTIONS` read | THE SPEC-SYNC SECTION'S SECOND QUESTION IS ALREADY ANSWERABLE FROM THE TREE AND LEAVING IT OPEN INVITES AN UNNECESSARY SPEC EDIT. It asked whether the per-type legality table "is specified in the spec or only in code" and said to declare the spec file if the former. Both are true and that is the point: §2.6 states the rule, §3.3/§3.4 give the per-status rows, AND the mapping is already transcribed into `run_selection_policy` as data. So the plan needs NO spec edit and no spec file in `- Scope-Paths:`. Also unstated: §3.3/§3.4 require the authoring action to pass the §4.8/§4.9 `SPEC-PLAN-*`/`BACKLOG-*` checklists, which this plan does not build, so a working `--action plan` still leaves real distance from §1.3's claim. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | The spec-sync section now records the answer (no spec edit needed, the transcription already exists, do not edit its cells) and states the remaining §4.8/§4.9 distance plainly, with the instruction that a full deferral under OQ-03 owes a durable backlog carrier rather than prose in a retired plan. |

CONFIRMED SOUND, AND THIS PLAN'S ARCHAEOLOGY IS ITS STRONGEST PART. Every one of its OBSOLESCENCE claims
holds and each was re-verified rather than trusted: `--action plan` really does fail closed with a named
refusal that starts no run (reproduced live, exit 2, no run directory created), so F-1 is right that it
must not be re-implemented; `discover_specs` really does exist with exactly the guarded construction the
plan describes, enumerating through `_iter_spec_records`, reading `_ITEM_ID_RE` and
`read_front_matter_status`, with a real AST test rejecting a new `"records/specs"` literal; the deliberate
no-`- Id:` skip is real and its docstring's honesty about the coverage gap is exactly as described; the
one-consumer claim was true as far as it went; the pre-flight ordering claim is correct
(`expand_selectors` -> `enforce_requested_action` -> mixed-type gate -> `mkdir`), so the no-durable-state
property the plan insists on is genuinely available; the import direction really is one-way at zero
reverse imports; the `--full-auto` correction the plan already made (F-9) is right and I confirmed both
defaults myself; and F-10's backlog-close analysis is exactly right, re-measured to the same verdict
string, which is a non-trivial thing to have got right about machinery this plan does not touch. The
refusal-quality instincts throughout ("never a silent partial", "assert the no-durable-state property on
the filesystem, not on a return code", "do not read the live spec tree") are the right instincts and I
strengthened rather than replaced them.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing about its direction, and almost everything about
its size. The plan's thesis is that a previous Set left one wire unconnected and this child connects it.
Measured, there is no wire: `discover_specs` is a leaf feeding an uncalled helper, the manifest is
plans-only by construction, the queue loop subscripts it bare, and the legality derivation has no
vocabulary for `plan` at all. So a change scoped to "consume the enumeration" lands a discovery edit and
a `KeyError`. Two things follow that I could not fix by editing prose. FIRST, the plan's own scope fence
forbade the only change that makes the feature possible, which is not a wording slip but a sign the
author had not located the mechanism; I relaxed it narrowly and escalated the API decision. SECOND, the
one-line summary of this review is that the plan's headline example is BROKEN TODAY in a way the plan
never noticed: `aw oc run --action plan 25kzda` does not merely refuse, it resolves `25kzda` to a plan
ABOUT the spec, and would have kept doing so after this plan shipped. That a self-review found it only by
running the resolver over all 184 candidate tokens, rather than by reading, is the lesson worth carrying:
this plan's obsolescence archaeology was excellent precisely because it measured, and its mechanism claims
were wrong precisely where it reasoned instead.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | The plan's central instruction ("consume `discover_specs`") misdescribes the work by roughly four times. Rewrite E-02 in place, or mark the plan REPLAN? | Rewrite E-02 in place around the four measured seams, and escalate the residual scope question as blocking OQ-03. | (a) `REJECT - NEEDS REPLAN`, rejected because the plan's GOAL, direction, exclusions and archaeology are all sound and independently verified; the defect is a mis-located mechanism, which is repairable with bounded edits to one E-item plus the fence, and discarding the plan would also discard the obsolescence work that is its most valuable content. (b) Rewrite silently and let the executor absorb the new size, rejected because the Set was SIZED on the wrong premise, so whether this is still one child is a scope judgement the maintainer owns, not something a reviewer may decide by editing prose. | `discover_specs` called (10/29); `grep` for its consumer's callers (tests only); `build_dynamic_manifest` on a throwaway repo -> `plans == {}` and a live `KeyError`; the four seams read in `initialize_run` and `expand_selectors`. | yes |
| D-2 | The plan's fence forbids changing `action_for`/`determine_action`, but no other derivation exists and neither can return `plan`. Relax the fence, or leave it and let the plan be unsatisfiable? | Relax it NARROWLY: permit ADDING a per-type derivation that consumes `run_selection_policy`'s existing table, require the two existing functions to keep their current return value for every input they handle today, forbid forking a third table, and escalate the API question as blocking OQ-04. | (a) Leave the fence intact, rejected because it makes the plan unexecutable by construction: measured, those two functions are the only derivation `enforce_requested_action` consults and `plan` is in neither's range, so an executor obeying the fence cannot deliver the goal. (b) Permit extending `action_for` to take a type and return `plan`, rejected as the default because it would fork spec 2.6's legality table into a second location, which is precisely the drift this plan's own F-7 warns about; it remains on the table as OQ-04 option (b) for the maintainer. (c) Widen `ACTION_IMPLEMENTED` and accept the mismatched refusal, rejected outright: reproduced, it reports the wrong action to the operator. | `action_for`/`determine_action` over every Kind x status pair -> `{execute, review, orchestrate}`; `'plan'` in neither body; `_SPEC_ACTIONS: approved -> ACTION_PLAN` and `_BACKLOG_ACTIONS: open -> ACTION_PLAN` read; refusal reproduced under a mutated frozenset. ESCALATED as OQ-04 with `Blocking: yes`. | no |
| D-3 | Four spec id6s already resolve to plans by filename substring. Fix precedence in this plan, file it separately, or note it? | Fix it HERE, with the six colliding tokens pinned as tests and V-02 failing the plan if `25kzda` resolves to `wenmg4`; record the blast-radius question as non-blocking OQ-05 with an escalation condition. | (a) File it as its own backlog item and leave this plan silent, rejected because this plan's whole deliverable is "a spec selector resolves for the plan action" and a selector that resolves to the wrong artifact does not satisfy that; shipping both would mean shipping a known mis-resolution under a plan that claims to fix resolution. (b) Note it without requiring a fix, rejected because the most natural implementation (append a spec branch to the existing chain) is exactly the wrong one, so a note without a test is a note an executor will pass. | `expand_selectors` run over every discoverable spec id6 and all 174 backlog id6s: four specs and two backlog items resolve to one plan each, `vwios6` to four; `expand_selectors(m,['25kzda'])` -> `['wenmg4']`. | yes |
| D-4 | Should the two undeclared test files and `run_selection_policy.py` be added to `- Scope-Paths:`, or left for the executor to justify out-of-scope? | Declare all three, with per-path scope narrowed in the scope check. | Leaving them undeclared and relying on `aw ipd finalize`'s `--scope-reason`, rejected because all three are REQUIRED by the work rather than discovered during it: the four test assertions go red on the first correct commit and the legality table is the only in-tree transcription of spec 2.6. Declaring a path that will certainly be edited is what the field is for; deferring it to a justification converts a foreseeable edit into a finalize-time surprise. | `ACTION_IMPLEMENTED` mutated -> `3 failed` at four named nodes; `_SPEC_ACTIONS`/`_BACKLOG_ACTIONS` are the only in-tree transcription of §2.6; `cli.py`/`ipd_lifecycle.py` scope-reconciliation behavior as recorded in sibling reviews. | yes |
| D-5 | The spec-sync section asks whether the per-type table is in the spec or only in code, and says to declare the spec file if the former. Resolve from evidence or leave open? | Resolve it: BOTH are true, no spec edit is owed, no spec file is declared, and the two transcribed dicts must not be edited. | Leaving it for the executor, rejected because the repository answers it plainly (§2.6 states the rule, §3.3/§3.4 give the rows, `run_selection_policy` holds the transcription) and an unresolved version invites the executor to declare and then edit an APPROVED spec to match a partial implementation, which the same section forbids two sentences earlier. | spec `25kzda` §2.6 `:270`, §3.3 approved-spec row, §3.4 open-backlog row; `_SPEC_ACTIONS`/`_BACKLOG_ACTIONS` read and called through `_action_for`. | yes |
| D-6 | Readiness: `go-pending-approval` (every finding fixed) or `no-go`? | `no-go`. | `go-pending-approval`; rejected because the vocabulary reserves `no-go` for a genuine not-ready condition and this plan has three: its own OQ-03 and OQ-04 are `Blocking: yes` and open, and its declared `- Item-Dependencies: executed:jxxec8` is genuinely unmet with `jxxec8` at `reviewed` in `pending/` and its own shape-deciding OQ still open. Two of the three are unresolvable without a human. | `aw ipd lint --phase review-finalize` exit 1 with exactly two `IPD-Q501`; `jxxec8` `- Status: reviewed`; the workflow's definition of NO-GO as "any open question, any unfixed BLOCKER/HIGH". | yes |
| D-7 | Two new blocking questions cannot be asked: no interactive channel exists in this session. Leave open, or downgrade to `Blocking: no` so the lint conforms? | Leave both OPEN and `Blocking: yes`, verdict `APPROVE WITH REVISIONS APPLIED` with readiness `no-go`, recording that an ask was attempted. | (a) Set `Blocking: no` so `aw ipd lint` conforms, rejected as the worst option available: it clears a hard gate by relabelling it, and the lint's own message offers that route, which is exactly why taking it would be a forged clearance. (b) Answer them from evidence, rejected because OQ-03 is a scope-and-risk judgement about a public CLI contract and OQ-04's chosen option makes a private function public API; both are the maintainer's. (c) Write `Readiness: go-pending-approval` so a runner picks the plan up, rejected as forging another role's attestation. | `plan-review.md` Step 3.3 non-interactive exception and memory-kernel rule 5; `AGENTS.md` on never writing another role's attestation field; siblings `yku4ga` and `drzbs9` carry the identical disposition for the same reason. | yes |
