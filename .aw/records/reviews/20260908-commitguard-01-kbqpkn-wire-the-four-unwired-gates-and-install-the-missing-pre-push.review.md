# Review: wire the four unwired gates and install the missing pre-push hook, child kbqpkn (Set commitguard)

- Subject-Id: kbqpkn
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `09a9987e`. `aw ipd lint --phase author` CONFORMING before semantic review. After
revision it reports EXIT 1 with one `IPD-Q501`, because OQ-01 is now `Blocking: yes`; that is the
escalation gate working on a finding this review raised, not a structural defect, and it is why
readiness is NO-GO.

DISCLOSURE: the same agent/model authored this plan, and its parent was reviewed immediately before
it, so this is a SELF-REVIEW. Its value came almost entirely from RUNNING things the plan only read.
Eleven measurements: all six gate verbs' occurrence counts in `.pre-commit-config.yaml`; the installed
hook list; ALL FOUR unwired gate verbs EXECUTED with unpiped exit codes; the 174 scope-gate refusals
attributed per plan; `grep -ci "opt-in"` across all six hook modules; `diundn` OQ-01 read in full; the
four `engine.create_*_hook` docstrings; `29wvmj` and `h9cn0y` front matter; `tests/test_executed_transition_gate.py`
read to its end; `CONTRIBUTING.md`'s `pre-commit install` instruction; and the bare suite. Two of those
inverted the plan's central premise.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-801 | BLOCKER | IN-SCOPE | D. Domain invariants; G. Plan executability | `diundn` OQ-01 (`RESOLVED - OPT-IN ... NEVER installed by default`); four installer docstrings; `grep -ci opt-in` = 0,0 vs 3,2,3,2 | **ALL FOUR GATES ARE DELIBERATELY OPT-IN BY A RESOLVED MAINTAINER DECISION, NOT ONE, SO THE PLAN'S CENTRAL PREMISE INVERTS AND ITS OWN RULE FORBIDS ALL FOUR WIRINGS.** The plan expected exactly one deliberate exclusion and treated the other three as oversights. Executed plan `diundn` OQ-01 reads "RESOLVED - OPT-IN, aligning with the established `bklggrad` pattern ... NEVER installed by default ... This keeps the toolkit's honest stance that local hooks are opt-in feedback, not an imposed authority boundary" and covers BOTH the scope gate and the push gate; `create_precommit_scope_gate_hook` and `create_prepush_authorization_gate_hook` each document "opt-in-only (never default-installed)"; `ipd_dependency_statement_gate.py` opens "OPT-IN local pre-commit gate"; AGENTS.md:179 documents the backlog gate the same way. The partition is mechanical and exact: the two WIRED gates contain "opt-in" zero times, the four unwired ones 3, 2, 3 and 2 times. Since E-01 states "a gate whose exclusion turns out to be deliberate must NOT be wired by this plan", the plan has NO wiring deliverable at all without a maintainer reversal. | C:Low; U:Medium; S:Low; F:High; Overall:High | OPEN | OQ-01 WIDENED from one gate to all four and PROMOTED to `Blocking: yes` with `- Finding: PR-801`, per the escalation rule for an unfixed BLOCKER. Concern, E-01, conventions, F-5/F-5b and the gate all rewritten to state the measured position and to say the refusal is the gate working. Not fixable by edit: the fix IS the maintainer's ruling. |
| PR-802 | BLOCKER | IN-SCOPE | A. Correctness; F. Prevent silent failure | `python3 -m agent_workflows precommit-scope-gate` exit 1, 174 `check.scope-drift` lines against a CLEAN tree | **`precommit-scope-gate` REFUSES THIS REPOSITORY RIGHT NOW, 174 TIMES, SO WIRING IT WOULD BLOCK EVERY COMMIT INCLUDING THIS PLAN'S OWN.** Every refusal is a `check.scope-drift` finding naming three OTHER agents' in-flight plans (`xdr83v`, `yvvf98`, `hp9rot`) whose frozen begin bases trail main; nothing about the committer is at fault. The plan's E-02 exists to catch exactly this and would have caught it at execution time, but the plan presented wiring as the expected outcome and OQ-02 as a wait-and-see. The reflexive consequence is the sharpest part: an executor who wires this gate cannot then commit the wiring, leaving `--no-verify` (forbidden by this plan's own contract, and the precise behavior `gjadwm` records as trained by false-positive gates) or a revert. | C:Low; U:High; S:Low; F:High; Overall:Medium | FIXED | Added F-6b. Concern, E-02, conventions, Validation and the gate's reflexive-hazard paragraph now carry the measurement and the EXCLUDE verdict; OQ-01's per-gate guidance says do not wire it today regardless of posture; V-02 requires the executor's own count. |
| PR-803 | HIGH | IN-SCOPE | Evidence accuracy; G. Executability | `h9cn0y` `- Status: executed`; gate still refuses 174 times | **OQ-02's REASONING IS SPENT: ITS NAMED CAUSE HAS LANDED AND THE FALSE POSITIVE REMAINS.** OQ-02 attributed the scope gate's risk to in-flight work in approved plan `h9cn0y` and told the executor to measure and wait. `h9cn0y` is now `executed`, and the gate still refuses a clean tree 174 times, so waiting resolves nothing. The actual cause is the stale-frozen-base defect in `check.scope-drift`, which plan `wmnmei` (`reviewed`) is graduating and which has not landed. An executor following OQ-02 as written would have looked for a landing that already happened and drawn the wrong conclusion. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | OQ-02 RESOLVED with the measurement, the corrected cause, and the instruction to exclude on evidence rather than defer to a sibling; `Owner: executor` dropped since the measurement is done. Added F-9. |
| PR-804 | HIGH | IN-SCOPE | E. Testing; scope declaration | `tests/test_executed_transition_gate.py:590-611` | **TWO EXISTING ASSERTIONS PIN THE EXACT VALUES E-04 CHANGES, AND THAT FILE WAS NOT IN `Scope-Paths`.** `test_both_hook_types_are_installed_by_default` asserts `default_install_hook_types == ["pre-commit", "pre-merge-commit"]` EXACTLY, so adding `pre-push` fails it; `test_only_the_gate_opts_into_pre_merge_commit` asserts `default_stages == ["pre-commit"]` and an exact opted-in list. The plan named neither, so E-04 would have broken the suite and the necessary fix would have been an undeclared out-of-scope edit that finalize refuses without a `--scope-reason`. The same class also carries the caveat E-06 should reuse ("This is a CONFIGURATION assertion only. It is NOT evidence that the stage fires."). | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-7b. `Scope-Paths` gained `tests/test_executed_transition_gate.py`; E-04 names both assertions and forbids weakening them to subset checks; E-06 must mirror rather than duplicate the class and adopt its caveat; V-04 requires the updated assertions pasted; Scope check explains the addition. |
| PR-805 | HIGH | IN-SCOPE | A. Correctness (two E-items in direct conflict) | E-03 "Do NOT change any gate's own code"; E-05 "UPDATE THE SENTENCE" | **E-03 FORBIDS TOUCHING GATE MODULES AND E-05 REQUIRES EDITING SENTENCES INSIDE THEM, WITH NO RECONCILIATION.** An executor wiring a gate must make the word "OPT-IN" false in that gate's own module, which E-03 prohibits outright. Worse, the correction is a THREE-SITE edit per gate (module docstring, printed refusal text, and the `engine.create_*_hook` docstring's "opt-in-only" claim), not the single sentence E-05 implies; measured counts are 3, 2, 3 and 2 occurrences. `agent_workflows/hooks/` was also not in `Scope-Paths`. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-05 now states the multi-site reality with counts, and resolves the conflict narrowly: E-03 forbids changing what a gate DECIDES, E-05 permits correcting a false DISCLOSURE STRING, proven by a `git diff` showing only docstring and literal-text lines. `agent_workflows/hooks/` added to `Scope-Paths` with the reason. E-05 also now forbids touching disclosures if nothing is wired. |
| PR-806 | MEDIUM | IN-SCOPE | Evidence accuracy; C. Architecture | `29wvmj` `- Status: executed`; `.pre-commit-config.yaml:17`, `:19-23` | **`29wvmj` IS `executed`, NOT "approved in flight", so E-04's collision warning is stale in the direction that matters.** Its `default_install_hook_types` and `default_stages` keys and comment block are already in the file, so E-04 extends a known two-entry baseline rather than racing a sibling, and the "if the two edits collide, STOP and report" instruction describes a race that cannot occur. Separately the plan never mentions that `default_stages: [pre-commit]` is pinned DELIBERATELY, for a reason the file documents (a second hook type would otherwise also run the content-MUTATING hooks in that stage) that applies directly to `pre-push`. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | F-7 rewritten; E-04 states the landed baseline, requires extending the first list only, leaving `default_stages` alone, and giving the push gate its own explicit `stages: [pre-push]`; a conventions bullet records the trap; V-04 requires both keys pasted. |
| PR-807 | MEDIUM | IN-SCOPE | E. Testing; G. Executability | E-06's "Assert it FAILS against HEAD `a2e0438a` for the gates E-01 cleared" | **E-06's NON-VACUITY PROOF PRESUMES GATES GET WIRED, WHICH PR-801 SHOWS MAY NOT HAPPEN.** If the cleared set is empty, "assert it fails against HEAD for the gates E-01 cleared" is unsatisfiable as written, and an executor might either skip the proof or wire something to make it satisfiable. The test is also the plan's most durable deliverable and is valuable under BOTH outcomes, since an exemption list closes the accumulation hazard just as well as a wired list; but only if a NEW gate verb left unclassified fails it, which requires discovering the verb set rather than hardcoding six names. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-06 rewritten: the exemption list is the point rather than a fallback, the verb set must be DISCOVERED, non-vacuity is proven by a reverted perturbation instead of a HEAD assertion, and the configuration-only caveat is adopted from the existing class. V-06 requires the perturbation and revert pasted. |
| PR-808 | MEDIUM | IN-SCOPE | G. Executability (an unstated valid outcome) | E-03, Scope check, Proposed changes | **NOTHING TOLD THE EXECUTOR THAT SHIPPING NO WIRING IS A LEGITIMATE COMPLETED OUTCOME**, which given PR-801 and PR-802 is the likely one. A plan whose E-items all read as "register the cleared gates" pressures an executor to widen the cleared set to have shipped something, which is exactly how a reversed decision or a known false positive gets wired. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-03 states an empty cleared set is a valid completed outcome and must not be dressed up; Proposed changes carries an expected-outcome line; Scope check's under-scope paragraph says the wiring itself may be out; Validation describes the reduced honest evidence set; V-03 and V-05 accept the no-change branch with proof. |
| PR-809 | MEDIUM | IN-SCOPE | A. Correctness (a safety criterion that cannot apply) | `python3 -m agent_workflows prepush-authorization-gate` exit 1 | E-02's clean-run criterion cannot clear `prepush-authorization-gate`: it refuses EVERY push without `AW_PUSH_AUTHORIZED=1`, which is its designed behavior, not a false positive. Applying "must pass clean" mechanically would either exclude it forever for the wrong reason or lead an executor to set the env var to manufacture a pass. Wiring it is a workflow decision (it changes local push behavior for everyone who installs hooks here), not a cleanliness one. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-6c. E-02 and E-04 both state the distinction; E-04 requires the executor to say which behavior it considers acceptable and check it against OQ-01; OQ-01's per-gate guidance records it. |
| PR-810 | LOW | IN-SCOPE | E. Testing; documentation | bare suite at review; `diundn` in `executed/`; `CONTRIBUTING.md:64`, `:72` | Three smaller items. (1) The baseline is wrong in count AND identity: measured `1 failed, 5919 passed, 3 skipped, 2 xfailed` failing on `test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose` (environmental, 189 untracked `opencode-recovery/*.md` files from another party), not `test_orchestrator_retirement`. (2) The spec-sync section misses the most important documentation authority: `diundn` OQ-01 is the recorded decision a wiring would reverse, and AGENTS.md forbids editing an executed plan, so the reversal must be recorded HERE citing `diundn`. (3) `pre-commit run --all-files` was specified without noting that `default_install_hook_types` takes effect only at install time, so a run against stale `.git/hooks` proves nothing; `CONTRIBUTING.md` already carries that instruction for `29wvmj` and may need extending rather than duplicating. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Baseline re-measured with the named environmental cause and a delta criterion; spec-sync gained the `diundn` supersession-recording requirement and the `CONTRIBUTING.md` note; Validation requires `pre-commit install` before the all-files run. |

CONFIRMED SOUND, AND THE PLAN'S SAFETY INSTINCTS ARE WHAT SAVED IT. Finding 2 reproduces exactly: the
four verbs at zero occurrences, the two wired ones at one and two, and only `pre-commit` installed. The
gates are real working code, verified by running all four. Most importantly, the plan's own two guard
rails are the reason this review found actionable evidence rather than a shipped mistake: E-01's rule
that a deliberately-excluded gate must NOT be wired is what turns PR-801 from a disaster into a
maintainer question, and E-02's insistence on a by-hand run before wiring is precisely the check that
catches PR-802. An author who had skipped either would have wired a gate that refuses its own commit.
The established-shape instruction for new hook entries is correct and matches the file, the `gjadwm`
false-positive citation is accurate and its cost even higher than quoted (a legitimate edit was
DROPPED), and the deferral of the item's Finding 1 is properly reasoned.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. No E-item was added or removed; six in, six out. What
changed is the plan's expected OUTCOME: from "wire four gates and install a stage" to "measure, record
a four-row decision table, ship a wiring test, and wire nothing unless the maintainer reverses a
resolved posture". OQ-01 is now the hinge and blocks the plan at every lint checkpoint, which is
correct for a plan whose remaining work would reverse an executed plan's recorded decision.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | All four gates are deliberately opt-in. Resolve OQ-01 myself as "keep opt-in", or escalate to the maintainer as blocking? | ESCALATED: OQ-01 widened to all four and promoted to `Blocking: yes` with `- Finding: PR-801`. | Resolving it as "keep all four opt-in" (defensible on the evidence, but it would decide the toolkit's POSTURE toward imposing local hooks, reversing or upholding a maintainer-resolved question on my own authority, which plan-review forbids); leaving it non-blocking (a BLOCKER left unfixed must be escalated so the lint gate stops execution, and with no gate clearable the plan would otherwise appear runnable while having no deliverable). | `diundn` OQ-01; plan-review Step 4 escalation rule; `review_findings_gate.block_at` default HIGH | yes |
| D-2 | E-03 forbids changing gate code; E-05 requires editing sentences inside gate modules. Which wins? | Narrow reconciliation: E-03 governs what a gate DECIDES, E-05 permits correcting a false DISCLOSURE STRING, with a `git diff` proving no logic changed. | Dropping E-05's edit requirement (would ship a gate whose printed text calls itself OPT-IN while being always-on, the exact dishonesty the Set's shared constraint forbids); dropping E-03's prohibition (would license arbitrary gate edits in a wiring plan). | the Set's honesty constraint via parent `ao1rb7` E-03; the item's "a bypassable guard must never be DESCRIBED as a boundary" | yes |
| D-3 | `precommit-scope-gate` fails its safety check. Exclude it in the plan, or leave the verdict to the executor? | Recorded the measurement and the EXCLUDE verdict in the plan, while still requiring the executor to re-measure. | Leaving it entirely to E-02 at execution time (the measurement exists now and hiding it would let an executor wire a gate that blocks its own commit); hard-excluding it with no re-measurement (the count is perishable and `wmnmei` may land first, so a permanent exclusion would be wrong for the wrong reason). | 174 refusals measured against a clean tree; `wmnmei` `- Status: reviewed`; `gjadwm`'s recorded cost | yes |
| D-4 | Two `Scope-Paths` additions were needed. Add them, or flag them for the executor? | ADDED (`tests/test_executed_transition_gate.py`, `agent_workflows/hooks/`) with the reason and an instruction to `--scope-ack` them if unmodified. | Leaving them undeclared (E-04 breaks two assertions and E-05 edits a hook module, so both edits would be undeclared out-of-scope work that finalize refuses without a `--scope-reason`); adding a prohibition on touching them (would make required work unimplementable). | `tests/test_executed_transition_gate.py:590-611`; `ipd_lifecycle` finalize scope reconciliation; AGENTS.md scope-declaration rule | yes |

## Commit result

- Pre-review snapshot: skipped, target plan committed and unchanged
- Hardened result: see the `plan-review: harden commitguard-01 kbqpkn (revisions applied)` commit
- Push: not performed

## Round 2

Round 2 exists ONLY to close PR-801, whose escalated question the maintainer answered on 2026-09-10. It
re-critiques nothing: every other round-1 finding was already `FIXED` and is superseded unchanged.

WHY IT IS NEEDED: the escalation contract (`plan-review.md:335-341`) defines the path INTO a blocking
question and no path back, so the answered question left PR-801 reading `OPEN` while
`subject_gating_blocks` kept refusing the plan on a settled decision. Measured at HEAD `dcb5a2a4`: plan
OQ-01 reads `- Status: resolved` carrying `- Finding: PR-801`. Appending a round is the sanctioned
mechanism, since `current_findings` reads only the last round (`review_findings.py:236-243`).

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-801 | BLOCKER | IN-SCOPE | F. Principles (wiring any gate reverses a recorded decision) | plan OQ-01 (`- Status: resolved`, `- Finding: PR-801`) | Carried forward from round 1 and now CLOSED BY THE MAINTAINER, who CONFIRMED round 1's reading rather than reversing it. Round 1 measured that all four gates are opt-in by a deliberate decision (`diundn` OQ-01) rather than by oversight, so every wiring this plan contemplated would reverse a recorded position and the plan had no clearable wiring deliverable. Ruling of 2026-09-10 (`/askme`): KEEP ALL FOUR OPT-IN, wire none, ship only E-06's installability test, which is exactly the disposition round 1 named as defensible. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Closed on the maintainer's recorded decision; `diundn` OQ-01's opt-in posture stands unreversed and E-01's rule (a deliberately-excluded gate must NOT be wired by this plan) now applies to all four. Their stated reasoning: the two clean gates would deliver the same verdict sooner and on one machine only, since `aw check` in required CI already enforces all four fail-closed. Re-measured before asking, at HEAD `81413101`: dependency gate exit 0, backlog close gate exit 0, `aw precommit-scope-gate` STILL refuses a clean tree, prepush gate refuses by design. |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Does the maintainer's ruling leave this plan with a deliverable, given it was authored to wire gates? | YES, and the scope narrows to E-06's installability test alone. | Retiring the plan as having nothing to do, rejected because E-06 proves each of the four CAN be installed correctly by anyone who wants it, which is currently unproven and is the one thing the ruling does not foreclose. | The maintainer's 2026-09-10 answer recorded in plan OQ-01, which explicitly selected "ship only E-06's wiring test"; round 1's own finding that this "loses nothing that CI does not already cover". | yes |
| D-2 | Does the `wmnmei` fix (making the scope advisory measure the isolated lane) authorize wiring the scope gate later? | NO. A future plan proposing to wire it needs a NEW decision and may not cite the `wmnmei` fix as authorization. | Treating the scope gate as provisionally approved once its clean-tree refusal is fixed, rejected because the ruling declined wiring on POSTURE grounds (imposition), not on the gate's correctness, so repairing the correctness defect does not address the reason it was declined. | The maintainer's ruling reasoning, recorded in plan OQ-01, turns on local hooks not being an imposed authority; `wmnmei` OQ-01's separate answer addresses only which tree the advisory measures. | yes |
