# Review: require Priority and Work-Kind on a plan going forward, orchestrator d0cbt3 (Set planprio)

- Subject-Id: d0cbt3
- Subject-Type: ipd
- Reviewed-At: 2026-09-12
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `3ae4735e`. Structural preflight `aw ipd lint --phase author` CONFORMED (clean, 0 findings)
before semantic review, and the same command conformed on all three children. At `--phase review-finalize`
the linter now reports one `IPD-Q501` error, which is the escalation gate working as designed: it is the
blocking question this review added, and it is the mechanism by which an unfixed BLOCKER stops execution
rather than merely being reported.

DISCLOSURE: this Set was authored in the same repository by the same model family, so treat this as a
near-self-review worth less than an independent one. Its value therefore rests on what was EXECUTED, not on
what was re-read. Nine things were measured rather than reasoned: the pending and terminal populations and
the whole inheritance graph were recomputed from disk; `check_plan_priority` and `check_plan_work_kind` were
DRIVEN against a plan carrying `grandfathered`, `unresolved` and `TODO` in each field; `ipd_lint.lint_file`
was driven on an APPROVED pending plan with its gate field removed at `--phase author` and on an `executed/`
plan at `--phase pre-execution`; `aw check plans` was run bare; the bare suite was run; `aw att --type plan`
was run piped, under `FORCE_COLOR=1`, and with `--format json`; the `-` clearing callers were grepped; and
the `oorry1` precedent plan was read in full.

SCOPE OF THE LEDGER. The invocation named the orchestrator only, so the ledger is that one plan. The three
children (`lkexaw`, `8u6770`, `lc4unl`) were read as EVIDENCE and were NOT edited, matching how this
repository has reviewed a Set before (`wfjsp4` carries its own record and each child a separate one).
Consequences are stated where they matter: the dependency-edge fix PR-001 needs lives in the children's own
front matter, which is why that finding is escalated rather than fixed.

THE SET'S THESIS IS SOUND AND I RE-VERIFIED IT. The fields really are recognized and unenforced
(`META_PRIORITY`/`META_WORK_KIND` in `META_RECOGNIZED`, neither in `META_REQUIRED`, `ipd_schema.py:210`,
`:222`, `:241-256`), the scaffold really emits neither (`ipd_authoring.py:158-185` writes `Scope-Paths`,
`Item-Dependencies`, `Status`, `Set`, `Order`, `Highest E allocated`, `Author`, `Id` and neither of these
two), the governing spec really is SILENT rather than contradictory, and adoption really is near-zero. The
maintainer's correction is supported by the artifacts, and the Set's four-part shape (gate, scaffold,
inherit, decide) is the right decomposition.

ITS SEQUENCING IS NOT SOUND, AND THAT IS THE WHOLE OF THE BLOCKER. The gate this Set installs fires on a
plan's persisted STATUS as well as on the requested checkpoint, so the instant it lands, every pending plan
already approved to run and missing a field becomes unrunnable, and the plans that would fix them are
declared to run AFTER it. Measured: 18 such plans, only 5 of which the mechanical backfill can reach. The
shipped precedent this Set explicitly copies (`oorry1`, which introduced the `Scope-Paths` gate) avoided
exactly this by stamping the grandfather sentinel onto the pre-cutoff pending corpus in the SAME child that
added the gate; this Set dropped that step, so its exemption exists in code and on zero artifacts.

THE ROOT CAUSE THE OTHER FINDINGS SHARE is worth naming: the Set reasoned about the gate as a NEW-plan
concern and about legacy as a TERMINAL-plan concern, and the pending-but-already-approved plan falls between
those two frames. PR-001 is that gap on the dependency graph, PR-008 is the same gap in the cross-check that
was supposed to prove the boundary, and completion criterion 3 was written as though only terminal plans
needed protection. Two further findings are the ordinary kind (four stale figures with one of them false)
and two are evidence commands that cannot produce what they ask for, each of which would have had an
executor either stall or fabricate.

WHAT REVIEW CHANGED. One finding is BLOCKER and left OPEN, escalated to a blocking question, because
choosing among its three costed options is a scope-and-appetite call and its fix spans two artifacts outside
the ledger. Eight were fixed in place. E-items stayed at 2 and V-items at 2 (the parent correctly holds no
work of its own, and review REMOVED the one deliverable it had parked there); completion criteria grew 5 to
6; the cross-IPD validation grew from a two-state to a three-state exercise with the third state named as
the one that bites; the deferred list gained the plan-versus-spec `-` asymmetry with its three named
callers; and the gate section gained a four-item "do not re-inherit" list.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | BLOCKER | UNDER-SCOPE | A. correctness; C. operability; G. executability | `ipd_lint.py:904-913` (`_scope_paths_gate_applies`); `ipd_lifecycle.py:991-1007`; child `8u6770:8` and `lc4unl:8` (`Item-Dependencies: executed:lkexaw`); `20260823-ipdgates-02-oorry1-...ipd.md:48` and `:50`; measured at HEAD `3ae4735e` | **Landing the gate before the backfill strands the plans that are already approved to run, and the precedent this Set copies solved that in the same child.** The gate fires when the checkpoint is `pre-execution` OR the plan's persisted `- Status:` is `approved`/`auto-approved`, so it applies at EVERY phase to an approved plan; verified by removing `Scope-Paths` from an approved pending plan and getting `IPD-M106` at `--phase author`. MEASURED: 25 pending plans are approved/auto-approved and 18 carry neither field; only 5 are reachable by Order 02's inheritance, the other 13 (one `lanectn`, two `orchprobe`, nine `runanalytics`, one `runnerbugs`) have no `- From-Backlog:` and need Order 03's human table. Both backfills declare `executed:lkexaw`, so from the moment Order 01 lands those 18 fail `aw ipd lint` at every phase and `aw ipd begin` refuses them fail-closed. The authored justification ("backfilling before the gate exists would write values nothing enforces") does not survive the mechanism: a value written first is what the gate then finds satisfied. `oorry1` E-03 stamped `Scope-Paths: grandfathered` onto ~21 pre-cutoff pending plans inside the gate child precisely so "none is retroactively blocked once E-02's gate is live"; nothing in this Set writes an exemption marker onto any existing pending plan | C:Medium; U:Medium; S:Low; F:Medium-High; Overall:Medium-High | OPEN | ESCALATED to OQ-02 with `- Blocking: yes` and `- Finding: PR-001`; verified mechanically that `aw ipd lint --phase review-finalize` now exits 1 with `IPD-Q501` at line 110, so the shipped gate refuses the plan until the maintainer answers. Three options costed in the question (stamp inside Order 01 / reverse the order / stage the gate) with (a) recommended on the `oorry1` precedent. The child table now marks all three edges `SEE OQ-02` and states that the children's own `Item-Dependencies` must be brought into agreement in the same change. Completion criterion 3 rewritten to make "no pending plan left unrunnable" the criterion. Under-scope note corrected from "intentional sequencing" to a genuine gap |
| PR-002 | HIGH | IN-SCOPE | A. correctness; D. domain invariants | `check_engine.py:2643-2695`, `:2697-2755`; `check_engine.py:247-259` (both rules registered at `error`); child `lkexaw` E-01/E-02; measured in a scratch tree | **The exemption sentinel the Set designs is rejected by two SHIPPED enum rules, so its own completion criterion is self-contradictory.** `check.priority-invalid` and `check.work-kind-invalid` accept ONLY the three priority members and the five work-kind members and treat ABSENT as fine. MEASURED by driving both checkers over a plan carrying each candidate value: `- Priority: grandfathered` yields `check.priority-invalid`, and so do `unresolved` and `TODO`; the same three yield `check.work-kind-invalid` on `- Work-Kind:`. So child 01's scaffold sentinel (E-01) and its `grandfathered` sentinel (E-02) both produce a plan the repository's own checker rejects, and criterion 3's "a value in the shared vocabulary, or the legacy exemption" is impossible as written. This is an inverted asymmetry with the `Scope-Paths` precedent, whose sentinel has no enum checker to collide with, which is exactly why copying that design without checking this one is the trap | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | New completion criterion 4 states the collision with both rule ids, their line ranges, the measured result for all three candidate values on both fields, and requires that whatever sentinel Order 01 chooses either be admitted by both rules in the same change (Order 01 already declares `check_engine.py` in its `Scope-Paths`) or not be a metadata value at all. Left the MECHANISM to Order 01 deliberately rather than prescribing one from outside its scope |
| PR-003 | HIGH | IN-SCOPE | E. testing; G. executability | `attention.py:2084-2101` (Priority column in `render_table`), `:2280-2296` (`render_board` delegates only when colored), `:1083` (json `priority` key); measured three surfaces side by side | **A required evidence command cannot produce the evidence it asks for: `aw att --type plan` has no Priority column on any surface an agent can capture.** V-02 and completion criterion 4 required pasting it "showing a non-empty Priority column". Measured: piped (and under `NO_COLOR`, and in agent mode) the board emits the stable `- [tree] path (status)` form with no columns at all; the Priority column exists only in the colored table renderer, reachable with `FORCE_COLOR=1`; the `--format json` payload carries a per-item `priority` key. An executor pasting evidence necessarily pipes, so it would see no column and either report a failure that is not one or fabricate the column | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Criterion renumbered to 5 and rewritten to name the mechanism, both surfaces that DO answer it, and the requirement to state the count of non-null priorities. V-02 now demands BOTH surfaces and explicitly forbids pasting the piped form and calling the missing column a failure. Added to the gate's four "do not re-inherit" corrections |
| PR-004 | HIGH | IN-SCOPE | E. testing; evidence accuracy | measured `aw check plans` bare: exit 1, `errors 140  warnings 0`; `check_scope_drift` 124, `check_lifecycle_transitions` 15, `check.review-decision-unescalated` 1; `.github/workflows/tests.yml:153-155` | **Two V-items required `aw check plans` CLEAN, and it is 140 errors deep before this Set touches anything.** None of the 140 is attributable to this Set: 124 `check.scope-drift` on two plans holding live begin receipts, 15 `check.lifecycle-transition-invalid` on recorded backwards transitions, 1 `check.review-decision-unescalated`. An executor told to paste it clean would either stall or, worse, "fix" other agents' pending plans to make the number fall, in a checkout the repository's own rules say must not be swept | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | The cross-IPD validation now records the measured 140 with its per-rule breakdown and requires a BASELINE-plus-delta comparison (no new rule id, no increased count) instead of a clean run, with an explicit prohibition on editing another party's plan to reduce the count. V-02 restated to match. Added to the gate's "do not re-inherit" list |
| PR-005 | HIGH | IN-SCOPE | C. architecture; G. executability | `oc_runipd.py:836-860` (`finalize_orchestrator`, administrative, no agent turn); spec `20260906-77tr3o-01-...spec.md:17` and `:226`; child `8u6770` E-01 and `lc4unl` E-01 | **The parent parked a measurement DELIVERABLE on an orchestrator the runner retires without performing or verifying its items.** The old E-01 required re-measuring three populations and V-01 required pasting them. But the runner retires an orchestrator administratively via `aw ipd set executed` with no agent turn and DELIBERATELY skips the pre-transition E/V checkpoint (the `77tr3o` OQ-1 ruling, chosen so the honesty checker is not taught a narrow exception). So E-01 would be marked complete having never been performed and V-01's evidence never collected. The measurement is already covered where it is CONSUMED: Order 02's E-01 re-derives the inheritance table and Order 03's E-01 re-derives the no-source population, and both are explicitly told to trust no count from any plan | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-01 converted from a measurement into genuine ORCHESTRATION (dispatch each child only after its predecessor reads `executed` in its own file), with the retirement-skip mechanism and the children's coverage cited in place so a later author does not re-add the deliverable. The re-measured figures moved into the Concern block as review-measured facts. V-01 rewritten to match. E-02 gained the refusal-is-not-a-failure rule. Added to the gate's "do not re-inherit" list as a general prohibition, with the AGENTS.md remedy (ADD A CHILD) named |
| PR-006 | MEDIUM | IN-SCOPE | Evidence accuracy | measured at HEAD `3ae4735e`: 120 pending, 11 with both fields, 92 with `From-Backlog` (0 dangling, 0 sources missing a field), 81 of those still missing a field, 28 with no source, 508 terminal (470/34/4) | **Four population figures are stale and one is FALSE, and all are quoted as fact in the Concern block, the Goal and the gate note.** Authored: "0 of 104 pending plans carry either field", 84 inheritable, 20 without a source, "470 terminal plans". Measured: 120 pending; 11 ALREADY carry both fields, so "adoption is consequently zero" is now wrong (the thesis survives: 109 of 120 remain unlabelled); 92 carry a resolving source with every source carrying both fields, of which 81 still lack a field; 28 carry no source; 508 terminal. The plan already instructs re-measurement, which is why this is MEDIUM rather than higher, but a figure stated as fact in a Concern block is what the next reader quotes | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Concern block carries the full re-measurement with its date and HEAD, states plainly that the zero-adoption claim is now false while the thesis holds, and instructs the reader not to re-quote the NEW figures either. Goal corrected 470 to 508 with the disposition split. Completion criterion 2 corrected. The whole list repeated as correction 1 in the gate's "do not re-inherit" section |
| PR-007 | MEDIUM | IN-SCOPE | D. domain invariants; E. testing | `tests/test_work_kind.py:212-236` (choices pinned for BOTH `("ipd","set")` and `("specs","set")`), `:362-377`, `:425-443`; `tests/test_ipd_priority.py:155-167`; `cli.py:1298-1316` vs `:4320-4340`; child `lkexaw` E-07 | **Removing the plan-side `-` clearing sentinel breaks a test that also guards the SPEC side, and the resulting plan/spec divergence is recorded nowhere.** Child 01's E-07 (authorized by the maintainer's 2026-09-10 ruling on `b5sfwm` OQ-02) removes or narrows `-` on `aw ipd set --priority`/`--work-kind`. But `test_the_cli_choices_match_the_shared_vocab` asserts the shared vocabulary UNION the clearing sentinel for `("ipd","set")` AND `("specs","set")` in one test, so the change necessarily edits an anti-drift fence that also covers a verb this Set deliberately leaves alone, and two further tests drive `--priority -`/`--work-kind -` through the real CLI. The parent's Deferred section defers "specs and research" for the REQUIREMENT but says nothing about the flag surface, so a knowing asymmetry reads as an oversight | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Deferred section gained an explicit item naming the spec-side sentinel as deliberately untouched, all three caller sites with line numbers, who owns updating them (Order 01), and WHY the asymmetry is correct: the field stays genuinely optional on a spec while becoming conditionally required on a plan, so clearing it means different things on the two types |
| PR-008 | MEDIUM | IN-SCOPE | D. domain invariants; E. testing | `ipd_lint.py:1052` (terminal short-circuit to `legacy`), `:904-913`; verified an `executed/` plan lints clean at `--phase pre-execution` and an approved pending plan reports `IPD-M106` at `--phase author` | **The cross-check asserts the grandfather boundary is the disposition, and it is the disposition OR the persisted status; the missing half is the half that breaks the Set.** The authored cross-check exercised two states (one pending, one terminal) "to prove the grandfather boundary is the disposition and not the date". Measured, both halves are real: a terminal plan with neither field is clean at `pre-execution` (0 findings) because terminal-dir files short-circuit to `legacy`, AND an approved PENDING plan is refused at `--phase author`, not only at `pre-execution`, because the predicate returns true on the status. A cross-check that omits the second state would pass while the corpus it was meant to protect is refused | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Cross-IPD validation rewritten as a three-state exercise, each state carrying the measurement and the mechanism: terminal clean at every phase, pending-at-`to-review` clean at `author`, and pending-at-`approved` REFUSED with the refusal shown at `author` as well as `pre-execution`. The third is labelled as the case that strands the approved queue and must be exercised deliberately rather than inferred |
| PR-009 | LOW | IN-SCOPE | E. testing | measured bare `python3 -m pytest`: `5971 passed, 3 skipped, 2 xfailed in 64.82s` at HEAD `3ae4735e` | The Set deliberately stated no suite baseline, which is the right call, but it left the executor with nothing to notice a pre-existing failure against, and this repository has twice shipped plans whose stated baseline named the wrong failing test | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Required-tests section now carries the review-measured figure explicitly labelled FOR REFERENCE ONLY, with the instruction to re-measure and judge on the delta, plus the bare-run rule (the configured `addopts` already supply `-q -n auto --dist=worksteal`, so adding `-n0` or a second `-q` makes the run slower and suppresses the summary line the contract requires) |

PR-001 is DEFERRED-AS-OPEN under the Fix Bar at overall Remediation Risk Medium-High on the FUNCTIONALITY
axis (with a secondary complexity axis), and every element the Bar demands is stated. The axis is
functionality because the remedy changes which artifacts are runnable at each point in the Set's execution,
and complexity because two of the three options also change a child's internal design. The risk reaches the
threshold for two independent reasons: the fix's shape is a genuine scope-and-appetite choice among three
options with different blast radii (one grows Order 01's diff by ~109 files in a shared checkout, one makes
the whole Set wait on a human answering a table, one deliberately ships a rule that gates nothing for a
while), and the edit itself lands in the children's own `- Item-Dependencies:` lines, which are artifacts
this review's scope ledger does not cover. The required decision is the maintainer's choice among the three
costed options in OQ-02. The consequence of leaving it unresolved is that executing the Set as authored
makes 18 already-approved plans unrunnable, 13 of them until a human answers a decision table, with the
refusal appearing on plans unrelated to the one being executed. Effort, time, cost and tokens played no part
in this deferral.

Per the escalation rule the finding is raised in the plan as an open question carrying `- Blocking: yes` and
`- Finding: PR-001`, and the refusal was verified mechanically rather than assumed: `aw ipd lint --phase
review-finalize` exits 1 with `IPD-Q501` naming OQ-02 at line 110, so the plan cannot reach `approved` or
`begin` until the maintainer answers.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Should the three children be reviewed as part of this invocation, or treated as evidence only? | EVIDENCE ONLY. The ledger is the named orchestrator; the children were read in full, cited by `path:line`, and left unedited. | (a) Adding them to the ledger and reviewing all four, rejected because the workflow builds the ledger from the explicitly named targets plus what a documented eligibility rule adds, and no rule in this repository adds a Set's children; doing so would also silently produce four review records the maintainer did not ask for. (b) Editing a child's front matter to fix PR-001 without reviewing it, rejected because it would change an artifact whose surrounding design I had not critiqued, and the child would then carry an unrecorded edit. | `plan-review.md:46-68` (ledger construction); the `specdirs` precedent, where orchestrator `wfjsp4` and children `y4bdoz`/`1bdxcp` each carry their OWN `.review.md` | yes |
| D-2 | PR-005's fix: delete the parent's parked measurement, add a child for it, or leave it with a warning? | DELETE IT and convert E-01 into orchestration, citing where the measurement is actually covered. | (a) Adding a child to carry the premise measurement, rejected because both backfill children ALREADY re-derive their own populations and are told to trust no stated count, so a new child would duplicate work that is performed where it is consumed; AGENTS.md's "ADD A CHILD" rule is for work no child covers, which is not this case. (b) Leaving E-01 with a note that the runner skips it, rejected because it fails OPEN: the item would still be marked `performed` by a retirement that performed nothing, which is the exact dishonesty the rule exists to prevent. (c) Removing the parent's checklist entirely, rejected because AGENTS.md is explicit that an orchestrator SHOULD carry a checklist of its children, and deleting it causes the partial-execution loss it exists to prevent. | `oc_runipd.py:836-860`; spec `77tr3o:17` (OQ-1 ruling: rollup keeps every gate except the E/V checkpoint); child `8u6770` E-01 and `lc4unl` E-01 both re-deriving; AGENTS.md orchestrator paragraph | yes |
| D-3 | PR-002's sentinel/enum collision: prescribe the fix (amend both `aw check` rules), or state the constraint and leave the mechanism to Order 01? | STATE THE CONSTRAINT as a completion criterion naming both rule ids and the measured failure, and leave the mechanism to Order 01. | (a) Prescribing "add the sentinel to both rules' accepted sets", rejected because those two rules are the enum contract for plans AND are documented as deliberately treating absent-as-fine, so widening them is a decision about the vocabulary's meaning that belongs inside the child that owns `check_engine.py` and can test it. (b) Prescribing "do not use a metadata sentinel at all", rejected because it would overturn the `Scope-Paths` precedent this Set is explicitly mirroring, from outside the plan that implements it. (c) Saying nothing, rejected because criterion 3 as authored is impossible to satisfy and an executor would discover that only after writing the sentinel onto plans. | measured `check_plan_priority`/`check_plan_work_kind` flagging `grandfathered`, `unresolved` and `TODO` on both fields; `check_engine.py:2643-2695`, `:2697-2755`; child `lkexaw:7` already declaring `check_engine.py` | yes |
| D-4 | PR-003: which surface should the plan require for the Priority column? | BOTH `FORCE_COLOR=1 aw att --type plan` and `aw att --type plan --format json`, with the json count of non-null priorities as the number to state. | (a) Requiring only the colored table, rejected because a runner captures piped output and a `FORCE_COLOR` capture carries ANSI escapes that make a pasted table hard to read and easy to mangle. (b) Requiring only `--format json`, rejected because the Set's stated benefit is that the BOARD becomes useful, and proving the human-facing rendering is part of that claim. (c) Dropping the criterion, rejected because "the board labels a priority" is the Set's user-visible payoff. | `attention.py:2084-2101`, `:2280-2296`, `:1083`; three measured surfaces | yes |
| D-5 | PR-004: replace "paste `aw check plans` clean" with a baseline-delta, or narrow the command to this Set's rules? | BASELINE-DELTA over the whole command, plus an explicit prohibition on editing another party's plan to reduce the count. | (a) Narrowing to `--rule check.priority-invalid,check.work-kind-invalid` style filtering, rejected because it would hide a NEW rule id this Set might introduce, which is the main thing the check is there to catch. (b) Requiring the 140 be fixed first, rejected outright: they belong to other plans and other agents' in-flight work, and sweeping them is exactly what the shared-checkout rule forbids. | measured 140 errors with per-rule counts; `.github/workflows/tests.yml:153-155` (the same engine is fail-closed in CI); AGENTS.md shared-checkout paragraph | yes |

Nothing in the Set's approach was rejected. The thesis, the four-way decomposition, the decision to make the
requirement conditional in the checkpoint layer rather than in `META_REQUIRED`, and the reuse of the
`Scope-Paths` pattern are all sound and all re-verified. What review supplied was the pending-but-approved
corpus the Set's two frames left out, the shipped enum rules its sentinel collides with, two evidence
commands that cannot answer their own question, one deliverable parked where nothing performs it, and four
corrected figures.
