# Review: move the verification default into the per-host runner registry row (child tm2cz8, Set hostdefault)

- Subject-Id: tm2cz8
- Subject-Type: ipd
- Reviewed-At: 2026-09-06
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `aeb71ca2`. Structural preflight `aw ipd lint --phase author` conformed before semantic
review (one advisory `IPD-Z602` density note, no error) and `--phase review-finalize` conformed after the
revisions.

DISCLOSURE: this plan was authored earlier in the same session, so this is a SELF-REVIEW and worth less
than an independent one. It also inherits a self-review from its predecessor `mn3gwr`, which makes the
chain of self-assessment two deep. That matters concretely here, because the three most serious findings
below are all cases where the AUTHORING measured something with a command that could not see the answer,
and then reported the number as if it were the whole truth.

THE PLAN'S THESIS IS TRUE AND I RE-VERIFIED IT FROM SCRATCH rather than trusting the authored prototype
transcript. I rebuilt the patch in an isolated worktree at HEAD and measured: with an EMPTY store,
`resolve(runner="oc").validate` is `False` with provenance `shipped-default` and
`resolve(runner="agy").validate` is `True` with the same provenance, so tier 4 really does become
per-host; `defaults.validate: false` still beats the row on agy (provenance `defaults`), so the row is a
FLOOR and not an override; an `agy` profile OMITTING `validate` inherits `True` while one declaring
`validate: false` resolves `False`/`profile`, so the tri-state does not collapse on the new host; a
profile for `agy` carrying `variant` is refused with the existing per-row message, so `supports_variant=False`
is load-bearing; and `canonical_runner` maps `agy`/`AGY`/`antigravity` to `agy`. The registry really is
absent from the validate chain (`RUNNER_REGISTRY` does not appear in `inspect.getsource(resolve)`), the
runner block really does resolve at function-relative line 28 against the chain at 102, and F-4's four
divergent host populations are all real (`ALL_ADAPTER_HOSTS` 6, `aw host capabilities` 2, `RUNNER_REGISTRY`
1, `RUNNER_ADAPTERS` 1). The diagnosis and the chosen location are correct.

WHAT ROUND 1 CHANGED is not the plan's shape but its HONESTY ABOUT ITS OWN MEASUREMENTS, plus two
authoring traps that were measured and forbidden, plus two E-items for work the row makes reachable and
the plan did not name. Scope-Paths grew from four to six; `Highest E allocated` from 06 to 08.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-501 | HIGH | IN-SCOPE | E. testing; evidence accuracy | `tests/test_runner_profiles_e2e.py:58`; `pyproject.toml:169`; measured `no tests ran in 2.00s` | **The blast radius is SIX tests, not five, and the sixth was structurally invisible to the command the plan mandates.** `tests/test_runner_profiles_e2e.py` sets `pytestmark = pytest.mark.slow` and the configured `addopts` supply `-m 'not slow'`, so the BARE `python3 -m pytest` this repository requires DESELECTS that file's 30 tests entirely; naming the path directly does not help either, reporting `no tests ran`. Re-measured with the prototype under `-m slow`: `NegativeE2E::test_wrong_runner_profile_is_not_launched_by_opencode` fails on BOTH its message assertions. The plan's F-7 already knew that test's comment had to move, but classified it as prose while it is a real failure, and the file was in Scope-Paths, so an executor pasting a green bare suite would have reported success with a Scope-Paths file red | C:Low; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | F-6 corrected to six failures in three files with both commands' measured output; new F-10 states why the sixth was invisible and generalizes the rule (a Scope-Paths file that is `slow`-marked forces a `-m slow` run). E-05 count corrected to SIX with the sixth enumerated and its observed message quoted. Validation section and the gate's HARD HONESTY RULE now require BOTH commands' summary lines, with the reason stated so it is not mistaken for flag-bolting. V-05 rejects a `no tests ran` paste as evidence |
| PR-502 | HIGH | IN-SCOPE | E. testing; G. executability | bare `1 failed, 5554 passed` and `-m slow` `6 to 8 failed` at `aeb71ca2`; `test_orchestrator_retirement.py:875` | **The plan's stated pass criterion is unmeetable, because HEAD is not clean and the authored baseline is stale.** The plan requires validating against `5462 passed, 3 skipped, 2 xfailed` from `f3e17ff6`. Measured at HEAD: the bare suite has a pre-existing failure (`RealRepositorySets::test_runprofile_refuses_for_R2_and_NOT_for_unauthored_rows`, `{'kgpptv': 'approved'} != {'kgpptv': 'reviewed'}`, which asserts against a SIBLING PLAN's live status and broke when that plan was approved), and the slow subset fails 6 to 8 tests varying between runs. An executor comparing to the plan's number faces a criterion it cannot meet, and the two available wrong responses are both bad: fix an unrelated failure inside a fenced plan, or paste a red suite and call it expected | C:Low; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | New F-11 records both measured baselines, names the pre-existing failure and its cause, and notes the slow subset's order-dependence. The validation section now requires the executor to MEASURE ITS OWN before-baseline and judge on the DELTA (after-minus-before must be empty), and the gate adds a BASELINE HONESTY paragraph forbidding both wrong responses by name |
| PR-503 | HIGH | UNDER-SCOPE | A. correctness; B. security posture | `runner_profiles.py:401-402`, `:517`, `:564`, `:884`; `run_dispatch.py:180-196`; live two-route probe | **Registering the row makes a new STORE STATE writable, and the plan's E-04 pinned only one of the two dispatch routes that state reaches.** `canonical_runner` is the write-time gate for `parse_profile`, `_validate_referential_integrity` AND `set_default_runner`, so `default_runner: agy` becomes storable, and `aw run ipd <selector>` then reaches `adapter_for` via `resolve_default_runner` with NO profile named at all. E-04 named only `aw run as <agy-profile>`. Measured with an isolated store holding `default_runner: agy`: both routes exit 2 with the registered-but-unimplemented message and launch nothing, so the fail-closed guarantee HOLDS - but it was unproven for the route the change newly opened, and the plan nowhere acknowledged that a store can now hold a configuration nothing can run, which partially inverts `canonical_runner`'s own documented rationale ("a failure the user should see at write time, not at 3am") | C:Low; U:Low; S:Medium; F:Low; Overall:Medium | FIXED | New F-13 records the write-time gate set, the newly-writable state, and the two-route live measurement. E-04 now requires BOTH routes and cites why the default route is the one that newly became reachable. V-04 declares a single-route paste a FAILED validation. Scope prose and the Deferred section now state the accepted consequence explicitly, with the mitigations that make it acceptable (clear exit-2 refusal at every entry point; the wizard still writes only `oc`; reaching the state requires hand-editing) and the honest follow-up if the adapter does not land soon (a store-load warning, not authorized here) |
| PR-504 | HIGH | IN-SCOPE | A. correctness; G. executability | measured `NameError: name 'RUNNER_REGISTRY' is not defined`; `runner_profiles.py:112` vs `:207` | **E-02's own instruction contains an import-breaking trap.** It says to retain `SHIPPED_VALIDATE_DEFAULT` as "the value the `oc` row carries", and the obvious reading of that sentence is `SHIPPED_VALIDATE_DEFAULT = RUNNER_REGISTRY["oc"].validate_default`. That is a module-level FORWARD REFERENCE: the constant is defined at `:112` and the registry at `:207`. Measured: `NameError` at import, which breaks the entire package rather than one test, and would present to the executor as a total collapse of the suite immediately after a one-line edit | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-02 now forbids that expression by name, pastes the measured `NameError`, states the two acceptable options (keep the literal, preferred; or move the constant below the registry and say so), and notes the docstring/reading-order coupling of the second. F-8 extended with the ordering fact. V-02 requires pasting a successful `import agent_workflows.runner_profiles`, and the validation section adds that import as a check. E-06 gains an assertion that the retained constant equals the row, which is what keeps a literal from drifting |
| PR-505 | HIGH | IN-SCOPE | E. testing; D. invariants | measured `RunnerSpec(..., validate_default="yes").validate_default == 'yes'` | **E-06(c), the guard the plan calls "the one that would have caught the defect this plan fixes", is specified in a form that does not catch the defect class it targets.** It says to assert every row's `validate_default` "is a `bool`", but a `NamedTuple` annotation is NOT a runtime check: measured, `RunnerSpec(..., validate_default="yes")` constructs fine and returns the string, which is TRUTHY, so an unexamined future row would resolve tier 4 to a silent verification-ON. Worse, the natural cheap implementation `assertIn(value, (True, False))` also passes for `1`, because `1 in (True, False)` is `True` in Python. So the guard as written could ship while admitting exactly the unexamined row it exists to stop | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-06(c) now states the measurement, mandates `isinstance(..., bool)` or `type(...) is bool`, and explicitly rejects the `assertIn` form with the `1 in (True, False)` reason. V-06 requires proof the guard fails in BOTH ways (omitted field AND non-`bool` truthy value), and states that catching only the omission is half a guard |
| PR-506 | MEDIUM | UNDER-SCOPE | A. correctness; F. principles | live `resolve(cfg, runner="agy", variant="high", agent="build")` under the prototype; `runner_profiles.py:517-531`; `agy_runipd.py:2667` | **The row makes an unexamined code path reachable that the plan never mentions.** `parse_profile` enforces `supports_variant`/`supports_agent` for a STORED profile, and the plan leans on that (F-9, E-03). But `resolve()`'s per-field block consults NEITHER flag: measured, `resolve(runner="agy", variant="high", agent="build")` returns both values with provenance `explicit`, silently carrying fields that host's argv builder does not emit (it appends only `--model`). Before this plan the path raised at `canonical_runner` and was unreachable; after it, it resolves. That is the store and the caller enforcing DIFFERENT row contracts, which is the inconsistency class this plan exists to remove one instance of | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | New E-08 requires the executor to DECIDE (refuse in `resolve()` for consistency with `parse_profile`, or accept and document on the ground that no dispatch path reaches agy anyway), comment the reason at the site, and pin the chosen behavior with a test; it is explicitly bounded to the two existing `supports_*` flags in `resolve()` for an explicit value, not a general capability audit. F-9 corrected to distinguish the stored case (enforced) from the explicit case (not). New V-08 requires the choice, its reason, the probe, the test, and a search for any shipped test asserting the permissive behavior |
| PR-507 | MEDIUM | UNDER-SCOPE | G. executability; honest documentation | `runner_profiles.py:194`, `:205-206`; `run_dispatch.py:101`; `runner_profile_wizard.py:71-73` | **Four in-code prose statements assert a one-host registry and become false, two of them in modules the plan's Scope-Paths did not include.** The plan updates only the module docstring's precedence block (E-02). It leaves `RunnerSpec`'s own docstring ("Version 1 registers OpenCode only" - the docstring of the very type it extends), the `RUNNER_REGISTRY` comment (which also enumerates only opencode's field support, now materially wrong since the two rows differ), `run_dispatch.py:101` ("registers OpenCode only, matching `runner_profiles.RUNNER_REGISTRY`" - wrong in the dangerous direction, since the two tables' DISAGREEMENT is precisely what makes `adapter_for`'s second branch reachable), and the wizard's premise. A reader who believes `run_dispatch.py:101` cannot understand why `adapter_for` refuses a name the schema accepted | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | New F-12 enumerates all four with their evidence. New E-07 corrects all four, COMMENT-ONLY, keeping each site's still-correct load-bearing sentence (the registry docstring's "Adding a host is ONE ROW here", the wizard's "a second host needs its own adapter, not a widened wizard") and keeping `RUNNER = "oc"` unchanged. `run_dispatch.py` and `runner_profile_wizard.py` added to Scope-Paths with the justification recorded in Scope check; the fence forbids any EXECUTABLE change in either. New V-07 requires the full diff of both modules and a statement that no executable line moved |
| PR-508 | MEDIUM | IN-SCOPE | Evidence accuracy | `oc_runipd.py:6279-6288` vs cited `:6212-6215`; `agy_runipd.py:3568-3574` vs cited `:3474-3482` | **Every driver line citation in the plan had drifted by roughly 70 to 95 lines between authoring and review, in ONE day.** The plan was authored at `f3e17ff6` and the two drivers changed by 246 insertions since. All cited constructs still EXIST and every claim about them is TRUE (verified at both revisions), so this is citation rot rather than a false claim, but a reviewer or executor spot-checking `oc_runipd.py:6212` today lands 70 lines from the gate | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Concern and F-2/F-9 citations re-resolved against HEAD. Added a paragraph to Concern recording that the drift was measured, quantifying it, and noting that this is why the gate's RE-LOCATE BY SYMBOL rule is not optional advice. Also noted that no driver is edited here, so these citations are evidence only |
| PR-509 | LOW | IN-SCOPE | E. testing; clarity | `tests/test_run_dispatch.py:266-267`, `:337-341`, `:395-401` | Two mechanical points E-05 does not cover. (1) `test_only_oc_is_registered_in_version_1` does NOT break (it reads `RUNNER_ADAPTERS`, and no adapter is added), and its continuing to pass is the best available PROOF that E-04 added no adapter - but the plan does not say so, and its name becomes misleading, inviting an executor to "fix" the very test that proves the no-adapter guarantee. (2) The two `mock.patch.dict` sites simulate a fictional `agy`; once `agy` is a REAL row they silently SHADOW shipped data instead of adding a host, which is a weaker test than intended | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-05 now names `test_only_oc_is_registered_in_version_1`, explains why it must stay UNCHANGED and that its passing is required proof, and permits only a clarifying comment. V-04 requires pasting it passing unchanged alongside `sorted(RUNNER_ADAPTERS)`. E-05(4)/(5) suggests renaming the simulated host to avoid shadowing a real row, marked a clarity improvement rather than a requirement |
| PR-510 | LOW | IN-SCOPE | Project rule conformance | `plan-review.md:353-362` (2026-09-01 maintainer ruling) | The gate's closing paragraph ended "If any of the five cannot be updated that way, STOP and report rather than proceeding." That is the STOP-over-a-scope-question wording the 2026-09-01 maintainer ruling specifically forbids: a fence is a DECLARATION so the runner can tell afterwards what happened, and a stop directive here contradicts the work done to stop `aw oc run` stranding unfinished turns. The condition it guards (a test that cannot be updated to pin the same property) is a scope/quality question, not a genuinely unsafe condition | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Rewritten to require RECORDING the gap in the plan and continuing with the remaining items, noting that the fence and `aw ipd finalize` already surface an unfinished item and that a half-updated suite left uncommitted is worse than a recorded gap. Count corrected to six |

No finding was DEFERRED, left OPEN, or marked REPLAN, so no escalation to a `- Blocking: yes` question was
required. Both pre-existing open questions (OQ-01 provenance vocabulary, OQ-02 retaining the constant) were
re-checked and remain correctly resolved and correctly non-blocking; OQ-02's reasoning is now STRONGER than
authored, because PR-504 shows retention must additionally be a literal.

Nothing was rejected. The five HIGH findings are all cases of a correct plan under-describing its own
blast radius or containing a trap for its executor, every one repairable with bounded in-place edits, and
none of them touches the thesis, the chosen location, or the measured host postures.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-501: does requiring `python3 -m pytest -m slow` violate the repository contract that says to run the suite BARE and not to bolt on flags? | No: require BOTH, with the reason stated in the plan. The contract's target is flags that DEGRADE the run (`-n0`, a second `-q`, `-p no:randomly`); `-m slow` REPLACES the configured marker filter to reach tests the bare run deselects, adding coverage rather than removing it. | Bare only, REJECTED: a Scope-Paths file would be unvalidated and a green paste would be misleading, which is exactly the failure this finding found. Removing the `slow` marker from the e2e file, REJECTED: out of scope, and it would slow every bare run for every future change. | `AGENTS.md` HOW TO RUN THE SUITE paragraph (which enumerates the forbidden flags by their degrading effect); `pyproject.toml:169`; `tests/test_runner_profiles_e2e.py:58`; measured `no tests ran` for the direct-path invocation | yes |
| D-2 | PR-503: is a store that can hold `runner: agy` while nothing can launch it an acceptable outcome, or must this plan add a dispatch adapter or a store-load warning? | Acceptable, with the consequence stated explicitly in Scope and Deferred and both refusal routes pinned by E-04/V-04. | Adding the `agy` adapter, REJECTED: that is host-neutral dispatch for a second host, with its own argv contract and its own `main`, and folding it in turns a data-shape plan into a dispatch plan. Adding a store-load warning, REJECTED for this plan: it is a new user-facing behavior on a path this plan does not otherwise touch, and it is named as the honest follow-up if the adapter does not land. Declining to register the row at all, REJECTED: that is the whole plan. | Measured two-route exit-2 refusal with an isolated store; `runner_profile_wizard.py:73` (`RUNNER = "oc"`, so the wizard cannot create the state); `runner_profiles.py:401-402` (the rationale being partially inverted, which is why it is stated rather than glossed) | yes |
| D-3 | PR-506: should E-08 MANDATE refusing an unsupported explicit `variant`/`agent`, or mandate a recorded decision? | Mandate a recorded decision plus a test, naming both acceptable options and their reasoning. | Mandating a refusal (the more consistent answer, and said so), REJECTED as a mandate because it changes `resolve()`'s behavior for a path no dispatch route can reach today, and the executor holds the current tree and can see whether any caller depends on the permissive behavior. Mandating acceptance, REJECTED: it leaves the store and the caller enforcing different row contracts with no comment saying that is deliberate. Leaving it unmentioned, REJECTED: the row makes the path reachable, so silence would ship an accident. | `runner_profiles.py:517-531` (the stored-profile enforcement the explicit path lacks); live probe returning `variant='high'` with provenance `explicit`; `agy_runipd.py:2667` (only `--model` emitted) | yes |
| D-4 | Are PR-501/502/503 BLOCKERs or HIGHs? | HIGH, all three. | BLOCKER, REJECTED for each: none silently violates an invariant or loses data at runtime. PR-501 and PR-502 produce a MISLEADING GREEN REPORT, which is serious but detectable by re-running the correct command; PR-503's guarantee was measured to HOLD on both routes, so the defect is an unproven claim rather than a broken one. MEDIUM, REJECTED for each: the wrong response to PR-501/502 is to report success falsely, and the wrong response to PR-503 is to ship an unproven fail-closed claim on a newly-opened route. | `plan-review.md:504-509` (severity definitions); the measured two-route refusal for PR-503 | yes |
| D-5 | PR-508: correct the drifted citations, or leave them and rely on the plan's own RE-LOCATE BY SYMBOL rule? | Correct them, AND record the drift with its magnitude as a stated reason the symbol rule exists. | Leaving them, REJECTED: a citation that lands 70 lines away costs every future reader a search and quietly erodes trust in the plan's other citations. Removing line numbers entirely, REJECTED: they are genuinely useful at the moment of writing and the symbol rule already covers their decay. | Cited constructs re-resolved at both `f3e17ff6` and `aeb71ca2`; `git diff --stat` showing 246 insertions across the two drivers in one day | yes |
| D-6 | Readiness value. | `go-pending-approval`. | `go`, REJECTED: `Status: reviewed`, no human sign-off has occurred. `no-go`, REJECTED: no open question remains and no BLOCKER or HIGH is left unfixed, all ten findings being FIXED in place. | `plan-review.md:531-546` | yes |

No `Reversible: no` decision was taken. D-2 is the one a maintainer may want to overrule, because it
accepts a user-reachable state (a store naming an unlaunchable host) that did not exist before, so it is
surfaced in the review report rather than left to be found in the diff.

### Verified claims

- The thesis holds, re-measured from a fresh prototype at HEAD `aeb71ca2` in an isolated worktree:
  `resolve(runner="oc").validate` is `False`/`shipped-default`, `resolve(runner="agy").validate` is
  `True`/`shipped-default`; `defaults.validate: false` beats the row on agy (`False`/`defaults`); an agy
  profile omitting `validate` inherits `True` while `validate: false` gives `False`/`profile`; a profile
  for agy carrying `variant` is refused (`profile 'bad': runner 'agy' does not support a model variant`);
  `canonical_runner` maps `agy`, `AGY` and `antigravity` to `agy`; `sorted(RUNNER_REGISTRY)` is
  `['agy', 'oc']`. The prototype was then reverted and its worktree removed.
- The registry really is absent from the validate chain: `RUNNER_REGISTRY` does not appear in
  `inspect.getsource(runner_profiles.resolve)`, so F-3's central claim (that `mn3gwr`'s blocker was
  overstated) is correct.
- The ordering claim is correct: the runner block resolves at function-relative line 28 and the validate
  chain begins at 102, so tier 4 can read the row with no restructuring.
- F-4's four divergent host populations are all real and re-counted: `ALL_ADAPTER_HOSTS` 6
  (`host_adapters.py:67-75`), `aw host capabilities` 2 (opencode, antigravity), `RUNNER_REGISTRY` 1,
  `RUNNER_ADAPTERS` 1.
- The two hosts' opposite postures are real, and the plan's characterization of oc's gate is correct at
  HEAD despite the citation drift: oc reads `opts.get("validate", False)` with a legacy fallback
  (`oc_runipd.py:6279-6288`) and its `--validate` is `BooleanOptionalAction, default=False`
  (`:7519-7527`); agy gates on `not no_verify` with `--no-verify` a `store_true`
  (`agy_runipd.py:3484-3490`, `:4564-4570`), and the "NOTE THE SEMANTIC DIFFERENCE" comment says so in as
  many words (`:3568-3574`).
- Antigravity really emits only `--model` (`agy_runipd.py:2667`) and declares only `--model`
  (`:4534-4538`), against opencode's three (`oc_runipd.py:5292-5297`), so the new row's `supports_*`
  values are measured rather than chosen.
- Spec `25kzda` says what the plan claims, quoted verbatim including the conditional reasoning that
  supports the host-level reading ("until per-role model routing exists ... a host-level control is the
  only available mechanism", `.aw/records/specs/20260826-0718-01-aw-run-deterministic-run-and-verify.spec.md:105`),
  so the plan is right that no spec change is authorized.
- Superseded plan `mn3gwr` is genuinely in `plans/superseded/` and child 02 `ybkmzp` genuinely declares
  `Item-Dependencies: executed:tm2cz8` with the same `From-Backlog: h7qsje` and `Blocks-Release: next`,
  so the inherited release gate is preserved across the split as the plan claims.
- Three tests really do reference `SHIPPED_VALIDATE_DEFAULT` (`tests/test_runner_profiles.py:889-893`,
  `tests/test_runner_profiles_e2e.py:807`), confirming F-8's retention argument.
- The `IPD-Z602` density advisory was reviewed rather than ignored: the plan now carries eight E-items
  across three task groups, each naming one deliverable in one module, and E-05/E-06 are the two largest
  by prose but are single-surface (one test file family each). No split is recommended.
