# Review: show what a source already graduated to before authoring another plan for it, orchestrator y9s4vm (Set graduate)

- Subject-Id: y9s4vm
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `d12c0c9d`. `aw ipd lint --phase author` returned `clean` exit 0 BEFORE semantic
review and `--phase review-finalize` returns `clean` exit 0 after every revision, so nothing found here
was structural. The plan's single open question (OQ-01) carries `- Blocking: no` and belongs to child
01's executor by its own `- Owner:` line, so no `IPD-Q501` gate applies and no maintainer decision was
invented.

DISCLOSURE: the same agent and model authored this plan, so this is a SELF-REVIEW. Its value therefore
rests on RUNNING the plan's claims rather than re-reading them, and that is where every finding came
from: nine load-bearing claims were re-measured, five held exactly, and four had drifted or were wrong.

SCOPE: only the orchestrator `y9s4vm` was a candidate. Both children (`jxxec8`, `iuxtjy`) were read as
EVIDENCE and edited only where a parent-level constraint binds them (the corrected corpus count, the
stale `f1sw71` status, the stale `--full-auto` premise, and the shared-traversal obligation); neither
was itself reviewed, and neither's `Status` was advanced. Also read: `check_engine.py`
(`check_from_spec_dangling`, `find_from_backlog_*`, `_iter_plan_ipds`, `_iter_spec_records`,
`_DEFAULT_RULESPEC`), `runner_shared.py` (`discover_specs`, `sweep_review_candidates_for_type`,
`dispatch_orchestrator_item`), `oc_runipd.py` and `agy_runipd.py` (`ACTION_IMPLEMENTED`,
`enforce_requested_action`, `evaluate_backlog_close`, `process_backlog_close`, both parsers),
`backlog.py` (`_render_item`), spec `25kzda` §1.3/§2.1/§3.3/§3.4, spec `6m4kow`, backlog `6h7y2y` and
`f1sw71`, pending plan `bwgyum` and its review record, and the plans corpus at both `a2e0438a` and
HEAD.

WHAT HELD, MEASURED RATHER THAN TRUSTED, and it is the large majority of this plan. `--action plan`
really does fail closed: `ACTION_IMPLEMENTED` is `frozenset(("review",))` in BOTH drivers
(`oc_runipd.py:2425`, `agy_runipd.py:1716`) and the refusal really does say "No run was started"
(`oc_runipd.py:2461-2467`), landed in `a3bb14bf` dated 2026-09-05 against the item's 2026-09-06 filing,
so the one-day-earlier claim is exact. `discover_specs` really exists at `runner_shared.py:1321`, really
is documented as "The SPEC sibling of :func:`discover_plans`", really adds no path literal (the AST
guard is `tests/test_spec_review_attestation.py:815`), really has exactly ONE consumer (the sweep at
`:1421`), and really found 9 of 28 specs at `a2e0438a` including `25kzda` at `approved` (verified in a
detached worktree at that commit; at HEAD it is 10 of 29, which is the corpus moving, not a wrong
claim). The forward dangling checks really are the only direction checked, `check.from-backlog-dangling`
really is `error` severity carrying `I-07`, and `_DEFAULT_RULESPEC` really is `error` with an empty
invariant. Spec `6m4kow` really has three executed plans (`eyh1fu`, `5slbpi`, `wpomxa`), which is the
plan's own motivating case and it is genuine. Spec `25kzda` §1.3 rows 101-102 and §2.1 line 292 really
do describe graduation as shipped behavior, so the compliance framing is right. The `25kzda` cluster
really is nine plans, eight `executed` and one `not-executed`, and the anti-uniqueness constraint really
is the correct call.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-901 | HIGH | UNDER-SCOPE | C. Architecture (duplicate mechanism; GUIDING_PRINCIPLES P8) | `.aw/records/plans/pending/20260908-setidhard-02-bwgyum-...ipd.md:14-16`, `:57`; `.aw/records/plans/pending/20260908-graduate-01-jxxec8-...ipd.md:37` | **ANOTHER SET IS BUILDING THE OPPOSITE DIRECTION OF THE SAME EDGE AND NEITHER PLAN KNOWS IT EXISTS.** `bwgyum` (Set `setidhard`, Order 02) is already `Status: reviewed` with `Readiness: go-pending-approval`, one approval away from executing, and its E-03 builds `check.graduated-to-dangling` by reading each plan's `- Set:` bullet through `check_engine._parse_setid` over `_iter_plan_ipds`. Child 01's E-01 builds the reverse index over the SAME iterator in the SAME module. These are the two directions of one relationship (which source became which Set) traversed twice, independently, which is the drift P8 forbids, and worse, the two could return DIFFERENT answers for the same source once a `Graduated-To` is hand-written and a `From-Backlog` is not, or vice versa. A grep of both Sets for cross-references returns ZERO hits in either direction, so nothing today would stop both from landing. | C:Medium; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | Added F-10 to the parent, F-9 to child 01, a binding hard constraint in the parent's shared-constraints block, and CID-7 requiring ONE mechanism after both Sets land with the two directions shown to agree. The constraint is symmetric and order-independent (whichever lands second consumes the first and says which case applied), so it cannot be satisfied by luck, and V-02 now demands the shared symbol be NAMED with an honest "`bwgyum` has not landed" answer permitted. Deliberately NOT resolved by re-sequencing the two Sets: neither depends on the other's OUTPUT, so a dependency edge would serialize work that can proceed in parallel, and the real requirement is one traversal rather than one order. |
| PR-902 | HIGH | IN-SCOPE | A. Correctness (the parent asserts an outcome its child may not produce) | `.aw/records/plans/pending/20260908-graduate-02-iuxtjy-...ipd.md:151`, `:205`; parent E-03 as authored | **THE PARENT REQUIRED `6h7y2y` TO READ `done` WHILE ITS OWN CHILD IS EXPLICITLY PERMITTED TO LEAVE IT `graduated`.** Child 02's OQ-01 permits deferring the backlog half (there is no `discover_backlog` to consume, unlike `discover_specs`), and child 02's own gate then requires `graduated` plus a filed follow-on. So the parent's acceptance criterion contradicted its child's sanctioned outcome: an executor honoring the parent would either fail a correctly-completed Set or close an item whose half is unbuilt, and closing it would drop the deferred work silently, which is precisely what the `graduated`-versus-`done` distinction exists to prevent. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-03 rewritten to a CONDITIONAL criterion (`done` if the backlog half shipped, `graduated` plus the follow-on id if OQ-01 deferred), the completion criteria and V-03 updated to match, and F-12 records it. |
| PR-903 | HIGH | IN-SCOPE | A. Correctness (asserted automation that cannot fire) | `oc_runipd.py:6863`; `agy_runipd.py:3907`; `runner_shared.py:3258`; `oc_runipd.py:1179-1203` | **THE PARENT SAID BACKLOG `6h7y2y` IS "CLOSED BY CHILD 02", AND THE RUNNER CANNOT DO THAT FOR THIS SET.** Measured three ways. `process_backlog_close` is called ONLY at an agent-executed IPD's finalize (the two sites above). Orchestrator retirement is not an agent turn and never calls it: `'backlog' in inspect.getsource(dispatch_orchestrator_item)` is False, and so is the same test on `ipd_lifecycle.retire_orchestrator`. And `evaluate_backlog_close` requires EVERY IPD carrier terminal `executed`, while the ORCHESTRATOR itself carries `- From-Backlog: 6h7y2y`; run live, the verdict is `close=False, reason='IPD carrier(s) not executed: ...y9s4vm..., ...jxxec8..., ...iuxtjy...'`. Since retirement is the last thing that happens and it does not close, no moment exists at which the run can close this item. The corpus confirms the pattern rather than contradicting it: all three auto-closed items in `done/` (`q5pdiy`, `0zj66l`, `2k42zu`) have ZERO orchestrator carriers, while the three whose Sets had one (`kjzlgw`, `1ap48y`, `kxkc04`) were each closed by hand afterwards with a written justification, two of them explicitly recording the unretired-orchestrator caveat. | C:Low; U:Medium; S:Low; F:Medium; Overall:Medium | FIXED | E-03 now states the mechanism with its measurements, names the deliberate `aw backlog set ... --status` act (the spelling that runs the release-gate predicate) as the closing route, and V-03 requires the ACTOR and the actual invocation or verdict to be pasted rather than a claim that the runner did it. F-12 on the parent and F-10 on child 02 record it, and child 02's gate now predicts the refusal instead of promising the close. |
| PR-904 | MEDIUM | IN-SCOPE | A. Correctness (central measurement wrong, with a design consequence) | re-walked a clean `a2e0438a` archive; five plans named | **THE HEADLINE CORPUS COUNT IS WRONG IN A WAY THAT WOULD PRODUCE A WRONG INDEX.** The plan states 125 source-linked plans in the Concern, in F-2, and in child 01 twice. At `a2e0438a` there are 125 source-link BULLETS but only 120 FILES carrying one, because FIVE plans carry BOTH a `From-Spec:` and a `From-Backlog:` bullet: `5942n7`, `pgq326`, `84j8d7`, `ueg5cf` (each `Backlog: kxkc04` + `Spec: 77tr3o`) and `h0zljh` (`Spec: 7ckptx` + `Backlog: vqv9im`). This is not pedantry about a number: child 01's E-01 says to reuse the existing readers, and those readers are single-match BY DESIGN (`find_from_backlog_plans` uses `_META_FROM_BACKLOG_RE.search`, `check_from_spec_dangling` uses `_ITEM_FROM_SPEC_RE.search`), which is correct for "does THIS plan's link resolve" and WRONG for a reverse index. An executor reusing a reader as instructed would drop one edge per dual-link plan and under-report the `77tr3o` and `vqv9im` clusters. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Corrected to 120 files / 125 bullets in the Concern and F-2, with the five plans named; F-9 added on the parent and F-8 on child 01; child 01's E-01 now requires indexing EVERY source bullet and explicitly warns that the reused readers are single-match; CID-6 added and V-01 on both plans now demands a dual-link plan be shown under BOTH its sources. The review-time figure (166 bullets / 106 sources / 24 clusters, one day later) is recorded as the derive-never-pin rule proving itself. |
| PR-905 | MEDIUM | IN-SCOPE | D. Anti-regression (stale premise for a live safety rule) | `agy_runipd.py:4792`; `build_parser().parse_args(['start','xxxxxx']).full_auto` on both hosts | **CHILD 02'S HOST-ASYMMETRY PREMISE IS FALSE AT HEAD, AND IT IS THE STATED REASON FOR A SAFETY CONSTRAINT.** Its E-05 and F-6 say `--full-auto` "DEFAULTS TO TRUE on the agy host and false on oc" and make that the reason a widened `ACTION_IMPLEMENTED` is dangerous there. Measured: BOTH hosts return `full_auto=False`, normalized by a maintainer ruling of 2026-09-04 whose in-code comment states the reason plainly ("this host declared `default=True` ... and this one defaulted to the LESS safe direction"). The danger is real on a different mechanism (`determine_action` returns `execute` for BOTH `approved` and `reviewed`, so `--full-auto` when PASSED still executes a merely-reviewed plan, which spec `25kzda` §2.6 forbids), but an executor who checks the default, finds `False`, and concludes the caution was overtaken would widen a legality table having disproved the only argument the plan gave them. | C:Low; U:Low; S:Medium; F:Medium; Overall:Medium | FIXED | Child 02's E-05 rewritten to state the correction, keep the caution on its surviving mechanism, and require the default to be RE-MEASURED on both hosts at execution time with a report if it has moved again; F-6 amended; F-9 added to child 02; a parent Step 0 convention and F-11 record it; child 02's V-05 now demands both hosts' measured defaults rather than agy's alone. |
| PR-906 | MEDIUM | IN-SCOPE | B/G (a cited blocker's status is wrong in three places) | `aw find backlog f1sw71`; `.aw/records/plans/pending/20260908-specreq-01-si24ia-...ipd.md:17` | **THE BLOCKER EVERY EXCLUSION RESTS ON IS CITED AS `open` AND IS `graduated`.** `f1sw71` sits in `.aw/records/backlog/graduated/` and has a carrier: decision plan `si24ia` (`to-review`, carrying `- From-Backlog: f1sw71`), which deliberately builds nothing and instead measures the corpus and RECOMMENDS, because the item is an open question whose requester said "definitely not sure". The GAP is genuinely unbuilt, so every exclusion resting on it stands unchanged, and this is a labelling defect rather than a design one. It still matters: three plans tell an executor the blocker is `open`, and an executor who looks it up, finds it graduated with a plan attached, may reasonably conclude the already-implemented case is now being solved and that the honest-limits requirement can be softened. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Corrected in all three plans (parent F-8 and its Deferred entry, child 01's history note and Deferred entry, child 02's Deferred entry), each stating that the item is `graduated` to `si24ia`, that `si24ia` builds nothing, and that the exclusion is therefore unchanged. A parent Step 0 convention records it. |
| PR-907 | MEDIUM | IN-SCOPE | E. Testing (stale baseline in all three plans) | measured bare `python3 -m pytest` at HEAD; `python3 -m pytest tests/test_orchestrator_retirement.py` | **THE SUITE BASELINE IS STALE IN BOTH HALVES AND NAMES A TEST THAT NOW PASSES.** All three plans declare `1 failed, 5648 passed` with the failure being `tests/test_orchestrator_retirement.py`. Measured bare at HEAD: `1 failed, 5919 passed, 3 skipped, 2 xfailed in 51.91s`, and the failing node is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, which is ENVIRONMENTAL, caused by a gitignored local `opencode-recovery/` dump in this checkout, so it may be absent elsewhere. `tests/test_orchestrator_retirement.py` runs `112 passed`. The plans' own compare-by-node-id rule is what keeps this from being harmful, but a stale attribution invites an executor to dismiss a real new failure as "the known one". | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Corrected in all three plans with the failure labelled environmental and possibly absent elsewhere, the named test recorded as now passing, and the instruction to measure your own and quote neither figure. |
| PR-908 | LOW | IN-SCOPE | F. KISS / G. Executability (an open question with no measured ground) | `aw find plans --help`; `aw check all --agent` | OQ-01 asks whether the view is a `check` rule, a read surface, or both, and lists considerations without measuring either option's cost, which invites the child to pick the cheap-looking one. Two facts change the balance. There is no read surface to extend: `aw find <type>` accepts `--status`, `--id`, `--set`, `--topic`, `--slug`, `--order` and nothing keyed on a source link, so "expose it via `find`" means a new filter dimension rather than a flag. And an `info` rule is not free either: `aw check all` reports 238 findings at HEAD (174 `check.scope-drift`, 40 `check.setid-collision`, 15 `check.lifecycle-transition-invalid`, plus singletons), so an `info` finding on every legitimate cluster lands in a stream readers already filter. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Both measurements added to OQ-01's rationale, explicitly stating that neither decides the question and that both raise the cost of the cheap-looking answer. Left OPEN and non-blocking: it is child 01's design decision by its own `- Owner:`, and the parent performs no work that depends on it. |
| PR-909 | LOW | IN-SCOPE | A. Correctness (verification of sound claims) | see resolution | Nine load-bearing claims were re-run rather than re-read, and five hold EXACTLY, which is worth recording so a later reader need not redo it: the `--action plan` fail-closed refusal and its `a3bb14bf`/2026-09-05 date against the item's 2026-09-06 filing; `discover_specs`'s existence, its no-new-path-literal AST guard, its single consumer, and its 9-of-28 coverage measured in a worktree AT `a2e0438a`; the absence of any reverse-direction rule with `check.from-backlog-dangling` at `error`/`I-07`; spec `6m4kow`'s three executed plans; and spec `25kzda` §1.3/§2.1 describing graduation as shipped. The nine-plan `25kzda` cluster and its 8-executed/1-not-executed split also verify exactly. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Recorded here; no plan edit needed. The four claims that had drifted are PR-904 through PR-907. |

WHAT THIS ROUND CHANGED. The plan's DESIGN is untouched and that is the honest summary: the
anti-uniqueness constraint, the advisory-not-refusing posture, the honest-limits requirement, the
consume-`discover_specs`-rather-than-duplicate rule, and the guard-before-verb sequencing are all
correct and all verified. Six of nine findings are measurement drift or a mislabelled status. The two
that matter are structural rather than cosmetic. PR-901 is the one a human should notice: a plan that is
one approval away from execution is building the mirror image of this Set's central mechanism, in the
same module, over the same iterator, and neither plan mentions the other, so absent the constraint added
here both would have landed and the repository would have had two independently-maintained answers to
"what did this source become". PR-903 is the one an executor would have tripped over: the parent
promised a backlog close that the runner provably cannot perform for a Set whose orchestrator carries
the link, which would have produced either a false completion claim or a confusing refusal at the end of
an otherwise successful Set.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-901: `bwgyum` builds the forward direction of the same edge and is already `reviewed`/`go-pending-approval`. Add a dependency edge between the Sets, ask the maintainer to sequence them, or impose a shared-traversal constraint? | IMPOSE A SYMMETRIC, ORDER-INDEPENDENT SHARED-TRAVERSAL CONSTRAINT on the parent, binding whichever Set executes second to consume the first's work and to record which case applied, verified by a new CID-7 and by V-02's demand that the shared symbol be named. | (a) Add `- Item-Dependencies:` from child 01 to `bwgyum`, rejected because neither Set needs the other's OUTPUT to function, so the edge would serialize work that can safely proceed in parallel and would block this Set behind another Set's approval gate for a reason that is not a real data dependency. (b) Ask the maintainer to choose an order, rejected because the repository answers the actual question: the requirement is ONE traversal, not one order, and P8 states that directly, so asking would be asking a human to decide something already decided. (c) Say nothing and let both land, rejected as the measured hazard: two traversals of one relationship in one module that can disagree about the same source. | GUIDING_PRINCIPLES P8 (single source of truth, no drift); `bwgyum:57` reads `- Set:` via `_parse_setid` over `_iter_plan_ipds` and child 01's E-01 walks the same iterator; zero cross-references between the two Sets | yes |
| D-2 | PR-902/PR-903: the parent asserts `6h7y2y` reads `done`, closed by child 02. Hold that, or change the criterion? | MAKE E-03 CONDITIONAL on child 02's OQ-01 outcome (`done` if the backlog half shipped, `graduated` plus a follow-on if deferred), AND state the close as a deliberate post-Set act with the measured reason the runner cannot do it. | (a) Keep `done` unconditional and remove child 02's deferral permission, rejected: the deferral is well-reasoned (there is no `discover_backlog` to consume, so that half means BUILDING an enumeration over a tree `aw backlog` owns) and the parent has no business overriding a child's sanctioned scope call from the outside. (b) Keep the parent's wording and let the executor discover the refusal, rejected: the refusal names the unretired orchestrator, which reads like a defect rather than the expected outcome, and an executor could "fix" it by closing the item with the positional `aw backlog set` spelling, which skips the release-gate predicate entirely. | child 02 OQ-01 and its gate paragraph; `evaluate_backlog_close(repo,'6h7y2y',[])` -> `close=False, 'IPD carrier(s) not executed: ...'`; `'backlog' in getsource(dispatch_orchestrator_item)` is False; three hand-closed `done/` precedents with orchestrator carriers versus three auto-closed with none | yes |
| D-3 | PR-904: the corpus figure is 125 bullets over 120 files. Correct the number, or also change child 01's instruction? | CORRECT THE NUMBER AND ALSO CHANGE THE INSTRUCTION, because the gap between the two figures IS the bug: five plans carry two links, the existing readers are single-match by design, and "reuse the existing readers" as written would drop one edge per such plan. | (a) Fix only the count, rejected: the count is a symptom and the instruction is the defect, so fixing the prose would leave an executor to reuse a `.search`-based reader and silently under-report two real clusters. (b) Drop the counts entirely so they cannot go stale, rejected: the count is what makes the anti-uniqueness constraint concrete (17 legitimate clusters is the whole argument against a `count > 1` rule), so instead each figure is dated, paired with the derive-never-pin rule, and shown drifting to 166/106/24 in one day. | re-walked a clean `a2e0438a` archive: 125 bullets / 120 files, five dual-link plans named; `_META_FROM_BACKLOG_RE.search` and `_ITEM_FROM_SPEC_RE.search` at their call sites | yes |
| D-4 | PR-905: child 02's `--full-auto` host-asymmetry premise is false at HEAD. Delete the caution, or re-found it? | RE-FOUND THE CAUTION on the mechanism that survives (`determine_action` returns `execute` for `approved` AND `reviewed`, so a passed `--full-auto` can execute a merely-reviewed plan) and require the defaults to be RE-MEASURED on both hosts at execution time. | (a) Delete the caution as obsolete, rejected and it would have been the dangerous answer: the legality table is safety content per its own docstring and per spec `25kzda` §2.6, and only the JUSTIFICATION went stale, not the risk. (b) Leave the false premise in place, rejected: an executor who measures `False` and finds the plan's only argument disproved could reasonably widen the table, which is the exact outcome the caution exists to prevent. | `build_parser().parse_args(['start','xxxxxx']).full_auto` is `False` on both hosts; the maintainer-ruling comment at `agy_runipd.py:4792`; `enforce_requested_action`'s docstring; spec `25kzda` §2.6 | yes |
| D-5 | PR-906: `f1sw71` is cited as `open` in three plans and is `graduated` with a decision plan attached. Does that overtake the exclusions? | NO. Correct the LABEL in all three plans and state explicitly that the gap is unbuilt so every exclusion resting on it is unchanged. | (a) Treat the exclusion as overtaken and require the already-implemented verdict, rejected on evidence: `si24ia` deliberately builds NOTHING (it measures, costs three options including doing nothing, and recommends), because the item is an open question whose requester said "definitely not sure". So no mechanism exists and the honest-limits requirement stands untouched. (b) Fix the label silently, rejected: an executor reading "open" and finding "graduated" needs to be told which conclusion NOT to draw, or the correction just moves the confusion. | `aw find backlog f1sw71` -> `graduated`; `si24ia` carries `- From-Backlog: f1sw71` and is `to-review`; the item's own graduation note records it as a decision plan | yes |
