# Review: ask the agent to adjudicate a failing suite instead of refusing every lane, child daexj1 (Set integearn)

- Subject-Id: daexj1
- Subject-Type: ipd
- Reviewed-At: 2026-09-08
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `2c727032`. Structural preflight `aw ipd lint --phase author` conformed before semantic
review (one advisory `IPD-Z602` density note, no error) and `--phase review-finalize` conformed after the
revisions.

DISCLOSURE: this plan was authored in the same session, so this is a self-review and worth less than an
independent one. It also succeeds `32ij2j`, which was itself reviewed and rejected here, so the chain of
self-assessment is two deep on the same problem.

THE PLAN'S DIAGNOSIS IS CORRECT AND I RE-VERIFIED IT rather than trusting the authored measurements.
`SuiteCheckResult` documents itself as an observed fact and sets `passing` True only on exit 0
(`oc_runipd.py:3614-3624`); `integration_is_earned`'s validation-OFF branch makes that binary suite result
the sole correctness signal, and it deliberately refuses to let a green suite override an explicit
verifier verdict (`:3740-3796`); both hosts' `make_integration_validation_runner` really do return a
closure whose entire body is `return True` (`oc_runipd.py:1988-2004`, `agy_runipd.py:1281-1293`) while the
gate's own docstring claims "per-lane green never implies integrated green"; and the eleven-lane incident
is present in the durable records, with all eleven ids found across `run-20260908T030747Z-1812636` and
`run-20260908T030809Z-1812970`. The maintainer's ruling that a programmatic delta gate is too fragile is
also vindicated by what review found in the code (see PR-404): the one shipped agent-verdict reader in
this area is a loose substring match that fails open.

WHAT ROUND 1 CHANGED. One finding is BLOCKING and left open, because it is a security-relevant policy
choice the maintainer must make and the plan's own proposed mitigation was measured unbuildable. Six were
fixed in place: a spec amendment that the plan deferred to the executor as a judgement when the answer is
determinate, a live shipped bug in the very function the plan modifies, a cited precedent that does not
exist, a fail-open pattern adjacent to the new code, a doubly-wrong baseline, and a dangling sibling id.
Scope-Paths grew from seven to eight; E-items from 9 to 10.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-401 | BLOCKER | IN-SCOPE | B. security; A. correctness | `oc_runipd.py:6649-6656`; `agy_runipd.py:3720`; `25kzda:830`; plan OQ-02 as authored | **An agent may clear a test it broke, and the plan's proposed mitigation cannot be built from what the plan provides.** The subject of the judgement is the agent that just did the work, the question is whether its own change may land, and `SAFE TO IGNORE.` is accepted with no independent check; since the suite is the ONLY correctness signal in the `--validate: false` default path, nothing then stands between a broken change and main. The plan defers this as non-blocking and offers a "cheap constraint" resting on having "the failing ids on both sides" - but MEASURED, `run_suite_check` is called exactly ONCE per attempt, AFTER the work, guarded by `if integration_gate_relevant and not validate`, and no baseline suite result exists anywhere in run state, so "which failures are NEW" is not computable without a SECOND full suite run (unscoped cost on top of E-04's already-doubled gate). Spec `25kzda:830` independently lists "a verifier's opinion" as not completion evidence, which is the same objection as contract | C:Medium; U:Low; S:Medium-High; F:Medium; Overall:Medium-High | OPEN | ESCALATED to OQ-02, reclassified `Blocking: yes` with `- Finding: PR-401`, so the shipped pre-execution gate refuses approval (`plan_readiness.has_unresolved_blocking_question` now returns True, verified). OQ-02 rewritten to state what is at stake, to record that the authored mitigation is unbuildable and why, and to cost three real options: (a) add a baseline suite run and refuse the verdict for newly-failing ids, (b) ship the hole and rely on the audit record, (c) make `SAFE TO IGNORE.` non-integrating. New F-14 records the measurement. Deferred under the Fix Bar on the security axis: every route either grants an agent authority over its own breakage or adds an unscoped suite run, and choosing is a maintainer call |
| PR-402 | HIGH | UNDER-SCOPE | Spec obligation; G. executability | `25kzda:64`, `:825-830`, `:897`; plan's spec-sync section as authored | **The plan hands the executor an open question about an approved contract, and the answer is determinate.** It said "THE EXECUTOR MUST READ THAT SECTION AND RECONCILE. If the spec forbids an agent verdict from clearing a red suite, this design is a spec AMENDMENT". Read at review: the spec DOES forbid it, in three places, and the decisive one is the integration clause `:897` ("Executor commits remain on the item's isolated branch until scope, commit-content, hook, dependency, and deterministic completion checks pass. Only then may the coordinator integrate"), with `:64` and section 5.1's not-completion-evidence list (`:825-830`, including "a verifier's opinion") reinforcing it. So the amendment is required work, not a contingency, and no `E-*` item performed it or `V-*` verified it | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | New F-11 quotes all three clauses. The spec path joined `- Scope-Paths:` so the runner announces the edit and the finalize gate reconciles it. New E-10 (Task group 4, `Depends on: none`) performs a NARROW amendment conditioned on the adjudication being captured evidence and on `finalize_precheck` still applying, forbids touching section 4.2's byte-equality finding-code table, and refuses to write the weaker wording while OQ-02 is unanswered. New V-10 requires the diff, the untouched 4.2 region, the tooled history line, and a statement of which OQ-02 option the wording assumes. The spec-sync section was rewritten from a question into the performed reconciliation |
| PR-403 | HIGH | UNDER-SCOPE | A. correctness; evidence accuracy | driven `run_evidence.capture_command` at review; `oc_runipd.py:3671-3672`, `:3691`; `run_evidence.py:318` | **`32ij2j`'s defect is ALREADY SHIPPED, in the exact function this plan modifies, and the plan faults it only as a proposal.** F-4 correctly says `build_tool_event` never writes `stdout_excerpt`; what it misses is that `run_suite_check` ALREADY READS that key (`stdout = str(tool_event.get("stdout_excerpt") or "")`, same for stderr) and parses the pytest summary from it. MEASURED by driving the real `capture_command`: the event's keys are exactly the eighteen digest/length/metadata fields, `'stdout_excerpt' in event` is False, and the string the summary regex searches is `''`. So `SuiteCheckResult.summary` is ALWAYS empty in production and every `reason` string has been reading `no summary line parsed` for that reason, not a regex fault. A plan that faults its predecessor for this defect while shipping it is not credible | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | New F-10 records the measurement and the key list. E-01 gains a paragraph naming the live bug, requiring the same capture change to yield the summary as well as the ids, requiring the code comment to state that this (not a regex problem) is why summaries have been empty, and forbidding a fix that reintroduces raw output. E-01's expected outcome now includes a non-empty summary on a real run. Scope check records this as deliberately in scope with the reason |
| PR-404 | HIGH | UNDER-SCOPE | B. security; C. architecture | `oc_runipd.py:6622-6633`; `agy_runipd.py:3682`; plan `m7gvuz:39-44`; measured zero-match grep | **The cited sentinel precedent does not exist, and the nearest real one FAILS OPEN in the same function this plan edits.** "The orchestrator probe accepts only two exact sentinels and fails closed" describes plan `m7gvuz` (`orchprobe-03`, `- Status: approved`, every E-item `Execution state: pending`); `ORCHESTRATOR: CONTAINS EXECUTIONS` greps to ZERO files under `agent_workflows/` and `tests/test_orchestrator_probe.py` does not exist, so the plan cites an unbuilt design as shipped precedent. Worse, the actual adjacent agent-verdict reader upper-cases the verifier's verdict, tests `"BLOCKED" in ... or "NOT CONFORMING" in ...` by loose substring, and falls to `else: verify_disp = "verified"`, with the `except` also defaulting to `"verified"` when `v_rc == 0`. An unrecognized or malformed verdict reads as VERIFIED. An executor pattern-matching on nearby code would reproduce precisely the failure mode E-05/E-06 exist to prevent | C:Low; U:Low; S:Medium; F:Medium; Overall:Low | FIXED | New F-12 corrects the attribution and names the real shipped precedents to copy (`lane_containment.parse_missing_input_token` with its single shared constant and explicit `REJECT_MALFORMED_TOKEN`, plus `run_selection_policy.is_confirmation_accepted` for exact-phrase-only). New F-13 records the fail-open reader with its line numbers and the default-to-verified path. Both are now Step 0 conventions, the second stated as a prohibition rather than a note |
| PR-405 | MEDIUM | IN-SCOPE | Evidence accuracy; E. testing | measured bare run at review; `tests/test_plan_readiness.py:790-798`; backlog `8kttqq` | **The stated baseline is wrong in BOTH its count and its named failure.** The plan says "1 failed, 5858 passed (the known `test_plan_readiness` failure)". Re-measured at review: the suite is `1 failed, 5865 passed, 3 skipped, 2 xfailed in 54.31s`, and that `test_plan_readiness` case now PASSES (`1 passed, 66 deselected`) because plan `32ij2j` was retired to `superseded/`, so no pending plan carries a negative verdict. The single current failure is `test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, which is environmental: it walks `REPO_ROOT.rglob("*")` skipping only four prefixes and consulting no `.gitignore`, so it trips over a gitignored local `opencode-recovery/` directory. An executor told to expect a different failure might "fix" this one by deleting another party's untracked files | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Validation section rewritten: measure your own before-baseline and judge on the delta, do not trust the authored number, with both re-measurements pasted, the environmental failure named with its mechanism and its backlog id (`8kttqq`), and an explicit instruction not to delete the untracked directory. F-9 updated to record that its predicted fix has already happened, which validates the diagnosis rather than weakening it |
| PR-406 | LOW | IN-SCOPE | Evidence accuracy | measured `find .aw/records -name "*nzpixq*"` empty; `.aw/records/plans/pending/20260908-integearn-04-ys1dor-...ipd.md` | The plan names its Order 04 sibling `nzpixq` twice (history line and deferral note), and no artifact with that id exists anywhere under `.aw/records/`. The real Order 04 is `ys1dor`. A dangling id in a deferral note means a reader cannot find the plan that owns the deferred half, which is the entire purpose of naming it | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | New F-15 records the measurement. The deferral note now names `ys1dor` with the falsification inline, so a future reader can see the correction rather than wondering which id is right. The historical authoring history line is left as written |
| PR-407 | LOW | IN-SCOPE | G. executability | plan gate section as authored | The gate told a reviewer three things but did not tell the EXECUTOR the one operational fact that matters most: whether the plan is approvable. With OQ-02 now blocking it is not, and an executor should learn that from the gate rather than from a refused `aw set approved`. The gate also omitted the ordering constraint the new spec amendment introduces | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Gate rewritten: states plainly that the plan is NOT approvable while OQ-02 is open, names the mechanical refusal (`plan_readiness.has_unresolved_blocking_question`, verified returning True), summarizes in one paragraph why OQ-02 blocks and what the three options cost, and adds the four things review established so an executor does not re-derive them. The reflexive-hazard warning and the recover-by-hand instruction were already present and are kept unchanged |

PR-401 is DEFERRED-AS-OPEN under the Fix Bar with overall Remediation Risk Medium-High on the SECURITY
axis, and every element the Bar demands is stated: the axis is security (and functionality, since option
(a) changes the cost profile); the risk reaches the threshold because there is no fix available from
repository evidence that does not either grant an agent authority to clear its own breakage or add an
unscoped second suite run; the required decision is the maintainer's choice among the three costed
options in OQ-02; and the consequence of leaving it unresolved is that the plan ships a path by which a
broken change reaches main with an agent's own say-so as the only authority. Effort and cost play no part
in the deferral.

Per the escalation rule, PR-401 is raised in the plan as an open question carrying `- Blocking: yes` and
`- Finding: PR-401`, which the shipped pre-execution gate then enforces; verified mechanically that
`has_unresolved_blocking_question` returns True and `aw set approved` will therefore refuse.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-401 has one obvious-looking fix (refuse `SAFE TO IGNORE.` for newly failing tests). Fix it, or escalate? | ESCALATED as blocking OQ-02 with three costed options. | (a) Specify the delta constraint myself, rejected on MEASUREMENT rather than principle: there is no baseline suite result in run state (one run, post-work), so the constraint needs a second full suite run, which is a cost decision on top of E-04's doubling and is not mine to make. (b) Leave OQ-02 non-blocking as authored, rejected because it fails OPEN: an executor would ship the self-clearing path silently and the audit record would be the only defense. (c) Narrow `SAFE TO IGNORE.` to non-integrating myself, rejected because it would silently defeat the stranding fix the maintainer ruled for. | `oc_runipd.py:6649-6656` and `agy_runipd.py:3720` show a single post-work suite call; `25kzda:830`; `plan-review` prohibition on inventing a decision that requires the human | yes |
| D-2 | The spec reconciliation was left to the executor. Perform it at review, or leave the question? | PERFORMED at review, and the answer recorded as determinate: the spec forbids the design, so the amendment is required and gets its own E-item. | Leaving it as the plan's conditional ("if the spec forbids ..."), rejected because it asks an executor to rule mid-implementation on an `approved` contract, which is the same class of act the repository's spec-amendment doctrine requires be DECLARED and visible in advance. Declining to amend and reshaping the design to fit the spec, rejected as contradicting the maintainer's 2026-09-08 ruling. | `25kzda:897` (integration clause), `:64`, `:825-830`; `AGENTS.md` plan-may-amend-a-spec rule requiring the path be declared in `Scope-Paths` | yes |
| D-3 | The always-empty `summary` bug (PR-403) is adjacent to, not identical with, this plan's purpose. Fix here or file separately? | FIX HERE, recorded in the scope check as deliberately in scope. | Filing it as backlog, rejected for two reasons: it is the SAME missing capture E-01 must add, in the same function, so separating it would mean two plans editing one seam; and shipping a plan that faults `32ij2j` for a defect the code still contains is not defensible. Leaving it unmentioned, rejected as the reason it survived review once already. | measured empty `stdout_excerpt` from the real `capture_command`; `oc_runipd.py:3671-3672`, `:3691` | yes |

Nothing was rejected. The approach, the maintainer's escalation design, and the post-merge revalidation
ruling are all sound; what review supplied was the spec reconciliation the plan deferred, a live bug at
its own seam, corrected precedent, and one blocking security question the maintainer must answer.
