# Review: map the verifier verdict through one fail-closed table, child 1bfppy (Set runverdict)

- Subject-Id: 1bfppy
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `6303afcc`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value therefore
rests on RE-MEASURING rather than re-reading. Twelve things were RUN rather than inspected: the
eight-string verdict truth table was executed against the real branch logic; both gate spans were
extracted and byte-compared; every `"verdict":` literal in `tests/` was enumerated; the outcome corpus
was re-counted and parsed; `run_state`, `verify_roles` and `runner_shared` were AST-walked for
first-party imports; both drivers were AST-walked for the three unimported modules and the importer
set was rebuilt per module; both hosts' verifier-gate defaults and their `integration_is_earned` call
sites were read; the retry budget was traced from parse to freeze to consumption; the spec's
`RUN-FRESH-VERIFIER` row and failure-action definitions were read in full; the refork guard's table
contract and five tests were read; all four sibling plans' front matter was read; and the bare suite
was run. Four of those twelve produced findings the plan did not contain.

SCOPE: only this child was a candidate. Siblings `fzxfph`, `bxx9af`, `w33lrl` and plan `r2i1b1` were
read as EVIDENCE, not reviewed, and are not in the ledger. Also read: `oc_runipd.py`, `agy_runipd.py`,
`runner_shared.py`, `run_state.py`, `verify_roles.py`, `run_recovery.py`, `agy_verifier.py`,
`ipd_lint.py`, `tests/test_runner_refork_guard.py`, `tests/test_runner_shared.py`,
`tests/test_oc_runipd.py`, `tests/test_agy_runipd_cli.py`, spec `25kzda`, and backlog `wyw936`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-D01 | BLOCKER | IN-SCOPE | A. Correctness; D. Anti-regression; E. Testing | grepped every `"verdict":` literal in `tests/`; ran the pre-fix branch on `CONFORMING` | **A FOURTH VERDICT IS IN LIVE USE, THE PLAN NEVER MENTIONS IT, AND THE PLAN AS WRITTEN TURNS TWELVE TESTS RED.** Twelve existing tests write `{"verdict": "CONFORMING"}` and depend on it reaching the SUCCESS path (six in `tests/test_oc_runipd.py` at `:2533`, `:2615`, `:2735`, `:2814`, `:2999`, `:3033`; six in `tests/test_agy_runipd_cli.py` at `:212`, `:255`, `:326`, `:363`, `:540`, `:565`), several driving self-finalize and integration end to end. It passes today ONLY by falling through the very `else` this plan removes. A table mapping the three documented verdicts and failing everything else closed therefore breaks twelve tests, and the executor's likely reflex (rewrite them to `VERIFIED`) would silently discard whatever behavior they cover. `CONFORMING` is also the plausible output of a verifier echoing `aw ipd lint`'s own only-pass disposition (`ipd_lint.py:12`, `:1019`), so it is a live value with a real producer rather than a test artifact. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Concern gains the measurement and the twelve call sites. Scope states the known set is FOUR values and that mapping the fourth is in scope. E-01 requires both `CONFORMING` aliases be decided explicitly with a safe default that keeps the suite green. OQ-02 reshaped onto this question. Scope check and the gate declare the twelve test updates in-fence if the decision goes the other way. V-01 and V-04 require the verdict be asserted either way. |
| PR-D02 | HIGH | IN-SCOPE | A. Correctness; B/E. Coverage of the exposed path | `agy_runipd.py:2047`, `:3625-3632`, `:3736-3740`; `oc_runipd.py:3045`; in-tree note at `agy_runipd.py:3710-3713` | **THE TWO HOSTS ARE NOT EQUALLY EXPOSED AND THE PLAN TREATS THEM AS ONE.** oc gates the verifier on `validate`, default FALSE; agy gates it on `not no_verify`, default TRUE, and passes `validate=verifier_expected` into the shared `integration_is_earned`. So the fail-open path this plan fixes sits on agy's SHIPPED DEFAULT and only on an opt-in oc path. Two consequences: the real-world severity is higher than "latent behind `--validate`", and a validation that exercises only oc leaves the exposed host unproven. The plan's byte-identical-gate framing invites exactly that oc-only test. | C:Low; U:Low; S:Low; F:Medium-High; Overall:Medium | FIXED | Added F-11 and a Step 0 convention. Concern states the asymmetry and that it raises agy's severity. E-05 gains a paragraph naming the widening risk per host. V-05 now requires lane-preservation evidence on BOTH hosts, oc with `validate=True` and agy on its default, and calls an oc-only paste incomplete. Harmonizing the defaults is explicitly fenced out. |
| PR-D03 | HIGH | IN-SCOPE | C. Architecture; A. Correctness (two plans, one line) | both plans' E-items and fences; both `- Item-Dependencies: none` | **TWO PLANS IN THIS SET EDIT THE SAME `except` ARM, EACH BELIEVING IT OWNS ONLY THE ADJACENT LINES.** Sibling `fzxfph` E-01 enumerates that exact site as the first of three facts collapsed into `unverified` and mints a NAME for it in the SAME module, while its own fence forbids it from touching the verdict mapping; this plan's E-02 routes the same arm through its mapping and attributes the site to a backlog id rather than to that plan. Both declare no dependency. Unreconciled, the unparseable-JSON case ends up with two competing names, or whichever plan runs second silently reverts the first. `fzxfph` already carries a coordination note naming `1bfppy`; this plan carried none in return. | C:Medium; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | Added F-12. E-02 gains an explicit reconciliation instruction: read `fzxfph`'s status on disk, adopt its name if landed, otherwise keep the arm minimal and record the name used. Deferred section states the division is NOT clean on this arm. V-02 requires the sibling's status pasted, the branch taken stated, and proof the two do not both name the case. Sibling references throughout switched from backlog ids to plan id6s. |
| PR-D04 | HIGH | IN-SCOPE | G. Plan executability; documentation accuracy | spec `25kzda:648`, `:586`, `:74`, `:693`, `:634-638` | **THE PLAN MISREADS THE SPEC ROW IT BUILDS AN ESCALATION PATH ON, AND ITS SECTION NUMBERS ARE WRONG.** §4.2's `RUN-FRESH-VERIFIER` inspects whether a valid verification ATTEMPT exists (fresh session, no inherited context, frozen predicates addressed) and its failure message is "`<item>` has no valid independent verification attempt". It says nothing about what the verdict CONCLUDED, so a `CORRECTION_REQUIRED` verdict is a VALID attempt and that row's `RETRY, then FAIL ITEM` does not fire on a rejection. The plan instead treated the row as possibly REQUIRING the requeue, instructed the executor to read it and escalate to the maintainer, and made the backlog close conditional on the answer. That is a manufactured gate on a question the spec already answers. Separately the quoted §1.2 text is actually §1.1 (`:74`), and the row's pass criterion was paraphrased as requiring a "valid evidence-linked envelope", which is `RUN-HOST-ATTEMPT`'s wording, not this row's. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-15. Spec-sync section rewritten with correct section numbers, verbatim quotes and the resolved reading, plus the observation that §4.1's RETRY definition (`:586`) is the nearest relevant text without mandating this row. OQ-01's owner and rationale updated: the spec half is RESOLVED and no longer the executor's. The gate's conditional close is replaced with the legitimate `graduated -> done` handoff. |
| PR-D05 | MEDIUM | IN-SCOPE | A. Correctness (a deferral justified by a mechanism that does not exist) | `oc_runipd.py:2783`; `agy_runipd.py:1838`; `runner_shared.py:2175-2176`; zero `get("retry_budget"` in either driver | OQ-01 defers the requeue because "requeuing consumes a retry, so it interacts with `--retry-budget`". MEASURED: the budget is resolved once per host at parse time and frozen into run options, but NEITHER DRIVER EVER READS IT BACK. There is no per-item retry loop in either driver for a requeue to consume. The deferral is still correct, and the true reason is STRONGER than the stated one: implementing the requeue means BUILDING the retry mechanism and wiring it to the frozen budget, not merely interacting with it. A deferral resting on a wrong mechanism is a deferral a reviewer cannot check. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-14 and a Step 0 convention. OQ-01 and the Deferred entry both restate the cost as "the mechanism does not exist and would have to be built", with the measurement cited. |
| PR-D06 | LOW | IN-SCOPE | C. Architecture (Set sequencing, naming clarity) | read `- Order:` from all four `runverdict` plans (5, 5, 6, 7); read `queue_sort_key` at `oc_runipd.py:3873-3879` | **ORDER COLLISION IN THE SET:** this plan and sibling `bxx9af` both carry `- Order: 5`, and both filenames use the `-05-` segment. INITIALLY GRADED MEDIUM ON THE ASSUMPTION THAT ORDER DRIVES DISPATCH; that assumption was then MEASURED AND IS FALSE, which is why this row is LOW. `queue_sort_key` sorts on `(dependency_depth, position, setid, order, id6)`, so `order` is only the fourth key and `id6` is a total tiebreak beneath it, leaving dispatch deterministic and reproducible; all four plans also declare `- Item-Dependencies: none`, and `dependency_depth` outranks everything regardless. The genuine residual is prose ambiguity: "this Set's Order 05" no longer names one plan, and `fzxfph`'s coordination note uses that phrase to mean this one. | C:Low; U:Low; S:Low; F:Low; Overall:Low | OPEN | Recorded as F-13 (regraded LOW with the sort-key evidence) and raised as OQ-03 (`Blocking: no`, `Finding: F-13`). NOT fixed here because renumbering RENAMES a sibling plan that was never a candidate in this ledger. Cross-references should resolve by id6, not by Order. |
| PR-D07 | MEDIUM | IN-SCOPE | E. Testing (a moving number used as a fixture size, and a weaker reason than the real one) | re-counted the corpus (`Counter({'VERIFIED': 35})`); `.aw/.gitignore:14`; `git ls-files` on the run tree | E-03 pins "the 34 real outcome files" and V-03 requires asserting "all 34". The corpus is 35 at review, one day later, because every run appends one; it is a monotonically growing target, so any literal is stale on arrival and teaches an executor to edit the number when a correct measurement disagrees. Separately, the plan's reason for using fixtures is the lane-worktree failure mode, but the stronger and simpler reason is that `.aw/records/runs/` is GITIGNORED with zero tracked files, so a test reading it fails in CI and in any fresh clone, not merely in a lane. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Concern, F-7, E-03 and V-03 all de-numbered: measure at execution time, report what you found, assert the PROPERTY not a count, and treat a size mismatch as expected. The gitignore reason is now stated with its evidence. |
| PR-D08 | MEDIUM | IN-SCOPE | G. Plan executability (navigation) | re-located every cited symbol by name at review | **EVERY LINE CITATION IN THE PLAN HAD MOVED IN ONE DAY**, which matters because the plan cites specific lines throughout while also warning against line navigation. The oc gate moved `:6422-6434` to `:6632-6646`, the agy gate `:3686-3698` to `:3683-3697`, the prompt `:4915` to `:4971`, `integration_is_earned` `:3684` to `:3740`, `self_finalize` `:3062` to `:3047`, `verify_roles` `:369` to the `:365-371` frozenset, and `run_state`'s edges by one line each. The plan's own importer list for the three unimported modules was also a merged union that misattributed which module each consumer imports. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All citations corrected throughout. The importer list is now given per module, re-measured. The gate's re-locate paragraph now enumerates the measured drift as evidence for its own instruction. `runner_shared`'s stale `determine_action`/`action_for` line numbers replaced with a find-by-name instruction. |
| PR-D09 | LOW | IN-SCOPE | A. Correctness (an ordering hazard inside the fix itself) | `"CONFORMING" in "NOT CONFORMING"` is True; the gate applies only `.upper()` | Two normalization traps the plan does not address. FIRST, `CONFORMING` is a SUBSTRING of `NOT CONFORMING`, so any implementation that keeps substring semantics and tests `CONFORMING` first maps a REJECTION to a pass, recreating the exact defect this plan exists to remove. SECOND, the gate never normalizes beyond `.upper()`, while a real model commonly writes `VERIFIED: <summary>` or pads the value; the plan's table has no case, whitespace or trailing-summary case, and the exact-versus-prefix choice decides whether such a turn fails or silently passes. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-16 and a Step 0 convention. E-01 requires exact matching on a normalized token and names the substring trap. E-04 adds the lowercase, padded and trailing-summary cases and raises the minimum input count to thirteen. V-04 requires showing `NOT CONFORMING` does not map to a pass. |
| PR-D10 | LOW | IN-SCOPE | E. Testing (a guard premise that is wrong, and work that is cheaper than written) | `tests/test_runner_refork_guard.py:161-167`, `:265`, `:287`, `:316`, `:336` | E-06 asserts `test_the_table_covers_both_runners` "already enforces" that its new row lists both runners. It does not: that test asserts the table as a WHOLE covers both, which it already did before any change, so it would pass a one-sided new row. Conversely E-06 is cheaper than written: `runner_shared` is already a legal owner in `_MODULES`, and a single well-formed row activates the AST half, the identity half and the owner-typo guard. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-17. E-06 states which three tests fire from one row, gives the row's literal form, and corrects the premise. V-06 forbids relying on that test as proof the row lists both runners. |
| PR-D11 | LOW | IN-SCOPE | E. Testing (stale baseline naming a passing test) | bare `python3 -m pytest` at HEAD `6303afcc` | The Step 0 baseline claims `1 failed, 5648 passed` with the failure in `tests/test_orchestrator_retirement.py`. Measured: `1 failed, 5919 passed, 3 skipped, 2 xfailed`, and that module now gives `112 passed`. The single real failure is the environmental `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, caused by 189 untracked `opencode-recovery/*.md` transcripts belonging to another party in this shared checkout, which an executor chasing a green baseline could "clean up". | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Baseline replaced with the measured figures and the named environmental failure, with the executor told not to quote them either. The scope fence now forbids deleting, moving or committing those untracked files. |

CONFIRMED SOUND, AND IT IS THE PLAN'S ENTIRE ARGUMENT. Every load-bearing claim was re-measured and
holds. THE DEFECT IS REAL AND REPRODUCED EXACTLY: the eight-string truth table came back identical, six
of eight inputs return `verified`, and only `BLOCKED` and `NOT CONFORMING` downgrade. THE TWO GATE
SPANS ARE STILL BYTE-IDENTICAL across hosts (extracted and compared, `True`), so it genuinely is one
change applied twice. THE STATE AUTHORITY REALLY EXISTS AND IS REALLY UNWIRED: `run_state` defines the
token and both edges, `verify_roles` grants the verifier all three transitions, `agy_verifier` already
implements the fail-closed direction, and an AST walk of both drivers returns ZERO imports of any of it.
`run_state` imports NO first-party module, so the proposed import cannot cycle, and `runner_shared`
already consumes `run_recovery` lazily, which is direct precedent. THE CORPUS IS UNANIMOUS, so the fix
provably breaks no historical turn and the item's own "will start blocking lanes" objection is correctly
falsified retrospectively. `integration_is_earned` really does refuse on a non-verified verdict with an
explicit reason, so the lane-preservation recovery route is real. AND THE PLAN'S SELF-DISCIPLINE IS
RIGHT: it refuses to promote the verdict to completion authority, refuses to touch the prompt, and
refuses to define a refusal record another plan owns.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing about the design; the fail-closed mapping in
the shared module is the correct fix and survived intact. What changed is the DOMAIN of the table. The
plan reasoned from the prompt's three documented values, and the code's real input alphabet is larger:
a fourth value is in live use by twelve tests, one of the two aliases is a substring of the other, and
nothing normalizes case or a trailing summary. A fail-closed table is exactly where an incomplete input
alphabet is most dangerous, because every unenumerated value becomes a refusal, so the revisions push
the plan to enumerate deliberately and to prove the two aliases cannot be confused. The second theme is
that this plan is not alone on its lines: a sibling rewrites the same `except` arm, and the two hosts
whose gates are byte-identical have different DEFAULTS, so the higher-exposure host is agy and must be
tested explicitly rather than assumed symmetric.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | OQ-01 asked the executor to read spec §4.2 and escalate to the maintainer if deferring the requeue leaves a spec gap. Resolve that reading here, or leave the escalation in place? | Resolve it: §4.2 does NOT mandate the requeue, so deferring leaves no spec gap and the escalation instruction is removed. The scheduling decision itself stays the maintainer's. | (a) Leave the escalation; rejected because the workflow's own rule is not to ask a human what the repository already answers, and the row's text settles it. (b) Treat the row as mandating RETRY and escalate now; rejected as a misreading, since the row's `inspects` and `pass_criterion` cells are about the ATTEMPT's freshness and predicate coverage, not the verdict's content, and its failure message is "has no valid independent verification attempt". | Read `25kzda:648` in full plus the failure-action definitions at `:586`. A `CORRECTION_REQUIRED` verdict is a valid attempt, so the row does not fire on a rejection. §4.1's RETRY definition describes the deferred behavior for rows that DO assign RETRY without making this row one of them. | yes |
| D-2 | OQ-02 asked whether `NOT CONFORMING` is a dead substring. Answer it as asked, or reshape it? | Reshape it: the load-bearing question is what the `CONFORMING`/`NOT CONFORMING` PAIR maps to, given twelve tests depend on the first passing. Keep both as accepted aliases by default and require the decision be stated. | (a) Answer only as asked ("nothing emits `NOT CONFORMING`, keep it as an alias"); rejected because that answer is TRUE and yet misses the consequential half, leaving the executor to discover the twelve broken tests during execution. (b) Decide firmly that `CONFORMING` must not pass; rejected as the reviewer's call to make on its own, since it requires editing twelve test call sites whose coverage must each be re-justified, and either answer is defensible. | Grepped every `"verdict":` literal in `tests/`: twelve `CONFORMING` writes across both host test modules, several driving self-finalize and integration. `NOT CONFORMING` appears nowhere but the two gate lines. `ipd_lint.py:12` makes `conforming` the linter's only pass disposition, giving the pair a plausible producer. The default chosen keeps the suite green, so the safe path needs no maintainer round trip. | yes |
| D-3 | The duplicate `Order: 5` (PR-D06): escalate it as a `Blocking: yes` question that stops execution, or record it as non-blocking? | Non-blocking (`Blocking: no`), regraded LOW, raised as OQ-03 with the finding id. | Escalating it as blocking; DRAFTED THAT WAY FIRST and then rejected on measurement, because the premise was wrong. I had assumed Order drives dispatch within a Set, which would have made the sequence arbitrary and warranted a stop. Also rejected: fixing it here by renumbering, since that renames a sibling plan nobody reviewed. | `queue_sort_key` (`oc_runipd.py:3873-3879`) returns `(dependency_depth, position, setid, order, id6)`, so `order` is the FOURTH key with `id6` as a total tiebreak: a duplicate Order falls through to a stable, reproducible tiebreak rather than becoming arbitrary. All four Set plans declare `- Item-Dependencies: none`, so no edge is violated. `aw ipd lint` also refuses an unresolved `Blocking: yes` at every checkpoint (`IPD-Q501`), so escalating a non-defect would have blocked a sound plan on a false premise. | yes |
