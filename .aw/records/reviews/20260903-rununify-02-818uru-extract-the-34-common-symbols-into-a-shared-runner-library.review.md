# Review: Extract the 34 common symbols into a shared runner library, pure move

- Plan-Id: 818uru
- Reviewed-At: 2026-09-03
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED

## Round 1

All claims verified at HEAD `25d3f0b050a2fbe81d532e59ceef70066c0a63d8`, working tree clean, target plan
committed and unchanged, so the pre-review snapshot was correctly skipped per Step 1. Structural
preflight `aw ipd lint --phase author` reported `conforming` before review and
`--phase review-finalize` reported `conforming` after the revisions.

Like its sibling, this child satisfies the orchestrator's SECOND GATE: it was authored from research
`tvnq50` after E-01 measured, and it corrects the orchestrator's own count (34, not 35) rather than
inheriting it. It also correctly declares `Item-Dependencies: executed:2r306y`, which is the right
edge: this plan EXTENDS the symmetric guard child 01 installs, so the dependency is real rather than
decorative.

I re-derived the inventory independently rather than trusting the research artifact, and the plan's
numbers hold: 88 symbols are defined in both runners, 35 AST-identical and 53 diverged; subtracting
the two child 01 owns (`_read_id`, `_read_status`) leaves exactly the 34 claimed. `StallTimeout`
correctly sits in the diverged set (the docstrings differ). F-3's `DriverError` claim is live and
verified: `agy_runipd.DriverError is oc_runipd.DriverError` returns False at this HEAD, and the
hand-written translation wrapper exists at `agy_runipd.py:1388-1411` exactly as described, with its
rationale comment at `:87-96`. F-4's 40-name count is exact, though only via nested `ImportFrom`
statements: a top-level scan alone shows 37, which is worth knowing since a future reader re-running
the check the obvious way will get a different number. F-7's collision claim is right and stronger
than stated: the three `_run_git` definitions differ in RETURN TYPE, not merely in body.

One finding dominated, and it is a genuine BLOCKER in the sense that the plan as written could not
have satisfied its own validation: E-02's two requirements contradicted each other. The maintainer
re-ruled the mechanism. A second finding of the same class (a fifth outside-dependency the inventory's
call-graph method structurally could not see) was found by re-running the analysis over FREE VARIABLES
rather than calls.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-301 | BLOCKER | IN-SCOPE | A. Correctness / E. Testing / G. Plan executability | E-02 requires `ast.dump(...)` fingerprint EQUALITY with the pre-move capture for all 34, AND requires four symbols to take a new injected parameter; measured call sites `save_state` 33 in oc + 31 in agy, `run_checked` 13 + 9 (~86 total); 7 `reviewed` `lanectn` plans plus `prpipy` and `2r306y` also declare both runner modules | THE PLAN'S TWO CENTRAL MECHANISMS CONTRADICTED EACH OTHER, and the contradiction lands on exactly the four riskiest symbols. E-02 makes AST-fingerprint equality the load-bearing proof that this is a "pure move", and simultaneously orders uniform parameter injection for the four symbols with outside dependencies. A function that gains a parameter has a different signature and a different body, so its fingerprint CANNOT equal its pre-move capture: the plan's own proof would fail precisely where the risk concentrates, leaving the executor to either abandon the proof or quietly exempt the four, and an exemption chosen mid-execution is how a real body change gets absorbed as "expected". Second, independent cost: threading a parameter through every call rewrites ~86 call sites in the two highest-contention files in the repo, manufacturing exactly the merge conflicts E-07 already warns about against `lanectn`. | C:Medium; U:Low; S:Low; F:Medium; Overall:Medium (a real mechanism change, but bounded, uniform, and each wrapper is one line whose correctness is mechanically checkable) | FIXED | Maintainer re-ruled OQ-02 to the THIN RUNNER-LOCAL WRAPPER: `runner_shared` owns the parameterized function, each runner keeps a one-line wrapper at the original name and signature binding its own dependency. Both original prohibitions survive (no registration seam, no module-level mutable state) and the dependency is still passed explicitly. Rewrote E-02 with the split fingerprint rule stated openly (strict equality for the ~30 clean symbols; equality-modulo-the-parameter plus a behavior test for the wrapped ones, enumerated not implicit), and updated E-05, E-06, E-08, V-02, V-05, V-06 and the honesty rule. OQ-02's resolution now records the refinement and why, so it is not re-proposed. Recorded as F-9. |
| PR-302 | HIGH | UNDER-SCOPE | A. Correctness and data integrity | oc `PlanRecord` fields `(id6, setid, status, order, path, rel_path, dependencies, kind, dependency_error, from_backlog)` versus agy `PlanRecord` at `agy_runipd.py:1414` which LACKS `kind`; `discover_plans` constructs `PlanRecord` via `parse_plan_file`; consumer `action_for` (`oc_runipd.py:2749`) branches on `kind` to detect an orchestrator | A FIFTH OUTSIDE-DEPENDENCY THE INVENTORY COULD NOT SEE, and the most dangerous kind because it fails silently and type-shaped. The plan's list was built from a CALL graph, so it caught `parse_plan_file` but not `PlanRecord`, which `discover_plans` closes over as a TYPE rather than calling as a function. The two runners' `PlanRecord` are different NamedTuples. A shared `discover_plans` must therefore construct one of them: build oc's and agy silently receives a field its code never expects; build agy's and oc silently LOSES `kind`, which `action_for` reads to decide whether a plan is an orchestrator - so an orchestrator would be treated as an ordinary executable plan with no error anywhere. The plan's `assertIs` and fingerprint checks would both PASS in that state, since neither inspects the record type. | C:Low; U:Low; S:Low; F:High -> mitigated to Medium by supplying the constructor per runner exactly like the other dependencies; Overall:Medium | FIXED | E-08 now names `PlanRecord` as a dependency of the same class, requires the constructor be supplied per runner, and forbids unifying the two definitions (a class (c) reconciliation this plan may not perform). V-08 now requires per-runner record-type evidence AND proof that oc's `kind` field survives, because a dropped `kind` disables orchestrator detection with no error. E-02's dependency list updated to five. Recorded as F-8. Also added to Deferred so the surviving divergence is explicit. |
| PR-303 | MEDIUM | IN-SCOPE | G. Plan executability | E-06 lists `load_json`, `atomic_write_json`, `append_jsonl`, `sha256_file`, `load_state`, `save_state`, `state_root` "if not already moved", plus `print_status`, headed "7 symbols" while enumerating 8; E-04 already moves `state_root` | E-06'S MOVE MANIFEST IS AMBIGUOUS ABOUT ITS OWN CONTENTS. It says 7, lists 8, and hedges one entry with "if not already moved" although E-04 unambiguously moves `state_root` already. In a plan whose entire safety argument is a per-symbol accounting (34 captured, 34 verified), an ambiguous manifest is how a symbol gets moved twice, or skipped and reported as done because a different item was believed to own it. The hedge also silently makes the two items' boundary depend on execution order. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-06 now states an EXACT 7-symbol manifest, removes the hedge, and says explicitly that `state_root` is E-04's and not this item's. V-06 now asks for seven, with the reason noted so a reader does not think a symbol went missing. |
| PR-304 | MEDIUM | IN-SCOPE | C. Architecture / F. Honest documentation | E-01 requires the docstring to state "this module holds only symbols PROVEN identical across both runners"; E-02 requires four (now five) of those symbols to take an injected parameter | THE MODULE'S STATED ADMISSION RULE IS FALSIFIED BY THE PLAN'S OWN DESIGN, from the first seam onward. A docstring asserting a stricter contract than the module keeps is worse than no docstring: the next reader either believes it (and is misled about what may live there) or notices the contradiction (and distrusts the whole contract, including the parts that are load-bearing, like "may never import a runner"). This repo treats module docstrings as the contract of record, so the wording is not cosmetic. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-01 now requires the rule be stated as "identical bodies MODULO an explicitly injected dependency, with the injection recorded", with the reason given so it is not "simplified" back into a false absolute. |
| PR-305 | LOW | IN-SCOPE | D. Anti-regression | `agy_runipd.py:383` `class StallTimeout(DriverError)`; agy `DriverError` handlers at `:479`, `:550`, `:1288`, `:1338` among others; `StallTimeout` is class (c) DIVERGED (docstrings differ) | E-03 UNDERSTATES WHAT IT IS DOING to a live exception. It says "verify the subclass relationship still holds", but unifying `DriverError` RE-PARENTS `agy_runipd.StallTimeout` from agy's local class onto the shared one, and `StallTimeout` is what the stall watchdog raises. An `issubclass` assertion proves the type lattice, not that every `except DriverError` site in agy is still reached. The failure mode if it is wrong is specific and bad: a stall that stops being caught turns a clean timeout into an unhandled traceback in an unattended overnight run, which is the scenario this driver exists to survive. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-03 now states plainly that it re-parents a live exception, names the handler sites, requires the WATCHDOG path be exercised rather than reasoned about, and forbids editing `StallTimeout`'s body (it is class (c)). V-03 now requires that demonstration and says `issubclass` alone does not satisfy it. The scope fence records the re-parenting as its one permitted class (c) contact so it is not mistaken for a violation. |
| PR-306 | LOW | IN-SCOPE | A. Correctness (reproducibility of a measurement) | F-1 states "551 lines of the runners' definition mass" / "551 oc lines total"; the two runners' copies differ in length, so a single figure is ambiguous without stating which side it counts | F-1's line figure is not reproducible from the stated method as written, because it silently counts oc-side definitions only while the surrounding sentence speaks of "the runners'". Minor, but this plan's credibility rests on its measurements being re-derivable, and the orchestrator's own history shows what happens when a stale or ambiguous count is inherited by a child (it re-scoped two children over exactly that). | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | F-1 now says "551 oc-side definition lines" with the qualifier's reason, and records that the 34/35/53 partition was re-derived independently at review. |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-301's mechanism change (uniform injection versus thin wrapper) reverses a maintainer ruling made earlier the same day. Decide it from evidence, or ask? | ASK. Put the measured consequences in front of the maintainer and let them re-rule. | (a) Decide it myself on the evidence, since the contradiction with E-02's own proof is objective. Rejected: the original instruction was an explicit maintainer ruling with a stated rationale ("remain visible at each call site"), and silently substituting a different mechanism would overwrite a decision the maintainer owns, even with good evidence. (b) Record it as a finding and leave the contradiction for execution time. Rejected: it would surface mid-execution, when the cheap options are gone. | The plan-review workflow's rule that a reviewer must never guess a human decision, and that resolving from evidence applies only where the repository already answers; the ruling's own text in OQ-02; measured ~86 call sites and the fingerprint contradiction | yes |
| D-2 | Should the wrapped symbols be exempted from the fingerprint harness silently, or should the split rule be written into the plan? | WRITE THE SPLIT RULE EXPLICITLY, enumerating which symbols are wrapped and requiring the exemption be listed in the test file. | (a) Silent exemption (skip the four/five in the harness). Rejected: an unenumerated exemption is indistinguishable from a harness bug, and this repo has already shipped a vacuous guard that passed against stubbed-out behavior - the orchestrator's own review cites a `grep "return True"` proof that could never fail. (b) Drop the fingerprint harness entirely and rely on behavior tests. Rejected: the harness is what makes "pure move" falsifiable for the 30 symbols where it genuinely applies, and those are the majority. | E-02's own requirement that the harness be proven load-bearing by deliberate alteration; `5e4sb6`'s V-03 requirement for AST-level rather than grep-level proof; the plan's honesty rule forbidding a de-duplication claim resting on a grep | yes |
| D-3 | `818uru` and `2r306y` both declare `tests/test_runner_refork_guard.py`, and `818uru` EXTENDS the table child 01 creates. Is that a scope collision to flag? | NO. It is a declared, ordered handoff, not a collision. | (a) Flag it as a Scope-Paths collision like the ones review flags between independent plans. Rejected on the evidence: the sharing is INTENTIONAL and SEQUENCED - `818uru` carries `Item-Dependencies: executed:2r306y`, so child 01 has necessarily landed before child 02 edits the file, which is the opposite of the concurrent-edit hazard a collision warning exists to catch. (b) Require child 02 to create its own guard file. Rejected: two guard tables answering one question is the exact anti-pattern child 01 exists to remove. | `818uru`'s `- Item-Dependencies: executed:2r306y`; the computed Scope-Paths intersection showing the shared file; `2r306y` E-01's table-driven design intended for extension | yes |
