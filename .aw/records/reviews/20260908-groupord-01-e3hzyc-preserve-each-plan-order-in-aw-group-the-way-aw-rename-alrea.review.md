# Review: preserve each plan Order in aw group the way aw rename already does, child e3hzyc (Set groupord)

- Subject-Id: e3hzyc
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `72a8f6ad`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value rests on
RE-RUNNING rather than re-reading, and for a plan about a CLI defect that is a strong position: a fresh
throwaway git repo was built (orchestrator at Order 0 plus two children at Orders 1 and 2) and SEVEN
behaviors were EXECUTED against the real CLI rather than reasoned about. Those seven: a bare
`--rename` regroup; a bare METADATA-ONLY regroup; a multi-plan explicit `--order 1`; a regroup of a plan
whose front-matter Date diverges from its filename date; a regroup of a plan with NO `- Date:` line; the
same no-Date case through `aw rename` for contrast; and `aw ipd lint` versus `aw check plans` versus bare
`aw check` on the corrupted result. FOUR of the seven produced findings the plan did not contain, and
TWO of those four change what the executor must build.

SCOPE: only this child was a candidate. Backlog `s9p5x5`, plans `k9awrq`/`dw7i3m`, and spec `4w7d6s`
were read as EVIDENCE, not reviewed, and are not in the ledger. Also read: `plans_refs.py`,
`artifact_rename.py`, `research_refs.py`, `cli.py`, `tests/test_awnaming_grammar_and_producers.py`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | LOW | IN-SCOPE | G. Plan executability (stale citations) | re-located every symbol at review | Every `plans_refs.py` line number in the plan was off by five to twelve lines: `plan_set_assign` `:207` -> `:208`; its `start_order` default `:212` -> `:213`; the `+ i` loop `:225` -> `:226`; `run_set_assign`'s flag read `:432` -> `:420` and its collapse `:437` -> `:425`; `run_mv`'s fallback `:467-475` -> `:453-462`. Individually trivial, but these are the exact lines a reader jumps to first, and the plan's own gate says to locate by name. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All citations corrected throughout; F-14 records the drift; the conventions section carries a dated re-measurement header; the gate's re-locate paragraph now cites this plan's own drift and lists eleven symbols to find by name. |
| PR-002 | HIGH | UNDER-SCOPE | A. Correctness (half the defect was invisible to the plan) | probe: `aw group plans bbb222 --set metaset --apply`; `plans_refs.py:226`, `:239-241` | **THE DEFECT ALSO FIRES WITH NO `--rename`, AND THAT CASE IS WORSE.** Measured: a metadata-only regroup writes `- Order: 0` while the filename stays `20260908-probeset-01-bbb222-...`, so the file CONTRADICTS ITS OWN NAME. The cause is structural: `order = start_order + i` is computed ABOVE the `if rename:` split and flows into the metadata-only `RenamePlan(src, src, id6, order=order)` too. In that exact state `aw check plans` exits 0, bare `aw check` exits 0, and `aw ipd lint` reports only `IPD-M104` with no name-mismatch code, so nothing in the toolchain names the divergence. The plan's Concern, Scope, E-01, E-02 and all four E-04 cases named ONLY the `--rename` path, so an executor could have conformed to every instruction, passed every stated test, and shipped the worse half of the bug. | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added F-11 and a Step 0 convention. The Concern and Scope now cover both branches; E-01 requires TWO failing tests, one per branch, with the divergence pasted; E-02 requires both branches be covered and explains why the metadata-only path is the worse one; E-04 gains the metadata-only preserve case; V-01, V-02 and V-04 updated; the Scope-check justification names the second `RenamePlan` site. |
| PR-003 | MEDIUM | IN-SCOPE | C. Architecture (the wrong precedent copied) | `artifact_rename.py:682`, `:142`, `:237` | **A SECOND AND BETTER PRECEDENT EXISTS AND THE PLAN DID NOT FIND IT, SO "THE FIX IS A PORT" WAS TRUE OF THE WRONG SHAPE.** `artifact_rename.run_group_generic`, the `group` backend for every NON-plan artifact type, already solves this defect class by threading an `Optional[int]`: `order_val = (start_order + i) if start_order is not None else None`, after which `compute_target_name` falls back to the filename's own `NN` and the metadata writer SKIPS the `- Order:` line entirely when the value is `None`. That shape fixes BOTH `group` branches by construction, which is exactly what PR-002 shows is required, whereas porting `run_mv`'s three-tier lookup to one call site naturally addresses only the rename path. The plan asserted the fix shape "needs no debate"; there are two shipped shapes and the unfound one is the better fit. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-10 and a Step 0 convention. The Concern names the second precedent; E-02 presents Shape A and Shape B with the reviewer's reading favoring B and requires a recorded choice; Proposed changes updated; V-02 requires naming which shape was copied and why; Deferred forbids editing `run_group_generic` while permitting reading it; the Scope check explains why `artifact_rename.py` is deliberately NOT declared. |
| PR-004 | MEDIUM | IN-SCOPE | G. Plan executability (two speculative deferrals were measurable) | probe date cases; `plans_refs.py:230`, `:113-118`, `:466`; `research_refs.py:326`, `:164`, `:181` | **TWO DEFERRALS WERE STATED AS UNKNOWNS THAT MEASUREMENT SETTLES, AND BOTH TURN OUT TO BE REAL DEFECTS.** (a) The date question ("whether `run_set_assign` shares that defect is a real question this plan does NOT answer"): it does. `plan_set_assign` derives the name date from `_plan_date(text)` alone, whose no-match fallback is the literal `"20260101"`, while `run_mv` prefers the FILENAME's date first. Verified: a plan with no `- Date:` line is renamed by `group` to `20260101-nodate-01-...` while `rename` on the same file preserves `20260908-...`. (b) "If another type's backend shares the shape, record it as a finding": exactly one does. `research_refs.run_set_assign` carries the byte-identical `start_order=start if start is not None else 0` against a planner defaulting to `0`, so `aw group research` has the same bug; and `aw research set-assign`'s own `--order` help documents the buggy default verbatim ("Starting NN (default 0)."). A deferral resting on an unknown is weaker than one resting on a measurement, and an executor told "record it if the fixture happens to show it" may simply not look. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-12 and F-13 with their measurements, plus two Step 0 conventions. Both deferrals rewritten as evidence-backed with a FILING obligation rather than a maybe; the non-plan-types deferral now splits correct (`run_group_generic`) from defective (`research_refs`); E-02 cites the measured date defect as the reason not to bundle it; E-05 forbids extracting a shared helper across the two defective backends; V-05 requires `research_refs.py` byte-unchanged and both filed item ids. |
| PR-005 | MEDIUM | IN-SCOPE | G. Plan executability (a mandated edit outside the declared fence) | plan `Scope-Paths`; `cli.py:3118` | **THE SPEC-SYNC SECTION MANDATES A `--help` REWRITE AND `cli.py` WAS NOT DECLARED.** It says "`aw group`'s `--help` for `--order` should say that omitting it PRESERVES each plan's Order", and that string lives in `cli.py:3118`. So an executor following the instruction goes out of fence and owes a `--scope-reason` at finalize for doing exactly what the plan told them. Also unstated: that string is SHARED by `rename` and `group` in the noun-verb parser, so one edit changes both verbs' help at once, and there is a SECOND `--order` help string (`cli.py:2246`) belonging to `aw research set-assign` which literally documents the buggy default and belongs to the deferred item. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | `agent_workflows/cli.py` added to `Scope-Paths` with a justification naming this finding. The spec-sync section now states the sharing, names both strings, and tells the executor which one is in scope and which needs a `--scope-reason`. A Step 0 convention records the sharing. |
| PR-006 | LOW | IN-SCOPE | E. Testing (wrong baseline) | bare suite at review | The recorded baseline is `1 failed, 5648 passed` with the failure in `tests/test_orchestrator_retirement.py`. Measured: `1 failed, 5919 passed, 3 skipped, 2 xfailed`, and the failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, caused by 189 untracked `opencode-recovery/*.md` files belonging to another party in this shared checkout. An executor expecting the wrong failure could chase a phantom regression or, worse, "fix" another party's untracked files. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-15. Baseline replaced in E-05, the conventions, and Required tests, with the named environmental cause, an instruction that it cancels in the delta and must not be fixed, and a no-added-flags note citing the configured `addopts`. The gate forbids touching those files. |
| PR-007 | LOW | UNDER-SCOPE | E. Testing (two missing cases, one unpassable assertion) | probe lint output; the sentinel change | Two test gaps and one assertion that cannot pass as written. FIRST, no case asserts that an EXPLICIT `--order 0` still lands at 0; since the whole fix is making `None` and `0` distinguishable, the risk of making zero unreachable is exactly what the change introduces, and it is the case an executor is likeliest to skip as a no-op. SECOND, no case covered the metadata-only branch (PR-002). THIRD, "assert the lint verdict flips" would be written as a clean-exit assertion and can never pass: a probe plan file also emits unrelated `IPD-H2xx` structural codes (observed: `IPD-H202: required H2 missing: Proposed changes`), so the assertion must match the ABSENCE OF `IPD-M104` specifically. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-04 is now six cases with the explicit-zero and metadata-only cases added and the multi-plan and explicit-zero cases each required to stand alone; the `IPD-M104` instruction narrowed to the code with the reason; Required tests and V-04 updated; a Step 0 convention records the lint-assertion trap. |

CONFIRMED SOUND, AND THE DIAGNOSIS IS EXEMPLARY. Every authored claim that was re-run HELD. The clobber
reproduces exactly as described (`20260908-probeset-01-bbb222-...` -> `20260908-newset-00-bbb222-...`
with `- Order: 0`, exit 0, no warning). The root cause is that one line with that ambiguous sentinel.
`run_mv` really does carry the fix with the `vf03z3` comment in as many words. `PlansMvPreservesOrderAndDateTests`
really pins Order and Date for `rename` and nothing pins either for `group`. The sweep really cannot see
the result: `aw ipd lint` exits 1 on `IPD-M104` while `aw check plans` and bare `aw check` both exit 0 in
the same tree. And the multi-plan explicit renumber really is a working feature (`--order 1` across two
plans yields `-01-`/`- Order: 1` and `-02-`/`- Order: 2`), so F-8's warning that a preserve-always fix
would break it is correct. The judgement that this is an oversight rather than a design choice is right,
and is now supported by two independent in-repo precedents instead of one.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing about the direction: preserve when absent,
renumber when explicit, in `plans_refs`, with the regression test `rename` already has. The pattern in
the revisions is that this plan traced ONE code path with unusual rigor and did not enumerate the
others. It followed `--rename` from the flag to the filename and missed that the same `order` value
flows into the metadata-only branch a dozen lines below; it found the `run_mv` precedent and missed the
`run_group_generic` one that solves the same problem better; and it deferred two questions as unknowns
that a single probe command each would have answered, both of which turned out to be real defects. The
second theme is narrower and worth stating because it recurs: a plan that mandates an operator-facing
text change must declare the file that text lives in, and a plan that asserts on a linter's verdict must
assert on the CODE rather than the exit status, because a fixture file will always carry unrelated
findings.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-002: the metadata-only branch shares the defect and the plan scoped only `--rename`. Widen this plan, or file the second branch separately? | WIDEN THIS PLAN to cover both branches, and require a failing test for each. | (a) File the metadata-only case as its own item; rejected because both branches read the SAME `order = start_order + i` line, so a fix to one is a one-line change away from the other and splitting would mean two plans editing the same expression, which is the merge hazard the repository keeps paying for. (b) Leave it unmentioned; rejected because it is the WORSE of the two states (a file contradicting its own name, invisible to every sweep) and the plan's fix would have left it live while reporting success. | Probe: `aw group plans bbb222 --set metaset --apply` yields front matter `- Order: 0` with filename `-01-`; `plans_refs.py:226` computes the order above the `if rename:` split and `:239-241` carries it into the metadata-only `RenamePlan`; `aw check plans`, `aw check` and lint's name rules all silent in that state. | yes |
| D-2 | PR-003: two shipped shapes solve this. Does the review pick one, or present both? | PRESENT BOTH with the reviewer's reading stated (Shape B, the threaded `Optional`), and require the executor to choose and record why. | (a) Mandate Shape B; rejected because the choice depends on how the executor structures `plan_set_assign`'s return, which is implementation detail a reviewer should not fix from outside, and because Shape A is genuinely viable if the metadata-only branch is guarded explicitly. (b) Leave the plan's single-precedent framing; rejected because "the fix shape needs no debate" is false once a second, better shape is known, and copying Shape A is the likeliest route to fixing only one branch. | `artifact_rename.py:682` computes `order_val = (start_order + i) if start_order is not None else None`; `:142` falls back to `int(m_uni.group("nn"))`; `:237` writes the `- Order:` line only `if order is not None`. `plans_refs.py:453-462` is the three-tier alternative. Both ship and both pass their tests. | yes |
| D-3 | PR-004: the deferred date and sibling-backend questions are measurable. Measure them, or leave them deferred as written? | MEASURE BOTH, keep both DEFERRED, and convert each into a filing obligation with the measurement attached. | (a) Fix them here; rejected because the date defect is a second field's behavior and `research_refs` is a second backend, and the plan's own reasoning that bundling would make the Order fix unreviewable is correct. (b) Leave them as written; rejected because "a real question this plan does NOT answer" and "if another backend shares the shape" both invite an executor not to look, and a deferral resting on an unknown cannot be checked by anyone later. | Probe: a plan with no `- Date:` line becomes `20260101-nodate-01-...` under `group` and keeps `20260908-...` under `rename`; `plans_refs.py:230` uses `_plan_date(text)` whose fallback is `"20260101"` (`:113-118`) while `:466` prefers the filename. `research_refs.py:326` is byte-identical to the defective line, planner default at `:164`. | yes |
| D-4 | PR-005: the plan mandates a `--help` edit in an undeclared file. Add `cli.py` to `Scope-Paths`, or drop the instruction? | ADD `cli.py` to `Scope-Paths` and keep the instruction, naming which of the two `--order` help strings is in scope. | (a) Drop the help-text instruction; rejected because the flag's absence silently meaning "renumber from zero" is precisely the operator-facing lie the fix exists to end, and leaving `--help` unchanged would leave the shipped command still misdescribing itself. (b) Leave the fence as authored and let the executor carry a `--scope-reason`; rejected because a plan should not require its executor to justify an out-of-scope edit the plan itself mandated; declaring the path is the honest form. | Plan `Scope-Paths` listed only `plans_refs.py` and the test module; the string is at `cli.py:3118` and is SHARED by `rename` and `group`; a second, deferred string sits at `cli.py:2246`. | yes |
