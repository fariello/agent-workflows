# Review: add the repository-policy tier of the retry-budget precedence chain, child y4adch (Set retrytier)

- Subject-Id: y4adch
- Subject-Type: ipd
- Reviewed-At: 2026-09-10
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `c8461ef5`. Structural preflight `aw ipd lint --phase author` conformed (clean, 0 findings)
before semantic review. At `--phase review-finalize` the linter reports exactly ONE finding, `IPD-Q501`
(OQ-01 is `Blocking: yes` and still `open`), which is this review's own escalation working as designed and
not a structural defect; there is no other diagnostic.

DISCLOSURE: authored in the same repository and by the same model family as the plan, so treat this as a
near-self-review and worth less than an independent one.

THE GRADUATION WORK HERE IS THE STRONGEST I HAVE REVIEWED IN THIS TREE, and I weakened none of it. The
author measured the source backlog item's central design caution and DISPROVED it (no schema change is
needed, because `parse_portable_policy` preserves unknown keys and two shipped keys deliberately bypass
`CONFIG_SCHEMA`), which is what makes this plan small. It searched five plan dispositions symbol by symbol
for overlap, distinguished itself from `xipfy1` in both directions with three quoted exclusions from that
plan's own text, corrected three of its own item's claims including a stale line number, and in OQ-03
argued AGAINST its own urgency by pointing out that the budget is never spent so this tier is
observationally inert. Every one of those claims re-verified true at review: the two precedents exist at
the cited lines with the cited shapes, `parse_portable_policy` really is permissive, `plan_retry` and
`retry_budget_remaining` really have no production callers, and the spec really does fix the three-tier
chain twice.

WHAT REVIEW FOUND IS THAT THE PLAN UNDER-MEASURED ITS OWN BLAST RADIUS, three times, and each would have
surfaced only mid-execution. The most consequential: the two `initialize_run` calls DISCARD the resolver's
return value and exist solely for the early refusal, while `freeze_run_policy_flags` is the ONLY call whose
result reaches `state["options"]`. E-03 treated all three as one change, so a faithful executor could thread
the root into the refusal path, satisfy every test the plan specified, and still freeze the default. Next,
there are ten existing call sites rather than three, six of them assertions, which makes the new parameter's
optionality a real design decision the plan never posed.

ONE FINDING IS ESCALATED AND IS THE ONLY THING A MAINTAINER MUST ACT ON. OQ-01 delegated the
fail-open/fail-closed choice to E-01 on the stated premise that the repository "has shipped BOTH postures".
That premise is false: `findings_gate_threshold` falls back to its default on an absent key, a non-string
value AND an out-of-vocabulary value and never raises, and `read_dependency_schema_cutover` swallows every
error. So no precedent selects an answer, fail-closed would introduce a posture the file does not have, and
an executor would have had to invent it mid-flight on a tracked shared file. OQ-01 is now `Blocking: yes`
with a third option recorded (fall back but WARN) as the reviewer's recommendation.

I COULD NOT ASK THE QUESTION IN THIS SESSION, having no interactive channel, so per `plan-review` Step 3.3
it is left explicitly OPEN with the required decision stated, the verdict is `REVIEWED - OPEN QUESTIONS`
and readiness is NO-GO. That is a genuine not-ready condition, not a missing signature.

Eight findings: seven FIXED in place, one (PR-004) escalated as a blocking open question. E-item and V-item
counts unchanged at six each; every fix belonged inside an existing item, and the new test case is a case
within E-05. No product code was modified.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-004 | BLOCKER | IN-SCOPE | B. security/consistency; G. executability | `config.py:1047-1069` returns None on any error; `config.py:1130-1145` returns `REVIEW_GATE_DEFAULT` on an absent key, a non-string value and an out-of-vocabulary value, never raising | **OQ-01's premise is false, so the question could not be resolved the way the plan delegated it.** The plan calls `review_findings_gate` "FAIL-CLOSED" and hands E-01 the fail-open/fail-closed choice on the basis that the repository has shipped both postures. Measured, NEITHER shipped key refuses a run over a bad value: both fall back quietly, and the gate key's own docstring frames that as landing "on the SAFE side". So the file holds ONE posture, fail-closed would add a second, and an executor would have been inventing a new failure posture for a TRACKED SHARED file mid-execution. The usual tie-breaker also fails here: a gate threshold has a safe side to fall back to, a retry budget does not, since a higher and a lower budget are merely different | C:Low; U:Medium; S:Medium; F:Medium; Overall:Medium-High | OPEN | ESCALATED. OQ-01 raised to `Blocking: yes` with `- Finding: PR-004`, so `aw ipd lint` refuses the plan at every checkpoint (confirmed: `IPD-Q501`). The three options are spelled out so the answer can be one word, the reviewer's recommendation (warn-and-fall-back, which dominates fail-open and avoids fail-closed's collective-refusal cost) is stated, and the inability to ask in this session is recorded in the question itself. E-01 now forbids choosing it and requires the maintainer's answer quoted verbatim with its date; V-01 fails if the executor pastes its own reasoning instead. New F-16 |
| PR-001 | HIGH | IN-SCOPE | A. correctness; G. executability | `oc_runipd.py:2783` and `agy_runipd.py:1838` are bare expression statements; the comment above each says "refuse an unhonorable flag BEFORE resolution... Nothing is re-decided here"; `runner_shared.py:2175-2176` feeds `oc_runipd.py:3059` / `agy_runipd.py:2063` | **The two driver calls discard the result, so "thread the root through the call sites" is two different jobs, and E-03 treated it as one.** Only `freeze_run_policy_flags`'s call produces the number that reaches `state["options"]`. Threading only the driver calls refuses correctly and then freezes the wrong value; threading only the freeze call resolves correctly but refuses late, after a run directory exists. An executor following E-03 literally could satisfy every case the plan specified while the middle tier changed nothing observable, because cases (a) to (g) all test the resolver directly | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Concern block and E-03 now distinguish the three production call sites and state WHY each needs the root, requiring the distinction in a code comment. New E-05 case (h) asserts the FROZEN value in `state["options"]` is the policy value rather than `2`, and required-tests marks it as not-optional-polish. V-03 fails a validation that pastes two call sites or treats all three alike; V-05 requires case (h) pasted separately. New F-14 |
| PR-002 | HIGH | UNDER-SCOPE | G. executability; E. testing | `tests/test_run_flag_surface.py` calls `resolve_retry_budget` at `:608`, `:611`, `:615`, `:618`, `:619`, `:625` and `freeze_run_policy_flags` four times; `grep -rln` over `tests/` finds no other module | **Ten existing call sites, not three, so the parameter's optionality is a design decision the plan never posed.** A REQUIRED parameter breaks all ten, six of them assertions that are correct as written; an OPTIONAL one leaves them green but lets a production caller silently omit the root, disabling the middle tier while every unit test still passes because they call the function directly. The plan named neither option, so the executor would have chosen under time pressure and, if it chose optional, would have had no guard against the silent-omission failure | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | New OQ-02-style question OQ-04 added and RESOLVED as a recommendation (optional keyword parameter) plus a MANDATORY guard: E-06 must assert all three production call sites pass a root, following the source-inspection pattern `test_an_out_of_range_value_refuses_the_whole_run` (`:642-649`) already uses. E-03 requires the choice stated; V-06 requires the guard and its rationale. New F-15 |
| PR-003 | MEDIUM | IN-SCOPE | E. testing; D. anti-regression | `tests/test_run_flag_surface.py:628` does `inspect.getsource(...).split('"""')[-1]` then `assertNotIn("10", ...)` | **The bound-delegation test constrains the CODE TEXT, not just behavior, and the plan told the executor to keep it green without saying how it works.** A config key name, parameter default, or comment containing `10` below the docstring FAILS it, and adding a second triple-quoted block changes what `split()[-1]` inspects. Told only "keep it green and do not weaken it", an executor who hits the failure while naming a key has an obvious wrong move available: relax the assertion | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | The mechanism is now spelled out in E-01, E-03, E-05 and required-tests, with the concrete instruction to choose a key name not containing `10` and to keep the whole rationale in ONE docstring. V-01 requires confirming the name, V-05 requires either the unchanged test or a before/after with the assertion shown intact. New F-17 |
| PR-005 | MEDIUM | IN-SCOPE | Evidence accuracy | measured: resolver `:2084` (plan said `:1987`), `RETRY_BUDGET_OWNER` `:2012` (`:1915`), `freeze_run_policy_flags` `:2155` (`:2058`), `refuse_frozen_flags_on_resume` `:2131` (`:2034`), agy call `:1838` (`:1792`), agy state write `:2063` (`:2017`) | **Every `runner_shared.py` anchor drifted about 97 lines.** The `config.py`, `run_recovery.py` and test anchors are all correct, which makes the pattern worth naming: one file moved and the plan's citations into it are uniformly stale. Two of the drifted lines are ones the plan forbids changing (the resume refusal and the owner constant), so a near-miss edit to a neighbouring line is the specific hazard | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All anchors re-measured and marked as review-HEAD data in the Concern, E-03, E-05, E-06 and F-18. The gate now records that these anchors were already wrong once and names the two forbidden lines among them |
| PR-006 | LOW | IN-SCOPE | E. testing | bare `python3 -m pytest` -> `1 failed, 5958 passed, 3 skipped, 2 xfailed`; failure walks 189 files in gitignored `opencode-recovery/` (1746 files) | **The plan correctly refused to quote a baseline, and the pre-existing failure still needed naming.** Requiring observed counts rather than a quoted baseline is the right instruction and I kept it. But an executor meeting an unexplained failure that walks a gitignored directory of another party's data has a live incentive to delete it to green the suite | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Recorded in required-tests and F-19 explicitly FOR RECOGNITION ONLY, not as a baseline to quote, with a prohibition on deleting, moving or modifying `opencode-recovery/` repeated in the gate and V-06. Bare-pytest invocation rule added (no `-n0`, no second `-q`, no `-p no:randomly`) |
| PR-007 | LOW | IN-SCOPE | A. correctness | `runner_shared.py:2459-2492`: the loop `continue`s on every `resume_rule == RESUME_REFUSE` row and never re-resolves | **E-05 case (g) is stated as if it guards a new risk, when the property is already structural.** `apply_run_policy_flags_on_resume` skips `--retry-budget` entirely, so a resume reads the frozen integer and has no path to consult config. The case is still worth keeping (it would catch a future implementation that added one), but framing it as the freeze guard overstates what is at risk and understates what the executor must NOT do | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-03 now cites the skip and states the real obligation (do not add a policy read to any resume path); E-05 case (g) is reframed as asserting an existing property; new F-20 records it |
| PR-008 | LOW | IN-SCOPE | G. executability | all three production sites in declared paths; all ten test sites in the declared `tests/test_run_flag_surface.py`; no other test module references either symbol | **`Scope-Paths` needed re-verifying once the caller set turned out larger, and it holds.** Worth stating rather than assuming, because a signature change reaching an undeclared test file would have been an out-of-scope edit discovered at finalize | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Scope check now records the verification and the grep that establishes no undeclared module is reachable |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|---|---|---|---|---|---|
| D-1 | OQ-01's premise is false (PR-004). Resolve the fail-posture question myself from the corrected evidence, or escalate it? | ESCALATE as `Blocking: yes`, with the three options spelled out and a stated recommendation | Resolve it as fail-open to match both precedents (rejected: the precedents' agreement is weaker evidence than it looks, since neither key had a reason to warn and neither guards a value where "safe" is undefined; and it would silently override a repo that believes it set a policy); resolve it as fail-closed for CLI consistency (rejected: one typo in a TRACKED SHARED file refuses every run for every agent and human until fixed and committed, which is a risk-appetite call on other people's work); leave it `Blocking: no` as the plan had it (rejected: with the premise corrected there is no precedent to follow, so a non-blocking question would be resolved by improvisation mid-execution) | `config.py:1047-1069`, `:1130-1145` measured; AGENTS.md reserves scope/risk-appetite calls to the human; `plan-review` Step 4 requires an unfixed BLOCKER to be escalated as a blocking question | no |
| D-2 | I have no interactive channel this session. Report NO-GO, or pick an answer to keep the plan moving? | Report it: leave OQ-01 OPEN, verdict `REVIEWED - OPEN QUESTIONS`, readiness NO-GO, and record inside the question itself that it could not be asked and how to answer it | Choose the recommended third option on my own authority (rejected: `plan-review`'s memory kernel forbids guessing a human decision, and this is an irreversible-in-spirit posture choice on a shared file); mark `Blocking: no` so the lint passes (rejected: that would trade a visible stop for an invisible improvisation, which is the exact failure the blocking-question gate exists to prevent) | `plan-review` Step 3.3 non-interactive exception; kernel rule 5; measured `IPD-Q501` now firing | yes |
| D-3 | The plan never posed the optional-versus-required parameter question (PR-002). Ask, or resolve it? | Resolve it with a recommendation plus a mandatory guard, as new OQ-04 | Ask the maintainer (rejected: it is an implementation-shape call with a measurable right answer, not a risk or scope decision, so asking would spend a human turn on something the call-site count settles); leave it unstated (rejected: it decides whether six correct assertions get rewritten, and an executor choosing under pressure would likely pick optional without adding the guard that makes optional safe) | Ten call sites counted; `test_an_out_of_range_value_refuses_the_whole_run` (`:642-649`) supplies the guard pattern | yes |
| D-4 | OQ-03 asks whether to land this before the budget is ever spent. Resolve it? | No. Leave it with the maintainer, `Blocking: no`, with the measurement confirmed and one consequence added for the decision | Resolve it as land-now (rejected: pure priority, which is the human's, and the plan is already written for that reading so nothing is blocked); resolve it as park (rejected: same reason, and it would discard completed design work over a scheduling preference) | `plan_retry`/`retry_budget_remaining` still callerless at review; `xipfy1` still `to-review`; AGENTS.md reserves priority to the human | yes |
| D-5 | The plan's `run_recovery.py`, `config.py` and test anchors are correct while every `runner_shared.py` anchor is stale (PR-005). Correct them silently, or name the pattern? | Correct them AND name the pattern, recording that these anchors were already wrong once and which two of them are lines the plan forbids editing | Just fix the numbers (rejected: the executor will re-read the file at a later HEAD where they may have drifted again, so the durable instruction is "re-locate by symbol, this file moves", not a fresher number) | Every anchor measured at `c8461ef5`; two drifted lines are the resume refusal and the owner constant, both in the plan's do-not-touch list | yes |
