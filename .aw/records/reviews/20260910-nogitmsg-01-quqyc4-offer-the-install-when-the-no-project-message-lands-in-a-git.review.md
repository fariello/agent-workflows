# Review: offer the install when the no-project message lands in a git repo, child quqyc4 (Set nogitmsg)

- Subject-Id: quqyc4
- Subject-Type: ipd
- Reviewed-At: 2026-09-10
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `379dab6b`. Structural preflight `aw ipd lint --phase author` conformed (clean, 0 findings)
before semantic review, and `--phase review-finalize` conformed after the revisions. This plan carries
`- Blocks-Release: next`, correctly inherited from backlog `okm6e6` (verified: the item carries it, and
`next` resolves to the single `planned` release `f33nrj`).

DISCLOSURE: authored in the same repository and by the same model family as the plan, so treat this as a
near-self-review and worth less than an independent one.

THE DIAGNOSIS IS EXACTLY RIGHT AND I REPRODUCED ALL OF IT rather than trusting the authored measurements. In
a fresh `git init` directory: `aw attention` (human) exits 3 correctly; `aw attention --json` exits 3 cleanly
with a well-formed payload; `aw attention --agent` CRASHES with the stated `ValueError` and a full traceback
at exit 1; `aw ipd board --agent` reproduces identically; `aw plans` really is an argparse invalid choice at
exit 2; and `aw attention --check` really answers exit 0 from its earlier branch. Finding the crash while
reproducing the item was the right instinct, and folding it in was the right call rather than scope creep,
since the item mandates a machine-readable fact on a `CommandResult` that cannot currently be emitted at all.

WHAT REVIEW CHANGED IS MOSTLY THE COMPLETENESS OF AN ALREADY-APPROVED DECISION. OQ-01 was resolved by the
maintainer to widen the schema, and that ruling is sound: the parity rule at `docs/cli-output-contract.md:89`
makes the alternative a documented contract violation rather than a mild dishonesty. But the ruling's
obligation list is incomplete in a way that matters, because one of the missing pieces turns a safety
improvement into a safety regression. That is the finding this review exists for.

THE PLAN'S OWN CITATIONS DRIFTED FOR THE SECOND TIME, which is worth stating because it shaped two fixes.
The item's `attention.py:1008` became the plan's `:2200` and is now `:2505`; the `cli.py` sites moved to
`:7290`; the `--check` branch to `:2482`. E-07 was about to write a transcribed line-number list into a
durable code comment; it now writes a re-derivation command instead.

Seven findings, all FIXED in place, no deferrals and no new open questions. E-item and V-item counts are
unchanged at seven each: every fix belonged inside an existing item.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-401 | HIGH | UNDER-SCOPE | B. security; A. correctness; D. anti-regression | `agent_schema.py:240-254` (`if 0 / elif 1 / elif 2`, no `else`); `:188-198`; `:275-279` | **The approved widening opens a greenwashing hole, because a THIRD constrained site exists and the ruling names only two.** The `kind == "result"` exit-code parity chain has no `else`, so the moment `exit: 3` is admitted it falls through and receives NO parity check whatsoever: a `result` record could then carry `exit: 3` beside a POSITIVE outcome (`clean`/`ok`/`conforms`) and validate. That chain exists precisely to stop greenwashing, so a widening that skips it makes the validator WEAKER, which is the opposite of this plan's intent. Also confirmed there is no exit-code test in `tests/test_agent_schema.py` at all (10 tests, none touching `exit`), so nothing would have caught it | C:Low; U:Low; S:Medium; F:High; Overall:Medium | FIXED | E-05 now enumerates all three sites by symbol and REQUIRES a new `elif exit_code == 3:` branch demanding `outcome == "cannot-run"`. Required-tests gains four schema cases including the negative one (a `result` with `exit: 3` and a positive outcome must be REFUSED). V-05 refuses completion without that diff and that test. OQ-01's obligation list corrected in place; new F-13 |
| PR-402 | HIGH | UNDER-SCOPE | A. data integrity; C. architecture | `docs/cli-agent-protocol.md:45`, `:96`, `:113`; `docs/cli-output-contract.md:56-64`; `tests/test_output_contract.py:49-53` | **A second published contract binds this and was not declared, and the paragraph actually being amended was never cited.** `docs/cli-agent-protocol.md` binds `exit: 2` to `cannot-run` and carries a STABILITY clause promising an `aw.agent/v2` bump for any change to field meaning; it was absent from `Scope-Paths`. And `docs/cli-output-contract.md` `## 3. Exit Code Semantics` declares "a uniform three-state exit classification across all verbs", the sentence that becomes FALSE on widening, which the ruling never cited (it cited `:45`/`:78`/`:88`); a shipped test asserts that section exists. Amending one doc and not the other leaves the two disagreeing, the exact failure the ruling's own obligation (2) exists to prevent | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | `docs/cli-agent-protocol.md` ADDED to `Scope-Paths` with the over-scope justification recorded. E-05 lists every real anchor in both docs, requires the three-state paragraph be amended, and requires a WRITTEN determination on the stability clause with an instruction to STOP rather than absorb a `v2` decision. Spec-sync section rewritten from conditional to declared. Required-tests adds re-reading both docs. New F-14, F-15 |
| PR-407 | HIGH | IN-SCOPE | E. testing; D. anti-regression | measured `1 failed, 5958 passed, 3 skipped, 2 xfailed`; `test_orchestrator_retirement.py` -> `112 passed`; `.gitignore:49` | **The suite baseline is wrong in both its count and its named failing test, and the real failure invites destroying a co-worker's work.** The plan cites `1 failed, 5648 passed` with the failure being `test_orchestrator_retirement`; that file PASSES outright. The single real failure walks the repo and trips over 189 files in a GITIGNORED `opencode-recovery/` directory belonging to another party. An executor told to expect a different failure could reasonably "clean up" that directory to green the suite, destroying uncommitted work in a shared checkout | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Required-tests carries the re-measured baseline, names the real failing node id and its mechanism, and adds an explicit prohibition on deleting or modifying `opencode-recovery/`. Focused-surface command extended to include `tests/test_agent_schema.py` and `tests/test_output_contract.py`, both of which E-05 now changes |
| PR-403 | MEDIUM | IN-SCOPE | F. UX; prevent silent failure | `cli.py:7290`/`:7293` pass verb `"plans"`; `aw plans` exits 2 (invalid choice) | **`aw ipd board` tells the operator to run a command that does not exist.** Its no-project message reads "aw plans: no AW project found here." because the call site passes the literal verb `"plans"`, while F-4 already proves `aw plans` is an argparse invalid choice. So the message whose entire purpose is to tell the operator what to do names something unrunnable. The plan records this as trivia in its history line and never fixes it, though it is a one-string change on a line E-03 already edits | C:Low; U:Medium; S:Low; F:Low; Overall:Low | FIXED | E-03 now requires the verb string corrected to `"ipd board"` and the dead unreachable `return 3` at `attention.py:2510` removed, both on lines that item already touches. V-03 requires the before/after of that message line. Scope-check records the two folded-in corrections with their justification. New F-18 |
| PR-405 | MEDIUM | IN-SCOPE | E. testing; F. UX | `attention.py:2481` (`not explicit_dir`); measured `--dir <git-only>` -> no output, unpiped exit 0 | **E-06's case (d) asserts a behavior that does not exist, and the real behavior is worse than the bug being fixed.** "`--dir <explicit>` honored verbatim and unaffected" cannot be asserted against the message, because both message branches are guarded by `not explicit_dir`, so `--dir` SKIPS the message entirely. Measured: an operator who explicitly names a git-but-not-AW directory gets NO output and a SUCCESS exit code, which is a worse dead end than the no-`--dir` path this plan exists to fix | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Case (d) split into (d1) `--dir` at a real project works unchanged and (d2) `--dir` at a git-only directory CHARACTERIZED with today's silence-and-exit-0 pinned as a baseline and REPORTED, with an explicit instruction not to change the guard here since it would alter which inputs produce a cannot-run. Added to the deferred list as a follow-up item to file. V-06 requires the measurement stated. New F-17 |
| PR-406 | MEDIUM | IN-SCOPE | Evidence accuracy; G. executability | sampled 5: `backlog.py:341`, `specs.py:415`, `prompts.py:180` correct; `releases.py:600`, `run_cli.py:97` DRIFTED; 57 call sites across 18 modules | **The list E-07 would immortalize is ~60 percent accurate and its headline count is wrong.** The plan correctly told its executor to spot-check rather than copy, so review did: two of five citations land on unrelated lines. And the fallback surface is 57 `resolve_verb_repo_root` call sites across 18 modules, not "about twenty". Writing either the list or the count into a DURABLE code comment guarantees it is stale within days, which is doubly ironic in a plan whose own citations drifted twice | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-07 now reports which sampled citations drifted, requires a DERIVED count, and forbids a transcribed line-number list in favor of a re-derivation command. V-07 states that a report claiming all citations are accurate is itself evidence the check was not performed. New F-20 |
| PR-404 | MEDIUM | IN-SCOPE | C. architecture | `project_context.py:339` interpolates `Path.cwd()`; callers resolve via `resolve_verb_repo_root` (`attention.py:2474`) | **`no_project_message` reads `Path.cwd()` itself and takes no directory, so its correctness rests on a caller guard rather than on its inputs.** Both callers have ALREADY resolved a root, and they agree with cwd today only because the message branch is guarded by `not explicit_dir`. A git probe written the same way inherits that accidental coupling, and a third caller without the guard would silently produce a message describing the wrong directory | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-01 now prefers an optional `start_dir` parameter fed from the caller's resolved root, and if the `cwd()` read is kept requires the dependence on the caller guard be documented in a comment. V-01 requires the executor to state which it did and paste it. New F-19 |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|---|---|---|---|---|---|
| D-1 | PR-401 found that the maintainer's OQ-01 ruling missed a third schema site whose omission would weaken the validator. Reopen the ruling, or implement it plus the missing piece? | Implement the ruling as decided and ADD the missing parity branch, treating it as completing the ruling rather than revisiting it. Did not reopen OQ-01 | Reopen OQ-01 and ask the maintainer to re-decide (rejected: the DIRECTION the maintainer chose is sound and the parity rule at `docs/cli-output-contract.md:89` supports it; only the obligation list was incomplete, which is an implementation detail); implement the two named sites literally (rejected: it would ship a validator that accepts `exit: 3` beside a positive outcome, i.e. greenwashing, which no reading of the ruling's intent supports) | `agent_schema.py:240-254` has no `else`; the chain's stated purpose is anti-greenwashing; the ruling's own obligation (3) says to preserve the cannot-run distinction | yes |
| D-2 | Does widening the permitted `exit` values count as a breaking change requiring an `aw.agent/v2` bump under `docs/cli-agent-protocol.md:113`? | Did NOT decide it myself. Recorded the assumption that it is not breaking, with the measured basis, and REQUIRED E-05 to state the determination in writing and STOP rather than absorb it if the executor concludes otherwise | Rule it non-breaking on my own authority (rejected: a protocol version bump is a published-interface decision, i.e. irreversible in the sense that matters, and belongs to the maintainer); rule it breaking and mark the plan NO-GO (rejected: no in-repo consumer switches on the value, so the evidence does not support blocking) | Measured: only the validator and `tests/conformance_matrix.py:240` read `exit`, and the latter does not branch on it; the field's meaning is unchanged while its domain widens | yes |
| D-3 | The `--dir` path silently exits 0 at a git-only directory (PR-405), arguably a worse defect than the one being fixed. Fold the fix in? | No. Required E-06 to CHARACTERIZE and REPORT it, and recommended filing a follow-up item | Fix it here (rejected: the `not explicit_dir` guard is deliberate and changing it alters which inputs produce a cannot-run, a contract change with its own blast radius, on a plan already carrying a release gate); ignore it (rejected: it is a live dead end and case (d) as written would have papered over it with an assertion about behavior that does not exist) | `attention.py:2481` guard read; measured no-output exit 0; the plan's own scope excludes changing what counts as a project | yes |
| D-4 | Two small defects (the wrong verb string, the dead `return 3`) sit on lines E-03 already edits. Fix in place or defer? | Fix in place, recorded as justified over-scope in the scope check | Defer both to separate items (rejected for the verb string: it is a live misdirection inside a help message, a one-string change, and deferring it means shipping a fix to a message that still names a non-existent command; the dead return is a zero-risk deletion on a line being read anyway) | `cli.py:7290`/`:7293` verb string; `aw plans` measured exit 2; `attention.py:2510` unreachable | yes |
