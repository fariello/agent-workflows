# Review: add the Graduated-To forward link and its dangling check mirroring From-Backlog, child bwgyum (Set setidhard)

- Subject-Id: bwgyum
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `643f1508`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value therefore
rests on EXECUTING the plan's claims rather than re-reading them. Five things were run rather than
inspected, on scratch repositories under `/tmp`: a backlog item carrying `- Graduated-To:` was pushed
through BOTH routing paths of `aw backlog set`; a spec carrying the field was pushed through
`aw specs set`; a plan carrying the field was fed to `aw ipd lint`; both sibling link regexes were
executed against the space-separated and no-space list forms; and both dangling twins were run against
a repo with no source trees. The first produced the BLOCKER, the second and third produced the two
HIGH findings that reshaped the plan's write site, and the fourth strengthened the plan's own headline
argument from "under-validates" to "reports clean".

SCOPE: only this child was a candidate. Read as evidence: `releases.py`, `check_engine.py`,
`backlog.py`, `specs.py`, `status_set.py`, `ipd_schema.py`, `plans.py`, `artifact_naming.py`,
`cli.py`, spec `4w7d6s`, backlog `sjsoqq` and `6h7y2y`, sibling plan `yku4ga`, and
`.aw/records/backlog/README.md`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-801 | BLOCKER | IN-SCOPE | A. Correctness and data integrity; G. Plan executability | measured on a scratch repo; `backlog.py:317-337` | **THE FIELD IS NOT MERELY UNBUILT, IT IS ACTIVELY DESTROYED BY THE VERB THIS PLAN WRITES IT WITH, so the plan's premise that adding an absent field is additive and safe is FALSE.** `backlog.run_set` rebuilds the bullet block from a FIXED six-field template (`_render_item`), dropping every field outside it. MEASURED end to end: an item carrying `- Graduated-To: somesetid, othersetid` went through `aw backlog set --status graduated <path>` and came out with the line GONE, exit 0, no warning, no finding. So E-04's writer would have produced a forward link that the NEXT status transition silently deletes, and a graduation is exactly such a transition. This is not hypothetical: the whole point of the field is to survive on a graduated source. | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added F-11 with the reproduction. Added a new E-item (now E-02) that must fix the destructive path BEFORE anything writes the field, following `Blocks-Release`'s existing in-tree answer (`backlog.py:563-571` re-applies a shared line primitive after render) rather than widening the template. Added V-02 requiring the BEFORE reproduction and the AFTER preservation both pasted. The Concern now states the corrected premise. PR-803's move of the write site to `status_set` also dissolves this for the primary path, but the fix is retained because the destructive path remains reachable. |
| PR-802 | HIGH | IN-SCOPE | A. Correctness and data integrity | measured both paths; `cli.py:11229-11245` | **ONE VERB, TWO IMPLEMENTATIONS, OPPOSITE DATA-PRESERVATION BEHAVIOR.** `cli.py` branches on whether `--status` was passed: bare `aw backlog set <status> <selector>` routes to `status_set.run_set_command`, which rewrites lines surgically and PRESERVED the field (measured); `--status <s> <path>` routes to `backlog.run_set`, which destroyed it (measured). So whether a graduation's link survives depends on which spelling the agent happened to use, and a fix validated on only one path leaves the other free to regress silently. The plan spoke of "the setter" as though it were one thing. | C:Low; U:Medium; S:Low; F:Medium; Overall:Medium | FIXED | Added F-12. E-02 now requires BOTH paths pinned by test, and its expected outcome says so. V-02 requires the round trip pasted for each path with an explicit statement that one path proves nothing about the other. OQ-03's resolution rewritten from "the setter" to name `status_set.run_set_command` specifically, with the two-implementation measurement as the reason the original answer was under-specified. |
| PR-803 | HIGH | UNDER-SCOPE | C. Architecture and operability; F. KISS (reuse existing mechanisms) | `status_set.py:715-760`; `cli.py:10927`, `:11233`, `:11269-11279` | **THE WRITE SITE WAS WRONG, AND CHOOSING THE RIGHT ONE DELETES A DEFERRAL THE PLAN HAD GRANTED ITSELF.** E-04 proposed a `--graduated-to` flag on the backlog setter and permitted implementing only the backlog half, recording the SPEC half of spec G3 as a finding "if that is a materially larger change". It is not larger at all. `status_set.run_set_command` is record-type-AGNOSTIC and is the shared handler for `aw backlog set`, `aw specs set`, `aw ipd set` AND bare `aw set`; it already carries FOUR precedents of precisely this shape (`Blocks-Release`, `From-Backlog`, `Item-Dependencies`, `Priority`), each hoisted OUT of the status branch so the write persists on a same-status no-op, each funnelling through ONE shared `releases.set_*_line` primitive under the comment "no duplicate write path". Measured: a spec carrying the field survived `aw specs set` through that handler. So the spec half is the SAME LINES OF CODE as the backlog half. Left as written, the plan would have added a fifth write mechanism next to four identical ones AND banked a deferral justified by a false premise. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-14. E-04 rewritten: the write is the FIFTH hoisted field-write in `status_set`, through a new `releases.set_graduated_to_line` mirroring `set_from_backlog_line`, with the four precedents named. The spec-side hedge is WITHDRAWN in E-04, in the Deferred section (as a withdrawal with its reason, not a deferral), and in V-04, which now states that a recorded finding in place of a spec-side implementation is a FAILED validation. `status_set.py` and `cli.py` added to `Scope-Paths`. |
| PR-804 | HIGH | UNDER-SCOPE | A. Correctness; E. Testing | measured `aw ipd lint` exit 1; `ipd_schema.py:204`, `:251` | **A PLAN CARRYING THE FIELD FAILS THE LINTER, AND THIS PLAN'S OWN CHECK SCANS PLANS.** `ipd_schema.META_RECOGNIZED` omits `Graduated-To`, so `aw ipd lint --phase author` on a plan carrying the field returns exit 1 with `IPD-M103` (measured on a copy of this very plan). E-03 deliberately scans plans as well as sources, following the twin's "tolerates it anywhere for symmetry" rule. So as authored, the plan builds a check that looks for a field the linter forbids anyone from writing in one of the three trees it scans. The schema entry is a one-line addition with two exact precedents (`META_FROM_BACKLOG`, `META_FROM_SPEC`), both recognized-but-optional for the stated reason that recognition only stops the unknown-field error while value validation lives in `aw check`. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-13. Folded into the new E-02 with the precedent named and the value-validation split stated. V-02 requires the before (exit 1, `IPD-M103`) and after (exit 0) lint runs plus the `META_RECOGNIZED` diff. `ipd_schema.py` added to `Scope-Paths`. |
| PR-805 | HIGH | IN-SCOPE | A. Correctness; E. Testing (a test that would pass a broken implementation) | measured both sibling patterns | **THE PLAN'S OWN HEADLINE ARGUMENT UNDERSTATES THE FAILURE, AND THE UNDERSTATEMENT MAKES ITS TEST WEAKER THAN IT THINKS.** The plan says a copied single-token regex "would capture only the first entry and silently validate one of several". MEASURED against both `_ITEM_FROM_BACKLOG_RE` (`(\S+)\s*$`) and `_ITEM_FROM_SPEC_RE`: on `- Graduated-To: first, second` BOTH match NOTHING, because `\S+` cannot span the space and the `$` anchor then fails. So the real failure is that a well-formed two-entry field reads as ABSENT and the file is reported CLEAN, which is strictly worse than validating one of two and is invisible rather than partial. It also has a test consequence the plan missed: on the NO-SPACE form `first,second` the sibling pattern DOES match (capturing the junk token), so a multi-entry fixture written without a space would pass against a copied regex and prove nothing. E-05's central assertion could have been satisfied by the very bug it exists to catch. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-3's corrected text with the measurement. E-01's prohibition rewritten with the actual failure mode and a `(.+)` whole-value match prescribed. E-05 now REQUIRES the multi-entry fixture to use a comma AND a space and to assert the parsed list equals `["first", "second"]`, with the reason stated inline. V-05 requires showing the fixture value contains a space and confirming both sibling patterns match nothing on it. |
| PR-806 | MEDIUM | IN-SCOPE | A. Correctness (false positives on an error-severity rule) | measured on a scratch repo; `check_engine.py:2992-2996` | **THE TWO TWINS DISAGREE ON FAIL-SAFETY AND THE PLAN SAID "MIRROR THE TWIN" WITHOUT SAYING WHICH.** `check_from_spec_dangling` returns early with NO findings when its known-id set is empty, with a docstring saying it cannot distinguish a dangling link from an invisible corpus; `check_from_backlog` has no such guard. MEASURED on a repo holding one plan with both links and NEITHER source tree: the spec scan reported 0 and the backlog scan reported 1 FALSE finding. Since this rule is `error`-severity and can drive a nonzero exit, inheriting the backlog twin's omission would make an unreadable or absent plans tree turn every valid forward link into a commit-blocking finding. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-15 and a Step 0 convention with the measurement. E-03 now requires the SPEC-side posture explicitly, with the instruction not to inherit the omission "just because this rule is described as its mirror". V-03 requires the empty-corpus run pasted showing zero findings. The spec-sync section notes the divergence belongs in `check_from_backlog`'s cross-reference docstring so a later reader does not harmonize the safer behavior away. |
| PR-807 | MEDIUM | IN-SCOPE | F. KISS and principles (single source of truth) | `plans.py:70-76`; `check_engine.py:816-829` | E-01 said to "validate the token shape against the naming authority" without naming it, and E-03 said to derive the plan-Set set from "the plans index or a scan" without naming that either. Both authorities already exist: `plans.is_set_id_valid` (lowercase-kebab, `MAX_SET_ID_LEN = 40`) and `check_engine._parse_setid` (which already handles the `<terse> (<descriptive>)` bullet form) over `_iter_plan_ipds`. Left vague, an executor writes a second setid regex and a second plans-path literal, which is the drift GUIDING_PRINCIPLES 8 forbids and which the sibling `check_from_spec_dangling` docstring explicitly congratulates itself on avoiding. There is also a message-quality consequence: a malformed token reported as "does not resolve to a real Set" sends a reader hunting a Set that was never named, when the defect is a typo. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-16 and a Step 0 convention naming both symbols with line anchors. E-01 now names `plans.is_set_id_valid` and requires malformed-versus-dangling to be distinct reports; E-03 names `_parse_setid` + `_iter_plan_ipds` and forbids a new parser or path literal. Both V-items require NEGATIVE search proof that no second mechanism was added, and E-05 gained a malformed-token assertion. |
| PR-808 | MEDIUM | IN-SCOPE | E. Testing; G. Plan executability (an unstated reach limit) | `check_engine.py:1740-1783` | The once-per-full-sweep seam runs only inside the `collisions` branch, entered only when `types == ["all"]`. So the new rule fires on `aw check all` and NOT on `aw check backlog` or `aw check plans`. The plan never said so, and its own validation section asks for `aw check all` counts without noting that a type-scoped check would report zero for a reason unrelated to correctness. An executor debugging "why does my new rule not fire" would lose time, and a later reader could reasonably believe the field is validated on every check. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-17. E-03 states the limit and requires it in the docstring; V-03 requires that docstring sentence pasted. Step 0 conventions record it alongside the seam description. |
| PR-809 | LOW | IN-SCOPE | A. Correctness (a measurement stated imprecisely enough to mislead) | bullet-anchored versus substring grep over `.aw/records/` | The plan's F-1 and E-06 rest on "`Graduated-To` occurs ZERO times", which is true of the PACKAGE and true of actual bullets, but a reader verifying it with a plain `grep -rc Graduated-To .aw/records/` gets **138 hits across 18 files** (the spec's own normative text, backlog `sjsoqq`, and history lines recording that the machine-readable field is "owed"). An executor running E-06's zero-findings check could see that number, conclude the tree already carries authored fields, and either doubt the baseline or start a migration this plan excludes. The distinction is prose versus bullet. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | F-1 restated with both figures and the distinction. E-06 now says to count `- Graduated-To:` BULLETS and warns that a naive grep returns a large number that is not authored fields. V-06 requires confirming bullets were counted. |
| PR-810 | LOW | IN-SCOPE | E. Testing (stale baselines) | measured at HEAD `643f1508` | Both Step 0 baselines are stale in both halves. `aw check all` is stated as 98 findings; measured **238** (174 `check.scope-drift`, 40 `check.setid-collision`, 15 `check.lifecycle-transition-invalid`, 3 `check.name-nonconformant`, 2 `check.ipd-dependency-findings-blocked`, and four singletons). The suite is stated as `1 failed, 5648 passed` attributed to `tests/test_orchestrator_retirement.py`; measured `1 failed, 5919 passed, 3 skipped, 2 xfailed`, the failure being `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, which is ENVIRONMENTAL (a gitignored local `opencode-recovery/` dump of 1746 files) and may be absent elsewhere. The twin's one-finding baseline DID hold and its location was captured. The plan's own instruction to compare by delta and per rule is what keeps this from being harmful. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-18. Step 0 baselines replaced with the measured values, the failure labelled environmental with its cause, the misattributed test named as wrong, and the twin's finding location recorded. E-06 and the validation section now tell the executor to measure its own totals and quote neither figure, and to compare the failure set by NODE ID. |

CONFIRMED SOUND, AND IT IS THE PLAN'S ENTIRE ARGUMENT. Every load-bearing claim was re-verified
rather than trusted, and all held: `Graduated-To` really is absent from the package (zero occurrences)
and from the records trees as a bullet; `check_from_backlog` really is the right mirror and really
does rglob three trees under both roots, skip the three index files, and ride the once-per-sweep seam
(`releases.py:580-619`); the seam really does give each scan its own `try/except` with the recorded
reason "so a failure in either scan cannot suppress the other" (`check_engine.py:1758-1783`); the
twin really is `RuleSpec("error", ASSURANCE_REPOSITORY, DET_DETERMINISTIC, "I-07")` at
`check_engine.py:114-116`; the id6/setid asymmetry really is called "intentional and correct" by spec
G4; `evaluate_blocking_close`'s HANDOFF/SATISFIED/DE-GATED ladder really is the consumer spec G7
names, and this plan really does not touch it; and there really is no graduation verb today (backlog
`6h7y2y` graduated a Set that would add one, but `y9s4vm`/`jxxec8`/`iuxtjy` are unexecuted). The
DEPENDENCY DIRECTION is right and worth restating: this child carries no `Item-Dependencies` and may
execute first, because Order 03 depends on IT rather than the reverse. `_iter_plan_ipds`'s
`*.ipd.md`-only glob was checked for a gap and has none today: all 606 plan files match, and the 8
that do not are the README/INDEX/STATUS files the skip list drops anyway.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. It grew by one E-item and shrank by one deferral, and
both movements came from the same source: running the plan's own instructions on a scratch repo instead
of reading them. The plan reasoned from absence ("the field does not exist, so adding it is additive")
and absence turned out to be three different things: not written, not preserved, and not permitted.
The write-site correction (PR-803) is the most valuable single edit, because it fixes the destruction
finding, deletes the spec-side deferral, and removes a fifth parallel write mechanism, all by moving
the code one module over to where four identical writes already live. Scope-Paths grew from seven to
eleven; that is real growth and it is justified per path in the plan, but the test applied was strict:
a fix is in scope only if the plan's deliverable does not survive execution without it, which is why
the general audit of other fixed-template renderers was pushed out (D-3) even though the same
data-loss class probably lives there too.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Do the two measured data-integrity defects (the field is destroyed by `aw backlog set`; a plan carrying it fails `IPD-M103`) belong in THIS plan, or in a follow-on so this child stays a pure link-adding change? | IN this plan, as a new E-item (E-02) with its own V-item. | (a) A separate corrective IPD, keeping this child minimal. Rejected: this child would then ship a writer whose output the next transition deletes and a check that scans a tree where the field is illegal, i.e. a feature that passes its own fixtures and loses data in the real tree. (b) Write the field only where it happens to survive and document the hazard. Rejected: that leaves a known silent data-loss path in the exact verb graduation uses. | `backlog.py:317-337` plus the measured round trip showing the line deleted at exit 0; `ipd_schema.py:251` plus the measured `IPD-M103` exit 1; the prerequisite test is "does the plan's deliverable survive its own execution without this", and both fixes pass it. Existing patterns make them small: `backlog.py:563-571` and `META_FROM_SPEC`. | yes |
| D-2 | Should the SPEC-side half of spec G3 stay deferrable with a recorded finding, as the plan permitted? | No: withdraw the hedge and require both halves. | Keeping the hedge. Rejected because its stated premise ("if that is a materially larger change") is measurably false, and an unexamined escape hatch is how a normative spec requirement quietly becomes a follow-up. | `cli.py:11269-11279` routes `aw specs set` through the SAME `status_set.run_set_command` as `aw backlog set` (`cli.py:11233`); measured, a spec carrying the field survived that path. The spec half is the same lines of code, so there is nothing to defer. | yes |
| D-3 | Should the plan also audit the other fixed-template record renderers for the same silent-field-drop class PR-801 found? | No: excluded with a reason, and recommended as its own backlog item. | Auditing them here. Rejected: it touches renderers this plan does not otherwise open and is a root-cause refactor rather than a prerequisite, so it fails the same test D-1 applied. Also rejected: saying nothing, which would let a real finding evaporate. | `Blocks-Release`'s per-field workaround at `backlog.py:563-571` is itself evidence the class was met before and patched per-field rather than at the root, so a general fix is a real and separate piece of work. GUIDING_PRINCIPLES 6 (guard against scope creep). | yes |
| D-4 | What resolves as "a real plan Set" for the dangling check, given the plan left OQ-01 answered but the mechanism unnamed? | Any setid carried by at least one plan in ANY lifecycle directory, read via `check_engine._parse_setid` over `_iter_plan_ipds`, with an empty-corpus fail-safe returning no findings. | (a) Live Sets only. Rejected: a graduated source's Set eventually becomes entirely `executed`, so the link would start resolving and later dangle because the work FINISHED, firing on the successful case. (b) A new scan or index read. Rejected: a second setid parser or plans-path literal is exactly the drift the sibling check's docstring records avoiding. | The plan's own OQ-01 already chose all-directories and mirrors how `From-Backlog` resolves against all backlog ids regardless of status; `check_engine.py:816-829` and `:1880` supply the parser and iterator; `check_engine.py:2992-2996` supplies the fail-safe precedent, and the measured 0-versus-1-false-finding comparison shows why the spec side's posture is the right one for an `error` rule. | yes |
