# Review: freeze the resolved model identity and rate card into the run record, child w33lrl (Set runverdict)

- Subject-Id: w33lrl
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `4cfa0a90`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value rests on
RE-MEASURING rather than re-reading. Nine things were RUN rather than inspected: all 140 `state.json`
were scanned for `options.model`, `launch_profile`, `verify_launch_profile` and any card-shaped key;
all 496 session logs were walked and the exact key set of every cost-bearing part enumerated, then
every model/provider key path in the corpus was located; the live OpenCode config was resolved
IN-PROCESS through `resolve_config_path` and its top-level `model`, `small_model`, `agent` block and
all 82 model cost blocks were counted; `models_from_config` was CALLED to see what it actually
returns; `per_million` and its one call site were read to establish the stored unit; every cost path
in the repository was traced from `extract_log_metrics` outward looking for a rate multiplication;
agy's model resolution was read at three sites and cross-checked against 4 live agy runs and their
session logs; the oc-to-agy import surface was re-measured by AST walk; and the bare suite was run.
FOUR of those nine produced findings that INVERT an instruction the plan gave its executor.

SCOPE: only this child was a candidate. Plans `5f2h8i`, `8hald1`, `aflsz3`, `5lxvl3`, `mm5p3v`,
`3cm15q`, `kgpptv` and the `fzxfph` review record were read as EVIDENCE, not reviewed, and are not in
the ledger. Also read: `oc_runipd.py`, `agy_runipd.py`, `oc_models.py`, `runner_profiles.py`,
`run_viewer.py`, `benchmark_metrics.py`, `lane_containment.py`, `tests/test_oc_models.py`,
`tests/test_runner_profiles.py`, `tests/test_oc_runipd.py`, spec `25kzda`, and the release record.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | MEDIUM | IN-SCOPE | G. Plan executability (stale measurement) | re-ran every count at `4cfa0a90` | **EVERY QUANTITY IN THE PLAN MOVED WITHIN ONE DAY, INCLUDING TWO USED AS ARGUMENTS.** 135 runs -> 140; `options.model` 5 -> 9; `launch_profile` 33 -> 34; cost-bearing steps 28158 -> 32333; oc-to-agy imports 47 -> 48; `launch_profile_record` `:2740` -> `:2703`; `PROVENANCE_HOST_DEFAULT` `:378` -> `:492`; `PROVENANCE_VALUES` `:380` -> `:500`. The plan's own gate warns against navigating by line number, so its citations were the natural place to check, and they were stale. The baseline was wrong too: claimed `1 failed, 5648 passed` with the failure in `test_orchestrator_retirement`; measured `1 failed, 5919 passed, 3 skipped, 2 xfailed` with the failure in `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, environmental, caused by 189 untracked `opencode-recovery/*.md` files belonging to another party in this shared checkout. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Every figure and citation corrected throughout. Added F-10 recording the drift, and the gate's re-locate paragraph now cites the plan's OWN drift as the evidence for its own instruction, with two more symbols added to the by-name list. Baseline replaced with the measured figures and the named environmental cause; the fence now forbids touching those untracked files. |
| PR-002 | HIGH | IN-SCOPE | A. Correctness; G. Plan executability | `oc_models.py:301`, `:330`; live config top-level `model` key | **E-01 POINTED THE EXECUTOR AT A READER THAT CANNOT ANSWER ITS QUESTION, AND THE FALLBACK IS THE ONE THING THE ITEM FORBIDS.** E-01 said to resolve the host default through `models_from_config` and leaned hard on that function's no-secret guarantee. Measured by calling it: it reads only `provider.<name>.models.<id>` KEYS and returned the 82-entry CATALOG. The DEFAULT is the config's top-level `model` key (live value: a concrete `provider/model` id, with a second one under `small_model`), and NOTHING in this repository reads it; `catalog_from_config` has the same limit by construction. So the instruction yields a list and no answer, and an executor could either stall or write the second config reader the plan explicitly forbids as "the exact way a credential reaches a run record". | C:Low; U:Low; S:Medium; F:Medium; Overall:Medium | FIXED | Added F-12. E-01 now states the measured limit, ADDS one small no-secret accessor to `oc_models` (already permitted by the Scope check) with the guarantee required in its own docstring and the read narrowed to the default key alone, and requires recording WHICH key supplied the value, since OpenCode also supports a per-agent `model` and a `small_model` (live: the only configured agent sets `temperature` and no model, so the top-level key is effective TODAY but that is one config, not a guarantee). Conventions, Proposed changes, Scope check and V-01 updated. |
| PR-003 | HIGH | IN-SCOPE | A. Correctness (a 1000000x error via the plan's own instruction) | `oc_models.py:501`, `:565-577`, module docstring `:6`; live cost block | **"REUSE `oc_models`' PRICE VOCABULARY" READS AS "CALL `per_million`", WHICH WOULD MULTIPLY AN ALREADY-CONVERTED VALUE BY 1e6.** The config stores `$/Mtok` by construction: the module docstring says so, and `build_models` calls `per_million` on the GATEWAY's per-token value before storing. Measured live, the default model's block is `{"input": 5.5, "cache_read": 0.55, "cache_write": 6.875, "output": 27.5}`, dollars per million on its face. E-02's very next sentence warns that mixing units is "how a 1000000x error enters a cost report", so the instruction and its own warning were in conflict. | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | Added F-14. E-02 now names the direction explicitly (`per_million` is the WRITE-side converter), requires reusing its VALIDATION POSTURE without the multiplication, requires recording the unit as `$/Mtok`, and requires a code comment stating why the function is not called so a later reader does not "fix" it. V-02 now FAILS if it shows a `per_million` call on a config value. |
| PR-004 | HIGH | IN-SCOPE | A. Correctness; C. Architecture (a fix for a defect that does not exist) | `run_viewer.py:563`; grep for any rate multiplication across `agent_workflows/` | **E-03'S PREMISE IS INVERTED: NOTHING IN THIS REPOSITORY PRICES TOKENS FROM A CARD, SO THERE IS NO SUBSTITUTION TO STOP.** Reported cost is `extract_log_metrics` summing `part.cost` from `step_finish` events, persisted onto the attempt as `cost`/`verify_cost`; no module outside `oc_models` multiplies tokens by a rate. So "make the recorded card the authority for that run's reported cost" describes a change with no target, and an executor hunting for the substitution could plausibly BUILD the tokens-times-card path that the same plan's E-06 and fence forbid as an `aflsz3`/`8hald1` deliverable. The property the item wants is real and worth protecting; it is just currently true by construction. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-15 and a Step 0 convention. E-03 rewritten as a stated invariant plus a REGRESSION GUARD, with an explicit instruction that it does not change how cost is reported. V-03 now requires the guard be shown to FAIL under a deliberate card-derived substitution, then reverted. Sibling E-item numbers corrected (`aflsz3` E-07, `8hald1` E-05). |
| PR-005 | HIGH | IN-SCOPE | C. Architecture; D. Domain invariants | `agy_runipd.py:383`, `:2039`, `:4714`; 4 agy `state.json`; the config's gemini entries | **THE AGY FRAMING IS BACKWARDS, AND THE HONEST GAP IS NARROWER AND FIRMER THAN THE ONE THE PLAN NAMED.** E-04 treated agy as the host lacking model identity. Measured: `DEFAULT_MODEL = "gemini-3.7-flash-high"` is written into `options["model"]` at run creation and is the `--model` default, all 4 agy runs carry it, and one corroborates it in its own session log's `init` event. So agy resolves a CONCRETE model with no profile machinery, and it is OC that records `host-default`. What agy genuinely cannot resolve is a CARD: that model is NOT among the 8 gemini entries in the OpenCode config, and Antigravity's rates live in no file this tool reads, so pointing it at OpenCode's config would attribute one vendor's prices to another host's model. | C:Low; U:Low; S:Medium; F:Medium; Overall:Medium | FIXED | Added F-16. E-04 rewritten: NO agy model work, an explicit not-resolvable-from-any-readable-config card marker, and a prohibition on inventing an Antigravity config reader (which the fence and the Deferred section now also carry). OQ-02 RESOLVED on this measurement with the stronger reason. V-04 requires the config search returning nothing as the justification. |
| PR-006 | HIGH | IN-SCOPE | A. Correctness (wrong schema for the real data) | live config: 82 models, 80 priced, 47 without `cache_read`/`cache_write` | **"THE FOUR COMPONENTS" IS THE WRONG SCHEMA, AND THE PARTIAL CARD IS THE CURRENT MAJORITY RATHER THAN A HISTORICAL ARTIFACT.** The plan's best instruction is the `x0spmh` absent-versus-zero rule, but it framed the card as four components with a missing one as the exception. Measured: 47 of the 80 priced models carry ONLY `input` and `output`, and 2 of 82 have no `cost` block at all. A four-component schema must therefore either invent zeros for 59 percent of the card -- the `x0spmh` trap at scale -- or carry a named unknown per component. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-13. E-02 now records the components PRESENT plus a named unknown per absent one, and cites `aflsz3` E-07's independent `cache_write`-is-always-zero measurement as ITS schema decision rather than a licence to drop an observed key here. The Concern and conventions carry the measured majority; E-05 and V-05 add the partial-card and genuine-zero cases. |
| PR-007 | MEDIUM | IN-SCOPE | Evidence accuracy (contradicts a sibling's better measurement) | `part.state.metadata.model.modelID` in 16 records; `aflsz3` F-5 | **"0 DISTINCT MODEL IDS" IS FALSE AND WOULD PUT TWO PLANS IN THIS REPOSITORY IN CONTRADICTION.** 16 real `modelID` values exist (2 distinct), on `task` TOOL CALLS, naming the SUB-AGENT a `task` spawned rather than the main agent; agy's `init` event carries its model in 2 more files. `aflsz3` F-5 had ALREADY measured exactly this and described it correctly. The claim that matters survives and is now exact: all 32333 cost-bearing `step_finish` parts carry the key set `{id, reason, snapshot, messageID, sessionID, type, tokens, cost}` and no model key. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-11; F-3 marked superseded in its second half. Gap 2 rewritten with the exact key set and an explicit instruction not to repeat the absolute claim. The Deferred section now states why the 16 values do NOT close the per-step gap. |
| PR-008 | MEDIUM | IN-SCOPE | A. Correctness; G. Plan executability (a live hazard recorded as future) | `oc_runipd.py:2680-2700`, `:3029-3032`, `:5427`; 0 of 140 runs | **OQ-01 CALLS `kgpptv` PENDING; IT IS EXECUTED, SO THE TWO-MODEL CASE EXISTS TODAY.** `--verify-with` ships, `resolve_launch_pair` freezes a second launch and refuses a mismatched digest, `launch_profile_record` is called twice, and the argv builder selects `verify_model`/`verify_variant`/`verify_agent` at the verifier call site. Measured, 0 of 140 runs configure a verifier profile, so no existing run has two models -- but the capability is live, and a run-level card and default that silently describe only the executor will misattribute the verifier's cost the first time the feature is used. The plan treated this as a future interaction to "record" rather than a coverage requirement. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | OQ-01 marked `resolved`, with both halves answered from evidence (no host emits a per-step id; `5f2h8i` E-02 owns the per-invocation home and neither supersedes the other) and the corrected `kgpptv` status. E-01/E-02 must now cover the verifier launch when `options.verify_launch_profile` is present or state explicitly that they do not and why; V-01 requires that paste. Under-scope note and Deferred updated. |
| PR-009 | LOW | IN-SCOPE | G. Plan executability (an investigation already answerable) | spec `25kzda` §5.6 preamble, §7; `tests/test_run_flag_surface.py`; `run_evidence.py:1649` | The Spec-sync section asked the executor to determine whether §5.6's per-item JSON or §7's "ledger schema" is a CLOSED shape. Answerable by reading: §5.6 says "Each final item has at LEAST" before the block, an explicit open lower bound, and §7 is a worked EXAMPLE (a table of hypothetical items and a narrated invocation), not a schema. Neither governs `state.json`'s `options`, so no amendment is required and the spec is correctly absent from `Scope-Paths`. Also unmentioned: the OTHER spec-bound-to-code test, `tests/test_run_flag_surface.py`, which fails in both directions on §2.1's flag grammar -- inert for this plan, which adds no flag, but worth naming so a later growth does not trip it. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Spec-sync now states the reading with the quoted preamble, keeps the re-read-at-execution instruction as a guard rather than an open investigation, names the §2.1 flag-grammar hazard with `5f2h8i` E-07 as precedent, and cites the 13-row length assertion behind the §4.2 prohibition. The fence now says outright not to edit `25kzda`. |

CONFIRMED SOUND, AND THE PRODUCER/CONSUMER JUDGEMENT IS THE PLAN'S REAL CONTRIBUTION. This plan's
central act was refusing to graduate almost all of its backlog item, and that judgement is correct
and now independently reproduced. Gap 2 holds exactly (0 of 32333 cost-bearing parts carry a model
key). Gap 3 holds (0 of 140 runs carry any card-shaped key; the card is not in the repository at
all; `sync_provider` demonstrably rewrites it). The container half of gap 1 really did ship with
`3cm15q` and must not be rebuilt (34 of 140 runs carry the full provenance object, 32 of them
reading `host-default`). The three named consumer plans really do declare model and price
deliverables they cannot supply themselves, and `aflsz3` E-08 already REFUSES model comparison at
1.1 percent coverage, which is precisely the gap this plan's forward-only fix narrows. The
`x0spmh` absent-versus-zero rule is the most valuable single instruction in the plan and is now
backed by a current majority rather than one historical incident.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing about the design; the producer/consumer
boundary is right and survives intact. The pattern in the revisions is that this plan reasoned
about mechanisms it had read the DOCSTRINGS of but not CALLED. `models_from_config`'s no-secret
guarantee is real and quoted correctly, but the function answers a different question than E-01
asked; `per_million`'s untrusted-input refusal is real and worth reusing, but its multiplication
points the wrong way for a value read back out of the config; and the card really is four
components in the docstring while being two components in 59 percent of the actual data. The second
theme is that a plan claiming a defect must locate the defect: E-03 protected against a price
substitution that no code path performs, and E-04 assigned a gap to the host that does not have it.
The third is that a sibling Set had already measured two of this plan's own figures more accurately,
which is an argument for reading a sibling's findings table before restating a measurement it
contains.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | E-01 cannot resolve the host default through any existing reader (PR-002). Add an accessor to `oc_models`, widen `Scope-Paths`, or leave the executor to decide? | ADD one narrowly-scoped no-secret accessor to `oc_models`, which is ALREADY in `Scope-Paths`, and require the guarantee in its own docstring plus a read narrowed to the default-model key. | (a) Leave it for the executor; rejected because the plan forbids a second config reader in absolute terms while its named reader cannot answer, so the executor faces a contradiction and the cheap way out is the forbidden one. (b) Point at `agent.<name>.model` or `small_model` as the answer; rejected as not the default in general, though both are now recorded as alternative sources the executor must NAME rather than silently pick. (c) Read the config directly in `oc_runipd`; rejected because that is exactly the second reader the item forbids, and `oc_models` is where the no-secret guarantee is documented and tested. | `models_from_config` (`oc_models.py:301`) reads only `provider.<name>.models.<id>` keys; called in-process it returned 82 catalog ids and no default. The live config's top-level `model` and `small_model` keys hold concrete ids that nothing in the tree reads. The plan's own Scope check already says `oc_models.py` is in scope "to expose or reuse the existing readers if a small accessor is genuinely needed". | yes |
| D-2 | PR-004 shows E-03 targets a substitution that does not exist. Delete the item, or repurpose it? | REPURPOSE it as a stated invariant plus a regression guard that must be shown to FAIL under a deliberate substitution. | (a) Delete E-03; rejected because the property (a config edit cannot reprice a past run) is genuinely the reason the whole record is worth writing, and deleting the item would leave nothing preventing a future change from breaking it silently. (b) Leave it as authored; rejected because an executor hunting a nonexistent substitution is the likeliest route to BUILDING the tokens-times-card recomputation that E-06 and the fence forbid as another plan's deliverable. | `run_viewer.extract_log_metrics` (`:563`) sums `part.cost` from `step_finish` only; the drivers persist that sum; a grep across `agent_workflows/` found no rate multiplication outside `oc_models`' write path. `aflsz3` E-07 owns recomputation and separates recorded from estimated cost. | yes |
| D-3 | PR-005 inverts the agy position. Record a card for agy from the OpenCode config, or record that it cannot be resolved? | RECORD THE NAMED INABILITY, and forbid inventing an Antigravity config reader in this plan. | (a) Price agy from the OpenCode config; rejected as actively wrong -- `gemini-3.7-flash-high` is absent from that config's 8 gemini entries, so the nearest match would attribute an OpenCode provider's rates to an Antigravity model, which is the fabrication the whole plan exists to prevent. (b) Build an Antigravity config reader here; rejected as a new security surface (a second credential-adjacent file reader) opened inside a plan about cost attribution, and now named in the Deferred section as separate work. (c) Say nothing about agy; rejected because its runs would then be indistinguishable from an oc run whose config was unparseable. | `agy_runipd.DEFAULT_MODEL` (`:383`) is `gemini-3.7-flash-high`, written to `options["model"]` at `:2039`; all 4 agy runs carry it. Searching the resolved OpenCode config for that id returns nothing (8 gemini models, closest `google/gemini-3.7-flash`). No Antigravity config reader exists anywhere in `agent_workflows/`. | yes |
| D-4 | PR-008: `kgpptv` shipped the two-model verifier case and 0 of 140 runs use it. Require verifier coverage now, or defer it to the first run that configures one? | REQUIRE E-01/E-02 to cover the verifier launch when `options.verify_launch_profile` is present, OR to state explicitly that they do not and why. | (a) Defer entirely; rejected because the capability is live TODAY, so the first configured verifier run would be silently misattributed, and a record that is wrong on its first real use is worse than one that declines to cover the case. (b) Require full verifier coverage unconditionally; rejected as possibly over-scoping a plan whose item is about the executor's model, so the escape hatch of an explicit stated non-coverage is left open and V-01 accepts either paste. | `resolve_launch_pair` freezes both launches and `launch_profile_record` is called twice (`oc_runipd.py:3029-3032`); the argv builder selects `verify_model`/`verify_variant`/`verify_agent` at the verifier call site (`:5427`); plan `kgpptv` is in `plans/executed/` with `- Status: executed`; measured 0 of 140 runs carry `options.verify_launch_profile`. | yes |
| D-5 | PR-006/PR-007: this plan's figures disagree with sibling `aflsz3`'s on two points. Whose measurement governs? | Take the more precise measurement in each case and CITE the sibling, so the two plans agree rather than contradict. | (a) Keep this plan's numbers; rejected because "0 distinct model ids" is simply false and `aflsz3` F-5 had already characterized the 16 `task`-call values correctly. (b) Defer wholly to the sibling; rejected because on the cost-bearing-step question this plan's framing is the sharper one (the key set of every priced part), and `aflsz3`'s `cache_write` schema decision is ITS choice about a rate column, not a licence to drop an observed key from this record. | `aflsz3` F-5 ("only 16 of 462 session files mention `modelID` ... identifies the SUB-AGENT's model") and F-3/E-07 (cache_write 0 in all 29611 cache-reporting steps); re-measured here as 16 occurrences, 2 distinct values, at `part.state.metadata.model.modelID` on `task` calls, and 32333 cost-bearing parts with a fixed 8-key set. | yes |
