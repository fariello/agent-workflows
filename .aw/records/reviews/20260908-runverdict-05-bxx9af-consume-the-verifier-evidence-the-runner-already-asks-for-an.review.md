# Review: consume the verifier evidence the runner already asks for, child bxx9af (Set runverdict)

- Subject-Id: bxx9af
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `a2b68ac5`. `aw ipd lint --phase author` reported ONE structural finding, `IPD-Q501`
(an unresolved `Blocking: yes` open question, refused at every checkpoint), which PR-E01 resolved;
`--phase review-finalize` CONFORMING after every revision.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value rests on
RE-MEASURING rather than re-reading. Eleven things were RUN rather than inspected: the whole outcome
corpus was re-parsed for field population, predicate pass rate, entry types, dict key-sets and command
and exit key frequencies; the per-outcome command coverage was computed to find outcomes a predicate
would refuse; the longest entry was measured; `v_data` and the ignored fields were grepped
package-wide; both hosts' `write_report` were read and diffed by eye against the divergence note;
every `verification_status` reader was enumerated and the two literal-matching branches read; the real
`integration_is_earned` was CALLED with four tokens across both validation paths; the run tree's
gitignore status and tracked-file count were checked; the spec was grepped for a disposition enum;
`wyw936`, `t74o5q`, `vlf75p`, `rbftpl` and plan `r2i1b1` were re-read on disk; and the bare suite was
run. Five of those eleven produced findings the plan did not contain.

SCOPE: only this child was a candidate. Plans `1bfppy`, `fzxfph`, `w33lrl` and `r2i1b1` were read as
EVIDENCE, not reviewed, and are not in the ledger. Also read: `oc_runipd.py`, `agy_runipd.py`,
`runner_shared.py`, `run_viewer.py`, `render_stream.py`, `tests/test_run_viewer.py`, spec `25kzda`, and
backlog `rbftpl`/`wyw936`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-E01 | BLOCKER | IN-SCOPE | G. Plan executability; C. Architecture (sequencing) | backlog `wyw936` `- Status: graduated`; plan `1bfppy` `- Status: reviewed`, `- Readiness: go-pending-approval`, `- From-Backlog: wyw936`; `aw ipd lint` `IPD-Q501` | **THE BLOCKING QUESTION'S PREMISE HAS EXPIRED, AND WHILE IT STANDS THE PLAN CANNOT EXECUTE AT ALL.** OQ-01 carries `Blocking: yes` on the measured-at-authoring finding that "`wyw936` is `open` with no plan", which made its own RECOMMENDED option (a) impossible and left the maintainer choosing between absorbing a sibling item's scope (forbidden by the graduation contract) and knowingly shipping a residual fail-open. Re-measured: `wyw936` is now `graduated` and OWNED by plan `1bfppy`, which is `reviewed` with `go-pending-approval` and replaces the substring gate with one shared exact-match fail-closed mapping. So option (a) exists. This is also a STRUCTURAL blocker, not only a semantic one: `aw ipd lint` refuses an unresolved `Blocking: yes` at EVERY checkpoint (`IPD-Q501`), so the plan as written could not pass its own author-phase gate, and the question was addressed to a maintainer who had nothing left to decide. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | OQ-01 marked `resolved`, `Blocking: no`, with the measurement and the three options re-adjudicated (b moot, c unnecessary). The recommendation is now DATA: `- Item-Dependencies: executed:1bfppy`, so the runner enforces the edge at dispatch. Verified the edge resolves in a real manifest with no reverse edge. Scope, Deferred, F-8 and the gate all updated; the gate's blocking claim replaced. |
| PR-E02 | BLOCKER | IN-SCOPE | A. Correctness; E. Testing (calibration) | measured: exactly one outcome (`02-ku93tn-verification.json`, run `run-20260826T000928Z-1641359`) has no entry carrying `command`/`cmd` | **THE PLAN REPEATS, ONE LEVEL DOWN, THE EXACT ERROR IT CAUGHT IN THE BACKLOG ITEM.** F-5 is that the item's bar would refuse 24 of 34 genuine verifications. E-02's own replacement bar names the command keys as `command`/`cmd`, and measured against the live corpus that refuses one real outcome whose six entries all put the command under `name` with output under `detail`. That outcome is a `VERIFIED` verdict with six executed commands and eleven evidence entries, so it is precisely the "genuine verification" the calibration exists to protect, and refusing it closes a lane. The plan's own dict-versus-string framing also understates the problem: there are NINE distinct dict key-sets, `rc` (which E-02 lists) appears ZERO times, and one shape names no command at all. | C:Low; U:Low; S:Low; F:Medium-High; Overall:Medium | FIXED | Added F-14. E-01 now enumerates all nine key-sets with counts and requires PER-OUTCOME reporting naming any outcome that would be refused, since an aggregate count is how this was missed. E-02 must accept `name` or test entry VALUES, must write down its command-like-content rule, must state the `rc` tolerance as defensive, and must decide the command-less shape explicitly; its expected outcome is now a ZERO refusal rate over the measured corpus. E-03's fixture must carry all nine key-sets. V-01, V-02 and V-03 updated. |
| PR-E03 | HIGH | IN-SCOPE | C. Architecture; F. UX (one host silently unimproved) | `oc_runipd.py:3179`, `agy_runipd.py:2113`, and the divergence note at `oc_runipd.py:3245-3247` | **THERE ARE TWO `write_report` IMPLEMENTATIONS AND E-06 NAMED ONLY ONE.** They diverge deliberately (different titles, `Verify` versus `Verification` column header) and the in-tree note states that sharing them is FORBIDDEN because it "would silently give BOTH drivers that one's format". So a section added to oc's report does not appear in agy's, and agy is the host whose verifier runs BY DEFAULT and therefore produces most of the evidence the section exists to show. The plan would have delivered its headline operator-facing improvement to the host least likely to need it, and its V-06 would have passed. | C:Low; U:Low; S:Low; F:Medium-High; Overall:Medium | FIXED | Added F-15 and a Step 0 convention. E-06 must render in BOTH or state which host and why the other is deferred, and is explicitly forbidden from "fixing" this by moving `write_report` into `runner_shared`. Scope and V-06 updated; the citations corrected to the by-phrase anchors. |
| PR-E04 | HIGH | IN-SCOPE | F. UX; A. Correctness (a new token that renders as nothing) | `run_viewer.py:1350-1360`, `:1617-1631`; ten `verification_status` readers enumerated | **THE DISTINCT TOKEN OQ-02 PREFERS RENDERS AS A BARE `-` IN `aw runs`, WHICH IS HOW "NO VERIFICATION RAN" RENDERS.** `verification_status` has ten readers; two match on literal values: one badges only `verified`/`failed`, the other maps `verified` to `yes`, the set `(unverified, verify-failed, failed)` to `no`, and EVERYTHING ELSE to `-`. So `unevidenced` would be strictly LESS informative in the operator surface than the imprecise `unverified` it replaces, which inverts the plan's purpose. E-04 said only to "check it against whatever consumes `verification_status`" without naming the consumers or the hazard. | C:Low; U:Low; S:Low; F:Medium-High; Overall:Medium | FIXED | Added F-16 and a Step 0 convention. E-04 now enumerates all ten readers, names the two literal-matching branches, and requires either extending them in the same change (in-fence, both files declared) or reusing an existing token. OQ-02 restated around this real tradeoff with a recommendation. V-04 requires pasting the chosen token's rendering and FAILS a token that renders as `-`. |
| PR-E05 | HIGH | IN-SCOPE | A. Correctness (a guarantee broader than delivered) | called `integration_is_earned` with four tokens on both paths; `oc_runipd.py:3045`, `agy_runipd.py:2047` | **THE FAIL-CLOSED GUARANTEE DOES NOT HOLD WHEN VALIDATION IS OFF, WHICH IS oc's SHIPPED DEFAULT.** Measured: with `validate=True`, any non-`verified` token gives `earned=False signal=verifier-declined`, exactly as intended. But with `validate=False` and a passing suite, `verify_disp='unevidenced'` still gives `earned=True signal=driver-run-suite`, because that branch never reads `verify_disp` at all. E-05 was written as an open "read and report" obligation, so the plan neither predicted this nor bounded its claim; and since oc defaults `validate` False while agy defaults its verifier ON, the effect is host-dependent. The behavior is DEFENSIBLE (no verifier ran, so there is no verdict to honor) but the plan must not claim a guarantee it delivers on only one path. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-17. E-05 converted from an open investigation to a confirm-and-extend with the measured result stated, both host defaults named, and an explicit prohibition on claiming a broader guarantee. Added to Deferred as a recorded non-goal. V-05 now requires CALLING the function on both paths rather than reasoning about it. |
| PR-E06 | MEDIUM | IN-SCOPE | G. Plan executability (an obligation that cannot fire) | grepped the spec for `verification_status`/`verify_disp`: zero matches | The Spec-sync section imposes a conditional duty ("if E-04 introduces a new disposition token, that IS a contract change and the spec path MUST be added to `Scope-Paths` and the amendment justified BEFORE editing") and OQ-02 repeats it. Measured: the spec pins NO verification-disposition enum and never mentions either field, so the condition cannot be met, no amendment is owed, and an executor would spend a pass hunting for an enum that does not exist, or worse, add a spec path to `Scope-Paths` on a false premise. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-18. Spec-sync rewritten to state the obligation does not fire and why, and to redirect the real constraint to the renderer set. OQ-02's spec sub-question marked answered. Added the standing prohibition on editing the §4.2 finding-code table. |
| PR-E07 | MEDIUM | IN-SCOPE | C. Architecture (three plans, one block) | this plan's E-04 versus `1bfppy` E-02 and `fzxfph` E-01 | THREE plans now converge on the same `v_outcome_file` block and the plan assigned ownership loosely. E-04 claims the `except Exception` arm, which `1bfppy` E-02 also rewrites, and refers to the no-outcome-file `else` by backlog id rather than by the plan that owns it (`fzxfph`). Unreconciled, the unparseable case gets fixed twice or one plan's change is reverted by another's. | C:Medium; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | Added F-10 rewritten and a gate paragraph naming all three owners. With the new dependency declared, E-04 must VERIFY the `except` arm is already fail-closed rather than re-fix it, and say so if it is not. V-04 requires `1bfppy`'s status pasted and the provenance of that behavior stated. |
| PR-E08 | MEDIUM | IN-SCOPE | G. Plan executability (stale ownership) | all four backlog items re-read; all four now `graduated` with owning plans | Every sibling is cited by BACKLOG id (`wyw936`, `t74o5q`, `vlf75p`, `rbftpl`) and described as "separately open". Measured: all four are `graduated` and each has an owning plan (`1bfppy`, `fzxfph`, `w33lrl`, and this one). An executor following the plan's prose would look for unowned items and could reasonably conclude the surrounding work is unstarted, which is the opposite of the truth and the direct cause of PR-E01. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Every sibling reference now names the owning PLAN alongside its backlog id, in Scope, Deferred, the conventions and the findings table. |
| PR-E09 | LOW | IN-SCOPE | E. Testing (stale baseline) | bare `python3 -m pytest` at `a2b68ac5` | The baseline claims `1 failed, 5648 passed` with the failure in `test_orchestrator_retirement`. Measured: `1 failed, 5919 passed, 3 skipped, 2 xfailed`, and that module now gives `112 passed`. The real failure is the environmental `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, caused by 189 untracked `opencode-recovery/*.md` files belonging to another party in this shared checkout, which an executor chasing a green baseline could "clean up". | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Baseline replaced with measured figures and the named environmental failure, with node-ids-not-totals stated and an explicit prohibition on touching those untracked files, repeated in the execution contract. |
| PR-E10 | LOW | IN-SCOPE | D. Anti-regression (an unsupported citation) | `tests/test_run_viewer.py` contains no such count; `oc_runipd.py:6660` gives 15 | E-03 and F-12 assert that `tests/test_run_viewer.py:1-30` "documents that 23 tests reading it fail in a fresh checkout". The file documents the HAZARD (at `:1-5` and `:22-27`) but states no count anywhere, and the nearest in-tree figure is 15. The conclusion is right and the citation does not support the number, which is the kind of detail that gets copied forward as fact. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-03 and F-12 now cite the hazard with the real anchors plus the independent gitignore evidence, and explicitly note the 23 figure is unsupported and that no count should be cited. |

CONFIRMED SOUND, AND THE PLAN'S CORE CONTRIBUTION IS ITS BEST FEATURE. F-5 is the reason this plan
exists and it REPRODUCES exactly: the backlog item's proposed bar still passes only 10 outcomes, so
implementing the item literally would refuse 25 of 35 genuine verifications, and E-01's
calibrate-before-enforcing structure is the correct and non-obvious answer. Re-verified besides: `v_data`
is read exactly twice per host and only for `verdict`; `tests_run` and `corrections_made` appear nowhere
in the package but the two prompt strings, so the four ignored fields are ignored by grep as well as by
design; the producing side is unanimously healthy (35 of 35 populate both fields, zero empty); the
bare-string prose really does carry commands and exit codes; `write_report`'s section-not-column
convention is real and stated twice in-tree; the run tree really is gitignored with zero tracked files,
so the fixture rule is right; and `r2i1b1` really does overlap four of five modules with one shared
predicate, so E-07's compose-do-not-fork instruction is warranted. The plan's refusal to normalize
`tests_run` or touch the prompt is also correct: it would invalidate the corpus as a fence.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Two themes, and neither touches the design. FIRST, the
plan was written against a world that had moved: its blocking question was addressed to a maintainer who
no longer had a decision to make, and every sibling it described as unowned now has an owning plan. The
fix converts a prose ordering note into a declared dependency edge, which the runner enforces at
dispatch, and that simultaneously cleared the structural lint refusal. SECOND, and more interesting, the
plan's own central discipline was applied one level shallower than it needed to be: it caught the
backlog item's bar refusing real verifications, then proposed a replacement bar that refuses one, and
its aggregate-count validation would not have noticed. So E-01 now reports per-outcome verdicts and names
any refusal, and E-02's target is a zero refusal rate over the measured corpus. The remaining revisions
are the same pattern in the reporting half: a section added to one of two divergent reports, and a new
status token that renders as nothing in the surface it was invented for.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | OQ-01 was `Blocking: yes` and addressed to the maintainer. Leave it for a human, or resolve it? | Resolve it and encode the answer as a declared dependency (`- Item-Dependencies: executed:1bfppy`), marking the question `resolved` and `Blocking: no`. | (a) Leave it blocking for the maintainer; rejected because the workflow forbids asking a human what the repository already answers, and the question's own recommended option is now simply AVAILABLE, so there is no judgement left. (b) Resolve it as "land independently" and accept the residual hole; rejected because the item's warning is specific and the fix now exists and is review-cleared, so accepting a known fail-open would be gratuitous. (c) Absorb the verdict fix into this plan; rejected because that scope is graduated and owned, so absorbing it is now a collision rather than a takeover. | `wyw936` reads `- Status: graduated`; plan `1bfppy` carries `- From-Backlog: wyw936`, `- Status: reviewed`, `- Readiness: go-pending-approval` and closes both the verdict mapping and the unparseable `except` arm. The declared edge was verified to resolve in a real `build_dynamic_manifest` with no reverse edge, so no cycle. `aw ipd lint`'s `IPD-Q501` refuses an unresolved blocking question at every checkpoint, so leaving it would have blocked execution on a settled question. | yes |
| D-2 | Should this review pick the disposition token for OQ-02, or leave it to the executor? | Leave the choice to the executor, but resolve both of its sub-questions and state the real tradeoff plus a recommendation. | (a) Pick the token here; rejected because the choice is genuinely balanced (precision versus immediate legibility in `aw runs`) and it is the executor's fence, not the reviewer's. (b) Leave the question as written; rejected because it rested on an unanswered spec question that is answerable in one grep, and it did not know the renderer hazard that dominates the decision. | The spec mentions neither `verification_status` nor `verify_disp`, so no enum constrains the token and no amendment is owed. The ten readers were enumerated and the two literal-matching branches read: an unrecognized token renders as `-`, identical to "no verification ran". `integration_is_earned` treats any non-`verified` token identically on the validation-ON path (measured), so integration behavior does not depend on the choice, which is what makes it safely the executor's. | yes |
