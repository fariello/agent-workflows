# Review: classify a run-record versus artifact difference by direction with read git evidence, child zexed1 (Set discclass)

- Subject-Id: zexed1
- Subject-Type: ipd
- Reviewed-At: 2026-09-10
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `37346bbf`. Structural preflight `aw ipd lint --phase author` conformed (clean, 0 findings)
before semantic review. After revisions, `--phase review-finalize` reports exactly one diagnostic, `IPD-Q501`
for the NEW blocking OQ-04, which is the gate working as designed rather than a structural defect; the two
most recent reviews in this repo (`xo3244`, `wfjsp4`) sit in the identical state.

DISCLOSURE: authored in the same repository and by the same model family as the plan, so treat this as a
near-self-review and worth less than an independent one.

THE DIAGNOSIS IS CORRECT AND THE UNBLOCKING INSIGHT IS SOUND. The table really does paint legitimate post-run
progress as a defect; the audit really does read only `actual_file.parent.name` and one `- Status:` regex, so a
legitimate finalize and a hand-edit really are indistinguishable to it; and the 2026-09-05 ruling that killed
the previous direction-only design really is satisfied by reading the in-tree finalize commit. The plan's
insistence that `resolved` require EVIDENCE rather than direction, and that everything unprovable stay a
VISIBLE `unknown`, is the right architecture and I did not weaken it. The authored `F-5` claim that all five
cited id6s have a reachable finalize commit re-measured true.

WHAT REVIEW CHANGED IS THE PLAN'S AIM AND ITS EVIDENCE PREDICATE, both from measurements the author did not
take. The plan was built around four rows. The table carries 508, and the two dominant shapes are the same
false alarm arriving through a route the item never mentions, so the authored forward set would have left
325+ rows red and shipped without meeting the plan's own goal. Separately, the plan's promised anti-regression
was not merely hard but IMPOSSIBLE, and proving that is what produced the one question review would not
answer.

THE BLOCKING QUESTION IS THE HONEST OUTPUT OF THIS REVIEW, not a hedge. Once PR-202 established that the
stranded-lane row is field-identical to 172 legitimate rows, it followed by measurement that this change
quiets it. That is a real cost the plan previously denied. Choosing between accepting it, holding for the
plans that own stranded-lane visibility, and widening this plan to compare lane against main copies is a
risk-appetite decision about missing lost work, which belongs to the maintainer. I raised OQ-04 with
`Blocking: yes` and `Finding: PR-202` rather than resolving it.

Six findings, all FIXED in place, no deferrals. E-item and V-item counts are unchanged at seven each: every
fix belonged inside an existing item, and the two additions the corpus forced (a `retired` class, a time
bound) are refinements of E-03 and E-02 rather than new work.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-201 | HIGH | IN-SCOPE | A. correctness; F. KISS/UX | `aw runs --issues --agent` -> 508 rows; shape counts `('reviewed','executed','executed')` 172, `('queued','executed','executed')` 153, `('integration-blocked','executed','executed')` 4; `oc_runipd.py:2977-2980` | **The plan aims at 4 of 508 rows and its enumerated forward set would leave 325+ still red, missing its own goal.** E-03 as authored gated `resolved` on run-status in `integration-blocked`/`substantially-complete`/`interrupted`. Measured over the whole live corpus, that covers 4 rows (0.8 percent). The two dominant shapes are the SAME false alarm arriving through a route the item never mentions: `initialize_run` writes a queue entry's status as `reviewed` for anything not `to-review`/`draft`/`approved`/`auto-approved`, and `queued` for an item the run never dispatched, so a plan that executed in a LATER run reads as a discrepancy forever. A fix proven only on the `integration-blocked` shape would ship and leave the table exactly as unusable | C:Low; U:Medium; S:Low; F:High; Overall:Medium | FIXED | E-03 now DERIVES the forward direction from "non-terminal run status" and instructs the executor to consume the runner's own `TERMINAL_STATES`/`EXECUTION_SUCCESS_STATES` (`oc_runipd.py:316-337`) rather than hand-list statuses that will drift. Concern block carries the corrected distribution with the `initialize_run` mechanism. E-01 gains a re-measure-the-distribution step; E-06 requires all THREE shapes proven non-red, not just the four rows; E-07 requires a before/after distribution with the residual red count as the only scale evidence. Scope line and Proposed-changes updated. New conventions entries |
| PR-202 | HIGH | IN-SCOPE | D. anti-regression; evidence accuracy | `grep -n "E-item" agent_workflows/run_viewer.py` -> unrelated plan-id comments only; `eulhzt` row measured `reviewed`/`executed`/`executed`; identical to 172 rows | **The plan's central anti-regression requirement is unsatisfiable, and its evidence for it is misattributed.** E-06 required the `eulhzt` row to stay RED, and the Concern credits the table with showing "main's plan copy at 5/8 E-items while its lane copy was finalized at 8/8". This audit reads NO E-item counts and no file content beyond one `- Status:` regex; the E-item comparison was a HUMAN act performed after the table drew attention to the item. Measured, that row is `reviewed`/`executed`/`executed`, byte-identical in every field the audit reads to the 172 legitimate rows PR-201 requires be quieted. So no classifier over these fields can keep it red while quieting them: the requirement is a contradiction, and an executor could satisfy it only by re-reddening the 172 rows the plan exists to fix | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | F-3 marked RETRACTED with the mechanism. Concern block states plainly that the row is not one this audit can earn. E-06's "keep the real find red" case REMOVED and replaced with an instruction to STOP and report if the executor finds itself able to satisfy both, since that would mean an out-of-scope data source was added. Required-tests and V-06 updated. Deferred list gains the lane-comparison approach as a separate plan. Consequence escalated as blocking OQ-04 (below) |
| PR-203 | HIGH | IN-SCOPE | B. security; A. correctness | `tests/test_executed_transition_gate.py:407` (`test_finalize_commit_already_on_head_is_not_evidence`); `executed_transition_gate.py:280`; `oc_runipd.py:6495`, `agy_runipd.py:3562`; 103/143 records and 439/491 attempts carry heads | **"Reachable from HEAD" is the wrong replacement question and re-opens the hole the hook deliberately closes.** The plan repeatedly specifies reachability from HEAD as the evidence test. The hook bounds its search to `HEAD..<incoming>` ON PURPOSE, and its suite pins exactly this: an OLD finalize commit must not authorize a NEW transition. An unbounded viewer check would classify `resolved` a plan that was legitimately finalized long ago, later hand-reverted to `pending/`, and hand-re-`git mv`d to `executed/`, since the old finalize commit is still reachable. That is the same hand-edit bypass the plan's own ruling exists to refuse, arriving one step later. THE BOUND WAS AVAILABLE AND UNNOTICED: run records already store `starting_head`/`ending_head` per attempt | C:Medium; U:Low; S:Medium; F:Medium; Overall:Medium | FIXED | E-02 retitled to require a TIME-BOUND check over `ending_head..HEAD`, mirroring the hook's range, with `tests/test_executed_transition_gate.py:407` cited as the reason and an explicit ban on the unbounded form. Threading `ending_head` to the audit is named as required work (`StepSummary` does not carry it). An absent head resolves to `unknown` with its own reason, never a fallback. E-06 gains a dedicated before-`ending_head` case; required-tests and V-02/V-06 demand it be quoted separately. Spec-sync gains a third obligation requiring the bound to carry its own WHY at the code. Concern block and new F-6b record the measurement |
| PR-205 | HIGH | UNDER-SCOPE | A. correctness; D. anti-regression | 82 rows with artifact in `superseded/` (74) or `not-executed/` (8); six recorded `complete` (`qcqhj7`, `rchpms`, `7p9n2v`, `58ha43`, `2c122z`, `bmh754`), all six carrying `RETIRED` banners; history has 190 `lifecycle(ID): finalize` subjects and ONE `lifecycle(ID): retire` | **The vocabulary has no bucket for a retired artifact, so 82 measured rows would render red forever.** The authored `regressed` rule is "run recorded `executed`/`complete` but the artifact is NOT in `executed/`". Six live rows satisfy it against artifacts in `superseded/`, and every one is a legitimate, banner-carrying retirement (verified by reading each). Worse, they can never be rescued by the `resolved` path either, because retirement writes NO `lifecycle(<id6>): finalize` commit, so the evidence form the plan depends on does not exist for this class. A plan whose stated purpose is removing false reds would have created a permanent 82-row false-red class | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-03 gains a sixth class, `retired`, evidenced by the artifact's own `RETIRED` banner plus status/directory agreement, explicitly NOT by a finalize commit, and rendered non-alarming. `regressed` narrowed to "recorded a success and the artifact is in neither `executed/` nor a retirement directory", with the note that it is then expected EMPTY on the live corpus so an executor does not manufacture a row. Scope line, E-04 styling, E-06 case (g), required-tests, V-03/V-04 and Proposed-changes updated. Spec-sync gains a fourth obligation recording why retirement cannot be evidenced the same way. New F-6c |
| PR-204 | MEDIUM | IN-SCOPE | A. data integrity; C. architecture | `run_viewer.py:2855` (`--json`), `:2899` (`--agent`) both `asdict()` the dataclass; `docs/cli-agent-protocol.md:113` | **The plan says "replace the boolean pair" without noticing two MACHINE consumers publish those fields.** Three of the five predicate sites are emission sites, and two of them serialize the whole dataclass into `aw.agent/v1` records. The documented stability rule makes additive fields backward compatible and any change to record shape or field meaning an `aw.agent/v2` bump. So "replace" as written would break published output that automation reads, in a plan that neither declares nor intends a version bump. The plan's own consumer list also omitted the machine sites entirely | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-01 gains an enumerate-all-five-consumers step naming the three machine sites and requiring an explicit additive-versus-breaking decision, with the additive route named as strongly preferred. E-03 requires the class be ADDED beside retained booleans rather than replacing them. E-05 states the machine sites are not interchangeable with the human ones and demands an explicit statement of whether the emitted row SET changed. E-07 and V-07 require a machine-compatibility proof (three legacy keys still present, class added, nothing removed or renamed, every line still parsing). Scope line, execution contract, deferred list and new F-6d record it |
| PR-206 | MEDIUM | IN-SCOPE | F. KISS and UX | 508 rows measured; selection predicate unchanged at `run_viewer.py:1485` | **The change improves honesty without improving readability, and the plan does not say so.** The classification recolors rows but does not narrow the selection predicate, so an operator who sees 508 red rows today will see 508 rows in five colors. For the plan's stated purpose (stop training operators to skim past the rows that matter) that is a partial fix presented as a complete one, and a 500-row table is skimmed regardless of palette | C:Low; U:Medium; S:Low; F:Low; Overall:Low | FIXED | E-04 now states the consequence plainly and requires two things: a per-class COUNT SUMMARY line above the table so the shape is legible without reading every row, and a stated decision on default-suppressing `resolved`/`retired` (permitted, since both carry evidence) with the measured row count that justified it. `unknown` and `regressed` are explicitly non-suppressible under any flag. Required-tests and V-04 updated; deferred list records that narrowing which rows are CANDIDATES is out of scope because it would change machine output |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|---|---|---|---|---|---|
| D-1 | OQ-02 as authored left "is one git subprocess per row acceptable?" open for the executor to decide. Should review resolve it? | Resolved to NO: one git pass, building an id6-to-finalize-commit map answered in Python. The question asked for a measurement and the measurement was cheap, so leaving it open would have handed the executor a question review could close | Leave it open as authored (rejected: the plan's own rationale said the answer depends on a measurement, and the measurement takes one command); allow per-row calls if "measurably fast enough" (rejected on the measurement) | Measured on this repository at 2885 commits: `git log --format=%s` ~46ms, one `--grep` pass ~50ms, against 508 live rows, so per-row spawning is ~25s for an interactive read-only view | yes |
| D-2 | Should the `resolved` class be shown quietly or suppressed behind a flag (the plan's OQ-01, already marked resolved by the author)? Does PR-206 change that answer? | Kept the author's resolution (quiet by default, flag permitted for `resolved`) and EXTENDED it to `retired`, plus authorized default suppression of both if the measured row count warrants. Left `unknown` and `regressed` non-suppressible | Suppress nothing (rejected: 508 rows stay unreadable); allow suppressing `unknown` too (rejected outright: an invisible confession is indistinguishable from a green OK, which is the ruling the plan exists to honor) | The plan's own OQ-01 rationale plus the 508-row measurement; both `resolved` and `retired` carry evidence, so hiding them loses nothing a reader needs | yes |
| D-3 | The plan's `Item-Dependencies` is `none` while its own OQ-03 argues `6ltz1y` should land first. Should review add a hard dependency edge? | No. Left `Item-Dependencies: none` and left OQ-03 open to the maintainer, correcting only the factual error that `r2i1b1` is `approved` rather than `reviewed` | Add an `executed:6ltz1y` edge (rejected: `6ltz1y` is `to-review`, so the edge would sit `dependency-blocked` indefinitely, which the plan's own rationale already reasons correctly); reorder the Set (rejected: not a reviewer's call) | The plan's OQ-03 reasoning verified sound; `6ltz1y` status read as `to-review` and `r2i1b1` as `approved` at review | yes |
| D-4 | The `nna8yz` row is a first-match-wins mis-resolution (`find_artifact_file` returns the first `id6 in p.name`, matching plan `3i0aaz` whose slug contains `nna8yz`). Fix it here? | No. Required E-01 to REPORT it and E-03 to classify an unattributable row as `unknown` rather than `regressed`, which contains the harm without doing another plan's work | Fix the lookup here (rejected: `6ltz1y` E-03 explicitly owns replacing it with `selectors`, and doing it here would collide with an unexecuted sibling); ignore it (rejected: under the narrowed `regressed` rule it would be the ONLY red row on the live corpus, i.e. the table's single alarm would be an artifact of the lookup) | `6ltz1y` E-03 scope read directly; row measured at review | yes |
