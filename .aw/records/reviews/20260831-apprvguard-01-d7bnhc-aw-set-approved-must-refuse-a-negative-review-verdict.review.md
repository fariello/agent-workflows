# Review: aw set approved must refuse a plan whose newest review verdict is negative

- Plan-Id: d7bnhc
- Reviewed-At: 2026-09-03
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED

## Round 1

All claims verified at HEAD `25d3f0b050a2fbe81d532e59ceef70066c0a63d8`, working tree clean, target plan
committed and unchanged, so the pre-review snapshot was correctly skipped per Step 1. Structural
preflight `aw ipd lint --phase author` reported `conforming` before review and
`--phase review-finalize` reported `conforming` after the revisions.

THE PLAN'S THESIS IS CORRECT AND STILL LIVE, which is the first thing to establish for a plan authored
three days ago in a repo that moves hourly. I re-read `validate_transition_allowed` in full: it
validates exactly three things (status validity, the spec transition plus `--by-human`, and the
backlog `blocked` gate), nothing plan-specific exists, and `tests/test_status_set.py:471` still
asserts that `draft -> approved` returns 0. `specs.run_set` is still a second, forked approval surface
with the dual dispatch documented at `specs.py:632-634`. And `--allow-open-questions` greps to ZERO
hits package-wide, so the override genuinely does not exist. The 2026-08-30 incident it was written
to prevent remains preventable only by this work.

What HAS changed is everything underneath it, and that is the dominant finding. On 2026-09-02, plan
`97df1z` shipped `agent_workflows/plan_readiness.py` in commit `209227d5`: the module this plan's E-01
was written to CREATE. That module already contains `extract_newest_history_entry` with both cited bugs
fixed, `oc_runipd.extract_last_history_entry` has been deleted from both runners (two tests now assert
its absence), `tests/test_plan_readiness.py` already ships 30+ tests covering several cases E-08
specifies, and a structured `- Readiness:` field now exists that the shipped predicate treats as
AUTHORITATIVE over prose. Three of this plan's nine E-items needed re-scoping from CREATE to EXTEND.
Executed literally, E-01 would have recreated shipped code and E-02/E-03 would have added a SECOND
verdict reader disagreeing with the shipped one about the same plan.

A second, systematic finding: not one of this plan's `status_set.py` line anchors still points where it
did, and one of them (E-05's insertion point) now names an EXISTING gate rather than the gap before it,
so following the number instead of the symbol would place the new gate on the wrong side of the backlog
check. Every citation was re-measured and corrected.

Verified TRUE and unchanged: the `return (False, message)` refusal convention with no exception class
anywhere in the module; the pre-flight loop's batch atomicity; `review_findings.is_gating` as the single
severity comparison with its explicit anti-fork guard test; absent-review-means-silent; F-5's discriminator
argument (all three of `6lu3rq`/`m73aet`/`wlxkoz` do merely NARRATE a predecessor's REJECT, so a naive
grep would falsely refuse them - the reasoning that makes E-03 necessary is sound); F-6's observation
that `- Verdict:` is parsed but has no enum and no reader; and F-7's `_UNFIXED_DECISIONS` exclusion of
`replan`, which is what makes the verdict half genuinely new work.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-401 | HIGH | OVER-SCOPE | A. Correctness / C. Architecture (duplicate path) | `git log --oneline -- agent_workflows/plan_readiness.py` -> `209227d5`; `plan_readiness.py:94-113` (`extract_newest_history_entry`, bounded via `attention._history_section_lines`, returns the FIRST record); `tests/test_oc_runipd.py:1379-1380` and `tests/test_agy_runipd_cli.py:737` assert `extract_last_history_entry` is GONE; `tests/test_plan_readiness.py` 472 lines with `test_section_is_bounded_at_the_next_h2`, `test_newest_first_selection`, `test_characterizes_the_old_broken_behavior` | THE PLAN'S FOUNDATION LANDED UNDERNEATH IT. E-01 says to CREATE `plan_readiness.py` with `extract_newest_history_entry`, fixing two named bugs in `oc_runipd.extract_last_history_entry`. All of that is done: the module exists, the function exists and fixes both bugs (reusing existing bounding and record-grammar helpers rather than hand-rolling a fourth parser), the broken function has been deleted from BOTH runners, and E-08's test file already ships with several of its specified cases. An executor following E-01 literally would recreate shipped code or, worse, add a parallel implementation. The plan's F-2/F-3 evidence now describes a state that no longer exists, and F-3's "safe only by accident" framing has inverted: `is_plan_review_approved` now returns True for 5 of 30 pending plans (measured), where F-3 claimed 0 of 26. | C:Low; U:Low; S:Low; F:Medium; Overall:Low (re-scoping three items from create to extend; the plan's thesis and its other six items are untouched) | FIXED | E-01 re-scoped to READ-AND-VERIFY, with "nothing was needed, and here is the proof" declared a PASSING outcome so an executor is not pressured to manufacture a change; E-08 re-scoped to EXTEND the existing test file with an instruction to read it first and add only genuinely new cases; F-2 and F-3 relabelled HISTORICAL with their current measurements; F-9 added recording the shift and its commit; the Deferred entry about the runner twins updated, since half of it is now resolved by someone else. V-01 now requires the already-exists evidence FIRST. |
| PR-402 | HIGH | IN-SCOPE | A. Correctness / C. Architecture (two gates, one question) | `plan_readiness.py:189-219` (`is_plan_review_approved`: valid field authoritative, absent -> prose fallback, out-of-vocab -> refuse outright); `:71`/`:77`/`:80` (three shipped vocabulary regexes); `ipd_schema.read_readiness`, `READINESS_APPROVABLE`; plan-review workflow's `Readiness` field requirement; measured: 1 of 30 pending plans carries the field (`eulhzt`) | E-02/E-03 WOULD BUILD A SECOND, DISAGREEING VERDICT READER. Since this plan was authored, the structured `- Readiness:` field became a required review output and the shipped predicate treats it as authoritative over prose, with a deliberate THREE-WAY rule: valid field wins; absent field falls back to prose; out-of-vocab value REFUSES OUTRIGHT rather than falling back (reasoned in the module as "absence means no signal, a bad value means the signal is corrupt"). The plan's `newest_verdict` is purely prose-based and its `VERDICTS` mapping duplicates vocabulary the module already encodes in three regexes. Two independent encodings of one vocabulary, and two different decision orders, means two gates can give opposite answers for the same plan - the precise failure this plan's own F-8 identifies as the reason a shared predicate is needed. Measured aggravating factor: only 1 of 30 pending plans carries the field, so the prose path is still the common one and BOTH halves must be right. | C:Medium; U:Low; S:Low; F:High -> reduced to Medium once the ordering is specified; Overall:Medium | FIXED | E-03 now requires field-first ordering matching `is_plan_review_approved`'s three-way rule exactly, with V-03 demanding it be demonstrated on a valid-field, an absent-field and a corrupt-field input and shown to AGREE with the shipped predicate. E-02 now requires `VERDICTS` to either express or replace-and-rewire the three shipped regexes, with V-02 requiring proof that exactly one encoding remains. E-04 now requires reuse of the shipped `has_unresolved_blocking_question` rather than a third copy, and that the strictness difference against `ipd_lint.py:683` be chosen deliberately and stated. |
| PR-403 | MEDIUM | IN-SCOPE | A. Correctness (citation accuracy with an executable consequence) | Re-read at HEAD `25d3f0b0`: `:502` is now `if rec.record_type == "backlog" and norm_status == "blocked":`, the spec refusal is `:502-505`, the backlog gate `:507`; history `insert` at `:799` not `:791`; `hist_entry` composition at `:793` not `:523`; `- Approval:` handling `:689-733` not `:688`/`:700`; pre-flight loop `:1225` not `:1217`; `aw set --by-human` at `cli.py:2741` not `:2692` | EVERY `status_set.py` ANCHOR HAS DRIFTED, and one drift is not merely stale but ACTIVELY MISLEADING. E-05 says to insert the new gate "at `status_set.py:502`, immediately after the specs block and before the backlog `blocked` block". Line 502 is now INSIDE the spec refusal, and the backlog block it should precede is at `:507`. An executor following the number would place the plan gate on the wrong side of an existing gate. The other citations are ordinary staleness (the substance behind each survives), but this plan is unusually citation-dense and presents its anchors as measured starting points, so a wrong number costs exactly the rediscovery it meant to save. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Every citation corrected and re-anchored BY SYMBOL rather than by line. E-05 now names the anchor as "the gap between the spec `--by-human` refusal and `if rec.record_type == "backlog"...`" with the current lines given as orientation only. The execution contract now states explicitly that every anchor in the plan has drifted and that E-05's in particular must be located by symbol. Recorded as F-10. |
| PR-404 | MEDIUM | IN-SCOPE | A. Correctness (citation accuracy) | `check_engine._UNFIXED_DECISIONS = ("open", "deferred")` is at `check_engine.py:2627`, consumed at `:2786`; plan cites `:2514`/`:2509-2514` | F-7's citation has drifted by ~113 lines. Its SUBSTANCE is verified and correct (the tuple still excludes `replan`, which is exactly the decision a `REJECT - NEEDS REPLAN` review records, so the verdict half of this gate is genuinely new work). Worth correcting because F-7 is the finding that justifies building anything at all rather than rewiring an existing gate, so a reader who cannot locate it may doubt the justification. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | F-7 corrected to `:2627` with its consumption site, and marked substance-re-verified. |
| PR-405 | MEDIUM | IN-SCOPE | E. Testing and verification | Live bare `python3 -m pytest` reports `4096 passed, 3 skipped, 4 xfailed`; plan's Required-tests and V-09 both name `3864 passed, 3 skipped, 4 xfailed` | THE SUITE BASELINE IS STALE BY ~6 PERCENT, and V-09 requires reconciling against it "with any delta explained change-by-change". Following that instruction would force the executor to explain 232 tests that other plans added, which is impossible and invites either a false explanation or an abandoned check. This repo has already been bitten by exactly this: `0soncw`'s history records a baseline stale by 42 percent and its correction established the rule that the invariant is parity against YOUR OWN pre-change measurement, never a number written in a plan. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Required tests and V-09 now require a self-measured pre-change baseline and explicitly forbid comparing against `3864`, with the reason stated (a stale comparison either manufactures a phantom regression or hides a real one). Recorded as F-11. |
| PR-406 | LOW | IN-SCOPE | A. Correctness (citation accuracy) | The verdict vocabulary is at `.aw/system/workflows/plan-review/plan-review.md:523-529` and the readiness vocabulary at `:531-546`; plan cites `:487-493` and `:495-507` | E-02's entire job is to transcribe two documented vocabularies into a closed mapping, and both citations point to the wrong region of a file that has since grown. Low severity because the vocabularies themselves are unchanged and findable, but a transcription task whose source citation is wrong is one where the executor may transcribe from memory instead. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-02 and F-4 both re-cited to `:523-529` and `:531-546`, with the correction marked. F-4's stale `oc_runipd.py:536` anchor also re-pointed to the shipped `plan_readiness._NEGATIVE_READINESS_RE`, which already keeps `CONDITIONAL-GO` negative with a comment - so that half of the finding is already discharged in code. |
| PR-407 | LOW | IN-SCOPE | E. Testing (fixture validity) | `ls .aw/records/plans/pending/*.ipd.md \| wc -l` -> 30, not 26; `mjx7ne`'s OQ-03 now reads `- Status: resolved`; `has_unresolved_blocking_question` over all 30 pending plans -> 0 | THREE FIXTURES THIS PLAN NAMES HAVE EXPIRED. V-01 asserts "all 26" pending plans; there are 30. V-04 and E-04 use `mjx7ne`/`OQ-03` as the live blocking-open-question case, but that question was resolved on 2026-09-01, and NO pending plan currently carries an unresolved blocking question - so the central case for the override half of this gate has no live subject. An executor would find the fixture inert and might conclude the code path is unreachable. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-04 and V-04 now require a CONSTRUCTED fixture with the reason given (a fixture cannot be invalidated by someone answering a question, which is strictly more robust than the live plan the item originally named); V-01 now requires the pending count be re-derived rather than trusting either 26 or 30; E-03's `6lu3rq`/`m73aet` fixtures re-pointed since both are now in `executed/`. Recorded as F-11. |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Given PR-401, is this plan SUPERSEDED by `97df1z` rather than merely stale? | NO. It is stale in three items and sound in its thesis; re-scope those three and keep the plan. | (a) Retire it as superseded. Rejected on measurement: `97df1z` built the PREDICATE and its history reader, but changed NOTHING about the approval path - `validate_transition_allowed` still has zero verdict awareness, `specs.run_set` is still a second ungated surface, and `--allow-open-questions` still greps to zero. The incident this plan prevents is still fully unprevented. (b) Rewrite it as a new plan against the current tree. Rejected: six of nine E-items and all eight findings' substance are unaffected, so a rewrite would discard correct work and lose the recorded incident analysis. | `status_set.validate_transition_allowed:449-511` read in full at this HEAD; `specs.py:498` and `:632-634`; `rg "allow.open.questions"` -> 0 hits; `git log -- agent_workflows/plan_readiness.py` -> single commit `209227d5` touching no approval surface | yes |
| D-2 | E-05 gates `norm_status == "approved"`. Should the review require it to gate `auto-approved` too, decide it, or raise it? | RAISE IT IN THE PLAN as a decision the executor must make and record, rather than deciding it myself. | (a) Mandate gating both. Rejected as not mine to impose: `auto-approved` is a deliberately distinct tier whose semantics (`97df1z` OQ-02, maintainer-resolved) are "an automated clear, NOT human approval", and whether an automated tier should consult a REVIEW verdict is a policy call with its own maintainer history, including a named follow-up (`rxya25`) for per-transition automation policy. (b) Say nothing. Rejected: both strings are in `READY_TO_EXECUTE`, so a gate on one leaves the other ungated at the setter, and silence would look like the plan had considered it. | `status_set.TYPE_STATUSES["plans"]:42-43`; `ipd_schema.READY_TO_EXECUTE:267` and `APPROVAL_STATUSES:264`; `oc_runipd.set_plan_approved:534-574` shelling out to `aw set auto-approved`; the caller-side guards at `oc_runipd.py:2954` and `:5477` | yes |
| D-3 | The plan had no explicit scope fence (only an execution contract). Add one, or treat the contract as sufficient? | ADD a fence, deriving it from the declared `Scope-Paths` and the existing prohibitions. | (a) Treat the execution contract as sufficient. Rejected: the plan-review workflow requires the gate to carry a scope fence as a DECLARATION so the runner can tell afterwards whether an out-of-scope file was edited, and `aw ipd finalize` enforces exactly that via `--scope-reason`/`--scope-ack`; an absent fence leaves the finalize gate nothing to check against. (b) Write a fence with a STOP-and-report clause for out-of-scope edits. Rejected deliberately: the 2026-09-01 maintainer ruling states a fence declares and does not halt, and that flagging or adding such a clause is itself a defect. | The plan-review workflow's execution-contract and scope-fence-wording rules; `aw ipd finalize`'s `--scope-reason`/`--scope-ack` enforcement; the measured `cli.py` overlap with 8 other unexecuted plans (2 approved) | yes |
