# Review: verify the whole Set against both measured incidents and record the residuals, child 3v7wo6 (Set integpath)

- Subject-Id: 3v7wo6
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `37df816`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value therefore
rests on RUNNING the plan's own instructions rather than re-reading them. Five things were executed
rather than inspected: the walkthrough name E-02 mandated was CONSTRUCTED in a scratch repository and
put through `check_collisions`; the lane-branch assertion V-01 mandated was RUN in this checkout; the
`--allow-dirty-base` grep E-01 mandated was RUN; both cited suite-collection counts were RE-MEASURED;
and the full bare suite was run to establish the real baseline. Four of those five produced findings,
and two of them (F-7, F-8) are cases where following this plan LITERALLY produces a failing or
forbidden outcome, which is exactly what a read-only review would have missed.

SCOPE: only this child was a candidate. Read as evidence: `ipd_lifecycle.py` (`ROLLUP_OMITTED_GATES`
:2076, `retire_orchestrator` :2187, `_clear_finalize_journal` :378/:2649/:2829), `check_engine.py`
(:101, `_check_identity_slots` :922), `doctor.py:787`, `artifact_core.py:156`, `artifact_naming.py`,
DECISIONS.md D140, `.aw/records/walkthroughs/README.md`, the four sibling `integpath` children, the
orchestrator `cczotj`, the rollup-retired `84j8d7`, backlog `p8ni63`/`5wdoze`/`yocdq4`/`rnl3b7`/
`h1ksy6`/`a8eufb`/`phawyy`/`hyx1dg`/`yf9fj9`/`nuanaw`, plans `3i0aaz`/`daexj1`/`ys1dor`/`32ij2j`/
`xtklpd`, and `tests/test_orchestrator_retirement.py`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | HIGH | IN-SCOPE | A. Correctness (a mandated name that fails a repository invariant) | reproduced in a scratch repo via `check_engine.check_collisions`; `check_engine.py:101`, `_check_identity_slots:922`; `doctor.py:787`; DECISIONS.md:2465; `.aw/records/walkthroughs/README.md:5` | **THE WALKTHROUGH FILENAME E-02 MANDATED VIOLATES D140 AND FAILS `aw check`.** The plan instructed, in an emphatic do-not-deviate sentence ("do NOT leave the artifact's type or path to the executor's judgement"), a path of the form `...-integpath-05-3v7wo6-<slug>-walkthrough.md`. That puts THIS PLAN's id6 in the WALKTHROUGH's own filename identity slot, which is precisely the `p7dqwz` reuse D140 exists to forbid: the identity slot is the file's OWN identity, and a reference to a documented plan must be the typed `- Target-Id:` field instead. CONSTRUCTED AND MEASURED rather than argued: a walkthrough so named, sitting beside this plan, yields `check.id6-identity-slot` -> `filename identity-slot id6 3v7wo6 is another file's identity (declared by ...3v7wo6-verify...ipd.md); this file declares no Id`, an `error`-severity rule. The plan's most forceful instruction therefore produced a record that fails the repository's own consistency check. Two aggravating details: the mandated `-walkthrough.md` suffix is the LEGACY form (canonical is the `.walkthrough.md` facet), and all three conforming walkthroughs in the directory already demonstrate the right shape with `- Target-Id:`. Also verified the SUFFIX half is NOT linted (`check_names` on a legacy-suffix name with a foreign slot id6 returned nothing), so only the identity half fails closed. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-7. E-02 now requires minting a FRESH id6, the canonical `.walkthrough.md` facet, and `- Target-Id: 3v7wo6`, and states the old instruction was wrong so it is not restored. V-02 requires pasting the `Target-Id` line plus a clean identity-slot `aw check`. Four Step 0 conventions record the canonical grammar, D140 and its enforcement sites, the optional-in-general exception, and that no `aw walkthrough` verb exists. The gate and step 5 carry it. |
| PR-002 | HIGH | IN-SCOPE | E. Testing (a validation that cannot pass as written without a forbidden act) | `git branch --list 'aw/lane/mm6wuz*'` -> `aw/lane/mm6wuz`, `_attempt2`, `_attempt3`; 29 `aw/lane/*` branches total; children 03/04 E-07; `nuanaw` history | **V-01's `NO _attempt2` ASSERTION FAILS ON PRE-EXISTING HISTORY, AND THE ONLY LITERAL WAY TO SATISFY IT IS FORBIDDEN.** E-01 and V-01 both demand that `git branch --list 'aw/lane/<id6>*'` show no `_attempt2` for the resumed item, while naming the item as `mm6wuz`. Measured in this checkout: all THREE of `aw/lane/mm6wuz`, `_attempt2` and `_attempt3` exist, among 29 lane branches with five `_attempt*` clusters. So an executor reading the plan literally sees the criterion fail regardless of how well children 03 and 04 work, and the shortcut that makes it pass is deleting another party's preserved lanes in a SHARED CHECKOUT, which AGENTS.md forbids and which `nuanaw`/`ys1dor` depend on as live evidence. The plan never said the reproduction is SYNTHETIC, even though both implementing children say so explicitly in their own E-07 ("Reconstruct its shape synthetically in a throwaway repository, since the original lanes are gone"). This is the one finding whose remediation risk touches functionality: a wrong resolution here destroys work. | C:Low; U:Low; S:Medium; F:Medium; Overall:Low | FIXED | Added F-8. E-01 now states the check runs INSIDE the synthetic repository against the SYNTHETIC id6 and never against `mm6wuz` here, quotes the measured three branches, and forbids deleting them. V-01 requires naming the synthetic id6 and the throwaway path, and declares an assertion run against `mm6wuz` here a FAILED validation. Step 0 records the 29 branches and the five clusters, plus that both children reproduce synthetically. The gate carries a do-not-delete-a-lane paragraph beside the do-not-weaken-a-refusal rule. |
| PR-003 | MEDIUM | IN-SCOPE | D. Anti-regression (a named baseline that is wrong) | `python3 -m pytest` -> `1 failed, 5919 passed, 3 skipped, 2 xfailed`; `python3 -m pytest tests/test_orchestrator_retirement.py` -> `112 passed`; `git check-ignore -v opencode-recovery` -> `.gitignore:49`; re-run with the dir moved aside -> `53 passed` | **THE PRE-EXISTING FAILURE THE PLAN NAMES DOES NOT EXIST, AND A WRONG-NAMED BASELINE IS WORSE THAN AN UNNAMED ONE.** F-4 and the required-tests section both instruct the executor to name and exclude a `test_orchestrator_retirement` failure inherited from `cczotj`. Measured: that file is fully GREEN at `112 passed`. The actual single bare-run failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, and it is ENVIRONMENTAL rather than a defect: it enumerates files carrying the reporting-contract prose and trips on 189 files in the gitignored, untracked local `opencode-recovery/` dump. Proven causal, not inferred: moving that directory aside turns the file green (`53 passed`). The harm is specific: an executor told to expect a retirement failure could excuse a REAL regression there as the known one, while being surprised by a reporting-contract failure that is not its fault and is machine-specific. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-6 with both measurements and the causal proof; F-4 rewritten to say the criterion is an empty delta and that its own named failure was wrong. Required-tests now carries the measured figures, orders a self-measured baseline compared by NODE ID, and states both numbers are machine-dependent. V-01 requires the summary line TWICE (before and after). |
| PR-004 | MEDIUM | UNDER-SCOPE | E. Testing (the bare run cannot see the surface most at risk) | `pyproject.toml` `addopts` `-m 'not slow'`; child 04 E-07's slow-suite paragraph; child 04 Step 0 F-12 | **VALIDATION DEMANDED ONLY THE BARE SUITE, WHICH DESELECTS THE TWO SUITES THAT WOULD CATCH WHAT CHILDREN 03 AND 04 ADD.** Child 03 registers `--integration-retry-limit` and `--on-integration-blocked`; child 04 adds an `integrate` verb, a cli leaf and a driver subcommand. `tests/test_cli_conformance_matrix.py` and `tests/test_command_surface_declarations.py` are both `pytest.mark.slow` and `addopts` carries `-m 'not slow'`, so a bare `N passed` is silent about the declaration surface. Child 04's own gate already recognizes this and requires the `-m ''` run plus names the pre-existing five-leaf `test_zero_undeclared_parser_leaves` failure; this child, which is the SET's whole-Set evidence, asked for less than its own children do. A whole-Set verification that cannot see a class of regression its children introduce is not verifying the Set's claim. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-9. Required-tests now mandates `make test-all` (or `-m ''`) for both files, pasted SEPARATELY, with only GROWTH of the undeclared-leaf set counting as a regression, and adds the real-checkout rule for `tests/test_run_viewer.py`. V-01 requires that separate paste and fails a lone bare summary. Step 0 records why the bare run cannot see them. Step 4 of the ordered changes carries it. |
| PR-005 | MEDIUM | IN-SCOPE | G. Plan executability (stale citations in the section whose whole job is legibility) | `32ij2j` and `xtklpd` both `- Status: superseded` under `superseded/`; `daexj1` and `ys1dor` both `reviewed` under `pending/`; `hyx1dg` at `backlog/graduated/`; `yf9fj9` at `backlog/done/`; `p8ni63` `graduated` -> `3i0aaz` | **THE BOUNDARY SECTION'S OWN CITATIONS ARE STALE, WHICH IS SELF-DEFEATING FOR AN ITEM THAT EXISTS TO MAKE BOUNDARIES LEGIBLE.** E-02 tells the executor to re-read current statuses ("several moved after the orchestrator was authored") and then hands it four citations of which several HAD already moved, unmarked. Measured: `32ij2j` and `xtklpd`, cited as the `integearn` sibling pair, are BOTH superseded, replaced by `daexj1` and `ys1dor`; and `hyx1dg` and `yf9fj9` are BACKLOG items, not plans, so describing them alongside a Set of plans mischaracterizes them. Separately, `p8ni63` is described twice as "still OPEN backlog" and is in fact `graduated` to `3i0aaz`, whose E-04 adds the very flag E-01 probes for. The residuals section's `phawyy`-is-RETRACTED and `76gsmv`-is-ANSWERED instructions were both re-verified and are CORRECT as written. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-02's sibling paragraph now records the superseded pair with its successors, marks `hyx1dg`/`yf9fj9` as backlog items, and keeps the re-read-yourself rule with a reason. E-01 and the deferred section carry the `p8ni63` correction with `3i0aaz` named. F-5 gained the measured dependency state (01/02 executed, 03/04 reviewed, so this child is currently `dependency-blocked` by design). |
| PR-006 | LOW | IN-SCOPE | E. Testing (a probe stated as prose rather than a command) | `grep -rn "allow-dirty-base\|allow_dirty_base" agent_workflows/` returns nothing at review HEAD; `3i0aaz` E-04 and its F-5 | E-01 said to "check whether `--allow-dirty-base` exists in either runner" without giving the command, so two executors could check differently and one could conclude "landed" from a plan that merely PLANS the flag. Ran the exact grep `3i0aaz` itself uses: it returns nothing, so ABSENT is the expected branch, and the flag is owed by `3i0aaz` E-04 (`to-review`). Naming the command makes the answer reproducible and makes the expected branch explicit, which matters because the plan already (correctly) refuses to let the gate's absence count as this Set's failure. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-01 now names the exact grep and records the measured empty result with `3i0aaz` as the owing plan, while keeping the re-run-it-yourself instruction. V-01 requires pasting that grep's output as the determination. Step 0 records the same. |
| PR-007 | LOW | IN-SCOPE | E. Testing (measured figures used as if durable) | `--collect-only`: `tests/test_oc_runipd.py: 178`, `tests/test_agy_runipd_cli.py: 60` | The host-asymmetry argument is correct and load-bearing, but its cited counts (148 versus 45, "measured 2026-09-07") are already wrong two days later: they are 178 versus 60. Nothing depends on the exact ratio, so this is not a criterion failure, but a plan that hands an executor a stale number invites it to be re-pasted as evidence. The ASYMMETRY is the durable claim and survives unchanged. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-01 and F-3 now carry the re-measured 178/60 with the instruction to measure your own and treat neither pair as a criterion; the asymmetry argument is unchanged. |
| PR-008 | LOW | IN-SCOPE | G. Plan executability (a scope fence broader than the work, and a wrong closure claim) | `- Scope-Paths:` before the fix; `rnl3b7` `done`, `5wdoze` `graduated`, `yocdq4` `graduated`; the four children's `- From-Backlog:` lines | Two small statement defects. FIRST, `Scope-Paths` declared the whole `.aw/records/plans/pending` directory when the only plan file this child touches is its own, which is lazily broad for a child that authors no product code (the precedent `zpbx7o` names its single plan file plus the walkthroughs dir). SECOND, the scope check said children 01 through 04 close "`rnl3b7`, `5wdoze`, and `yocdq4` respectively", which cannot be respective: three items map to four children, and `5wdoze` is the `From-Backlog` of BOTH 02 and 03 while `rnl3b7` is already `done`. The closure claim about THIS child (it closes nothing) is correct and unchanged. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | `Scope-Paths` narrowed to `.aw/records/walkthroughs/` plus this plan's own file, with a justification line added to the scope check. The under-scope sentence rewritten with each item's measured status and its carrying child, dropping "respectively". |

CONFIRMED SOUND, AND THE STRUCTURAL ARGUMENT FOR THIS PLAN'S EXISTENCE IS CORRECT IN EVERY PART. The
premise was verified at the code rather than trusted, and it holds exactly as stated:
`ROLLUP_OMITTED_GATES['pre-transition-ev-checkpoint']` exists (`ipd_lifecycle.py:2076-2088`) and its own
docstring gives the same reason the plan gives, including the sentence that the E/V requirement is
"untouched for CHILD plans, which this route refuses outright"; `retire_orchestrator` does refuse a
`Kind: child` plan (`:2210-2211`); and the measured consequence is real, with `84j8d7` sitting in
`executed/` today carrying `- Kind: orchestrator`, `- Status: executed`, `Execution state: pending` on
its E-01 and `Result: pending` on its V-01. So F-1 and the Concern's "it cannot live on the
orchestrator" argument are established, not asserted, and the plan's shape as a child is right.

F-2 is right and is the reason this child is worth its cost: the four mechanisms are separately tested
and the prevented failure is their interaction. F-3's asymmetry claim is right (only its numbers were
stale). The `76gsmv` instruction was checked against child 01's actual F-12/F-13 and OQ-01 and is
faithful, including the journal-lifetime cause at `ipd_lifecycle.py:2649` (verified: `_clear_finalize_journal`
is called on completion at `:2829` and on the rollback/stale paths at `:2615`/`:2630`/`:2680`) and the
four identical `R06x` diff shapes; the plan is also right to forbid restating the rename explanation.
`phawyy` really is `- Status: parked` with a 2026-09-07 history line beginning "RETRACTED: the premise
is false", so warning against citing it as live work is correct. `h1ksy6` (`graduated`) and `a8eufb`
(`graduated`, narrowed at graduation to step 1 only) are correctly characterized as residuals. OQ-01 is
legitimately `resolved` and needed no reopening. The plan carries no `- Readiness:` field of its own
authoring, correctly leaving that to this review.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing about WHAT it verifies, and a great deal about
whether its instructions can actually be FOLLOWED. The pattern across the two HIGH findings is the
same and worth naming: this is a plan about producing trustworthy evidence, and in two places its most
emphatic instructions would have produced UNtrustworthy evidence: a walkthrough that fails `aw check`,
and a lane assertion whose literal reading fails on history and whose shortcut destroys a co-worker's
work. Both are now specific about the synthetic repository and the minted id6. The second pattern is
staleness: five of the eight findings are cases where a figure or status the plan hands the executor had
already moved (a named baseline failure that no longer exists, two superseded plans, a graduated backlog
item described as open, two collection counts), which for a verification plan matters more than usual,
because a stale expected-failure is exactly what lets a real regression be excused.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | The mandated walkthrough name violates D140. Should the fix mint a new id6 for the walkthrough, or drop the identity slot by using a legacy-style name with no id6? | Mint a FRESH id6, use the canonical `.walkthrough.md` facet, and link the plan with `- Target-Id: 3v7wo6`. | (a) A legacy `YYYYMMDD-<slug>-walkthrough.md` name with no identity slot at all; rejected because the slot-less legacy form is explicitly the OLD grammar the README keeps only for reading, and choosing it to dodge a rule would leave the newest record less conformant than the three that already do it right. (b) Keeping `3v7wo6` in the slot and adding a `- Id: 3v7wo6` to the walkthrough so rule (a) compares equal; rejected because it would then be a genuine id6 COLLISION (two files declaring one identity), trading an identity-slot error for a collision error. | DECISIONS.md D140 (the slot is the file's own identity; use a typed field for the reference); `check_engine.py:101` rule severity `error`; reproduced the violation and confirmed the three conforming walkthroughs use `- Target-Id:`. | yes |
| D-2 | Should the `_attempt2` assertion be re-scoped to the synthetic repository, or should the plan instead permit tidying the historical `mm6wuz` lanes so the assertion can run here? | Re-scope it to the synthetic repository, on the synthetic id6, and explicitly forbid deleting the historical lanes. | Permitting cleanup of the three `mm6wuz` branches; rejected as forbidden and destructive: they are another party's preserved work in a shared checkout, `nuanaw` and `ys1dor` cite exactly those attempt clusters as live evidence for unbuilt reporting work, and a pass obtained by deleting the evidence would be a fabricated pass, which is the failure mode this whole Set exists to prevent. | AGENTS.md shared-checkout rule; children 03 and 04 E-07 both already reconstruct synthetically because the original lanes are historical; `git branch --list 'aw/lane/mm6wuz*'` returns three branches; `nuanaw`'s history names the clusters. | yes |
| D-3 | The plan's named pre-existing failure is wrong. Should the review substitute the one measured failure as the new expected baseline, or remove the named baseline entirely? | Record the measured failure AND state it is environmental and machine-dependent, while making the binding criterion a self-measured node-id delta. | (a) Substituting `test_reporting_contract` as the expected failure with no caveat; rejected because it is caused by a gitignored local dump and will simply not occur on another machine, so it would become the next stale expectation. (b) Removing any named baseline; rejected because the plan inherited a WRONG name and silently deleting it loses the correction, leaving a later reader unable to tell whether the retirement failure was fixed or merely forgotten. | Measured `112 passed` for the retirement file; measured the reporting-contract failure and proved it causal by moving `opencode-recovery/` aside (`53 passed`); `git check-ignore -v` shows it gitignored and it is untracked. | yes |
| D-4 | Does the slow-suite gap need the maintainer's scope call, given this child authors no code? | No: add the requirement from the children's own evidence. | Leaving it as an open question for the maintainer; rejected because the repository already settles it. Child 04's gate independently requires the `-m ''` run for exactly these two files and names the pre-existing five-leaf failure, so requiring the Set's whole-Set verification to do what its own child does is a consistency repair, not a new scope decision. | `pyproject.toml` `addopts` `-m 'not slow'`; both files carry `pytest.mark.slow`; child 04 E-07's slow-suite paragraph and its Step 0 F-12. | yes |
