# Review: commit-path safety orchestrator, parent ao1rb7 (Set commitguard)

- Subject-Id: ao1rb7
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `2845941c`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision. All three Set members and the external
dependency `lqly9m` lint conforming, so nothing in this round is structural.

SCOPE: only the Order-00 parent was a candidate. Children `kbqpkn` and `y9vpvv`, the dependency
`lqly9m`, the executed sibling `29wvmj`, backlog `wjl471`, `.pre-commit-config.yaml`, `work_cmd.py`,
`engine.py`, `ipd_lifecycle.py`, `ipd_set_plan.py` and `oc_runipd.py` were read as EVIDENCE, not
reviewed, and are not in the ledger. NEITHER CHILD WAS MODIFIED by this review.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW, and for an orchestrator
that is a particular hazard because a parent's correctness is mostly claims ABOUT other artifacts and
about runner behavior. Nine things were RUN rather than read: every gate verb's occurrence count in
`.pre-commit-config.yaml`; the installed hook list in `.git/hooks`; `aw commit`'s selector refusal at
`work_cmd.py:138`; the PREFER paragraph at `engine.py:1252`; the front matter of both children,
`lqly9m`, `29wvmj` and `6sb3yu`-adjacent siblings; `aw ipd lint` on all four plans; the bare suite;
`ipd_lifecycle.ROLLUP_OMITTED_GATES` read in full; and `ipd_set_plan.parse_child_table` CALLED on this
very file. The last two produced the findings that matter, and one of them contradicts a sentence the
plan's own gate asserts.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-701 | HIGH | IN-SCOPE | D. Domain invariants; G. Plan executability | `ipd_lifecycle.ROLLUP_OMITTED_GATES["pre-transition-ev-checkpoint"]`; the gate's own sentence | **THE GATE ASSERTS A RUNNER "SKIPS NOTHING THAT WAS NEVER PERFORMED ELSEWHERE", AND THAT IS FALSE FOR E-03.** The claim holds for E-01 and E-02, which a runner genuinely supersedes by executing the children itself. E-03 is different: it COMPARES Order 01's shipped hook disclosures against Order 02's shipped managed-block sentences, and neither child can see the other's output (verified: `kbqpkn` E-05 guards only the two hook modules; `y9vpvv`'s spec-sync guards only the retry sentence). `retire_orchestrator` deliberately skips the `pre-transition` E/V checkpoint on the recorded premise that "an orchestrator's items are performed by NOBODY", so under `aw oc run` this parent reaches `executed` with E-03 unperformed, V-03 unverified, and the Set's own completion criterion ("no sentence shipped by either child describes a bypassable guard as an authority boundary") silently unchecked. That criterion is the one the backlog item says "still matters". | C:Low; U:Medium; S:Low; F:High; Overall:Medium | FIXED | E-03 NOT deleted (AGENTS.md: a parent should carry orchestration, and this is genuine orchestration; deleting it would lose the only cross-child comparison). Instead E-03 now states the runner caveat with the omitted-gate citation, requires a NAMED PERFORMER, and forbids reading the omitted-gate doc as permission; V-03 refuses a completion mark without a performer; the gate's false sentence is corrected and replaced with the E-01/E-02-versus-E-03 distinction; new OQ-02 puts keep-here-versus-add-an-Order-03 to the maintainer, quoting AGENTS.md's "ADD A CHILD for it" rule as option (b). |
| PR-702 | MEDIUM | IN-SCOPE | Evidence accuracy; C. Architecture (a coordination risk that no longer exists) | `aw find plans 29wvmj` -> `executed`; `.pre-commit-config.yaml:17`, `:23` | **`29wvmj` IS `executed`, NOT "approved", AND ITS WORK HAS ALREADY LANDED, so the stated double-edit risk is stale in the direction that matters.** The cross-IPD check told Order 01 to "extend that list rather than replace it" as though racing a pending sibling. Measured: `default_install_hook_types: [pre-commit, pre-merge-commit]` and `default_stages: [pre-commit]` are already in the file with the explanatory comment block, and `ipd-executed-transition-gate` already opts into `pre-merge-commit` via its own `stages:`. So the check becomes a CONFIRMATION against a known two-entry baseline rather than a coordination hazard, which is both easier and more precise. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | The cross-IPD bullet rewritten with the measured state, naming the exact expected post-condition (`pre-commit`, `pre-merge-commit` AND `pre-push`) and requiring `29wvmj`'s two keys plus its comment block be byte-unchanged. |
| PR-703 | MEDIUM | IN-SCOPE | A. Correctness (a live trap for the child's main deliverable) | `.pre-commit-config.yaml:19-23` and its comment | **`default_stages` IS PINNED TO `[pre-commit]` DELIBERATELY, AND NOTHING IN THIS SET MENTIONS IT.** The file's own comment records why: it would otherwise default to ALL stages, so merely adding a hook type would also run every content-MUTATING hook (ruff --fix, whitespace/eof fixers) in that stage, "rewriting a merge result mid-merge". The identical hazard applies to `pre-push`: adding the stage without giving the push gate its OWN explicit `stages:` would either not run it or run the mutating hooks on push. A parent whose job is cross-child coordination is the right place to surface a trap that its child's central deliverable walks into. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | New cross-IPD bullet quoting the constraint and requiring Order 01 follow the established pattern (pin `default_stages`, opt the one gate in explicitly); the completion criterion for the `pre-push` stage now carries the same note. |
| PR-704 | MEDIUM | IN-SCOPE | G. Plan executability (a Set that cannot finish as sequenced) | `lqly9m` front matter: `- Status: to-review`, no `Readiness`; both children `to-review` | **THE SET CANNOT COMPLETE IN ONE PASS AND THE PLAN NOWHERE SAYS SO.** Order 02 declares `Item-Dependencies: executed:lqly9m`, and `lqly9m` is `to-review` with no readiness, so the edge is UNSATISFIED today; both children are also unapproved. Under a runner, `edge_satisfied` re-checks at dispatch and marks Order 02 `dependency-blocked`, continuing the run, which is correct behavior but means a `commitguard` run lands Order 01 and stops. An executor expecting a single sitting would either report a failure that is not one or be tempted to relax the edge the Set exists to hold. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-01 states both children are unapproved; E-02 states the measured `lqly9m` status, explains the `dependency-blocked` outcome is designed and not a defect, and its expected outcome now admits the deferred branch; V-02 requires recording the item OUTSTANDING rather than complete on a `dependency-blocked` outcome; the gate states the Set needs at least two sittings. |
| PR-705 | MEDIUM | IN-SCOPE | A. Correctness (a table cell that looks broken and is not) | `parse_child_table` CALLED: `{'1': (), '2': ()}`, reason `None`; `_dep_tokens:264-273` | **THE CHILD TABLE'S `executed:lqly9m` CELL CONTRIBUTES NO ORDERING, AND A READER WOULD REASONABLY "FIX" THAT.** Calling the parser on this file returns empty dependency tuples for both rows with no error, because `_dep_tokens` deliberately ignores a typed edge naming a plan OUTSIDE the Set, "exactly how the runner treats an out-of-queue target". The enforcement lives in the child's own `- Item-Dependencies:` field at dispatch. Undocumented, this looks like a parse failure and invites either a table edit or a false conclusion that the hard ordering constraint is unenforced. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | The Child IPDs section now records the measured parse output, explains why it is correct, states where enforcement actually happens, and tells the reader not to "fix" the table; E-02 carries the same note. |
| PR-706 | MEDIUM | IN-SCOPE | E. Testing (a wrong baseline in both count and identity) | bare `python3 -m pytest` at review | **THE BASELINE NAMES THE WRONG FAILING TEST AND THE WRONG COUNT.** The plan says "1 failed, 5648 passed on `main`, the known `test_orchestrator_retirement` failure". Measured: `1 failed, 5919 passed, 3 skipped, 2 xfailed`, and the failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, environmental, caused by 189 untracked `opencode-recovery/*.md` files belonging to another party in this shared checkout. An executor comparing against a named test that does not fail would either chase a phantom or accept a real regression as "the known failure". | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Validation now carries the re-measured baseline with the named environmental cause, marks it perishable, and requires judging on the failure-set DELTA with node ids named rather than on counts. |
| PR-707 | LOW | IN-SCOPE | E. Testing (validating a change to the validator) | `.git/hooks` contains only `pre-commit`; `default_install_hook_types` semantics | The parent's validation calls for `pre-commit run --all-files` clean AFTER a Set whose Order 01 changes `.pre-commit-config.yaml`, without noting that `default_install_hook_types` takes effect only at INSTALL time. A green run against a stale `.git/hooks` (currently `pre-commit` only) would say nothing about whether the new stage works. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Validation now requires re-running `pre-commit install` before the all-files run and states the starting hook state measured at review. |
| PR-708 | LOW | IN-SCOPE | G. Plan executability (an unexplained Scope-Paths shape and an actionable OQ) | `Scope-Paths` naming only the two children; both children carry `- From-Backlog: wjl471`; item carries no `Blocks-Release` | Two small gaps. (1) A parent declaring only its child plan files is unusual and the plan never says what E-03 may READ versus WRITE, so an executor finding a bad sentence might edit a hook module or `engine.py` from the parent's fence. (2) OQ-01's recommendation to keep `wjl471` open is sound but not actionable: it does not say that both children already carry the machine-readable `From-Backlog` handoff, nor that the item carries no `Blocks-Release`, so a reader cannot tell whether closing it would trip a gate. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Scope check gained a justification paragraph distinguishing read from declare and routing any correction to the owning child (or a corrective IPD if both are terminal, per the no-commits-to-an-executed-plan rule); OQ-01 gained the measured handoff and gate facts so the recommendation is checkable. |

CONFIRMED SOUND, AND THE SPLIT DECISION IS THE PLAN'S REAL CONTRIBUTION. Every factual claim about the
defect reproduced exactly: the four gate verbs at zero occurrences and the two wired ones at one and
two, the single installed `pre-commit` hook, `aw commit`'s selector refusal, and the surviving
"immune to this by construction" claim in the PREFER paragraph. The argument for a Set rather than one
plan is correct and well made: Order 01 is near-free wiring of working code that depends on nothing,
Order 02 rewrites a contract installed into every managed repo and carries a hard ordering constraint,
and coupling them would block the free half behind the gated one. The deliberate exclusion of the
item's FINDING 1 (the agent-context detector and the new guard, with its four open design questions) is
the right call and is properly reasoned rather than an omission. The refusal to hand-write a
`- Readiness:` field, stated explicitly in the gate with the reason, is exactly right and is worth
noting because the opposite error has been measured in this repository.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing structural: three E-items in, three out, no
child touched. What changed is that the parent no longer claims a runner can safely retire it, the one
item a runner cannot perform is now loud about that with a named-performer requirement, the Set's
inability to finish in one pass is stated, and two live traps (the pinned `default_stages`, the
install-time semantics of `default_install_hook_types`) are surfaced for the child that walks into
them. OQ-02 is new and non-blocking because option (a) is now safe by disclosure rather than by hope.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | E-03 is work only the parent covers, and a runner will never perform it. Delete it, move it into a child, or keep it with a warning? | KEPT with an explicit runner caveat, a named-performer requirement in V-03, and the Set-shape question raised as OQ-02 for the maintainer. | DELETING it (AGENTS.md: deleting a parent's checklist causes the lost work it exists to prevent, and this comparison is genuine orchestration); MOVING it into a child (neither child can see the other's shipped output, so it would be unperformable where it landed); ADDING an Order 03 myself (a Set-shape change that alters the child table and completion criteria, which is the maintainer's call, not a bounded review edit). | AGENTS.md orchestrator paragraph ("do NOT delete it: ADD A CHILD for it"); `ROLLUP_OMITTED_GATES["pre-transition-ev-checkpoint"]`; `kbqpkn` E-05 and `y9vpvv` spec-sync scopes | yes |
| D-2 | Order 02's `executed:lqly9m` edge produces an empty parse in the child table. Change the table, or document it? | DOCUMENTED as correct, with the measured parse output and the enforcement site named. | Editing the table to make the edge parse (would misrepresent an out-of-Set edge as intra-Set ordering and fight a deliberate `_dep_tokens` behavior); saying nothing (a reader would read the empty parse as a broken constraint). | `parse_child_table` called on this file; `_dep_tokens:264-273` docstring; `oc_runipd.dependency_depth` out-of-queue handling | yes |
| D-3 | `lqly9m` is `to-review`, so the Set cannot complete. Raise it as a blocking question, or as an execution note? | Execution note plus a V-02 branch requiring the item be recorded OUTSTANDING. | A `Blocking: yes` question (nothing is undecided; the sequencing is already correct and the runner already handles it by deferring, so blocking would stop a plan that has no unanswered question); silence (an executor would misread `dependency-blocked` as a failure or relax the edge). | `lqly9m` front matter; `edge_satisfied` dispatch-time re-check; AGENTS.md runner paragraph on `dependency-blocked` | yes |
| D-4 | Should the review correct the children too, since PR-703's trap lands in `kbqpkn`'s scope? | No. Surfaced in the parent's cross-IPD section only; neither child file was opened for editing. | Editing `kbqpkn` directly (outside this review's ledger; the invocation named only the parent, and plan-review must not silently widen scope to another artifact); ignoring it (the trap would reach execution unmentioned). | plan-review Step 0 ledger rule ("fix it in the owning plan and cross-reference it from dependent plans" applies within scope; here the parent IS the cross-reference point) | yes |

## Commit result

- Pre-review snapshot: skipped, target plan committed and unchanged
- Hardened result: see the `plan-review: harden commitguard-00 ao1rb7 (revisions applied)` commit
- Push: not performed
