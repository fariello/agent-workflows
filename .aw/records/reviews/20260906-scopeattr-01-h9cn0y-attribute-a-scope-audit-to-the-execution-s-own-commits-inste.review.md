# Review: attribute a scope audit to the execution's own commits instead of everything since its base (child h9cn0y, Set scopeattr)

- Subject-Id: h9cn0y
- Subject-Type: ipd
- Reviewed-At: 2026-09-07
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `4cbee5fa`. Structural preflight `aw ipd lint --phase author` conformed before semantic
review (exit 0, zero findings) and `--phase review-finalize` conformed after the revisions.

DISCLOSURE: this plan was authored by the same model family in an adjacent session, so treat this as
close to a self-review. That matters concretely here, because the three most serious findings are all
the same failure mode: the AUTHORING asserted a mechanism from a plausible reading of a docstring and
did not check whether the mechanism is REACHABLE from the code path that needs it.

THE DEFECT IS REAL AND THE PLAN'S CORE INSTINCT IS CORRECT, and I re-verified the incident from
scratch rather than trusting the authored account. `mm6wuz`'s executed record does carry eight
`--scope-reason` entries that each say NOT THIS PLAN'S EDIT and name another agent's commit
(`git show b2bda903`, and the plan file's history line), thirteen non-merge commits do sit in
`6091014c..b2bda903^`, and the three cited foreign commits are confirmed by `git log -1 -- <path>`
(`976409e3` attention.py, `798c5cb5` commit_lock.py, `efc7a1a1` ipd_lifecycle.py). The record
corruption is not hypothetical. The code facts the plan builds on are also real: `_changed_path_sources`
does split the halves (`ipd_lifecycle.py:1170`), the union surface IS documented as a
`check_scope_drift` compatibility constraint (`:1204-1207`), `_working_tree_path_is_owned` does guard
only the working-tree half (`:1414-1428`), and its docstring does already argue this plan's case
(`:1289-1302`).

WHAT ROUND 1 CHANGED IS THE PLAN'S MECHANISM, not its goal. Three premises were measured false, and
each one, left in place, would have sent the executor at something that does not exist. The plan lost
one task group (the isolated fix, which fixes nothing because the isolated path is already correct),
lost its two candidate evidence sources (both unreachable), gained an explicit statement of the cost
the fix actually incurs, and gained negative proofs in the validation items so the retired approaches
cannot be silently reintroduced.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | BLOCKER | OVER-SCOPE | A. correctness; G. executability | `ipd_lifecycle.py:424-438`; `oc_runipd.py:6441`, `:6435`; measured 11 vs 47 paths; run `run-20260907T010730Z-3199043` state.json | **E-01 solves a non-problem, and the plan's stated trigger is wrong.** The plan says the audit misfires because it diffs against `base_head`, and proposes teaching finalize to diff the lane branch instead. But the ISOLATED PATH ALREADY DOES THE RIGHT THING: `_repo_root` returns the WORKTREE deliberately ("for a managed lane that is CORRECT") and the runner passes `finalize_repo = Path(work_dir)` for an isolated turn, so `git diff base..HEAD` already runs INSIDE the lane. MEASURED: that command returns ELEVEN paths (exactly `mm6wuz`'s own work, zero foreign) in `aw/lane/mm6wuz_attempt3` and FORTY-SEVEN in the main checkout. The incident's actual cause is that `mm6wuz` never reached the runner's finalize (`status: substantially-complete`, `integration_signal: "suite-failed"`, `verifier_ran: False`, so `integration_gate_relevant and integration.earned` was false) and a HUMAN recovered the lane and finalized by hand from main. An executor implementing E-01 as authored would have added a second, redundant attribution path to the one case that already works, while the case that actually broke stayed unaddressed | C:Medium; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | Concern gained a corrected-trigger paragraph with all four measurements. E-01 rewritten from a fix into a CHARACTERIZATION test that pins the already-correct lane behavior and must pass BEFORE E-02. Scope narrowed to the in-place and hand-finalize case. New F-12 records the measurement. The runner's earned-integration gap is named in Deferred as `integearn`'s territory |
| PR-002 | BLOCKER | IN-SCOPE | A. correctness; G. executability | `ipd_lifecycle.py:865`, `:912`, `:2797`; receipt keys measured; `worktree_lease.py:673-702` | **The discriminator E-01 instructs the executor to use is not available at finalize time.** The plan says "THE DISCRIMINATOR ALREADY EXISTS: `isolated_baseline`". It does not, for finalize: `isolated_baseline` is a parameter of `_baseline_ambiguity` and `begin` only, transported by `AW_ISOLATED_BASELINE` and read at `:2797` inside `run_begin`. The receipt does NOT persist it (measured keys: `actor, base_head, kind, plan_content_digest, plan_id, plan_path, pre_execution, requirement_digest, schema_version, scope_paths, timestamp`) and no finalize function receives it. Worse, the lane-branch diff the plan calls "an exact boundary" is unsafe after integration: `teardown_worktree` deletes the per-lane branch and its own docstring records that the commit then "survives only as an UNREFERENCED object". So the authored E-01 is not merely redundant, it is unimplementable as specified and would decay | C:Medium; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | E-01 now forbids consuming `isolated_baseline` and forbids adding a lane-branch diff, citing both reasons. New F-15 records the teardown hazard; F-8 struck through and redirected to F-12. Conventions section corrected. The gate forbids persisting `isolated_baseline` into the receipt (a schema change) as a workaround. V-01 requires a NEGATIVE proof (a grep showing no new occurrence in any finalize function) |
| PR-003 | BLOCKER | IN-SCOPE | A. correctness; C. operability | `.aw/.gitignore:14`; measured absent dir in `.aw/worktrees/mm6wuz_attempt3/.aw/records/`; `ipd_lifecycle.py:1940`, `:2014`; `oc_runipd.py:6382` vs `:6456` | **E-02's evidence source is unreachable from the code it must run in, and the plan presents it as the preferred mechanism.** "THE RUN ALREADY RECORDS THE ANSWER ... so this is consumption rather than inference" is half true: `last_outcome.commits[].sha`/`.paths` DO exist (verified in the `mm6wuz` run state, carrying `paths`/`sha`/`subject`). But finalize cannot read them, for three independent reasons: the record lives under `.aw/records/runs/`, which is GITIGNORED and measurably ABSENT from a lane worktree; `finalize` receives no run id on any surface (`run_id` appears in `ipd_lifecycle.py` only inside `retire_orchestrator`); and it is written at `oc_runipd.py:6382` in the same driver function that calls `driver_finalize` at `:6456`, so a hand finalize (the very case PR-001 shows caused the incident) has no record at all. Following the plan, the executor would either wire a gitignored, lane-absent run-record dependency into the lifecycle gate, or discover mid-execution that the plan's primary mechanism is fiction | C:Medium; U:Low; S:Medium; F:Medium; Overall:Medium | FIXED | E-02 rewritten around the OWNERSHIP PREDICATE alone, with an explicit DO NOT introduce a run-record dependency and all three reasons cited. New F-13; F-9 struck through and redirected. The gate forbids reading `.aw/records/runs/` or `last_outcome` from the lifecycle gate. V-02 requires negative proof that no such read exists. OQ-01 rewritten: both candidate sources were measured unavailable, so the question dissolved |
| PR-004 | HIGH | IN-SCOPE | B. honest reporting; A. correctness | measured `git log -1 --format="an=%an ae=%ae cn=%cn ce=%ce"` on `976409e3`, `798c5cb5`, `efc7a1a1`, `16847597`, `6d56930e` (all identical) | **E-03 requires the refusal message to name a sha as another actor's, which the code cannot substantiate, so the plan mandates writing a fabricated attribution claim into operator-facing text.** "Name the sha" and "was committed by `<sha>`, which is not this execution's" both assert authorship the system cannot determine: MEASURED, all five incident commits, the three other actors' and `mm6wuz`'s own two alike, carry the IDENTICAL author and committer identity, because every agent commits as the maintainer. `%an`/`%ae` partitions nothing. This is the same class of defect the plan itself exists to fix (a record asserting something untrue about who changed what), reintroduced in the fix's own error message | C:Low; U:Medium; S:Low; F:Medium; Overall:Medium | FIXED | E-03's sha requirement retired and replaced with a message stating only checkable facts (outside the declared `Scope-Paths`, disregarded as not attributable to this execution). New F-14. E-02 gained a DO NOT reach for authorship, with the measurement. OQ-03's resolution rewritten to the substantiable wording. The gate forbids authorship as an attribution signal. Doc-sync section states the message constraint. Deferred now records that trailers are the ONLY identified attribution channel, raising that follow-up's value rather than treating it as a nicety |
| PR-005 | HIGH | UNDER-SCOPE | D. invariants; honest documentation | `ipd_lifecycle.py:1400-1412` ("no weakening whatsoever"), `:1289-1302` (the working-tree half's ACCEPTED COST) | **The plan never admits the cost its own fix incurs, and leaves in place a code comment that will become false.** With no per-commit attribution available (PR-003, PR-004), the predicate cannot distinguish "another actor committed this undeclared path" from "this execution committed it outside its fence"; both are simply committed-and-unowned. So excluding the class ALSO stops demanding a reason for the executor's own out-of-scope COMMIT. That is a genuine weakening of the gate, and it is precisely the trade the working-tree half already made and DOCUMENTED as an accepted cost; the plan claims instead that the fix is a pure win. Meanwhile the comment at the filter site states "COMMITTED half: treated exactly as before ... still demands a reason (no weakening whatsoever)", which is the sentence a future reader would trust and which this plan falsifies | C:Low; U:Low; S:Medium; F:Medium; Overall:Medium | FIXED | Cost stated in Scope, required as a code comment at the site by E-02 (in the same voice the working-tree half uses), and stated in the gate. OQ-02's resolution rewritten: the authored answer assumed an unobservable distinction. Doc-sync section now requires correcting the `:1400-1412` comment block by name, and notes `_working_tree_path_is_owned` becomes a misnomer once it judges both halves |
| PR-006 | HIGH | IN-SCOPE | E. testing; anti-gaming | plan's authored V-03 and E-03 wording; consequence of PR-003/PR-004 | **The plan's self-described most-important validation was unsatisfiable as written, in the direction that produces a false pass.** V-03 demanded "a committed out-of-scope path with NO positive attribution to another actor STILL demands a reason", and the gate said "a path is excluded ONLY on positive evidence that another actor committed it". Since nothing can establish that another actor committed anything (PR-004), the authored test is not merely hard, it is undefined; an executor needing V-03 to pass would most plausibly construct something that superficially refuses and paste it. That is worse than no check, because this plan makes a gate demand LESS and V-03 is the only thing standing between that and an invisible over-broad exclusion | C:Low; U:Low; S:Medium; F:Medium; Overall:Medium | FIXED | E-03 and the gate restated on the predicate's ACTUAL positive evidence, with an explicit note that the authored wording was unimplementable so the executor does not try to satisfy it. E-04 and V-03 now require EITHER a demonstrated refusal OR an explicit report that E-02 excluded the whole committed class, and forbid a manufactured pass by name. The gate adds a paragraph inviting the executor to raise the trade as a design objection instead of widening the exclusion |
| PR-007 | MEDIUM | IN-SCOPE | E. testing; evidence accuracy | measured `1 failed, 5613 passed, 3 skipped, 2 xfailed in 72.91s` at `4cbee5fa`; `tests/test_orchestrator_retirement.py:875` | The authored baseline (`1 failed, 5612 passed`) was one test stale and named the pre-existing failure only by class. Re-measured bare at HEAD: `1 failed, 5613 passed, 3 skipped, 2 xfailed`, the failure being `RealRepositorySets::test_runprofile_refuses_for_R2_and_NOT_for_unauthored_rows` with `{'kgpptv': 'approved'} != {'kgpptv': 'reviewed'}`. That test asserts against a SIBLING PLAN's live status, so it will keep drifting; an executor comparing message text rather than the failure SET could mistake a new message for a new failure | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Baseline re-measured and stated with the full summary line in E-05, the validation section, and the gate's BASELINE HONESTY paragraph, which now names the test in full and warns that its message may differ again. The delta criterion (after-minus-before empty) was already correct and is retained |
| PR-008 | MEDIUM | UNDER-SCOPE | E. testing; D. invariants | measured `11 tests collected` in `tests/test_finalize_scope_ownership.py`; `:test_own_UNCOMMITTED_out_of_scope_edit_is_now_disregarded_ACCEPTED_REGRESSION` | E-05 requires proving `check_scope_drift` and the `--scope-ack` direction unchanged, but never mentions the ELEVEN existing tests in the very file E-02 changes the sibling half of. Those tests pin the working-tree half's behavior including its accepted regression, and since E-02 routes the committed half through the SAME predicate, any behavioral drift surfaces there FIRST. A whole-suite line can hide a swap (one new test passing while one old one silently changed meaning) in a way a per-file summary cannot | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-05 now requires the eleven pre-existing tests to pass unchanged and the file's OWN summary line to be pasted, not only the whole-suite line. V-05 requires the same. The count is recorded in the validation section and in Scope-Paths justification |
| PR-009 | LOW | IN-SCOPE | G. executability; honest documentation | plan's Scope-Paths and Scope check sections | With E-01 demoted to a characterization test, `tests/test_finalize_isolated_commit.py` remains in `Scope-Paths` but its ROLE changed: it now holds a test of behavior that is already correct rather than tests of an isolated-mode code change. Left unstated, an executor could read the unchanged Scope-Paths line as evidence that an isolated-mode code change is still expected, or could treat "I made no non-test edit there" as an unfinished item and go looking for work to do | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Scope check's Scope-Paths justification now states the role change explicitly and says that finishing with no non-test edit to that path is the EXPECTED outcome, not an incomplete item. Under-scope list extended with the four new exclusions (receipt persistence of `isolated_baseline`, run-record reads, authorship, the runner integration gap) |

No finding was DEFERRED, left OPEN, or marked REPLAN, so no escalation to a `- Blocking: yes` question was
required. All three pre-existing open questions were re-examined and REWRITTEN rather than merely
re-confirmed: OQ-01's two candidate answers were both measured unavailable so the question dissolved,
OQ-02's authored answer turned on a distinction that is not observable, and OQ-03's authored wording
required an unsubstantiable attribution claim. All three remain correctly non-blocking, because each is
now answered from repository evidence rather than needing a human decision.

Nothing was rejected. Note the three BLOCKERs are all the SAME failure mode and worth naming as one
lesson: the authoring read a docstring or a persisted field, inferred that the value was therefore
available where it was needed, and did not trace the call path. `isolated_baseline` exists but not in
finalize; `last_outcome.commits[]` exists but not in a lane or a hand finalize; git authorship exists
but does not vary. A plan whose mechanism rests on an available-looking signal should verify
REACHABILITY from the exact function that will consume it, which is why every affected validation item
now demands a NEGATIVE proof rather than only a positive one.

WHAT I DID NOT CHANGE, deliberately. The plan's decision to fix this with the existing ownership
predicate rather than waiting for commit trailers stands, and I strengthened its rationale rather than
reopening it: trailers are now recorded as the ONLY identified attribution channel, which makes the
follow-up more valuable, but blocking this plan on a repo-wide commit-convention change would leave the
record corruption in place meanwhile. I also left the plan at five E-items rather than splitting it; the
scope SHRANK in review (one fix became one characterization test) and each remaining item is a single
concern in one file.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-001: with the isolated case measured already correct, should E-01 be deleted or repurposed? | Repurposed into a CHARACTERIZATION test that must pass BEFORE E-02, plus explicit DO NOTs against the retired approach. | Deleting E-01 outright, REJECTED for two reasons: the property is currently unpinned, so a later change to `_repo_root` or to `finalize_repo` would silently reintroduce the lane misattribution with no test complaining; and the authored diagnosis was persuasive enough that the next reader would likely re-derive it, so the plan should carry the measurement that refutes it. Keeping E-01 as a fix "for safety", REJECTED as adding a redundant second attribution path plus a lane-branch dependency that teardown deletes. | `ipd_lifecycle.py:424-438`; `oc_runipd.py:6441`; measured 11 vs 47 paths in `aw/lane/mm6wuz_attempt3` vs main; `worktree_lease.py:701` | yes |
| D-2 | PR-003/PR-004: with both candidate evidence sources unreachable, does this plan still have a viable fix, or should it be REPLANned to wait for commit trailers? | Viable: apply the EXISTING ownership predicate to the committed half, and state the resulting cost honestly. | REPLAN to wait for `a8eufb` commit trailers, REJECTED: trailers are the only channel that would give true attribution, but essentially no commit carries one today, so the wait is open-ended while the record corruption is active and already measured once. Inventing an attribution channel inside this plan (a lifecycle-owned commit registry), REJECTED as far beyond a three-path child and as a durable new mechanism that belongs in its own spec. | `git log --format=%B -200 \| grep -c "AW-Run\|AW-Item"` = 1; `ipd_lifecycle.py:1289-1302` (the predicate and its precedent); commit `b2bda903` (the active corruption) | yes |
| D-3 | PR-005: is the weakening (an executor's own out-of-scope COMMIT stops needing a reason) acceptable, or is it the maintainer's call? | Accepted in the plan, but stated in three places AND the gate explicitly invites the executor to STOP and raise it as a design objection if implementation shows it is worse than analysis suggests. | Silently accepting it, REJECTED: it is exactly the invisible-mistake direction the plan itself warns about, and an undocumented weakening of a gate is how a gate stops meaning anything. Escalating it to a blocking question now, REJECTED because the working-tree half already made the IDENTICAL trade deliberately (Order 01 OQ-01/F3) and the maintainer accepted it there, so treating the mirrored case as newly contentious would block on a settled precedent. | `ipd_lifecycle.py:1289-1302` ("ACCEPTED COST (Order 01 OQ-01/F3)"); `:1400-1412` (the comment this falsifies) | yes |
| D-4 | PR-006: how should V-03 handle the possibility that E-02 excludes the ENTIRE committed-unowned class, leaving no fail-closed case to demonstrate? | Require the executor to REPORT that outcome explicitly as a finding, and forbid pasting a constructed case that merely looks like a refusal. | Requiring a fail-closed case unconditionally, REJECTED as the setup that produces a fabricated pass: an executor who cannot construct the case and must show one will show something. Dropping the fail-closed requirement, REJECTED because it is the single check standing between this change and an unnoticed over-broad exclusion. | plan's own gate ("the direction in which a mistake becomes invisible"); consequence of PR-003/PR-004 measurements | yes |
| D-5 | Should the runner gap that actually forced the hand finalize (`integration_signal: "suite-failed"`, integration never earned) be pulled into this plan? | No; named in Deferred as `integearn`'s (`32ij2j`, `ciesaj`) territory, with this plan scoped to making the hand-finalize path HONEST. | Widening this child to fix the earned-integration gate, REJECTED: different mechanism, different module (`oc_runipd.py`, outside this fence), and a plan already exists for it. Leaving the connection unstated, REJECTED because a reader of the corrected Concern will now ask why a human had to finalize at all, and the answer belongs on the record. | run `run-20260907T010730Z-3199043` state.json; `oc_runipd.py:6435` (`integration_gate_relevant and integration.earned`) | yes |

No `Reversible: no` decision was taken, so no escalation under the irreversible-decision rule was
required. Every decision above is a plan edit that a later maintainer can revert by editing the plan.
