# Review: decide what RUN-NO-PUSH enforcement means before binding it, child 4h7tt0 (Set nopush)

- Subject-Id: 4h7tt0
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `c0a9de73`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Re-reading one's own
plan is nearly worthless, so the method here was to PERFORM the retirement the plan prescribes, in a
scratch copy of the repository, and measure what it did. That single act produced the blocker and it
was invisible from the prose. Six things were executed rather than inspected: the retirement itself
(spec row plus code entry removed) with the full suite run against a measured baseline;
`validate_finding_table()` after the removal; a computation of which finding code claims each spec-4.1
abort class; the provenance of the commit that asserts the maintainer's OQ-01 decision; the
`test_run_viewer.py` file the plan tells the executor to disregard; and `mjx7ne`'s recorded OQ-03
answer, which the plan cites as its precedent.

SCOPE: only this child was a candidate; the Set has exactly one member. Read as evidence:
`agent_workflows/run_evidence.py`, `agent_workflows/host_sandbox_profile.py`,
`tests/test_run_evidence_completion.py`, `tests/test_host_capability_extension.py`,
`tests/test_host_sandbox_profile.py`, `tests/test_run_viewer.py`, spec
`20260826-0718-01-aw-run-deterministic-run-and-verify.spec.md` (`25kzda`), executed plan `mjx7ne`,
backlog item `d07nz2`, and commit `b23d447d`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | BLOCKER | UNDER-SCOPE | A. Correctness; E. Testing (the prescribed change ships a self-invalidating table) | measured in a scratch copy at HEAD `c0a9de73`; `run_evidence.py:1649-1656`; `tests/test_run_evidence_completion.py:1082`, `:1090`, `:1293-1301` | **THE PLAN'S CENTRAL INSTRUCTION, "AMEND NOTHING ELSE", MAKES THE RETIREMENT IMPOSSIBLE TO DO CORRECTLY.** I performed outcome (c) end to end (removed the row from the spec table AND the matching `RunFindingCode`) and ran the suite: `5 failed, 5915 passed` against a `1 failed, 5919 passed` baseline, i.e. FOUR new failures, every one a hardcoded THIRTEEN that retirement necessarily falsifies (`test_spec_defines_exactly_thirteen_run_codes`, `test_table_enumerates_exactly_the_specs_thirteen_codes`, `test_table_self_validation_passes`, `test_measured_binding_partition_is_recorded`, the last asserting `unbuilt == ["RUN-NO-PUSH"]`). WORSE THAN A TEST BREAK, and this is what makes it a blocker rather than a chore: `validate_finding_table` hard-fails `len(RUN_FINDING_CODES) != 13` with `RC-COUNT`, so a retirement done as the plan describes leaves the SHIPPED table reporting ITSELF INVALID at runtime. The count is part of the contract in four enforcing places plus ten prose ones, including the spec's own sentence "carries all 13". An executor obeying the fence would have had to either edit an undeclared file or leave the suite red. | C:Medium; U:Low; S:Low; F:High; Overall:Medium | FIXED | E-03 rewritten to own the count in all four enforcing sites and the spec's own sentence, with the measured failure list inline; E-04 rewritten to drop the invariant to 12 and update the four assertions; `tests/test_run_evidence_completion.py` added to `Scope-Paths`; V-04 now requires `validate_finding_table()` VALID; added F-8; Scope check and spec-sync updated. |
| PR-002 | HIGH | IN-SCOPE | B. Security (retiring a report could delete a live refusal) | `host_sandbox_profile.py:1324`, `:1345`, `:1366`; `tests/test_host_capability_extension.py:78`, `:190`, `:255-257`, `:333`, `:473` | **THE PLAN NEVER DISTINGUISHES THE 4.2 FINDING CODE FROM THE 5.2 CAPABILITY, SO "COMPLETING THE RETIREMENT" COULD REMOVE A GATE THAT STILL REFUSES.** `CAP_DENY_PUSH` is not merely a reporting name: it is a required capability in the action-requirement map for `review`, `mutate` and `contractless-prompt`, so those three action classes fail closed at preflight and will CONTINUE to after the finding code is gone. The plan's E-04 said only "leave `supports_deny_push` `False`", with no statement that the capability and its requirements are load-bearing and out of bounds, and its own Concern conflates the two by describing the code as "the capability it would read". A tidy-minded executor removing "the now-unused deny_push machinery" would silently convert a fail-closed preflight into no check at all, which is the precise fail-open outcome this whole plan exists to avoid, achieved by way of a change nominally about honesty. | C:Low; U:Low; S:High; F:High; Overall:Medium | OPEN | Provisionally resolved as PRESERVE and written into the plan (E-04 forbids touching the capability and names the five pinning assertions that must pass UNEDITED; V-04 requires both capability test files green without edits and FAILS the item if either needed editing; F-9 and a Step 0 convention added; Scope check records the exclusion). But the underlying decision is IRREVERSIBLE (D-2, `Reversible: no`), so it is ESCALATED rather than closed on reviewer authority: raised as plan OQ-02 with `Blocking: yes`, `Finding: PR-002`, owner maintainer. `aw ipd lint` now refuses the plan with `IPD-Q501` at every checkpoint (verified), which is the intended fail-closed stop. A PRESERVE answer unblocks with no further edits. |
| PR-003 | HIGH | UNDER-SCOPE | A. Correctness (retiring one row leaves the spec self-contradictory) | grepped `25kzda` at review time: `:652`, `:848`, `:864`, `:1115`, `:1129`, `:1154`; `host_sandbox_profile.py:204`, `:549-554` | **THE SPEC PROMISES NO-PUSH IN SIX PLACES AND THE PLAN AMENDS ONE.** After E-03 the withdrawn guarantee would still be asserted at `:848` ("deny push-capable network routes and withhold remote credentials"), `:864` (the review capability row listing "no-push enforcement"), `:1115` (design principle 4), `:1129` (descriptor fields) and `:1154` (the worked example). A reader could not tell which sentence is current, which is exactly the promise-versus-reality drift the plan exists to end, merely relocated. Most of these legitimately describe the PRESERVED 5.2 capability and should be defended rather than deleted, so the fix is a per-site verdict, not a sweep. One is independently wrong: `:1154` claims the `oc` descriptor "positively proves ... no-push enforcement", which contradicts `supports_deny_push` being hardcoded `False` and declared-not-probed, and was wrong before this plan existed. | C:Low; U:Low; S:Low; F:High; Overall:Low | FIXED | Added E-05 and V-05 requiring an EDITED-or-DEFENDED verdict per site with reasons, and requiring the `:1154` contradiction be addressed explicitly rather than passed over; added F-10; Scope and spec-sync updated. |
| PR-004 | HIGH | IN-SCOPE | G. Executability (the gate contradicts the plan's own resolved question) | the plan's `Blocking: no` / `Status: resolved` at OQ-01 versus its gate paragraph | **THE GATE TELLS THE EXECUTOR TO WAIT FOR A DECISION THE PLAN ALREADY RECORDS.** OQ-01 reads `Blocking: no`, `Status: resolved`, `Owner: none`, with the maintainer's answer written out in full; forty lines later the gate says "OQ-01 must be answered first: it is `Blocking: yes`, so `aw ipd set approved` will refuse while it stands, which is deliberate", and adds that E-03/E-04 cannot run. Both cannot be true. The stale half also makes a false claim about tooling behavior (approval would not refuse, because the question is not blocking), and it is precisely the kind of internal contradiction that stalls an execution turn on a question nobody needs to answer. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Gate rewritten to state OQ-01 is resolved and non-blocking, to name the maintainer decision and its commit, and to say that only human approval is outstanding; added F-11. |
| PR-005 | MEDIUM | IN-SCOPE | A. Correctness (a cited precedent says the opposite) | `mjx7ne` E-02's recorded OQ-03 answer, 2026-09-01 | **THE PLAN'S GOVERNING PRECEDENT DOES NOT SUPPORT WHAT IT IS CITED FOR.** Step 0 and F-5 present `mjx7ne` OQ-03 as establishing that retirement is "sanctioned rather than a cop-out". Read at source, that maintainer answer is "option (a), KEEP ALL THREE capabilities, honestly labelled", i.e. it authorizes DECLARING a capability `False` with a `probe_notes` reason instead of probing it - which is exactly the state E-04 now PRESERVES - and says nothing about deleting a spec row. Overstating it matters twice: it blurs the code-versus-capability distinction PR-002 turns on, and it dresses a single fresh maintainer decision in a false lineage. The retirement is legitimate on its own authority (OQ-01, `b23d447d`); it does not need a borrowed one. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Step 0 and spec-sync now state the precedent precisely (it authorizes keeping the capability, not retiring the code) and name `b23d447d` as the sole authority for the retirement; added F-12. |
| PR-006 | MEDIUM | IN-SCOPE | E. Testing (a validation instruction that licenses ignoring real failures) | ran `tests/test_run_viewer.py` at HEAD: `75 passed` | V-04 instructs the executor that "~14 `test_run_viewer.py` failures belong to the separate `agrlvw` defect (plan `utwr6y`); do not report them as this plan's". Measured at HEAD, that file is `75 passed` with zero failures. A standing instruction to disregard a named failure set is safe only while the set exists; once empty it becomes a licence to wave away genuine regressions in that file, and this plan touches shared run-evidence machinery that the run viewer consumes. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | V-04's disregard instruction replaced with the measured `75 passed` state and an explicit statement that any failure there IS this plan's to explain; the review-measured bare baseline substituted; added F-14. |
| PR-007 | MEDIUM | IN-SCOPE | G. Executability (a non-existent artifact named as evidence) | `tests/test_run_evidence_completion.py:1097-1136`, `:1165-1166` | The plan refers three times to "the byte-equality test between 4.2 and `RUN_FINDING_CODES`" and V-03 requires pasting it. No such test exists. The pinning is FOUR per-field comparisons (message, action, inspects, pass_criterion) driven by a spec-table parser. The practical constraint the plan draws from it is correct, but an executor told to paste a named test that does not exist either invents a plausible substitute or reports something else under that name, and V-03's evidence bar becomes unmeetable as written. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Step 0 convention and F-6 corrected to describe field-by-field pinning; V-03 now names the four real tests and forbids claiming a byte-equality run. |
| PR-008 | LOW | IN-SCOPE | A. Correctness (a consequence nobody should tidy) | computed abort-class usage at HEAD; `run_evidence.py:1184-1191`, `:1508`; `tests/test_run_evidence_completion.py:1165-1166` | `RUN-NO-PUSH` is the ONLY code naming spec 4.1's "Push attempt" abort class, so retirement orphans that class. That is fine and should stay fine: an unused class already exists today (`Unknown or non-idempotent external outcome`, verified), and the class list is pinned to 4.1 under its own test, so an executor who "cleans up" the now-unused class would make an unauthorized 4.1 edit and fail that test. Worth recording precisely because the tidy instinct here produces a scope violation rather than an improvement. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-15, a Step 0 convention, and an explicit Scope-check line stating the class is deliberately left orphaned. |

CONFIRMED SOUND, AND THE PLAN'S JUDGEMENT ABOUT WHAT IT IS DOING IS RIGHT. The central argument survives
scrutiny: retirement removes a PROMISE and not a PROTECTION, because `supports_deny_push` is already
hardcoded `False` and declared-not-probed (`host_sandbox_profile.py:204`, note at `:549-554`), so the
gate fails closed today and continues to. F-1 verified by symbol: `RUN-BASELINE-OWNERSHIP` really is
BOUND, `worktree_lease.LeaseTable.claim` and `run_evidence.dirty_within` both exist, so halving the
backlog item was correct and no second lease-overlap check may be built. F-2 verified verbatim: the
anti-inference prohibition is written at `host_sandbox_profile.py:88-95` in the words the plan quotes.
F-4 and F-7 hold. The spec is `- Status: approved`, so treating a 4.2 edit as a declared amendment
requiring the spec in `Scope-Paths` is right. AND THE ATTESTATION IS GENUINE, which I checked rather
than assumed because a plan asserting its own maintainer approval is the exact forgery shape this
repository guards against: commit `b23d447d` is authored by the maintainer and its message states the
decision, the two declined options and the three evasions independently of the plan body. So outcome
(c) stands on real authority, and the plan's refusal to pick the winner itself was correct.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing about the DECISION, which is the maintainer's
and is sound. Everything about the BLAST RADIUS of executing it. The pattern behind PR-001, PR-002 and
PR-003 is one mistake repeated at three scales: the plan treated "retire a finding code" as a local
edit, when the code's identity is woven into an enforced count, a separate capability gate, and five
other spec sentences. Each was invisible from reading and each surfaced immediately on doing. The
narrower theme is stale internal state: a gate contradicting its own resolved question, a precedent
that says the opposite of its citation, a named test that does not exist, and an instruction to ignore
failures that no longer occur. A plan this careful about not fail-open-binding a security guarantee was
notably less careful about whether its own prescribed edit would leave the shipped table reporting
itself invalid.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Retirement breaks four hardcoded-13 assertions and the runtime `RC-COUNT` invariant (PR-001). Amend the count to 12, make the invariant derive from the table, or keep 13 rows by leaving a tombstone row? | Amend the count to 12 in all four enforcing places plus the prose, and declare the test file. | (a) Make `validate_finding_table` derive its expected count from the table itself; rejected because the literal is the POINT: it is a deliberate tripwire so a code cannot be added or dropped silently, and self-deriving it would delete the guard while appearing to fix it. (b) Keep 13 by leaving a retired/tombstone row in 4.2; rejected because it keeps the withdrawn promise in the published vocabulary, which is the very thing being retired, and the field-by-field pinning would still demand text for it. | Measured: removal yields exactly four new failures and `validate_finding_table` reporting `RC-COUNT`; the invariant's own docstring frames the count as a structural rule a later edit must not quietly break (`run_evidence.py:1631-1643`), which is an argument for updating it deliberately rather than removing it. | yes |
| D-2 | Does retiring the 4.2 code imply removing the 5.2 `supports_deny_push` capability (PR-002)? | PROVISIONALLY no: preserve the capability, its `probe_notes`, and its action-class requirements untouched, and forbid editing their tests. ESCALATED, not settled on reviewer authority: raised as plan OQ-02 (`Blocking: yes`, `Finding: PR-002`, owner maintainer), so `aw ipd lint` refuses the plan with `IPD-Q501` until the maintainer answers. | Removing the capability as now-unused; rejected because it is NOT unused - it gates `review`, `mutate` and `contractless-prompt` at preflight, so removal would convert a fail-closed refusal into no check, which is a security regression achieved under cover of an honesty change. Also considered narrowing the capability; rejected as outside OQ-01's authorization, which addressed the finding code only. Also considered recording this decision WITHOUT escalating; rejected because recording alone is sufficient only for a reversible decision, and loosening a live security gate cannot be undone by editing a plan. | `host_sandbox_profile.py:1324`/`:1345`/`:1366` list `CAP_DENY_PUSH` as required for three action classes; `tests/test_host_capability_extension.py:473` asserts it appears in `missing` for the refusal path; `mjx7ne` OQ-03's answer was expressly "KEEP ALL THREE capabilities, honestly labelled"; OQ-01's maintainer decision (`b23d447d`) authorized retiring the 4.2 CODE and is silent on the capability. | no |
| D-3 | Five other spec sites still promise no-push (PR-003). Sweep them, defend them, or require a per-site verdict? | Require a per-site EDITED-or-DEFENDED verdict with reasons, and flag `:1154` specifically. | (a) Bulk-delete every no-push mention; rejected because most describe the PRESERVED capability or a design principle that retirement does not falsify, so deleting them would over-withdraw and weaken the spec's actual guarantees. (b) Leave them all, treating 4.2 as the only reporting surface; rejected because the spec would then promise in five places what it withdrew in one, leaving a reader unable to tell which is current. | Grepped all six sites; `:848`/`:864`/`:1129` describe the 5.2 capability, `:1115` states a principle whose "must fail closed" remains true, while `:1154` asserts the `oc` descriptor "positively proves ... no-push enforcement" against a hardcoded `False`, which is a live contradiction independent of this plan. | yes |
| D-4 | The gate claims OQ-01 is blocking and unanswered while OQ-01 reads resolved (PR-004). Which is authoritative? | The resolved question: OQ-01 is answered, non-blocking; only human approval remains. | Treating the gate as authoritative and re-opening the question to the maintainer; rejected because the answer demonstrably exists with maintainer provenance, and re-asking a settled security decision wastes the maintainer's time on a question they already ruled on, which is exactly what the workflow forbids. | The OQ block reads `Blocking: no` / `Status: resolved` / `Owner: none` with the decision written out; commit `b23d447d` is maintainer-authored and states the same decision and reasoning independently, so the resolution is attested rather than self-asserted. | yes |
| D-5 | After retirement no code claims spec 4.1's "Push attempt" abort class (PR-008). Remove the class, or leave it orphaned? | Leave it orphaned, and say so explicitly so nobody tidies it. | Removing the class from 4.1 and `ABORT_CLASSES`; rejected because 4.1 is outside this plan's authorized amendment (OQ-01 addressed 4.2's row), the class list is byte-pinned to the spec under its own test so removal fails that test, and an orphaned class is already an accepted state here. | Computed per-class usage: `Unknown or non-idempotent external outcome` already has zero claiming codes today, so orphaned classes are tolerated by construction; `tests/test_run_evidence_completion.py:1165-1166` pins the list to the spec's six. | yes |

## Round 2

Round 2 exists to CLOSE PR-002, whose escalated question the maintainer answered on 2026-09-10, and to
record that the answer went AGAINST round 1's provisional recommendation. It re-critiques nothing: every
other round-1 finding was already `FIXED` and is superseded unchanged.

WHY THIS ROUND IS NEEDED AT ALL, since the fix is bookkeeping: the escalation contract
(`plan-review.md:335-341`) defines only the path INTO a blocking question and no path back, so answering
the question left PR-002 reading `OPEN` forever while `subject_gating_blocks` kept refusing the plan on
it. Measured at HEAD `dcb5a2a4`: plan OQ-02 read `- Status: resolved` with the maintainer's decision
recorded, and the finding still gated. Appending a round is the sanctioned mechanism, because
`ReviewDocument.current_findings` reads only the LAST round (`review_findings.py:236-243`).

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-002 | HIGH | IN-SCOPE | B. Security (retiring a report could delete a live refusal) | plan OQ-02 (`- Status: resolved`, `- Finding: PR-002`); `agent_workflows/host_sandbox_profile.py:1324`, `:1345`, `:1366` | Carried forward from round 1 and now CLOSED BY THE MAINTAINER, whose answer REVERSED round 1's provisional PRESERVE. Ruling of 2026-09-10 (`/askme`): REMOVE `supports_deny_push` AND the three unenforced action verdicts, in a SEPARATE plan. Round 1's premise was measured FALSE: `ACTION_REQUIREMENTS` has no consumer outside `host_sandbox_profile.py`, the runners import only five sandbox helpers (`oc_runipd.py:58-65`), and the `ALLOWED`/`REFUSED` lines are rendered by `host_cmd.py:104-107` alone, so there was never a preflight refusal to preserve; the flag only PRINTED a protection that did not exist. It also never shipped (added `30108f78` 2026-09-04; newest tag `v1.3.0-rc.1` is 2026-07-24). | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Closed on the maintainer's own decision, which supersedes D-2. THIS PLAN's scope is UNCHANGED: it still retires only the spec 4.2 sentence, because loosening a gate needs its own scope and review. The removal is carried by backlog item `aagh7v`, filed 2026-09-10 with the measurement and the standing prohibition on presence-based inference (`host_sandbox_profile.py:88-95`). E-04/V-04 remain written as PRESERVE and MUST be re-pointed or retired by whoever executes this plan. |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-6 | Round 1's D-2 recommended PRESERVE and recorded it `Reversible: no`. The maintainer then ruled REMOVE. Does closing PR-002 require re-opening the plan's scope? | NO. Close PR-002 as `FIXED` on the maintainer's ruling and leave this plan's scope at the spec-sentence retirement; the capability removal is carried by a separate backlog item. | (a) Absorb the removal into this plan, rejected because it would loosen a live-looking gate inside a plan whose declared purpose is correcting a document, evading the review a safety change deserves and forcing edits to five pinning assertions. (b) Leave PR-002 `OPEN` since the ruling contradicts the recommendation, rejected because the finding's question is settled and a permanently-open finding gates the plan forever on a decision that has been made. | The maintainer's 2026-09-10 `/askme` answer recorded verbatim in plan OQ-02; the falsified premise re-measured at HEAD `8dffd7a0` (no consumer of `ACTION_REQUIREMENTS`); backlog `aagh7v` carries the removal with its own scope. | yes |
| D-7 | An unverified citation was used to support round 1's PRESERVE recommendation: `mjx7ne` OQ-03 as "KEEP ALL THREE capabilities, honestly labelled". Does it stand? | WITHDRAWN. It must not be cited as authority by any successor, and D-2's basis is weakened accordingly. | Leaving the citation in place unremarked, rejected because the maintainer stated they did not recall being asked it and no plain-language record of that question was found, so treating it as settled authority would launder an unverified attribution into a security decision. | The maintainer's reply of 2026-09-10 ("I don't remember this. Did you call it something chock full of jargon and I accidentally agreed to something I did not understand?"); no plain-language attestation located for `mjx7ne` OQ-03. | yes |
