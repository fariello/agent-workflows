# Review: Migrate every spec into status subdirs and make location agree with status

- Subject-Id: 1bdxcp
- Subject-Type: ipd
- Reviewed-At: 2026-09-10
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS

## Round 1

Reviewed at HEAD `2c3e93d0`. The target plan was committed and unchanged, so the pre-review snapshot was
correctly skipped. `aw ipd lint` conformed at `--phase author` before semantic review; at
`--phase review-finalize` the only findings are the two IPD-Q501 blocking-question gates this review
deliberately added, which is the same shape the parent orchestrator `wfjsp4` currently carries.
`aw sanitize --agent` clean.

THE PLAN'S THESIS IS SOUND AND MEASUREMENT STRENGTHENED IT. Specs really are the only lifecycle-bearing
type not partitioned, and the case is now unambiguous rather than nearly so: `reviews` carries a
`- Status:` on ZERO of 161 files (the plan claimed 1 of 81), `roadmaps` 0 of 1, `walkthroughs` 2 of 17,
while all 29 specs carry one over a real nine-value vocabulary. The Order 01 dependency is a genuine
correctness edge (`_spec_files` uses a flat `glob("*.md")`, `specs.py:89`), and OQ-01's
one-directory-per-status ruling is well argued.

TWO BLOCKERS PREVENT EXECUTION, both maintainer decisions and both matching blockers the PARENT
orchestrator already escalated (it is itself `Readiness: no-go` on the identical pair, so this child
could not be cleared ahead of it in any case). The plan teaches ONE of three spec writers where a file
belongs, and it falsifies an approved spec it does not declare. Six further findings were fixed in
place, including a resolved open question whose stated justification turned out to be false.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-201 | BLOCKER | UNDER-SCOPE | C. Architecture (duplicate paths); G. Executability | `cli.py:11270-11287`; `status_set.py:822-863`; `specs.py:498,550-553,563-568`; three throwaway-repo probes | The plan fixes ONE of THREE spec writers, so the invariant it establishes decays on the next write. `aw specs set <status> <selector>` (no `--status`) routes to `status_set.run_set_command`, whose relocation block covers `("plans","prompts")` and `backlog` and has NO `specs` branch; PROVEN, it reported `draft -> to-review` and LEFT the file in `.aw/records/specs/draft/`. `aw specs new` previews a path at the FLAT ROOT with `- Status: draft`. The plan names only the forked `specs.run_set` and declares only `agent_workflows/specs.py`. This is the dual-dispatch bypass the module warns about twice in its own comments. | C:Med-High; U:Low; S:Low; F:Med-High; Overall:Medium-High | OPEN | Escalated as OQ-04 (`Blocking: yes`, `Finding: PR-201`). Not fixed on review authority: the choice between adding a `specs` branch to `status_set` and routing specs through the forked setter touches a twice-bitten dual dispatch AND changes this plan's `Scope-Paths`, i.e. its own scope. E-03 now carries the measurement and a do-not-guess instruction; V-03 demands dual-spelling evidence. Parent `wfjsp4` OQ-04 recommends a separate writer child. |
| PR-202 | BLOCKER | UNDER-SCOPE | G. Executability (spec sync) | `.aw/records/specs/20260901-kw5y2s-...spec.md:104` (`- Status: approved`); `layout.py:167-173`; `tests/test_layout.py:182-192` | The spec-sync section asserted "No spec amendment is expected". FALSE: the approved `kw5y2s` record-class table states the `specs` row is `Single directory; frontmatter status tracking`, which this migration falsifies. The code agrees with the spec, not the plan: `specs` has no `lifecycle_subdirs` and the layout test asserts modeled-versus-live subdirs for `backlog`/`plans` only. No path in `Scope-Paths` permits the amendment. | C:Low; U:Low; S:Low; F:Med-High; Overall:Medium-High | OPEN | Escalated as OQ-05 (`Blocking: yes`, `Finding: PR-202`). Amending an approved spec changes the contract every other plan is reviewed against, which is a maintainer call, and the declaration must exist BEFORE the edit. Spec-sync section rewritten to state the requirement and name the three files an amendment needs; V-04 demands proof the declaration preceded any edit. Parent `wfjsp4` OQ-05 recommends amending within the Set. |
| PR-203 | HIGH | IN-SCOPE | A. Correctness; C. Architecture (canonical mechanisms) | throwaway-repo `git log --follow` probe; `backlog.py:603-605`; `status_set.py:899-907`; `artifact_core.py:135` | OQ-02 resolved to `git mv` on a premise measured FALSE ("a rewrite-and-delete would make `git log --follow` stop at the migration"): `--follow` traversed a rewrite-and-delete and reported both sides, since git infers renames from content similarity at log time. Worse, it contradicted the plan's own instruction to follow `aw backlog set` "exactly": NO status setter uses `git mv`; both `backlog.run_set` and `status_set.apply_status_change` `atomic_write` then `unlink`. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | OQ-02 reversed to the setter precedent with the falsification recorded; E-03's contradictory `git mv` instruction replaced and the fail-safe ordering (write-dest-then-unlink) noted; execution contract updated; V-03 requires the precedent. |
| PR-204 | MEDIUM | IN-SCOPE | A. Correctness (measurement) | tracked-file census at review | The citation blast radius was understated by an order of magnitude. The plan called `wenmg4` "a known instance"; re-measured, **18 plans** declare a literal spec path in `Scope-Paths` (10 live, 8 terminal), across **176 citation lines in 101 tracked files** naming 41 distinct spec filenames. An executor budgeting for one repair would badly under-scope. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Counts added to Concern, E-02, F-5 and V-02 with a measure-locally instruction. Two new hazards named: the 9 test-file spec paths are SYNTHETIC fixtures that must NOT be rewritten (verified they name nonexistent specs), and 8 declaring plans are terminal receipts whose recorded scope arguably must not be revised; E-02 must record the rule and V-02/V-04 must show what was deliberately left alone. |
| PR-205 | MEDIUM | IN-SCOPE | A. Correctness (mechanism claim) | `ipd_lifecycle.py:1600-1607`, `:1757-1761`, `:2499` | The plan asserted three times that a vanished declared path "would fail its finalize scope gate". Overstated: it becomes an `in_scope_unmodified` entry, which is ACKNOWLEDGE-AND-PROCEED via `--scope-ack`, fail-closed only in a HEADLESS run with no answer supplied. The true cost is friction on 10 live plans plus a false "declared but never touched" audit claim. Left uncorrected, it mis-prioritizes the citation work and would make a reader distrust the plan's other severity claims. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Corrected in the Concern and F-5, keeping the hazard as real friction rather than a gate failure. |
| PR-206 | MEDIUM | IN-SCOPE | E. Testing and verification | bare `python3 -m pytest`; `git ls-files opencode-recovery` empty | The suite baseline was wrong in count AND named a test file that does not exist (`tests/test_orchestrator_retirement.py`). Measured: `1 failed, 5958 passed, 3 skipped, 2 xfailed` against the plan's `1 failed, 5648 passed`. The one real failure trips on 189 files in an UNTRACKED, git-ignored `opencode-recovery/` directory, which invites an executor to "fix" it by deleting another party's work. The "roughly 32 further failures in a lane" claim is unsupported at HEAD. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-06 and Required tests carry the re-measured baseline, name the real failure, add an explicit do-not-delete instruction for the untracked directory, and tell the executor to establish its own baseline. Also corrected the assumption that a spec-path test break implies a missed citation. |
| PR-207 | MEDIUM | IN-SCOPE | A. Correctness (measurement) | recursive census at review | Every count is stale, including the TITLE's. 29 specs not 28; 13 live not 12 (`approved` is 7, not 6); `prompts` has 6 subdirs not 5; the flat-type census moved materially (`reviews` 0 of 161, not 1 of 81). The thesis strengthens, but no figure should be cited as written. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Title generalized to "every spec"; counts corrected in Concern, conventions, E-02, F-15 and V-02, each with a measure-locally instruction. |
| PR-208 | LOW | IN-SCOPE | E. Testing (observability of a criterion) | `aw specs check` human vs `--agent` output at review | V-06 and Required tests demanded `aw specs check`'s "examined count", but the human output prints NO count (only `all specs conform.`), so the criterion was unobservable on the surface named. The count exists only in `--agent`'s `checked` field (29 at review). | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | V-06 now points the criterion at `--agent`'s `checked` field and states the human output carries no count. E-02 also told to enumerate recursively so an Order-01-visible spec is not missed. |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-201 and PR-202 duplicate blockers the parent orchestrator `wfjsp4` already escalated. Should this child restate them, or defer to the parent's OQ-04/OQ-05? | Restate them as this child's OWN blocking OQ-04/OQ-05, cross-referencing the parent's. | Cite the parent only and leave this child clean; treat them as already-escalated and non-blocking here. | The lint gate reads the plan it is given: a blocking question recorded only on the parent does not stop THIS plan at its own checkpoints, and `check.review-finding-unescalated` requires a finding at or above the gate threshold left OPEN to be raised as a blocking question in the reviewed plan. Both defects are also specifically about THIS plan's `Scope-Paths` and E-items, so the child is where an executor reads them. | yes |
| D-2 | Should I author the writer fix (or the spec amendment) myself, given both are mechanically small? | No. Escalate both and revise the plan's text only. | Add `status_set.py` to `Scope-Paths` and write the branch; amend `kw5y2s` and `layout.py` directly. | Both are scope decisions on the maintainer's own accepted trade, and the amendment edits an APPROVED contract that this plan does not declare, which the spec-amendment rule exists to prevent. The writer choice additionally touches a dual dispatch documented as twice-bitten (`specs.py:550-553`, `:563-568`), so picking a shape on review authority would preempt a design decision with a real failure history. The parent's review reached the same conclusion independently. | yes |
| D-3 | OQ-02 was already `Status: resolved`. May a reviewer reverse a resolved open question? | Yes, reverse it in place, recording the falsification and keeping the surviving motivation. | Leave the resolution and note the objection elsewhere; escalate it as a new blocking question. | Its stated justification was measurably false (`git log --follow` traverses a rewrite-and-delete) and it contradicted the plan's own "follow `aw backlog set` exactly" instruction, so leaving it would have shipped a mechanism divergence on a false premise. It does not need the maintainer because the repository precedent already answers it uniformly (every status setter uses `atomic_write`+`unlink`), so this is applying an existing convention, not making a new call. | yes |
| D-4 | Should the citation rewrite cover the 8 terminal plans and the 9 synthetic test fixtures? | Do not prescribe the outcome; require E-02 to RECORD a rule and V-02/V-04 to show what was left untouched, with the defensible default stated (repair live artifacts, leave terminal receipts and synthetic fixtures alone). | Mandate rewriting all 176 lines; mandate skipping all non-plan citations. | Rewriting an executed plan's recorded `Scope-Paths` revises a historical receipt of what that execution declared, which the house rules treat as not-to-be-revised-in-place, and rewriting synthetic fixture paths would corrupt tests that name specs which never existed (verified). But whether a terminal record's prose citation should be repaired for navigability is a judgement with legitimate answers either way, so the plan should force an explicit, evidenced choice rather than my picking one. | yes |

## Round 2

Round 2 exists ONLY to close the finding(s) below, whose escalated question(s) the maintainer answered on
2026-09-10. It re-critiques nothing: every other round-1 finding was already `FIXED` and is superseded
unchanged.

WHY IT IS NEEDED: the escalation contract (`plan-review.md:335-341`) defines the path INTO a blocking
question and no path back, so an answered question leaves its finding reading `OPEN` forever while
`subject_gating_blocks` keeps refusing the plan on a decision that has been made. Appending a round is
the sanctioned mechanism, since `ReviewDocument.current_findings` reads only the LAST round
(`review_findings.py:236-243`). This is the SECOND such cleanup in one session; the durable fix is plan
`qhy3i3` E-07, which is authored and awaiting approval.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-201 | BLOCKER | UNDER-SCOPE | C. Architecture (one of two setter spellings) | plan OQ-04 (`- Status: resolved`, `- Finding: PR-201`) | Carried forward from round 1 and now CLOSED by an answer that SUPERSEDES the question's framing. It asked which of two `aw specs set` spellings this plan owns; the maintainer answered a level up: NEITHER owns placement. One shared library, called by every setter AND every creation verb, adopted per type. So this plan becomes an ADOPTER and the divide-the-writers question dissolves. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Closed on the maintainer's decision. This plan's own evidence supported the wider answer: the dual dispatch it cites is documented in place as having bitten this codebase twice (`specs.py:550-553`, `:563-568`), both times fixed by consuming ONE shared predicate. Its third-writer measurement stands: `aw specs new` places at the flat root, so the invariant breaks at CREATION even with both setters fixed. |
| PR-202 | BLOCKER | UNDER-SCOPE | B. Contracts (approved spec, undeclared path) | plan OQ-05 (`- Status: resolved`, `- Finding: PR-202`) | Carried forward from round 1 and now CLOSED, identically to the parent orchestrator's PR-002 which the maintainer answered in the same round. Ruling of 2026-09-10: amend `kw5y2s` within the Set with the path declared up front. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Closed on the maintainer's decision. WHICH plan carries the edit is now settled by PR-201's answer: the amendment belongs with the LIBRARY work, not with this migration child, so two plans in one Set do not both edit an approved contract. Re-verified the spec row, `layout.py`'s missing `lifecycle_subdirs` for specs, and that `tests/test_layout.py:182-192` covers backlog and plans only, so extending that test is part of the amendment. |
