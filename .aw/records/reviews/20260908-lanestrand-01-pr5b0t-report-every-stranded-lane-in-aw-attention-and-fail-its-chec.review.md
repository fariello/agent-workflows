# Review: report every stranded lane in aw attention, child pr5b0t (Set lanestrand)

- Subject-Id: pr5b0t
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `930cfa5b`. `aw ipd lint --phase author` CONFORMING before semantic review and
`--phase review-finalize` CONFORMING after every revision, so nothing in this round is structural.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW, and it was authored
INSIDE A LANE WORKTREE where `.aw/records/runs/` is gitignored and absent. Its value rests on
re-measuring from the PRIMARY checkout, which is what exposed both blockers. Twelve things were RUN
rather than read: every grep count in both attention modules; the exhaustive `integration_signal`
occurrence set; `SCAN_ROOTS`; the seven driver write sites; `aw next --check` and `aw attention --check`
unpiped; the `aw/lane/*` branch count and `git worktree list`; all 140 run records scanned for absolute
`preserved_worktree` values; `worktree_lease.inspect_lane` CALLED on a purpose-built fixture before and
after a merge; the same fixture rebuilt a second time after the first construction proved unfaithful to
the runner; `render_json` and all four `--check` return paths traced to `drift_exit_code`; the attention
spec read for `--check` clauses; and the front matter of `ys1dor`, `rl67b0`, `51vw4y` and `6sb3yu`. TWO
of those produced BLOCKERS that invert instructions the plan gave its executor.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-601 | BLOCKER | IN-SCOPE | A. Correctness; D. Domain invariants | `worktree_lease.py:189-190`, `:231-255`, `:308-315`; fixture run before/after merge | **THE PREDICATE AS SPECIFIED CANNOT DISTINGUISH A STRANDED LANE FROM A RECOVERED ONE, AND WOULD REPORT EVERY INTEGRATED LANE FOREVER.** E-02 says "BUILD ON `describe_lane`... It already answers `commits_ahead`, `holds_work` and `owner_live`, which is most of E-01's predicate", and E-01 treats reachability as "at most one git question" (optional). Measured: `holds_work` is `state == LANE_HOLDS_WORK`, and `commits_ahead` is `rev-list --count <base_sha>..<head>` where `base_sha` is the commit the lane was CUT FROM, read from the creation reflog. That never compares against `main`. On a fixture built as the runner builds a lane (`git worktree add -b`), after merging into `main` with `--no-ff`, `inspect_lane` still returns `state HOLDS-WORK commits_ahead 1 holds_work True reclaimable False`, while `git merge-base --is-ancestor` correctly reports it landed. So a `holds_work` predicate fails E-07 case (c) for every recovered lane, which is the exact false-positive class the item says trains operators to ignore the alarm, and this repo has 29 lane branches including already-integrated ones. | C:Low; U:High; S:Low; F:High; Overall:Medium | FIXED | Added F-6b (BLOCKER) with the pasted experiment. E-01 now states the predicate as TWO named questions and declares the reachability check REQUIRED, naming the target and fallback; E-02 must ADD it and record in the docstring why `holds_work` is insufficient; E-07 case (c) must be built runner-faithfully with the construction stated; V-01 requires the before/after `inspect_lane` demonstration; V-02 requires the added call be shown; a conventions bullet and the gate carry it. |
| PR-602 | BLOCKER | IN-SCOPE | B. Security and privacy | 71 of 140 run records carry `/home/<user>/...` in `preserved_worktree`; `ys1dor` F-11 | **E-04 MANDATED PRINTING AN ABSOLUTE HOME PATH INTO A PAYLOAD AGENTS PASTE.** It required "the four facts the item names: the lane branch, the worktree path, the `integration_signal`, and the plan id6". Measured across the live records, 71 items carry an absolute `preserved_worktree`. `ys1dor`'s review measured the SAME field independently, FORBADE printing it, required a repository-relative rendering, and made `aw sanitize --agent` over a captured render its load-bearing evidence; `integration_detail` embeds an absolute repo path too. `aw attention --json` is consumed by CI and pasted by agents, and `Item.path` is contractually "repo-relative POSIX", so an absolute worktree breaks both D92 and the payload's own field contract. The plan even flagged the sanitizer as mattering "more here than usual" while still instructing the leak. | C:Low; U:Low; S:High; F:Low; Overall:Medium | FIXED | Added F-15 (BLOCKER). E-04 now forbids printing `preserved_worktree` as recorded, requires branch plus a repo-relative or omitted worktree, and extends the rule to detail text; E-03 notes the `Item.path` contract as an independent reason; E-07 gains case (h), a no-absolute-path regression test on a fixture whose recorded value IS absolute; V-04 requires a `/home/` grep over both captured surfaces plus `aw sanitize`; the Concern, conventions and gate all carry it. |
| PR-603 | HIGH | IN-SCOPE | C. Architecture (two questions that are one) | `attention.py:1073`, `:2674`, `:2746`, `:2749`, `:2958`; `artifact_core.py:405-415` | **`valid` AND THE `--check` EXIT CODE ARE THE SAME MECHANISM, WHICH ALREADY ANSWERS E-05's AND E-06's OPEN DESIGN QUESTIONS.** E-05 said to "decide and record how the two interact" and E-06 treated the exit change as new work. Measured: `render_json` sets `valid = len(drift) == 0`, and EVERY `--check` return is `core.drift_exit_code(drift)`, which is `1 if any(d.severity != "info")`. So emitting the lane as a non-`info` `Drift` yields the honest `valid: false` AND the nonzero exit with no change to exit logic at all, while an `Item`-only design yields NEITHER and would tempt a second exit path (a second definition of validity, which the plan's own one-reader constraint forbids). | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-16. E-05 now states the mechanism and requires the choice be recorded with a stable rule id per spec F4; E-06 states the nonzero exit needs no new exit path and that `info` would not fail; V-05 requires `valid: false` shown with its guaranteeing mechanism named; V-06 requires the exit be shown to come from the existing helper. |
| PR-604 | HIGH | IN-SCOPE | G. Plan executability; spec obligations | spec G3, `:134`, `:201`, F3; `- Status: implemented` | **OQ-02 WAS LEFT OPEN FOR THE MAINTAINER WHEN THE SPEC THE PLAN CITED ANSWERS IT.** The plan took position (a) (no spec change) and told E-06 to "STOP and add the spec path" if it found otherwise. Read: the attention spec is `implemented` and governs `--check` in four places, including F3's EXHAUSTIVE enumeration of failure conditions, all of them per-artifact contract violations in tracked trees. A stranded lane is none of those, so E-06 adds a condition to a closed list in an implemented spec, which the repository rule says must be declared and amended in the same change. Leaving it open would have stranded a resolvable question and risked an undeclared spec edit at execution time. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-17. OQ-02 RESOLVED with the quoted clauses; the spec path DECLARED in `Scope-Paths`; the spec-sync section rewritten to say what the amendment must and must not do (additive to F3, no weakening, READ-ONLY language untouched, and the class-mapping half needs no spec change); V-06 requires the diff; the gate announces the declared spec edit. |
| PR-605 | HIGH | IN-SCOPE | Evidence accuracy (a premise stale in the risky direction) | `rl67b0`, `51vw4y` front matter; `aw find plans 6sb3yu` | **`rl67b0` IS NOT `no-go`, AND THE PLAN LEANS ON THAT CLAIM THREE TIMES.** E-02, F-9, the conventions and OQ-03 all state it reads `Readiness: no-go` and that "the chain is `51vw4y` -> `rl67b0`" is blocked. Measured: `rl67b0` is `Status: reviewed` / `Readiness: go-pending-approval`, `51vw4y` likewise, and `51vw4y`'s own dependency `6sb3yu` is already `executed`. So the chain awaits human APPROVAL, not unblocking, and `rl67b0` may land BEFORE this plan. The convergence design remains right, but the risk inverts: an executor told the resolver will not exist may build a local one that immediately duplicates a shipped one. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | F-9 rewritten with the measured statuses; E-02 corrected and told to prefer `rl67b0`'s resolver if it has landed; conventions and the gate's division-of-labour paragraph corrected; OQ-03's reasoning updated. `Item-Dependencies: none` retained as still correct. |
| PR-606 | MEDIUM | IN-SCOPE | A. Correctness (every citation into three modules) | symbols re-located by name | **EVERY LINE NUMBER INTO THE DRIVERS AND `runner_shared.py` DRIFTED WITHIN ONE DAY.** `preserved_*` is at `oc_runipd.py:6893-6900` (cited `:6880-6887`) and `agy_runipd.py:3936-3943` (cited `:3863-3870`); `integration_signal` at `:6688-6690` (cited `:6675-6677`) and `:3742-3744` (cited `:3669-3671`); `_lane_records_from_state` `:609` (cited `:512`), `describe_lane` `:651` (cited `:554`), `format_lane_report` `:682` (cited `:585`), `build_recovery_lane_notice` `:759` (cited `:662`); `attention.run` `:2457`, the valid messages `:2498`/`:2716`, `get_active_runs_map` `:1768`. The plan's own gate tells the executor to re-locate by name, so its citations were the natural place to check. `SCAN_ROOTS`, `class_of`, `CLASS_MAPS`, `UnknownNativeStatus`, `scan`, `_record_for` and `render_json` were all correct. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All citations corrected throughout (Concern, E-items, findings, conventions, Scope check). The gate's re-locate instruction now cites the plan's OWN drift as evidence for itself. |
| PR-607 | MEDIUM | IN-SCOPE | A. Correctness; F. Prevent silent failure | `attention.py:2480-2498` | `attention.run` has an EARLY RETURN that prints "aw attention --check: the view is valid." and returns 0 BEFORE any scan, when the directory is not a project dir. E-06 changes "the check path" without mentioning it, so a lane check added only to the scanning path would leave a route that reports a clean view having looked at nothing, which is the same silent-OK the plan forbids for the UNKNOWN case. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-06 now names the early return and requires an explicit decision about whether the lane check applies there; V-06 expands from four states to five and requires that path's behavior pasted; a conventions bullet records it. |
| PR-608 | MEDIUM | IN-SCOPE | A. Correctness (a three-valued field read as boolean) | `worktree_lease.py:286-287` | `owner_live` is `Optional[bool]`, set to `None` when no owner record exists. E-01 says to reuse it for the live-run exclusion without noting that, so a boolean reading would classify an UNKNOWN owner as "not live" and report the lane, which is the false positive E-01 exists to prevent. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Added F-18. E-01 states the three-valued handling, its expected outcome requires it, E-07's UNKNOWN case gains the `owner_live is None` variant, V-01 requires the handling stated, and a conventions bullet records it. |
| PR-609 | MEDIUM | IN-SCOPE | E. Testing (a fixture that can pass for the wrong reason) | the two fixture constructions run at review | **THE MOST IMPORTANT TEST CASE CAN PASS WHILE THE PREDICATE IS BROKEN, DEPENDING ON HOW THE FIXTURE IS BUILT.** Review's first attempt created the lane with `git branch <name>` from a branch NAME, which writes `branch: Created from main`; `_lane_base_sha` then resolved the base to the branch's CURRENT tip and `commits_ahead` read 0, so a `holds_work` predicate would have "passed" case (c) for a reason unrelated to correctness. Rebuilding as `git worktree add -b <branch> <path> <base_sha>`, which is what the runner does, produced `Created from <sha>` and exposed the real behavior. E-07 gave no construction guidance. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-07 case (c) now specifies the runner-faithful construction, explains why the alternative passes wrongly, and requires the construction be stated; V-07 requires the construction commands pasted. |
| PR-610 | MEDIUM | IN-SCOPE | E. Testing; G. Executability (an apparent contradiction) | E-07 cases (c) and (f) | Cases (c) "a merged-and-executed lane is NOT reported" and (f) "the HISTORICAL run still reports what it did at the time" look contradictory, and the plan never reconciles them. An executor resolving the tension the wrong way would break one of the two properties that matter most, and a later reader would suspect a bug. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-07 now states both are correct because they answer different questions and requires the distinction be visible in the test NAMES; V-07 requires a one-sentence statement of why they do not contradict. |
| PR-611 | LOW | IN-SCOPE | Evidence accuracy; A. Correctness | `render_json:1069-1098`; `ys1dor` OQ-02, F-10; measured counts | Four smaller inaccuracies, each cheap to fix and each capable of costing an executor a wrong turn: (1) `render_json` pins a CANONICAL shape and carries `schema_version`/`mapping_version`, so E-05's "add a field" is a versioned schema change needing a decision; (2) `integration_detail` is on the ATTEMPT not the item (`ys1dor` F-10), so E-01's field list reads as if it were on the item; (3) `ys1dor`'s outcome word is already RESOLVED as `STRANDED` and that plan is `reviewed`, so E-04's "if it has NOT landed, pick one" presents a decided question as open; (4) the branch count is 29 not 28, the lane-token line count 8 not 7, and the plan cited `ys1dor:98`/`:101` where the text is at `:127`/`:130`. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Added F-19. E-05 requires the version decision; E-01 notes the attempt-level field; E-04 and the conventions state `STRANDED` is decided; all counts and `ys1dor` line references corrected; the Concern marks the counts perishable. |
| PR-612 | LOW | IN-SCOPE | E. Testing (a wrong baseline and a lane-only caveat) | bare suite run in the primary checkout | The suite baseline said "roughly 32 failures are environmental" inside a lane, with no primary-checkout figure, so an executor in the main tree had nothing to compare against. Measured in the primary checkout: `1 failed, 5919 passed, 3 skipped, 2 xfailed`, the failure being `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, environmental, caused by 189 untracked `opencode-recovery/*.md` files belonging to another party in this shared checkout. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Validation now carries the measured primary-checkout baseline with its named environmental cause, keeps the lane caveat, and requires the executor to state which tree it ran in; the gate warns not to stage the untracked recovery files. |

CONFIRMED SOUND, AND THE PLAN'S RESEARCH IS UNUSUALLY GOOD. The gap is real and every structural claim
about it reproduced exactly: zero lane tokens in both attention modules, `integration_signal` with
exactly five occurrences (four writes plus one allowlist name), `SCAN_ROOTS` excluding both
`.aw/worktrees` and `.aw/records/runs` at the cited lines, `scan()` being file-shaped, and both
`aw next --check` and `aw attention --check` exiting 0 with 29 lane branches present. The plan's best
judgement is its F-6 correction of its own backlog item (a lane reader already exists, which SHRINKS the
work), and its second best is refusing to add `.aw/worktrees` to `SCAN_ROOTS` on `xtklpd`'s measured
history-rewriting ruling. The division of labour with `ys1dor` and `rl67b0` is correctly drawn, the
deferral of `aw lanes` is properly reasoned rather than an oversight, and the fixtures-only fence was
already right and is now stronger.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. No E-item was added or removed; the E/V bijection is
unchanged at seven. What changed is that the PREDICATE now asks two questions instead of one, the
report no longer leaks a home path, `--check` and `valid` are one mechanism rather than two decisions,
and the spec amendment is declared rather than left to an execution-time discovery. Both blockers were
found only by executing code, and both would have shipped a working-looking feature that either cried
wolf on every recovered lane or leaked the maintainer's home directory into every agent's payload.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | OQ-02 asked whether `--check` failing on a lane changes a spec-governed contract, and the plan left it for the maintainer. Resolve it or leave it open? | RESOLVED from the spec: it DOES, so the spec file is declared in `Scope-Paths` and E-06 amends F3 additively. | Leaving it open (the spec answers it, and plan-review must not ask the human what the repository already answers); taking the plan's position (a) that no spec change is needed (contradicted by F3's exhaustive enumeration in an `implemented` spec). | spec `20260808-1945-01` G3, `:134`, `:201`, F3, `- Status: implemented`; AGENTS.md spec-amendment rule | yes |
| D-2 | The predicate needs a landing question `describe_lane` does not answer. Extend `describe_lane`, or add a helper beside the predicate? | Left to the executor as an explicit either/or, requiring the choice and its reason be recorded in the docstring. | Mandating a `describe_lane` change (it is consumed by both runners' report paths, so widening it is a bigger blast radius than this plan's fence warrants); mandating a local helper (would foreclose the cleaner option if `rl67b0`'s resolver lands first). | `runner_shared.py:651` call sites; `rl67b0` E-01 overlap; the plan's one-reader constraint | yes |
| D-3 | The item asks for "the worktree path" as a reported fact, but the recorded value is an absolute home path. Print it, omit it, or transform it? | Repository-relative rendering, or omission, with the branch as the primary identifier. | Printing as recorded (D92 leak into an agent-pasted payload; 71 of 140 records affected); omitting the worktree entirely (loses a fact the item asked for when a safe form exists). | `ys1dor` E-03/F-11 ruling on the same field; `attention.py:39` `path` contract; AGENTS.md leak rule | yes |
| D-4 | Should `Scope-Paths` gain `worktree_lease.py`, since E-01/E-02 must understand `_lane_base_sha`? | No. Declared as read-only context in the Scope check instead, with the reachability question sited in `runner_shared`. | Declaring it (would invite an edit to lease internals both runners depend on, for a question that belongs beside the predicate); saying nothing (would leave an executor unsure whether reading it is in bounds). | `worktree_lease.inspect_lane` consumers; the plan's own fence rationale | yes |

## Commit result

- Pre-review snapshot: skipped, target plan committed and unchanged
- Hardened result: see the `plan-review: harden lanestrand-01 pr5b0t (revisions applied)` commit
- Push: not performed
