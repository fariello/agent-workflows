# Review: emit a refusal record when an orchestrator is deferred, child zyw4n3 (Set runghostid)

- Subject-Id: zyw4n3
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `2093b9d1`. `aw ipd lint --phase author` CONFORMING before semantic review, and
`--phase review-finalize` CONFORMING after all revisions, with zero findings at both checkpoints; nothing
in this plan was structural, and the pre-existing OQ-01 was non-blocking and is now resolved.

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Its value therefore rests
on EXECUTING the plan's claims rather than re-reading them. NINE things were run rather than inspected:
`dispatch_orchestrator_item` was DRIVEN on a purpose-built two-plan temporary repository to reach the
RECONSIDER branch, with the resulting item keys and event payload dumped; the same function was driven to
reach TERMINATE; both reason-constant blocks were printed and their value sets differenced both ways;
`render_run_summary_table` was rendered on a `queued` orchestrator; `render_stream`'s first-party imports
were AST-walked; every consumer of the two bespoke refusal fields was grepped across `agent_workflows/`
and `tests/`; both hosts' `## Dependency blocks (why)` implementations were located and their status gate
read; `evaluate_backlog_close` was run for `i2fjf8` and its carrier set grepped; and the full bare suite
plus the plan's named module were run.

SCOPE: only this child was a candidate. Read as evidence: `runner_shared.dispatch_orchestrator_item`
(`:3258`, its RECONSIDER branch `:3356-3372`, its TERMINATE tail `:3375-3403`), `decide_orchestrator_dispatch`
and its translation branches (`:3136-3168`), the `ORCH_REASON_*` block (`:3031-3042`), the
`RETIRE_REFUSED_*` block (`:2555-2558`), the `render_stream` import at `:147`;
`render_stream.render_run_summary_table`'s diagnostics block (`:2152-2178`); both hosts' dispatch call
sites (`oc_runipd.py:7386`, `agy_runipd.py:4398`) and both `## Dependency blocks (why)` copies
(`oc_runipd.py:3222`, `agy_runipd.py:2152`); `oc_runipd.process_backlog_close` (`:1426`);
`tests/test_orchestrator_retirement.py:3295-3312`; `tests/test_run_viewer.py:1-30`; sibling plans
`r2i1b1`, `m7gvuz`, `8tgg6g`, `yeh7gc`; backlog `i2fjf8`; and release `f33nrj`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-901 | HIGH | IN-SCOPE | A. Correctness (an enumeration that is missing a whole vocabulary) | both constant blocks printed and differenced; `decide_orchestrator_dispatch` translation branches read at `:3136-3168` | **A SECOND REASON VOCABULARY EXISTS AND E-02 ENUMERATED ONLY ONE OF THEM, WITH A NEAR-MISS PAIR BETWEEN THEM.** `RETIRE_REFUSED_*` (4 values, `:2555-2558`) is what `evaluate_set_retirement` produces and `decide_orchestrator_dispatch` CONSUMES, translating it into `ORCH_REASON_*` (7 values) before any item field is written. Measured, three values are shared verbatim (`no-children`, `no-orchestrator`, `unauthored-child-rows`) and one pair differs only in word order: `RETIRE_REFUSED_UNFINISHED_CHILDREN` is `unfinished-children` while `ORCH_REASON_UNFINISHED_CHILDREN` is `children-unfinished`. A dict-keyed mapping that assumed one vocabulary, or keyed the wrong one, would silently miss and fall through to exactly the generic string E-02 forbids. The plan says "seven reasons" as though the space were closed at seven. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-02 now names all seven values explicitly, records the second vocabulary with its shared values and the near-miss pair, requires the mapping's keyed vocabulary to be STATED, and requires the `RETIRE_REFUSED_*` -> `ORCH_REASON_*` translation to be ASSERTED rather than assumed (it is correct today, verified, but a future forwarding branch would degrade the mapping silently). E-02 also now requires an unknown code to be handled deliberately and never as an empty remedy. V-02 demands both value sets pasted plus the unknown-code behavior. F-13 added. |
| PR-902 | HIGH | IN-SCOPE | C. Architecture (an import direction the plan could violate by accident) | `runner_shared.py:147` (`from agent_workflows.render_stream import ...`); AST walk showing `render_stream` has ZERO first-party imports; `r2i1b1` E-01 | **THE RECORD THIS PLAN WRITES IS DEFINED IN `render_stream.py`, WHICH THIS PLAN DOES NOT DECLARE, AND MOVING IT WOULD CREATE A CIRCULAR IMPORT.** `r2i1b1` E-01 sites the dataclass in `render_stream` for a mechanical reason: `runner_shared` already imports `render_stream` at module level and `render_stream` imports no first-party module, so the reverse edge cannot exist. This plan says only "use `r2i1b1`'s shared constructor" without saying where it lives, so an executor who reaches for it in `runner_shared` (the file they ARE editing, and the file the plan's own Step 0 associates with shared runner logic) finds nothing and would plausibly define or re-export it there, breaking the property `r2i1b1` E-08 asserts. Importing it is legal and needs no new declared path; defining it does not. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-01 now states where the record lives, why, and that importing is legal while defining or re-exporting in `runner_shared` is forbidden, with a STOP instruction if `render_stream.py` appears to need editing. V-01 requires the import line plus a grep proving no local definition, and declares a locally-defined look-alike a FAILED validation. E-04 gained an identity assertion against the imported type. Step 0 and the gate carry the rule; F-14 added. |
| PR-903 | MEDIUM | IN-SCOPE | D. Anti-regression (a "no consumer" claim that is false in a way that matters) | grep for `orchestrator_refusal_reason`/`orchestrator_refusal_detail` across `agent_workflows/` and `tests/`: `tests/test_orchestrator_retirement.py:3310-3312` asserts both | THE BESPOKE FIELDS HAVE A LIVE CONSUMER, SO "GREP SHOWS NO SURFACE READS" IS WRONG AND THE RIGHT CLAIM IS NARROWER. E-03 keeps the fields on the reasoning that "a consumer outside this grep may exist", a hedge; measured, a consumer exists INSIDE the repository, in the very module E-03's change is most likely to disturb. The conclusion (keep them) is unchanged and in fact better supported, but the plan's evidence understated the risk of renaming and gave no instruction to run that module. Note the same test pins the field to a `RETIRE_REFUSED_*` constant whose value coincides with its `ORCH_REASON_*` twin, which is PR-901's aliasing hazard appearing in a test assertion. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-03 rewritten to state that no RENDER surface reads them while a test does, citing the node, and to note the coincidental constant. V-03 now requires `tests/test_orchestrator_retirement.py` pasted GREEN before and after, with the statement that any failure there is the executor's own. Step 0 and F-8's neighbourhood corrected; F-15 added. |
| PR-904 | MEDIUM | IN-SCOPE | E. Testing (a named known failure that does not exist) | bare suite: `1 failed, 5929 passed, 3 skipped, 2 xfailed`; `tests/test_orchestrator_retirement.py` alone: `112 passed`; grep for the named test: no match anywhere in `tests/` | **THE PLAN TELLS THE EXECUTOR TO EXCUSE A FAILURE IN THE ONE MODULE MOST LIKELY TO CATCH THEIR MISTAKE, AND THAT FAILURE DOES NOT EXIST.** It states the baseline as `1 failed, 5648 passed` with "the known `test_orchestrator_retirement` failure", names it `test_runprofile_refuses_for_R2_and_NOT_for_unauthored_rows`, and instructs "judge that module on the delta and do NOT claim to have fixed or broken it". Measured: the module is entirely green at 112 passed, that test name exists nowhere, and the suite's single failure is the ENVIRONMENTAL `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`. Since that module reads the exact fields E-03 modifies (PR-903), an executor primed to excuse a red result there would excuse the regression the test exists to find. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | The required-tests section now carries the real baseline, names the real (environmental) failing node, states that `test_orchestrator_retirement.py` must be GREEN before AND after, that any failure there is the executor's, and explains WHY that module matters here. Step 0 records the 112-passed measurement and the nonexistent test name. The gate requires comparing node ids against the executor's own baseline. F-18 added. |
| PR-905 | MEDIUM | IN-SCOPE | A. Correctness (a single-host citation for a two-host mechanism, plus an unstated gate) | `oc_runipd.py:3222` and `agy_runipd.py:2152`; the `blocked` comprehension's `status == "dependency-blocked"` filter | THE ONE PLACE A DEFERRAL SURFACES TODAY IS TWO PLACES, AND IT IS STATUS-GATED. The plan cites `## Dependency blocks (why)` only in `oc_runipd.py` and treats it as the existing safety net whose rendering E-03 must preserve. Measured, it exists in BOTH hosts, and both gate on `status == "dependency-blocked"`, so it renders only for a TERMINATE that took the default `terminal_status` and would silently render nothing for any other terminal status. A preservation claim checked on one host, against a section whose gate the plan does not mention, is weaker than it reads. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-03 records both copies and the status gate, with the explicit warning not to treat the section as a general fallback. V-03 requires stating that both hosts' copies were checked. Step 0 gained the two-host note and the observation that both hosts share the dispatch, which is why one source path suffices. F-16 added. |
| PR-906 | MEDIUM | IN-SCOPE | G. Executability (every line number in the plan is wrong) | `ORCH_REASON_*` at `:3031-3042` (plan: `:2692-2703`); RECONSIDER branch at `:3356-3372` (plan: `:3017-3033`); bespoke fields at `:3387-3388` (plan: `:3047-3048`) | ALL OF THIS PLAN'S `runner_shared.py` COORDINATES DRIFTED BY ROUGHLY 340 LINES IN ONE DAY. The plan already warns to re-locate by symbol, which is right, but it then cites line numbers six times in E-items and findings, and an executor who anchors on any of them lands in unrelated code inside a 3400-line file under concurrent edit by three approved plans. The plan's own warning and its own citations disagree. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Every citation re-measured in place, each paired with a symbol or anchor-comment to locate by instead. The gate now states the measured drift as concrete evidence and lists the exact symbols to search for (`ORCH_DISPATCH_RECONSIDER`, `dispatch_orchestrator_item`, `decide_orchestrator_dispatch`, both constant blocks, `# Failure / Dependency block diagnostics`). F-12's citation corrected. |
| PR-907 | LOW | IN-SCOPE | G. Executability; release gate (an unstated close obligation) | `evaluate_backlog_close(repo,'i2fjf8',[])` -> `close=False` naming ONLY this plan; grep for `From-Backlog: i2fjf8` -> this plan alone; release `f33nrj` (2.0.0, `planned`) | THIS PLAN IS THE SOLE CARRIER OF A RELEASE-BLOCKING ITEM AND NEVER SAYS SO. `i2fjf8` is `graduated` with `- Blocks-Release: next`, and this plan is the only artifact carrying the link, so unlike the multi-carrier Sets in this tree (where an orchestrator also carries it and retirement never closes) a clean finalize here CAN legitimately fire the runner's own close path. The plan inherits the gate correctly in front matter but gives the executor no instruction about it, leaving open both the hazard of hand-closing in anticipation and the hazard of a silent non-close. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | The spec-sync section now records the sole-carrier measurement, that a clean finalize can close the item through `process_backlog_close`, that the item must NOT be hand-closed in anticipation, and that a non-firing close must be reported with its recorded verdict rather than set by hand. F-19 added. |

CONFIRMED SOUND, AND THE CENTRAL DIAGNOSIS IS EXACTLY RIGHT, WHICH IS WHY THIS IS A GO. The defect
reproduces precisely as described: driving the real `dispatch_orchestrator_item` on a purpose-built
deferred orchestrator gives `outcome=reconsider`, `reason=children-unfinished`, `status` still `queued`,
and item keys of exactly `action, attempts, id6, position, setid, status` with NO refusal-bearing field,
while the reason and a rich detail string go only to `events.jsonl`. That is F-6 and F-7 verbatim. The
TERMINATE half was driven too and does write the bespoke fields, so F-8's asymmetry is real. The
no-status invariant is real and the plan is right to guard it. The seven typed reasons exist. The
`r2i1b1` division of labour is accurately read: that plan really does own the record and both render
surfaces and really does exclude new refusals, and `m7gvuz` really does own a different refusal, so the
deferral genuinely has no owner but this plan. The fixture discipline is correct and its justification
checks out (`.aw/records/runs/` has zero tracked files). Both hosts share the dispatch, so one source
path really is enough. And the defect-1-is-dead narrowing, which is the largest single judgement in the
plan, is well argued on three independent measurements and correctly kept OUT of scope rather than
re-implemented.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing. This is the first plan in this sequence whose
direction, scope, dependency and narrowing all survived review intact, and whose blocking-question count
went DOWN rather than up. Every finding is a correction to the plan's surroundings rather than to its
thesis: a second reason vocabulary it did not know about, a record whose home it did not name, a test
consumer it hedged about instead of grepping, a known failure it invented, a section it found in one host
of two, coordinates that rotted in a day, and a release gate it inherited without instructions. The one
judgement I had to make on the plan's behalf was OQ-01, which was addressed to the reviewer: the answer
is NO, the remedy must not mention `--full-auto`, because a refusal message is the single worst place to
advertise a route around the approval gate, and the deferral detail already names the children and
statuses a better remedy can be built from. The lesson worth carrying is the mirror of the previous
reviews in this sequence: this plan cited the mechanism by symbol AND drove it, which is why its core
claim held under execution while its peripheral claims did not.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | OQ-01 asks whether the remedy should mention `--full-auto`, and names the REVIEWER as owner. Answer it, or defer again? | ANSWER IT: NO. Record the reasoning, name the remedy to use instead, and forbid `--full-auto` in any remedy string. | (a) Defer to the maintainer, rejected because the question is explicitly owned by the reviewer, the plan already reasoned it out correctly, and deferring a question whose answer is clear from repository policy would leave an executor free to implement the worse option. (b) Allow it with a caveat, rejected because a caveat inside an error message is not read: the operator is at that moment looking for the fastest way to make the run do something, which is exactly when a bypass suggestion is most likely to be taken. | `--full-auto` clears `reviewed` -> `auto-approved` through the auto-approve predicate; `AGENTS.md` records that a prohibition-only gate gets complied with by deletion, so remedies name constructive actions and must not name shortcuts past the refusal; `m7gvuz` E-06 sets the same wording discipline for the sibling refusal; the measured deferral detail already names the blocking children and their statuses. | yes |
| D-2 | Two reason vocabularies exist with a near-miss pair. Pick the mapping key myself, or leave it to the executor? | Resolve the KEY from evidence (`ORCH_REASON_*` is correct, because `decide_orchestrator_dispatch` translates every `RETIRE_REFUSED_*` branch before returning) and require the executor to ASSERT that translation rather than trust my measurement. | (a) Leave the whole thing to E-02 as "map the seven reasons", rejected because the plan's enumeration is what an executor will implement against, and it silently excludes a second vocabulary that shares three values, so the most likely outcome is a mapping that looks complete and misses. (b) Require the mapping to accept BOTH vocabularies, rejected as defensive duplication: it would encode the aliasing rather than removing it, and the translation already guarantees only one set reaches the write site. | Both constant blocks printed and differenced; the four translation branches read at `:3136-3168`; `dispatch_orchestrator_item` writes `decision.reason` only. | yes |
| D-3 | The refusal record lives in `render_stream.py`, undeclared here. Add it to `Scope-Paths`, or forbid editing it? | FORBID editing it and state that importing needs no declaration. | Adding `render_stream.py` to `Scope-Paths`, rejected because declaring a path invites edits to it and `aw ipd finalize` then demands a `--scope-ack` when it finishes unmodified; worse, the file is `r2i1b1`'s deliverable and is being edited by that approved plan, so declaring it would create exactly the racing-edit contention this plan elsewhere tells the executor to avoid. | `runner_shared.py:147` import; AST walk showing `render_stream` has no first-party imports; `r2i1b1` E-01's stated reason and E-08's assertion; the finalize scope-reconciliation behavior. | yes |
| D-4 | The plan names a known baseline failure that does not exist. Correct the baseline only, or say more? | Correct it AND state that the named module must be green before and after, with the reason it matters here. | Correcting the numbers alone, rejected because the dangerous part is not the stale total but the standing instruction to excuse a failure in `test_orchestrator_retirement.py`, which is the module that reads the fields E-03 edits; leaving that instruction with fresh numbers beside it would preserve the hazard. | Bare suite run; module run in isolation (112 passed); grep proving the named test does not exist; `tests/test_orchestrator_retirement.py:3310-3312` reads the fields E-03 touches. | yes |
| D-5 | Readiness: `go-pending-approval` or `no-go`? | `go-pending-approval`. | `no-go`; rejected because the vocabulary reserves it for a genuine not-ready condition and this plan has none: the verdict is APPROVE WITH REVISIONS APPLIED, every finding is FIXED with nothing deferred, no BLOCKER or HIGH remains unfixed, the only open question was non-blocking and is now RESOLVED, and `aw ipd lint --phase review-finalize` reports zero findings. The unmet `executed:ybkmzp`-style dependency (here `executed:r2i1b1`) is NOT a not-ready condition of this plan: it is a correctly declared edge the runner enforces, and the plan is otherwise cleared to be approved and queued behind it. | `aw ipd lint --phase review-finalize` exit 0, zero findings; all seven findings FIXED; OQ-01 `- Status: resolved` with `- Blocking: no`; the workflow's definition of GO - PENDING HUMAN APPROVAL as the correct readiness for a clean reviewed plan awaiting sign-off. | yes |
| D-6 | The plan's dependency on `r2i1b1` is argued rather than proven. Accept the argument, or verify it? | VERIFY it by rendering, and record the result as a finding so the dependency cannot later be read as optional tidiness. | Accepting the plan's prose, rejected because the claim ("without it the record is invisible") is the sole justification for an ordering constraint that delays a release-blocking item, and it is cheap to test: rendering the real summary on a `queued` orchestrator settles it in one call. | `render_stream.py:2152-2178` allowlist read; `render_run_summary_table` rendered on a `queued` orchestrator showing no `Diagnostics` line and `Verify` of `-`. | yes |
