# Review: specify the lifecycle automation policy before any verb implements one, child u06zo2 (Set lcpolicy)

- Subject-Id: u06zo2
- Subject-Type: ipd
- Reviewed-At: 2026-09-09
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: REVIEWED - OPEN QUESTIONS
- Readiness: no-go

## Round 1

Reviewed at HEAD `d45bf884`. `aw ipd lint --phase author` CONFORMING before semantic review; at
`--phase review-finalize` the only remaining finding is the deliberate `IPD-Q501` stop from the
blocking question this review raises (one intermediate `IPD-I305` was introduced and fixed when a
non-`E-*` value was briefly written into `Depends on`).

DISCLOSURE: the same agent/model authored this plan, so this is a SELF-REVIEW. Re-reading one's own
plan is nearly worthless, so the method was to go HUNTING for the thing the plan's central deliverable
depends on: every place automation already advances an artifact's lifecycle. That search produced the
first blocker, and counting the plan's own open questions against the item's produced the second.
Seven things were executed rather than inspected: a search for automated lifecycle-status writes across
both drivers; a read of both `--full-auto` call sites in each driver; the orchestrator retirement path;
the backlog auto-close path; the provenance of the commit asserting the two maintainer decisions; the
`Scope-Paths` grammar rule for a bare directory entry; and the bare suite plus the specific test file
the plan names as its baseline failure.

SCOPE: only this child was a candidate; the Set has exactly one member. Read as evidence:
`agent_workflows/oc_runipd.py`, `agent_workflows/agy_runipd.py`, `agent_workflows/plan_readiness.py`,
`agent_workflows/ipd_schema.py`, `agent_workflows/attention_contract.py`,
`agent_workflows/check_engine.py`, backlog item `rxya25`, executed plan `97df1z`, specs
`20260815-0151-01-honest-human-approval-attestation.spec.md` and
`20260802-1904-01-ipd-structure-and-linting.spec.md`, and commit `0f0eb2b4`.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-001 | BLOCKER | UNDER-SCOPE | A. Correctness; B. Security (an omitted transition is a permitted one) | `oc_runipd.py:836-859`; `oc_runipd.py:2934` and `:6926`; `agy_runipd.py:1972` and `:3982`; `process_backlog_close` at `oc_runipd.py:1426`, called `:6863` | **THE INVENTORY THE WHOLE SPEC RESTS ON IS INCOMPLETE, AND OQ-01's PERMISSIVE DEFAULT TURNS THAT FROM A DOCUMENTATION GAP INTO A SAFETY HOLE.** Normally a missing inventory row means a gate is not applied; under allow-unless-forbidden it means NO GATE EXISTS for that transition. The item's five surfaces omit at least three real automated advances, all measured rather than reasoned about. `finalize_orchestrator` transitions an orchestrator to the TERMINAL `executed` state via `aw ipd set executed`, with actor `aw oc run (orchestrator rollup)`, no agent turn and no human, which is precisely the item's own candidate condition "whether an automated actor may write a terminal state at all" already happening in production. The `--full-auto` clear fires at TWO sites PER DRIVER, not one: the queue-build path and the post-review path, the latter also rewriting the queue item's `action` to `execute` and `status` to `queued`, so it is not merely a status set. And `process_backlog_close` advances a backlog item to `done` after a run. This also falsifies a load-bearing sentence in OQ-01's own recorded reasoning ("measured at HEAD, `--full-auto` performs exactly ONE advance"), which the maintainer's permissive default partly rested on. | C:Low; U:Low; S:High; F:High; Overall:Medium | FIXED | E-01 rewritten to require EXHAUSTIVENESS, naming all three omissions with citations and mandating a search over lifecycle-setting calls and automated actor strings; V-01 now FAILS a table containing only the item's five surfaces and requires the search output pasted; added F-11 and two Step 0 conventions. |
| PR-002 | BLOCKER | UNDER-SCOPE | G. Executability (the plan would hardcode three answers it exists to stop hardcoding) | the plan's E-03 versus its two OQ blocks | **THREE OF THE ITEM'S FIVE DESIGN QUESTIONS HAD NO OPEN-QUESTION BLOCK, SO E-03 WOULD HAVE ANSWERED THEM ON THE PLAN'S OWN AUTHORITY.** The plan's entire justification for being spec-first is that "a plan that implemented a policy engine would have to answer all five by fiat, which is exactly the hardcoding the item exists to end", and its own gate says the answers are "PROPOSALS until they have" the maintainer's attestation. Yet only questions 2 and 3 carried OQs; questions 1 (policy key shape), 4 (indistinguishability) and 5 (retirement/reversal) were simply asserted inside E-03 and E-05. That relocates the hardcoding from code into a spec rather than removing it. Question 4 is the acute one: E-05 instructed the executor to "state that the policy may never be used to make an automated actor's advance indistinguishable from a human's", which is a permanent property no reviewer may grant, because once advances are recorded indistinguishably no later edit re-attributes them. | C:Low; U:Low; S:Medium; F:High; Overall:Medium | FIXED | Added OQ-03, OQ-04 and OQ-05, each with a recommendation and its evidence; OQ-04 marked `Blocking: yes`; E-03 rewritten to transcribe answers FROM the OQ blocks and to distinguish the two already-decided questions from the three newly raised; E-05 no longer asserts the prohibition but writes OQ-04's answer; V-03/V-05 now require OQ provenance per answer; added F-12. |
| PR-003 | HIGH | IN-SCOPE | A. Correctness (a measured claim inside a maintainer decision is wrong) | `oc_runipd.py:2934`, `:6926`; `agy_runipd.py:1972`, `:3982` | OQ-01's recorded reasoning states as its SECOND ground that "measured at HEAD, `--full-auto` performs exactly ONE advance, `reviewed` -> `auto-approved`". The transition TARGET is right and the honesty property it relies on is real, but the count is not: there are four call sites (two per driver), and the post-review one additionally sets the queue item's action to `execute`. This matters because the permissive default was justified partly on the shipped automation being narrow, so an executor writing the spec's reconciliation passage from that sentence would understate what the default permits on day one. The decision itself is not thereby wrong (the honesty property holds at every site), which is why this is a correction rather than a re-escalation. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-01 records both sites per driver explicitly and V-03 requires the reconciliation passage to name the transitions "as measured by E-01" rather than from the OQ prose; F-11 states the count. |
| PR-004 | HIGH | IN-SCOPE | G. Executability (the gate contradicts the plan's own resolved questions) | the plan's OQ-01 block versus its gate paragraph; `git show 0f0eb2b4` | **THE GATE TELLS THE EXECUTOR TO WAIT FOR A DECISION THE PLAN ALREADY RECORDS.** It says "OQ-01 is BLOCKING for the spec's CONTENT" and "E-03 cannot answer question 3 without the maintainer's ruling on the default", while OQ-01 reads `Blocking: no`, `Status: resolved`, with that ruling written out at length. Both cannot be true. The stale half would stall an execution turn on a settled security-adjacent question, and it also misdescribes the tooling, since a non-blocking question does not refuse approval. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Gate rewritten to state both questions are answered with their commit, to name OQ-04 as the one thing that does block, and to say the recommendation is already written into E-05 so an affirming answer unblocks with no edits; added F-13. |
| PR-005 | MEDIUM | UNDER-SCOPE | C. Architecture (the precedent to follow is only half-adopted) | `oc_runipd.py:735`; `agy_runipd.py:834`, `:104-111` | E-04 tells the spec to require ONE predicate "following the pattern the repository has already proved", but that pattern is currently HALF adopted for the very feature in question, and the plan does not say which half. The DECISION is genuinely shared: `plan_readiness.is_plan_review_approved` has one definition, and `agy_runipd.py:104-111` records that its near-copy was removed because "a fix to one driver left `aw agy run --full-auto` broken" - live evidence of the drift. The ACTION is not: `set_plan_approved` is defined twice, once per driver, and the agy copy's own docstring concedes it is "kept byte-for-byte equivalent to the oc twin" by convention rather than by construction, along with duplicated `FULL_AUTO_ACTOR` and message constants. So "follow the pattern" is ambiguous exactly where it matters, and a spec that leaves it ambiguous licenses the drift the shared predicate was introduced to end. | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-04 now requires the spec to state explicitly whether single-authority covers the PERMIT DECISION only or also the TRANSITION-PERFORMING call, with the measured half-adoption recorded; V-04 requires that sentence pasted; added F-15 and a Step 0 convention. |
| PR-006 | MEDIUM | IN-SCOPE | G. Executability (stale citations, two inherited from the item) | read at HEAD `d45bf884` | Three cited line numbers are wrong: `evaluate_blocking_close` is `check_engine.py:1980` (cited `:1951` in E-04, Step 0 and F-5), `driver_begin` is `oc_runipd.py:903` (cited `:894` in E-01), and the `Approval`-field enforcement is `ipd_schema.py:399-405` (cited variously `:396-402` and `:398-402`). Two are inherited from the backlog item, which is worth noting because the plan otherwise re-verified the item's claims carefully; these slipped through as transcription. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All three corrected at every occurrence, with a Step 0 re-locate-by-symbol convention and F-16 recording the drift. |
| PR-007 | MEDIUM | IN-SCOPE | E. Testing (a wrong baseline used as a validation criterion) | measured at HEAD `d45bf884`; `tests/test_orchestrator_retirement.py` run separately | The stated baseline is "1 failed, 5648 passed (the known `test_orchestrator_retirement` failure)". Measured: `1 failed, 5919 passed, 3 skipped, 2 xfailed`, and the failure is `tests/test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, an ENVIRONMENTAL gitignored-dump case; `test_orchestrator_retirement.py` now reports `112 passed`. V-06 tells the executor to compare against the stated baseline, so an executor would chase a passing test and could read its own correct counts as drift. Low harm because the plan also says to judge on the delta. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Required-tests section corrected with both measured figures, the real node id and its environmental cause, plus an instruction to compare by NODE ID; added F-17. |
| PR-008 | LOW | IN-SCOPE | A. Correctness (an unverified attestation claim) | `git show 0f0eb2b4` | The plan records two maintainer decisions in its own body without any pointer to their provenance, which is the shape this repository treats as a forgery risk (an agent writing another role's attestation). Verified genuine: commit `0f0eb2b4` is authored by the maintainer and its message states both decisions and their reasoning independently of the plan text. Raised as LOW and FIXED rather than waved off, because the fix (cite the commit) is what lets the next reader check it without redoing this. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | The gate now names commit `0f0eb2b4` as the recorded provenance and states it was verified maintainer-authored; added F-14. |

CONFIRMED SOUND, AND THE PLAN'S CENTRAL JUDGEMENT IS RIGHT. Spec-first is the correct shape here, and the
strongest evidence is the code's own admission: `oc_runipd.set_plan_approved`'s docstring states that the
configurable per-transition policy "is deliberately NOT built here; it spans every lifecycle verb and is
tracked as backlog `rxya25`. `--full-auto` is the first consumer of a policy that does not exist yet, so
its behavior is hardcoded-but-honest until that lands." So the plan is not inventing a need. F-1 verified
(`97df1z` is executed, making the first consumer live). F-2 verified by grep. F-3 and F-4 verified at
source: `READY_TO_EXECUTE = frozenset(("approved", "auto-approved"))` with the comment that
`auto-approved` "records an automated clear, NOT human approval", and the schema refusing `Approval` on
anything but `approved`. F-5, F-6 (`TRANSITION_AUTHORITY` at `attention_contract.py:416`), F-7, F-9 and
F-10 all verified, including that the cited prior-art spec really is `- Status: implemented`. The
`Scope-Paths: .aw/records/specs` declaration is legal grammar (a bounded concrete-directory entry).
And the honesty property the whole design turns on genuinely holds at every `--full-auto` site: both
drivers record an automated actor string and the message "auto-approved by --full-auto: review readiness
cleared (not human approval)", never `--by-human`.

WHAT THIS ROUND CHANGED ABOUT THE PLAN'S SHAPE. Nothing about the deliverable, which should be a spec and
should not be code. Two things about its RIGOR, and they are the same mistake at two levels. The plan
argued forcefully that answering design questions by fiat is the defect to be removed, and then did it
three times itself by asserting questions 1, 4 and 5 inside E-items instead of asking. And it accepted
the backlog item's list of five surfaces as the inventory's boundary, when the inventory is precisely the
artifact that must not inherit someone else's incompleteness, especially once the maintainer's permissive
default made an unlisted transition a permitted one. Both are now closed by making the plan ask what it
was asserting and search where it was trusting. The remaining stale-state findings (a self-contradicting
gate, three transcribed line numbers, a wrong baseline, an uncited attestation) are ordinary drift.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | Three of the item's five design questions had no OQ block (PR-002). Answer them in review, leave them asserted, or raise them as open questions? | Raise all three as OQ-03/OQ-04/OQ-05 with recommendations, and mark OQ-04 blocking. | (a) Resolve all three from evidence on reviewer authority; rejected because the plan's own gate states these answers are proposals until the maintainer attests them, and because question 4 is unrepairable (records written indistinguishably cannot be re-attributed), so it is exactly the class the escalation rule exists for. (b) Leave them asserted inside the E-items as authored; rejected because that is the hardcoding-by-fiat the plan exists to end, relocated from code into a spec. (c) Mark all three blocking; rejected as over-blocking, since questions 1 and 5 change the policy's shape but are fixable by editing an unapproved spec. | The plan's own gate ("the five answered questions are PROPOSALS until they have it"); the item's framing of these as questions "for whoever specs this"; and for question 4 specifically, the fact that misattributed history is not repairable by a later edit, unlike a spec's key shape. | yes |
| D-2 | Is the incomplete inventory a documentation gap or a safety defect (PR-001)? | A safety defect, and E-01 must be exhaustive with the search that proves it. | Treating it as a completeness nicety and simply adding the three missing rows; rejected because the rows are a symptom: the plan took the item's five surfaces as a boundary, so the next automated advance added anywhere would be omitted the same way. Requiring a search plus a stated exhaustiveness method addresses the method, not just the instances. | OQ-01 resolved the default to allow-unless-forbidden, so an unlisted transition is PERMITTED rather than merely undocumented; measured, three real automated advances already sit outside the item's list, one of them writing a TERMINAL state with no human (`finalize_orchestrator`). | yes |
| D-3 | OQ-01's recorded reasoning contains a wrong measurement ("exactly ONE advance", PR-003). Re-escalate the decision, or correct the fact and keep the decision? | Correct the fact, keep the decision, and make the spec's reconciliation passage cite E-01's measurement rather than the OQ prose. | Re-escalating OQ-01 to the maintainer on the ground that a premise was wrong; rejected because the premise that actually carries the decision is the HONESTY property (no site claims human `approved`), and that holds at all four sites; the count understates breadth but does not reverse the argument. Re-asking would spend the maintainer's time on a conclusion the corrected facts still support. | Read all four call sites: each routes through `set_plan_approved`, which sets `auto-approved` with an automated actor string and never `--by-human`; the post-review site additionally sets `action: execute`, which is breadth, not a change in honesty. | yes |
| D-4 | E-05 asserted the indistinguishability prohibition itself. Keep it as an assertion with strong evidence, or make it OQ-04's answer? | Make it OQ-04's answer; E-05 writes the ruling rather than deciding it. | Keeping the assertion on the strength of `97df1z`'s precedent; rejected because that ruling covered ONE transition on ONE path, whereas this would be a universal non-overridable property of every transition under a new policy - and under OQ-02's both-directions answer someone will eventually ask whether a loosening override may relax it, which needs an attested "never" to refuse. | `97df1z`'s OQ-02 scope (one transition); the mechanical enforcement that already exists (`auto-approved` as a separate status, `Approval` refused on it, automated actor strings in both drivers) which is what makes YES the recommendation rather than an open toss-up. | no |
| D-5 | Is `Scope-Paths: .aw/records/specs` (a bare directory) legal, given the plan produces one spec file? | Legal; left unchanged. | Requiring a narrowed entry or the eventual filename; rejected because the filename is minted by `aw specs new` during execution and so cannot be declared in advance, and the IPD spec expressly allows a bounded pathspec whose leading segment is a concrete directory. | Spec `20260802-1904-01-ipd-structure-and-linting.spec.md:183`: "an entry whose leading segment is a concrete directory bounds the blast radius, so `tests/`, `agent_workflows/**` ... are all legal". | yes |
