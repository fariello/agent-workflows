# Review: report a run whose work never landed as NOT DONE in red, child ys1dor (Set integearn)

- Subject-Id: ys1dor
- Subject-Type: ipd
- Reviewed-At: 2026-09-08
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `1cda9c4d`. Structural preflight `aw ipd lint --phase author` conformed CLEAN (no
advisory) before semantic review, and `--phase review-finalize` conformed clean after the revisions.

DISCLOSURE: this plan was authored in the same session, so this is a self-review and worth less than an
independent one. It also succeeds `xtklpd`, reviewed and rejected here, so the chain is two deep on the
same problem. What that cost is visible in PR-501: the plan tells the executor to print two fields, and
neither the authoring nor the predecessor's review noticed that both contain absolute home paths.

THE PREMISE IS CONFIRMED LIVE, not accepted from the narrative. I rendered the cited run rather than
trusting the quoted summary: `render_run_summary_table` on `run-20260908T140845Z-2489897` prints
`Outcome: \x1b[32mCOMPLETED\x1b[0m` and `Progress: 1/1 [##########] 100% (1 substantially-complete)`,
while that run's single queue item carries `status: substantially-complete`,
`integration_signal: suite-failed`, and `preserved_branch: aw/lane/03ie04_attempt2`. So a run whose
entire product was stranded does report green success at 100 percent. The plan's other structural claims
also hold: the outcome is computed from per-item status, `substantially-complete` is inside the success
tuple, the color path is string-keyed, the facts are persisted by both drivers, and `grep` finds no
reporting consumer of `integration_signal`. The decision to derive from the run's own record rather than
the filesystem is correct and its rejection of `xtklpd`'s approach is sound.

WHAT ROUND 1 CHANGED. Every finding was fixed in place; none was deferred or left open. The most serious
is a privacy defect in the plan's own instructions. Three others are cases where an instruction could not
be followed as written, and two are accuracy corrections. No new E-item was needed: the six existing
items were the right decomposition, and the repairs went into their bodies and into the V-items.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-501 | HIGH | IN-SCOPE | B. security and privacy; D92 | both values read from the live `state.json`; `AGENTS.md` D92 rule | **The two fields E-03 tells the executor to print CONTAIN ABSOLUTE HOME PATHS, and this is the most-copied output in the product.** MEASURED on `run-20260908T140845Z-2489897`: `preserved_worktree` is an absolute path under the maintainer's home directory including the username, and `integration_detail`'s recorded text embeds the repository's absolute path too (`... suite FAILED with exit 1 in <abs repo path> (no summary line parsed)`). The end-of-run summary is what an operator pastes into an issue, a chat, or a plan's `Observed evidence`, and D92 forbids machine-identifying strings in a public artifact. The plan says to print both, mentions neither risk, and its V-03 asked only for the rendered text | C:Low; U:Low; S:Medium-High; F:Low; Overall:Medium | FIXED | New F-11 records both measured values. E-03 now forbids printing `preserved_worktree`, requires any detail text be rendered repository-relative, and states why (the paste path). V-03's load-bearing evidence became `aw sanitize --agent` over a CAPTURED RENDER asserting `findings: 0`, with an absolute path anywhere in the render declared a FAILED validation. E-06 gains the sanitizer assertion, and the validation section carries it as a required check rather than a courtesy. A Step 0 convention records the hazard so it is discoverable outside the E-item |
| PR-502 | HIGH | IN-SCOPE | A. correctness; D. anti-regression | `render_stream.py:1857-1880`; branch `:1861-1866`; `exit_reason` `:1858-1859` | **The outcome block has SEVEN branches, not five, and the plan never mentions the one that already handles part of this problem.** A `FAILED` branch fires when any item's status is in `("failed-safely", "integration-blocked", "merge-conflict")`, so an `integration-blocked` item ALREADY reports `FAILED` in red; the true gap is narrower, being the item that kept a SUCCESS-TUPLE status while its `integration_signal` recorded a refusal. Two consequences the plan missed: E-04 promised to preserve "all five existing outcomes" while there are seven (the `exit_reason` passthrough short-circuits the whole block), and an E-01 implementation testing the signal BEFORE the status would RELABEL that existing `FAILED` outcome, which is a regression dressed as the feature and would pass every test the plan specified | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | New F-8 records the branch, the short-circuit, and the relabel hazard. E-01 gains a paragraph naming the narrower gap and requiring the new test not shadow `FAILED`. E-04 rewritten to seven cases with `FAILED` and `exit_reason` called out individually and the relabel risk stated. E-06 gains two of the three added cases. V-04 now demands EIGHT rendered samples; V-01 demands the `integration-blocked` case explicitly |
| PR-503 | HIGH | IN-SCOPE | C. architecture; G. executability | measured AST import scan; `oc_runipd.py:3725-3729`; `agy_runipd.py:40`, `runner_shared.py:147` | **E-01's instruction to read the refusal values from "the runner's own definitions" is unbuildable.** The constants `INTEGRATION_REFUSED_VERIFIER_DECLINED`/`_SUITE_FAILED`/`_NO_SIGNAL` live in `oc_runipd.py`, but `render_stream.py` imports ZERO in-package modules (measured by AST) and is imported BY both runners and `runner_shared`, so it is a leaf; importing the runner would invert the dependency and create a cycle. The plan's stated requirement (a new refusal reason must not silently read as success) is achievable without any coupling, but the plan did not say how, so an executor would either create the cycle or fall back to the hand-copied list the plan explicitly forbids | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | New F-9 records the measurement and the import direction. E-01 now states two options and PREFERS (b): test for the two EARNED values and treat every other non-empty signal as a refusal, so an unknown future signal falls to the refusal side BY CONSTRUCTION with no new import. Option (a) (relocate the constants) is permitted with the fence consequence recorded in the scope check. V-01 requires the chosen option be stated and requires proof no cycle was introduced; E-06 gains the unknown-signal case that proves the property |
| PR-504 | MEDIUM | IN-SCOPE | A. correctness; G. executability | live item key list; `oc_runipd.py:6676`; `agy_runipd.py:3739` | **`integration_detail` is not on the item, so E-03 cannot be followed as written.** MEASURED: the item's 21 keys include `integration_signal`, `preserved_branch`, `preserved_worktree`, `preserved_lane_id`, `preserved_base` and `preserved_disposition` but NO `integration_detail`; both drivers write it onto the ATTEMPT. An executor following E-03 literally would read a missing key and print nothing, or would "fix" it by adding a driver write, which is a durable-state change smuggled into a rendering plan and outside the fence | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | New F-10 records the key list and both write sites. E-03 now says to read `attempts[-1]["integration_detail"]` and to tolerate absence (no attempts, or an older record), and forbids adding a driver write. A Step 0 convention states it. The gate closes with an explicit prohibition on touching either driver to make this easier |
| PR-505 | MEDIUM | IN-SCOPE | E. testing | `.gitignore`; `tests/test_run_viewer.py`'s standing caveat | **E-05's real-record proof cannot be an automated test, because `.aw/records/runs/` is gitignored.** A test asserting against `run-20260908T140845Z-2489897` passes only in this checkout and fails in every other and in every clean worktree, which is the exact pathology `tests/test_run_viewer.py` already documents and pays for. The plan makes the real record one of six E-06 cases with no caveat | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | New F-13 records it. E-05 now requires a COMMITTED REDACTED fixture as the automated assertion (redacted per PR-501) with the live render kept as a one-time manual demonstration, and states that a vacuously-passing skip is unacceptable while an announced skip is tolerable. E-06 and V-05 updated to match |
| PR-506 | MEDIUM | IN-SCOPE | F. UX; A. correctness | `render_stream.py:1892-1902`, read in full | The plan says an unhandled outcome "silently inherits a non-red color" but does not say WHICH, and the answer changes the risk: the else-branch is CYAN, a colour that looks deliberate rather than obviously broken, so the miss would not be noticed by eye. Separately, the selection tests `"FAIL" in outcome_str`, so a word containing `FAIL` would be red with NO edit to the color block, a coincidence a future word would not share; and `STRANDED` contains none of `FAIL`/`INTERRUPT`/`STOP`, so it renders cyan unless an explicit branch is added | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | New F-12 records the else-branch colour, the substring shortcut, and the `STRANDED` consequence. E-02 now requires an EXPLICIT branch even if the chosen word would be red by coincidence, and says so with the reason. V-02 requires the raw `\x1b[31m` sequence immediately preceding the word plus the pre-change render for contrast |
| PR-507 | MEDIUM | IN-SCOPE | Evidence accuracy | measured bare run; backlog `8kttqq`; `32ij2j` now `superseded` | The stated baseline is wrong in BOTH halves: it says "1 failed, 5858 passed (the known `test_plan_readiness` failure)", but re-measured the suite is `1 failed, 5865 passed, 3 skipped, 2 xfailed` and that `test_plan_readiness` case now PASSES because plan `32ij2j` was retired to `superseded/`. The single current failure is `test_reporting_contract.py::ParityTests::test_only_expected_files_contain_the_full_contract_prose`, which is environmental (an `rglob` that ignores `.gitignore` walking a gitignored `opencode-recovery/` directory). An executor told to expect a different failure might clear this one by deleting another party's untracked files | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | Validation section rewritten to a delta rule with both re-measurements, the environmental failure named with its mechanism and backlog id, and an explicit prohibition on deleting the untracked directory. The gate repeats the prohibition. V-06 requires naming it rather than counting it |
| PR-508 | LOW | IN-SCOPE | Evidence accuracy; G. executability | all citations re-resolved at `1cda9c4d` | Every line citation in the plan had drifted, including the two the plan leans on most: the outcome block from `:1869-1881` to `:1857-1880` and the color path from `:1890-1900` to `:1892-1902`. Every cited construct exists and every claim about it is true, so this is citation rot, but the plan's own gate already warns that these coordinates drift, which makes leaving them stale self-contradictory | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All citations re-resolved. The gate's RE-LOCATE paragraph now names the two measured drifts as the reason, so the rule reads as a consequence rather than boilerplate. Step 0 conventions re-anchored |

No finding was DEFERRED, left OPEN, or marked REPLAN, so no escalation to a `- Blocking: yes` question was
required and the repository's `HIGH` gate threshold is not engaged. PR-501 is the one with real potential
for harm, and it was fixed in place because the fix is bounded and verifiable (print a git ref, not a
path, and prove it with the shipped sanitizer) rather than requiring a decision.

OQ-02 was RESOLVED at review (it was marked `Owner: executor` but needed no executor decision).
OQ-01 remains deferred to the maintainer, non-blocking, with a new observation that strengthens the case
for eventually changing the exit code: `25kzda:1057` defines exit `0` as "Every actionable item is
verified", which a stranded item is not, so the current exit code may already violate the spec rather
than merely being incomplete. It stays non-blocking because this plan's fence is one rendering file, the
printed outcome is what misled a human, and `nuanaw` already offers automation a fail-closed signal.

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | PR-501: print the recorded facts as the plan says, or constrain what may be printed? | CONSTRAIN: forbid `preserved_worktree`, require repository-relative detail text, and make the shipped sanitizer the evidence. | (a) Print both and note the risk in prose, rejected because prose does not stop a leak and the summary is the most-copied output in the product. (b) Print nothing but the branch, rejected as under-serving the plan's own goal (an alarm without a reason trains operators to ignore it); the reason can be kept, made relative. (c) Escalate as a blocking question, rejected because the fix is bounded and mechanically verifiable, so it needs no human judgement. | measured absolute paths in `preserved_worktree` and `integration_detail`; `AGENTS.md` D92; `aw sanitize --agent` as the deterministic check | yes |
| D-2 | PR-503: how should the renderer know which signals mean refusal, given it cannot import the runner? | PREFER option (b): recognize the two EARNED values, treat every other non-empty signal as a refusal. | (a) Relocate the constants to a leaf module and re-export, permitted but not preferred: it adds a path to the fence and one new in-package import to a module that currently has none. (c) Hand-copy the refusal list, rejected outright because the plan's own requirement is that a NEW refusal reason must not read as success, and a copied list fails exactly then. | measured AST scan showing `render_stream` imports nothing in-package while both runners import from it; `oc_runipd.py:3725-3729` | yes |
| D-3 | OQ-02 was open with `Owner: executor`. Close it or carry it? | CLOSED as STRANDED at review, with the plan's reasoning upheld and strengthened. | Leaving it open, rejected because it required no executor decision and an open question in the plan is a live gate input; carrying a decided question forward invites an executor to re-litigate it. Reporting `PARTIAL` for a partial strand, rejected on the plan's reasoning plus a precedent it did not cite: the existing `FAILED` branch already fires on `any(...)`, so "one bad item colors the outcome" is this function's established convention. | `render_stream.py:1861-1866` `any(...)` precedent; the recovery section making the alarm specific | yes |

Nothing was rejected. The plan's diagnosis, its run-own-record rule, and its rejection of the
filesystem-derived alternative are all sound; what review supplied was a privacy constraint on what may
be printed, a branch the plan did not know existed, a buildable substitute for an unbuildable
instruction, and a current map of the code and the suite.
