# Review: wire the resolved verification decision into both host drivers (child ybkmzp, Set hostdefault)

- Subject-Id: ybkmzp
- Subject-Type: ipd
- Reviewed-At: 2026-09-08
- Reviewer: opencode its_direct/pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `fac69fbd`. Structural preflight `aw ipd lint --phase author` conformed before semantic
review; `--phase review-finalize` conformed after the revisions.

DISCLOSURE: this plan was authored in the same session, and it is the second child of a Set whose first
child was also self-reviewed, so this is the third self-assessment in the chain and is worth less than an
independent review. That matters concretely here, because the most serious finding below is a case where
the AUTHORING was correct at the moment it was written and became wrong when a SIBLING PLAN merged two
days later, which is exactly the class of error a self-review is worst at catching and which only
re-measurement against live HEAD surfaces.

THE PLAN'S THESIS IS TRUE AND I RE-VERIFIED IT rather than trusting the authored prose. Measured live at
review: `resolve(empty_config(), runner="oc").validate` is `False` with provenance `shipped-default` and
`resolve(..., runner="agy").validate` is `True` with the same provenance, so child 01 really did make
tier 4 per host; `defaults.validate: false` still beats the row on agy (provenance `defaults`), so the row
is a floor; an agy profile omitting `validate` inherits `True` while one declaring `validate: false`
resolves `False`/`profile`, so the tri-state does not collapse. No driver consumes any of it: the live
resolution call passes no `validate=`, the freeze site reads `getattr(args, "validate", False)`, and
`agy_runipd.py` contains the string `runner_profiles` exactly zero times. The polarity inversion the plan
centers on is real and the hosts' postures are genuinely opposite (bare `oc start` yields
`validate=False`; bare `agy start` yields `no_verify=False`, meaning verification ON). The diagnosis, the
shared-library placement, and the negate-at-the-boundary design are all correct.

WHAT ROUND 1 CHANGED is the opencode half's TARGET, the antigravity half's FLAG MECHANICS, and two whole
classes of consequence the plan did not name (operator reachability, and shipped assertions that pin the
claim this plan falsifies). Scope-Paths grew from seven to ten; E-items from 7 to 9.

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-601 | BLOCKER | IN-SCOPE | A. correctness; G. executability | AST call-graph walk at `fac69fbd`; `oc_runipd.py:2613` (def, 0 callers) vs `:2652` (def) and `:2742` (sole call); `git log -S resolve_launch_pair` -> `3798d236` | **The function E-03 tells the executor to edit is DEAD CODE, so the opencode half would have been a silent no-op.** Executed plan `kgpptv` (`runprofile-06`) merged `3798d236` on 2026-09-08, AFTER this plan was authored, and replaced `initialize_run`'s call to `resolve_launch_profile` with `resolve_launch_pair`. Measured by walking `oc_runipd.py`'s AST for calls to either name: `resolve_launch_pair` is called exactly ONCE (`:2742`), `resolve_launch_profile` has ZERO callers anywhere in `agent_workflows/`, and its only surviving references are two test modules plus a `run_dispatch.py` docstring. An executor following E-03 literally would have added `validate=` to a function no run executes: no behavior change, no test failure, and every resolver-level V-item still passing, so the failure is INVISIBLE to the plan's own validation. This is a BLOCKER because the plan would have been marked executed while delivering nothing on the primary host | C:Low; U:Low; S:Low; F:High; Overall:Medium | FIXED | E-03 rewritten to name `resolve_launch_pair` and its executor `resolve()` call. New F-12 records the AST measurement and the moving commit. The gate now carries a one-command re-measurement the executor MUST run before editing either driver, with the reason stated (this failure mode produces zero test failures, so the suite cannot catch it) |
| PR-602 | BLOCKER | UNDER-SCOPE | A. correctness; B. security posture | live probes on `agy_runipd.build_parser()` at `fac69fbd`; `agy_runipd.py:4663-4669`; `cli.py:611`, `:3730` | **E-02's flag spelling raises at parser build, and its obvious workaround silently disables verification on antigravity.** The plan says to mirror oc's `--validate`, which oc spells with aliases `("--validate", "--verify", "--audit")`. `BooleanOptionalAction` auto-generates a `--no-X` per option string, so that list also generates `--no-verify` and `--no-audit`, both ALREADY on agy's `start`. Measured: `ArgumentError: ... conflicting option strings: --no-verify, --no-audit`, at `build_parser()` time, so EVERY `aw agy` invocation dies. The natural fix is worse and is the security-relevant half: measured with `conflict_handler="resolve"` (a spelling this repo does use, at `cli.py:611`), the new action STEALS `--no-verify`/`--no-audit`, after which `args.no_verify` does not exist, agy's freeze site reads `False` unconditionally, and antigravity stops verifying by default with no error anywhere. That is a silent bypass of the exact mechanism this Set exists to make configurable | C:Low; U:Medium; S:Medium; F:High; Overall:Medium | FIXED | New F-14 records both measurements with the exact `ArgumentError` text and the `no_verify`-vanishes result. E-02 now mandates the BARE `--validate` form, forbids the alias list and `conflict_handler="resolve"` by name, and pastes the working measurement. V-02 now requires `no_verify` to appear in EVERY parsed-args row and rejects a missing attribute as a failed validation. The scope fence and the gate's second-silent-bypass paragraph both name it |
| PR-603 | HIGH | UNDER-SCOPE | E. testing; honest documentation | `tests/test_oc_runipd.py:5256-5273`; `tests/test_runner_profiles_e2e.py:58`, `:852-869`; `pyproject.toml:169`; `docs/runner-profiles.md:139`, `:187-189`; measured `2 passed` and `no tests ran in 2.80s` | **Three shipped assertions pin the claim this plan deliberately falsifies, one of them invisible to the mandated bare suite, and the plan named none of them.** `test_the_agy_runner_has_no_profile_integration_at_all` asserts `agy_runipd` contains `runner_profiles`/`launch_profile`/`verify_with` exactly ZERO times, with the message "the OC-only claim needs re-measuring"; it passes today and breaks in the BARE suite. `PublishedContractParityTests::test_the_doc_states_the_verify_with_limits_rather_than_implying_parity` asserts the same zero-count AND that the doc contains "does not read runner profiles"; both halves break, and that file is `pytest.mark.slow` against `addopts = ... -m 'not slow'`, so the bare run DESELECTS it and a bare-only paste shows green while it is red (measured: naming the path directly reports `no tests ran`). `docs/runner-profiles.md:187` states the claim as published fact, and `:139` still describes tier 4 as one shipped default "which is off", which child 01 already made per-host. This is the identical trap child 01's review recorded as its F-10, recurring in the very next child | C:Low; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | New F-16 enumerates all four with measured pass/deselect output. New E-09 owns them, requiring each test be REWRITTEN to pin the new true property (agy in the `validate` chain, oc-only for `verify_with`) rather than deleted, skipped, or weakened to a tautology. `tests/test_runner_profiles_e2e.py` and `docs/runner-profiles.md` added to Scope-Paths. The validation section and the gate's honesty rule now require `python3 -m pytest -m slow` with the reason stated; V-09 rejects a `no tests ran` paste as evidence |
| PR-604 | HIGH | UNDER-SCOPE | F. principles; G. executability | `runner_profile_wizard.py:76`; `cli.py:10153`; live `aw agy profile list` exit 2; package-wide grep for `set_validate_default` | **No shipped command can write the configuration this plan makes effective on antigravity, so the capability ships unreachable and the plan never says so.** Measured: the wizard hardcodes `RUNNER = "oc"` and `aw oc profile add` writes that constant, so every profile any shipped verb can create is an `oc` profile; `aw agy profile` does not exist (`invalid choice: 'profile'`, exit 2); and `runner_profiles.set_validate_default`, the only writer for `defaults.validate`, has NO caller in the package (grep finds the definition and its unit tests only). So after this plan the agy chain is exercisable ONLY by hand-editing `runner-profiles.json`, which is a legitimate way to ship but is a DECISION that must reach the operator, and the operator's own authority currently says the host "does not read runner profiles at all" | C:Low; U:Medium; S:Low; F:Medium; Overall:Medium | FIXED | New F-15 records the three measurements. New E-08 requires the documented-hand-edit path: the literal JSON for an agy profile INCLUDING its `defaults.profiles` entry, the literal `defaults.validate` JSON, an explicit statement that no `aw` command writes either, and a backlog item for the writer surface. Resolved to that option rather than left to the executor (see D-1). V-08 requires the documented JSON be EXECUTED against an isolated store and its frozen effect pasted, so a doc example that was never run is not accepted as evidence. New OQ-04 records the question and its resolution |
| PR-605 | MEDIUM | IN-SCOPE | A. correctness; C. architecture | `oc_runipd.py:2652-2701` (docstring and digest assertion); `docs/runner-profiles.md:182-184` | **The live resolution site has two `resolve()` calls and a cross-call invariant, and the plan, written against a single-call function, gives no rule for either.** `resolve_launch_pair` resolves the executor and, when `executor.verify_with` is set, the verifier profile, deliberately from ONE store read (its docstring: two loads "would leave a window in which the file is edited between them"), and it REFUSES if the two `config_digest` values differ. Two hazards follow that the authored plan could not have named. Threading `validate=` into the VERIFIER call would let a verifier profile's own `validate` re-decide the gate, which is a second switch for one behavior and contradicts the published contract that the two fields are independent ("IT SAYS WHICH, NOT WHETHER"). And an executor reaching for a fresh `runner_profiles.load()` to get the resolved value would reintroduce the very window `kgpptv` closed | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | New F-13 records the two-call shape, the one-read rationale, and the digest assertion. E-03 now forbids passing `validate=` to the verifier call with the reason and the citation, forbids a second `load()`, requires the digest assertion be left intact, and states that reading `resolved_launch.validate` at the existing freeze site is sufficient and adds no plumbing. Both prohibitions are also in the scope fence, and V-03 requires pasting the verifier call showing no `validate=` and the assertion still present |
| PR-606 | MEDIUM | IN-SCOPE | Evidence accuracy; C. architecture | live `oc_runipd.DriverError is agy_runipd.DriverError is runner_shared.DriverError` -> `True`; `runner_shared.py:170`; `agy_runipd.py:1343-1350` | **The plan prescribes rebuilding a translation wrapper for a problem that was already closed, on a premise that is now false.** Both E-01 and the Step 0 conventions state that "the two drivers define their OWN `DriverError` class (two distinct classes)" and instruct the helper to raise a neutral type so each driver can translate. Measured: there is now ONE class, defined at `runner_shared.py:170`, and both drivers bind that same object; `rununify` Order 02 (`818uru`) closed it, and agy's `enforce_dependency_preflight` wrapper explicitly records that its translation half "was DELETED" leaving only a `RuntimeError` guard. Acting on the stale premise would add a per-driver wrapper that translates a class into itself, which is dead code that future readers must reason about | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-01's typing paragraph rewritten to state the measured single-class fact, cite the closing plan and agy's own comment, name the two acceptable raise choices, and forbid adding a translation wrapper. The Step 0 convention bullet corrected from the false claim to the measured one. V-01 requires citing the live check |
| PR-607 | MEDIUM | UNDER-SCOPE | E. testing; A. correctness | live `getattr(args, "profile", ...)` absent on agy's parsed `start`; `oc_runipd.py:8081`; live `resolve(cfg, runner="agy")` with `defaults.profiles` -> `False`/`default-profile` | **E-06 and E-07 require pinning "a profile value" on agy, and there is no way to name a profile on that host, so the tests as specified cannot be written.** Measured: agy's `start` declares no `--profile`, and `extract_profile_clause` (the `as <profile>` grammar) is oc-only. The profile tier IS reachable on agy, but only through `defaults.profiles["agy"]`, and its provenance is then `default-profile`, not `profile`. An executor writing the agy rows the way the plan words them would either fail to construct the case or, worse, assert the wrong provenance string and adjust the test until it passed | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-04 now states that agy passes profile `None` and enumerates the tiers actually reachable there. E-07 gains a paragraph requiring the agy rows be built against `defaults.profiles["agy"]` and noting the provenance is `default-profile` on that host, with the live measurement cited. A Step 0 convention bullet records the parser fact so it is discoverable outside the E-item |
| PR-608 | MEDIUM | UNDER-SCOPE | A. correctness; G. executability | `agy_runipd.py:1788-1791` (refusal seam) vs `:1884-1885` (run dir); `oc_runipd.py:2742` vs `:2891-2895` | **E-04 says where to freeze but not where to RESOLVE, and agy has no existing profile resolution to mirror, so the durable-write guarantee oc enforces could be lost on the other host.** oc resolves as the first statement of `initialize_run` deliberately, so a malformed store refuses before any run id, directory, events, or state exist (`3cm15q` F-13). agy's `initialize_run` has no profile resolution at all; its pre-durable refusal seam is the three shared calls at `:1788-1791` and its run directory is created 96 lines later. A call placed after `run_dir.mkdir` would leave an orphan run directory on a bad store, which is precisely the failure `3cm15q` closed on oc | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | E-04 gains an explicit placement instruction naming both line ranges and the reason, framed as a DECISION rather than a copy of oc's. E-02's refusal is placed at the same seam for the same reason, raising `DriverError` so agy's `main` exits 2 cleanly rather than via `parser.error`. V-02 requires proof that no run directory appeared on the refusal path |
| PR-609 | MEDIUM | IN-SCOPE | E. testing; G. executability | `mn3gwr`/`tm2cz8` review F-11; authored baseline `5462 passed` at `f3e17ff6` | **The plan's stated pass criterion is an absolute number from a stale revision, and child 01's review already measured HEAD as not clean in both subsets.** Requiring the executor to match `5462 passed, 3 skipped, 2 xfailed` leaves two available wrong responses, both bad: fix an unrelated pre-existing failure inside a fenced plan, or paste a red suite and call it expected without evidence. This is the same defect child 01's review recorded as its PR-502, recurring because both plans were authored from the same stale baseline | C:Low; U:Low; S:Low; F:Medium; Overall:Low | FIXED | The absolute baseline was removed from the validation section and replaced with a MEASURE-YOUR-OWN-BEFORE rule judged on the delta (after minus before must be empty), naming both wrong responses and forbidding each, and noting child 01's measured pre-existing failures. The authoring history line's baseline is left as a historical record rather than a criterion |
| PR-610 | LOW | IN-SCOPE | Evidence accuracy | all citations re-resolved at `fac69fbd`; `git log -S` for `3798d236` | Every driver line citation in the plan had drifted, by 70 to 200 lines, in the two days between authoring and review: oc's freeze site `:2969` to `:3045`, its verifier gate `:6212` to `:6555`, `launch_profile_record` `:2657` to `:2703`, agy's freeze site `:1837` to `:2001`, its gate `:3392` to `:3553`, its semantic-difference note `:3474` to `:3636`, its `--no-verify` declaration `:4447` to `:4663`, `freeze_run_policy_flags` `:1673` to `:2058`, and the resume overwrite `:7882` to `:8315`. Every cited construct still EXISTS and every claim about it is TRUE, so this is citation rot and not a false claim, but a reader spot-checking any of them lands in unrelated code. Same class as child 01's PR-508 | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | All citations re-resolved against HEAD. New F-17 records the measured drift with the moving commit named, and the gate's RE-LOCATE BY SYMBOL paragraph now cites it so the rule reads as a measured consequence rather than generic advice |

No finding was DEFERRED, left OPEN, or marked REPLAN, so no escalation to a `- Blocking: yes` question was
required. The two BLOCKER findings are both cases where the plan's INSTRUCTIONS would have produced a
non-working or silently-unsafe result while the plan's own validation stayed green; neither reflects an
unsound approach, and both were repairable with bounded in-place edits, which is why the verdict is
APPROVE WITH REVISIONS APPLIED rather than REJECT.

The three pre-existing open questions (OQ-01 flag coexistence, OQ-02 resume overwrite, OQ-03 whether a
stored value may turn verification ON) were re-checked and remain correctly resolved and correctly
non-blocking. OQ-01's reasoning is now STRONGER than authored, because PR-602 shows that coexistence is
not merely preferable but that the alternative spelling actively breaks the host. OQ-04 was added at
review and resolved from repository evidence (see D-1).

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-1 | The agy capability is unreachable without hand-editing the store (PR-604). Should this plan add a writer surface (an `aw agy profile` verb, or widening the wizard), or ship the wiring with a documented manual step plus a backlog item? | Ship the wiring with a documented manual step, and require the documented JSON be EXECUTED as validation evidence | Adding an `aw agy profile add` verb; widening `runner_profile_wizard` to take a runner argument; calling `set_validate_default` from a new `aw oc profile` flag; leaving the choice to the executor as an either/or E-item | Child 01's scope fence states "Do NOT widen the wizard to a second host" and "Do NOT add a `run_dispatch` adapter" (`.aw/records/plans/executed/20260906-hostdefault-01-tm2cz8-...ipd.md`), so a writer here would contradict the immediately preceding child of this same Set; and `runner_profile_wizard.py:76` plus `cli.py:10153` show the writer path is single-host by construction, making it a design question rather than a line change | yes |
| D-2 | E-09 must change two shipped tests that currently assert agy reads no profile symbol. Rewrite them, or delete them as obsolete? | Rewrite both to pin the NEW true property (agy participates in the `validate` chain; `verify_with` model routing remains oc-only), keeping the `verify_with` half of each assertion unchanged | Deleting both as obsolete; marking them `xfail`; skipping them on the grounds that the claim they pin is intentionally being changed | Each test's own message states its purpose is re-measurement, not permanence ("the OC-only claim needs re-measuring", `tests/test_oc_runipd.py:5262-5272`), and the `verify_with` limit they also pin stays TRUE after this plan because E-05 deliberately gives agy no `launch_profile` record and `RUNNER_REGISTRY["agy"]` sets `supports_variant`/`supports_agent` False; deleting would discard a still-valid guarantee to avoid editing one assertion | yes |
| D-3 | oc's `--validate` currently defaults `False` on `start`, so the tri-state cannot be recovered. Change the default to `None` (breaking a shipped premise assertion), or inspect argv? | Left as the plan's authored either/or with (a) preferred, unchanged by review | Deciding (a) outright here; deciding (b) outright | The plan already states both options, names the exact shipped assertion that (a) breaks (`tests/test_novalnomerge_integration.py:47-79`), requires the choice be RECORDED, and requires the updated test still pin that a bare invocation verifies OFF; V-03 rejects a deleted, skipped, or xfailed paste. That is a sufficient contract, and the trade (honesty of the flag surface vs. an argv-inspection hack) is a genuine implementation judgement best made with the code open | yes |

Nothing was rejected. The plan's approach, its shared-library placement, and its negate-at-the-boundary
design are sound; what review supplied was a current map of the code it must edit.
