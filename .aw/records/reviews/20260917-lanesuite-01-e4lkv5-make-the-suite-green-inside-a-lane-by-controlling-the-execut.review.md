# Review findings: plan e4lkv5

- Subject-Id: e4lkv5
- Subject-Type: ipd
- Reviewed-At: 2026-09-18
- Reviewer: opencode/its_direct-pt3-claude-opus-5-1m-us
- Verdict: APPROVE WITH REVISIONS APPLIED
- Readiness: go-pending-approval

## Round 1

Reviewed at HEAD `95b08fca`. The plan on disk is byte-identical to the lane input (`diff -q` clean).
Structural preflight `aw ipd lint --phase author --agent` reported `clean`, 0 findings, exit 0.

THE DIAGNOSIS IS CORRECT AND EVERY NUMBER IN IT REPRODUCED EXACTLY, which is worth stating first
because it is rare and it changes what this review is for. I re-measured from scratch rather than
trusting the plan, and the review ran INSIDE the very condition the plan describes
(`AW_EXECUTION_ROLE=worker` was set in my own environment, so this was a live reproduction rather
than a simulation):

```text
python3 -m pytest                        -> 31 failed, 7962 passed, 3 skipped, 2 xfailed in 104.96s
env -u AW_EXECUTION_ROLE python3 -m pytest -> 7993 passed, 3 skipped, 2 xfailed in 108.16s
```

Per-file failure counts, matching the plan's F-2 list exactly (10/9/8/2/1/1 across six files):

```text
     10 tests/test_runner_backlog_close_in_lane.py
      9 tests/test_oc_runipd.py
      8 tests/test_agy_runipd_cli.py
      2 tests/test_ipd_lifecycle_cli.py
      1 tests/test_worker_role_refusal.py
      1 tests/test_novalnomerge_integration.py
```

F-3 reproduced verbatim, including the assertion message the plan quotes
(`AssertionError: 'worker' == 'worker'`). F-4 reproduced. F-5 reproduced. F-6's two commits both
exist and say what the plan says (`cdef9c90` "fix(i452hf): stop stranding a lane the agent already
finalized"; `2cfdb85d` "integrate(63425h): land the lane the driver refused as 'no begin receipt'").
The backlog provenance is accurate down to the priorities (`770fkp` high, `s0303g` medium, both
`graduated`, `s0303g`'s Set is indeed `lanesuite`, neither carries `Blocks-Release`). I found NO
false claim in the plan.

So the findings below are about GAPS, not errors. Three matter: an approved plan already forbids the
edit this plan's F-3 requires (PR-101, and it is the one that would have collided in execution); the
prescribed helper mechanism cannot work on the test classes it targets (PR-102, `monkeypatch` is not
injected into `unittest.TestCase`, proven); and the plan never considers the one-line alternative that
I measured to make all 7993 pass, which it needs to reject ON THE RECORD rather than by omission,
because a future maintainer will otherwise "simplify" this work into the vacuous version (PR-103).

### Findings

| ID | Severity | Scope | Area | Evidence | Finding | Remediation Risk | Decision | Resolution |
|----|----------|-------|------|----------|---------|------------------|----------|------------|
| PR-101 | BLOCKER | IN-SCOPE | C (architecture) | `.aw/records/plans/pending/20260908-roleadv-01-8b9ufm-...ipd.md:11`, `:44`, `:99`, `:130`; this plan `:70` (E-02), `:80` (E-04) | TWO LIVE PLANS GIVE OPPOSITE INSTRUCTIONS ABOUT THE SAME TEST. APPROVED plan `8b9ufm` declares `tests/test_worker_role_refusal.py` in its `Scope-Paths` and says of `test_driver_own_process_is_not_worker_role`: "it FAILS BY CONSTRUCTION inside a managed worker lane ... **Do not 'fix' it** and do not let new assertions depend on ambient env" (`:99`), recording it as its own F-18. This plan's E-02 names that same test as "the sharpest case" and E-04 would make it pass. `8b9ufm` is `Status: approved` (ready to execute) while this one is `to-review`, so the approved plan's instruction currently wins and an executor of THIS plan would either collide with it or be told by review-of-record to do the opposite of what an approved plan says. | C:Medium; U:Low; S:Low; F:Low; Overall:Medium | FIXED | Resolved by DIVISION OF LABOR rather than by overriding an approved plan (D-101). The plan now states that `8b9ufm` owns that file and that this plan does NOT edit it; E-02 keeps the test as the classification EXEMPLAR (it explains the defect class) but E-04's scope explicitly excludes it; `tests/test_worker_role_refusal.py` is REMOVED from `Scope-Paths`; and the residual one failure is declared as expected-red with the reason, so E-06 cannot claim a clean 7993 it will not get. |
| PR-102 | HIGH | IN-SCOPE | G (executability) | this plan `:75` (E-03); proven by probe; `tests/test_oc_runipd.py` 42 `unittest.TestCase`, `test_runner_backlog_close_in_lane.py` 8, `test_agy_runipd_cli.py` 14, `test_ipd_lifecycle_cli.py` 11, `test_novalnomerge_integration.py` 7 | E-03 PRESCRIBES A MECHANISM THAT CANNOT WORK ON THESE TESTS. It says "a context manager / fixture" so "31 call sites do not each hand-roll `monkeypatch.delenv`". But every failing test is a `unittest.TestCase` method, and pytest does NOT inject fixtures into those. Proven with a minimal probe: `def test_mp(self, monkeypatch=None)` in a `TestCase` gets `monkeypatch = None`, `1 failed`. `monkeypatch` appears 0 times in the three largest failing files, so there is no local precedent to copy either. An executor following E-03 literally would build a pytest fixture, find it never runs, and improvise. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-03 rewritten to name the mechanisms that DO work on a `unittest.TestCase` (a `setUp`-based context manager / `unittest.mock.patch.dict(os.environ, ...)` / an explicit `env=` argument), with the probe result recorded and the word "fixture" removed. It now also points at the SHIPPED precedent this repo already has for exactly this problem, which the plan did not cite: `tests/test_orchestrator_retirement.py:1294-1303` passes `env={}` explicitly and its comment gives this plan's own rationale ("the suite itself may run inside a managed lane ... a test that silently read os.environ would then refuse for the wrong reason and pass vacuously"), and `tests/test_worker_role_refusal.py:112-124` `_run_cli(role=...)` is already the E-03 helper shape. |
| PR-103 | HIGH | UNDER-SCOPE | D (anti-regression) | this plan `:186-198` (OQ-01); measured this session | THE CHEAPEST FIX IS NOT CONSIDERED, AND IT IS THE ONE A LATER MAINTAINER WILL REACH FOR. I measured a SINGLE process-level `os.environ.pop("AW_EXECUTION_ROLE", None)` before pytest collection: `7993 passed, 3 skipped, 2 xfailed`, i.e. all 31 fixed at one call site, xdist workers included (they inherit the parent env). OQ-01 rejects "the RUNNER unsetting the marker", which is a DIFFERENT proposal, so the plan's reasoning does not actually reach this one. It must be rejected on its own merits, and there IS a decisive reason: I also measured that the same one-liner makes `test_worker_role_refusal.py` pass 7/7, i.e. it SILENTLY MAKES THE GUARD'S OWN TEST VACUOUS, which is precisely the failure mode E-05's V-item exists to prevent. Unrecorded, this alternative is a standing invitation to undo the plan's whole point in one line. | C:Low; U:Low; S:Low; F:Medium; Overall:Medium | FIXED | Added OQ-03 (`Status: resolved`) recording the measurement, the rejection, and the reason (it buys green by making the guard's test assert nothing); and added an explicit prohibition to the gate so an executor cannot take the shortcut. This is the highest-value addition in the review: the alternative WORKS, which is exactly why it needed to be refuted with evidence rather than left unmentioned. |
| PR-104 | MEDIUM | IN-SCOPE | E (testing) | this plan `:97-100` (E-07); `agent_workflows/runner_shared.py:9467-9470`; `tests/test_worker_role_refusal.py:200-203` | E-07 ASKS FOR A GUARD BUT NOT THE ONLY KIND THAT WORKS, and this repo has already measured the trap. The obvious implementation is a source-text search (there is precedent at `test_worker_role_refusal.py:200-203`), and `runner_shared.py:9467-9470` records that the previous such guard "was measurably satisfiable by a COMMENT, and on oc a comment is what satisfied it", replaced by a BEHAVIORAL assertion. E-07 says "keep it narrow and honest about what it can detect" but does not require behavioral detection, so the likely outcome is a guard that a comment satisfies. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-07 rewritten to REQUIRE a behavioral guard (run the affected files in both ambient conditions and compare), citing the repo's own measured refutation of the source-search approach. Feasibility measured and recorded so the executor does not reject it as too slow: the six affected files run in 14s (`409 passed in 13.99s`), so a two-condition targeted guard costs ~28s, against ~210s for two full-suite runs. |
| PR-105 | MEDIUM | IN-SCOPE | G (executability) | this plan `:7` (`Scope-Paths`); `ls tests/support.py`; `find . -maxdepth 3 -name conftest.py` | `Scope-Paths` DECLARES A PATH THAT DOES NOT EXIST IN THE SHAPE CLAIMED. It lists `tests/support` (a directory); the repo has `tests/support.py` (a module, 4445 bytes) and NO `tests/support/` directory. There is also no `tests/conftest.py` (only a ROOT `conftest.py`), which matters because it is the other natural home for a shared helper and E-03 does not say which to use. A wrong path in `Scope-Paths` degrades the finalize scope reconciliation, which compares declared against actually-modified paths. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | `Scope-Paths` corrected to `tests/support.py`, and E-03 now states explicitly that the helper goes in that existing module (not a new package, not the root `conftest.py`, with the reason the root conftest is the wrong home: a process-level mutation there is PR-103's rejected shortcut). |
| PR-106 | MEDIUM | IN-SCOPE | A (correctness) | this plan `:68` (E-02), `:85` (E-05); measured across `tests/test_ipd_lint.py`, `tests/test_orchestrator_retirement.py`, `tests/test_turn_bounds.py`, `tests/test_worker_role_refusal.py` | E-05's TARGET SET IS PROBABLY EMPTY, WHICH MAKES THE ITEM UNFALSIFIABLE AS WRITTEN. E-05 applies the helper to "the class (b) refusal tests" among the 31. I searched for every test that asserts the refusal: they live in four files, and THREE of them (`test_ipd_lint.py`, `test_orchestrator_retirement.py`, `test_turn_bounds.py`) are NOT in the failing set and NOT in `Scope-Paths`, because they already pass the role explicitly. Measured green in the lane condition: 78, 137 and 40 passed. The fourth (`test_worker_role_refusal.py`) is 6/7 already ambient-immune via `_run_cli(role=...)`, and its 1 failure is the ambient-ASSERTING test that PR-101 hands to `8b9ufm`. So within the 31, class (b) is empty and E-05 may be satisfied vacuously by doing nothing. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | E-05 rewritten to state the measurement and to make the honest outcome explicit: its first obligation is to CONFIRM whether any of the 31 is a refusal test, with the measured expectation that none is, and to record "no class (b) member in the failing set" as a VALID result. It must then verify the three out-of-scope refusal-test files stay green in both conditions (that is the real anti-regression value), rather than editing them. V-05 rewritten to match, so it can no longer be satisfied by silence. |
| PR-107 | MEDIUM | IN-SCOPE | D (anti-regression) | this plan `:135` (F-8); `.aw/records/plans/pending/20260917-finidem-01-ld8lb3-...ipd.md:8`; `grep Item-Dependencies` across `pending/` | F-8 CLAIMS THIS PLAN GATES OTHER WORK, BUT NO DEPENDENCY EDGE EXISTS IN EITHER DIRECTION. F-8 says `ld8lb3` is blocked because it declares `tests/test_ipd_lifecycle_cli.py`, two of whose tests are in the failing set. The file claim is TRUE (verified at `ld8lb3:7`), and `ld8lb3` is `Status: reviewed`. But `ld8lb3` carries `Item-Dependencies: none`, and no pending plan declares an edge on `e4lkv5`. Since the runner sorts by dependency depth and re-checks edges at dispatch, an undeclared "gates" relationship is invisible to it: `ld8lb3` can be dispatched first and hit the same red baseline this plan exists to remove. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | F-8 rewritten to say precisely what is true (a SHARED-FILE and shared-baseline relationship, not a declared edge) and to state that making it machine-readable requires an edit to `ld8lb3` (adding `Item-Dependencies: ipd:e4lkv5`), which is NOT in this plan's scope. Recorded as a note for the approver rather than silently asserted, and added to the Deferred section so the ordering risk is visible instead of implied. |
| PR-108 | LOW | IN-SCOPE | E (testing) | this plan `:174-176`, `:255-261` (V-07) | V-07 BUNDLES TWO UNRELATED PROOFS, ONE OF WHICH THE EXECUTOR MAY NOT BE ABLE TO PERFORM. It requires both the E-07 guard demonstration AND "a REAL lane check: one plan run through `aw oc run`". The second is the right acceptance criterion and I agree with including it, but it is a separate act (it needs an approved plan to run, a driver invocation, and possibly a human), and pinning it to E-07's V-item means a perfectly good guard cannot be validated if no plan is available to run. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | Split. V-07 now validates the guard only; the real-lane check moved into V-06 beside the two-condition suite evidence (where the "is the lane actually green" question belongs), with an explicit fallback: if no plan is available to run through `aw oc run`, say so and report the two-condition evidence as the substitute rather than silently dropping it. |
| PR-109 | LOW | IN-SCOPE | G (executability) | this plan `:5` (Concern), `:128` (F-1); measured at `95b08fca` | THE BASELINE NUMBERS ARE ALREADY STALE, which matters only because E-01 is instructed to refuse on divergence. The plan cites `31 failed, 7866 passed` / `7869 passed` at `edb9ba85`. At `95b08fca` I measured `31 failed, 7962 passed` / `7993 passed`: the FAILURE count and the six-file split are unchanged (the load-bearing facts), but the pass totals moved by ~96/124 as the suite grew. An executor reading "any divergence is stated, not absorbed" could read a moved pass total as a reason to stop. | C:Low; U:Low; S:Low; F:Low; Overall:Low | FIXED | F-1 and E-01 now carry BOTH measurements with their HEADs and state which facts are load-bearing (the 31 and the six-file split) versus which drift benignly with suite growth (the pass totals), so E-01's refusal rule triggers on a real divergence and not on normal growth. |

### Decisions

| ID | Question | Chosen | Alternatives considered | Basis | Reversible |
|----|----------|--------|-------------------------|-------|------------|
| D-101 | Approved plan `8b9ufm` says do NOT fix `test_driver_own_process_is_not_worker_role`; this plan's E-02/E-04 would fix it. Which wins? | `8b9ufm` wins: this plan does NOT touch `tests/test_worker_role_refusal.py`, and declares the residual 1 failure as expected-red with the reason. | (a) Let this plan fix the test and note the overlap: rejected, it contradicts an APPROVED plan's explicit instruction and both declare the file, so the two would collide on the same lines. (b) Ask the maintainer to re-scope `8b9ufm`: rejected as disproportionate, since the division of labor needs no change to the approved plan at all. (c) Have this plan rewrite the test to assert behaviorally (about a constructed env rather than ambient): rejected HERE but noted as the right eventual fix, and it is `8b9ufm`'s F-18 to make. | `8b9ufm:11` declares the file; `:99` "Do not 'fix' it and do not let new assertions depend on ambient env"; `:130` records it as F-18; `8b9ufm` is `Status: approved` while this plan is `to-review`. Measured: that file is 6/7 ambient-immune already, so the residual is exactly one test. | yes |
| D-102 | Is the plan's premise (fix the tests) right, or should the runner stop exporting the marker for test runs? | Fix the tests; OQ-01's reasoning is sound and I add a measurement it lacked. | (a) Runner-side unset: rejected for OQ-01's two reasons, which I verified are real (the runner cannot distinguish a pytest child from any other, and the marker's arrival at the child is `pinned_child_env` copying `os.environ`, so an exception would have to be forgeable-heuristic based). (b) A single conftest-level pop: measured to work (7993 passed) and rejected separately as PR-103/OQ-03, because it also makes the guard's own test vacuous. | `runner_shared.py:9471` `env={**env_builder(), ...}` and `oc_runipd.py:605` `merged = os.environ.copy()` show the marker reaches children by env inheritance, so there is no test-only seam. Measured both alternatives this session. | yes |
| D-103 | E-03's helper mechanism: pytest fixture, or something a `unittest.TestCase` can use? | A `unittest`-compatible mechanism (`setUp` context manager / `mock.patch.dict` / explicit `env=`), reusing the two shipped precedents. | (a) pytest fixture as the plan says: rejected, PROVEN not to be injected into `unittest.TestCase` (probe: `monkeypatch = None`, 1 failed). (b) Convert the 85 test classes to plain pytest functions: rejected, enormously out of scope for a plan whose point is a green baseline. | Probe result above; `tests/test_orchestrator_retirement.py:1294-1303` (explicit `env={}` with this exact rationale) and `tests/test_worker_role_refusal.py:112-124` (`_run_cli(role=...)`) are the shipped precedents. | yes |
| D-104 | Where does the shared helper live, given `Scope-Paths` names a nonexistent `tests/support` directory? | `tests/support.py`, the existing module. | (a) A new `tests/support/` package: rejected, it would convert a working module into a package for one helper. (b) The ROOT `conftest.py`: rejected because a process-level mutation there IS the PR-103 shortcut, and putting the helper there invites exactly the vacuous outcome. (c) A new `tests/conftest.py`: rejected, none exists today and adding one for this is a second import surface. | `ls tests/support.py` (exists, 4445 bytes, "Shared helpers for the framework self-tests"); no `tests/support/` dir; `find` shows only the root `conftest.py`. | yes |
| D-105 | E-07's guard: source-text search or behavioral? | Behavioral (two ambient conditions over the affected files), and required rather than suggested. | (a) Source-text search: rejected on this repo's OWN measured evidence that such a guard was satisfied by a comment. (b) Full-suite twice: rejected as the default because it doubles the suite (~210s) for no extra detection over the targeted set. | `runner_shared.py:9467-9470` records the comment-satisfiable failure and the switch to behavioral assertion. Measured: the six affected files are `409 passed in 13.99s`, so two conditions cost ~28s. | yes |

### Reversibility note

Every decision is `Reversible: yes`: each changes only plan text, and the one that touches another
artifact's territory (D-101) does so by NARROWING this plan's scope, which is the conservative
direction. D-101 is nonetheless the decision most worth a maintainer's eye, because it resolves a
conflict between two live plans by deferring to the approved one; if the maintainer would rather
`8b9ufm` yield, that is a scope call only they can make, and I raise it in the final report.
