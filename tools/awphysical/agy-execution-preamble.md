<!-- Durable turn-1 execution preamble fed by tools/agy_run.py before the execution instruction. Purpose: establish a high diligence standard for Antigravity (Gemini 3.7 Flash High) executing an AW IPD. Version-controlled on purpose. -->
Before executing the plan below, adhere strictly to these non-negotiable engineering and execution standards:

1. Implement real behavior, not merely vocabulary. If the plan specifies a class, enum, flag, or policy, it must be fully wired into and enforced by the active code path (resolver, CLI, producer, or runtime engine). Defining symbols that nothing consumes or tests does not satisfy an execution item.
2. Every test written or modified MUST be falsifiable and MUST assert the actual requirement, including its negative/failure conditions. For each new or changed test, prove it can fail: verify it goes RED when the implementation is broken or missing, confirm it goes GREEN when restored, and paste that actual red-then-green runner output.
3. Do not write shallow assertions. Do not merely assert that a planted violation exists; assert that it is DETECTED and REJECTED as specified (e.g. raised exception, nonzero exit code, error output). Assert actual runtime behavior, never that an enum equals itself or a fixture equals hardcoded text.
4. Run the full test suite using EXACTLY the repository's canonical command `make test` (this is `pytest -n auto`, ~40s). Do NOT run `python3 -m unittest discover` or any serial full-suite variant for the full run: it is many times slower and is not the canonical command. Use `make test` for the full suite; only if `make test` itself errors out should you report that error and stop. Targeted single-module runs during development are fine, but the full-suite report MUST come from `make test`. Regressions in other areas caused by your changes are your responsibility to investigate and resolve. Report the full-suite result with actual `make test` output.
5. Respect scope boundaries. Stay strictly inside this plan's stated scope; do not preemptively implement later orders or refactor unrelated files.
6. Paste actual command outputs. Never fabricate, reconstruct, or approximate test outputs.
7. Follow the repository execution contract: commit path-scoped (`git commit -m msg -- <paths>`), never use `git add -A` or `-a`, and never push. Do NOT transition the plan to `executed/` or declare the lifecycle complete—the orchestrator retains authority over terminal transitions and independently verifies all evidence.
8. If any requirement is genuinely blocked or ambiguous, stop and state the blocker explicitly instead of papering over it with a hollow test or unverified assumption.
