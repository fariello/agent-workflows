<!-- Durable turn-1 execution preamble fed by tools/antigravity_execute_ipd.py before the "read and execute <plan>" instruction. Purpose: raise the diligence bar for Antigravity/Gemini executing an AW IPD, based on repeated observed failures. Edit this file to tune the framing; it is version-controlled on purpose. -->
Before you read and execute the plan below, understand the standard you are being held to on THIS work.

We have executed IPDs with you before, and your normal effort has repeatedly been unacceptable here. On prior orders you: shipped tests that PASS but TEST NOTHING (e.g. asserting a file exists instead of asserting the forbidden thing is rejected - a test that can never fail); wrote enum/vocabulary code and wired it into nothing while claiming the feature was implemented; wrote "fixtures" that just contain the answer the test then asserts; ran only your own handful of tests and missed regressions the full suite would have caught; moved the plan to executed/ and committed on your own authority when that was not yours to do; and then signed a self-audit swearing everything conformed when it did not. Every one of those forced a full redo. We are not doing that again.

So do this order at a MUCH higher level of diligence than you normally would. Specifically, non-negotiably:

1. Actually implement the behavior, not just the vocabulary. If the plan says a class/enum/policy exists, it must be WIRED INTO and ENFORCED by the real code path (the resolver/CLI/producer the plan names), not merely defined. Defining a symbol nothing uses does not satisfy an execution item.
2. Every test you write or touch MUST be falsifiable and MUST test the actual requirement, including its stated failure condition. For each such test, PROVE it can fail: temporarily break the implementation (or the fixture), run the test, confirm it goes RED, restore, confirm GREEN, and PASTE that red-then-green output. A test you cannot make fail is worthless and will be rejected.
3. Do NOT write a test that asserts a planted violation merely "exists." Assert that it is DETECTED/REJECTED exactly as the plan's required-failure-condition states (e.g. via `git ls-files --cached`, a raised error, a nonzero exit), and assert real behavior, not that an enum equals itself or that a fixture you wrote is true.
4. Run the FULL test suite (`python3 -m unittest discover -s tests -t .`), not just the tests you added. If your change alters observable behavior that another test asserts, that other test failing is YOUR responsibility to understand and resolve (fix your work, or update a test only when the behavior change is intended and in-scope). Report the full-suite result with actual output.
5. Stay inside THIS order's scope. Read the plan's Scope line and honor it; do not do a later order's work.
6. Paste ACTUAL command output for everything you claim. Never reconstruct, summarize, or imagine output. "Tests pass" without the real runner output is not acceptable.
7. Commit your work PATH-SCOPED (`git commit -- <paths>`); never `git add -A`/`-a`; never push. Do NOT move the plan to executed/ and do NOT declare the order complete or its lifecycle transitioned - the orchestrator owns the terminal transition and will INDEPENDENTLY re-verify your work (reading your test bodies, mutation-probing whether your tests can fail, running the full suite, and confirming the real code path actually changed). If your work is hollow, it will be rejected again.
8. If any requirement is genuinely blocked, ambiguous, or out of scope, STOP and say so explicitly. Do not paper over it with a passing-but-empty test or a silent assumption.

Assume the orchestrator will catch shortcuts, because it will. Do the work properly the first time. Now:
