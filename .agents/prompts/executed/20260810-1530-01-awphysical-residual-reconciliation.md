<!-- aw-prompt: Kind: run-once | Status: executed | Results: GPT-5.6 residual reconciliation in commit cc2d184 (spec + 13 plans + tools/scenarios + code/tests); independently re-reviewed 2026-08-10 (commit 77d41f6). Remaining residuals carried forward into 20260810-1544-01 | Created: 2026-08-10 | Author: opencode (Opus 4.8, its_direct/pt3-claude-opus-4.8-1m-us) | Targets: GPT-5.6 High (Codex), author of the awphysical Set + physical-layout spec | Concerns: .agents/docs/specs/20260810-1447-01-physical-aw-hierarchy-placement-and-migration.spec.md, .agents/plans/pending/20260810-awphysical-00..12, tools/awphysical/migration-scenarios.json, agent_workflows/project_schema.py | Results-go-to: revised spec (kept to-review) + revised plans + scenarios . This HTML comment is pipeline metadata only; it is invisible when pasted into a chat and is not part of the prompt. -->
You are GPT-5.6 High (Codex), author of the `agent-workflows` physical-layout spec `20260810-1447-01` and the `awphysical` IPD Set (Orders 00-12). I am Opus 4.8 (opencode). I ran an independent re-review (the multi-lane `plan-review-long` workflow: one read-only evidence lane per plan plus a dedicated spec lane, every claim checked against repository source at path:line). Result: you resolved essentially every finding from the prior handoff. Verified from evidence: the physical spec exists and supersedes `20260809-2211-01` (`aw specs check` passes; D130 recorded); `--phase executor` is gone Set-wide; the orchestrator E-item dependencies now mirror the child DAG (all 14 rows); the config.json -> project.json/local.json split is defined; `tools/awphysical/` is tracked; every plan carries an honest blocking open question; all 13 lint conforming at author and review-finalize; nothing claims execution. The package is materially sound and correctly NO-GO pending the maintainer's spec approval.

The maintainer asked me to hand the RESIDUAL findings back to you (rather than have me edit your 14 artifacts piecemeal) so you can fix them coherently across the spec, plans, and `migration-scenarios.json`. All residuals are LOW/MEDIUM remediation-risk; a few are HIGH-severity but low-risk-to-fix. Keep the spec at `Status: to-review` and preserve the human-approval blocker (`OQ-01`) in every plan; do not execute any IPD, approve the spec, migrate any repository, push, tag, or release. Commit path-scoped only. When done, report and hand back for a final re-review, after which human spec approval is the sole remaining design gate.

# R1 (HIGH-severity, LOW-risk): the 25->42 crosswalk claims coverage that the scenario catalog does not encode

Spec Section 12 maps the old scenarios to the 42-row catalog, but two spec-mandated behaviors are mapped to AWP scenarios whose `expected` sets do not actually encode them, so Order 12's "scenario set equals evidence set" gate (V-06) can pass while these ship untested:

- Old #6 "incomplete/first-install fails closed" is mapped to AWP-041, but AWP-041 in `tools/awphysical/migration-scenarios.json` is `malformed-or-future-policy` with `expected: [fail-closed, no-policy-overwrite, source-specific-diagnostic]` - that is malformed/future POLICY content, not closed-stdin/EOF/missing-choices at first install. Spec Section 8 and Section 13 make first-install EOF-fail-closed a MUST/acceptance criterion; no catalog row encodes it.
- Old #22 (color/terminal modes) and #23 (screen-reader without color) are mapped to "an Order 03/12 acceptance case paired with AWP-001", but AWP-001 (`fresh-private-target`) has no color/`NO_COLOR`/`TERM=dumb`/screen-reader assertion; the strings color/screen do not appear in the catalog (only the substring inside `inaccessible-external-root`). Spec Section 8 mandates the accessibility surface.

Fix: add real `expected` tokens (or dedicated AWP rows, e.g. a first-install-EOF-fail-closed scenario and a color/screen-reader-accessibility scenario) so each old #6/#22/#23 behavior binds to a concrete executable assertion; and strengthen Section 12 so the machine-checkable crosswalk Order 12 builds asserts per-behavior coverage (a named assertion/test per retained old-scenario), not merely that a parent AWP id exists. The "Retained ... paired with AWP-XXX" disposition currently is not machine-verifiable at the behavior level for the 8 retained rows.

# R2 (MEDIUM): per-item V-evidence is weakly discriminating; mega-items collapse many failing conditions into one row

Across nearly all children, each E maps to a single `test_eNN` whose "Required positive assertion" is a verbatim copy of the E expected-outcome and whose "Required failure condition" is an identical generic string ("command is nonzero, fixture is absent, or the stated positive assertion is not observed"), and the V-* bodies are byte-identical boilerplate pointing at the matrix row. This is now command-bearing (good) but not discriminating: an executor can satisfy V by running one positive fixture without proving the specific property, and no NAMED negative/planted-violation fixture is required. It is worst where one row covers many independent conditions: Order 04 E-05 (4 capabilities) and E-07 (12 test modes + Windows fallback); Order 07 E-08 (~12 fault classes: stale inputs, concurrent writers, copy failure, verify mismatch, switch failure, process-kill-between-switch-and-completion, disk/permission loss, cross-Git partial staging, resume, rollback, cleanup refusal, source protection); Order 10 E-07 (~10 deceptive classes).

Fix: for the safety-critical rows, either split the E/V pair per condition (then `aw ipd sync` to re-id) or give the matrix row a NAMED negative/planted-violation fixture per condition and a per-condition assertion (e.g. Order 07: after a kill between the authoritative switch-write and journal completion, assert which single backend is authoritative + retained-source hash equality + git-index cleanliness + expected exit; Order 04: a positive test that NO write reaches canonical source under any mode, a spoof test, AND an ambiguous-evidence fail-closed test). Replace the copied-outcome positive assertions with independently observable predicates.

# R3 (MEDIUM): durability-state model drift between the spec and the shipped enum (Order 05)

Spec Section 10 lists observable durability states `unversioned, local-git, acknowledged-durable, repository-managed, unreachable, unknown` (6). The shipped `DurabilityState` enum in `agent_workflows/project_schema.py` has 5: `unversioned, local-git, durable-private, repository-managed, unknown` - it renames nothing (`durable-private` != `acknowledged-durable`) and has no `unreachable`, and `get_storage_status` in `agent_workflows/storage.py` never produces an unacknowledged-remote or unreachable state. Order 05 E-04 asserts these six states as an outcome but names no schema change. Fix: reconcile explicitly (rename/map `durable-private` -> `acknowledged-durable`, add `unreachable` and the unacknowledged-remote state), name the owning Order (05 consuming or 02 owning the schema), update `test_storage`/`test_acceptance_matrix`, and treat the shared-schema edit under the coordination gate in spec Section 14.

# R4 (HIGH-severity, MEDIUM-risk): the independent postcheck trusts self-reported context for exactly the checks meant to catch a lying migrator (Order 10)

Verified empirically: `tools/awphysical/aw_layout_compare.py` is genuinely independent (re-hashes the filesystem, deterministic `comparison_id`, catches missing/wrong/stale destinations) - keep it. But `tools/awphysical/aw_layout_postcheck.py` reads the migrator-authored context JSON for producer routing, authority, phase, and rollback (a fabricated-clean context passes), emits findings in code-append/input order (not sorted, no digest, so it is NOT byte-deterministic under reordered input), and has no rule for the "copied adapter logic" or "wrong Git index" deceptive classes that Order 10 E-07 claims are caught. Order 10 E-03 also promises companion+source Git checks, adapter-purity, attention, package-role, sanitizer gates, and a determinism digest that the tracked prototype does not implement.

Fix: make the postcheck genuinely independent - resolve where producers ACTUALLY write and read Git facts directly rather than trusting context; add a fabricated-clean-context fixture that MUST be caught; sort every emitted collection and emit a determinism digest (symmetric with compare's `comparison_id`); add the missing "copied adapter logic" and "wrong-git-index" rules (or delegate them explicitly to Orders 06-09 and drop Order 10's over-claim); and split/decompose E-03/E-07 so V cannot pass on partial coverage. Reconcile the prototype's repo-root `tools/awphysical/` home with the spec's `system`-root deterministic-helper placement.

# R5 (MEDIUM): Order 11 source-repo self-migration - packaging-move ownership + integrity predicate + intra-plan DAG

Order 11 migrates THIS repo, whose `.agents/workflows` is simultaneously the live tracked-mode system root AND the packaged distribution source (`pyproject.toml` force-include + sdist include, `agent_workflows/_compat.py` `_DATA_RELATIVE`, `hatch_build.py`, `versioning.py`). The plan gestures at "without breaking package or self-host resolution" but never names those touch-points or states whether Order 11 performs those edits or consumes them as already done by Order 04 E-02 (risking double-edit or an unverified assumption). Also: (a) the intra-plan E-DAG is flattened (E-02..E-07 all `Depends on: E-01`), so real mutation E-04 could run before a green rehearsal E-03 - encode the true sequential chain; (b) the E-06 integrity check says "compare count/tip" but does not state the hard-fail predicate (post commit-count >= pre for every artifact tree AND full history, tip reachability preserved, any missing/changed tracked path fails); (c) Order 11's entry gate should REQUIRE citing Order 04's actual passing source-checkout/spoofed-source test output before mutating the real repo, not just a terminal-status check; (d) name the external-root `--root` declaration step (spec Section 11.1). The maintainer confirmed Order 11 stays IN scope (dogfood the physical layout) but must be hard-gated on a real before/after baseline of the actual repo.

# Minor (LOW, fix where cheap)

- Add controlling-spec section anchors (e.g. Section 4.2, 11.3, 12) to the E-items that implement them, across the Set, for traceability.
- Order 12 E-08: name the specific stale CHANGELOG 2.0.0 lines to reconcile (the "Four logical roots" bullet and the superseded-spec citation) so the executor does not miss one.
- Order 06 E-02: the inventory tool does not emit an `unmerged` git-state and its `mixed:` members are not sorted, and `external` is omitted from the plan's "closed classification" list - reconcile the claimed classification set with what `aw_layout_inventory.py` actually produces (or extend the tool).
- Order 06/08: the evidence-matrix test methods target `tests/test_layout_migration.py`, a module owned by the SUPERSEDED awlayout Set - point new physical-layout tests at a new module (or the tracked tool test file) to avoid provenance/collision.
- Order 08: L08-05 residual - remove genuine writers (`specs.py`, `research_cmd.py`, `artifact_core.py`) from `record_producers.LEGACY_ALLOWLIST` and add a static no-legacy-write-sink guard; L08-02 - add a code-discovery->manifest parity test (not just declared-set==test-set).

# Deliverable

Fix R1-R5 and the minors coherently across the spec (kept `to-review`), the 13 plans, `tools/awphysical/migration-scenarios.json`, and `agent_workflows/project_schema.py`/`storage.py` where a schema reconciliation is required. Keep every plan `aw ipd lint --phase author`/`--phase review-finalize` conforming, keep `aw specs check` clean, and preserve the human-approval blocker (OQ-01) in every plan. Do not execute, approve, migrate, push, tag, or release. Report what changed and hand back for a final independent re-review.
