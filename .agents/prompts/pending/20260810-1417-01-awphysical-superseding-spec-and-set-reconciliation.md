<!-- aw-prompt: Kind: run-once | Status: pending | Created: 2026-08-10 | Author: opencode (Opus 4.8, its_direct/pt3-claude-opus-4.8-1m-us) | Targets: GPT-5.6 High (Codex), the author of the awphysical Set | Concerns: .agents/plans/pending/20260810-awphysical-00..12 and a new superseding physical-layout spec | Results-go-to: a new spec under .agents/docs/specs/ + revised awphysical plans . This HTML comment is pipeline metadata only; it is invisible when pasted into a chat and is not part of the prompt. -->
You are GPT-5.6 High (Codex). You authored the `awphysical` IPD Set (Orders 00-12) in the `agent-workflows` repository. I am Opus 4.8 (running as opencode); I just completed a rigorous, parallel, evidence-based `/plan-review` (the multi-lane `plan-review-long` workflow, one read-only audit lane per plan) of all 13 plans, verifying every material claim against the actual repository source at `path:line`. The plans are structurally excellent (all 13 lint `conforming` at `--phase author`), the migration design (copy-verify-switch-retain, no deletion during cutover, independent postcheck gated on deterministic evidence) is sound, and the privacy/security posture is honest. But the review surfaced one foundational blocker and several Set-wide defects that block a GO verdict. The maintainer has asked me to hand these back to you for resolution, starting with the single most important task below. Work in the repository (the maintainer will give you the latest pushed state). Do not push, tag, or release; commit path-scoped only; and remember an agent may not set a spec to `approved` (that is the human maintainer's act).

# Task 1 (the blocker): author a NEW superseding physical-layout specification

The entire awphysical Set gates on orchestrator E-01 / V-01, which require an APPROVED "amended physical-layout specification" defining the concrete physical `.aw/` hierarchy. That spec does not exist. The only layout spec in the repo is `.agents/docs/specs/20260809-2211-01-aw-project-layout-storage-wizard-and-state.spec.md` (the `awlayout` Set), which is `Status: implemented`, `Canonical: true`, and explicitly declares the four-root tree LOGICAL: "This tree is logical. Depending on delivery and storage choices, some or all roots may resolve outside the target repository and no literal target `.aw/` directory may exist." (that spec, Section 4, ~line 73). Your awphysical Set instead makes those roots PHYSICAL (`.aw/system`, `.aw/config`, `.aw/state`, `.aw/records` as target-resident directories), splits `state` into `state/durable/` + `state/runtime/` and `config` into `config/project.json` (trackable) + `config/local.json` (never tracked), and adds a source-checkout role. That is a genuine design change, not an amendment gloss.

The maintainer has decided (in answer to my question): author a NEW dated physical-layout spec that SUPERSEDES `20260809-2211-01`, rather than editing the implemented spec in place. Please:

1. Author the new spec under `.agents/docs/specs/` using the `aw specs` tooling and the spec conventions (read `.agents/docs/specs/README.md` and the ipd/spec contract). It must contain, as the single normative source every awphysical child cites: the complete physical `.aw/{system,config,state,records}` tree; the durable-vs-runtime and portable-vs-local splits with their exact filenames; the placement/preset matrix (all-in-private-target, public-plus-private-companion, clean-target, local-only, custom); the Git-policy invariants (what is trackable vs never-tracked); the source-checkout exception; the migration retention rule (no deletion during cutover); and the compatibility/release boundary.
2. Record the supersession explicitly: mark `20260809-2211-01` superseded-by the new spec (via `aw specs`), preserve its history, and add a DECISIONS.md entry explaining the logical->physical change and why (dogfooding the physical layout that will be forced on every repo that installs or updates).
3. Reconcile the existing config filename: the shipped resolver reads durable config from `.aw/config/config.json` (see `agent_workflows/project_context.py`, ~line 222) and the awlayout spec places a per-project `project.json` at the AW_HOME project root. Define the exact legacy->new mapping (`config.json` -> `config/project.json` + `config/local.json`) so the compatibility parser has a named source.
4. Reconcile scenarios: the awlayout spec's Section 19 has 25 acceptance scenarios; your `tools/awphysical/migration-scenarios.json` has 42. Define which supersede/extend/retire which, so "every named scenario has executable evidence" is enforceable across both sets (Order 12 owns the crosswalk).
5. Leave approval to the human. Author it to `to-review`; the maintainer will drive `to-review -> reviewed -> approved`. No awphysical child may execute until this spec is `approved`.

# Task 2: resolve the Set-wide defects my review confirmed

I already applied ONE fix myself because it was unambiguous and its correctness is deterministic: every plan and the orchestrator cited `aw ipd lint --phase executor`, but `executor` is not a valid phase (valid: `author | review-finalize | pre-execution | pre-transition | post-transition`); run as written it errors with exit 2, which the lifecycle treats as a hard stop. I replaced all 14 occurrences with `--phase pre-transition` (the phase the plans' own gate prose already intended); all 13 still lint conforming. Please confirm this matches your intent and adjust if a different phase was meant for any Order.

The remaining Set-wide item I did NOT change, because it is a design decision I should not guess:

- `tools/awphysical/` (the inventory/compare/postcheck tools, the 42-row `migration-scenarios.json`, `test_awphysical_tools.py`, `README.md`, `migration-followup-review.md`) is entirely UNTRACKED in git. The orchestrator's Required-tests and Orders 06/07/10/12 depend on running these, and the Set's own closeout (E-14) requires replay "from a clean checkout" - which would not contain untracked files. Your own `tools/awphysical/README.md` calls them "an executable starting point ... not the production migration implementation." Decide and encode: are these committed now as tracked prototypes, or authored/promoted during a specific Order's execution (name which Order owns tracking them, and reconcile with the clean-checkout gate)? Whatever you choose, make it explicit in the owning plan and ensure the clean-checkout replay is satisfiable. If you commit them, run `aw sanitize --agent` first (the README contains example paths).

# Task 3: address the per-plan findings (summarized; full detail below)

Recurring across the Set (fix in each owning plan):

- V-item evidence is largely boilerplate ("A scoped diff, the exact focused commands named in this plan, and direct filesystem/Git/output assertions...") while the plans name almost no concrete focused commands. For a migration Set whose entire point is trustworthy, independently-reproduced evidence (the anti-green-washing safeguard is Order 10), each V-* must name the specific command/test/fixture and the specific failure it would reveal. Tighten V-* to concrete, falsifiable, per-item evidence (this is the repo's just-added `verify-execution` intent-audit + fidelity discipline; see `.agents/workflows/verify-execution/`).
- "No open questions" is asserted in most plans while the foundational spec was unwritten/unapproved. Replace with honest structured open questions (Blocking: yes) where a real dependency is open.
- The orchestrator's execution-item `Depends on:` fields all flatten to E-01, contradicting the richer child dependency DAG in the orchestrator's own table (e.g. Order 07, the first mutation-capable order, actually depends on 02/04/05/06). Make the orchestrator E-item dependencies mirror the true DAG.

Order-specific highlights:

- Order 01: introduces `config/project.json` + `config/local.json` without naming/mapping the shipped `.aw/config/config.json`; the "append architecture decision, do not rewrite D126-D129" instruction points at ARCHITECTURE.md but D126-D129 live in DECISIONS.md. V-01 (the spec amendment, the highest-impact item) has the weakest, command-free evidence.
- Order 02: names a test module `tests/test_project_schema` that does not exist (it is `tests/test_project_layout.py`); introduces an "absent/unconfigured" provenance state without reconciling it with the existing 6-value `PrecedenceLevel` enum (`agent_workflows/project_schema.py`); path-safety scope overlaps existing primitives in `project_context.py` (`_canonical_path`, `_is_safe_subpath`, `_check_path_security`) and should extend/harden them (there is a latent symlink-escape gap in `_check_path_security` worth closing) rather than build a parallel layer.
- Order 03: cites a corrective plan `791mt4` that does not exist in the repo; "Replace the current coarse ProjectPolicy" understates the blast radius (it is consumed by install_wizard/project_layout/cli); first-install interactive EOF currently defaults to a policy rather than failing closed (`agent_workflows/install_wizard.py`); the exact-path preview must be required to use home-relative display and never be persisted to a public/tracked artifact.
- Order 04: the source-overwrite protection (the plan's headline safety property) has under-specified verification - add a positive test that a write actually reaches canonical source under no mode, a spoof test, and an ambiguous-detection fail-closed test; reconcile with the existing `is_self` guard in `engine.py` (~line 1930) so there are not two divergent self-detection paths; the packaging touch-points that must change in lockstep if the source tree moves (`pyproject.toml` force-include, `agent_workflows/_compat.py` `_DATA_RELATIVE`, `versioning.py` VERSION path, `hatch_build.py`) are not enumerated.
- Order 05: real privacy risk - add a concrete leak-negative test (target `git status --porcelain` + `git ls-files` + merge-base diff show zero records/companion paths; no machine path in any tracked target file); material scope overlap with Order 02 on companion identity + path validators (delineate ownership); E-05 references a `doctor` flow that the CLI intentionally removed (`agent_workflows/cli.py` top comment says there is intentionally no `doctor`) - use `aw storage status`/preflight WARN or record a decision to add `aw doctor privacy`.
- Order 06: the inventory tool does NOT record `timestamps` or Git `conflict/unmerged` status though E-02 claims both; "external/cross-Git-boundary" coverage depends on the operator remembering `--root` flags with no resolver-driven completeness check; the `not-listed` and `mixed:*` git-states are real edge buckets the plan never names; "implement vs use/promote" the prototype is ambiguous vs the README's "not production" stance. (Positive: the tool is genuinely read-only, does not follow symlinks, and sanitizes paths - verified.)
- Order 07 (first mutation-capable): the SHIPPED `agent_workflows/layout_migration.py` `execute_migration` writes the journal and flips the backend but never actually COPIES the enumerated record bytes, and its journal is write-only with no read/rollback/resume path; a real compensating ledger exists elsewhere (`agent_workflows/project_layout.py` `TransactionJournal.compensate`) plus `engine.run_rollback` - build on those rather than a third mechanism; the data-loss/rollback/resume V-items need concrete per-fault, per-hash, per-exit-code evidence (kill between switch-write and completion; assert single authoritative backend; retained-source hash equality).
- Order 08: the inherited `record_producers.py` PRODUCER_INVENTORY points at anchors that do not exist (`plans.py:scaffold_plan`, `specs.py:create_spec`, `research_cmd.py:create_research`) while the real writers are `ipd_authoring.run_scaffold`, `research_cmd.plan_new*`, `specs._set_status`, plus `plans_index/archive/refs`, `research_index/archive/refs`, `attention*`, and the `engine.py` scaffolders - re-derive the inventory FROM code and add a parity test that fails if any real writer is omitted; add a negative test that a producer writing a legacy path RAISES before file creation; the `LEGACY_ALLOWLIST` currently lists active writers as "readers" and no test enforces read-only.
- Order 09: the shipped `agent_workflows/clean_delta.py` "zero-target-write" proof is fabricated (hardcoded `target_writes: 0`, a no-op check, fake D113 evidence pairs) and dead (imported by nothing but its test) - name it for replacement, not extension; the zero-delta proof must use the merge-base diff (per `clean-delta-and-tracking-modes.spec.md`), not just `git status` (a clean working tree is explicitly NOT sufficient proof); define the adapter-purity boundary against the generator's real embedded `assess`/`advise` prose in `engine.py` (~lines 449-490).
- Order 10 (anti-green-washing): the prototype postcheck/compare DO deterministically catch a lying migrator (verified: deceptive fixture -> exit 2, valid=false, stable rule IDs) - strong. But the 10 deceptive-defect classes in E-07 are collapsed into one E/V pair with no per-class fixture->rule mapping (adapter-purity and wrong-Git-index have no prototype coverage); determinism is incidental to code order, not enforced by a sort/test; evidence storage must run `aw sanitize` before commit (reports embed absolute paths).
- Order 11 (self-migrate this repo): the maintainer confirmed this repo SHOULD dogfood the physical layout (that is the point). Gate it hard: the plan currently verifies hashes against the REHEARSAL copy, not a captured before/after baseline of the REAL repo - add an explicit pre-mutation baseline capture (inventory + content digests + `git rev-list --count`/tip hashes of every artifact tree AND full history) and a before-vs-after comparison on the real repo that hard-fails on any missing/changed tracked path or reduced commit count; require a full `git clone --mirror` (not `cp`) rehearsal; make Order 04's source-checkout protection VERIFIED (cite its test output) before Order 11 mutates the source repo; enumerate how `.agents/workflows` (simultaneously the live system root AND the packaged distribution source) moves to `.aw/system` without breaking packaging or self-hosting.
- Order 12: the already-committed CHANGELOG 2.0.0 entry describes the LOGICAL layout - it must be reconciled/rewritten to the physical behavior (one behavior); the 25-vs-42 scenario crosswalk must be explicit; E-05 puts a compatibility-guard IMPLEMENTATION inside the doc/release Order (belongs with Orders 07/08 - avoid a second compatibility writer); reference RELEASING.md rung selection + bake-then-tag; respect the release boundary (no tag/push/publish without the explicit human GO in release-review Section 9) - which the plan does correctly today.

# Deliverable

Author the superseding physical-layout spec (Task 1) as the priority, then revise the awphysical plans (Tasks 2-3) to cite it and to carry concrete falsifiable V-evidence, an honest open-questions section, and a corrected dependency DAG. Keep each plan structurally conforming (`aw ipd lint --phase author` / `--phase review-finalize`). Do not execute any child, do not approve the spec, do not push/tag/release. When done, report what changed and hand back for a re-review + the maintainer's spec approval.
