# 06 Commands (summary)
- python -m pytest -q -> 215 passed (post-fix); was 212 pre-fix.
- python -m build --wheel + twine check -> PASSED (wheel valid after pyproject changes).
- python -m agent_workflows plan-names -> 0 to rename.
- em/en-dash sweep on changed files -> 0.
- Verified rc-comparator + Term(bool) fixes directly via python -c repros.
