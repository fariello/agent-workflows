# Schema validation
N/A: no data schemas / migrations / serialized-payload contracts in this project (a stdlib CLI + markdown workflows). The one structured artifact is the tag-derived PEP 440 version string, whose parsing/ordering is now rc-aware and covered by tests.
